import {
  ANONYMOUS_CONSENT_STATUS,
  ActivatedRoute,
  ActivatedRoutesService,
  AnonymousConsentsConfig,
  AnonymousConsentsService,
  AuthConfigService,
  AuthGuard,
  AuthService,
  BaseCoreModule,
  BaseSiteService,
  CONSECUTIVE_CHARACTERS,
  CURRENCY_CONTEXT_ID,
  CmsConfig,
  CmsService,
  Config,
  ConfigChunk,
  ConfigInitializerService,
  ConfigModule,
  ContainerBackgroundOptions,
  ContextServiceMap,
  CurrencyService,
  CustomerCouponService,
  CxDatePipe,
  CxEvent,
  DEFAULT_URL_MATCHER,
  DefaultConfigChunk,
  DeferLoadingStrategy,
  DynamicAttributeService,
  EMAIL_PATTERN,
  EventService,
  EventType,
  FeatureConfigService,
  FeatureDirective,
  FeatureModulesService,
  FeatureToggles,
  FeaturesConfigModule,
  GlobalMessageService,
  GlobalMessageType,
  I18nModule,
  LANGUAGE_CONTEXT_ID,
  LanguageService,
  LoggerService,
  MAX_CHARACTERS_PATTERN,
  MIN_EIGHT_CHARACTERS_PATTERN,
  MIN_ONE_DIGIT_PATTERN,
  MIN_ONE_SPECIAL_CHARACTER_PATTERN,
  MIN_ONE_UPPER_CASE_CHARACTER_PATTERN,
  MIN_SIX_CHARACTERS_PATTERN,
  MODULE_INITIALIZER,
  NavigationEnd,
  NavigationStart,
  NotificationType,
  OAuthFlow,
  OCC_USER_ID_ANONYMOUS,
  PageMetaModule,
  PageMetaService,
  PageType,
  Priority,
  ProductReferenceService,
  ProductReviewService,
  ProductScope,
  ProductSearchByCategoryService,
  ProductSearchByCodeService,
  ProductSearchService,
  ProductService,
  ProtectedRoutesGuard,
  ProtectedRoutesService,
  ROUTER_CONFIGURATION,
  ROUTER_NAVIGATED,
  RouteLoadStrategy,
  Router,
  RouterLink,
  RouterModule,
  RouterOutlet,
  RoutingConfigService,
  RoutingModule,
  RoutingService,
  SECURE_PASSWORD_PATTERN,
  STRONG_PASSWORD_PATTERN,
  ScriptLoader,
  Scroll,
  ScrollBehavior,
  SearchboxService,
  SemanticPathService,
  SiteAdapter,
  SiteContextConfig,
  SiteContextModule,
  SiteThemeModule,
  SiteThemeService,
  TranslatePipe,
  TranslationChunkService,
  TranslationService,
  UnifiedInjector,
  UrlMatcherService,
  UrlModule,
  UrlPipe,
  UserConsentService,
  UserIdService,
  UserInterestsService,
  UserNotificationPreferenceService,
  UserPaymentService,
  WindowRef,
  createFrom,
  deepMerge,
  getLastValueSync,
  isNotNullable,
  isNotUndefined,
  isObject,
  productGroup_actions,
  provideConfig,
  provideDefaultConfig,
  provideDefaultConfigFactory,
  resolveApplicable,
  useFeatureStyles,
  wrapIntoObservable
} from "./chunk-HZV3DCGS.js";
import {
  DomSanitizer,
  Meta,
  Title
} from "./chunk-TSY3UDII.js";
import {
  HttpUrlEncodingCodec
} from "./chunk-CK3GQE66.js";
import {
  AsyncPipe,
  CommonModule,
  DecimalPipe,
  LowerCasePipe,
  NgClass,
  NgForOf,
  NgIf,
  NgSwitch,
  NgSwitchCase,
  NgTemplateOutlet,
  SlicePipe,
  ViewportScroller,
  getDOM
} from "./chunk-H3D45PN7.js";
import {
  DOCUMENT,
  isPlatformBrowser,
  isPlatformServer
} from "./chunk-EI6X5VM7.js";
import {
  ofType
} from "./chunk-XJ7W2FYZ.js";
import {
  ActionsSubject,
  takeUntilDestroyed
} from "./chunk-MA6MY2PQ.js";
import {
  APP_BOOTSTRAP_LISTENER,
  APP_INITIALIZER,
  ApplicationRef,
  Attribute,
  BehaviorSubject,
  ChangeDetectionStrategy,
  ChangeDetectorRef,
  Component,
  ComponentFactory$1,
  ComponentFactoryResolver$1,
  ContentChild,
  ContentChildren,
  DestroyRef,
  Directive,
  EMPTY,
  ElementRef,
  EventEmitter,
  Host,
  HostAttributeToken,
  HostBinding,
  HostListener,
  Inject,
  Injectable,
  InjectionToken,
  Injector,
  Input,
  NEVER,
  NgModule,
  NgZone,
  Observable,
  Optional,
  Output,
  PLATFORM_ID,
  Pipe,
  Renderer2,
  RendererFactory2,
  ReplaySubject,
  RuntimeError,
  SecurityContext,
  Self,
  SkipSelf,
  Subject,
  Subscription,
  TemplateRef,
  Version,
  ViewChild,
  ViewChildren,
  ViewContainerRef,
  ViewEncapsulation,
  __decorate,
  __param,
  animationFrameScheduler,
  asapScheduler,
  auditTime,
  booleanAttribute,
  catchError,
  combineLatest,
  computed,
  concat,
  concatMap,
  debounceTime,
  defer,
  delayWhen,
  distinctUntilChanged,
  endWith,
  filter,
  finalize,
  first,
  forkJoin,
  formatRuntimeError,
  forwardRef,
  from,
  fromEvent,
  inject,
  input,
  interval,
  isDevMode,
  isPromise,
  isSubscribable,
  lastValueFrom,
  makeEnvironmentProviders,
  map,
  merge,
  mergeMap,
  numberAttribute,
  observeOn,
  of,
  pairwise,
  provideAppInitializer,
  scan,
  setClassMetadata,
  share,
  shareReplay,
  signal,
  skip,
  skipWhile,
  startWith,
  switchMap,
  take,
  takeUntil,
  tap,
  throttleTime,
  timer,
  untracked,
  using,
  withLatestFrom,
  zip,
  ɵɵInheritDefinitionFeature,
  ɵɵNgOnChangesFeature,
  ɵɵProvidersFeature,
  ɵɵadvance,
  ɵɵattribute,
  ɵɵclassMap,
  ɵɵclassMapInterpolate1,
  ɵɵclassProp,
  ɵɵcomponentInstance,
  ɵɵconditional,
  ɵɵcontentQuery,
  ɵɵdefineComponent,
  ɵɵdefineDirective,
  ɵɵdefineInjectable,
  ɵɵdefineInjector,
  ɵɵdefineNgModule,
  ɵɵdefinePipe,
  ɵɵdirectiveInject,
  ɵɵelement,
  ɵɵelementContainer,
  ɵɵelementContainerEnd,
  ɵɵelementContainerStart,
  ɵɵelementEnd,
  ɵɵelementStart,
  ɵɵgetCurrentView,
  ɵɵgetInheritedFactory,
  ɵɵhostProperty,
  ɵɵinject,
  ɵɵinjectAttribute,
  ɵɵlistener,
  ɵɵloadQuery,
  ɵɵnextContext,
  ɵɵpipe,
  ɵɵpipeBind1,
  ɵɵpipeBind2,
  ɵɵpipeBind3,
  ɵɵprojection,
  ɵɵprojectionDef,
  ɵɵproperty,
  ɵɵpropertyInterpolate,
  ɵɵpureFunction0,
  ɵɵpureFunction1,
  ɵɵpureFunction2,
  ɵɵpureFunction3,
  ɵɵpureFunction4,
  ɵɵqueryRefresh,
  ɵɵreference,
  ɵɵrepeater,
  ɵɵrepeaterCreate,
  ɵɵresetView,
  ɵɵresolveDocument,
  ɵɵresolveWindow,
  ɵɵrestoreView,
  ɵɵsanitizeHtml,
  ɵɵsanitizeStyle,
  ɵɵsanitizeUrl,
  ɵɵstyleMap,
  ɵɵstyleProp,
  ɵɵtemplate,
  ɵɵtemplateRefExtractor,
  ɵɵtext,
  ɵɵtextInterpolate,
  ɵɵtextInterpolate1,
  ɵɵtextInterpolate2,
  ɵɵtextInterpolate4,
  ɵɵviewQuery
} from "./chunk-QWELERTA.js";
import {
  __spreadProps,
  __spreadValues
} from "./chunk-K4KU7J5H.js";

// node_modules/@angular/service-worker/fesm2022/service-worker.mjs
var ERR_SW_NOT_SUPPORTED = "Service workers are disabled or not supported by this browser";
var NgswCommChannel = class {
  serviceWorker;
  worker;
  registration;
  events;
  constructor(serviceWorker, injector) {
    this.serviceWorker = serviceWorker;
    if (!serviceWorker) {
      this.worker = this.events = this.registration = new Observable((subscriber) => subscriber.error(new RuntimeError(5601, (typeof ngDevMode === "undefined" || ngDevMode) && ERR_SW_NOT_SUPPORTED)));
    } else {
      let currentWorker = null;
      const workerSubject = new Subject();
      this.worker = new Observable((subscriber) => {
        if (currentWorker !== null) {
          subscriber.next(currentWorker);
        }
        return workerSubject.subscribe((v) => subscriber.next(v));
      });
      const updateController = () => {
        const {
          controller
        } = serviceWorker;
        if (controller === null) {
          return;
        }
        currentWorker = controller;
        workerSubject.next(currentWorker);
      };
      serviceWorker.addEventListener("controllerchange", updateController);
      updateController();
      this.registration = this.worker.pipe(switchMap(() => serviceWorker.getRegistration()));
      const _events = new Subject();
      this.events = _events.asObservable();
      const messageListener = (event) => {
        const {
          data
        } = event;
        if (data?.type) {
          _events.next(data);
        }
      };
      serviceWorker.addEventListener("message", messageListener);
      const appRef = injector?.get(ApplicationRef, null, {
        optional: true
      });
      appRef?.onDestroy(() => {
        serviceWorker.removeEventListener("controllerchange", updateController);
        serviceWorker.removeEventListener("message", messageListener);
      });
    }
  }
  postMessage(action, payload) {
    return new Promise((resolve) => {
      this.worker.pipe(take(1)).subscribe((sw) => {
        sw.postMessage(__spreadValues({
          action
        }, payload));
        resolve();
      });
    });
  }
  postMessageWithOperation(type, payload, operationNonce) {
    const waitForOperationCompleted = this.waitForOperationCompleted(operationNonce);
    const postMessage = this.postMessage(type, payload);
    return Promise.all([postMessage, waitForOperationCompleted]).then(([, result]) => result);
  }
  generateNonce() {
    return Math.round(Math.random() * 1e7);
  }
  eventsOfType(type) {
    let filterFn;
    if (typeof type === "string") {
      filterFn = (event) => event.type === type;
    } else {
      filterFn = (event) => type.includes(event.type);
    }
    return this.events.pipe(filter(filterFn));
  }
  nextEventOfType(type) {
    return this.eventsOfType(type).pipe(take(1));
  }
  waitForOperationCompleted(nonce) {
    return new Promise((resolve, reject) => {
      this.eventsOfType("OPERATION_COMPLETED").pipe(filter((event) => event.nonce === nonce), take(1), map((event) => {
        if (event.result !== void 0) {
          return event.result;
        }
        throw new Error(event.error);
      })).subscribe({
        next: resolve,
        error: reject
      });
    });
  }
  get isEnabled() {
    return !!this.serviceWorker;
  }
};
var SwPush = class _SwPush {
  sw;
  /**
   * Emits the payloads of the received push notification messages.
   */
  messages;
  /**
   * Emits the payloads of the received push notification messages as well as the action the user
   * interacted with. If no action was used the `action` property contains an empty string `''`.
   *
   * Note that the `notification` property does **not** contain a
   * [Notification][Mozilla Notification] object but rather a
   * [NotificationOptions](https://notifications.spec.whatwg.org/#dictdef-notificationoptions)
   * object that also includes the `title` of the [Notification][Mozilla Notification] object.
   *
   * [Mozilla Notification]: https://developer.mozilla.org/en-US/docs/Web/API/Notification
   */
  notificationClicks;
  /**
   * Emits the currently active
   * [PushSubscription](https://developer.mozilla.org/en-US/docs/Web/API/PushSubscription)
   * associated to the Service Worker registration or `null` if there is no subscription.
   */
  subscription;
  /**
   * True if the Service Worker is enabled (supported by the browser and enabled via
   * `ServiceWorkerModule`).
   */
  get isEnabled() {
    return this.sw.isEnabled;
  }
  pushManager = null;
  subscriptionChanges = new Subject();
  constructor(sw) {
    this.sw = sw;
    if (!sw.isEnabled) {
      this.messages = NEVER;
      this.notificationClicks = NEVER;
      this.subscription = NEVER;
      return;
    }
    this.messages = this.sw.eventsOfType("PUSH").pipe(map((message) => message.data));
    this.notificationClicks = this.sw.eventsOfType("NOTIFICATION_CLICK").pipe(map((message) => message.data));
    this.pushManager = this.sw.registration.pipe(map((registration) => registration.pushManager));
    const workerDrivenSubscriptions = this.pushManager.pipe(switchMap((pm) => pm.getSubscription()));
    this.subscription = new Observable((subscriber) => {
      const workerDrivenSubscription = workerDrivenSubscriptions.subscribe(subscriber);
      const subscriptionChanges = this.subscriptionChanges.subscribe(subscriber);
      return () => {
        workerDrivenSubscription.unsubscribe();
        subscriptionChanges.unsubscribe();
      };
    });
  }
  /**
   * Subscribes to Web Push Notifications,
   * after requesting and receiving user permission.
   *
   * @param options An object containing the `serverPublicKey` string.
   * @returns A Promise that resolves to the new subscription object.
   */
  requestSubscription(options) {
    if (!this.sw.isEnabled || this.pushManager === null) {
      return Promise.reject(new Error(ERR_SW_NOT_SUPPORTED));
    }
    const pushOptions = {
      userVisibleOnly: true
    };
    let key = this.decodeBase64(options.serverPublicKey.replace(/_/g, "/").replace(/-/g, "+"));
    let applicationServerKey = new Uint8Array(new ArrayBuffer(key.length));
    for (let i = 0; i < key.length; i++) {
      applicationServerKey[i] = key.charCodeAt(i);
    }
    pushOptions.applicationServerKey = applicationServerKey;
    return new Promise((resolve, reject) => {
      this.pushManager.pipe(switchMap((pm) => pm.subscribe(pushOptions)), take(1)).subscribe({
        next: (sub) => {
          this.subscriptionChanges.next(sub);
          resolve(sub);
        },
        error: reject
      });
    });
  }
  /**
   * Unsubscribes from Service Worker push notifications.
   *
   * @returns A Promise that is resolved when the operation succeeds, or is rejected if there is no
   *          active subscription or the unsubscribe operation fails.
   */
  unsubscribe() {
    if (!this.sw.isEnabled) {
      return Promise.reject(new Error(ERR_SW_NOT_SUPPORTED));
    }
    const doUnsubscribe = (sub) => {
      if (sub === null) {
        throw new RuntimeError(5602, (typeof ngDevMode === "undefined" || ngDevMode) && "Not subscribed to push notifications.");
      }
      return sub.unsubscribe().then((success) => {
        if (!success) {
          throw new RuntimeError(5603, (typeof ngDevMode === "undefined" || ngDevMode) && "Unsubscribe failed!");
        }
        this.subscriptionChanges.next(null);
      });
    };
    return new Promise((resolve, reject) => {
      this.subscription.pipe(take(1), switchMap(doUnsubscribe)).subscribe({
        next: resolve,
        error: reject
      });
    });
  }
  decodeBase64(input2) {
    return atob(input2);
  }
  static ɵfac = function SwPush_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _SwPush)(ɵɵinject(NgswCommChannel));
  };
  static ɵprov = ɵɵdefineInjectable({
    token: _SwPush,
    factory: _SwPush.ɵfac
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(SwPush, [{
    type: Injectable
  }], () => [{
    type: NgswCommChannel
  }], null);
})();
var SwUpdate = class _SwUpdate {
  sw;
  /**
   * Emits a `VersionDetectedEvent` event whenever a new version is detected on the server.
   *
   * Emits a `VersionInstallationFailedEvent` event whenever checking for or downloading a new
   * version fails.
   *
   * Emits a `VersionReadyEvent` event whenever a new version has been downloaded and is ready for
   * activation.
   */
  versionUpdates;
  /**
   * Emits an `UnrecoverableStateEvent` event whenever the version of the app used by the service
   * worker to serve this client is in a broken state that cannot be recovered from without a full
   * page reload.
   */
  unrecoverable;
  /**
   * True if the Service Worker is enabled (supported by the browser and enabled via
   * `ServiceWorkerModule`).
   */
  get isEnabled() {
    return this.sw.isEnabled;
  }
  constructor(sw) {
    this.sw = sw;
    if (!sw.isEnabled) {
      this.versionUpdates = NEVER;
      this.unrecoverable = NEVER;
      return;
    }
    this.versionUpdates = this.sw.eventsOfType(["VERSION_DETECTED", "VERSION_INSTALLATION_FAILED", "VERSION_READY", "NO_NEW_VERSION_DETECTED"]);
    this.unrecoverable = this.sw.eventsOfType("UNRECOVERABLE_STATE");
  }
  /**
   * Checks for an update and waits until the new version is downloaded from the server and ready
   * for activation.
   *
   * @returns a promise that
   * - resolves to `true` if a new version was found and is ready to be activated.
   * - resolves to `false` if no new version was found
   * - rejects if any error occurs
   */
  checkForUpdate() {
    if (!this.sw.isEnabled) {
      return Promise.reject(new Error(ERR_SW_NOT_SUPPORTED));
    }
    const nonce = this.sw.generateNonce();
    return this.sw.postMessageWithOperation("CHECK_FOR_UPDATES", {
      nonce
    }, nonce);
  }
  /**
   * Updates the current client (i.e. browser tab) to the latest version that is ready for
   * activation.
   *
   * In most cases, you should not use this method and instead should update a client by reloading
   * the page.
   *
   * <div class="docs-alert docs-alert-important">
   *
   * Updating a client without reloading can easily result in a broken application due to a version
   * mismatch between the application shell and other page resources,
   * such as lazy-loaded chunks, whose filenames may change between
   * versions.
   *
   * Only use this method, if you are certain it is safe for your specific use case.
   *
   * </div>
   *
   * @returns a promise that
   *  - resolves to `true` if an update was activated successfully
   *  - resolves to `false` if no update was available (for example, the client was already on the
   *    latest version).
   *  - rejects if any error occurs
   */
  activateUpdate() {
    if (!this.sw.isEnabled) {
      return Promise.reject(new RuntimeError(5601, (typeof ngDevMode === "undefined" || ngDevMode) && ERR_SW_NOT_SUPPORTED));
    }
    const nonce = this.sw.generateNonce();
    return this.sw.postMessageWithOperation("ACTIVATE_UPDATE", {
      nonce
    }, nonce);
  }
  static ɵfac = function SwUpdate_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _SwUpdate)(ɵɵinject(NgswCommChannel));
  };
  static ɵprov = ɵɵdefineInjectable({
    token: _SwUpdate,
    factory: _SwUpdate.ɵfac
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(SwUpdate, [{
    type: Injectable
  }], () => [{
    type: NgswCommChannel
  }], null);
})();
var SCRIPT = new InjectionToken(ngDevMode ? "NGSW_REGISTER_SCRIPT" : "");
function ngswAppInitializer() {
  if (false) {
    return;
  }
  const options = inject(SwRegistrationOptions);
  if (!("serviceWorker" in navigator && options.enabled !== false)) {
    return;
  }
  const script = inject(SCRIPT);
  const ngZone = inject(NgZone);
  const appRef = inject(ApplicationRef);
  ngZone.runOutsideAngular(() => {
    const sw = navigator.serviceWorker;
    const onControllerChange = () => sw.controller?.postMessage({
      action: "INITIALIZE"
    });
    sw.addEventListener("controllerchange", onControllerChange);
    appRef.onDestroy(() => {
      sw.removeEventListener("controllerchange", onControllerChange);
    });
  });
  ngZone.runOutsideAngular(() => {
    let readyToRegister;
    const {
      registrationStrategy
    } = options;
    if (typeof registrationStrategy === "function") {
      readyToRegister = new Promise((resolve) => registrationStrategy().subscribe(() => resolve()));
    } else {
      const [strategy, ...args] = (registrationStrategy || "registerWhenStable:30000").split(":");
      switch (strategy) {
        case "registerImmediately":
          readyToRegister = Promise.resolve();
          break;
        case "registerWithDelay":
          readyToRegister = delayWithTimeout(+args[0] || 0);
          break;
        case "registerWhenStable":
          readyToRegister = Promise.race([appRef.whenStable(), delayWithTimeout(+args[0])]);
          break;
        default:
          throw new RuntimeError(5600, (typeof ngDevMode === "undefined" || ngDevMode) && `Unknown ServiceWorker registration strategy: ${options.registrationStrategy}`);
      }
    }
    readyToRegister.then(() => {
      if (appRef.destroyed) {
        return;
      }
      navigator.serviceWorker.register(script, {
        scope: options.scope
      }).catch((err) => console.error(formatRuntimeError(5604, (typeof ngDevMode === "undefined" || ngDevMode) && "Service worker registration failed with: " + err)));
    });
  });
}
function delayWithTimeout(timeout) {
  return new Promise((resolve) => setTimeout(resolve, timeout));
}
function ngswCommChannelFactory(opts, injector) {
  const isBrowser = true;
  return new NgswCommChannel(isBrowser && opts.enabled !== false ? navigator.serviceWorker : void 0, injector);
}
var SwRegistrationOptions = class {
  /**
   * Whether the ServiceWorker will be registered and the related services (such as `SwPush` and
   * `SwUpdate`) will attempt to communicate and interact with it.
   *
   * Default: true
   */
  enabled;
  /**
   * A URL that defines the ServiceWorker's registration scope; that is, what range of URLs it can
   * control. It will be used when calling
   * [ServiceWorkerContainer#register()](https://developer.mozilla.org/en-US/docs/Web/API/ServiceWorkerContainer/register).
   */
  scope;
  /**
   * Defines the ServiceWorker registration strategy, which determines when it will be registered
   * with the browser.
   *
   * The default behavior of registering once the application stabilizes (i.e. as soon as there are
   * no pending micro- and macro-tasks) is designed to register the ServiceWorker as soon as
   * possible but without affecting the application's first time load.
   *
   * Still, there might be cases where you want more control over when the ServiceWorker is
   * registered (for example, there might be a long-running timeout or polling interval, preventing
   * the app from stabilizing). The available option are:
   *
   * - `registerWhenStable:<timeout>`: Register as soon as the application stabilizes (no pending
   *     micro-/macro-tasks) but no later than `<timeout>` milliseconds. If the app hasn't
   *     stabilized after `<timeout>` milliseconds (for example, due to a recurrent asynchronous
   *     task), the ServiceWorker will be registered anyway.
   *     If `<timeout>` is omitted, the ServiceWorker will only be registered once the app
   *     stabilizes.
   * - `registerImmediately`: Register immediately.
   * - `registerWithDelay:<timeout>`: Register with a delay of `<timeout>` milliseconds. For
   *     example, use `registerWithDelay:5000` to register the ServiceWorker after 5 seconds. If
   *     `<timeout>` is omitted, is defaults to `0`, which will register the ServiceWorker as soon
   *     as possible but still asynchronously, once all pending micro-tasks are completed.
   * - An Observable factory function: A function that returns an `Observable`.
   *     The function will be used at runtime to obtain and subscribe to the `Observable` and the
   *     ServiceWorker will be registered as soon as the first value is emitted.
   *
   * Default: 'registerWhenStable:30000'
   */
  registrationStrategy;
};
function provideServiceWorker(script, options = {}) {
  return makeEnvironmentProviders([SwPush, SwUpdate, {
    provide: SCRIPT,
    useValue: script
  }, {
    provide: SwRegistrationOptions,
    useValue: options
  }, {
    provide: NgswCommChannel,
    useFactory: ngswCommChannelFactory,
    deps: [SwRegistrationOptions, Injector]
  }, provideAppInitializer(ngswAppInitializer)]);
}
var ServiceWorkerModule = class _ServiceWorkerModule {
  /**
   * Register the given Angular Service Worker script.
   *
   * If `enabled` is set to `false` in the given options, the module will behave as if service
   * workers are not supported by the browser, and the service worker will not be registered.
   */
  static register(script, options = {}) {
    return {
      ngModule: _ServiceWorkerModule,
      providers: [provideServiceWorker(script, options)]
    };
  }
  static ɵfac = function ServiceWorkerModule_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _ServiceWorkerModule)();
  };
  static ɵmod = ɵɵdefineNgModule({
    type: _ServiceWorkerModule
  });
  static ɵinj = ɵɵdefineInjector({
    providers: [SwPush, SwUpdate]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(ServiceWorkerModule, [{
    type: NgModule,
    args: [{
      providers: [SwPush, SwUpdate]
    }]
  }], null, null);
})();

// node_modules/@angular/forms/fesm2022/forms.mjs
var BaseControlValueAccessor = class _BaseControlValueAccessor {
  _renderer;
  _elementRef;
  /**
   * The registered callback function called when a change or input event occurs on the input
   * element.
   * @docs-private
   */
  onChange = (_) => {
  };
  /**
   * The registered callback function called when a blur event occurs on the input element.
   * @docs-private
   */
  onTouched = () => {
  };
  constructor(_renderer, _elementRef) {
    this._renderer = _renderer;
    this._elementRef = _elementRef;
  }
  /**
   * Helper method that sets a property on a target element using the current Renderer
   * implementation.
   * @docs-private
   */
  setProperty(key, value) {
    this._renderer.setProperty(this._elementRef.nativeElement, key, value);
  }
  /**
   * Registers a function called when the control is touched.
   * @docs-private
   */
  registerOnTouched(fn) {
    this.onTouched = fn;
  }
  /**
   * Registers a function called when the control value changes.
   * @docs-private
   */
  registerOnChange(fn) {
    this.onChange = fn;
  }
  /**
   * Sets the "disabled" property on the range input element.
   * @docs-private
   */
  setDisabledState(isDisabled) {
    this.setProperty("disabled", isDisabled);
  }
  static ɵfac = function BaseControlValueAccessor_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _BaseControlValueAccessor)(ɵɵdirectiveInject(Renderer2), ɵɵdirectiveInject(ElementRef));
  };
  static ɵdir = ɵɵdefineDirective({
    type: _BaseControlValueAccessor
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(BaseControlValueAccessor, [{
    type: Directive
  }], () => [{
    type: Renderer2
  }, {
    type: ElementRef
  }], null);
})();
var BuiltInControlValueAccessor = class _BuiltInControlValueAccessor extends BaseControlValueAccessor {
  static ɵfac = /* @__PURE__ */ (() => {
    let ɵBuiltInControlValueAccessor_BaseFactory;
    return function BuiltInControlValueAccessor_Factory(__ngFactoryType__) {
      return (ɵBuiltInControlValueAccessor_BaseFactory || (ɵBuiltInControlValueAccessor_BaseFactory = ɵɵgetInheritedFactory(_BuiltInControlValueAccessor)))(__ngFactoryType__ || _BuiltInControlValueAccessor);
    };
  })();
  static ɵdir = ɵɵdefineDirective({
    type: _BuiltInControlValueAccessor,
    features: [ɵɵInheritDefinitionFeature]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(BuiltInControlValueAccessor, [{
    type: Directive
  }], null, null);
})();
var NG_VALUE_ACCESSOR = new InjectionToken(ngDevMode ? "NgValueAccessor" : "");
var CHECKBOX_VALUE_ACCESSOR = {
  provide: NG_VALUE_ACCESSOR,
  useExisting: forwardRef(() => CheckboxControlValueAccessor),
  multi: true
};
var CheckboxControlValueAccessor = class _CheckboxControlValueAccessor extends BuiltInControlValueAccessor {
  /**
   * Sets the "checked" property on the input element.
   * @docs-private
   */
  writeValue(value) {
    this.setProperty("checked", value);
  }
  static ɵfac = /* @__PURE__ */ (() => {
    let ɵCheckboxControlValueAccessor_BaseFactory;
    return function CheckboxControlValueAccessor_Factory(__ngFactoryType__) {
      return (ɵCheckboxControlValueAccessor_BaseFactory || (ɵCheckboxControlValueAccessor_BaseFactory = ɵɵgetInheritedFactory(_CheckboxControlValueAccessor)))(__ngFactoryType__ || _CheckboxControlValueAccessor);
    };
  })();
  static ɵdir = ɵɵdefineDirective({
    type: _CheckboxControlValueAccessor,
    selectors: [["input", "type", "checkbox", "formControlName", ""], ["input", "type", "checkbox", "formControl", ""], ["input", "type", "checkbox", "ngModel", ""]],
    hostBindings: function CheckboxControlValueAccessor_HostBindings(rf, ctx) {
      if (rf & 1) {
        ɵɵlistener("change", function CheckboxControlValueAccessor_change_HostBindingHandler($event) {
          return ctx.onChange($event.target.checked);
        })("blur", function CheckboxControlValueAccessor_blur_HostBindingHandler() {
          return ctx.onTouched();
        });
      }
    },
    standalone: false,
    features: [ɵɵProvidersFeature([CHECKBOX_VALUE_ACCESSOR]), ɵɵInheritDefinitionFeature]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(CheckboxControlValueAccessor, [{
    type: Directive,
    args: [{
      selector: "input[type=checkbox][formControlName],input[type=checkbox][formControl],input[type=checkbox][ngModel]",
      host: {
        "(change)": "onChange($event.target.checked)",
        "(blur)": "onTouched()"
      },
      providers: [CHECKBOX_VALUE_ACCESSOR],
      standalone: false
    }]
  }], null, null);
})();
var DEFAULT_VALUE_ACCESSOR = {
  provide: NG_VALUE_ACCESSOR,
  useExisting: forwardRef(() => DefaultValueAccessor),
  multi: true
};
function _isAndroid() {
  const userAgent = getDOM() ? getDOM().getUserAgent() : "";
  return /android (\d+)/.test(userAgent.toLowerCase());
}
var COMPOSITION_BUFFER_MODE = new InjectionToken(ngDevMode ? "CompositionEventMode" : "");
var DefaultValueAccessor = class _DefaultValueAccessor extends BaseControlValueAccessor {
  _compositionMode;
  /** Whether the user is creating a composition string (IME events). */
  _composing = false;
  constructor(renderer, elementRef, _compositionMode) {
    super(renderer, elementRef);
    this._compositionMode = _compositionMode;
    if (this._compositionMode == null) {
      this._compositionMode = !_isAndroid();
    }
  }
  /**
   * Sets the "value" property on the input element.
   * @docs-private
   */
  writeValue(value) {
    const normalizedValue = value == null ? "" : value;
    this.setProperty("value", normalizedValue);
  }
  /** @internal */
  _handleInput(value) {
    if (!this._compositionMode || this._compositionMode && !this._composing) {
      this.onChange(value);
    }
  }
  /** @internal */
  _compositionStart() {
    this._composing = true;
  }
  /** @internal */
  _compositionEnd(value) {
    this._composing = false;
    this._compositionMode && this.onChange(value);
  }
  static ɵfac = function DefaultValueAccessor_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _DefaultValueAccessor)(ɵɵdirectiveInject(Renderer2), ɵɵdirectiveInject(ElementRef), ɵɵdirectiveInject(COMPOSITION_BUFFER_MODE, 8));
  };
  static ɵdir = ɵɵdefineDirective({
    type: _DefaultValueAccessor,
    selectors: [["input", "formControlName", "", 3, "type", "checkbox"], ["textarea", "formControlName", ""], ["input", "formControl", "", 3, "type", "checkbox"], ["textarea", "formControl", ""], ["input", "ngModel", "", 3, "type", "checkbox"], ["textarea", "ngModel", ""], ["", "ngDefaultControl", ""]],
    hostBindings: function DefaultValueAccessor_HostBindings(rf, ctx) {
      if (rf & 1) {
        ɵɵlistener("input", function DefaultValueAccessor_input_HostBindingHandler($event) {
          return ctx._handleInput($event.target.value);
        })("blur", function DefaultValueAccessor_blur_HostBindingHandler() {
          return ctx.onTouched();
        })("compositionstart", function DefaultValueAccessor_compositionstart_HostBindingHandler() {
          return ctx._compositionStart();
        })("compositionend", function DefaultValueAccessor_compositionend_HostBindingHandler($event) {
          return ctx._compositionEnd($event.target.value);
        });
      }
    },
    standalone: false,
    features: [ɵɵProvidersFeature([DEFAULT_VALUE_ACCESSOR]), ɵɵInheritDefinitionFeature]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(DefaultValueAccessor, [{
    type: Directive,
    args: [{
      selector: "input:not([type=checkbox])[formControlName],textarea[formControlName],input:not([type=checkbox])[formControl],textarea[formControl],input:not([type=checkbox])[ngModel],textarea[ngModel],[ngDefaultControl]",
      // TODO: vsavkin replace the above selector with the one below it once
      // https://github.com/angular/angular/issues/3011 is implemented
      // selector: '[ngModel],[formControl],[formControlName]',
      host: {
        "(input)": "$any(this)._handleInput($event.target.value)",
        "(blur)": "onTouched()",
        "(compositionstart)": "$any(this)._compositionStart()",
        "(compositionend)": "$any(this)._compositionEnd($event.target.value)"
      },
      providers: [DEFAULT_VALUE_ACCESSOR],
      standalone: false
    }]
  }], () => [{
    type: Renderer2
  }, {
    type: ElementRef
  }, {
    type: void 0,
    decorators: [{
      type: Optional
    }, {
      type: Inject,
      args: [COMPOSITION_BUFFER_MODE]
    }]
  }], null);
})();
function isEmptyInputValue(value) {
  return value == null || lengthOrSize(value) === 0;
}
function lengthOrSize(value) {
  if (value == null) {
    return null;
  } else if (Array.isArray(value) || typeof value === "string") {
    return value.length;
  } else if (value instanceof Set) {
    return value.size;
  }
  return null;
}
var NG_VALIDATORS = new InjectionToken(ngDevMode ? "NgValidators" : "");
var NG_ASYNC_VALIDATORS = new InjectionToken(ngDevMode ? "NgAsyncValidators" : "");
var EMAIL_REGEXP = /^(?=.{1,254}$)(?=.{1,64}@)[a-zA-Z0-9!#$%&'*+/=?^_`{|}~-]+(?:\.[a-zA-Z0-9!#$%&'*+/=?^_`{|}~-]+)*@[a-zA-Z0-9](?:[a-zA-Z0-9-]{0,61}[a-zA-Z0-9])?(?:\.[a-zA-Z0-9](?:[a-zA-Z0-9-]{0,61}[a-zA-Z0-9])?)*$/;
var Validators = class {
  /**
   * @description
   * Validator that requires the control's value to be greater than or equal to the provided number.
   *
   * @usageNotes
   *
   * ### Validate against a minimum of 3
   *
   * ```ts
   * const control = new FormControl(2, Validators.min(3));
   *
   * console.log(control.errors); // {min: {min: 3, actual: 2}}
   * ```
   *
   * @returns A validator function that returns an error map with the
   * `min` property if the validation check fails, otherwise `null`.
   *
   * @see {@link /api/forms/AbstractControl#updateValueAndValidity updateValueAndValidity}
   *
   */
  static min(min) {
    return minValidator(min);
  }
  /**
   * @description
   * Validator that requires the control's value to be less than or equal to the provided number.
   *
   * @usageNotes
   *
   * ### Validate against a maximum of 15
   *
   * ```ts
   * const control = new FormControl(16, Validators.max(15));
   *
   * console.log(control.errors); // {max: {max: 15, actual: 16}}
   * ```
   *
   * @returns A validator function that returns an error map with the
   * `max` property if the validation check fails, otherwise `null`.
   *
   * @see {@link /api/forms/AbstractControl#updateValueAndValidity updateValueAndValidity}
   *
   */
  static max(max) {
    return maxValidator(max);
  }
  /**
   * @description
   * Validator that requires the control have a non-empty value.
   *
   * @usageNotes
   *
   * ### Validate that the field is non-empty
   *
   * ```ts
   * const control = new FormControl('', Validators.required);
   *
   * console.log(control.errors); // {required: true}
   * ```
   *
   * @returns An error map with the `required` property
   * if the validation check fails, otherwise `null`.
   *
   * @see {@link /api/forms/AbstractControl#updateValueAndValidity updateValueAndValidity}
   *
   */
  static required(control) {
    return requiredValidator(control);
  }
  /**
   * @description
   * Validator that requires the control's value be true. This validator is commonly
   * used for required checkboxes.
   *
   * @usageNotes
   *
   * ### Validate that the field value is true
   *
   * ```ts
   * const control = new FormControl('some value', Validators.requiredTrue);
   *
   * console.log(control.errors); // {required: true}
   * ```
   *
   * @returns An error map that contains the `required` property
   * set to `true` if the validation check fails, otherwise `null`.
   *
   * @see {@link /api/forms/AbstractControl#updateValueAndValidity updateValueAndValidity}
   *
   */
  static requiredTrue(control) {
    return requiredTrueValidator(control);
  }
  /**
   * @description
   * Validator that requires the control's value pass an email validation test.
   *
   * Tests the value using a [regular
   * expression](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Guide/Regular_Expressions)
   * pattern suitable for common use cases. The pattern is based on the definition of a valid email
   * address in the [WHATWG HTML
   * specification](https://html.spec.whatwg.org/multipage/input.html#valid-e-mail-address) with
   * some enhancements to incorporate more RFC rules (such as rules related to domain names and the
   * lengths of different parts of the address).
   *
   * The differences from the WHATWG version include:
   * - Disallow `local-part` (the part before the `@` symbol) to begin or end with a period (`.`).
   * - Disallow `local-part` to be longer than 64 characters.
   * - Disallow the whole address to be longer than 254 characters.
   *
   * If this pattern does not satisfy your business needs, you can use `Validators.pattern()` to
   * validate the value against a different pattern.
   *
   * @usageNotes
   *
   * ### Validate that the field matches a valid email pattern
   *
   * ```ts
   * const control = new FormControl('bad@', Validators.email);
   *
   * console.log(control.errors); // {email: true}
   * ```
   *
   * @returns An error map with the `email` property
   * if the validation check fails, otherwise `null`.
   *
   * @see {@link /api/forms/AbstractControl#updateValueAndValidity updateValueAndValidity}
   *
   */
  static email(control) {
    return emailValidator(control);
  }
  /**
   * @description
   * Validator that requires the number of items in the control's value to be greater than or equal
   * to the provided minimum length. This validator is also provided by default if you use
   * the HTML5 `minlength` attribute. Note that the `minLength` validator is intended to be used
   * only for types that have a numeric `length` or `size` property, such as strings, arrays or
   * sets. The `minLength` validator logic is also not invoked for values when their `length` or
   * `size` property is 0 (for example in case of an empty string or an empty array), to support
   * optional controls. You can use the standard `required` validator if empty values should not be
   * considered valid.
   *
   * @usageNotes
   *
   * ### Validate that the field has a minimum of 3 characters
   *
   * ```ts
   * const control = new FormControl('ng', Validators.minLength(3));
   *
   * console.log(control.errors); // {minlength: {requiredLength: 3, actualLength: 2}}
   * ```
   *
   * ```html
   * <input minlength="5">
   * ```
   *
   * @returns A validator function that returns an error map with the
   * `minlength` property if the validation check fails, otherwise `null`.
   *
   * @see {@link /api/forms/AbstractControl#updateValueAndValidity updateValueAndValidity}
   *
   */
  static minLength(minLength) {
    return minLengthValidator(minLength);
  }
  /**
   * @description
   * Validator that requires the number of items in the control's value to be less than or equal
   * to the provided maximum length. This validator is also provided by default if you use
   * the HTML5 `maxlength` attribute. Note that the `maxLength` validator is intended to be used
   * only for types that have a numeric `length` or `size` property, such as strings, arrays or
   * sets.
   *
   * @usageNotes
   *
   * ### Validate that the field has maximum of 5 characters
   *
   * ```ts
   * const control = new FormControl('Angular', Validators.maxLength(5));
   *
   * console.log(control.errors); // {maxlength: {requiredLength: 5, actualLength: 7}}
   * ```
   *
   * ```html
   * <input maxlength="5">
   * ```
   *
   * @returns A validator function that returns an error map with the
   * `maxlength` property if the validation check fails, otherwise `null`.
   *
   * @see {@link /api/forms/AbstractControl#updateValueAndValidity updateValueAndValidity}
   *
   */
  static maxLength(maxLength) {
    return maxLengthValidator(maxLength);
  }
  /**
   * @description
   * Validator that requires the control's value to match a regex pattern. This validator is also
   * provided by default if you use the HTML5 `pattern` attribute.
   *
   * @usageNotes
   *
   * ### Validate that the field only contains letters or spaces
   *
   * ```ts
   * const control = new FormControl('1', Validators.pattern('[a-zA-Z ]*'));
   *
   * console.log(control.errors); // {pattern: {requiredPattern: '^[a-zA-Z ]*$', actualValue: '1'}}
   * ```
   *
   * ```html
   * <input pattern="[a-zA-Z ]*">
   * ```
   *
   * ### Pattern matching with the global or sticky flag
   *
   * `RegExp` objects created with the `g` or `y` flags that are passed into `Validators.pattern`
   * can produce different results on the same input when validations are run consecutively. This is
   * due to how the behavior of `RegExp.prototype.test` is
   * specified in [ECMA-262](https://tc39.es/ecma262/#sec-regexpbuiltinexec)
   * (`RegExp` preserves the index of the last match when the global or sticky flag is used).
   * Due to this behavior, it is recommended that when using
   * `Validators.pattern` you **do not** pass in a `RegExp` object with either the global or sticky
   * flag enabled.
   *
   * ```ts
   * // Not recommended (since the `g` flag is used)
   * const controlOne = new FormControl('1', Validators.pattern(/foo/g));
   *
   * // Good
   * const controlTwo = new FormControl('1', Validators.pattern(/foo/));
   * ```
   *
   * @param pattern A regular expression to be used as is to test the values, or a string.
   * If a string is passed, the `^` character is prepended and the `$` character is
   * appended to the provided string (if not already present), and the resulting regular
   * expression is used to test the values.
   *
   * @returns A validator function that returns an error map with the
   * `pattern` property if the validation check fails, otherwise `null`.
   *
   * @see {@link /api/forms/AbstractControl#updateValueAndValidity updateValueAndValidity}
   *
   */
  static pattern(pattern) {
    return patternValidator(pattern);
  }
  /**
   * @description
   * Validator that performs no operation.
   *
   * @see {@link /api/forms/AbstractControl#updateValueAndValidity updateValueAndValidity}
   *
   */
  static nullValidator(control) {
    return nullValidator();
  }
  static compose(validators) {
    return compose(validators);
  }
  /**
   * @description
   * Compose multiple async validators into a single function that returns the union
   * of the individual error objects for the provided control.
   *
   * @returns A validator function that returns an error map with the
   * merged error objects of the async validators if the validation check fails, otherwise `null`.
   *
   * @see {@link /api/forms/AbstractControl#updateValueAndValidity updateValueAndValidity}
   *
   */
  static composeAsync(validators) {
    return composeAsync(validators);
  }
};
function minValidator(min) {
  return (control) => {
    if (control.value == null || min == null) {
      return null;
    }
    const value = parseFloat(control.value);
    return !isNaN(value) && value < min ? {
      "min": {
        "min": min,
        "actual": control.value
      }
    } : null;
  };
}
function maxValidator(max) {
  return (control) => {
    if (control.value == null || max == null) {
      return null;
    }
    const value = parseFloat(control.value);
    return !isNaN(value) && value > max ? {
      "max": {
        "max": max,
        "actual": control.value
      }
    } : null;
  };
}
function requiredValidator(control) {
  return isEmptyInputValue(control.value) ? {
    "required": true
  } : null;
}
function requiredTrueValidator(control) {
  return control.value === true ? null : {
    "required": true
  };
}
function emailValidator(control) {
  if (isEmptyInputValue(control.value)) {
    return null;
  }
  return EMAIL_REGEXP.test(control.value) ? null : {
    "email": true
  };
}
function minLengthValidator(minLength) {
  return (control) => {
    const length = control.value?.length ?? lengthOrSize(control.value);
    if (length === null || length === 0) {
      return null;
    }
    return length < minLength ? {
      "minlength": {
        "requiredLength": minLength,
        "actualLength": length
      }
    } : null;
  };
}
function maxLengthValidator(maxLength) {
  return (control) => {
    const length = control.value?.length ?? lengthOrSize(control.value);
    if (length !== null && length > maxLength) {
      return {
        "maxlength": {
          "requiredLength": maxLength,
          "actualLength": length
        }
      };
    }
    return null;
  };
}
function patternValidator(pattern) {
  if (!pattern) return nullValidator;
  let regex;
  let regexStr;
  if (typeof pattern === "string") {
    regexStr = "";
    if (pattern.charAt(0) !== "^") regexStr += "^";
    regexStr += pattern;
    if (pattern.charAt(pattern.length - 1) !== "$") regexStr += "$";
    regex = new RegExp(regexStr);
  } else {
    regexStr = pattern.toString();
    regex = pattern;
  }
  return (control) => {
    if (isEmptyInputValue(control.value)) {
      return null;
    }
    const value = control.value;
    return regex.test(value) ? null : {
      "pattern": {
        "requiredPattern": regexStr,
        "actualValue": value
      }
    };
  };
}
function nullValidator(control) {
  return null;
}
function isPresent(o) {
  return o != null;
}
function toObservable(value) {
  const obs = isPromise(value) ? from(value) : value;
  if ((typeof ngDevMode === "undefined" || ngDevMode) && !isSubscribable(obs)) {
    let errorMessage = `Expected async validator to return Promise or Observable.`;
    if (typeof value === "object") {
      errorMessage += " Are you using a synchronous validator where an async validator is expected?";
    }
    throw new RuntimeError(-1101, errorMessage);
  }
  return obs;
}
function mergeErrors(arrayOfErrors) {
  let res = {};
  arrayOfErrors.forEach((errors) => {
    res = errors != null ? __spreadValues(__spreadValues({}, res), errors) : res;
  });
  return Object.keys(res).length === 0 ? null : res;
}
function executeValidators(control, validators) {
  return validators.map((validator) => validator(control));
}
function isValidatorFn(validator) {
  return !validator.validate;
}
function normalizeValidators(validators) {
  return validators.map((validator) => {
    return isValidatorFn(validator) ? validator : (c) => validator.validate(c);
  });
}
function compose(validators) {
  if (!validators) return null;
  const presentValidators = validators.filter(isPresent);
  if (presentValidators.length == 0) return null;
  return function(control) {
    return mergeErrors(executeValidators(control, presentValidators));
  };
}
function composeValidators(validators) {
  return validators != null ? compose(normalizeValidators(validators)) : null;
}
function composeAsync(validators) {
  if (!validators) return null;
  const presentValidators = validators.filter(isPresent);
  if (presentValidators.length == 0) return null;
  return function(control) {
    const observables = executeValidators(control, presentValidators).map(toObservable);
    return forkJoin(observables).pipe(map(mergeErrors));
  };
}
function composeAsyncValidators(validators) {
  return validators != null ? composeAsync(normalizeValidators(validators)) : null;
}
function mergeValidators(controlValidators, dirValidator) {
  if (controlValidators === null) return [dirValidator];
  return Array.isArray(controlValidators) ? [...controlValidators, dirValidator] : [controlValidators, dirValidator];
}
function getControlValidators(control) {
  return control._rawValidators;
}
function getControlAsyncValidators(control) {
  return control._rawAsyncValidators;
}
function makeValidatorsArray(validators) {
  if (!validators) return [];
  return Array.isArray(validators) ? validators : [validators];
}
function hasValidator(validators, validator) {
  return Array.isArray(validators) ? validators.includes(validator) : validators === validator;
}
function addValidators(validators, currentValidators) {
  const current = makeValidatorsArray(currentValidators);
  const validatorsToAdd = makeValidatorsArray(validators);
  validatorsToAdd.forEach((v) => {
    if (!hasValidator(current, v)) {
      current.push(v);
    }
  });
  return current;
}
function removeValidators(validators, currentValidators) {
  return makeValidatorsArray(currentValidators).filter((v) => !hasValidator(validators, v));
}
var AbstractControlDirective = class {
  /**
   * @description
   * Reports the value of the control if it is present, otherwise null.
   */
  get value() {
    return this.control ? this.control.value : null;
  }
  /**
   * @description
   * Reports whether the control is valid. A control is considered valid if no
   * validation errors exist with the current value.
   * If the control is not present, null is returned.
   */
  get valid() {
    return this.control ? this.control.valid : null;
  }
  /**
   * @description
   * Reports whether the control is invalid, meaning that an error exists in the input value.
   * If the control is not present, null is returned.
   */
  get invalid() {
    return this.control ? this.control.invalid : null;
  }
  /**
   * @description
   * Reports whether a control is pending, meaning that async validation is occurring and
   * errors are not yet available for the input value. If the control is not present, null is
   * returned.
   */
  get pending() {
    return this.control ? this.control.pending : null;
  }
  /**
   * @description
   * Reports whether the control is disabled, meaning that the control is disabled
   * in the UI and is exempt from validation checks and excluded from aggregate
   * values of ancestor controls. If the control is not present, null is returned.
   */
  get disabled() {
    return this.control ? this.control.disabled : null;
  }
  /**
   * @description
   * Reports whether the control is enabled, meaning that the control is included in ancestor
   * calculations of validity or value. If the control is not present, null is returned.
   */
  get enabled() {
    return this.control ? this.control.enabled : null;
  }
  /**
   * @description
   * Reports the control's validation errors. If the control is not present, null is returned.
   */
  get errors() {
    return this.control ? this.control.errors : null;
  }
  /**
   * @description
   * Reports whether the control is pristine, meaning that the user has not yet changed
   * the value in the UI. If the control is not present, null is returned.
   */
  get pristine() {
    return this.control ? this.control.pristine : null;
  }
  /**
   * @description
   * Reports whether the control is dirty, meaning that the user has changed
   * the value in the UI. If the control is not present, null is returned.
   */
  get dirty() {
    return this.control ? this.control.dirty : null;
  }
  /**
   * @description
   * Reports whether the control is touched, meaning that the user has triggered
   * a `blur` event on it. If the control is not present, null is returned.
   */
  get touched() {
    return this.control ? this.control.touched : null;
  }
  /**
   * @description
   * Reports the validation status of the control. Possible values include:
   * 'VALID', 'INVALID', 'DISABLED', and 'PENDING'.
   * If the control is not present, null is returned.
   */
  get status() {
    return this.control ? this.control.status : null;
  }
  /**
   * @description
   * Reports whether the control is untouched, meaning that the user has not yet triggered
   * a `blur` event on it. If the control is not present, null is returned.
   */
  get untouched() {
    return this.control ? this.control.untouched : null;
  }
  /**
   * @description
   * Returns a multicasting observable that emits a validation status whenever it is
   * calculated for the control. If the control is not present, null is returned.
   */
  get statusChanges() {
    return this.control ? this.control.statusChanges : null;
  }
  /**
   * @description
   * Returns a multicasting observable of value changes for the control that emits every time the
   * value of the control changes in the UI or programmatically.
   * If the control is not present, null is returned.
   */
  get valueChanges() {
    return this.control ? this.control.valueChanges : null;
  }
  /**
   * @description
   * Returns an array that represents the path from the top-level form to this control.
   * Each index is the string name of the control on that level.
   */
  get path() {
    return null;
  }
  /**
   * Contains the result of merging synchronous validators into a single validator function
   * (combined using `Validators.compose`).
   */
  _composedValidatorFn;
  /**
   * Contains the result of merging asynchronous validators into a single validator function
   * (combined using `Validators.composeAsync`).
   */
  _composedAsyncValidatorFn;
  /**
   * Set of synchronous validators as they were provided while calling `setValidators` function.
   * @internal
   */
  _rawValidators = [];
  /**
   * Set of asynchronous validators as they were provided while calling `setAsyncValidators`
   * function.
   * @internal
   */
  _rawAsyncValidators = [];
  /**
   * Sets synchronous validators for this directive.
   * @internal
   */
  _setValidators(validators) {
    this._rawValidators = validators || [];
    this._composedValidatorFn = composeValidators(this._rawValidators);
  }
  /**
   * Sets asynchronous validators for this directive.
   * @internal
   */
  _setAsyncValidators(validators) {
    this._rawAsyncValidators = validators || [];
    this._composedAsyncValidatorFn = composeAsyncValidators(this._rawAsyncValidators);
  }
  /**
   * @description
   * Synchronous validator function composed of all the synchronous validators registered with this
   * directive.
   */
  get validator() {
    return this._composedValidatorFn || null;
  }
  /**
   * @description
   * Asynchronous validator function composed of all the asynchronous validators registered with
   * this directive.
   */
  get asyncValidator() {
    return this._composedAsyncValidatorFn || null;
  }
  /*
   * The set of callbacks to be invoked when directive instance is being destroyed.
   */
  _onDestroyCallbacks = [];
  /**
   * Internal function to register callbacks that should be invoked
   * when directive instance is being destroyed.
   * @internal
   */
  _registerOnDestroy(fn) {
    this._onDestroyCallbacks.push(fn);
  }
  /**
   * Internal function to invoke all registered "on destroy" callbacks.
   * Note: calling this function also clears the list of callbacks.
   * @internal
   */
  _invokeOnDestroyCallbacks() {
    this._onDestroyCallbacks.forEach((fn) => fn());
    this._onDestroyCallbacks = [];
  }
  /**
   * @description
   * Resets the control with the provided value if the control is present.
   */
  reset(value = void 0) {
    if (this.control) this.control.reset(value);
  }
  /**
   * @description
   * Reports whether the control with the given path has the error specified.
   *
   * @param errorCode The code of the error to check
   * @param path A list of control names that designates how to move from the current control
   * to the control that should be queried for errors.
   *
   * @usageNotes
   * For example, for the following `FormGroup`:
   *
   * ```ts
   * form = new FormGroup({
   *   address: new FormGroup({ street: new FormControl() })
   * });
   * ```
   *
   * The path to the 'street' control from the root form would be 'address' -> 'street'.
   *
   * It can be provided to this method in one of two formats:
   *
   * 1. An array of string control names, e.g. `['address', 'street']`
   * 1. A period-delimited list of control names in one string, e.g. `'address.street'`
   *
   * If no path is given, this method checks for the error on the current control.
   *
   * @returns whether the given error is present in the control at the given path.
   *
   * If the control is not present, false is returned.
   */
  hasError(errorCode, path) {
    return this.control ? this.control.hasError(errorCode, path) : false;
  }
  /**
   * @description
   * Reports error data for the control with the given path.
   *
   * @param errorCode The code of the error to check
   * @param path A list of control names that designates how to move from the current control
   * to the control that should be queried for errors.
   *
   * @usageNotes
   * For example, for the following `FormGroup`:
   *
   * ```ts
   * form = new FormGroup({
   *   address: new FormGroup({ street: new FormControl() })
   * });
   * ```
   *
   * The path to the 'street' control from the root form would be 'address' -> 'street'.
   *
   * It can be provided to this method in one of two formats:
   *
   * 1. An array of string control names, e.g. `['address', 'street']`
   * 1. A period-delimited list of control names in one string, e.g. `'address.street'`
   *
   * @returns error data for that particular error. If the control or error is not present,
   * null is returned.
   */
  getError(errorCode, path) {
    return this.control ? this.control.getError(errorCode, path) : null;
  }
};
var ControlContainer = class extends AbstractControlDirective {
  /**
   * @description
   * The name for the control
   */
  name;
  /**
   * @description
   * The top-level form directive for the control.
   */
  get formDirective() {
    return null;
  }
  /**
   * @description
   * The path to this group.
   */
  get path() {
    return null;
  }
};
var NgControl = class extends AbstractControlDirective {
  /**
   * @description
   * The parent form for the control.
   *
   * @internal
   */
  _parent = null;
  /**
   * @description
   * The name for the control
   */
  name = null;
  /**
   * @description
   * The value accessor for the control
   */
  valueAccessor = null;
};
var AbstractControlStatus = class {
  _cd;
  constructor(cd) {
    this._cd = cd;
  }
  get isTouched() {
    this._cd?.control?._touched?.();
    return !!this._cd?.control?.touched;
  }
  get isUntouched() {
    return !!this._cd?.control?.untouched;
  }
  get isPristine() {
    this._cd?.control?._pristine?.();
    return !!this._cd?.control?.pristine;
  }
  get isDirty() {
    return !!this._cd?.control?.dirty;
  }
  get isValid() {
    this._cd?.control?._status?.();
    return !!this._cd?.control?.valid;
  }
  get isInvalid() {
    return !!this._cd?.control?.invalid;
  }
  get isPending() {
    return !!this._cd?.control?.pending;
  }
  get isSubmitted() {
    this._cd?._submitted?.();
    return !!this._cd?.submitted;
  }
};
var ngControlStatusHost = {
  "[class.ng-untouched]": "isUntouched",
  "[class.ng-touched]": "isTouched",
  "[class.ng-pristine]": "isPristine",
  "[class.ng-dirty]": "isDirty",
  "[class.ng-valid]": "isValid",
  "[class.ng-invalid]": "isInvalid",
  "[class.ng-pending]": "isPending"
};
var ngGroupStatusHost = __spreadProps(__spreadValues({}, ngControlStatusHost), {
  "[class.ng-submitted]": "isSubmitted"
});
var NgControlStatus = class _NgControlStatus extends AbstractControlStatus {
  constructor(cd) {
    super(cd);
  }
  static ɵfac = function NgControlStatus_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _NgControlStatus)(ɵɵdirectiveInject(NgControl, 2));
  };
  static ɵdir = ɵɵdefineDirective({
    type: _NgControlStatus,
    selectors: [["", "formControlName", ""], ["", "ngModel", ""], ["", "formControl", ""]],
    hostVars: 14,
    hostBindings: function NgControlStatus_HostBindings(rf, ctx) {
      if (rf & 2) {
        ɵɵclassProp("ng-untouched", ctx.isUntouched)("ng-touched", ctx.isTouched)("ng-pristine", ctx.isPristine)("ng-dirty", ctx.isDirty)("ng-valid", ctx.isValid)("ng-invalid", ctx.isInvalid)("ng-pending", ctx.isPending);
      }
    },
    standalone: false,
    features: [ɵɵInheritDefinitionFeature]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(NgControlStatus, [{
    type: Directive,
    args: [{
      selector: "[formControlName],[ngModel],[formControl]",
      host: ngControlStatusHost,
      standalone: false
    }]
  }], () => [{
    type: NgControl,
    decorators: [{
      type: Self
    }]
  }], null);
})();
var NgControlStatusGroup = class _NgControlStatusGroup extends AbstractControlStatus {
  constructor(cd) {
    super(cd);
  }
  static ɵfac = function NgControlStatusGroup_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _NgControlStatusGroup)(ɵɵdirectiveInject(ControlContainer, 10));
  };
  static ɵdir = ɵɵdefineDirective({
    type: _NgControlStatusGroup,
    selectors: [["", "formGroupName", ""], ["", "formArrayName", ""], ["", "ngModelGroup", ""], ["", "formGroup", ""], ["form", 3, "ngNoForm", ""], ["", "ngForm", ""]],
    hostVars: 16,
    hostBindings: function NgControlStatusGroup_HostBindings(rf, ctx) {
      if (rf & 2) {
        ɵɵclassProp("ng-untouched", ctx.isUntouched)("ng-touched", ctx.isTouched)("ng-pristine", ctx.isPristine)("ng-dirty", ctx.isDirty)("ng-valid", ctx.isValid)("ng-invalid", ctx.isInvalid)("ng-pending", ctx.isPending)("ng-submitted", ctx.isSubmitted);
      }
    },
    standalone: false,
    features: [ɵɵInheritDefinitionFeature]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(NgControlStatusGroup, [{
    type: Directive,
    args: [{
      selector: "[formGroupName],[formArrayName],[ngModelGroup],[formGroup],form:not([ngNoForm]),[ngForm]",
      host: ngGroupStatusHost,
      standalone: false
    }]
  }], () => [{
    type: ControlContainer,
    decorators: [{
      type: Optional
    }, {
      type: Self
    }]
  }], null);
})();
var formControlNameExample = `
  <div [formGroup]="myGroup">
    <input formControlName="firstName">
  </div>

  In your class:

  this.myGroup = new FormGroup({
      firstName: new FormControl()
  });`;
var formGroupNameExample = `
  <div [formGroup]="myGroup">
      <div formGroupName="person">
        <input formControlName="firstName">
      </div>
  </div>

  In your class:

  this.myGroup = new FormGroup({
      person: new FormGroup({ firstName: new FormControl() })
  });`;
var formArrayNameExample = `
  <div [formGroup]="myGroup">
    <div formArrayName="cities">
      <div *ngFor="let city of cityArray.controls; index as i">
        <input [formControlName]="i">
      </div>
    </div>
  </div>

  In your class:

  this.cityArray = new FormArray([new FormControl('SF')]);
  this.myGroup = new FormGroup({
    cities: this.cityArray
  });`;
var ngModelGroupExample = `
  <form>
      <div ngModelGroup="person">
        <input [(ngModel)]="person.name" name="firstName">
      </div>
  </form>`;
var ngModelWithFormGroupExample = `
  <div [formGroup]="myGroup">
      <input formControlName="firstName">
      <input [(ngModel)]="showMoreControls" [ngModelOptions]="{standalone: true}">
  </div>
`;
function controlParentException(nameOrIndex) {
  return new RuntimeError(1050, `formControlName must be used with a parent formGroup directive. You'll want to add a formGroup
      directive and pass it an existing FormGroup instance (you can create one in your class).

      ${describeFormControl(nameOrIndex)}

    Example:

    ${formControlNameExample}`);
}
function describeFormControl(nameOrIndex) {
  if (nameOrIndex == null || nameOrIndex === "") {
    return "";
  }
  const valueType = typeof nameOrIndex === "string" ? "name" : "index";
  return `Affected Form Control ${valueType}: "${nameOrIndex}"`;
}
function ngModelGroupException() {
  return new RuntimeError(1051, `formControlName cannot be used with an ngModelGroup parent. It is only compatible with parents
      that also have a "form" prefix: formGroupName, formArrayName, or formGroup.

      Option 1:  Update the parent to be formGroupName (reactive form strategy)

      ${formGroupNameExample}

      Option 2: Use ngModel instead of formControlName (template-driven strategy)

      ${ngModelGroupExample}`);
}
function missingFormException() {
  return new RuntimeError(1052, `formGroup expects a FormGroup instance. Please pass one in.

      Example:

      ${formControlNameExample}`);
}
function groupParentException() {
  return new RuntimeError(1053, `formGroupName must be used with a parent formGroup directive.  You'll want to add a formGroup
    directive and pass it an existing FormGroup instance (you can create one in your class).

    Example:

    ${formGroupNameExample}`);
}
function arrayParentException() {
  return new RuntimeError(1054, `formArrayName must be used with a parent formGroup directive.  You'll want to add a formGroup
      directive and pass it an existing FormGroup instance (you can create one in your class).

      Example:

      ${formArrayNameExample}`);
}
var disabledAttrWarning = `
  It looks like you're using the disabled attribute with a reactive form directive. If you set disabled to true
  when you set up this control in your component class, the disabled attribute will actually be set in the DOM for
  you. We recommend using this approach to avoid 'changed after checked' errors.

  Example:
  // Specify the \`disabled\` property at control creation time:
  form = new FormGroup({
    first: new FormControl({value: 'Nancy', disabled: true}, Validators.required),
    last: new FormControl('Drew', Validators.required)
  });

  // Controls can also be enabled/disabled after creation:
  form.get('first')?.enable();
  form.get('last')?.disable();
`;
var asyncValidatorsDroppedWithOptsWarning = `
  It looks like you're constructing using a FormControl with both an options argument and an
  async validators argument. Mixing these arguments will cause your async validators to be dropped.
  You should either put all your validators in the options object, or in separate validators
  arguments. For example:

  // Using validators arguments
  fc = new FormControl(42, Validators.required, myAsyncValidator);

  // Using AbstractControlOptions
  fc = new FormControl(42, {validators: Validators.required, asyncValidators: myAV});

  // Do NOT mix them: async validators will be dropped!
  fc = new FormControl(42, {validators: Validators.required}, /* Oops! */ myAsyncValidator);
`;
function ngModelWarning(directiveName) {
  return `
  It looks like you're using ngModel on the same form field as ${directiveName}.
  Support for using the ngModel input property and ngModelChange event with
  reactive form directives has been deprecated in Angular v6 and will be removed
  in a future version of Angular.

  For more information on this, see our API docs here:
  https://angular.io/api/forms/${directiveName === "formControl" ? "FormControlDirective" : "FormControlName"}#use-with-ngmodel
  `;
}
function describeKey(isFormGroup, key) {
  return isFormGroup ? `with name: '${key}'` : `at index: ${key}`;
}
function noControlsError(isFormGroup) {
  return `
    There are no form controls registered with this ${isFormGroup ? "group" : "array"} yet. If you're using ngModel,
    you may want to check next tick (e.g. use setTimeout).
  `;
}
function missingControlError(isFormGroup, key) {
  return `Cannot find form control ${describeKey(isFormGroup, key)}`;
}
function missingControlValueError(isFormGroup, key) {
  return `Must supply a value for form control ${describeKey(isFormGroup, key)}`;
}
var VALID = "VALID";
var INVALID = "INVALID";
var PENDING = "PENDING";
var DISABLED = "DISABLED";
var ControlEvent = class {
};
var ValueChangeEvent = class extends ControlEvent {
  value;
  source;
  constructor(value, source) {
    super();
    this.value = value;
    this.source = source;
  }
};
var PristineChangeEvent = class extends ControlEvent {
  pristine;
  source;
  constructor(pristine, source) {
    super();
    this.pristine = pristine;
    this.source = source;
  }
};
var TouchedChangeEvent = class extends ControlEvent {
  touched;
  source;
  constructor(touched, source) {
    super();
    this.touched = touched;
    this.source = source;
  }
};
var StatusChangeEvent = class extends ControlEvent {
  status;
  source;
  constructor(status, source) {
    super();
    this.status = status;
    this.source = source;
  }
};
var FormSubmittedEvent = class extends ControlEvent {
  source;
  constructor(source) {
    super();
    this.source = source;
  }
};
var FormResetEvent = class extends ControlEvent {
  source;
  constructor(source) {
    super();
    this.source = source;
  }
};
function pickValidators(validatorOrOpts) {
  return (isOptionsObj(validatorOrOpts) ? validatorOrOpts.validators : validatorOrOpts) || null;
}
function coerceToValidator(validator) {
  return Array.isArray(validator) ? composeValidators(validator) : validator || null;
}
function pickAsyncValidators(asyncValidator, validatorOrOpts) {
  if (typeof ngDevMode === "undefined" || ngDevMode) {
    if (isOptionsObj(validatorOrOpts) && asyncValidator) {
      console.warn(asyncValidatorsDroppedWithOptsWarning);
    }
  }
  return (isOptionsObj(validatorOrOpts) ? validatorOrOpts.asyncValidators : asyncValidator) || null;
}
function coerceToAsyncValidator(asyncValidator) {
  return Array.isArray(asyncValidator) ? composeAsyncValidators(asyncValidator) : asyncValidator || null;
}
function isOptionsObj(validatorOrOpts) {
  return validatorOrOpts != null && !Array.isArray(validatorOrOpts) && typeof validatorOrOpts === "object";
}
function assertControlPresent(parent, isGroup, key) {
  const controls = parent.controls;
  const collection = isGroup ? Object.keys(controls) : controls;
  if (!collection.length) {
    throw new RuntimeError(1e3, typeof ngDevMode === "undefined" || ngDevMode ? noControlsError(isGroup) : "");
  }
  if (!controls[key]) {
    throw new RuntimeError(1001, typeof ngDevMode === "undefined" || ngDevMode ? missingControlError(isGroup, key) : "");
  }
}
function assertAllValuesPresent(control, isGroup, value) {
  control._forEachChild((_, key) => {
    if (value[key] === void 0) {
      throw new RuntimeError(1002, typeof ngDevMode === "undefined" || ngDevMode ? missingControlValueError(isGroup, key) : "");
    }
  });
}
var AbstractControl = class {
  /** @internal */
  _pendingDirty = false;
  /**
   * Indicates that a control has its own pending asynchronous validation in progress.
   * It also stores if the control should emit events when the validation status changes.
   *
   * @internal
   */
  _hasOwnPendingAsyncValidator = null;
  /** @internal */
  _pendingTouched = false;
  /** @internal */
  _onCollectionChange = () => {
  };
  /** @internal */
  _updateOn;
  _parent = null;
  _asyncValidationSubscription;
  /**
   * Contains the result of merging synchronous validators into a single validator function
   * (combined using `Validators.compose`).
   *
   * @internal
   */
  _composedValidatorFn;
  /**
   * Contains the result of merging asynchronous validators into a single validator function
   * (combined using `Validators.composeAsync`).
   *
   * @internal
   */
  _composedAsyncValidatorFn;
  /**
   * Synchronous validators as they were provided:
   *  - in `AbstractControl` constructor
   *  - as an argument while calling `setValidators` function
   *  - while calling the setter on the `validator` field (e.g. `control.validator = validatorFn`)
   *
   * @internal
   */
  _rawValidators;
  /**
   * Asynchronous validators as they were provided:
   *  - in `AbstractControl` constructor
   *  - as an argument while calling `setAsyncValidators` function
   *  - while calling the setter on the `asyncValidator` field (e.g. `control.asyncValidator =
   * asyncValidatorFn`)
   *
   * @internal
   */
  _rawAsyncValidators;
  /**
   * The current value of the control.
   *
   * * For a `FormControl`, the current value.
   * * For an enabled `FormGroup`, the values of enabled controls as an object
   * with a key-value pair for each member of the group.
   * * For a disabled `FormGroup`, the values of all controls as an object
   * with a key-value pair for each member of the group.
   * * For a `FormArray`, the values of enabled controls as an array.
   *
   */
  value;
  /**
   * Initialize the AbstractControl instance.
   *
   * @param validators The function or array of functions that is used to determine the validity of
   *     this control synchronously.
   * @param asyncValidators The function or array of functions that is used to determine validity of
   *     this control asynchronously.
   */
  constructor(validators, asyncValidators) {
    this._assignValidators(validators);
    this._assignAsyncValidators(asyncValidators);
  }
  /**
   * Returns the function that is used to determine the validity of this control synchronously.
   * If multiple validators have been added, this will be a single composed function.
   * See `Validators.compose()` for additional information.
   */
  get validator() {
    return this._composedValidatorFn;
  }
  set validator(validatorFn) {
    this._rawValidators = this._composedValidatorFn = validatorFn;
  }
  /**
   * Returns the function that is used to determine the validity of this control asynchronously.
   * If multiple validators have been added, this will be a single composed function.
   * See `Validators.compose()` for additional information.
   */
  get asyncValidator() {
    return this._composedAsyncValidatorFn;
  }
  set asyncValidator(asyncValidatorFn) {
    this._rawAsyncValidators = this._composedAsyncValidatorFn = asyncValidatorFn;
  }
  /**
   * The parent control.
   */
  get parent() {
    return this._parent;
  }
  /**
   * The validation status of the control.
   *
   * @see {@link FormControlStatus}
   *
   * These status values are mutually exclusive, so a control cannot be
   * both valid AND invalid or invalid AND disabled.
   */
  get status() {
    return untracked(this.statusReactive);
  }
  set status(v) {
    untracked(() => this.statusReactive.set(v));
  }
  /** @internal */
  _status = computed(() => this.statusReactive());
  statusReactive = signal(void 0);
  /**
   * A control is `valid` when its `status` is `VALID`.
   *
   * @see {@link AbstractControl.status}
   *
   * @returns True if the control has passed all of its validation tests,
   * false otherwise.
   */
  get valid() {
    return this.status === VALID;
  }
  /**
   * A control is `invalid` when its `status` is `INVALID`.
   *
   * @see {@link AbstractControl.status}
   *
   * @returns True if this control has failed one or more of its validation checks,
   * false otherwise.
   */
  get invalid() {
    return this.status === INVALID;
  }
  /**
   * A control is `pending` when its `status` is `PENDING`.
   *
   * @see {@link AbstractControl.status}
   *
   * @returns True if this control is in the process of conducting a validation check,
   * false otherwise.
   */
  get pending() {
    return this.status == PENDING;
  }
  /**
   * A control is `disabled` when its `status` is `DISABLED`.
   *
   * Disabled controls are exempt from validation checks and
   * are not included in the aggregate value of their ancestor
   * controls.
   *
   * @see {@link AbstractControl.status}
   *
   * @returns True if the control is disabled, false otherwise.
   */
  get disabled() {
    return this.status === DISABLED;
  }
  /**
   * A control is `enabled` as long as its `status` is not `DISABLED`.
   *
   * @returns True if the control has any status other than 'DISABLED',
   * false if the status is 'DISABLED'.
   *
   * @see {@link AbstractControl.status}
   *
   */
  get enabled() {
    return this.status !== DISABLED;
  }
  /**
   * An object containing any errors generated by failing validation,
   * or null if there are no errors.
   */
  errors;
  /**
   * A control is `pristine` if the user has not yet changed
   * the value in the UI.
   *
   * @returns True if the user has not yet changed the value in the UI; compare `dirty`.
   * Programmatic changes to a control's value do not mark it dirty.
   */
  get pristine() {
    return untracked(this.pristineReactive);
  }
  set pristine(v) {
    untracked(() => this.pristineReactive.set(v));
  }
  /** @internal */
  _pristine = computed(() => this.pristineReactive());
  pristineReactive = signal(true);
  /**
   * A control is `dirty` if the user has changed the value
   * in the UI.
   *
   * @returns True if the user has changed the value of this control in the UI; compare `pristine`.
   * Programmatic changes to a control's value do not mark it dirty.
   */
  get dirty() {
    return !this.pristine;
  }
  /**
   * True if the control is marked as `touched`.
   *
   * A control is marked `touched` once the user has triggered
   * a `blur` event on it.
   */
  get touched() {
    return untracked(this.touchedReactive);
  }
  set touched(v) {
    untracked(() => this.touchedReactive.set(v));
  }
  /** @internal */
  _touched = computed(() => this.touchedReactive());
  touchedReactive = signal(false);
  /**
   * True if the control has not been marked as touched
   *
   * A control is `untouched` if the user has not yet triggered
   * a `blur` event on it.
   */
  get untouched() {
    return !this.touched;
  }
  /**
   * Exposed as observable, see below.
   *
   * @internal
   */
  _events = new Subject();
  /**
   * A multicasting observable that emits an event every time the state of the control changes.
   * It emits for value, status, pristine or touched changes.
   *
   * **Note**: On value change, the emit happens right after a value of this control is updated. The
   * value of a parent control (for example if this FormControl is a part of a FormGroup) is updated
   * later, so accessing a value of a parent control (using the `value` property) from the callback
   * of this event might result in getting a value that has not been updated yet. Subscribe to the
   * `events` of the parent control instead.
   * For other event types, the events are emitted after the parent control has been updated.
   *
   */
  events = this._events.asObservable();
  /**
   * A multicasting observable that emits an event every time the value of the control changes, in
   * the UI or programmatically. It also emits an event each time you call enable() or disable()
   * without passing along {emitEvent: false} as a function argument.
   *
   * **Note**: the emit happens right after a value of this control is updated. The value of a
   * parent control (for example if this FormControl is a part of a FormGroup) is updated later, so
   * accessing a value of a parent control (using the `value` property) from the callback of this
   * event might result in getting a value that has not been updated yet. Subscribe to the
   * `valueChanges` event of the parent control instead.
   */
  valueChanges;
  /**
   * A multicasting observable that emits an event every time the validation `status` of the control
   * recalculates.
   *
   * @see {@link FormControlStatus}
   * @see {@link AbstractControl.status}
   */
  statusChanges;
  /**
   * Reports the update strategy of the `AbstractControl` (meaning
   * the event on which the control updates itself).
   * Possible values: `'change'` | `'blur'` | `'submit'`
   * Default value: `'change'`
   */
  get updateOn() {
    return this._updateOn ? this._updateOn : this.parent ? this.parent.updateOn : "change";
  }
  /**
   * Sets the synchronous validators that are active on this control.  Calling
   * this overwrites any existing synchronous validators.
   *
   * When you add or remove a validator at run time, you must call
   * `updateValueAndValidity()` for the new validation to take effect.
   *
   * If you want to add a new validator without affecting existing ones, consider
   * using `addValidators()` method instead.
   */
  setValidators(validators) {
    this._assignValidators(validators);
  }
  /**
   * Sets the asynchronous validators that are active on this control. Calling this
   * overwrites any existing asynchronous validators.
   *
   * When you add or remove a validator at run time, you must call
   * `updateValueAndValidity()` for the new validation to take effect.
   *
   * If you want to add a new validator without affecting existing ones, consider
   * using `addAsyncValidators()` method instead.
   */
  setAsyncValidators(validators) {
    this._assignAsyncValidators(validators);
  }
  /**
   * Add a synchronous validator or validators to this control, without affecting other validators.
   *
   * When you add or remove a validator at run time, you must call
   * `updateValueAndValidity()` for the new validation to take effect.
   *
   * Adding a validator that already exists will have no effect. If duplicate validator functions
   * are present in the `validators` array, only the first instance would be added to a form
   * control.
   *
   * @param validators The new validator function or functions to add to this control.
   */
  addValidators(validators) {
    this.setValidators(addValidators(validators, this._rawValidators));
  }
  /**
   * Add an asynchronous validator or validators to this control, without affecting other
   * validators.
   *
   * When you add or remove a validator at run time, you must call
   * `updateValueAndValidity()` for the new validation to take effect.
   *
   * Adding a validator that already exists will have no effect.
   *
   * @param validators The new asynchronous validator function or functions to add to this control.
   */
  addAsyncValidators(validators) {
    this.setAsyncValidators(addValidators(validators, this._rawAsyncValidators));
  }
  /**
   * Remove a synchronous validator from this control, without affecting other validators.
   * Validators are compared by function reference; you must pass a reference to the exact same
   * validator function as the one that was originally set. If a provided validator is not found,
   * it is ignored.
   *
   * @usageNotes
   *
   * ### Reference to a ValidatorFn
   *
   * ```
   * // Reference to the RequiredValidator
   * const ctrl = new FormControl<string | null>('', Validators.required);
   * ctrl.removeValidators(Validators.required);
   *
   * // Reference to anonymous function inside MinValidator
   * const minValidator = Validators.min(3);
   * const ctrl = new FormControl<string | null>('', minValidator);
   * expect(ctrl.hasValidator(minValidator)).toEqual(true)
   * expect(ctrl.hasValidator(Validators.min(3))).toEqual(false)
   *
   * ctrl.removeValidators(minValidator);
   * ```
   *
   * When you add or remove a validator at run time, you must call
   * `updateValueAndValidity()` for the new validation to take effect.
   *
   * @param validators The validator or validators to remove.
   */
  removeValidators(validators) {
    this.setValidators(removeValidators(validators, this._rawValidators));
  }
  /**
   * Remove an asynchronous validator from this control, without affecting other validators.
   * Validators are compared by function reference; you must pass a reference to the exact same
   * validator function as the one that was originally set. If a provided validator is not found, it
   * is ignored.
   *
   * When you add or remove a validator at run time, you must call
   * `updateValueAndValidity()` for the new validation to take effect.
   *
   * @param validators The asynchronous validator or validators to remove.
   */
  removeAsyncValidators(validators) {
    this.setAsyncValidators(removeValidators(validators, this._rawAsyncValidators));
  }
  /**
   * Check whether a synchronous validator function is present on this control. The provided
   * validator must be a reference to the exact same function that was provided.
   *
   * @usageNotes
   *
   * ### Reference to a ValidatorFn
   *
   * ```
   * // Reference to the RequiredValidator
   * const ctrl = new FormControl<number | null>(0, Validators.required);
   * expect(ctrl.hasValidator(Validators.required)).toEqual(true)
   *
   * // Reference to anonymous function inside MinValidator
   * const minValidator = Validators.min(3);
   * const ctrl = new FormControl<number | null>(0, minValidator);
   * expect(ctrl.hasValidator(minValidator)).toEqual(true)
   * expect(ctrl.hasValidator(Validators.min(3))).toEqual(false)
   * ```
   *
   * @param validator The validator to check for presence. Compared by function reference.
   * @returns Whether the provided validator was found on this control.
   */
  hasValidator(validator) {
    return hasValidator(this._rawValidators, validator);
  }
  /**
   * Check whether an asynchronous validator function is present on this control. The provided
   * validator must be a reference to the exact same function that was provided.
   *
   * @param validator The asynchronous validator to check for presence. Compared by function
   *     reference.
   * @returns Whether the provided asynchronous validator was found on this control.
   */
  hasAsyncValidator(validator) {
    return hasValidator(this._rawAsyncValidators, validator);
  }
  /**
   * Empties out the synchronous validator list.
   *
   * When you add or remove a validator at run time, you must call
   * `updateValueAndValidity()` for the new validation to take effect.
   *
   */
  clearValidators() {
    this.validator = null;
  }
  /**
   * Empties out the async validator list.
   *
   * When you add or remove a validator at run time, you must call
   * `updateValueAndValidity()` for the new validation to take effect.
   *
   */
  clearAsyncValidators() {
    this.asyncValidator = null;
  }
  markAsTouched(opts = {}) {
    const changed = this.touched === false;
    this.touched = true;
    const sourceControl = opts.sourceControl ?? this;
    if (this._parent && !opts.onlySelf) {
      this._parent.markAsTouched(__spreadProps(__spreadValues({}, opts), {
        sourceControl
      }));
    }
    if (changed && opts.emitEvent !== false) {
      this._events.next(new TouchedChangeEvent(true, sourceControl));
    }
  }
  /**
   * Marks the control and all its descendant controls as `touched`.
   * @see {@link markAsTouched()}
   *
   * @param opts Configuration options that determine how the control propagates changes
   * and emits events after marking is applied.
   * * `emitEvent`: When true or not supplied (the default), the `events`
   * observable emits a `TouchedChangeEvent` with the `touched` property being `true`.
   * When false, no events are emitted.
   */
  markAllAsTouched(opts = {}) {
    this.markAsTouched({
      onlySelf: true,
      emitEvent: opts.emitEvent,
      sourceControl: this
    });
    this._forEachChild((control) => control.markAllAsTouched(opts));
  }
  markAsUntouched(opts = {}) {
    const changed = this.touched === true;
    this.touched = false;
    this._pendingTouched = false;
    const sourceControl = opts.sourceControl ?? this;
    this._forEachChild((control) => {
      control.markAsUntouched({
        onlySelf: true,
        emitEvent: opts.emitEvent,
        sourceControl
      });
    });
    if (this._parent && !opts.onlySelf) {
      this._parent._updateTouched(opts, sourceControl);
    }
    if (changed && opts.emitEvent !== false) {
      this._events.next(new TouchedChangeEvent(false, sourceControl));
    }
  }
  markAsDirty(opts = {}) {
    const changed = this.pristine === true;
    this.pristine = false;
    const sourceControl = opts.sourceControl ?? this;
    if (this._parent && !opts.onlySelf) {
      this._parent.markAsDirty(__spreadProps(__spreadValues({}, opts), {
        sourceControl
      }));
    }
    if (changed && opts.emitEvent !== false) {
      this._events.next(new PristineChangeEvent(false, sourceControl));
    }
  }
  markAsPristine(opts = {}) {
    const changed = this.pristine === false;
    this.pristine = true;
    this._pendingDirty = false;
    const sourceControl = opts.sourceControl ?? this;
    this._forEachChild((control) => {
      control.markAsPristine({
        onlySelf: true,
        emitEvent: opts.emitEvent
      });
    });
    if (this._parent && !opts.onlySelf) {
      this._parent._updatePristine(opts, sourceControl);
    }
    if (changed && opts.emitEvent !== false) {
      this._events.next(new PristineChangeEvent(true, sourceControl));
    }
  }
  markAsPending(opts = {}) {
    this.status = PENDING;
    const sourceControl = opts.sourceControl ?? this;
    if (opts.emitEvent !== false) {
      this._events.next(new StatusChangeEvent(this.status, sourceControl));
      this.statusChanges.emit(this.status);
    }
    if (this._parent && !opts.onlySelf) {
      this._parent.markAsPending(__spreadProps(__spreadValues({}, opts), {
        sourceControl
      }));
    }
  }
  disable(opts = {}) {
    const skipPristineCheck = this._parentMarkedDirty(opts.onlySelf);
    this.status = DISABLED;
    this.errors = null;
    this._forEachChild((control) => {
      control.disable(__spreadProps(__spreadValues({}, opts), {
        onlySelf: true
      }));
    });
    this._updateValue();
    const sourceControl = opts.sourceControl ?? this;
    if (opts.emitEvent !== false) {
      this._events.next(new ValueChangeEvent(this.value, sourceControl));
      this._events.next(new StatusChangeEvent(this.status, sourceControl));
      this.valueChanges.emit(this.value);
      this.statusChanges.emit(this.status);
    }
    this._updateAncestors(__spreadProps(__spreadValues({}, opts), {
      skipPristineCheck
    }), this);
    this._onDisabledChange.forEach((changeFn) => changeFn(true));
  }
  /**
   * Enables the control. This means the control is included in validation checks and
   * the aggregate value of its parent. Its status recalculates based on its value and
   * its validators.
   *
   * By default, if the control has children, all children are enabled.
   *
   * @see {@link AbstractControl.status}
   *
   * @param opts Configure options that control how the control propagates changes and
   * emits events when marked as untouched
   * * `onlySelf`: When true, mark only this control. When false or not supplied,
   * marks all direct ancestors. Default is false.
   * * `emitEvent`: When true or not supplied (the default), the `statusChanges`,
   * `valueChanges` and `events`
   * observables emit events with the latest status and value when the control is enabled.
   * When false, no events are emitted.
   */
  enable(opts = {}) {
    const skipPristineCheck = this._parentMarkedDirty(opts.onlySelf);
    this.status = VALID;
    this._forEachChild((control) => {
      control.enable(__spreadProps(__spreadValues({}, opts), {
        onlySelf: true
      }));
    });
    this.updateValueAndValidity({
      onlySelf: true,
      emitEvent: opts.emitEvent
    });
    this._updateAncestors(__spreadProps(__spreadValues({}, opts), {
      skipPristineCheck
    }), this);
    this._onDisabledChange.forEach((changeFn) => changeFn(false));
  }
  _updateAncestors(opts, sourceControl) {
    if (this._parent && !opts.onlySelf) {
      this._parent.updateValueAndValidity(opts);
      if (!opts.skipPristineCheck) {
        this._parent._updatePristine({}, sourceControl);
      }
      this._parent._updateTouched({}, sourceControl);
    }
  }
  /**
   * Sets the parent of the control
   *
   * @param parent The new parent.
   */
  setParent(parent) {
    this._parent = parent;
  }
  /**
   * The raw value of this control. For most control implementations, the raw value will include
   * disabled children.
   */
  getRawValue() {
    return this.value;
  }
  updateValueAndValidity(opts = {}) {
    this._setInitialStatus();
    this._updateValue();
    if (this.enabled) {
      const shouldHaveEmitted = this._cancelExistingSubscription();
      this.errors = this._runValidator();
      this.status = this._calculateStatus();
      if (this.status === VALID || this.status === PENDING) {
        this._runAsyncValidator(shouldHaveEmitted, opts.emitEvent);
      }
    }
    const sourceControl = opts.sourceControl ?? this;
    if (opts.emitEvent !== false) {
      this._events.next(new ValueChangeEvent(this.value, sourceControl));
      this._events.next(new StatusChangeEvent(this.status, sourceControl));
      this.valueChanges.emit(this.value);
      this.statusChanges.emit(this.status);
    }
    if (this._parent && !opts.onlySelf) {
      this._parent.updateValueAndValidity(__spreadProps(__spreadValues({}, opts), {
        sourceControl
      }));
    }
  }
  /** @internal */
  _updateTreeValidity(opts = {
    emitEvent: true
  }) {
    this._forEachChild((ctrl) => ctrl._updateTreeValidity(opts));
    this.updateValueAndValidity({
      onlySelf: true,
      emitEvent: opts.emitEvent
    });
  }
  _setInitialStatus() {
    this.status = this._allControlsDisabled() ? DISABLED : VALID;
  }
  _runValidator() {
    return this.validator ? this.validator(this) : null;
  }
  _runAsyncValidator(shouldHaveEmitted, emitEvent) {
    if (this.asyncValidator) {
      this.status = PENDING;
      this._hasOwnPendingAsyncValidator = {
        emitEvent: emitEvent !== false
      };
      const obs = toObservable(this.asyncValidator(this));
      this._asyncValidationSubscription = obs.subscribe((errors) => {
        this._hasOwnPendingAsyncValidator = null;
        this.setErrors(errors, {
          emitEvent,
          shouldHaveEmitted
        });
      });
    }
  }
  _cancelExistingSubscription() {
    if (this._asyncValidationSubscription) {
      this._asyncValidationSubscription.unsubscribe();
      const shouldHaveEmitted = this._hasOwnPendingAsyncValidator?.emitEvent ?? false;
      this._hasOwnPendingAsyncValidator = null;
      return shouldHaveEmitted;
    }
    return false;
  }
  setErrors(errors, opts = {}) {
    this.errors = errors;
    this._updateControlsErrors(opts.emitEvent !== false, this, opts.shouldHaveEmitted);
  }
  /**
   * Retrieves a child control given the control's name or path.
   *
   * @param path A dot-delimited string or array of string/number values that define the path to the
   * control. If a string is provided, passing it as a string literal will result in improved type
   * information. Likewise, if an array is provided, passing it `as const` will cause improved type
   * information to be available.
   *
   * @usageNotes
   * ### Retrieve a nested control
   *
   * For example, to get a `name` control nested within a `person` sub-group:
   *
   * * `this.form.get('person.name');`
   *
   * -OR-
   *
   * * `this.form.get(['person', 'name'] as const);` // `as const` gives improved typings
   *
   * ### Retrieve a control in a FormArray
   *
   * When accessing an element inside a FormArray, you can use an element index.
   * For example, to get a `price` control from the first element in an `items` array you can use:
   *
   * * `this.form.get('items.0.price');`
   *
   * -OR-
   *
   * * `this.form.get(['items', 0, 'price']);`
   */
  get(path) {
    let currPath = path;
    if (currPath == null) return null;
    if (!Array.isArray(currPath)) currPath = currPath.split(".");
    if (currPath.length === 0) return null;
    return currPath.reduce((control, name) => control && control._find(name), this);
  }
  /**
   * @description
   * Reports error data for the control with the given path.
   *
   * @param errorCode The code of the error to check
   * @param path A list of control names that designates how to move from the current control
   * to the control that should be queried for errors.
   *
   * @usageNotes
   * For example, for the following `FormGroup`:
   *
   * ```ts
   * form = new FormGroup({
   *   address: new FormGroup({ street: new FormControl() })
   * });
   * ```
   *
   * The path to the 'street' control from the root form would be 'address' -> 'street'.
   *
   * It can be provided to this method in one of two formats:
   *
   * 1. An array of string control names, e.g. `['address', 'street']`
   * 1. A period-delimited list of control names in one string, e.g. `'address.street'`
   *
   * @returns error data for that particular error. If the control or error is not present,
   * null is returned.
   */
  getError(errorCode, path) {
    const control = path ? this.get(path) : this;
    return control && control.errors ? control.errors[errorCode] : null;
  }
  /**
   * @description
   * Reports whether the control with the given path has the error specified.
   *
   * @param errorCode The code of the error to check
   * @param path A list of control names that designates how to move from the current control
   * to the control that should be queried for errors.
   *
   * @usageNotes
   * For example, for the following `FormGroup`:
   *
   * ```ts
   * form = new FormGroup({
   *   address: new FormGroup({ street: new FormControl() })
   * });
   * ```
   *
   * The path to the 'street' control from the root form would be 'address' -> 'street'.
   *
   * It can be provided to this method in one of two formats:
   *
   * 1. An array of string control names, e.g. `['address', 'street']`
   * 1. A period-delimited list of control names in one string, e.g. `'address.street'`
   *
   * If no path is given, this method checks for the error on the current control.
   *
   * @returns whether the given error is present in the control at the given path.
   *
   * If the control is not present, false is returned.
   */
  hasError(errorCode, path) {
    return !!this.getError(errorCode, path);
  }
  /**
   * Retrieves the top-level ancestor of this control.
   */
  get root() {
    let x = this;
    while (x._parent) {
      x = x._parent;
    }
    return x;
  }
  /** @internal */
  _updateControlsErrors(emitEvent, changedControl, shouldHaveEmitted) {
    this.status = this._calculateStatus();
    if (emitEvent) {
      this.statusChanges.emit(this.status);
    }
    if (emitEvent || shouldHaveEmitted) {
      this._events.next(new StatusChangeEvent(this.status, changedControl));
    }
    if (this._parent) {
      this._parent._updateControlsErrors(emitEvent, changedControl, shouldHaveEmitted);
    }
  }
  /** @internal */
  _initObservables() {
    this.valueChanges = new EventEmitter();
    this.statusChanges = new EventEmitter();
  }
  _calculateStatus() {
    if (this._allControlsDisabled()) return DISABLED;
    if (this.errors) return INVALID;
    if (this._hasOwnPendingAsyncValidator || this._anyControlsHaveStatus(PENDING)) return PENDING;
    if (this._anyControlsHaveStatus(INVALID)) return INVALID;
    return VALID;
  }
  /** @internal */
  _anyControlsHaveStatus(status) {
    return this._anyControls((control) => control.status === status);
  }
  /** @internal */
  _anyControlsDirty() {
    return this._anyControls((control) => control.dirty);
  }
  /** @internal */
  _anyControlsTouched() {
    return this._anyControls((control) => control.touched);
  }
  /** @internal */
  _updatePristine(opts, changedControl) {
    const newPristine = !this._anyControlsDirty();
    const changed = this.pristine !== newPristine;
    this.pristine = newPristine;
    if (this._parent && !opts.onlySelf) {
      this._parent._updatePristine(opts, changedControl);
    }
    if (changed) {
      this._events.next(new PristineChangeEvent(this.pristine, changedControl));
    }
  }
  /** @internal */
  _updateTouched(opts = {}, changedControl) {
    this.touched = this._anyControlsTouched();
    this._events.next(new TouchedChangeEvent(this.touched, changedControl));
    if (this._parent && !opts.onlySelf) {
      this._parent._updateTouched(opts, changedControl);
    }
  }
  /** @internal */
  _onDisabledChange = [];
  /** @internal */
  _registerOnCollectionChange(fn) {
    this._onCollectionChange = fn;
  }
  /** @internal */
  _setUpdateStrategy(opts) {
    if (isOptionsObj(opts) && opts.updateOn != null) {
      this._updateOn = opts.updateOn;
    }
  }
  /**
   * Check to see if parent has been marked artificially dirty.
   *
   * @internal
   */
  _parentMarkedDirty(onlySelf) {
    const parentDirty = this._parent && this._parent.dirty;
    return !onlySelf && !!parentDirty && !this._parent._anyControlsDirty();
  }
  /** @internal */
  _find(name) {
    return null;
  }
  /**
   * Internal implementation of the `setValidators` method. Needs to be separated out into a
   * different method, because it is called in the constructor and it can break cases where
   * a control is extended.
   */
  _assignValidators(validators) {
    this._rawValidators = Array.isArray(validators) ? validators.slice() : validators;
    this._composedValidatorFn = coerceToValidator(this._rawValidators);
  }
  /**
   * Internal implementation of the `setAsyncValidators` method. Needs to be separated out into a
   * different method, because it is called in the constructor and it can break cases where
   * a control is extended.
   */
  _assignAsyncValidators(validators) {
    this._rawAsyncValidators = Array.isArray(validators) ? validators.slice() : validators;
    this._composedAsyncValidatorFn = coerceToAsyncValidator(this._rawAsyncValidators);
  }
};
var FormGroup = class extends AbstractControl {
  /**
   * Creates a new `FormGroup` instance.
   *
   * @param controls A collection of child controls. The key for each child is the name
   * under which it is registered.
   *
   * @param validatorOrOpts A synchronous validator function, or an array of
   * such functions, or an `AbstractControlOptions` object that contains validation functions
   * and a validation trigger.
   *
   * @param asyncValidator A single async validator or array of async validator functions
   *
   */
  constructor(controls, validatorOrOpts, asyncValidator) {
    super(pickValidators(validatorOrOpts), pickAsyncValidators(asyncValidator, validatorOrOpts));
    (typeof ngDevMode === "undefined" || ngDevMode) && validateFormGroupControls(controls);
    this.controls = controls;
    this._initObservables();
    this._setUpdateStrategy(validatorOrOpts);
    this._setUpControls();
    this.updateValueAndValidity({
      onlySelf: true,
      // If `asyncValidator` is present, it will trigger control status change from `PENDING` to
      // `VALID` or `INVALID`. The status should be broadcasted via the `statusChanges` observable,
      // so we set `emitEvent` to `true` to allow that during the control creation process.
      emitEvent: !!this.asyncValidator
    });
  }
  controls;
  registerControl(name, control) {
    if (this.controls[name]) return this.controls[name];
    this.controls[name] = control;
    control.setParent(this);
    control._registerOnCollectionChange(this._onCollectionChange);
    return control;
  }
  addControl(name, control, options = {}) {
    this.registerControl(name, control);
    this.updateValueAndValidity({
      emitEvent: options.emitEvent
    });
    this._onCollectionChange();
  }
  /**
   * Remove a control from this group. In a strongly-typed group, required controls cannot be
   * removed.
   *
   * This method also updates the value and validity of the control.
   *
   * @param name The control name to remove from the collection
   * @param options Specifies whether this FormGroup instance should emit events after a
   *     control is removed.
   * * `emitEvent`: When true or not supplied (the default), both the `statusChanges` and
   * `valueChanges` observables emit events with the latest status and value when the control is
   * removed. When false, no events are emitted.
   */
  removeControl(name, options = {}) {
    if (this.controls[name]) this.controls[name]._registerOnCollectionChange(() => {
    });
    delete this.controls[name];
    this.updateValueAndValidity({
      emitEvent: options.emitEvent
    });
    this._onCollectionChange();
  }
  setControl(name, control, options = {}) {
    if (this.controls[name]) this.controls[name]._registerOnCollectionChange(() => {
    });
    delete this.controls[name];
    if (control) this.registerControl(name, control);
    this.updateValueAndValidity({
      emitEvent: options.emitEvent
    });
    this._onCollectionChange();
  }
  contains(controlName) {
    return this.controls.hasOwnProperty(controlName) && this.controls[controlName].enabled;
  }
  /**
   * Sets the value of the `FormGroup`. It accepts an object that matches
   * the structure of the group, with control names as keys.
   *
   * @usageNotes
   * ### Set the complete value for the form group
   *
   * ```ts
   * const form = new FormGroup({
   *   first: new FormControl(),
   *   last: new FormControl()
   * });
   *
   * console.log(form.value);   // {first: null, last: null}
   *
   * form.setValue({first: 'Nancy', last: 'Drew'});
   * console.log(form.value);   // {first: 'Nancy', last: 'Drew'}
   * ```
   *
   * @throws When strict checks fail, such as setting the value of a control
   * that doesn't exist or if you exclude a value of a control that does exist.
   *
   * @param value The new value for the control that matches the structure of the group.
   * @param options Configuration options that determine how the control propagates changes
   * and emits events after the value changes.
   * The configuration options are passed to the {@link AbstractControl#updateValueAndValidity
   * updateValueAndValidity} method.
   *
   * * `onlySelf`: When true, each change only affects this control, and not its parent. Default is
   * false.
   * * `emitEvent`: When true or not supplied (the default), both the `statusChanges` and
   * `valueChanges`
   * observables emit events with the latest status and value when the control value is updated.
   * When false, no events are emitted.
   */
  setValue(value, options = {}) {
    assertAllValuesPresent(this, true, value);
    Object.keys(value).forEach((name) => {
      assertControlPresent(this, true, name);
      this.controls[name].setValue(value[name], {
        onlySelf: true,
        emitEvent: options.emitEvent
      });
    });
    this.updateValueAndValidity(options);
  }
  /**
   * Patches the value of the `FormGroup`. It accepts an object with control
   * names as keys, and does its best to match the values to the correct controls
   * in the group.
   *
   * It accepts both super-sets and sub-sets of the group without throwing an error.
   *
   * @usageNotes
   * ### Patch the value for a form group
   *
   * ```ts
   * const form = new FormGroup({
   *    first: new FormControl(),
   *    last: new FormControl()
   * });
   * console.log(form.value);   // {first: null, last: null}
   *
   * form.patchValue({first: 'Nancy'});
   * console.log(form.value);   // {first: 'Nancy', last: null}
   * ```
   *
   * @param value The object that matches the structure of the group.
   * @param options Configuration options that determine how the control propagates changes and
   * emits events after the value is patched.
   * * `onlySelf`: When true, each change only affects this control and not its parent. Default is
   * true.
   * * `emitEvent`: When true or not supplied (the default), both the `statusChanges` and
   * `valueChanges` observables emit events with the latest status and value when the control value
   * is updated. When false, no events are emitted. The configuration options are passed to
   * the {@link AbstractControl#updateValueAndValidity updateValueAndValidity} method.
   */
  patchValue(value, options = {}) {
    if (value == null) return;
    Object.keys(value).forEach((name) => {
      const control = this.controls[name];
      if (control) {
        control.patchValue(
          /* Guaranteed to be present, due to the outer forEach. */
          value[name],
          {
            onlySelf: true,
            emitEvent: options.emitEvent
          }
        );
      }
    });
    this.updateValueAndValidity(options);
  }
  /**
   * Resets the `FormGroup`, marks all descendants `pristine` and `untouched` and sets
   * the value of all descendants to their default values, or null if no defaults were provided.
   *
   * You reset to a specific form state by passing in a map of states
   * that matches the structure of your form, with control names as keys. The state
   * is a standalone value or a form state object with both a value and a disabled
   * status.
   *
   * @param value Resets the control with an initial value,
   * or an object that defines the initial value and disabled state.
   *
   * @param options Configuration options that determine how the control propagates changes
   * and emits events when the group is reset.
   * * `onlySelf`: When true, each change only affects this control, and not its parent. Default is
   * false.
   * * `emitEvent`: When true or not supplied (the default), both the `statusChanges` and
   * `valueChanges`
   * observables emit events with the latest status and value when the control is reset.
   * When false, no events are emitted.
   * The configuration options are passed to the {@link AbstractControl#updateValueAndValidity
   * updateValueAndValidity} method.
   *
   * @usageNotes
   *
   * ### Reset the form group values
   *
   * ```ts
   * const form = new FormGroup({
   *   first: new FormControl('first name'),
   *   last: new FormControl('last name')
   * });
   *
   * console.log(form.value);  // {first: 'first name', last: 'last name'}
   *
   * form.reset({ first: 'name', last: 'last name' });
   *
   * console.log(form.value);  // {first: 'name', last: 'last name'}
   * ```
   *
   * ### Reset the form group values and disabled status
   *
   * ```ts
   * const form = new FormGroup({
   *   first: new FormControl('first name'),
   *   last: new FormControl('last name')
   * });
   *
   * form.reset({
   *   first: {value: 'name', disabled: true},
   *   last: 'last'
   * });
   *
   * console.log(form.value);  // {last: 'last'}
   * console.log(form.get('first').status);  // 'DISABLED'
   * ```
   */
  reset(value = {}, options = {}) {
    this._forEachChild((control, name) => {
      control.reset(value ? value[name] : null, {
        onlySelf: true,
        emitEvent: options.emitEvent
      });
    });
    this._updatePristine(options, this);
    this._updateTouched(options, this);
    this.updateValueAndValidity(options);
  }
  /**
   * The aggregate value of the `FormGroup`, including any disabled controls.
   *
   * Retrieves all values regardless of disabled status.
   */
  getRawValue() {
    return this._reduceChildren({}, (acc, control, name) => {
      acc[name] = control.getRawValue();
      return acc;
    });
  }
  /** @internal */
  _syncPendingControls() {
    let subtreeUpdated = this._reduceChildren(false, (updated, child) => {
      return child._syncPendingControls() ? true : updated;
    });
    if (subtreeUpdated) this.updateValueAndValidity({
      onlySelf: true
    });
    return subtreeUpdated;
  }
  /** @internal */
  _forEachChild(cb) {
    Object.keys(this.controls).forEach((key) => {
      const control = this.controls[key];
      control && cb(control, key);
    });
  }
  /** @internal */
  _setUpControls() {
    this._forEachChild((control) => {
      control.setParent(this);
      control._registerOnCollectionChange(this._onCollectionChange);
    });
  }
  /** @internal */
  _updateValue() {
    this.value = this._reduceValue();
  }
  /** @internal */
  _anyControls(condition) {
    for (const [controlName, control] of Object.entries(this.controls)) {
      if (this.contains(controlName) && condition(control)) {
        return true;
      }
    }
    return false;
  }
  /** @internal */
  _reduceValue() {
    let acc = {};
    return this._reduceChildren(acc, (acc2, control, name) => {
      if (control.enabled || this.disabled) {
        acc2[name] = control.value;
      }
      return acc2;
    });
  }
  /** @internal */
  _reduceChildren(initValue, fn) {
    let res = initValue;
    this._forEachChild((control, name) => {
      res = fn(res, control, name);
    });
    return res;
  }
  /** @internal */
  _allControlsDisabled() {
    for (const controlName of Object.keys(this.controls)) {
      if (this.controls[controlName].enabled) {
        return false;
      }
    }
    return Object.keys(this.controls).length > 0 || this.disabled;
  }
  /** @internal */
  _find(name) {
    return this.controls.hasOwnProperty(name) ? this.controls[name] : null;
  }
};
function validateFormGroupControls(controls) {
  const invalidKeys = Object.keys(controls).filter((key) => key.includes("."));
  if (invalidKeys.length > 0) {
    console.warn(`FormGroup keys cannot include \`.\`, please replace the keys for: ${invalidKeys.join(",")}.`);
  }
}
var UntypedFormGroup = FormGroup;
var FormRecord = class extends FormGroup {
};
var CALL_SET_DISABLED_STATE = new InjectionToken(typeof ngDevMode === "undefined" || ngDevMode ? "CallSetDisabledState" : "", {
  providedIn: "root",
  factory: () => setDisabledStateDefault
});
var setDisabledStateDefault = "always";
function controlPath(name, parent) {
  return [...parent.path, name];
}
function setUpControl(control, dir, callSetDisabledState = setDisabledStateDefault) {
  if (typeof ngDevMode === "undefined" || ngDevMode) {
    if (!control) _throwError(dir, "Cannot find control with");
    if (!dir.valueAccessor) _throwMissingValueAccessorError(dir);
  }
  setUpValidators(control, dir);
  dir.valueAccessor.writeValue(control.value);
  if (control.disabled || callSetDisabledState === "always") {
    dir.valueAccessor.setDisabledState?.(control.disabled);
  }
  setUpViewChangePipeline(control, dir);
  setUpModelChangePipeline(control, dir);
  setUpBlurPipeline(control, dir);
  setUpDisabledChangeHandler(control, dir);
}
function cleanUpControl(control, dir, validateControlPresenceOnChange = true) {
  const noop = () => {
    if (validateControlPresenceOnChange && (typeof ngDevMode === "undefined" || ngDevMode)) {
      _noControlError(dir);
    }
  };
  if (dir.valueAccessor) {
    dir.valueAccessor.registerOnChange(noop);
    dir.valueAccessor.registerOnTouched(noop);
  }
  cleanUpValidators(control, dir);
  if (control) {
    dir._invokeOnDestroyCallbacks();
    control._registerOnCollectionChange(() => {
    });
  }
}
function registerOnValidatorChange(validators, onChange) {
  validators.forEach((validator) => {
    if (validator.registerOnValidatorChange) validator.registerOnValidatorChange(onChange);
  });
}
function setUpDisabledChangeHandler(control, dir) {
  if (dir.valueAccessor.setDisabledState) {
    const onDisabledChange = (isDisabled) => {
      dir.valueAccessor.setDisabledState(isDisabled);
    };
    control.registerOnDisabledChange(onDisabledChange);
    dir._registerOnDestroy(() => {
      control._unregisterOnDisabledChange(onDisabledChange);
    });
  }
}
function setUpValidators(control, dir) {
  const validators = getControlValidators(control);
  if (dir.validator !== null) {
    control.setValidators(mergeValidators(validators, dir.validator));
  } else if (typeof validators === "function") {
    control.setValidators([validators]);
  }
  const asyncValidators = getControlAsyncValidators(control);
  if (dir.asyncValidator !== null) {
    control.setAsyncValidators(mergeValidators(asyncValidators, dir.asyncValidator));
  } else if (typeof asyncValidators === "function") {
    control.setAsyncValidators([asyncValidators]);
  }
  const onValidatorChange = () => control.updateValueAndValidity();
  registerOnValidatorChange(dir._rawValidators, onValidatorChange);
  registerOnValidatorChange(dir._rawAsyncValidators, onValidatorChange);
}
function cleanUpValidators(control, dir) {
  let isControlUpdated = false;
  if (control !== null) {
    if (dir.validator !== null) {
      const validators = getControlValidators(control);
      if (Array.isArray(validators) && validators.length > 0) {
        const updatedValidators = validators.filter((validator) => validator !== dir.validator);
        if (updatedValidators.length !== validators.length) {
          isControlUpdated = true;
          control.setValidators(updatedValidators);
        }
      }
    }
    if (dir.asyncValidator !== null) {
      const asyncValidators = getControlAsyncValidators(control);
      if (Array.isArray(asyncValidators) && asyncValidators.length > 0) {
        const updatedAsyncValidators = asyncValidators.filter((asyncValidator) => asyncValidator !== dir.asyncValidator);
        if (updatedAsyncValidators.length !== asyncValidators.length) {
          isControlUpdated = true;
          control.setAsyncValidators(updatedAsyncValidators);
        }
      }
    }
  }
  const noop = () => {
  };
  registerOnValidatorChange(dir._rawValidators, noop);
  registerOnValidatorChange(dir._rawAsyncValidators, noop);
  return isControlUpdated;
}
function setUpViewChangePipeline(control, dir) {
  dir.valueAccessor.registerOnChange((newValue) => {
    control._pendingValue = newValue;
    control._pendingChange = true;
    control._pendingDirty = true;
    if (control.updateOn === "change") updateControl(control, dir);
  });
}
function setUpBlurPipeline(control, dir) {
  dir.valueAccessor.registerOnTouched(() => {
    control._pendingTouched = true;
    if (control.updateOn === "blur" && control._pendingChange) updateControl(control, dir);
    if (control.updateOn !== "submit") control.markAsTouched();
  });
}
function updateControl(control, dir) {
  if (control._pendingDirty) control.markAsDirty();
  control.setValue(control._pendingValue, {
    emitModelToViewChange: false
  });
  dir.viewToModelUpdate(control._pendingValue);
  control._pendingChange = false;
}
function setUpModelChangePipeline(control, dir) {
  const onChange = (newValue, emitModelEvent) => {
    dir.valueAccessor.writeValue(newValue);
    if (emitModelEvent) dir.viewToModelUpdate(newValue);
  };
  control.registerOnChange(onChange);
  dir._registerOnDestroy(() => {
    control._unregisterOnChange(onChange);
  });
}
function setUpFormContainer(control, dir) {
  if (control == null && (typeof ngDevMode === "undefined" || ngDevMode)) _throwError(dir, "Cannot find control with");
  setUpValidators(control, dir);
}
function cleanUpFormContainer(control, dir) {
  return cleanUpValidators(control, dir);
}
function _noControlError(dir) {
  return _throwError(dir, "There is no FormControl instance attached to form control element with");
}
function _throwError(dir, message) {
  const messageEnd = _describeControlLocation(dir);
  throw new Error(`${message} ${messageEnd}`);
}
function _describeControlLocation(dir) {
  const path = dir.path;
  if (path && path.length > 1) return `path: '${path.join(" -> ")}'`;
  if (path?.[0]) return `name: '${path}'`;
  return "unspecified name attribute";
}
function _throwMissingValueAccessorError(dir) {
  const loc = _describeControlLocation(dir);
  throw new RuntimeError(-1203, `No value accessor for form control ${loc}.`);
}
function _throwInvalidValueAccessorError(dir) {
  const loc = _describeControlLocation(dir);
  throw new RuntimeError(1200, `Value accessor was not provided as an array for form control with ${loc}. Check that the \`NG_VALUE_ACCESSOR\` token is configured as a \`multi: true\` provider.`);
}
function isPropertyUpdated(changes, viewModel) {
  if (!changes.hasOwnProperty("model")) return false;
  const change = changes["model"];
  if (change.isFirstChange()) return true;
  return !Object.is(viewModel, change.currentValue);
}
function isBuiltInAccessor(valueAccessor) {
  return Object.getPrototypeOf(valueAccessor.constructor) === BuiltInControlValueAccessor;
}
function syncPendingControls(form, directives2) {
  form._syncPendingControls();
  directives2.forEach((dir) => {
    const control = dir.control;
    if (control.updateOn === "submit" && control._pendingChange) {
      dir.viewToModelUpdate(control._pendingValue);
      control._pendingChange = false;
    }
  });
}
function selectValueAccessor(dir, valueAccessors) {
  if (!valueAccessors) return null;
  if (!Array.isArray(valueAccessors) && (typeof ngDevMode === "undefined" || ngDevMode)) _throwInvalidValueAccessorError(dir);
  let defaultAccessor = void 0;
  let builtinAccessor = void 0;
  let customAccessor = void 0;
  valueAccessors.forEach((v) => {
    if (v.constructor === DefaultValueAccessor) {
      defaultAccessor = v;
    } else if (isBuiltInAccessor(v)) {
      if (builtinAccessor && (typeof ngDevMode === "undefined" || ngDevMode)) _throwError(dir, "More than one built-in value accessor matches form control with");
      builtinAccessor = v;
    } else {
      if (customAccessor && (typeof ngDevMode === "undefined" || ngDevMode)) _throwError(dir, "More than one custom value accessor matches form control with");
      customAccessor = v;
    }
  });
  if (customAccessor) return customAccessor;
  if (builtinAccessor) return builtinAccessor;
  if (defaultAccessor) return defaultAccessor;
  if (typeof ngDevMode === "undefined" || ngDevMode) {
    _throwError(dir, "No valid value accessor for form control with");
  }
  return null;
}
function removeListItem$1(list, el) {
  const index = list.indexOf(el);
  if (index > -1) list.splice(index, 1);
}
function _ngModelWarning(name, type, instance, warningConfig) {
  if (warningConfig === "never") return;
  if ((warningConfig === null || warningConfig === "once") && !type._ngModelWarningSentOnce || warningConfig === "always" && !instance._ngModelWarningSent) {
    console.warn(ngModelWarning(name));
    type._ngModelWarningSentOnce = true;
    instance._ngModelWarningSent = true;
  }
}
var formDirectiveProvider$1 = {
  provide: ControlContainer,
  useExisting: forwardRef(() => NgForm)
};
var resolvedPromise$1 = (() => Promise.resolve())();
var NgForm = class _NgForm extends ControlContainer {
  callSetDisabledState;
  /**
   * @description
   * Returns whether the form submission has been triggered.
   */
  get submitted() {
    return untracked(this.submittedReactive);
  }
  /** @internal */
  _submitted = computed(() => this.submittedReactive());
  submittedReactive = signal(false);
  _directives = /* @__PURE__ */ new Set();
  /**
   * @description
   * The `FormGroup` instance created for this form.
   */
  form;
  /**
   * @description
   * Event emitter for the "ngSubmit" event
   */
  ngSubmit = new EventEmitter();
  /**
   * @description
   * Tracks options for the `NgForm` instance.
   *
   * **updateOn**: Sets the default `updateOn` value for all child `NgModels` below it
   * unless explicitly set by a child `NgModel` using `ngModelOptions`). Defaults to 'change'.
   * Possible values: `'change'` | `'blur'` | `'submit'`.
   *
   */
  options;
  constructor(validators, asyncValidators, callSetDisabledState) {
    super();
    this.callSetDisabledState = callSetDisabledState;
    this.form = new FormGroup({}, composeValidators(validators), composeAsyncValidators(asyncValidators));
  }
  /** @docs-private */
  ngAfterViewInit() {
    this._setUpdateStrategy();
  }
  /**
   * @description
   * The directive instance.
   */
  get formDirective() {
    return this;
  }
  /**
   * @description
   * The internal `FormGroup` instance.
   */
  get control() {
    return this.form;
  }
  /**
   * @description
   * Returns an array representing the path to this group. Because this directive
   * always lives at the top level of a form, it is always an empty array.
   */
  get path() {
    return [];
  }
  /**
   * @description
   * Returns a map of the controls in this group.
   */
  get controls() {
    return this.form.controls;
  }
  /**
   * @description
   * Method that sets up the control directive in this group, re-calculates its value
   * and validity, and adds the instance to the internal list of directives.
   *
   * @param dir The `NgModel` directive instance.
   */
  addControl(dir) {
    resolvedPromise$1.then(() => {
      const container = this._findContainer(dir.path);
      dir.control = container.registerControl(dir.name, dir.control);
      setUpControl(dir.control, dir, this.callSetDisabledState);
      dir.control.updateValueAndValidity({
        emitEvent: false
      });
      this._directives.add(dir);
    });
  }
  /**
   * @description
   * Retrieves the `FormControl` instance from the provided `NgModel` directive.
   *
   * @param dir The `NgModel` directive instance.
   */
  getControl(dir) {
    return this.form.get(dir.path);
  }
  /**
   * @description
   * Removes the `NgModel` instance from the internal list of directives
   *
   * @param dir The `NgModel` directive instance.
   */
  removeControl(dir) {
    resolvedPromise$1.then(() => {
      const container = this._findContainer(dir.path);
      if (container) {
        container.removeControl(dir.name);
      }
      this._directives.delete(dir);
    });
  }
  /**
   * @description
   * Adds a new `NgModelGroup` directive instance to the form.
   *
   * @param dir The `NgModelGroup` directive instance.
   */
  addFormGroup(dir) {
    resolvedPromise$1.then(() => {
      const container = this._findContainer(dir.path);
      const group = new FormGroup({});
      setUpFormContainer(group, dir);
      container.registerControl(dir.name, group);
      group.updateValueAndValidity({
        emitEvent: false
      });
    });
  }
  /**
   * @description
   * Removes the `NgModelGroup` directive instance from the form.
   *
   * @param dir The `NgModelGroup` directive instance.
   */
  removeFormGroup(dir) {
    resolvedPromise$1.then(() => {
      const container = this._findContainer(dir.path);
      if (container) {
        container.removeControl(dir.name);
      }
    });
  }
  /**
   * @description
   * Retrieves the `FormGroup` for a provided `NgModelGroup` directive instance
   *
   * @param dir The `NgModelGroup` directive instance.
   */
  getFormGroup(dir) {
    return this.form.get(dir.path);
  }
  /**
   * Sets the new value for the provided `NgControl` directive.
   *
   * @param dir The `NgControl` directive instance.
   * @param value The new value for the directive's control.
   */
  updateModel(dir, value) {
    resolvedPromise$1.then(() => {
      const ctrl = this.form.get(dir.path);
      ctrl.setValue(value);
    });
  }
  /**
   * @description
   * Sets the value for this `FormGroup`.
   *
   * @param value The new value
   */
  setValue(value) {
    this.control.setValue(value);
  }
  /**
   * @description
   * Method called when the "submit" event is triggered on the form.
   * Triggers the `ngSubmit` emitter to emit the "submit" event as its payload.
   *
   * @param $event The "submit" event object
   */
  onSubmit($event) {
    this.submittedReactive.set(true);
    syncPendingControls(this.form, this._directives);
    this.ngSubmit.emit($event);
    this.form._events.next(new FormSubmittedEvent(this.control));
    return $event?.target?.method === "dialog";
  }
  /**
   * @description
   * Method called when the "reset" event is triggered on the form.
   */
  onReset() {
    this.resetForm();
  }
  /**
   * @description
   * Resets the form to an initial value and resets its submitted status.
   *
   * @param value The new value for the form.
   */
  resetForm(value = void 0) {
    this.form.reset(value);
    this.submittedReactive.set(false);
    this.form._events.next(new FormResetEvent(this.form));
  }
  _setUpdateStrategy() {
    if (this.options && this.options.updateOn != null) {
      this.form._updateOn = this.options.updateOn;
    }
  }
  _findContainer(path) {
    path.pop();
    return path.length ? this.form.get(path) : this.form;
  }
  static ɵfac = function NgForm_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _NgForm)(ɵɵdirectiveInject(NG_VALIDATORS, 10), ɵɵdirectiveInject(NG_ASYNC_VALIDATORS, 10), ɵɵdirectiveInject(CALL_SET_DISABLED_STATE, 8));
  };
  static ɵdir = ɵɵdefineDirective({
    type: _NgForm,
    selectors: [["form", 3, "ngNoForm", "", 3, "formGroup", ""], ["ng-form"], ["", "ngForm", ""]],
    hostBindings: function NgForm_HostBindings(rf, ctx) {
      if (rf & 1) {
        ɵɵlistener("submit", function NgForm_submit_HostBindingHandler($event) {
          return ctx.onSubmit($event);
        })("reset", function NgForm_reset_HostBindingHandler() {
          return ctx.onReset();
        });
      }
    },
    inputs: {
      options: [0, "ngFormOptions", "options"]
    },
    outputs: {
      ngSubmit: "ngSubmit"
    },
    exportAs: ["ngForm"],
    standalone: false,
    features: [ɵɵProvidersFeature([formDirectiveProvider$1]), ɵɵInheritDefinitionFeature]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(NgForm, [{
    type: Directive,
    args: [{
      selector: "form:not([ngNoForm]):not([formGroup]),ng-form,[ngForm]",
      providers: [formDirectiveProvider$1],
      host: {
        "(submit)": "onSubmit($event)",
        "(reset)": "onReset()"
      },
      outputs: ["ngSubmit"],
      exportAs: "ngForm",
      standalone: false
    }]
  }], () => [{
    type: void 0,
    decorators: [{
      type: Optional
    }, {
      type: Self
    }, {
      type: Inject,
      args: [NG_VALIDATORS]
    }]
  }, {
    type: void 0,
    decorators: [{
      type: Optional
    }, {
      type: Self
    }, {
      type: Inject,
      args: [NG_ASYNC_VALIDATORS]
    }]
  }, {
    type: void 0,
    decorators: [{
      type: Optional
    }, {
      type: Inject,
      args: [CALL_SET_DISABLED_STATE]
    }]
  }], {
    options: [{
      type: Input,
      args: ["ngFormOptions"]
    }]
  });
})();
function removeListItem(list, el) {
  const index = list.indexOf(el);
  if (index > -1) list.splice(index, 1);
}
function isFormControlState(formState) {
  return typeof formState === "object" && formState !== null && Object.keys(formState).length === 2 && "value" in formState && "disabled" in formState;
}
var FormControl = class FormControl2 extends AbstractControl {
  /** @publicApi */
  defaultValue = null;
  /** @internal */
  _onChange = [];
  /** @internal */
  _pendingValue;
  /** @internal */
  _pendingChange = false;
  constructor(formState = null, validatorOrOpts, asyncValidator) {
    super(pickValidators(validatorOrOpts), pickAsyncValidators(asyncValidator, validatorOrOpts));
    this._applyFormState(formState);
    this._setUpdateStrategy(validatorOrOpts);
    this._initObservables();
    this.updateValueAndValidity({
      onlySelf: true,
      // If `asyncValidator` is present, it will trigger control status change from `PENDING` to
      // `VALID` or `INVALID`.
      // The status should be broadcasted via the `statusChanges` observable, so we set
      // `emitEvent` to `true` to allow that during the control creation process.
      emitEvent: !!this.asyncValidator
    });
    if (isOptionsObj(validatorOrOpts) && (validatorOrOpts.nonNullable || validatorOrOpts.initialValueIsDefault)) {
      if (isFormControlState(formState)) {
        this.defaultValue = formState.value;
      } else {
        this.defaultValue = formState;
      }
    }
  }
  setValue(value, options = {}) {
    this.value = this._pendingValue = value;
    if (this._onChange.length && options.emitModelToViewChange !== false) {
      this._onChange.forEach((changeFn) => changeFn(this.value, options.emitViewToModelChange !== false));
    }
    this.updateValueAndValidity(options);
  }
  patchValue(value, options = {}) {
    this.setValue(value, options);
  }
  reset(formState = this.defaultValue, options = {}) {
    this._applyFormState(formState);
    this.markAsPristine(options);
    this.markAsUntouched(options);
    this.setValue(this.value, options);
    this._pendingChange = false;
  }
  /**  @internal */
  _updateValue() {
  }
  /**  @internal */
  _anyControls(condition) {
    return false;
  }
  /**  @internal */
  _allControlsDisabled() {
    return this.disabled;
  }
  registerOnChange(fn) {
    this._onChange.push(fn);
  }
  /** @internal */
  _unregisterOnChange(fn) {
    removeListItem(this._onChange, fn);
  }
  registerOnDisabledChange(fn) {
    this._onDisabledChange.push(fn);
  }
  /** @internal */
  _unregisterOnDisabledChange(fn) {
    removeListItem(this._onDisabledChange, fn);
  }
  /** @internal */
  _forEachChild(cb) {
  }
  /** @internal */
  _syncPendingControls() {
    if (this.updateOn === "submit") {
      if (this._pendingDirty) this.markAsDirty();
      if (this._pendingTouched) this.markAsTouched();
      if (this._pendingChange) {
        this.setValue(this._pendingValue, {
          onlySelf: true,
          emitModelToViewChange: false
        });
        return true;
      }
    }
    return false;
  }
  _applyFormState(formState) {
    if (isFormControlState(formState)) {
      this.value = this._pendingValue = formState.value;
      formState.disabled ? this.disable({
        onlySelf: true,
        emitEvent: false
      }) : this.enable({
        onlySelf: true,
        emitEvent: false
      });
    } else {
      this.value = this._pendingValue = formState;
    }
  }
};
var UntypedFormControl = FormControl;
var isFormControl = (control) => control instanceof FormControl;
var AbstractFormGroupDirective = class _AbstractFormGroupDirective extends ControlContainer {
  /**
   * @description
   * The parent control for the group
   *
   * @internal
   */
  _parent;
  /** @docs-private */
  ngOnInit() {
    this._checkParentType();
    this.formDirective.addFormGroup(this);
  }
  /** @docs-private */
  ngOnDestroy() {
    if (this.formDirective) {
      this.formDirective.removeFormGroup(this);
    }
  }
  /**
   * @description
   * The `FormGroup` bound to this directive.
   */
  get control() {
    return this.formDirective.getFormGroup(this);
  }
  /**
   * @description
   * The path to this group from the top-level directive.
   */
  get path() {
    return controlPath(this.name == null ? this.name : this.name.toString(), this._parent);
  }
  /**
   * @description
   * The top-level directive for this group if present, otherwise null.
   */
  get formDirective() {
    return this._parent ? this._parent.formDirective : null;
  }
  /** @internal */
  _checkParentType() {
  }
  static ɵfac = /* @__PURE__ */ (() => {
    let ɵAbstractFormGroupDirective_BaseFactory;
    return function AbstractFormGroupDirective_Factory(__ngFactoryType__) {
      return (ɵAbstractFormGroupDirective_BaseFactory || (ɵAbstractFormGroupDirective_BaseFactory = ɵɵgetInheritedFactory(_AbstractFormGroupDirective)))(__ngFactoryType__ || _AbstractFormGroupDirective);
    };
  })();
  static ɵdir = ɵɵdefineDirective({
    type: _AbstractFormGroupDirective,
    standalone: false,
    features: [ɵɵInheritDefinitionFeature]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(AbstractFormGroupDirective, [{
    type: Directive,
    args: [{
      standalone: false
    }]
  }], null, null);
})();
function modelParentException() {
  return new RuntimeError(1350, `
    ngModel cannot be used to register form controls with a parent formGroup directive.  Try using
    formGroup's partner directive "formControlName" instead.  Example:

    ${formControlNameExample}

    Or, if you'd like to avoid registering this form control, indicate that it's standalone in ngModelOptions:

    Example:

    ${ngModelWithFormGroupExample}`);
}
function formGroupNameException() {
  return new RuntimeError(1351, `
    ngModel cannot be used to register form controls with a parent formGroupName or formArrayName directive.

    Option 1: Use formControlName instead of ngModel (reactive strategy):

    ${formGroupNameExample}

    Option 2:  Update ngModel's parent be ngModelGroup (template-driven strategy):

    ${ngModelGroupExample}`);
}
function missingNameException() {
  return new RuntimeError(1352, `If ngModel is used within a form tag, either the name attribute must be set or the form
    control must be defined as 'standalone' in ngModelOptions.

    Example 1: <input [(ngModel)]="person.firstName" name="first">
    Example 2: <input [(ngModel)]="person.firstName" [ngModelOptions]="{standalone: true}">`);
}
function modelGroupParentException() {
  return new RuntimeError(1353, `
    ngModelGroup cannot be used with a parent formGroup directive.

    Option 1: Use formGroupName instead of ngModelGroup (reactive strategy):

    ${formGroupNameExample}

    Option 2:  Use a regular form tag instead of the formGroup directive (template-driven strategy):

    ${ngModelGroupExample}`);
}
var modelGroupProvider = {
  provide: ControlContainer,
  useExisting: forwardRef(() => NgModelGroup)
};
var NgModelGroup = class _NgModelGroup extends AbstractFormGroupDirective {
  /**
   * @description
   * Tracks the name of the `NgModelGroup` bound to the directive. The name corresponds
   * to a key in the parent `NgForm`.
   */
  name = "";
  constructor(parent, validators, asyncValidators) {
    super();
    this._parent = parent;
    this._setValidators(validators);
    this._setAsyncValidators(asyncValidators);
  }
  /** @internal */
  _checkParentType() {
    if (!(this._parent instanceof _NgModelGroup) && !(this._parent instanceof NgForm) && (typeof ngDevMode === "undefined" || ngDevMode)) {
      throw modelGroupParentException();
    }
  }
  static ɵfac = function NgModelGroup_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _NgModelGroup)(ɵɵdirectiveInject(ControlContainer, 5), ɵɵdirectiveInject(NG_VALIDATORS, 10), ɵɵdirectiveInject(NG_ASYNC_VALIDATORS, 10));
  };
  static ɵdir = ɵɵdefineDirective({
    type: _NgModelGroup,
    selectors: [["", "ngModelGroup", ""]],
    inputs: {
      name: [0, "ngModelGroup", "name"]
    },
    exportAs: ["ngModelGroup"],
    standalone: false,
    features: [ɵɵProvidersFeature([modelGroupProvider]), ɵɵInheritDefinitionFeature]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(NgModelGroup, [{
    type: Directive,
    args: [{
      selector: "[ngModelGroup]",
      providers: [modelGroupProvider],
      exportAs: "ngModelGroup",
      standalone: false
    }]
  }], () => [{
    type: ControlContainer,
    decorators: [{
      type: Host
    }, {
      type: SkipSelf
    }]
  }, {
    type: void 0,
    decorators: [{
      type: Optional
    }, {
      type: Self
    }, {
      type: Inject,
      args: [NG_VALIDATORS]
    }]
  }, {
    type: void 0,
    decorators: [{
      type: Optional
    }, {
      type: Self
    }, {
      type: Inject,
      args: [NG_ASYNC_VALIDATORS]
    }]
  }], {
    name: [{
      type: Input,
      args: ["ngModelGroup"]
    }]
  });
})();
var formControlBinding$1 = {
  provide: NgControl,
  useExisting: forwardRef(() => NgModel)
};
var resolvedPromise = (() => Promise.resolve())();
var NgModel = class _NgModel extends NgControl {
  _changeDetectorRef;
  callSetDisabledState;
  control = new FormControl();
  // At runtime we coerce arbitrary values assigned to the "disabled" input to a "boolean".
  // This is not reflected in the type of the property because outside of templates, consumers
  // should only deal with booleans. In templates, a string is allowed for convenience and to
  // match the native "disabled attribute" semantics which can be observed on input elements.
  // This static member tells the compiler that values of type "string" can also be assigned
  // to the input in a template.
  /** @docs-private */
  static ngAcceptInputType_isDisabled;
  /** @internal */
  _registered = false;
  /**
   * Internal reference to the view model value.
   * @docs-private
   */
  viewModel;
  /**
   * @description
   * Tracks the name bound to the directive. If a parent form exists, it
   * uses this name as a key to retrieve this control's value.
   */
  name = "";
  /**
   * @description
   * Tracks whether the control is disabled.
   */
  isDisabled;
  /**
   * @description
   * Tracks the value bound to this directive.
   */
  model;
  /**
   * @description
   * Tracks the configuration options for this `ngModel` instance.
   *
   * **name**: An alternative to setting the name attribute on the form control element. See
   * the [example](api/forms/NgModel#using-ngmodel-on-a-standalone-control) for using `NgModel`
   * as a standalone control.
   *
   * **standalone**: When set to true, the `ngModel` will not register itself with its parent form,
   * and acts as if it's not in the form. Defaults to false. If no parent form exists, this option
   * has no effect.
   *
   * **updateOn**: Defines the event upon which the form control value and validity update.
   * Defaults to 'change'. Possible values: `'change'` | `'blur'` | `'submit'`.
   *
   */
  options;
  /**
   * @description
   * Event emitter for producing the `ngModelChange` event after
   * the view model updates.
   */
  update = new EventEmitter();
  constructor(parent, validators, asyncValidators, valueAccessors, _changeDetectorRef, callSetDisabledState) {
    super();
    this._changeDetectorRef = _changeDetectorRef;
    this.callSetDisabledState = callSetDisabledState;
    this._parent = parent;
    this._setValidators(validators);
    this._setAsyncValidators(asyncValidators);
    this.valueAccessor = selectValueAccessor(this, valueAccessors);
  }
  /** @docs-private */
  ngOnChanges(changes) {
    this._checkForErrors();
    if (!this._registered || "name" in changes) {
      if (this._registered) {
        this._checkName();
        if (this.formDirective) {
          const oldName = changes["name"].previousValue;
          this.formDirective.removeControl({
            name: oldName,
            path: this._getPath(oldName)
          });
        }
      }
      this._setUpControl();
    }
    if ("isDisabled" in changes) {
      this._updateDisabled(changes);
    }
    if (isPropertyUpdated(changes, this.viewModel)) {
      this._updateValue(this.model);
      this.viewModel = this.model;
    }
  }
  /** @docs-private */
  ngOnDestroy() {
    this.formDirective && this.formDirective.removeControl(this);
  }
  /**
   * @description
   * Returns an array that represents the path from the top-level form to this control.
   * Each index is the string name of the control on that level.
   */
  get path() {
    return this._getPath(this.name);
  }
  /**
   * @description
   * The top-level directive for this control if present, otherwise null.
   */
  get formDirective() {
    return this._parent ? this._parent.formDirective : null;
  }
  /**
   * @description
   * Sets the new value for the view model and emits an `ngModelChange` event.
   *
   * @param newValue The new value emitted by `ngModelChange`.
   */
  viewToModelUpdate(newValue) {
    this.viewModel = newValue;
    this.update.emit(newValue);
  }
  _setUpControl() {
    this._setUpdateStrategy();
    this._isStandalone() ? this._setUpStandalone() : this.formDirective.addControl(this);
    this._registered = true;
  }
  _setUpdateStrategy() {
    if (this.options && this.options.updateOn != null) {
      this.control._updateOn = this.options.updateOn;
    }
  }
  _isStandalone() {
    return !this._parent || !!(this.options && this.options.standalone);
  }
  _setUpStandalone() {
    setUpControl(this.control, this, this.callSetDisabledState);
    this.control.updateValueAndValidity({
      emitEvent: false
    });
  }
  _checkForErrors() {
    if ((typeof ngDevMode === "undefined" || ngDevMode) && !this._isStandalone()) {
      checkParentType$1(this._parent);
    }
    this._checkName();
  }
  _checkName() {
    if (this.options && this.options.name) this.name = this.options.name;
    if (!this._isStandalone() && !this.name && (typeof ngDevMode === "undefined" || ngDevMode)) {
      throw missingNameException();
    }
  }
  _updateValue(value) {
    resolvedPromise.then(() => {
      this.control.setValue(value, {
        emitViewToModelChange: false
      });
      this._changeDetectorRef?.markForCheck();
    });
  }
  _updateDisabled(changes) {
    const disabledValue = changes["isDisabled"].currentValue;
    const isDisabled = disabledValue !== 0 && booleanAttribute(disabledValue);
    resolvedPromise.then(() => {
      if (isDisabled && !this.control.disabled) {
        this.control.disable();
      } else if (!isDisabled && this.control.disabled) {
        this.control.enable();
      }
      this._changeDetectorRef?.markForCheck();
    });
  }
  _getPath(controlName) {
    return this._parent ? controlPath(controlName, this._parent) : [controlName];
  }
  static ɵfac = function NgModel_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _NgModel)(ɵɵdirectiveInject(ControlContainer, 9), ɵɵdirectiveInject(NG_VALIDATORS, 10), ɵɵdirectiveInject(NG_ASYNC_VALIDATORS, 10), ɵɵdirectiveInject(NG_VALUE_ACCESSOR, 10), ɵɵdirectiveInject(ChangeDetectorRef, 8), ɵɵdirectiveInject(CALL_SET_DISABLED_STATE, 8));
  };
  static ɵdir = ɵɵdefineDirective({
    type: _NgModel,
    selectors: [["", "ngModel", "", 3, "formControlName", "", 3, "formControl", ""]],
    inputs: {
      name: "name",
      isDisabled: [0, "disabled", "isDisabled"],
      model: [0, "ngModel", "model"],
      options: [0, "ngModelOptions", "options"]
    },
    outputs: {
      update: "ngModelChange"
    },
    exportAs: ["ngModel"],
    standalone: false,
    features: [ɵɵProvidersFeature([formControlBinding$1]), ɵɵInheritDefinitionFeature, ɵɵNgOnChangesFeature]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(NgModel, [{
    type: Directive,
    args: [{
      selector: "[ngModel]:not([formControlName]):not([formControl])",
      providers: [formControlBinding$1],
      exportAs: "ngModel",
      standalone: false
    }]
  }], () => [{
    type: ControlContainer,
    decorators: [{
      type: Optional
    }, {
      type: Host
    }]
  }, {
    type: void 0,
    decorators: [{
      type: Optional
    }, {
      type: Self
    }, {
      type: Inject,
      args: [NG_VALIDATORS]
    }]
  }, {
    type: void 0,
    decorators: [{
      type: Optional
    }, {
      type: Self
    }, {
      type: Inject,
      args: [NG_ASYNC_VALIDATORS]
    }]
  }, {
    type: void 0,
    decorators: [{
      type: Optional
    }, {
      type: Self
    }, {
      type: Inject,
      args: [NG_VALUE_ACCESSOR]
    }]
  }, {
    type: ChangeDetectorRef,
    decorators: [{
      type: Optional
    }, {
      type: Inject,
      args: [ChangeDetectorRef]
    }]
  }, {
    type: void 0,
    decorators: [{
      type: Optional
    }, {
      type: Inject,
      args: [CALL_SET_DISABLED_STATE]
    }]
  }], {
    name: [{
      type: Input
    }],
    isDisabled: [{
      type: Input,
      args: ["disabled"]
    }],
    model: [{
      type: Input,
      args: ["ngModel"]
    }],
    options: [{
      type: Input,
      args: ["ngModelOptions"]
    }],
    update: [{
      type: Output,
      args: ["ngModelChange"]
    }]
  });
})();
function checkParentType$1(parent) {
  if (!(parent instanceof NgModelGroup) && parent instanceof AbstractFormGroupDirective) {
    throw formGroupNameException();
  } else if (!(parent instanceof NgModelGroup) && !(parent instanceof NgForm)) {
    throw modelParentException();
  }
}
var ɵNgNoValidate = class _ɵNgNoValidate {
  static ɵfac = function ɵNgNoValidate_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _ɵNgNoValidate)();
  };
  static ɵdir = ɵɵdefineDirective({
    type: _ɵNgNoValidate,
    selectors: [["form", 3, "ngNoForm", "", 3, "ngNativeValidate", ""]],
    hostAttrs: ["novalidate", ""],
    standalone: false
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(ɵNgNoValidate, [{
    type: Directive,
    args: [{
      selector: "form:not([ngNoForm]):not([ngNativeValidate])",
      host: {
        "novalidate": ""
      },
      standalone: false
    }]
  }], null, null);
})();
var NUMBER_VALUE_ACCESSOR = {
  provide: NG_VALUE_ACCESSOR,
  useExisting: forwardRef(() => NumberValueAccessor),
  multi: true
};
var NumberValueAccessor = class _NumberValueAccessor extends BuiltInControlValueAccessor {
  /**
   * Sets the "value" property on the input element.
   * @docs-private
   */
  writeValue(value) {
    const normalizedValue = value == null ? "" : value;
    this.setProperty("value", normalizedValue);
  }
  /**
   * Registers a function called when the control value changes.
   * @docs-private
   */
  registerOnChange(fn) {
    this.onChange = (value) => {
      fn(value == "" ? null : parseFloat(value));
    };
  }
  static ɵfac = /* @__PURE__ */ (() => {
    let ɵNumberValueAccessor_BaseFactory;
    return function NumberValueAccessor_Factory(__ngFactoryType__) {
      return (ɵNumberValueAccessor_BaseFactory || (ɵNumberValueAccessor_BaseFactory = ɵɵgetInheritedFactory(_NumberValueAccessor)))(__ngFactoryType__ || _NumberValueAccessor);
    };
  })();
  static ɵdir = ɵɵdefineDirective({
    type: _NumberValueAccessor,
    selectors: [["input", "type", "number", "formControlName", ""], ["input", "type", "number", "formControl", ""], ["input", "type", "number", "ngModel", ""]],
    hostBindings: function NumberValueAccessor_HostBindings(rf, ctx) {
      if (rf & 1) {
        ɵɵlistener("input", function NumberValueAccessor_input_HostBindingHandler($event) {
          return ctx.onChange($event.target.value);
        })("blur", function NumberValueAccessor_blur_HostBindingHandler() {
          return ctx.onTouched();
        });
      }
    },
    standalone: false,
    features: [ɵɵProvidersFeature([NUMBER_VALUE_ACCESSOR]), ɵɵInheritDefinitionFeature]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(NumberValueAccessor, [{
    type: Directive,
    args: [{
      selector: "input[type=number][formControlName],input[type=number][formControl],input[type=number][ngModel]",
      host: {
        "(input)": "onChange($event.target.value)",
        "(blur)": "onTouched()"
      },
      providers: [NUMBER_VALUE_ACCESSOR],
      standalone: false
    }]
  }], null, null);
})();
var RADIO_VALUE_ACCESSOR = {
  provide: NG_VALUE_ACCESSOR,
  useExisting: forwardRef(() => RadioControlValueAccessor),
  multi: true
};
function throwNameError() {
  throw new RuntimeError(1202, `
      If you define both a name and a formControlName attribute on your radio button, their values
      must match. Ex: <input type="radio" formControlName="food" name="food">
    `);
}
var RadioControlRegistry = class _RadioControlRegistry {
  _accessors = [];
  /**
   * @description
   * Adds a control to the internal registry. For internal use only.
   */
  add(control, accessor) {
    this._accessors.push([control, accessor]);
  }
  /**
   * @description
   * Removes a control from the internal registry. For internal use only.
   */
  remove(accessor) {
    for (let i = this._accessors.length - 1; i >= 0; --i) {
      if (this._accessors[i][1] === accessor) {
        this._accessors.splice(i, 1);
        return;
      }
    }
  }
  /**
   * @description
   * Selects a radio button. For internal use only.
   */
  select(accessor) {
    this._accessors.forEach((c) => {
      if (this._isSameGroup(c, accessor) && c[1] !== accessor) {
        c[1].fireUncheck(accessor.value);
      }
    });
  }
  _isSameGroup(controlPair, accessor) {
    if (!controlPair[0].control) return false;
    return controlPair[0]._parent === accessor._control._parent && controlPair[1].name === accessor.name;
  }
  static ɵfac = function RadioControlRegistry_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _RadioControlRegistry)();
  };
  static ɵprov = ɵɵdefineInjectable({
    token: _RadioControlRegistry,
    factory: _RadioControlRegistry.ɵfac,
    providedIn: "root"
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(RadioControlRegistry, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], null, null);
})();
var RadioControlValueAccessor = class _RadioControlValueAccessor extends BuiltInControlValueAccessor {
  _registry;
  _injector;
  /** @internal */
  _state;
  /** @internal */
  _control;
  /** @internal */
  _fn;
  setDisabledStateFired = false;
  /**
   * The registered callback function called when a change event occurs on the input element.
   * Note: we declare `onChange` here (also used as host listener) as a function with no arguments
   * to override the `onChange` function (which expects 1 argument) in the parent
   * `BaseControlValueAccessor` class.
   * @docs-private
   */
  onChange = () => {
  };
  /**
   * @description
   * Tracks the name of the radio input element.
   */
  name;
  /**
   * @description
   * Tracks the name of the `FormControl` bound to the directive. The name corresponds
   * to a key in the parent `FormGroup` or `FormArray`.
   */
  formControlName;
  /**
   * @description
   * Tracks the value of the radio input element
   */
  value;
  callSetDisabledState = inject(CALL_SET_DISABLED_STATE, {
    optional: true
  }) ?? setDisabledStateDefault;
  constructor(renderer, elementRef, _registry, _injector) {
    super(renderer, elementRef);
    this._registry = _registry;
    this._injector = _injector;
  }
  /** @docs-private */
  ngOnInit() {
    this._control = this._injector.get(NgControl);
    this._checkName();
    this._registry.add(this._control, this);
  }
  /** @docs-private */
  ngOnDestroy() {
    this._registry.remove(this);
  }
  /**
   * Sets the "checked" property value on the radio input element.
   * @docs-private
   */
  writeValue(value) {
    this._state = value === this.value;
    this.setProperty("checked", this._state);
  }
  /**
   * Registers a function called when the control value changes.
   * @docs-private
   */
  registerOnChange(fn) {
    this._fn = fn;
    this.onChange = () => {
      fn(this.value);
      this._registry.select(this);
    };
  }
  /** @docs-private */
  setDisabledState(isDisabled) {
    if (this.setDisabledStateFired || isDisabled || this.callSetDisabledState === "whenDisabledForLegacyCode") {
      this.setProperty("disabled", isDisabled);
    }
    this.setDisabledStateFired = true;
  }
  /**
   * Sets the "value" on the radio input element and unchecks it.
   *
   * @param value
   */
  fireUncheck(value) {
    this.writeValue(value);
  }
  _checkName() {
    if (this.name && this.formControlName && this.name !== this.formControlName && (typeof ngDevMode === "undefined" || ngDevMode)) {
      throwNameError();
    }
    if (!this.name && this.formControlName) this.name = this.formControlName;
  }
  static ɵfac = function RadioControlValueAccessor_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _RadioControlValueAccessor)(ɵɵdirectiveInject(Renderer2), ɵɵdirectiveInject(ElementRef), ɵɵdirectiveInject(RadioControlRegistry), ɵɵdirectiveInject(Injector));
  };
  static ɵdir = ɵɵdefineDirective({
    type: _RadioControlValueAccessor,
    selectors: [["input", "type", "radio", "formControlName", ""], ["input", "type", "radio", "formControl", ""], ["input", "type", "radio", "ngModel", ""]],
    hostBindings: function RadioControlValueAccessor_HostBindings(rf, ctx) {
      if (rf & 1) {
        ɵɵlistener("change", function RadioControlValueAccessor_change_HostBindingHandler() {
          return ctx.onChange();
        })("blur", function RadioControlValueAccessor_blur_HostBindingHandler() {
          return ctx.onTouched();
        });
      }
    },
    inputs: {
      name: "name",
      formControlName: "formControlName",
      value: "value"
    },
    standalone: false,
    features: [ɵɵProvidersFeature([RADIO_VALUE_ACCESSOR]), ɵɵInheritDefinitionFeature]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(RadioControlValueAccessor, [{
    type: Directive,
    args: [{
      selector: "input[type=radio][formControlName],input[type=radio][formControl],input[type=radio][ngModel]",
      host: {
        "(change)": "onChange()",
        "(blur)": "onTouched()"
      },
      providers: [RADIO_VALUE_ACCESSOR],
      standalone: false
    }]
  }], () => [{
    type: Renderer2
  }, {
    type: ElementRef
  }, {
    type: RadioControlRegistry
  }, {
    type: Injector
  }], {
    name: [{
      type: Input
    }],
    formControlName: [{
      type: Input
    }],
    value: [{
      type: Input
    }]
  });
})();
var RANGE_VALUE_ACCESSOR = {
  provide: NG_VALUE_ACCESSOR,
  useExisting: forwardRef(() => RangeValueAccessor),
  multi: true
};
var RangeValueAccessor = class _RangeValueAccessor extends BuiltInControlValueAccessor {
  /**
   * Sets the "value" property on the input element.
   * @docs-private
   */
  writeValue(value) {
    this.setProperty("value", parseFloat(value));
  }
  /**
   * Registers a function called when the control value changes.
   * @docs-private
   */
  registerOnChange(fn) {
    this.onChange = (value) => {
      fn(value == "" ? null : parseFloat(value));
    };
  }
  static ɵfac = /* @__PURE__ */ (() => {
    let ɵRangeValueAccessor_BaseFactory;
    return function RangeValueAccessor_Factory(__ngFactoryType__) {
      return (ɵRangeValueAccessor_BaseFactory || (ɵRangeValueAccessor_BaseFactory = ɵɵgetInheritedFactory(_RangeValueAccessor)))(__ngFactoryType__ || _RangeValueAccessor);
    };
  })();
  static ɵdir = ɵɵdefineDirective({
    type: _RangeValueAccessor,
    selectors: [["input", "type", "range", "formControlName", ""], ["input", "type", "range", "formControl", ""], ["input", "type", "range", "ngModel", ""]],
    hostBindings: function RangeValueAccessor_HostBindings(rf, ctx) {
      if (rf & 1) {
        ɵɵlistener("change", function RangeValueAccessor_change_HostBindingHandler($event) {
          return ctx.onChange($event.target.value);
        })("input", function RangeValueAccessor_input_HostBindingHandler($event) {
          return ctx.onChange($event.target.value);
        })("blur", function RangeValueAccessor_blur_HostBindingHandler() {
          return ctx.onTouched();
        });
      }
    },
    standalone: false,
    features: [ɵɵProvidersFeature([RANGE_VALUE_ACCESSOR]), ɵɵInheritDefinitionFeature]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(RangeValueAccessor, [{
    type: Directive,
    args: [{
      selector: "input[type=range][formControlName],input[type=range][formControl],input[type=range][ngModel]",
      host: {
        "(change)": "onChange($event.target.value)",
        "(input)": "onChange($event.target.value)",
        "(blur)": "onTouched()"
      },
      providers: [RANGE_VALUE_ACCESSOR],
      standalone: false
    }]
  }], null, null);
})();
var NG_MODEL_WITH_FORM_CONTROL_WARNING = new InjectionToken(ngDevMode ? "NgModelWithFormControlWarning" : "");
var formControlBinding = {
  provide: NgControl,
  useExisting: forwardRef(() => FormControlDirective)
};
var FormControlDirective = class _FormControlDirective extends NgControl {
  _ngModelWarningConfig;
  callSetDisabledState;
  /**
   * Internal reference to the view model value.
   * @docs-private
   */
  viewModel;
  /**
   * @description
   * Tracks the `FormControl` instance bound to the directive.
   */
  form;
  /**
   * @description
   * Triggers a warning in dev mode that this input should not be used with reactive forms.
   */
  set isDisabled(isDisabled) {
    if (typeof ngDevMode === "undefined" || ngDevMode) {
      console.warn(disabledAttrWarning);
    }
  }
  // TODO(kara): remove next 4 properties once deprecation period is over
  /** @deprecated as of v6 */
  model;
  /** @deprecated as of v6 */
  update = new EventEmitter();
  /**
   * @description
   * Static property used to track whether any ngModel warnings have been sent across
   * all instances of FormControlDirective. Used to support warning config of "once".
   *
   * @internal
   */
  static _ngModelWarningSentOnce = false;
  /**
   * @description
   * Instance property used to track whether an ngModel warning has been sent out for this
   * particular `FormControlDirective` instance. Used to support warning config of "always".
   *
   * @internal
   */
  _ngModelWarningSent = false;
  constructor(validators, asyncValidators, valueAccessors, _ngModelWarningConfig, callSetDisabledState) {
    super();
    this._ngModelWarningConfig = _ngModelWarningConfig;
    this.callSetDisabledState = callSetDisabledState;
    this._setValidators(validators);
    this._setAsyncValidators(asyncValidators);
    this.valueAccessor = selectValueAccessor(this, valueAccessors);
  }
  /** @docs-private */
  ngOnChanges(changes) {
    if (this._isControlChanged(changes)) {
      const previousForm = changes["form"].previousValue;
      if (previousForm) {
        cleanUpControl(
          previousForm,
          this,
          /* validateControlPresenceOnChange */
          false
        );
      }
      setUpControl(this.form, this, this.callSetDisabledState);
      this.form.updateValueAndValidity({
        emitEvent: false
      });
    }
    if (isPropertyUpdated(changes, this.viewModel)) {
      if (typeof ngDevMode === "undefined" || ngDevMode) {
        _ngModelWarning("formControl", _FormControlDirective, this, this._ngModelWarningConfig);
      }
      this.form.setValue(this.model);
      this.viewModel = this.model;
    }
  }
  /** @docs-private */
  ngOnDestroy() {
    if (this.form) {
      cleanUpControl(
        this.form,
        this,
        /* validateControlPresenceOnChange */
        false
      );
    }
  }
  /**
   * @description
   * Returns an array that represents the path from the top-level form to this control.
   * Each index is the string name of the control on that level.
   */
  get path() {
    return [];
  }
  /**
   * @description
   * The `FormControl` bound to this directive.
   */
  get control() {
    return this.form;
  }
  /**
   * @description
   * Sets the new value for the view model and emits an `ngModelChange` event.
   *
   * @param newValue The new value for the view model.
   */
  viewToModelUpdate(newValue) {
    this.viewModel = newValue;
    this.update.emit(newValue);
  }
  _isControlChanged(changes) {
    return changes.hasOwnProperty("form");
  }
  static ɵfac = function FormControlDirective_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _FormControlDirective)(ɵɵdirectiveInject(NG_VALIDATORS, 10), ɵɵdirectiveInject(NG_ASYNC_VALIDATORS, 10), ɵɵdirectiveInject(NG_VALUE_ACCESSOR, 10), ɵɵdirectiveInject(NG_MODEL_WITH_FORM_CONTROL_WARNING, 8), ɵɵdirectiveInject(CALL_SET_DISABLED_STATE, 8));
  };
  static ɵdir = ɵɵdefineDirective({
    type: _FormControlDirective,
    selectors: [["", "formControl", ""]],
    inputs: {
      form: [0, "formControl", "form"],
      isDisabled: [0, "disabled", "isDisabled"],
      model: [0, "ngModel", "model"]
    },
    outputs: {
      update: "ngModelChange"
    },
    exportAs: ["ngForm"],
    standalone: false,
    features: [ɵɵProvidersFeature([formControlBinding]), ɵɵInheritDefinitionFeature, ɵɵNgOnChangesFeature]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(FormControlDirective, [{
    type: Directive,
    args: [{
      selector: "[formControl]",
      providers: [formControlBinding],
      exportAs: "ngForm",
      standalone: false
    }]
  }], () => [{
    type: void 0,
    decorators: [{
      type: Optional
    }, {
      type: Self
    }, {
      type: Inject,
      args: [NG_VALIDATORS]
    }]
  }, {
    type: void 0,
    decorators: [{
      type: Optional
    }, {
      type: Self
    }, {
      type: Inject,
      args: [NG_ASYNC_VALIDATORS]
    }]
  }, {
    type: void 0,
    decorators: [{
      type: Optional
    }, {
      type: Self
    }, {
      type: Inject,
      args: [NG_VALUE_ACCESSOR]
    }]
  }, {
    type: void 0,
    decorators: [{
      type: Optional
    }, {
      type: Inject,
      args: [NG_MODEL_WITH_FORM_CONTROL_WARNING]
    }]
  }, {
    type: void 0,
    decorators: [{
      type: Optional
    }, {
      type: Inject,
      args: [CALL_SET_DISABLED_STATE]
    }]
  }], {
    form: [{
      type: Input,
      args: ["formControl"]
    }],
    isDisabled: [{
      type: Input,
      args: ["disabled"]
    }],
    model: [{
      type: Input,
      args: ["ngModel"]
    }],
    update: [{
      type: Output,
      args: ["ngModelChange"]
    }]
  });
})();
var formDirectiveProvider = {
  provide: ControlContainer,
  useExisting: forwardRef(() => FormGroupDirective)
};
var FormGroupDirective = class _FormGroupDirective extends ControlContainer {
  callSetDisabledState;
  /**
   * @description
   * Reports whether the form submission has been triggered.
   */
  get submitted() {
    return untracked(this._submittedReactive);
  }
  // TODO(atscott): Remove once invalid API usage is cleaned up internally
  set submitted(value) {
    this._submittedReactive.set(value);
  }
  /** @internal */
  _submitted = computed(() => this._submittedReactive());
  _submittedReactive = signal(false);
  /**
   * Reference to an old form group input value, which is needed to cleanup
   * old instance in case it was replaced with a new one.
   */
  _oldForm;
  /**
   * Callback that should be invoked when controls in FormGroup or FormArray collection change
   * (added or removed). This callback triggers corresponding DOM updates.
   */
  _onCollectionChange = () => this._updateDomValue();
  /**
   * @description
   * Tracks the list of added `FormControlName` instances
   */
  directives = [];
  /**
   * @description
   * Tracks the `FormGroup` bound to this directive.
   */
  form = null;
  /**
   * @description
   * Emits an event when the form submission has been triggered.
   */
  ngSubmit = new EventEmitter();
  constructor(validators, asyncValidators, callSetDisabledState) {
    super();
    this.callSetDisabledState = callSetDisabledState;
    this._setValidators(validators);
    this._setAsyncValidators(asyncValidators);
  }
  /** @docs-private */
  ngOnChanges(changes) {
    if ((typeof ngDevMode === "undefined" || ngDevMode) && !this.form) {
      throw missingFormException();
    }
    if (changes.hasOwnProperty("form")) {
      this._updateValidators();
      this._updateDomValue();
      this._updateRegistrations();
      this._oldForm = this.form;
    }
  }
  /** @docs-private */
  ngOnDestroy() {
    if (this.form) {
      cleanUpValidators(this.form, this);
      if (this.form._onCollectionChange === this._onCollectionChange) {
        this.form._registerOnCollectionChange(() => {
        });
      }
    }
  }
  /**
   * @description
   * Returns this directive's instance.
   */
  get formDirective() {
    return this;
  }
  /**
   * @description
   * Returns the `FormGroup` bound to this directive.
   */
  get control() {
    return this.form;
  }
  /**
   * @description
   * Returns an array representing the path to this group. Because this directive
   * always lives at the top level of a form, it always an empty array.
   */
  get path() {
    return [];
  }
  /**
   * @description
   * Method that sets up the control directive in this group, re-calculates its value
   * and validity, and adds the instance to the internal list of directives.
   *
   * @param dir The `FormControlName` directive instance.
   */
  addControl(dir) {
    const ctrl = this.form.get(dir.path);
    setUpControl(ctrl, dir, this.callSetDisabledState);
    ctrl.updateValueAndValidity({
      emitEvent: false
    });
    this.directives.push(dir);
    return ctrl;
  }
  /**
   * @description
   * Retrieves the `FormControl` instance from the provided `FormControlName` directive
   *
   * @param dir The `FormControlName` directive instance.
   */
  getControl(dir) {
    return this.form.get(dir.path);
  }
  /**
   * @description
   * Removes the `FormControlName` instance from the internal list of directives
   *
   * @param dir The `FormControlName` directive instance.
   */
  removeControl(dir) {
    cleanUpControl(
      dir.control || null,
      dir,
      /* validateControlPresenceOnChange */
      false
    );
    removeListItem$1(this.directives, dir);
  }
  /**
   * Adds a new `FormGroupName` directive instance to the form.
   *
   * @param dir The `FormGroupName` directive instance.
   */
  addFormGroup(dir) {
    this._setUpFormContainer(dir);
  }
  /**
   * Performs the necessary cleanup when a `FormGroupName` directive instance is removed from the
   * view.
   *
   * @param dir The `FormGroupName` directive instance.
   */
  removeFormGroup(dir) {
    this._cleanUpFormContainer(dir);
  }
  /**
   * @description
   * Retrieves the `FormGroup` for a provided `FormGroupName` directive instance
   *
   * @param dir The `FormGroupName` directive instance.
   */
  getFormGroup(dir) {
    return this.form.get(dir.path);
  }
  /**
   * Performs the necessary setup when a `FormArrayName` directive instance is added to the view.
   *
   * @param dir The `FormArrayName` directive instance.
   */
  addFormArray(dir) {
    this._setUpFormContainer(dir);
  }
  /**
   * Performs the necessary cleanup when a `FormArrayName` directive instance is removed from the
   * view.
   *
   * @param dir The `FormArrayName` directive instance.
   */
  removeFormArray(dir) {
    this._cleanUpFormContainer(dir);
  }
  /**
   * @description
   * Retrieves the `FormArray` for a provided `FormArrayName` directive instance.
   *
   * @param dir The `FormArrayName` directive instance.
   */
  getFormArray(dir) {
    return this.form.get(dir.path);
  }
  /**
   * Sets the new value for the provided `FormControlName` directive.
   *
   * @param dir The `FormControlName` directive instance.
   * @param value The new value for the directive's control.
   */
  updateModel(dir, value) {
    const ctrl = this.form.get(dir.path);
    ctrl.setValue(value);
  }
  /**
   * @description
   * Method called with the "submit" event is triggered on the form.
   * Triggers the `ngSubmit` emitter to emit the "submit" event as its payload.
   *
   * @param $event The "submit" event object
   */
  onSubmit($event) {
    this._submittedReactive.set(true);
    syncPendingControls(this.form, this.directives);
    this.ngSubmit.emit($event);
    this.form._events.next(new FormSubmittedEvent(this.control));
    return $event?.target?.method === "dialog";
  }
  /**
   * @description
   * Method called when the "reset" event is triggered on the form.
   */
  onReset() {
    this.resetForm();
  }
  /**
   * @description
   * Resets the form to an initial value and resets its submitted status.
   *
   * @param value The new value for the form.
   */
  resetForm(value = void 0) {
    this.form.reset(value);
    this._submittedReactive.set(false);
    this.form._events.next(new FormResetEvent(this.form));
  }
  /** @internal */
  _updateDomValue() {
    this.directives.forEach((dir) => {
      const oldCtrl = dir.control;
      const newCtrl = this.form.get(dir.path);
      if (oldCtrl !== newCtrl) {
        cleanUpControl(oldCtrl || null, dir);
        if (isFormControl(newCtrl)) {
          setUpControl(newCtrl, dir, this.callSetDisabledState);
          dir.control = newCtrl;
        }
      }
    });
    this.form._updateTreeValidity({
      emitEvent: false
    });
  }
  _setUpFormContainer(dir) {
    const ctrl = this.form.get(dir.path);
    setUpFormContainer(ctrl, dir);
    ctrl.updateValueAndValidity({
      emitEvent: false
    });
  }
  _cleanUpFormContainer(dir) {
    if (this.form) {
      const ctrl = this.form.get(dir.path);
      if (ctrl) {
        const isControlUpdated = cleanUpFormContainer(ctrl, dir);
        if (isControlUpdated) {
          ctrl.updateValueAndValidity({
            emitEvent: false
          });
        }
      }
    }
  }
  _updateRegistrations() {
    this.form._registerOnCollectionChange(this._onCollectionChange);
    if (this._oldForm) {
      this._oldForm._registerOnCollectionChange(() => {
      });
    }
  }
  _updateValidators() {
    setUpValidators(this.form, this);
    if (this._oldForm) {
      cleanUpValidators(this._oldForm, this);
    }
  }
  static ɵfac = function FormGroupDirective_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _FormGroupDirective)(ɵɵdirectiveInject(NG_VALIDATORS, 10), ɵɵdirectiveInject(NG_ASYNC_VALIDATORS, 10), ɵɵdirectiveInject(CALL_SET_DISABLED_STATE, 8));
  };
  static ɵdir = ɵɵdefineDirective({
    type: _FormGroupDirective,
    selectors: [["", "formGroup", ""]],
    hostBindings: function FormGroupDirective_HostBindings(rf, ctx) {
      if (rf & 1) {
        ɵɵlistener("submit", function FormGroupDirective_submit_HostBindingHandler($event) {
          return ctx.onSubmit($event);
        })("reset", function FormGroupDirective_reset_HostBindingHandler() {
          return ctx.onReset();
        });
      }
    },
    inputs: {
      form: [0, "formGroup", "form"]
    },
    outputs: {
      ngSubmit: "ngSubmit"
    },
    exportAs: ["ngForm"],
    standalone: false,
    features: [ɵɵProvidersFeature([formDirectiveProvider]), ɵɵInheritDefinitionFeature, ɵɵNgOnChangesFeature]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(FormGroupDirective, [{
    type: Directive,
    args: [{
      selector: "[formGroup]",
      providers: [formDirectiveProvider],
      host: {
        "(submit)": "onSubmit($event)",
        "(reset)": "onReset()"
      },
      exportAs: "ngForm",
      standalone: false
    }]
  }], () => [{
    type: void 0,
    decorators: [{
      type: Optional
    }, {
      type: Self
    }, {
      type: Inject,
      args: [NG_VALIDATORS]
    }]
  }, {
    type: void 0,
    decorators: [{
      type: Optional
    }, {
      type: Self
    }, {
      type: Inject,
      args: [NG_ASYNC_VALIDATORS]
    }]
  }, {
    type: void 0,
    decorators: [{
      type: Optional
    }, {
      type: Inject,
      args: [CALL_SET_DISABLED_STATE]
    }]
  }], {
    form: [{
      type: Input,
      args: ["formGroup"]
    }],
    ngSubmit: [{
      type: Output
    }]
  });
})();
var formGroupNameProvider = {
  provide: ControlContainer,
  useExisting: forwardRef(() => FormGroupName)
};
var FormGroupName = class _FormGroupName extends AbstractFormGroupDirective {
  /**
   * @description
   * Tracks the name of the `FormGroup` bound to the directive. The name corresponds
   * to a key in the parent `FormGroup` or `FormArray`.
   * Accepts a name as a string or a number.
   * The name in the form of a string is useful for individual forms,
   * while the numerical form allows for form groups to be bound
   * to indices when iterating over groups in a `FormArray`.
   */
  name = null;
  constructor(parent, validators, asyncValidators) {
    super();
    this._parent = parent;
    this._setValidators(validators);
    this._setAsyncValidators(asyncValidators);
  }
  /** @internal */
  _checkParentType() {
    if (hasInvalidParent(this._parent) && (typeof ngDevMode === "undefined" || ngDevMode)) {
      throw groupParentException();
    }
  }
  static ɵfac = function FormGroupName_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _FormGroupName)(ɵɵdirectiveInject(ControlContainer, 13), ɵɵdirectiveInject(NG_VALIDATORS, 10), ɵɵdirectiveInject(NG_ASYNC_VALIDATORS, 10));
  };
  static ɵdir = ɵɵdefineDirective({
    type: _FormGroupName,
    selectors: [["", "formGroupName", ""]],
    inputs: {
      name: [0, "formGroupName", "name"]
    },
    standalone: false,
    features: [ɵɵProvidersFeature([formGroupNameProvider]), ɵɵInheritDefinitionFeature]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(FormGroupName, [{
    type: Directive,
    args: [{
      selector: "[formGroupName]",
      providers: [formGroupNameProvider],
      standalone: false
    }]
  }], () => [{
    type: ControlContainer,
    decorators: [{
      type: Optional
    }, {
      type: Host
    }, {
      type: SkipSelf
    }]
  }, {
    type: void 0,
    decorators: [{
      type: Optional
    }, {
      type: Self
    }, {
      type: Inject,
      args: [NG_VALIDATORS]
    }]
  }, {
    type: void 0,
    decorators: [{
      type: Optional
    }, {
      type: Self
    }, {
      type: Inject,
      args: [NG_ASYNC_VALIDATORS]
    }]
  }], {
    name: [{
      type: Input,
      args: ["formGroupName"]
    }]
  });
})();
var formArrayNameProvider = {
  provide: ControlContainer,
  useExisting: forwardRef(() => FormArrayName)
};
var FormArrayName = class _FormArrayName extends ControlContainer {
  /** @internal */
  _parent;
  /**
   * @description
   * Tracks the name of the `FormArray` bound to the directive. The name corresponds
   * to a key in the parent `FormGroup` or `FormArray`.
   * Accepts a name as a string or a number.
   * The name in the form of a string is useful for individual forms,
   * while the numerical form allows for form arrays to be bound
   * to indices when iterating over arrays in a `FormArray`.
   */
  name = null;
  constructor(parent, validators, asyncValidators) {
    super();
    this._parent = parent;
    this._setValidators(validators);
    this._setAsyncValidators(asyncValidators);
  }
  /**
   * A lifecycle method called when the directive's inputs are initialized. For internal use only.
   * @throws If the directive does not have a valid parent.
   * @docs-private
   */
  ngOnInit() {
    if (hasInvalidParent(this._parent) && (typeof ngDevMode === "undefined" || ngDevMode)) {
      throw arrayParentException();
    }
    this.formDirective.addFormArray(this);
  }
  /**
   * A lifecycle method called before the directive's instance is destroyed. For internal use only.
   * @docs-private
   */
  ngOnDestroy() {
    this.formDirective?.removeFormArray(this);
  }
  /**
   * @description
   * The `FormArray` bound to this directive.
   */
  get control() {
    return this.formDirective.getFormArray(this);
  }
  /**
   * @description
   * The top-level directive for this group if present, otherwise null.
   */
  get formDirective() {
    return this._parent ? this._parent.formDirective : null;
  }
  /**
   * @description
   * Returns an array that represents the path from the top-level form to this control.
   * Each index is the string name of the control on that level.
   */
  get path() {
    return controlPath(this.name == null ? this.name : this.name.toString(), this._parent);
  }
  static ɵfac = function FormArrayName_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _FormArrayName)(ɵɵdirectiveInject(ControlContainer, 13), ɵɵdirectiveInject(NG_VALIDATORS, 10), ɵɵdirectiveInject(NG_ASYNC_VALIDATORS, 10));
  };
  static ɵdir = ɵɵdefineDirective({
    type: _FormArrayName,
    selectors: [["", "formArrayName", ""]],
    inputs: {
      name: [0, "formArrayName", "name"]
    },
    standalone: false,
    features: [ɵɵProvidersFeature([formArrayNameProvider]), ɵɵInheritDefinitionFeature]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(FormArrayName, [{
    type: Directive,
    args: [{
      selector: "[formArrayName]",
      providers: [formArrayNameProvider],
      standalone: false
    }]
  }], () => [{
    type: ControlContainer,
    decorators: [{
      type: Optional
    }, {
      type: Host
    }, {
      type: SkipSelf
    }]
  }, {
    type: void 0,
    decorators: [{
      type: Optional
    }, {
      type: Self
    }, {
      type: Inject,
      args: [NG_VALIDATORS]
    }]
  }, {
    type: void 0,
    decorators: [{
      type: Optional
    }, {
      type: Self
    }, {
      type: Inject,
      args: [NG_ASYNC_VALIDATORS]
    }]
  }], {
    name: [{
      type: Input,
      args: ["formArrayName"]
    }]
  });
})();
function hasInvalidParent(parent) {
  return !(parent instanceof FormGroupName) && !(parent instanceof FormGroupDirective) && !(parent instanceof FormArrayName);
}
var controlNameBinding = {
  provide: NgControl,
  useExisting: forwardRef(() => FormControlName)
};
var FormControlName = class _FormControlName extends NgControl {
  _ngModelWarningConfig;
  _added = false;
  /**
   * Internal reference to the view model value.
   * @internal
   */
  viewModel;
  /**
   * @description
   * Tracks the `FormControl` instance bound to the directive.
   */
  control;
  /**
   * @description
   * Tracks the name of the `FormControl` bound to the directive. The name corresponds
   * to a key in the parent `FormGroup` or `FormArray`.
   * Accepts a name as a string or a number.
   * The name in the form of a string is useful for individual forms,
   * while the numerical form allows for form controls to be bound
   * to indices when iterating over controls in a `FormArray`.
   */
  name = null;
  /**
   * @description
   * Triggers a warning in dev mode that this input should not be used with reactive forms.
   */
  set isDisabled(isDisabled) {
    if (typeof ngDevMode === "undefined" || ngDevMode) {
      console.warn(disabledAttrWarning);
    }
  }
  // TODO(kara): remove next 4 properties once deprecation period is over
  /** @deprecated as of v6 */
  model;
  /** @deprecated as of v6 */
  update = new EventEmitter();
  /**
   * @description
   * Static property used to track whether any ngModel warnings have been sent across
   * all instances of FormControlName. Used to support warning config of "once".
   *
   * @internal
   */
  static _ngModelWarningSentOnce = false;
  /**
   * @description
   * Instance property used to track whether an ngModel warning has been sent out for this
   * particular FormControlName instance. Used to support warning config of "always".
   *
   * @internal
   */
  _ngModelWarningSent = false;
  constructor(parent, validators, asyncValidators, valueAccessors, _ngModelWarningConfig) {
    super();
    this._ngModelWarningConfig = _ngModelWarningConfig;
    this._parent = parent;
    this._setValidators(validators);
    this._setAsyncValidators(asyncValidators);
    this.valueAccessor = selectValueAccessor(this, valueAccessors);
  }
  /** @docs-private */
  ngOnChanges(changes) {
    if (!this._added) this._setUpControl();
    if (isPropertyUpdated(changes, this.viewModel)) {
      if (typeof ngDevMode === "undefined" || ngDevMode) {
        _ngModelWarning("formControlName", _FormControlName, this, this._ngModelWarningConfig);
      }
      this.viewModel = this.model;
      this.formDirective.updateModel(this, this.model);
    }
  }
  /** @docs-private */
  ngOnDestroy() {
    if (this.formDirective) {
      this.formDirective.removeControl(this);
    }
  }
  /**
   * @description
   * Sets the new value for the view model and emits an `ngModelChange` event.
   *
   * @param newValue The new value for the view model.
   */
  viewToModelUpdate(newValue) {
    this.viewModel = newValue;
    this.update.emit(newValue);
  }
  /**
   * @description
   * Returns an array that represents the path from the top-level form to this control.
   * Each index is the string name of the control on that level.
   */
  get path() {
    return controlPath(this.name == null ? this.name : this.name.toString(), this._parent);
  }
  /**
   * @description
   * The top-level directive for this group if present, otherwise null.
   */
  get formDirective() {
    return this._parent ? this._parent.formDirective : null;
  }
  _setUpControl() {
    if (typeof ngDevMode === "undefined" || ngDevMode) {
      checkParentType(this._parent, this.name);
    }
    this.control = this.formDirective.addControl(this);
    this._added = true;
  }
  static ɵfac = function FormControlName_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _FormControlName)(ɵɵdirectiveInject(ControlContainer, 13), ɵɵdirectiveInject(NG_VALIDATORS, 10), ɵɵdirectiveInject(NG_ASYNC_VALIDATORS, 10), ɵɵdirectiveInject(NG_VALUE_ACCESSOR, 10), ɵɵdirectiveInject(NG_MODEL_WITH_FORM_CONTROL_WARNING, 8));
  };
  static ɵdir = ɵɵdefineDirective({
    type: _FormControlName,
    selectors: [["", "formControlName", ""]],
    inputs: {
      name: [0, "formControlName", "name"],
      isDisabled: [0, "disabled", "isDisabled"],
      model: [0, "ngModel", "model"]
    },
    outputs: {
      update: "ngModelChange"
    },
    standalone: false,
    features: [ɵɵProvidersFeature([controlNameBinding]), ɵɵInheritDefinitionFeature, ɵɵNgOnChangesFeature]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(FormControlName, [{
    type: Directive,
    args: [{
      selector: "[formControlName]",
      providers: [controlNameBinding],
      standalone: false
    }]
  }], () => [{
    type: ControlContainer,
    decorators: [{
      type: Optional
    }, {
      type: Host
    }, {
      type: SkipSelf
    }]
  }, {
    type: void 0,
    decorators: [{
      type: Optional
    }, {
      type: Self
    }, {
      type: Inject,
      args: [NG_VALIDATORS]
    }]
  }, {
    type: void 0,
    decorators: [{
      type: Optional
    }, {
      type: Self
    }, {
      type: Inject,
      args: [NG_ASYNC_VALIDATORS]
    }]
  }, {
    type: void 0,
    decorators: [{
      type: Optional
    }, {
      type: Self
    }, {
      type: Inject,
      args: [NG_VALUE_ACCESSOR]
    }]
  }, {
    type: void 0,
    decorators: [{
      type: Optional
    }, {
      type: Inject,
      args: [NG_MODEL_WITH_FORM_CONTROL_WARNING]
    }]
  }], {
    name: [{
      type: Input,
      args: ["formControlName"]
    }],
    isDisabled: [{
      type: Input,
      args: ["disabled"]
    }],
    model: [{
      type: Input,
      args: ["ngModel"]
    }],
    update: [{
      type: Output,
      args: ["ngModelChange"]
    }]
  });
})();
function checkParentType(parent, name) {
  if (!(parent instanceof FormGroupName) && parent instanceof AbstractFormGroupDirective) {
    throw ngModelGroupException();
  } else if (!(parent instanceof FormGroupName) && !(parent instanceof FormGroupDirective) && !(parent instanceof FormArrayName)) {
    throw controlParentException(name);
  }
}
var SELECT_VALUE_ACCESSOR = {
  provide: NG_VALUE_ACCESSOR,
  useExisting: forwardRef(() => SelectControlValueAccessor),
  multi: true
};
function _buildValueString$1(id, value) {
  if (id == null) return `${value}`;
  if (value && typeof value === "object") value = "Object";
  return `${id}: ${value}`.slice(0, 50);
}
function _extractId$1(valueString) {
  return valueString.split(":")[0];
}
var SelectControlValueAccessor = class _SelectControlValueAccessor extends BuiltInControlValueAccessor {
  /** @docs-private */
  value;
  /** @internal */
  _optionMap = /* @__PURE__ */ new Map();
  /** @internal */
  _idCounter = 0;
  /**
   * @description
   * Tracks the option comparison algorithm for tracking identities when
   * checking for changes.
   */
  set compareWith(fn) {
    if (typeof fn !== "function" && (typeof ngDevMode === "undefined" || ngDevMode)) {
      throw new RuntimeError(1201, `compareWith must be a function, but received ${JSON.stringify(fn)}`);
    }
    this._compareWith = fn;
  }
  _compareWith = Object.is;
  /**
   * Sets the "value" property on the select element.
   * @docs-private
   */
  writeValue(value) {
    this.value = value;
    const id = this._getOptionId(value);
    const valueString = _buildValueString$1(id, value);
    this.setProperty("value", valueString);
  }
  /**
   * Registers a function called when the control value changes.
   * @docs-private
   */
  registerOnChange(fn) {
    this.onChange = (valueString) => {
      this.value = this._getOptionValue(valueString);
      fn(this.value);
    };
  }
  /** @internal */
  _registerOption() {
    return (this._idCounter++).toString();
  }
  /** @internal */
  _getOptionId(value) {
    for (const id of this._optionMap.keys()) {
      if (this._compareWith(this._optionMap.get(id), value)) return id;
    }
    return null;
  }
  /** @internal */
  _getOptionValue(valueString) {
    const id = _extractId$1(valueString);
    return this._optionMap.has(id) ? this._optionMap.get(id) : valueString;
  }
  static ɵfac = /* @__PURE__ */ (() => {
    let ɵSelectControlValueAccessor_BaseFactory;
    return function SelectControlValueAccessor_Factory(__ngFactoryType__) {
      return (ɵSelectControlValueAccessor_BaseFactory || (ɵSelectControlValueAccessor_BaseFactory = ɵɵgetInheritedFactory(_SelectControlValueAccessor)))(__ngFactoryType__ || _SelectControlValueAccessor);
    };
  })();
  static ɵdir = ɵɵdefineDirective({
    type: _SelectControlValueAccessor,
    selectors: [["select", "formControlName", "", 3, "multiple", ""], ["select", "formControl", "", 3, "multiple", ""], ["select", "ngModel", "", 3, "multiple", ""]],
    hostBindings: function SelectControlValueAccessor_HostBindings(rf, ctx) {
      if (rf & 1) {
        ɵɵlistener("change", function SelectControlValueAccessor_change_HostBindingHandler($event) {
          return ctx.onChange($event.target.value);
        })("blur", function SelectControlValueAccessor_blur_HostBindingHandler() {
          return ctx.onTouched();
        });
      }
    },
    inputs: {
      compareWith: "compareWith"
    },
    standalone: false,
    features: [ɵɵProvidersFeature([SELECT_VALUE_ACCESSOR]), ɵɵInheritDefinitionFeature]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(SelectControlValueAccessor, [{
    type: Directive,
    args: [{
      selector: "select:not([multiple])[formControlName],select:not([multiple])[formControl],select:not([multiple])[ngModel]",
      host: {
        "(change)": "onChange($event.target.value)",
        "(blur)": "onTouched()"
      },
      providers: [SELECT_VALUE_ACCESSOR],
      standalone: false
    }]
  }], null, {
    compareWith: [{
      type: Input
    }]
  });
})();
var NgSelectOption = class _NgSelectOption {
  _element;
  _renderer;
  _select;
  /**
   * @description
   * ID of the option element
   */
  id;
  constructor(_element, _renderer, _select) {
    this._element = _element;
    this._renderer = _renderer;
    this._select = _select;
    if (this._select) this.id = this._select._registerOption();
  }
  /**
   * @description
   * Tracks the value bound to the option element. Unlike the value binding,
   * ngValue supports binding to objects.
   */
  set ngValue(value) {
    if (this._select == null) return;
    this._select._optionMap.set(this.id, value);
    this._setElementValue(_buildValueString$1(this.id, value));
    this._select.writeValue(this._select.value);
  }
  /**
   * @description
   * Tracks simple string values bound to the option element.
   * For objects, use the `ngValue` input binding.
   */
  set value(value) {
    this._setElementValue(value);
    if (this._select) this._select.writeValue(this._select.value);
  }
  /** @internal */
  _setElementValue(value) {
    this._renderer.setProperty(this._element.nativeElement, "value", value);
  }
  /** @docs-private */
  ngOnDestroy() {
    if (this._select) {
      this._select._optionMap.delete(this.id);
      this._select.writeValue(this._select.value);
    }
  }
  static ɵfac = function NgSelectOption_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _NgSelectOption)(ɵɵdirectiveInject(ElementRef), ɵɵdirectiveInject(Renderer2), ɵɵdirectiveInject(SelectControlValueAccessor, 9));
  };
  static ɵdir = ɵɵdefineDirective({
    type: _NgSelectOption,
    selectors: [["option"]],
    inputs: {
      ngValue: "ngValue",
      value: "value"
    },
    standalone: false
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(NgSelectOption, [{
    type: Directive,
    args: [{
      selector: "option",
      standalone: false
    }]
  }], () => [{
    type: ElementRef
  }, {
    type: Renderer2
  }, {
    type: SelectControlValueAccessor,
    decorators: [{
      type: Optional
    }, {
      type: Host
    }]
  }], {
    ngValue: [{
      type: Input,
      args: ["ngValue"]
    }],
    value: [{
      type: Input,
      args: ["value"]
    }]
  });
})();
var SELECT_MULTIPLE_VALUE_ACCESSOR = {
  provide: NG_VALUE_ACCESSOR,
  useExisting: forwardRef(() => SelectMultipleControlValueAccessor),
  multi: true
};
function _buildValueString(id, value) {
  if (id == null) return `${value}`;
  if (typeof value === "string") value = `'${value}'`;
  if (value && typeof value === "object") value = "Object";
  return `${id}: ${value}`.slice(0, 50);
}
function _extractId(valueString) {
  return valueString.split(":")[0];
}
var SelectMultipleControlValueAccessor = class _SelectMultipleControlValueAccessor extends BuiltInControlValueAccessor {
  /**
   * The current value.
   * @docs-private
   */
  value;
  /** @internal */
  _optionMap = /* @__PURE__ */ new Map();
  /** @internal */
  _idCounter = 0;
  /**
   * @description
   * Tracks the option comparison algorithm for tracking identities when
   * checking for changes.
   */
  set compareWith(fn) {
    if (typeof fn !== "function" && (typeof ngDevMode === "undefined" || ngDevMode)) {
      throw new RuntimeError(1201, `compareWith must be a function, but received ${JSON.stringify(fn)}`);
    }
    this._compareWith = fn;
  }
  _compareWith = Object.is;
  /**
   * Sets the "value" property on one or of more of the select's options.
   * @docs-private
   */
  writeValue(value) {
    this.value = value;
    let optionSelectedStateSetter;
    if (Array.isArray(value)) {
      const ids = value.map((v) => this._getOptionId(v));
      optionSelectedStateSetter = (opt, o) => {
        opt._setSelected(ids.indexOf(o.toString()) > -1);
      };
    } else {
      optionSelectedStateSetter = (opt, o) => {
        opt._setSelected(false);
      };
    }
    this._optionMap.forEach(optionSelectedStateSetter);
  }
  /**
   * Registers a function called when the control value changes
   * and writes an array of the selected options.
   * @docs-private
   */
  registerOnChange(fn) {
    this.onChange = (element) => {
      const selected = [];
      const selectedOptions = element.selectedOptions;
      if (selectedOptions !== void 0) {
        const options = selectedOptions;
        for (let i = 0; i < options.length; i++) {
          const opt = options[i];
          const val = this._getOptionValue(opt.value);
          selected.push(val);
        }
      } else {
        const options = element.options;
        for (let i = 0; i < options.length; i++) {
          const opt = options[i];
          if (opt.selected) {
            const val = this._getOptionValue(opt.value);
            selected.push(val);
          }
        }
      }
      this.value = selected;
      fn(selected);
    };
  }
  /** @internal */
  _registerOption(value) {
    const id = (this._idCounter++).toString();
    this._optionMap.set(id, value);
    return id;
  }
  /** @internal */
  _getOptionId(value) {
    for (const id of this._optionMap.keys()) {
      if (this._compareWith(this._optionMap.get(id)._value, value)) return id;
    }
    return null;
  }
  /** @internal */
  _getOptionValue(valueString) {
    const id = _extractId(valueString);
    return this._optionMap.has(id) ? this._optionMap.get(id)._value : valueString;
  }
  static ɵfac = /* @__PURE__ */ (() => {
    let ɵSelectMultipleControlValueAccessor_BaseFactory;
    return function SelectMultipleControlValueAccessor_Factory(__ngFactoryType__) {
      return (ɵSelectMultipleControlValueAccessor_BaseFactory || (ɵSelectMultipleControlValueAccessor_BaseFactory = ɵɵgetInheritedFactory(_SelectMultipleControlValueAccessor)))(__ngFactoryType__ || _SelectMultipleControlValueAccessor);
    };
  })();
  static ɵdir = ɵɵdefineDirective({
    type: _SelectMultipleControlValueAccessor,
    selectors: [["select", "multiple", "", "formControlName", ""], ["select", "multiple", "", "formControl", ""], ["select", "multiple", "", "ngModel", ""]],
    hostBindings: function SelectMultipleControlValueAccessor_HostBindings(rf, ctx) {
      if (rf & 1) {
        ɵɵlistener("change", function SelectMultipleControlValueAccessor_change_HostBindingHandler($event) {
          return ctx.onChange($event.target);
        })("blur", function SelectMultipleControlValueAccessor_blur_HostBindingHandler() {
          return ctx.onTouched();
        });
      }
    },
    inputs: {
      compareWith: "compareWith"
    },
    standalone: false,
    features: [ɵɵProvidersFeature([SELECT_MULTIPLE_VALUE_ACCESSOR]), ɵɵInheritDefinitionFeature]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(SelectMultipleControlValueAccessor, [{
    type: Directive,
    args: [{
      selector: "select[multiple][formControlName],select[multiple][formControl],select[multiple][ngModel]",
      host: {
        "(change)": "onChange($event.target)",
        "(blur)": "onTouched()"
      },
      providers: [SELECT_MULTIPLE_VALUE_ACCESSOR],
      standalone: false
    }]
  }], null, {
    compareWith: [{
      type: Input
    }]
  });
})();
var ɵNgSelectMultipleOption = class _ɵNgSelectMultipleOption {
  _element;
  _renderer;
  _select;
  id;
  /** @internal */
  _value;
  constructor(_element, _renderer, _select) {
    this._element = _element;
    this._renderer = _renderer;
    this._select = _select;
    if (this._select) {
      this.id = this._select._registerOption(this);
    }
  }
  /**
   * @description
   * Tracks the value bound to the option element. Unlike the value binding,
   * ngValue supports binding to objects.
   */
  set ngValue(value) {
    if (this._select == null) return;
    this._value = value;
    this._setElementValue(_buildValueString(this.id, value));
    this._select.writeValue(this._select.value);
  }
  /**
   * @description
   * Tracks simple string values bound to the option element.
   * For objects, use the `ngValue` input binding.
   */
  set value(value) {
    if (this._select) {
      this._value = value;
      this._setElementValue(_buildValueString(this.id, value));
      this._select.writeValue(this._select.value);
    } else {
      this._setElementValue(value);
    }
  }
  /** @internal */
  _setElementValue(value) {
    this._renderer.setProperty(this._element.nativeElement, "value", value);
  }
  /** @internal */
  _setSelected(selected) {
    this._renderer.setProperty(this._element.nativeElement, "selected", selected);
  }
  /** @docs-private */
  ngOnDestroy() {
    if (this._select) {
      this._select._optionMap.delete(this.id);
      this._select.writeValue(this._select.value);
    }
  }
  static ɵfac = function ɵNgSelectMultipleOption_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _ɵNgSelectMultipleOption)(ɵɵdirectiveInject(ElementRef), ɵɵdirectiveInject(Renderer2), ɵɵdirectiveInject(SelectMultipleControlValueAccessor, 9));
  };
  static ɵdir = ɵɵdefineDirective({
    type: _ɵNgSelectMultipleOption,
    selectors: [["option"]],
    inputs: {
      ngValue: "ngValue",
      value: "value"
    },
    standalone: false
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(ɵNgSelectMultipleOption, [{
    type: Directive,
    args: [{
      selector: "option",
      standalone: false
    }]
  }], () => [{
    type: ElementRef
  }, {
    type: Renderer2
  }, {
    type: SelectMultipleControlValueAccessor,
    decorators: [{
      type: Optional
    }, {
      type: Host
    }]
  }], {
    ngValue: [{
      type: Input,
      args: ["ngValue"]
    }],
    value: [{
      type: Input,
      args: ["value"]
    }]
  });
})();
function toInteger(value) {
  return typeof value === "number" ? value : parseInt(value, 10);
}
function toFloat(value) {
  return typeof value === "number" ? value : parseFloat(value);
}
var AbstractValidatorDirective = class _AbstractValidatorDirective {
  _validator = nullValidator;
  _onChange;
  /**
   * A flag that tracks whether this validator is enabled.
   *
   * Marking it `internal` (vs `protected`), so that this flag can be used in host bindings of
   * directive classes that extend this base class.
   * @internal
   */
  _enabled;
  /** @docs-private */
  ngOnChanges(changes) {
    if (this.inputName in changes) {
      const input2 = this.normalizeInput(changes[this.inputName].currentValue);
      this._enabled = this.enabled(input2);
      this._validator = this._enabled ? this.createValidator(input2) : nullValidator;
      if (this._onChange) {
        this._onChange();
      }
    }
  }
  /** @docs-private */
  validate(control) {
    return this._validator(control);
  }
  /** @docs-private */
  registerOnValidatorChange(fn) {
    this._onChange = fn;
  }
  /**
   * @description
   * Determines whether this validator should be active or not based on an input.
   * Base class implementation checks whether an input is defined (if the value is different from
   * `null` and `undefined`). Validator classes that extend this base class can override this
   * function with the logic specific to a particular validator directive.
   */
  enabled(input2) {
    return input2 != null;
  }
  static ɵfac = function AbstractValidatorDirective_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _AbstractValidatorDirective)();
  };
  static ɵdir = ɵɵdefineDirective({
    type: _AbstractValidatorDirective,
    features: [ɵɵNgOnChangesFeature]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(AbstractValidatorDirective, [{
    type: Directive
  }], null, null);
})();
var MAX_VALIDATOR = {
  provide: NG_VALIDATORS,
  useExisting: forwardRef(() => MaxValidator),
  multi: true
};
var MaxValidator = class _MaxValidator extends AbstractValidatorDirective {
  /**
   * @description
   * Tracks changes to the max bound to this directive.
   */
  max;
  /** @internal */
  inputName = "max";
  /** @internal */
  normalizeInput = (input2) => toFloat(input2);
  /** @internal */
  createValidator = (max) => maxValidator(max);
  static ɵfac = /* @__PURE__ */ (() => {
    let ɵMaxValidator_BaseFactory;
    return function MaxValidator_Factory(__ngFactoryType__) {
      return (ɵMaxValidator_BaseFactory || (ɵMaxValidator_BaseFactory = ɵɵgetInheritedFactory(_MaxValidator)))(__ngFactoryType__ || _MaxValidator);
    };
  })();
  static ɵdir = ɵɵdefineDirective({
    type: _MaxValidator,
    selectors: [["input", "type", "number", "max", "", "formControlName", ""], ["input", "type", "number", "max", "", "formControl", ""], ["input", "type", "number", "max", "", "ngModel", ""]],
    hostVars: 1,
    hostBindings: function MaxValidator_HostBindings(rf, ctx) {
      if (rf & 2) {
        ɵɵattribute("max", ctx._enabled ? ctx.max : null);
      }
    },
    inputs: {
      max: "max"
    },
    standalone: false,
    features: [ɵɵProvidersFeature([MAX_VALIDATOR]), ɵɵInheritDefinitionFeature]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(MaxValidator, [{
    type: Directive,
    args: [{
      selector: "input[type=number][max][formControlName],input[type=number][max][formControl],input[type=number][max][ngModel]",
      providers: [MAX_VALIDATOR],
      host: {
        "[attr.max]": "_enabled ? max : null"
      },
      standalone: false
    }]
  }], null, {
    max: [{
      type: Input
    }]
  });
})();
var MIN_VALIDATOR = {
  provide: NG_VALIDATORS,
  useExisting: forwardRef(() => MinValidator),
  multi: true
};
var MinValidator = class _MinValidator extends AbstractValidatorDirective {
  /**
   * @description
   * Tracks changes to the min bound to this directive.
   */
  min;
  /** @internal */
  inputName = "min";
  /** @internal */
  normalizeInput = (input2) => toFloat(input2);
  /** @internal */
  createValidator = (min) => minValidator(min);
  static ɵfac = /* @__PURE__ */ (() => {
    let ɵMinValidator_BaseFactory;
    return function MinValidator_Factory(__ngFactoryType__) {
      return (ɵMinValidator_BaseFactory || (ɵMinValidator_BaseFactory = ɵɵgetInheritedFactory(_MinValidator)))(__ngFactoryType__ || _MinValidator);
    };
  })();
  static ɵdir = ɵɵdefineDirective({
    type: _MinValidator,
    selectors: [["input", "type", "number", "min", "", "formControlName", ""], ["input", "type", "number", "min", "", "formControl", ""], ["input", "type", "number", "min", "", "ngModel", ""]],
    hostVars: 1,
    hostBindings: function MinValidator_HostBindings(rf, ctx) {
      if (rf & 2) {
        ɵɵattribute("min", ctx._enabled ? ctx.min : null);
      }
    },
    inputs: {
      min: "min"
    },
    standalone: false,
    features: [ɵɵProvidersFeature([MIN_VALIDATOR]), ɵɵInheritDefinitionFeature]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(MinValidator, [{
    type: Directive,
    args: [{
      selector: "input[type=number][min][formControlName],input[type=number][min][formControl],input[type=number][min][ngModel]",
      providers: [MIN_VALIDATOR],
      host: {
        "[attr.min]": "_enabled ? min : null"
      },
      standalone: false
    }]
  }], null, {
    min: [{
      type: Input
    }]
  });
})();
var REQUIRED_VALIDATOR = {
  provide: NG_VALIDATORS,
  useExisting: forwardRef(() => RequiredValidator),
  multi: true
};
var CHECKBOX_REQUIRED_VALIDATOR = {
  provide: NG_VALIDATORS,
  useExisting: forwardRef(() => CheckboxRequiredValidator),
  multi: true
};
var RequiredValidator = class _RequiredValidator extends AbstractValidatorDirective {
  /**
   * @description
   * Tracks changes to the required attribute bound to this directive.
   */
  required;
  /** @internal */
  inputName = "required";
  /** @internal */
  normalizeInput = booleanAttribute;
  /** @internal */
  createValidator = (input2) => requiredValidator;
  /** @docs-private */
  enabled(input2) {
    return input2;
  }
  static ɵfac = /* @__PURE__ */ (() => {
    let ɵRequiredValidator_BaseFactory;
    return function RequiredValidator_Factory(__ngFactoryType__) {
      return (ɵRequiredValidator_BaseFactory || (ɵRequiredValidator_BaseFactory = ɵɵgetInheritedFactory(_RequiredValidator)))(__ngFactoryType__ || _RequiredValidator);
    };
  })();
  static ɵdir = ɵɵdefineDirective({
    type: _RequiredValidator,
    selectors: [["", "required", "", "formControlName", "", 3, "type", "checkbox"], ["", "required", "", "formControl", "", 3, "type", "checkbox"], ["", "required", "", "ngModel", "", 3, "type", "checkbox"]],
    hostVars: 1,
    hostBindings: function RequiredValidator_HostBindings(rf, ctx) {
      if (rf & 2) {
        ɵɵattribute("required", ctx._enabled ? "" : null);
      }
    },
    inputs: {
      required: "required"
    },
    standalone: false,
    features: [ɵɵProvidersFeature([REQUIRED_VALIDATOR]), ɵɵInheritDefinitionFeature]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(RequiredValidator, [{
    type: Directive,
    args: [{
      selector: ":not([type=checkbox])[required][formControlName],:not([type=checkbox])[required][formControl],:not([type=checkbox])[required][ngModel]",
      providers: [REQUIRED_VALIDATOR],
      host: {
        "[attr.required]": '_enabled ? "" : null'
      },
      standalone: false
    }]
  }], null, {
    required: [{
      type: Input
    }]
  });
})();
var CheckboxRequiredValidator = class _CheckboxRequiredValidator extends RequiredValidator {
  /** @internal */
  createValidator = (input2) => requiredTrueValidator;
  static ɵfac = /* @__PURE__ */ (() => {
    let ɵCheckboxRequiredValidator_BaseFactory;
    return function CheckboxRequiredValidator_Factory(__ngFactoryType__) {
      return (ɵCheckboxRequiredValidator_BaseFactory || (ɵCheckboxRequiredValidator_BaseFactory = ɵɵgetInheritedFactory(_CheckboxRequiredValidator)))(__ngFactoryType__ || _CheckboxRequiredValidator);
    };
  })();
  static ɵdir = ɵɵdefineDirective({
    type: _CheckboxRequiredValidator,
    selectors: [["input", "type", "checkbox", "required", "", "formControlName", ""], ["input", "type", "checkbox", "required", "", "formControl", ""], ["input", "type", "checkbox", "required", "", "ngModel", ""]],
    hostVars: 1,
    hostBindings: function CheckboxRequiredValidator_HostBindings(rf, ctx) {
      if (rf & 2) {
        ɵɵattribute("required", ctx._enabled ? "" : null);
      }
    },
    standalone: false,
    features: [ɵɵProvidersFeature([CHECKBOX_REQUIRED_VALIDATOR]), ɵɵInheritDefinitionFeature]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(CheckboxRequiredValidator, [{
    type: Directive,
    args: [{
      selector: "input[type=checkbox][required][formControlName],input[type=checkbox][required][formControl],input[type=checkbox][required][ngModel]",
      providers: [CHECKBOX_REQUIRED_VALIDATOR],
      host: {
        "[attr.required]": '_enabled ? "" : null'
      },
      standalone: false
    }]
  }], null, null);
})();
var EMAIL_VALIDATOR = {
  provide: NG_VALIDATORS,
  useExisting: forwardRef(() => EmailValidator),
  multi: true
};
var EmailValidator = class _EmailValidator extends AbstractValidatorDirective {
  /**
   * @description
   * Tracks changes to the email attribute bound to this directive.
   */
  email;
  /** @internal */
  inputName = "email";
  /** @internal */
  normalizeInput = booleanAttribute;
  /** @internal */
  createValidator = (input2) => emailValidator;
  /** @docs-private */
  enabled(input2) {
    return input2;
  }
  static ɵfac = /* @__PURE__ */ (() => {
    let ɵEmailValidator_BaseFactory;
    return function EmailValidator_Factory(__ngFactoryType__) {
      return (ɵEmailValidator_BaseFactory || (ɵEmailValidator_BaseFactory = ɵɵgetInheritedFactory(_EmailValidator)))(__ngFactoryType__ || _EmailValidator);
    };
  })();
  static ɵdir = ɵɵdefineDirective({
    type: _EmailValidator,
    selectors: [["", "email", "", "formControlName", ""], ["", "email", "", "formControl", ""], ["", "email", "", "ngModel", ""]],
    inputs: {
      email: "email"
    },
    standalone: false,
    features: [ɵɵProvidersFeature([EMAIL_VALIDATOR]), ɵɵInheritDefinitionFeature]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(EmailValidator, [{
    type: Directive,
    args: [{
      selector: "[email][formControlName],[email][formControl],[email][ngModel]",
      providers: [EMAIL_VALIDATOR],
      standalone: false
    }]
  }], null, {
    email: [{
      type: Input
    }]
  });
})();
var MIN_LENGTH_VALIDATOR = {
  provide: NG_VALIDATORS,
  useExisting: forwardRef(() => MinLengthValidator),
  multi: true
};
var MinLengthValidator = class _MinLengthValidator extends AbstractValidatorDirective {
  /**
   * @description
   * Tracks changes to the minimum length bound to this directive.
   */
  minlength;
  /** @internal */
  inputName = "minlength";
  /** @internal */
  normalizeInput = (input2) => toInteger(input2);
  /** @internal */
  createValidator = (minlength) => minLengthValidator(minlength);
  static ɵfac = /* @__PURE__ */ (() => {
    let ɵMinLengthValidator_BaseFactory;
    return function MinLengthValidator_Factory(__ngFactoryType__) {
      return (ɵMinLengthValidator_BaseFactory || (ɵMinLengthValidator_BaseFactory = ɵɵgetInheritedFactory(_MinLengthValidator)))(__ngFactoryType__ || _MinLengthValidator);
    };
  })();
  static ɵdir = ɵɵdefineDirective({
    type: _MinLengthValidator,
    selectors: [["", "minlength", "", "formControlName", ""], ["", "minlength", "", "formControl", ""], ["", "minlength", "", "ngModel", ""]],
    hostVars: 1,
    hostBindings: function MinLengthValidator_HostBindings(rf, ctx) {
      if (rf & 2) {
        ɵɵattribute("minlength", ctx._enabled ? ctx.minlength : null);
      }
    },
    inputs: {
      minlength: "minlength"
    },
    standalone: false,
    features: [ɵɵProvidersFeature([MIN_LENGTH_VALIDATOR]), ɵɵInheritDefinitionFeature]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(MinLengthValidator, [{
    type: Directive,
    args: [{
      selector: "[minlength][formControlName],[minlength][formControl],[minlength][ngModel]",
      providers: [MIN_LENGTH_VALIDATOR],
      host: {
        "[attr.minlength]": "_enabled ? minlength : null"
      },
      standalone: false
    }]
  }], null, {
    minlength: [{
      type: Input
    }]
  });
})();
var MAX_LENGTH_VALIDATOR = {
  provide: NG_VALIDATORS,
  useExisting: forwardRef(() => MaxLengthValidator),
  multi: true
};
var MaxLengthValidator = class _MaxLengthValidator extends AbstractValidatorDirective {
  /**
   * @description
   * Tracks changes to the maximum length bound to this directive.
   */
  maxlength;
  /** @internal */
  inputName = "maxlength";
  /** @internal */
  normalizeInput = (input2) => toInteger(input2);
  /** @internal */
  createValidator = (maxlength) => maxLengthValidator(maxlength);
  static ɵfac = /* @__PURE__ */ (() => {
    let ɵMaxLengthValidator_BaseFactory;
    return function MaxLengthValidator_Factory(__ngFactoryType__) {
      return (ɵMaxLengthValidator_BaseFactory || (ɵMaxLengthValidator_BaseFactory = ɵɵgetInheritedFactory(_MaxLengthValidator)))(__ngFactoryType__ || _MaxLengthValidator);
    };
  })();
  static ɵdir = ɵɵdefineDirective({
    type: _MaxLengthValidator,
    selectors: [["", "maxlength", "", "formControlName", ""], ["", "maxlength", "", "formControl", ""], ["", "maxlength", "", "ngModel", ""]],
    hostVars: 1,
    hostBindings: function MaxLengthValidator_HostBindings(rf, ctx) {
      if (rf & 2) {
        ɵɵattribute("maxlength", ctx._enabled ? ctx.maxlength : null);
      }
    },
    inputs: {
      maxlength: "maxlength"
    },
    standalone: false,
    features: [ɵɵProvidersFeature([MAX_LENGTH_VALIDATOR]), ɵɵInheritDefinitionFeature]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(MaxLengthValidator, [{
    type: Directive,
    args: [{
      selector: "[maxlength][formControlName],[maxlength][formControl],[maxlength][ngModel]",
      providers: [MAX_LENGTH_VALIDATOR],
      host: {
        "[attr.maxlength]": "_enabled ? maxlength : null"
      },
      standalone: false
    }]
  }], null, {
    maxlength: [{
      type: Input
    }]
  });
})();
var PATTERN_VALIDATOR = {
  provide: NG_VALIDATORS,
  useExisting: forwardRef(() => PatternValidator),
  multi: true
};
var PatternValidator = class _PatternValidator extends AbstractValidatorDirective {
  /**
   * @description
   * Tracks changes to the pattern bound to this directive.
   */
  pattern;
  // This input is always defined, since the name matches selector.
  /** @internal */
  inputName = "pattern";
  /** @internal */
  normalizeInput = (input2) => input2;
  /** @internal */
  createValidator = (input2) => patternValidator(input2);
  static ɵfac = /* @__PURE__ */ (() => {
    let ɵPatternValidator_BaseFactory;
    return function PatternValidator_Factory(__ngFactoryType__) {
      return (ɵPatternValidator_BaseFactory || (ɵPatternValidator_BaseFactory = ɵɵgetInheritedFactory(_PatternValidator)))(__ngFactoryType__ || _PatternValidator);
    };
  })();
  static ɵdir = ɵɵdefineDirective({
    type: _PatternValidator,
    selectors: [["", "pattern", "", "formControlName", ""], ["", "pattern", "", "formControl", ""], ["", "pattern", "", "ngModel", ""]],
    hostVars: 1,
    hostBindings: function PatternValidator_HostBindings(rf, ctx) {
      if (rf & 2) {
        ɵɵattribute("pattern", ctx._enabled ? ctx.pattern : null);
      }
    },
    inputs: {
      pattern: "pattern"
    },
    standalone: false,
    features: [ɵɵProvidersFeature([PATTERN_VALIDATOR]), ɵɵInheritDefinitionFeature]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PatternValidator, [{
    type: Directive,
    args: [{
      selector: "[pattern][formControlName],[pattern][formControl],[pattern][ngModel]",
      providers: [PATTERN_VALIDATOR],
      host: {
        "[attr.pattern]": "_enabled ? pattern : null"
      },
      standalone: false
    }]
  }], null, {
    pattern: [{
      type: Input
    }]
  });
})();
var SHARED_FORM_DIRECTIVES = [ɵNgNoValidate, NgSelectOption, ɵNgSelectMultipleOption, DefaultValueAccessor, NumberValueAccessor, RangeValueAccessor, CheckboxControlValueAccessor, SelectControlValueAccessor, SelectMultipleControlValueAccessor, RadioControlValueAccessor, NgControlStatus, NgControlStatusGroup, RequiredValidator, MinLengthValidator, MaxLengthValidator, PatternValidator, CheckboxRequiredValidator, EmailValidator, MinValidator, MaxValidator];
var TEMPLATE_DRIVEN_DIRECTIVES = [NgModel, NgModelGroup, NgForm];
var REACTIVE_DRIVEN_DIRECTIVES = [FormControlDirective, FormGroupDirective, FormControlName, FormGroupName, FormArrayName];
var ɵInternalFormsSharedModule = class _ɵInternalFormsSharedModule {
  static ɵfac = function ɵInternalFormsSharedModule_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _ɵInternalFormsSharedModule)();
  };
  static ɵmod = ɵɵdefineNgModule({
    type: _ɵInternalFormsSharedModule,
    declarations: [ɵNgNoValidate, NgSelectOption, ɵNgSelectMultipleOption, DefaultValueAccessor, NumberValueAccessor, RangeValueAccessor, CheckboxControlValueAccessor, SelectControlValueAccessor, SelectMultipleControlValueAccessor, RadioControlValueAccessor, NgControlStatus, NgControlStatusGroup, RequiredValidator, MinLengthValidator, MaxLengthValidator, PatternValidator, CheckboxRequiredValidator, EmailValidator, MinValidator, MaxValidator],
    exports: [ɵNgNoValidate, NgSelectOption, ɵNgSelectMultipleOption, DefaultValueAccessor, NumberValueAccessor, RangeValueAccessor, CheckboxControlValueAccessor, SelectControlValueAccessor, SelectMultipleControlValueAccessor, RadioControlValueAccessor, NgControlStatus, NgControlStatusGroup, RequiredValidator, MinLengthValidator, MaxLengthValidator, PatternValidator, CheckboxRequiredValidator, EmailValidator, MinValidator, MaxValidator]
  });
  static ɵinj = ɵɵdefineInjector({});
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(ɵInternalFormsSharedModule, [{
    type: NgModule,
    args: [{
      declarations: SHARED_FORM_DIRECTIVES,
      exports: SHARED_FORM_DIRECTIVES
    }]
  }], null, null);
})();
var FormArray = class extends AbstractControl {
  /**
   * Creates a new `FormArray` instance.
   *
   * @param controls An array of child controls. Each child control is given an index
   * where it is registered.
   *
   * @param validatorOrOpts A synchronous validator function, or an array of
   * such functions, or an `AbstractControlOptions` object that contains validation functions
   * and a validation trigger.
   *
   * @param asyncValidator A single async validator or array of async validator functions
   *
   */
  constructor(controls, validatorOrOpts, asyncValidator) {
    super(pickValidators(validatorOrOpts), pickAsyncValidators(asyncValidator, validatorOrOpts));
    this.controls = controls;
    this._initObservables();
    this._setUpdateStrategy(validatorOrOpts);
    this._setUpControls();
    this.updateValueAndValidity({
      onlySelf: true,
      // If `asyncValidator` is present, it will trigger control status change from `PENDING` to
      // `VALID` or `INVALID`.
      // The status should be broadcasted via the `statusChanges` observable, so we set `emitEvent`
      // to `true` to allow that during the control creation process.
      emitEvent: !!this.asyncValidator
    });
  }
  controls;
  /**
   * Get the `AbstractControl` at the given `index` in the array.
   *
   * @param index Index in the array to retrieve the control. If `index` is negative, it will wrap
   *     around from the back, and if index is greatly negative (less than `-length`), the result is
   * undefined. This behavior is the same as `Array.at(index)`.
   */
  at(index) {
    return this.controls[this._adjustIndex(index)];
  }
  /**
   * Insert a new `AbstractControl` at the end of the array.
   *
   * @param control Form control to be inserted
   * @param options Specifies whether this FormArray instance should emit events after a new
   *     control is added.
   * * `emitEvent`: When true or not supplied (the default), both the `statusChanges` and
   * `valueChanges` observables emit events with the latest status and value when the control is
   * inserted. When false, no events are emitted.
   */
  push(control, options = {}) {
    this.controls.push(control);
    this._registerControl(control);
    this.updateValueAndValidity({
      emitEvent: options.emitEvent
    });
    this._onCollectionChange();
  }
  /**
   * Insert a new `AbstractControl` at the given `index` in the array.
   *
   * @param index Index in the array to insert the control. If `index` is negative, wraps around
   *     from the back. If `index` is greatly negative (less than `-length`), prepends to the array.
   * This behavior is the same as `Array.splice(index, 0, control)`.
   * @param control Form control to be inserted
   * @param options Specifies whether this FormArray instance should emit events after a new
   *     control is inserted.
   * * `emitEvent`: When true or not supplied (the default), both the `statusChanges` and
   * `valueChanges` observables emit events with the latest status and value when the control is
   * inserted. When false, no events are emitted.
   */
  insert(index, control, options = {}) {
    this.controls.splice(index, 0, control);
    this._registerControl(control);
    this.updateValueAndValidity({
      emitEvent: options.emitEvent
    });
  }
  /**
   * Remove the control at the given `index` in the array.
   *
   * @param index Index in the array to remove the control.  If `index` is negative, wraps around
   *     from the back. If `index` is greatly negative (less than `-length`), removes the first
   *     element. This behavior is the same as `Array.splice(index, 1)`.
   * @param options Specifies whether this FormArray instance should emit events after a
   *     control is removed.
   * * `emitEvent`: When true or not supplied (the default), both the `statusChanges` and
   * `valueChanges` observables emit events with the latest status and value when the control is
   * removed. When false, no events are emitted.
   */
  removeAt(index, options = {}) {
    let adjustedIndex = this._adjustIndex(index);
    if (adjustedIndex < 0) adjustedIndex = 0;
    if (this.controls[adjustedIndex]) this.controls[adjustedIndex]._registerOnCollectionChange(() => {
    });
    this.controls.splice(adjustedIndex, 1);
    this.updateValueAndValidity({
      emitEvent: options.emitEvent
    });
  }
  /**
   * Replace an existing control.
   *
   * @param index Index in the array to replace the control. If `index` is negative, wraps around
   *     from the back. If `index` is greatly negative (less than `-length`), replaces the first
   *     element. This behavior is the same as `Array.splice(index, 1, control)`.
   * @param control The `AbstractControl` control to replace the existing control
   * @param options Specifies whether this FormArray instance should emit events after an
   *     existing control is replaced with a new one.
   * * `emitEvent`: When true or not supplied (the default), both the `statusChanges` and
   * `valueChanges` observables emit events with the latest status and value when the control is
   * replaced with a new one. When false, no events are emitted.
   */
  setControl(index, control, options = {}) {
    let adjustedIndex = this._adjustIndex(index);
    if (adjustedIndex < 0) adjustedIndex = 0;
    if (this.controls[adjustedIndex]) this.controls[adjustedIndex]._registerOnCollectionChange(() => {
    });
    this.controls.splice(adjustedIndex, 1);
    if (control) {
      this.controls.splice(adjustedIndex, 0, control);
      this._registerControl(control);
    }
    this.updateValueAndValidity({
      emitEvent: options.emitEvent
    });
    this._onCollectionChange();
  }
  /**
   * Length of the control array.
   */
  get length() {
    return this.controls.length;
  }
  /**
   * Sets the value of the `FormArray`. It accepts an array that matches
   * the structure of the control.
   *
   * This method performs strict checks, and throws an error if you try
   * to set the value of a control that doesn't exist or if you exclude the
   * value of a control.
   *
   * @usageNotes
   * ### Set the values for the controls in the form array
   *
   * ```ts
   * const arr = new FormArray([
   *   new FormControl(),
   *   new FormControl()
   * ]);
   * console.log(arr.value);   // [null, null]
   *
   * arr.setValue(['Nancy', 'Drew']);
   * console.log(arr.value);   // ['Nancy', 'Drew']
   * ```
   *
   * @param value Array of values for the controls
   * @param options Configure options that determine how the control propagates changes and
   * emits events after the value changes
   *
   * * `onlySelf`: When true, each change only affects this control, and not its parent. Default
   * is false.
   * * `emitEvent`: When true or not supplied (the default), both the `statusChanges` and
   * `valueChanges`
   * observables emit events with the latest status and value when the control value is updated.
   * When false, no events are emitted.
   * The configuration options are passed to the {@link AbstractControl#updateValueAndValidity
   * updateValueAndValidity} method.
   */
  setValue(value, options = {}) {
    assertAllValuesPresent(this, false, value);
    value.forEach((newValue, index) => {
      assertControlPresent(this, false, index);
      this.at(index).setValue(newValue, {
        onlySelf: true,
        emitEvent: options.emitEvent
      });
    });
    this.updateValueAndValidity(options);
  }
  /**
   * Patches the value of the `FormArray`. It accepts an array that matches the
   * structure of the control, and does its best to match the values to the correct
   * controls in the group.
   *
   * It accepts both super-sets and sub-sets of the array without throwing an error.
   *
   * @usageNotes
   * ### Patch the values for controls in a form array
   *
   * ```ts
   * const arr = new FormArray([
   *    new FormControl(),
   *    new FormControl()
   * ]);
   * console.log(arr.value);   // [null, null]
   *
   * arr.patchValue(['Nancy']);
   * console.log(arr.value);   // ['Nancy', null]
   * ```
   *
   * @param value Array of latest values for the controls
   * @param options Configure options that determine how the control propagates changes and
   * emits events after the value changes
   *
   * * `onlySelf`: When true, each change only affects this control, and not its parent. Default
   * is false.
   * * `emitEvent`: When true or not supplied (the default), both the `statusChanges` and
   * `valueChanges` observables emit events with the latest status and value when the control
   * value is updated. When false, no events are emitted. The configuration options are passed to
   * the {@link AbstractControl#updateValueAndValidity updateValueAndValidity} method.
   */
  patchValue(value, options = {}) {
    if (value == null) return;
    value.forEach((newValue, index) => {
      if (this.at(index)) {
        this.at(index).patchValue(newValue, {
          onlySelf: true,
          emitEvent: options.emitEvent
        });
      }
    });
    this.updateValueAndValidity(options);
  }
  /**
   * Resets the `FormArray` and all descendants are marked `pristine` and `untouched`, and the
   * value of all descendants to null or null maps.
   *
   * You reset to a specific form state by passing in an array of states
   * that matches the structure of the control. The state is a standalone value
   * or a form state object with both a value and a disabled status.
   *
   * @usageNotes
   * ### Reset the values in a form array
   *
   * ```ts
   * const arr = new FormArray([
   *    new FormControl(),
   *    new FormControl()
   * ]);
   * arr.reset(['name', 'last name']);
   *
   * console.log(arr.value);  // ['name', 'last name']
   * ```
   *
   * ### Reset the values in a form array and the disabled status for the first control
   *
   * ```ts
   * arr.reset([
   *   {value: 'name', disabled: true},
   *   'last'
   * ]);
   *
   * console.log(arr.value);  // ['last']
   * console.log(arr.at(0).status);  // 'DISABLED'
   * ```
   *
   * @param value Array of values for the controls
   * @param options Configure options that determine how the control propagates changes and
   * emits events after the value changes
   *
   * * `onlySelf`: When true, each change only affects this control, and not its parent. Default
   * is false.
   * * `emitEvent`: When true or not supplied (the default), both the `statusChanges` and
   * `valueChanges`
   * observables emit events with the latest status and value when the control is reset.
   * When false, no events are emitted.
   * The configuration options are passed to the {@link AbstractControl#updateValueAndValidity
   * updateValueAndValidity} method.
   */
  reset(value = [], options = {}) {
    this._forEachChild((control, index) => {
      control.reset(value[index], {
        onlySelf: true,
        emitEvent: options.emitEvent
      });
    });
    this._updatePristine(options, this);
    this._updateTouched(options, this);
    this.updateValueAndValidity(options);
  }
  /**
   * The aggregate value of the array, including any disabled controls.
   *
   * Reports all values regardless of disabled status.
   */
  getRawValue() {
    return this.controls.map((control) => control.getRawValue());
  }
  /**
   * Remove all controls in the `FormArray`.
   *
   * @param options Specifies whether this FormArray instance should emit events after all
   *     controls are removed.
   * * `emitEvent`: When true or not supplied (the default), both the `statusChanges` and
   * `valueChanges` observables emit events with the latest status and value when all controls
   * in this FormArray instance are removed. When false, no events are emitted.
   *
   * @usageNotes
   * ### Remove all elements from a FormArray
   *
   * ```ts
   * const arr = new FormArray([
   *    new FormControl(),
   *    new FormControl()
   * ]);
   * console.log(arr.length);  // 2
   *
   * arr.clear();
   * console.log(arr.length);  // 0
   * ```
   *
   * It's a simpler and more efficient alternative to removing all elements one by one:
   *
   * ```ts
   * const arr = new FormArray([
   *    new FormControl(),
   *    new FormControl()
   * ]);
   *
   * while (arr.length) {
   *    arr.removeAt(0);
   * }
   * ```
   */
  clear(options = {}) {
    if (this.controls.length < 1) return;
    this._forEachChild((control) => control._registerOnCollectionChange(() => {
    }));
    this.controls.splice(0);
    this.updateValueAndValidity({
      emitEvent: options.emitEvent
    });
  }
  /**
   * Adjusts a negative index by summing it with the length of the array. For very negative
   * indices, the result may remain negative.
   * @internal
   */
  _adjustIndex(index) {
    return index < 0 ? index + this.length : index;
  }
  /** @internal */
  _syncPendingControls() {
    let subtreeUpdated = this.controls.reduce((updated, child) => {
      return child._syncPendingControls() ? true : updated;
    }, false);
    if (subtreeUpdated) this.updateValueAndValidity({
      onlySelf: true
    });
    return subtreeUpdated;
  }
  /** @internal */
  _forEachChild(cb) {
    this.controls.forEach((control, index) => {
      cb(control, index);
    });
  }
  /** @internal */
  _updateValue() {
    this.value = this.controls.filter((control) => control.enabled || this.disabled).map((control) => control.value);
  }
  /** @internal */
  _anyControls(condition) {
    return this.controls.some((control) => control.enabled && condition(control));
  }
  /** @internal */
  _setUpControls() {
    this._forEachChild((control) => this._registerControl(control));
  }
  /** @internal */
  _allControlsDisabled() {
    for (const control of this.controls) {
      if (control.enabled) return false;
    }
    return this.controls.length > 0 || this.disabled;
  }
  _registerControl(control) {
    control.setParent(this);
    control._registerOnCollectionChange(this._onCollectionChange);
  }
  /** @internal */
  _find(name) {
    return this.at(name) ?? null;
  }
};
var UntypedFormArray = FormArray;
function isAbstractControlOptions(options) {
  return !!options && (options.asyncValidators !== void 0 || options.validators !== void 0 || options.updateOn !== void 0);
}
var FormBuilder = class _FormBuilder {
  useNonNullable = false;
  /**
   * @description
   * Returns a FormBuilder in which automatically constructed `FormControl` elements
   * have `{nonNullable: true}` and are non-nullable.
   *
   * **Constructing non-nullable controls**
   *
   * When constructing a control, it will be non-nullable, and will reset to its initial value.
   *
   * ```ts
   * let nnfb = new FormBuilder().nonNullable;
   * let name = nnfb.control('Alex'); // FormControl<string>
   * name.reset();
   * console.log(name); // 'Alex'
   * ```
   *
   * **Constructing non-nullable groups or arrays**
   *
   * When constructing a group or array, all automatically created inner controls will be
   * non-nullable, and will reset to their initial values.
   *
   * ```ts
   * let nnfb = new FormBuilder().nonNullable;
   * let name = nnfb.group({who: 'Alex'}); // FormGroup<{who: FormControl<string>}>
   * name.reset();
   * console.log(name); // {who: 'Alex'}
   * ```
   * **Constructing *nullable* fields on groups or arrays**
   *
   * It is still possible to have a nullable field. In particular, any `FormControl` which is
   * *already* constructed will not be altered. For example:
   *
   * ```ts
   * let nnfb = new FormBuilder().nonNullable;
   * // FormGroup<{who: FormControl<string|null>}>
   * let name = nnfb.group({who: new FormControl('Alex')});
   * name.reset(); console.log(name); // {who: null}
   * ```
   *
   * Because the inner control is constructed explicitly by the caller, the builder has
   * no control over how it is created, and cannot exclude the `null`.
   */
  get nonNullable() {
    const nnfb = new _FormBuilder();
    nnfb.useNonNullable = true;
    return nnfb;
  }
  group(controls, options = null) {
    const reducedControls = this._reduceControls(controls);
    let newOptions = {};
    if (isAbstractControlOptions(options)) {
      newOptions = options;
    } else if (options !== null) {
      newOptions.validators = options.validator;
      newOptions.asyncValidators = options.asyncValidator;
    }
    return new FormGroup(reducedControls, newOptions);
  }
  /**
   * @description
   * Constructs a new `FormRecord` instance. Accepts a single generic argument, which is an object
   * containing all the keys and corresponding inner control types.
   *
   * @param controls A collection of child controls. The key for each child is the name
   * under which it is registered.
   *
   * @param options Configuration options object for the `FormRecord`. The object should have the
   * `AbstractControlOptions` type and might contain the following fields:
   * * `validators`: A synchronous validator function, or an array of validator functions.
   * * `asyncValidators`: A single async validator or array of async validator functions.
   * * `updateOn`: The event upon which the control should be updated (options: 'change' | 'blur'
   * | submit').
   */
  record(controls, options = null) {
    const reducedControls = this._reduceControls(controls);
    return new FormRecord(reducedControls, options);
  }
  /**
   * @description
   * Constructs a new `FormControl` with the given state, validators and options. Sets
   * `{nonNullable: true}` in the options to get a non-nullable control. Otherwise, the
   * control will be nullable. Accepts a single generic argument, which is the type  of the
   * control's value.
   *
   * @param formState Initializes the control with an initial state value, or
   * with an object that contains both a value and a disabled status.
   *
   * @param validatorOrOpts A synchronous validator function, or an array of
   * such functions, or a `FormControlOptions` object that contains
   * validation functions and a validation trigger.
   *
   * @param asyncValidator A single async validator or array of async validator
   * functions.
   *
   * @usageNotes
   *
   * ### Initialize a control as disabled
   *
   * The following example returns a control with an initial value in a disabled state.
   *
   * {@example forms/ts/formBuilder/form_builder_example.ts region='disabled-control'}
   */
  control(formState, validatorOrOpts, asyncValidator) {
    let newOptions = {};
    if (!this.useNonNullable) {
      return new FormControl(formState, validatorOrOpts, asyncValidator);
    }
    if (isAbstractControlOptions(validatorOrOpts)) {
      newOptions = validatorOrOpts;
    } else {
      newOptions.validators = validatorOrOpts;
      newOptions.asyncValidators = asyncValidator;
    }
    return new FormControl(formState, __spreadProps(__spreadValues({}, newOptions), {
      nonNullable: true
    }));
  }
  /**
   * Constructs a new `FormArray` from the given array of configurations,
   * validators and options. Accepts a single generic argument, which is the type of each control
   * inside the array.
   *
   * @param controls An array of child controls or control configs. Each child control is given an
   *     index when it is registered.
   *
   * @param validatorOrOpts A synchronous validator function, or an array of such functions, or an
   *     `AbstractControlOptions` object that contains
   * validation functions and a validation trigger.
   *
   * @param asyncValidator A single async validator or array of async validator functions.
   */
  array(controls, validatorOrOpts, asyncValidator) {
    const createdControls = controls.map((c) => this._createControl(c));
    return new FormArray(createdControls, validatorOrOpts, asyncValidator);
  }
  /** @internal */
  _reduceControls(controls) {
    const createdControls = {};
    Object.keys(controls).forEach((controlName) => {
      createdControls[controlName] = this._createControl(controls[controlName]);
    });
    return createdControls;
  }
  /** @internal */
  _createControl(controls) {
    if (controls instanceof FormControl) {
      return controls;
    } else if (controls instanceof AbstractControl) {
      return controls;
    } else if (Array.isArray(controls)) {
      const value = controls[0];
      const validator = controls.length > 1 ? controls[1] : null;
      const asyncValidator = controls.length > 2 ? controls[2] : null;
      return this.control(value, validator, asyncValidator);
    } else {
      return this.control(controls);
    }
  }
  static ɵfac = function FormBuilder_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _FormBuilder)();
  };
  static ɵprov = ɵɵdefineInjectable({
    token: _FormBuilder,
    factory: _FormBuilder.ɵfac,
    providedIn: "root"
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(FormBuilder, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], null, null);
})();
var NonNullableFormBuilder = class _NonNullableFormBuilder {
  static ɵfac = function NonNullableFormBuilder_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _NonNullableFormBuilder)();
  };
  static ɵprov = ɵɵdefineInjectable({
    token: _NonNullableFormBuilder,
    factory: () => (() => inject(FormBuilder).nonNullable)(),
    providedIn: "root"
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(NonNullableFormBuilder, [{
    type: Injectable,
    args: [{
      providedIn: "root",
      useFactory: () => inject(FormBuilder).nonNullable
    }]
  }], null, null);
})();
var UntypedFormBuilder = class _UntypedFormBuilder extends FormBuilder {
  group(controlsConfig, options = null) {
    return super.group(controlsConfig, options);
  }
  /**
   * Like `FormBuilder#control`, except the resulting control is untyped.
   */
  control(formState, validatorOrOpts, asyncValidator) {
    return super.control(formState, validatorOrOpts, asyncValidator);
  }
  /**
   * Like `FormBuilder#array`, except the resulting array is untyped.
   */
  array(controlsConfig, validatorOrOpts, asyncValidator) {
    return super.array(controlsConfig, validatorOrOpts, asyncValidator);
  }
  static ɵfac = /* @__PURE__ */ (() => {
    let ɵUntypedFormBuilder_BaseFactory;
    return function UntypedFormBuilder_Factory(__ngFactoryType__) {
      return (ɵUntypedFormBuilder_BaseFactory || (ɵUntypedFormBuilder_BaseFactory = ɵɵgetInheritedFactory(_UntypedFormBuilder)))(__ngFactoryType__ || _UntypedFormBuilder);
    };
  })();
  static ɵprov = ɵɵdefineInjectable({
    token: _UntypedFormBuilder,
    factory: _UntypedFormBuilder.ɵfac,
    providedIn: "root"
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(UntypedFormBuilder, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], null, null);
})();
var VERSION = new Version("19.2.19");
var FormsModule = class _FormsModule {
  /**
   * @description
   * Provides options for configuring the forms module.
   *
   * @param opts An object of configuration options
   * * `callSetDisabledState` Configures whether to `always` call `setDisabledState`, which is more
   * correct, or to only call it `whenDisabled`, which is the legacy behavior.
   */
  static withConfig(opts) {
    return {
      ngModule: _FormsModule,
      providers: [{
        provide: CALL_SET_DISABLED_STATE,
        useValue: opts.callSetDisabledState ?? setDisabledStateDefault
      }]
    };
  }
  static ɵfac = function FormsModule_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _FormsModule)();
  };
  static ɵmod = ɵɵdefineNgModule({
    type: _FormsModule,
    declarations: [NgModel, NgModelGroup, NgForm],
    exports: [ɵInternalFormsSharedModule, NgModel, NgModelGroup, NgForm]
  });
  static ɵinj = ɵɵdefineInjector({
    imports: [ɵInternalFormsSharedModule]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(FormsModule, [{
    type: NgModule,
    args: [{
      declarations: TEMPLATE_DRIVEN_DIRECTIVES,
      exports: [ɵInternalFormsSharedModule, TEMPLATE_DRIVEN_DIRECTIVES]
    }]
  }], null, null);
})();
var ReactiveFormsModule = class _ReactiveFormsModule {
  /**
   * @description
   * Provides options for configuring the reactive forms module.
   *
   * @param opts An object of configuration options
   * * `warnOnNgModelWithFormControl` Configures when to emit a warning when an `ngModel`
   * binding is used with reactive form directives.
   * * `callSetDisabledState` Configures whether to `always` call `setDisabledState`, which is more
   * correct, or to only call it `whenDisabled`, which is the legacy behavior.
   */
  static withConfig(opts) {
    return {
      ngModule: _ReactiveFormsModule,
      providers: [{
        provide: NG_MODEL_WITH_FORM_CONTROL_WARNING,
        useValue: opts.warnOnNgModelWithFormControl ?? "always"
      }, {
        provide: CALL_SET_DISABLED_STATE,
        useValue: opts.callSetDisabledState ?? setDisabledStateDefault
      }]
    };
  }
  static ɵfac = function ReactiveFormsModule_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _ReactiveFormsModule)();
  };
  static ɵmod = ɵɵdefineNgModule({
    type: _ReactiveFormsModule,
    declarations: [FormControlDirective, FormGroupDirective, FormControlName, FormGroupName, FormArrayName],
    exports: [ɵInternalFormsSharedModule, FormControlDirective, FormGroupDirective, FormControlName, FormGroupName, FormArrayName]
  });
  static ɵinj = ɵɵdefineInjector({
    imports: [ɵInternalFormsSharedModule]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(ReactiveFormsModule, [{
    type: NgModule,
    args: [{
      declarations: [REACTIVE_DRIVEN_DIRECTIVES],
      exports: [ɵInternalFormsSharedModule, REACTIVE_DRIVEN_DIRECTIVES]
    }]
  }], null, null);
})();

// node_modules/@ng-select/ng-select/fesm2022/ng-select-ng-select.mjs
var _c0 = ["content"];
var _c1 = ["scroll"];
var _c2 = ["padding"];
var _c3 = ["*"];
var _c4 = (a0) => ({
  searchTerm: a0
});
function NgDropdownPanelComponent_Conditional_0_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div", 3);
    ɵɵelementContainer(1, 6);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    ɵɵadvance();
    ɵɵproperty("ngTemplateOutlet", ctx_r0.headerTemplate)("ngTemplateOutletContext", ɵɵpureFunction1(2, _c4, ctx_r0.filterValue));
  }
}
function NgDropdownPanelComponent_Conditional_8_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div", 5);
    ɵɵelementContainer(1, 6);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    ɵɵadvance();
    ɵɵproperty("ngTemplateOutlet", ctx_r0.footerTemplate)("ngTemplateOutletContext", ɵɵpureFunction1(2, _c4, ctx_r0.filterValue));
  }
}
var _c5 = ["searchInput"];
var _c6 = ["clearButton"];
var _c7 = (a0, a1, a2) => ({
  item: a0,
  clear: a1,
  label: a2
});
var _c8 = (a0, a1) => ({
  items: a0,
  clear: a1
});
var _c9 = (a0, a1, a2, a3) => ({
  item: a0,
  item$: a1,
  index: a2,
  searchTerm: a3
});
function NgSelectComponent_Conditional_2_ng_template_0_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div", 20);
    ɵɵtext(1);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext(2);
    ɵɵadvance();
    ɵɵtextInterpolate(ctx_r1.placeholder);
  }
}
function NgSelectComponent_Conditional_2_ng_template_2_Template(rf, ctx) {
}
function NgSelectComponent_Conditional_2_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵtemplate(0, NgSelectComponent_Conditional_2_ng_template_0_Template, 2, 1, "ng-template", null, 1, ɵɵtemplateRefExtractor)(2, NgSelectComponent_Conditional_2_ng_template_2_Template, 0, 0, "ng-template", 19);
  }
  if (rf & 2) {
    const defaultPlaceholderTemplate_r3 = ɵɵreference(1);
    const ctx_r1 = ɵɵnextContext();
    ɵɵadvance(2);
    ɵɵproperty("ngTemplateOutlet", ctx_r1.placeholderTemplate || defaultPlaceholderTemplate_r3);
  }
}
function NgSelectComponent_Conditional_3_For_1_ng_template_1_Template(rf, ctx) {
  if (rf & 1) {
    const _r4 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "span", 23);
    ɵɵlistener("click", function NgSelectComponent_Conditional_3_For_1_ng_template_1_Template_span_click_0_listener() {
      ɵɵrestoreView(_r4);
      const item_r5 = ɵɵnextContext().$implicit;
      const ctx_r1 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r1.unselect(item_r5));
    });
    ɵɵtext(1, "×");
    ɵɵelementEnd();
    ɵɵelement(2, "span", 24);
  }
  if (rf & 2) {
    const item_r5 = ɵɵnextContext().$implicit;
    const ctx_r1 = ɵɵnextContext(2);
    ɵɵadvance(2);
    ɵɵproperty("ngItemLabel", item_r5.label)("escape", ctx_r1.escapeHTML);
  }
}
function NgSelectComponent_Conditional_3_For_1_ng_template_3_Template(rf, ctx) {
}
function NgSelectComponent_Conditional_3_For_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div", 22);
    ɵɵtemplate(1, NgSelectComponent_Conditional_3_For_1_ng_template_1_Template, 3, 2, "ng-template", null, 2, ɵɵtemplateRefExtractor)(3, NgSelectComponent_Conditional_3_For_1_ng_template_3_Template, 0, 0, "ng-template", 12);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const item_r5 = ctx.$implicit;
    const defaultLabelTemplate_r6 = ɵɵreference(2);
    const ctx_r1 = ɵɵnextContext(2);
    ɵɵclassProp("ng-value-disabled", item_r5.disabled);
    ɵɵadvance(3);
    ɵɵproperty("ngTemplateOutlet", ctx_r1.labelTemplate || defaultLabelTemplate_r6)("ngTemplateOutletContext", ɵɵpureFunction3(4, _c7, item_r5.value, ctx_r1.clearItem, item_r5.label));
  }
}
function NgSelectComponent_Conditional_3_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵrepeaterCreate(0, NgSelectComponent_Conditional_3_For_1_Template, 4, 8, "div", 21, ɵɵcomponentInstance().trackByOption, true);
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext();
    ɵɵrepeater(ctx_r1.selectedItems);
  }
}
function NgSelectComponent_Conditional_4_ng_template_0_Template(rf, ctx) {
}
function NgSelectComponent_Conditional_4_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵtemplate(0, NgSelectComponent_Conditional_4_ng_template_0_Template, 0, 0, "ng-template", 12);
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext();
    ɵɵproperty("ngTemplateOutlet", ctx_r1.multiLabelTemplate)("ngTemplateOutletContext", ɵɵpureFunction2(2, _c8, ctx_r1.selectedValues, ctx_r1.clearItem));
  }
}
function NgSelectComponent_Conditional_8_ng_template_0_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "div", 25);
  }
}
function NgSelectComponent_Conditional_8_ng_template_2_Template(rf, ctx) {
}
function NgSelectComponent_Conditional_8_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵtemplate(0, NgSelectComponent_Conditional_8_ng_template_0_Template, 1, 0, "ng-template", null, 3, ɵɵtemplateRefExtractor)(2, NgSelectComponent_Conditional_8_ng_template_2_Template, 0, 0, "ng-template", 19);
  }
  if (rf & 2) {
    const defaultLoadingSpinnerTemplate_r8 = ɵɵreference(1);
    const ctx_r1 = ɵɵnextContext();
    ɵɵadvance(2);
    ɵɵproperty("ngTemplateOutlet", ctx_r1.loadingSpinnerTemplate || defaultLoadingSpinnerTemplate_r8);
  }
}
function NgSelectComponent_Conditional_9_Conditional_0_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainer(0, 19);
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext(2);
    ɵɵproperty("ngTemplateOutlet", ctx_r1.clearButtonTemplate);
  }
}
function NgSelectComponent_Conditional_9_Conditional_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "span", 26, 4)(2, "span", 27);
    ɵɵtext(3, "×");
    ɵɵelementEnd()();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext(2);
    ɵɵpropertyInterpolate("title", ctx_r1.clearAllText);
    ɵɵattribute("tabindex", ctx_r1.tabFocusOnClearButton() ? 0 : -1);
  }
}
function NgSelectComponent_Conditional_9_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵtemplate(0, NgSelectComponent_Conditional_9_Conditional_0_Template, 1, 1, "ng-container", 19)(1, NgSelectComponent_Conditional_9_Conditional_1_Template, 4, 2, "span", 26);
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext();
    ɵɵconditional(ctx_r1.clearButtonTemplate ? 0 : 1);
  }
}
function NgSelectComponent_Conditional_12_For_3_ng_template_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "span", 32);
  }
  if (rf & 2) {
    const item_r11 = ɵɵnextContext().$implicit;
    const ctx_r1 = ɵɵnextContext(2);
    ɵɵproperty("ngItemLabel", item_r11.label)("escape", ctx_r1.escapeHTML);
  }
}
function NgSelectComponent_Conditional_12_For_3_ng_template_3_Template(rf, ctx) {
}
function NgSelectComponent_Conditional_12_For_3_Template(rf, ctx) {
  if (rf & 1) {
    const _r10 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "div", 31);
    ɵɵlistener("click", function NgSelectComponent_Conditional_12_For_3_Template_div_click_0_listener() {
      const item_r11 = ɵɵrestoreView(_r10).$implicit;
      const ctx_r1 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r1.toggleItem(item_r11));
    })("mouseover", function NgSelectComponent_Conditional_12_For_3_Template_div_mouseover_0_listener() {
      const item_r11 = ɵɵrestoreView(_r10).$implicit;
      const ctx_r1 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r1.onItemHover(item_r11));
    });
    ɵɵtemplate(1, NgSelectComponent_Conditional_12_For_3_ng_template_1_Template, 1, 2, "ng-template", null, 5, ɵɵtemplateRefExtractor)(3, NgSelectComponent_Conditional_12_For_3_ng_template_3_Template, 0, 0, "ng-template", 12);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const item_r11 = ctx.$implicit;
    const defaultOptionTemplate_r12 = ɵɵreference(2);
    const ctx_r1 = ɵɵnextContext(2);
    ɵɵclassProp("ng-option-disabled", item_r11.disabled)("ng-option-selected", item_r11.selected)("ng-optgroup", item_r11.children)("ng-option", !item_r11.children)("ng-option-child", !!item_r11.parent)("ng-option-marked", item_r11 === ctx_r1.itemsList.markedItem);
    ɵɵattribute("role", item_r11.children ? "group" : "option")("aria-selected", item_r11.selected)("id", item_r11 == null ? null : item_r11.htmlId)("aria-setsize", ctx_r1.itemsList.filteredItems.length)("aria-posinset", item_r11.index + 1);
    ɵɵadvance(3);
    ɵɵproperty("ngTemplateOutlet", item_r11.children ? ctx_r1.optgroupTemplate || defaultOptionTemplate_r12 : ctx_r1.optionTemplate || defaultOptionTemplate_r12)("ngTemplateOutletContext", ɵɵpureFunction4(19, _c9, item_r11.value, item_r11, item_r11.index, ctx_r1.searchTerm));
  }
}
function NgSelectComponent_Conditional_12_Conditional_4_ng_template_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "span")(1, "span", 34);
    ɵɵtext(2);
    ɵɵelementEnd();
    ɵɵtext(3);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext(3);
    ɵɵadvance(2);
    ɵɵtextInterpolate(ctx_r1.addTagText);
    ɵɵadvance();
    ɵɵtextInterpolate1('"', ctx_r1.searchTerm, '"');
  }
}
function NgSelectComponent_Conditional_12_Conditional_4_ng_template_3_Template(rf, ctx) {
}
function NgSelectComponent_Conditional_12_Conditional_4_Template(rf, ctx) {
  if (rf & 1) {
    const _r13 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "div", 33);
    ɵɵlistener("mouseover", function NgSelectComponent_Conditional_12_Conditional_4_Template_div_mouseover_0_listener() {
      ɵɵrestoreView(_r13);
      const ctx_r1 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r1.itemsList.unmarkItem());
    })("click", function NgSelectComponent_Conditional_12_Conditional_4_Template_div_click_0_listener() {
      ɵɵrestoreView(_r13);
      const ctx_r1 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r1.selectTag());
    });
    ɵɵtemplate(1, NgSelectComponent_Conditional_12_Conditional_4_ng_template_1_Template, 4, 2, "ng-template", null, 6, ɵɵtemplateRefExtractor)(3, NgSelectComponent_Conditional_12_Conditional_4_ng_template_3_Template, 0, 0, "ng-template", 12);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const defaultTagTemplate_r14 = ɵɵreference(2);
    const ctx_r1 = ɵɵnextContext(2);
    ɵɵclassProp("ng-option-marked", !ctx_r1.itemsList.markedItem);
    ɵɵadvance(3);
    ɵɵproperty("ngTemplateOutlet", ctx_r1.tagTemplate || defaultTagTemplate_r14)("ngTemplateOutletContext", ɵɵpureFunction1(4, _c4, ctx_r1.searchTerm));
  }
}
function NgSelectComponent_Conditional_12_Conditional_5_ng_template_0_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div", 35);
    ɵɵtext(1);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext(3);
    ɵɵadvance();
    ɵɵtextInterpolate(ctx_r1.notFoundText);
  }
}
function NgSelectComponent_Conditional_12_Conditional_5_ng_template_2_Template(rf, ctx) {
}
function NgSelectComponent_Conditional_12_Conditional_5_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵtemplate(0, NgSelectComponent_Conditional_12_Conditional_5_ng_template_0_Template, 2, 1, "ng-template", null, 7, ɵɵtemplateRefExtractor)(2, NgSelectComponent_Conditional_12_Conditional_5_ng_template_2_Template, 0, 0, "ng-template", 12);
  }
  if (rf & 2) {
    const defaultNotFoundTemplate_r15 = ɵɵreference(1);
    const ctx_r1 = ɵɵnextContext(2);
    ɵɵadvance(2);
    ɵɵproperty("ngTemplateOutlet", ctx_r1.notFoundTemplate || defaultNotFoundTemplate_r15)("ngTemplateOutletContext", ɵɵpureFunction1(2, _c4, ctx_r1.searchTerm));
  }
}
function NgSelectComponent_Conditional_12_Conditional_6_ng_template_0_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div", 35);
    ɵɵtext(1);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext(3);
    ɵɵadvance();
    ɵɵtextInterpolate(ctx_r1.typeToSearchText);
  }
}
function NgSelectComponent_Conditional_12_Conditional_6_ng_template_2_Template(rf, ctx) {
}
function NgSelectComponent_Conditional_12_Conditional_6_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵtemplate(0, NgSelectComponent_Conditional_12_Conditional_6_ng_template_0_Template, 2, 1, "ng-template", null, 8, ɵɵtemplateRefExtractor)(2, NgSelectComponent_Conditional_12_Conditional_6_ng_template_2_Template, 0, 0, "ng-template", 19);
  }
  if (rf & 2) {
    const defaultTypeToSearchTemplate_r16 = ɵɵreference(1);
    const ctx_r1 = ɵɵnextContext(2);
    ɵɵadvance(2);
    ɵɵproperty("ngTemplateOutlet", ctx_r1.typeToSearchTemplate || defaultTypeToSearchTemplate_r16);
  }
}
function NgSelectComponent_Conditional_12_Conditional_7_ng_template_0_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div", 35);
    ɵɵtext(1);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext(3);
    ɵɵadvance();
    ɵɵtextInterpolate(ctx_r1.loadingText);
  }
}
function NgSelectComponent_Conditional_12_Conditional_7_ng_template_2_Template(rf, ctx) {
}
function NgSelectComponent_Conditional_12_Conditional_7_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵtemplate(0, NgSelectComponent_Conditional_12_Conditional_7_ng_template_0_Template, 2, 1, "ng-template", null, 9, ɵɵtemplateRefExtractor)(2, NgSelectComponent_Conditional_12_Conditional_7_ng_template_2_Template, 0, 0, "ng-template", 12);
  }
  if (rf & 2) {
    const defaultLoadingTextTemplate_r17 = ɵɵreference(1);
    const ctx_r1 = ɵɵnextContext(2);
    ɵɵadvance(2);
    ɵɵproperty("ngTemplateOutlet", ctx_r1.loadingTextTemplate || defaultLoadingTextTemplate_r17)("ngTemplateOutletContext", ɵɵpureFunction1(2, _c4, ctx_r1.searchTerm));
  }
}
function NgSelectComponent_Conditional_12_Template(rf, ctx) {
  if (rf & 1) {
    const _r9 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "ng-dropdown-panel", 28);
    ɵɵlistener("update", function NgSelectComponent_Conditional_12_Template_ng_dropdown_panel_update_0_listener($event) {
      ɵɵrestoreView(_r9);
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.viewPortItems = $event);
    })("scroll", function NgSelectComponent_Conditional_12_Template_ng_dropdown_panel_scroll_0_listener($event) {
      ɵɵrestoreView(_r9);
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.scroll.emit($event));
    })("scrollToEnd", function NgSelectComponent_Conditional_12_Template_ng_dropdown_panel_scrollToEnd_0_listener($event) {
      ɵɵrestoreView(_r9);
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.scrollToEnd.emit($event));
    })("outsideClick", function NgSelectComponent_Conditional_12_Template_ng_dropdown_panel_outsideClick_0_listener() {
      ɵɵrestoreView(_r9);
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.close());
    });
    ɵɵelementContainerStart(1);
    ɵɵrepeaterCreate(2, NgSelectComponent_Conditional_12_For_3_Template, 4, 24, "div", 29, ɵɵcomponentInstance().trackByOption, true);
    ɵɵtemplate(4, NgSelectComponent_Conditional_12_Conditional_4_Template, 4, 6, "div", 30);
    ɵɵelementContainerEnd();
    ɵɵtemplate(5, NgSelectComponent_Conditional_12_Conditional_5_Template, 3, 4)(6, NgSelectComponent_Conditional_12_Conditional_6_Template, 3, 1)(7, NgSelectComponent_Conditional_12_Conditional_7_Template, 3, 4);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext();
    ɵɵclassProp("ng-select-multiple", ctx_r1.multiple);
    ɵɵproperty("virtualScroll", ctx_r1.virtualScroll)("bufferAmount", ctx_r1.bufferAmount)("appendTo", ctx_r1.appendTo)("position", ctx_r1.dropdownPosition)("headerTemplate", ctx_r1.headerTemplate)("footerTemplate", ctx_r1.footerTemplate)("filterValue", ctx_r1.searchTerm)("items", ctx_r1.itemsList.filteredItems)("markedItem", ctx_r1.itemsList.markedItem)("ngClass", ctx_r1.appendTo ? ctx_r1.ngClass ? ctx_r1.ngClass : ctx_r1.classes : null)("id", ctx_r1.dropdownId)("ariaLabelDropdown", ctx_r1.ariaLabelDropdown);
    ɵɵadvance(2);
    ɵɵrepeater(ctx_r1.viewPortItems);
    ɵɵadvance(2);
    ɵɵconditional(ctx_r1.showAddTag ? 4 : -1);
    ɵɵadvance();
    ɵɵconditional(ctx_r1.showNoItemsFound() ? 5 : -1);
    ɵɵadvance();
    ɵɵconditional(ctx_r1.showTypeToSearch() ? 6 : -1);
    ɵɵadvance();
    ɵɵconditional(ctx_r1.loading && ctx_r1.itemsList.filteredItems.length === 0 ? 7 : -1);
  }
}
function NgSelectComponent_Conditional_14_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵtext(0);
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext();
    ɵɵtextInterpolate1(" ", ctx_r1.notFoundText, " ");
  }
}
var unescapedHTMLExp = /[&<>"']/g;
var hasUnescapedHTMLExp = RegExp(unescapedHTMLExp.source);
var htmlEscapes = {
  "&": "&amp;",
  "<": "&lt;",
  ">": "&gt;",
  '"': "&quot;",
  "'": "&#39;"
};
function escapeHTML(value) {
  return value && hasUnescapedHTMLExp.test(value) ? value.replace(unescapedHTMLExp, (chr) => htmlEscapes[chr]) : value;
}
function isDefined(value) {
  return value !== void 0 && value !== null;
}
function isObject2(value) {
  return typeof value === "object" && isDefined(value);
}
function isPromise2(value) {
  return value instanceof Promise;
}
function isFunction(value) {
  return value instanceof Function;
}
var NgItemLabelDirective = class _NgItemLabelDirective {
  constructor(element) {
    this.element = element;
    this.escape = true;
  }
  ngOnChanges(changes) {
    this.element.nativeElement.innerHTML = this.escape ? escapeHTML(this.ngItemLabel) : this.ngItemLabel;
  }
  static {
    this.ɵfac = function NgItemLabelDirective_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _NgItemLabelDirective)(ɵɵdirectiveInject(ElementRef));
    };
  }
  static {
    this.ɵdir = ɵɵdefineDirective({
      type: _NgItemLabelDirective,
      selectors: [["", "ngItemLabel", ""]],
      inputs: {
        ngItemLabel: "ngItemLabel",
        escape: "escape"
      },
      features: [ɵɵNgOnChangesFeature]
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(NgItemLabelDirective, [{
    type: Directive,
    args: [{
      selector: "[ngItemLabel]",
      standalone: true
    }]
  }], () => [{
    type: ElementRef
  }], {
    ngItemLabel: [{
      type: Input
    }],
    escape: [{
      type: Input
    }]
  });
})();
var NgOptionTemplateDirective = class _NgOptionTemplateDirective {
  constructor(template) {
    this.template = template;
  }
  static {
    this.ɵfac = function NgOptionTemplateDirective_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _NgOptionTemplateDirective)(ɵɵdirectiveInject(TemplateRef));
    };
  }
  static {
    this.ɵdir = ɵɵdefineDirective({
      type: _NgOptionTemplateDirective,
      selectors: [["", "ng-option-tmp", ""]]
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(NgOptionTemplateDirective, [{
    type: Directive,
    args: [{
      // eslint-disable-next-line @angular-eslint/directive-selector
      selector: "[ng-option-tmp]",
      standalone: true
    }]
  }], () => [{
    type: TemplateRef
  }], null);
})();
var NgOptgroupTemplateDirective = class _NgOptgroupTemplateDirective {
  constructor(template) {
    this.template = template;
  }
  static {
    this.ɵfac = function NgOptgroupTemplateDirective_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _NgOptgroupTemplateDirective)(ɵɵdirectiveInject(TemplateRef));
    };
  }
  static {
    this.ɵdir = ɵɵdefineDirective({
      type: _NgOptgroupTemplateDirective,
      selectors: [["", "ng-optgroup-tmp", ""]]
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(NgOptgroupTemplateDirective, [{
    type: Directive,
    args: [{
      // eslint-disable-next-line @angular-eslint/directive-selector
      selector: "[ng-optgroup-tmp]",
      standalone: true
    }]
  }], () => [{
    type: TemplateRef
  }], null);
})();
var NgLabelTemplateDirective = class _NgLabelTemplateDirective {
  constructor(template) {
    this.template = template;
  }
  static {
    this.ɵfac = function NgLabelTemplateDirective_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _NgLabelTemplateDirective)(ɵɵdirectiveInject(TemplateRef));
    };
  }
  static {
    this.ɵdir = ɵɵdefineDirective({
      type: _NgLabelTemplateDirective,
      selectors: [["", "ng-label-tmp", ""]]
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(NgLabelTemplateDirective, [{
    type: Directive,
    args: [{
      // eslint-disable-next-line @angular-eslint/directive-selector
      selector: "[ng-label-tmp]",
      standalone: true
    }]
  }], () => [{
    type: TemplateRef
  }], null);
})();
var NgMultiLabelTemplateDirective = class _NgMultiLabelTemplateDirective {
  constructor(template) {
    this.template = template;
  }
  static {
    this.ɵfac = function NgMultiLabelTemplateDirective_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _NgMultiLabelTemplateDirective)(ɵɵdirectiveInject(TemplateRef));
    };
  }
  static {
    this.ɵdir = ɵɵdefineDirective({
      type: _NgMultiLabelTemplateDirective,
      selectors: [["", "ng-multi-label-tmp", ""]]
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(NgMultiLabelTemplateDirective, [{
    type: Directive,
    args: [{
      // eslint-disable-next-line @angular-eslint/directive-selector
      selector: "[ng-multi-label-tmp]",
      standalone: true
    }]
  }], () => [{
    type: TemplateRef
  }], null);
})();
var NgHeaderTemplateDirective = class _NgHeaderTemplateDirective {
  constructor(template) {
    this.template = template;
  }
  static {
    this.ɵfac = function NgHeaderTemplateDirective_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _NgHeaderTemplateDirective)(ɵɵdirectiveInject(TemplateRef));
    };
  }
  static {
    this.ɵdir = ɵɵdefineDirective({
      type: _NgHeaderTemplateDirective,
      selectors: [["", "ng-header-tmp", ""]]
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(NgHeaderTemplateDirective, [{
    type: Directive,
    args: [{
      // eslint-disable-next-line @angular-eslint/directive-selector
      selector: "[ng-header-tmp]",
      standalone: true
    }]
  }], () => [{
    type: TemplateRef
  }], null);
})();
var NgFooterTemplateDirective = class _NgFooterTemplateDirective {
  constructor(template) {
    this.template = template;
  }
  static {
    this.ɵfac = function NgFooterTemplateDirective_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _NgFooterTemplateDirective)(ɵɵdirectiveInject(TemplateRef));
    };
  }
  static {
    this.ɵdir = ɵɵdefineDirective({
      type: _NgFooterTemplateDirective,
      selectors: [["", "ng-footer-tmp", ""]]
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(NgFooterTemplateDirective, [{
    type: Directive,
    args: [{
      // eslint-disable-next-line @angular-eslint/directive-selector
      selector: "[ng-footer-tmp]",
      standalone: true
    }]
  }], () => [{
    type: TemplateRef
  }], null);
})();
var NgNotFoundTemplateDirective = class _NgNotFoundTemplateDirective {
  constructor(template) {
    this.template = template;
  }
  static {
    this.ɵfac = function NgNotFoundTemplateDirective_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _NgNotFoundTemplateDirective)(ɵɵdirectiveInject(TemplateRef));
    };
  }
  static {
    this.ɵdir = ɵɵdefineDirective({
      type: _NgNotFoundTemplateDirective,
      selectors: [["", "ng-notfound-tmp", ""]]
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(NgNotFoundTemplateDirective, [{
    type: Directive,
    args: [{
      // eslint-disable-next-line @angular-eslint/directive-selector
      selector: "[ng-notfound-tmp]",
      standalone: true
    }]
  }], () => [{
    type: TemplateRef
  }], null);
})();
var NgPlaceholderTemplateDirective = class _NgPlaceholderTemplateDirective {
  constructor(template) {
    this.template = template;
  }
  static {
    this.ɵfac = function NgPlaceholderTemplateDirective_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _NgPlaceholderTemplateDirective)(ɵɵdirectiveInject(TemplateRef));
    };
  }
  static {
    this.ɵdir = ɵɵdefineDirective({
      type: _NgPlaceholderTemplateDirective,
      selectors: [["", "ng-placeholder-tmp", ""]]
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(NgPlaceholderTemplateDirective, [{
    type: Directive,
    args: [{
      // eslint-disable-next-line @angular-eslint/directive-selector
      selector: "[ng-placeholder-tmp]",
      standalone: true
    }]
  }], () => [{
    type: TemplateRef
  }], null);
})();
var NgTypeToSearchTemplateDirective = class _NgTypeToSearchTemplateDirective {
  constructor(template) {
    this.template = template;
  }
  static {
    this.ɵfac = function NgTypeToSearchTemplateDirective_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _NgTypeToSearchTemplateDirective)(ɵɵdirectiveInject(TemplateRef));
    };
  }
  static {
    this.ɵdir = ɵɵdefineDirective({
      type: _NgTypeToSearchTemplateDirective,
      selectors: [["", "ng-typetosearch-tmp", ""]]
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(NgTypeToSearchTemplateDirective, [{
    type: Directive,
    args: [{
      // eslint-disable-next-line @angular-eslint/directive-selector
      selector: "[ng-typetosearch-tmp]",
      standalone: true
    }]
  }], () => [{
    type: TemplateRef
  }], null);
})();
var NgLoadingTextTemplateDirective = class _NgLoadingTextTemplateDirective {
  constructor(template) {
    this.template = template;
  }
  static {
    this.ɵfac = function NgLoadingTextTemplateDirective_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _NgLoadingTextTemplateDirective)(ɵɵdirectiveInject(TemplateRef));
    };
  }
  static {
    this.ɵdir = ɵɵdefineDirective({
      type: _NgLoadingTextTemplateDirective,
      selectors: [["", "ng-loadingtext-tmp", ""]]
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(NgLoadingTextTemplateDirective, [{
    type: Directive,
    args: [{
      // eslint-disable-next-line @angular-eslint/directive-selector
      selector: "[ng-loadingtext-tmp]",
      standalone: true
    }]
  }], () => [{
    type: TemplateRef
  }], null);
})();
var NgTagTemplateDirective = class _NgTagTemplateDirective {
  constructor(template) {
    this.template = template;
  }
  static {
    this.ɵfac = function NgTagTemplateDirective_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _NgTagTemplateDirective)(ɵɵdirectiveInject(TemplateRef));
    };
  }
  static {
    this.ɵdir = ɵɵdefineDirective({
      type: _NgTagTemplateDirective,
      selectors: [["", "ng-tag-tmp", ""]]
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(NgTagTemplateDirective, [{
    type: Directive,
    args: [{
      // eslint-disable-next-line @angular-eslint/directive-selector
      selector: "[ng-tag-tmp]",
      standalone: true
    }]
  }], () => [{
    type: TemplateRef
  }], null);
})();
var NgLoadingSpinnerTemplateDirective = class _NgLoadingSpinnerTemplateDirective {
  constructor(template) {
    this.template = template;
  }
  static {
    this.ɵfac = function NgLoadingSpinnerTemplateDirective_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _NgLoadingSpinnerTemplateDirective)(ɵɵdirectiveInject(TemplateRef));
    };
  }
  static {
    this.ɵdir = ɵɵdefineDirective({
      type: _NgLoadingSpinnerTemplateDirective,
      selectors: [["", "ng-loadingspinner-tmp", ""]]
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(NgLoadingSpinnerTemplateDirective, [{
    type: Directive,
    args: [{
      // eslint-disable-next-line @angular-eslint/directive-selector
      selector: "[ng-loadingspinner-tmp]",
      standalone: true
    }]
  }], () => [{
    type: TemplateRef
  }], null);
})();
var NgClearButtonTemplateDirective = class _NgClearButtonTemplateDirective {
  constructor(template) {
    this.template = template;
  }
  static {
    this.ɵfac = function NgClearButtonTemplateDirective_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _NgClearButtonTemplateDirective)(ɵɵdirectiveInject(TemplateRef));
    };
  }
  static {
    this.ɵdir = ɵɵdefineDirective({
      type: _NgClearButtonTemplateDirective,
      selectors: [["", "ng-clearbutton-tmp", ""]]
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(NgClearButtonTemplateDirective, [{
    type: Directive,
    args: [{
      selector: "[ng-clearbutton-tmp]",
      standalone: true
    }]
  }], () => [{
    type: TemplateRef
  }], null);
})();
function newId() {
  return "axxxxxxxxxxx".replace(/[x]/g, () => {
    const val = Math.random() * 16 | 0;
    return val.toString(16);
  });
}
var diacritics = {
  "Ⓐ": "A",
  "Ａ": "A",
  "À": "A",
  "Á": "A",
  "Â": "A",
  "Ầ": "A",
  "Ấ": "A",
  "Ẫ": "A",
  "Ẩ": "A",
  "Ã": "A",
  "Ā": "A",
  "Ă": "A",
  "Ằ": "A",
  "Ắ": "A",
  "Ẵ": "A",
  "Ẳ": "A",
  "Ȧ": "A",
  "Ǡ": "A",
  "Ä": "A",
  "Ǟ": "A",
  "Ả": "A",
  "Å": "A",
  "Ǻ": "A",
  "Ǎ": "A",
  "Ȁ": "A",
  "Ȃ": "A",
  "Ạ": "A",
  "Ậ": "A",
  "Ặ": "A",
  "Ḁ": "A",
  "Ą": "A",
  "Ⱥ": "A",
  "Ɐ": "A",
  "Ꜳ": "AA",
  "Æ": "AE",
  "Ǽ": "AE",
  "Ǣ": "AE",
  "Ꜵ": "AO",
  "Ꜷ": "AU",
  "Ꜹ": "AV",
  "Ꜻ": "AV",
  "Ꜽ": "AY",
  "Ⓑ": "B",
  "Ｂ": "B",
  "Ḃ": "B",
  "Ḅ": "B",
  "Ḇ": "B",
  "Ƀ": "B",
  "Ƃ": "B",
  "Ɓ": "B",
  "Ⓒ": "C",
  "Ｃ": "C",
  "Ć": "C",
  "Ĉ": "C",
  "Ċ": "C",
  "Č": "C",
  "Ç": "C",
  "Ḉ": "C",
  "Ƈ": "C",
  "Ȼ": "C",
  "Ꜿ": "C",
  "Ⓓ": "D",
  "Ｄ": "D",
  "Ḋ": "D",
  "Ď": "D",
  "Ḍ": "D",
  "Ḑ": "D",
  "Ḓ": "D",
  "Ḏ": "D",
  "Đ": "D",
  "Ƌ": "D",
  "Ɗ": "D",
  "Ɖ": "D",
  "Ꝺ": "D",
  "Ǳ": "DZ",
  "Ǆ": "DZ",
  "ǲ": "Dz",
  "ǅ": "Dz",
  "Ⓔ": "E",
  "Ｅ": "E",
  "È": "E",
  "É": "E",
  "Ê": "E",
  "Ề": "E",
  "Ế": "E",
  "Ễ": "E",
  "Ể": "E",
  "Ẽ": "E",
  "Ē": "E",
  "Ḕ": "E",
  "Ḗ": "E",
  "Ĕ": "E",
  "Ė": "E",
  "Ë": "E",
  "Ẻ": "E",
  "Ě": "E",
  "Ȅ": "E",
  "Ȇ": "E",
  "Ẹ": "E",
  "Ệ": "E",
  "Ȩ": "E",
  "Ḝ": "E",
  "Ę": "E",
  "Ḙ": "E",
  "Ḛ": "E",
  "Ɛ": "E",
  "Ǝ": "E",
  "Ⓕ": "F",
  "Ｆ": "F",
  "Ḟ": "F",
  "Ƒ": "F",
  "Ꝼ": "F",
  "Ⓖ": "G",
  "Ｇ": "G",
  "Ǵ": "G",
  "Ĝ": "G",
  "Ḡ": "G",
  "Ğ": "G",
  "Ġ": "G",
  "Ǧ": "G",
  "Ģ": "G",
  "Ǥ": "G",
  "Ɠ": "G",
  "Ꞡ": "G",
  "Ᵹ": "G",
  "Ꝿ": "G",
  "Ⓗ": "H",
  "Ｈ": "H",
  "Ĥ": "H",
  "Ḣ": "H",
  "Ḧ": "H",
  "Ȟ": "H",
  "Ḥ": "H",
  "Ḩ": "H",
  "Ḫ": "H",
  "Ħ": "H",
  "Ⱨ": "H",
  "Ⱶ": "H",
  "Ɥ": "H",
  "Ⓘ": "I",
  "Ｉ": "I",
  "Ì": "I",
  "Í": "I",
  "Î": "I",
  "Ĩ": "I",
  "Ī": "I",
  "Ĭ": "I",
  "İ": "I",
  "Ï": "I",
  "Ḯ": "I",
  "Ỉ": "I",
  "Ǐ": "I",
  "Ȉ": "I",
  "Ȋ": "I",
  "Ị": "I",
  "Į": "I",
  "Ḭ": "I",
  "Ɨ": "I",
  "Ⓙ": "J",
  "Ｊ": "J",
  "Ĵ": "J",
  "Ɉ": "J",
  "Ⓚ": "K",
  "Ｋ": "K",
  "Ḱ": "K",
  "Ǩ": "K",
  "Ḳ": "K",
  "Ķ": "K",
  "Ḵ": "K",
  "Ƙ": "K",
  "Ⱪ": "K",
  "Ꝁ": "K",
  "Ꝃ": "K",
  "Ꝅ": "K",
  "Ꞣ": "K",
  "Ⓛ": "L",
  "Ｌ": "L",
  "Ŀ": "L",
  "Ĺ": "L",
  "Ľ": "L",
  "Ḷ": "L",
  "Ḹ": "L",
  "Ļ": "L",
  "Ḽ": "L",
  "Ḻ": "L",
  "Ł": "L",
  "Ƚ": "L",
  "Ɫ": "L",
  "Ⱡ": "L",
  "Ꝉ": "L",
  "Ꝇ": "L",
  "Ꞁ": "L",
  "Ǉ": "LJ",
  "ǈ": "Lj",
  "Ⓜ": "M",
  "Ｍ": "M",
  "Ḿ": "M",
  "Ṁ": "M",
  "Ṃ": "M",
  "Ɱ": "M",
  "Ɯ": "M",
  "Ⓝ": "N",
  "Ｎ": "N",
  "Ǹ": "N",
  "Ń": "N",
  "Ñ": "N",
  "Ṅ": "N",
  "Ň": "N",
  "Ṇ": "N",
  "Ņ": "N",
  "Ṋ": "N",
  "Ṉ": "N",
  "Ƞ": "N",
  "Ɲ": "N",
  "Ꞑ": "N",
  "Ꞥ": "N",
  "Ǌ": "NJ",
  "ǋ": "Nj",
  "Ⓞ": "O",
  "Ｏ": "O",
  "Ò": "O",
  "Ó": "O",
  "Ô": "O",
  "Ồ": "O",
  "Ố": "O",
  "Ỗ": "O",
  "Ổ": "O",
  "Õ": "O",
  "Ṍ": "O",
  "Ȭ": "O",
  "Ṏ": "O",
  "Ō": "O",
  "Ṑ": "O",
  "Ṓ": "O",
  "Ŏ": "O",
  "Ȯ": "O",
  "Ȱ": "O",
  "Ö": "O",
  "Ȫ": "O",
  "Ỏ": "O",
  "Ő": "O",
  "Ǒ": "O",
  "Ȍ": "O",
  "Ȏ": "O",
  "Ơ": "O",
  "Ờ": "O",
  "Ớ": "O",
  "Ỡ": "O",
  "Ở": "O",
  "Ợ": "O",
  "Ọ": "O",
  "Ộ": "O",
  "Ǫ": "O",
  "Ǭ": "O",
  "Ø": "O",
  "Ǿ": "O",
  "Ɔ": "O",
  "Ɵ": "O",
  "Ꝋ": "O",
  "Ꝍ": "O",
  "Ƣ": "OI",
  "Ꝏ": "OO",
  "Ȣ": "OU",
  "Ⓟ": "P",
  "Ｐ": "P",
  "Ṕ": "P",
  "Ṗ": "P",
  "Ƥ": "P",
  "Ᵽ": "P",
  "Ꝑ": "P",
  "Ꝓ": "P",
  "Ꝕ": "P",
  "Ⓠ": "Q",
  "Ｑ": "Q",
  "Ꝗ": "Q",
  "Ꝙ": "Q",
  "Ɋ": "Q",
  "Ⓡ": "R",
  "Ｒ": "R",
  "Ŕ": "R",
  "Ṙ": "R",
  "Ř": "R",
  "Ȑ": "R",
  "Ȓ": "R",
  "Ṛ": "R",
  "Ṝ": "R",
  "Ŗ": "R",
  "Ṟ": "R",
  "Ɍ": "R",
  "Ɽ": "R",
  "Ꝛ": "R",
  "Ꞧ": "R",
  "Ꞃ": "R",
  "Ⓢ": "S",
  "Ｓ": "S",
  "ẞ": "S",
  "Ś": "S",
  "Ṥ": "S",
  "Ŝ": "S",
  "Ṡ": "S",
  "Š": "S",
  "Ṧ": "S",
  "Ṣ": "S",
  "Ṩ": "S",
  "Ș": "S",
  "Ş": "S",
  "Ȿ": "S",
  "Ꞩ": "S",
  "Ꞅ": "S",
  "Ⓣ": "T",
  "Ｔ": "T",
  "Ṫ": "T",
  "Ť": "T",
  "Ṭ": "T",
  "Ț": "T",
  "Ţ": "T",
  "Ṱ": "T",
  "Ṯ": "T",
  "Ŧ": "T",
  "Ƭ": "T",
  "Ʈ": "T",
  "Ⱦ": "T",
  "Ꞇ": "T",
  "Ꜩ": "TZ",
  "Ⓤ": "U",
  "Ｕ": "U",
  "Ù": "U",
  "Ú": "U",
  "Û": "U",
  "Ũ": "U",
  "Ṹ": "U",
  "Ū": "U",
  "Ṻ": "U",
  "Ŭ": "U",
  "Ü": "U",
  "Ǜ": "U",
  "Ǘ": "U",
  "Ǖ": "U",
  "Ǚ": "U",
  "Ủ": "U",
  "Ů": "U",
  "Ű": "U",
  "Ǔ": "U",
  "Ȕ": "U",
  "Ȗ": "U",
  "Ư": "U",
  "Ừ": "U",
  "Ứ": "U",
  "Ữ": "U",
  "Ử": "U",
  "Ự": "U",
  "Ụ": "U",
  "Ṳ": "U",
  "Ų": "U",
  "Ṷ": "U",
  "Ṵ": "U",
  "Ʉ": "U",
  "Ⓥ": "V",
  "Ｖ": "V",
  "Ṽ": "V",
  "Ṿ": "V",
  "Ʋ": "V",
  "Ꝟ": "V",
  "Ʌ": "V",
  "Ꝡ": "VY",
  "Ⓦ": "W",
  "Ｗ": "W",
  "Ẁ": "W",
  "Ẃ": "W",
  "Ŵ": "W",
  "Ẇ": "W",
  "Ẅ": "W",
  "Ẉ": "W",
  "Ⱳ": "W",
  "Ⓧ": "X",
  "Ｘ": "X",
  "Ẋ": "X",
  "Ẍ": "X",
  "Ⓨ": "Y",
  "Ｙ": "Y",
  "Ỳ": "Y",
  "Ý": "Y",
  "Ŷ": "Y",
  "Ỹ": "Y",
  "Ȳ": "Y",
  "Ẏ": "Y",
  "Ÿ": "Y",
  "Ỷ": "Y",
  "Ỵ": "Y",
  "Ƴ": "Y",
  "Ɏ": "Y",
  "Ỿ": "Y",
  "Ⓩ": "Z",
  "Ｚ": "Z",
  "Ź": "Z",
  "Ẑ": "Z",
  "Ż": "Z",
  "Ž": "Z",
  "Ẓ": "Z",
  "Ẕ": "Z",
  "Ƶ": "Z",
  "Ȥ": "Z",
  "Ɀ": "Z",
  "Ⱬ": "Z",
  "Ꝣ": "Z",
  "ⓐ": "a",
  "ａ": "a",
  "ẚ": "a",
  "à": "a",
  "á": "a",
  "â": "a",
  "ầ": "a",
  "ấ": "a",
  "ẫ": "a",
  "ẩ": "a",
  "ã": "a",
  "ā": "a",
  "ă": "a",
  "ằ": "a",
  "ắ": "a",
  "ẵ": "a",
  "ẳ": "a",
  "ȧ": "a",
  "ǡ": "a",
  "ä": "a",
  "ǟ": "a",
  "ả": "a",
  "å": "a",
  "ǻ": "a",
  "ǎ": "a",
  "ȁ": "a",
  "ȃ": "a",
  "ạ": "a",
  "ậ": "a",
  "ặ": "a",
  "ḁ": "a",
  "ą": "a",
  "ⱥ": "a",
  "ɐ": "a",
  "ꜳ": "aa",
  "æ": "ae",
  "ǽ": "ae",
  "ǣ": "ae",
  "ꜵ": "ao",
  "ꜷ": "au",
  "ꜹ": "av",
  "ꜻ": "av",
  "ꜽ": "ay",
  "ⓑ": "b",
  "ｂ": "b",
  "ḃ": "b",
  "ḅ": "b",
  "ḇ": "b",
  "ƀ": "b",
  "ƃ": "b",
  "ɓ": "b",
  "ⓒ": "c",
  "ｃ": "c",
  "ć": "c",
  "ĉ": "c",
  "ċ": "c",
  "č": "c",
  "ç": "c",
  "ḉ": "c",
  "ƈ": "c",
  "ȼ": "c",
  "ꜿ": "c",
  "ↄ": "c",
  "ⓓ": "d",
  "ｄ": "d",
  "ḋ": "d",
  "ď": "d",
  "ḍ": "d",
  "ḑ": "d",
  "ḓ": "d",
  "ḏ": "d",
  "đ": "d",
  "ƌ": "d",
  "ɖ": "d",
  "ɗ": "d",
  "ꝺ": "d",
  "ǳ": "dz",
  "ǆ": "dz",
  "ⓔ": "e",
  "ｅ": "e",
  "è": "e",
  "é": "e",
  "ê": "e",
  "ề": "e",
  "ế": "e",
  "ễ": "e",
  "ể": "e",
  "ẽ": "e",
  "ē": "e",
  "ḕ": "e",
  "ḗ": "e",
  "ĕ": "e",
  "ė": "e",
  "ë": "e",
  "ẻ": "e",
  "ě": "e",
  "ȅ": "e",
  "ȇ": "e",
  "ẹ": "e",
  "ệ": "e",
  "ȩ": "e",
  "ḝ": "e",
  "ę": "e",
  "ḙ": "e",
  "ḛ": "e",
  "ɇ": "e",
  "ɛ": "e",
  "ǝ": "e",
  "ⓕ": "f",
  "ｆ": "f",
  "ḟ": "f",
  "ƒ": "f",
  "ꝼ": "f",
  "ⓖ": "g",
  "ｇ": "g",
  "ǵ": "g",
  "ĝ": "g",
  "ḡ": "g",
  "ğ": "g",
  "ġ": "g",
  "ǧ": "g",
  "ģ": "g",
  "ǥ": "g",
  "ɠ": "g",
  "ꞡ": "g",
  "ᵹ": "g",
  "ꝿ": "g",
  "ⓗ": "h",
  "ｈ": "h",
  "ĥ": "h",
  "ḣ": "h",
  "ḧ": "h",
  "ȟ": "h",
  "ḥ": "h",
  "ḩ": "h",
  "ḫ": "h",
  "ẖ": "h",
  "ħ": "h",
  "ⱨ": "h",
  "ⱶ": "h",
  "ɥ": "h",
  "ƕ": "hv",
  "ⓘ": "i",
  "ｉ": "i",
  "ì": "i",
  "í": "i",
  "î": "i",
  "ĩ": "i",
  "ī": "i",
  "ĭ": "i",
  "ï": "i",
  "ḯ": "i",
  "ỉ": "i",
  "ǐ": "i",
  "ȉ": "i",
  "ȋ": "i",
  "ị": "i",
  "į": "i",
  "ḭ": "i",
  "ɨ": "i",
  "ı": "i",
  "ⓙ": "j",
  "ｊ": "j",
  "ĵ": "j",
  "ǰ": "j",
  "ɉ": "j",
  "ⓚ": "k",
  "ｋ": "k",
  "ḱ": "k",
  "ǩ": "k",
  "ḳ": "k",
  "ķ": "k",
  "ḵ": "k",
  "ƙ": "k",
  "ⱪ": "k",
  "ꝁ": "k",
  "ꝃ": "k",
  "ꝅ": "k",
  "ꞣ": "k",
  "ⓛ": "l",
  "ｌ": "l",
  "ŀ": "l",
  "ĺ": "l",
  "ľ": "l",
  "ḷ": "l",
  "ḹ": "l",
  "ļ": "l",
  "ḽ": "l",
  "ḻ": "l",
  "ſ": "l",
  "ł": "l",
  "ƚ": "l",
  "ɫ": "l",
  "ⱡ": "l",
  "ꝉ": "l",
  "ꞁ": "l",
  "ꝇ": "l",
  "ǉ": "lj",
  "ⓜ": "m",
  "ｍ": "m",
  "ḿ": "m",
  "ṁ": "m",
  "ṃ": "m",
  "ɱ": "m",
  "ɯ": "m",
  "ⓝ": "n",
  "ｎ": "n",
  "ǹ": "n",
  "ń": "n",
  "ñ": "n",
  "ṅ": "n",
  "ň": "n",
  "ṇ": "n",
  "ņ": "n",
  "ṋ": "n",
  "ṉ": "n",
  "ƞ": "n",
  "ɲ": "n",
  "ŉ": "n",
  "ꞑ": "n",
  "ꞥ": "n",
  "ǌ": "nj",
  "ⓞ": "o",
  "ｏ": "o",
  "ò": "o",
  "ó": "o",
  "ô": "o",
  "ồ": "o",
  "ố": "o",
  "ỗ": "o",
  "ổ": "o",
  "õ": "o",
  "ṍ": "o",
  "ȭ": "o",
  "ṏ": "o",
  "ō": "o",
  "ṑ": "o",
  "ṓ": "o",
  "ŏ": "o",
  "ȯ": "o",
  "ȱ": "o",
  "ö": "o",
  "ȫ": "o",
  "ỏ": "o",
  "ő": "o",
  "ǒ": "o",
  "ȍ": "o",
  "ȏ": "o",
  "ơ": "o",
  "ờ": "o",
  "ớ": "o",
  "ỡ": "o",
  "ở": "o",
  "ợ": "o",
  "ọ": "o",
  "ộ": "o",
  "ǫ": "o",
  "ǭ": "o",
  "ø": "o",
  "ǿ": "o",
  "ɔ": "o",
  "ꝋ": "o",
  "ꝍ": "o",
  "ɵ": "o",
  "ƣ": "oi",
  "ȣ": "ou",
  "ꝏ": "oo",
  "ⓟ": "p",
  "ｐ": "p",
  "ṕ": "p",
  "ṗ": "p",
  "ƥ": "p",
  "ᵽ": "p",
  "ꝑ": "p",
  "ꝓ": "p",
  "ꝕ": "p",
  "ⓠ": "q",
  "ｑ": "q",
  "ɋ": "q",
  "ꝗ": "q",
  "ꝙ": "q",
  "ⓡ": "r",
  "ｒ": "r",
  "ŕ": "r",
  "ṙ": "r",
  "ř": "r",
  "ȑ": "r",
  "ȓ": "r",
  "ṛ": "r",
  "ṝ": "r",
  "ŗ": "r",
  "ṟ": "r",
  "ɍ": "r",
  "ɽ": "r",
  "ꝛ": "r",
  "ꞧ": "r",
  "ꞃ": "r",
  "ⓢ": "s",
  "ｓ": "s",
  "ß": "s",
  "ś": "s",
  "ṥ": "s",
  "ŝ": "s",
  "ṡ": "s",
  "š": "s",
  "ṧ": "s",
  "ṣ": "s",
  "ṩ": "s",
  "ș": "s",
  "ş": "s",
  "ȿ": "s",
  "ꞩ": "s",
  "ꞅ": "s",
  "ẛ": "s",
  "ⓣ": "t",
  "ｔ": "t",
  "ṫ": "t",
  "ẗ": "t",
  "ť": "t",
  "ṭ": "t",
  "ț": "t",
  "ţ": "t",
  "ṱ": "t",
  "ṯ": "t",
  "ŧ": "t",
  "ƭ": "t",
  "ʈ": "t",
  "ⱦ": "t",
  "ꞇ": "t",
  "ꜩ": "tz",
  "ⓤ": "u",
  "ｕ": "u",
  "ù": "u",
  "ú": "u",
  "û": "u",
  "ũ": "u",
  "ṹ": "u",
  "ū": "u",
  "ṻ": "u",
  "ŭ": "u",
  "ü": "u",
  "ǜ": "u",
  "ǘ": "u",
  "ǖ": "u",
  "ǚ": "u",
  "ủ": "u",
  "ů": "u",
  "ű": "u",
  "ǔ": "u",
  "ȕ": "u",
  "ȗ": "u",
  "ư": "u",
  "ừ": "u",
  "ứ": "u",
  "ữ": "u",
  "ử": "u",
  "ự": "u",
  "ụ": "u",
  "ṳ": "u",
  "ų": "u",
  "ṷ": "u",
  "ṵ": "u",
  "ʉ": "u",
  "ⓥ": "v",
  "ｖ": "v",
  "ṽ": "v",
  "ṿ": "v",
  "ʋ": "v",
  "ꝟ": "v",
  "ʌ": "v",
  "ꝡ": "vy",
  "ⓦ": "w",
  "ｗ": "w",
  "ẁ": "w",
  "ẃ": "w",
  "ŵ": "w",
  "ẇ": "w",
  "ẅ": "w",
  "ẘ": "w",
  "ẉ": "w",
  "ⱳ": "w",
  "ⓧ": "x",
  "ｘ": "x",
  "ẋ": "x",
  "ẍ": "x",
  "ⓨ": "y",
  "ｙ": "y",
  "ỳ": "y",
  "ý": "y",
  "ŷ": "y",
  "ỹ": "y",
  "ȳ": "y",
  "ẏ": "y",
  "ÿ": "y",
  "ỷ": "y",
  "ẙ": "y",
  "ỵ": "y",
  "ƴ": "y",
  "ɏ": "y",
  "ỿ": "y",
  "ⓩ": "z",
  "ｚ": "z",
  "ź": "z",
  "ẑ": "z",
  "ż": "z",
  "ž": "z",
  "ẓ": "z",
  "ẕ": "z",
  "ƶ": "z",
  "ȥ": "z",
  "ɀ": "z",
  "ⱬ": "z",
  "ꝣ": "z",
  "Ά": "Α",
  "Έ": "Ε",
  "Ή": "Η",
  "Ί": "Ι",
  "Ϊ": "Ι",
  "Ό": "Ο",
  "Ύ": "Υ",
  "Ϋ": "Υ",
  "Ώ": "Ω",
  "ά": "α",
  "έ": "ε",
  "ή": "η",
  "ί": "ι",
  "ϊ": "ι",
  "ΐ": "ι",
  "ό": "ο",
  "ύ": "υ",
  "ϋ": "υ",
  "ΰ": "υ",
  "ω": "ω",
  "ς": "σ"
};
function stripSpecialChars(text) {
  const match = (a) => diacritics[a] || a;
  return text.replace(/[^\u0000-\u007E]/g, match);
}
var ItemsList = class {
  constructor(_ngSelect, _selectionModel) {
    this._ngSelect = _ngSelect;
    this._selectionModel = _selectionModel;
    this._items = [];
    this._filteredItems = [];
    this._markedIndex = -1;
  }
  get items() {
    return this._items;
  }
  get filteredItems() {
    return this._filteredItems;
  }
  get markedIndex() {
    return this._markedIndex;
  }
  get selectedItems() {
    return this._selectionModel.value;
  }
  get markedItem() {
    return this._filteredItems[this._markedIndex];
  }
  get noItemsToSelect() {
    return this._ngSelect.hideSelected && this._items.length === this.selectedItems.length;
  }
  get maxItemsSelected() {
    return this._ngSelect.multiple && this._ngSelect.maxSelectedItems <= this.selectedItems.length;
  }
  get lastSelectedItem() {
    let i = this.selectedItems.length - 1;
    for (; i >= 0; i--) {
      const item = this.selectedItems[i];
      if (!item.disabled) {
        return item;
      }
    }
    return null;
  }
  setItems(items) {
    this._items = items.map((item, index) => this.mapItem(item, index));
    if (this._ngSelect.groupBy) {
      this._groups = this._groupBy(this._items, this._ngSelect.groupBy);
      this._items = this._flatten(this._groups);
    } else {
      this._groups = /* @__PURE__ */ new Map();
      this._groups.set(void 0, this._items);
    }
    this._filteredItems = [...this._items];
  }
  select(item) {
    if (item.selected || this.maxItemsSelected) {
      return;
    }
    const multiple = this._ngSelect.multiple;
    if (!multiple) {
      this.clearSelected();
    }
    this._selectionModel.select(item, multiple, this._ngSelect.selectableGroupAsModel);
    if (this._ngSelect.hideSelected) {
      this._hideSelected(item);
    }
  }
  unselect(item) {
    if (!item.selected) {
      return;
    }
    this._selectionModel.unselect(item, this._ngSelect.multiple);
    if (this._ngSelect.hideSelected && isDefined(item.index) && this._ngSelect.multiple) {
      this._showSelected(item);
    }
  }
  findItem(value) {
    let findBy;
    if (this._ngSelect.compareWith) {
      findBy = (item) => this._ngSelect.compareWith(item.value, value);
    } else if (this._ngSelect.bindValue) {
      findBy = (item) => !item.children && this.resolveNested(item.value, this._ngSelect.bindValue) === value;
    } else {
      findBy = (item) => item.value === value || !item.children && item.label && item.label === this.resolveNested(value, this._ngSelect.bindLabel);
    }
    return this._items.find((item) => findBy(item));
  }
  addItem(item) {
    const option = this.mapItem(item, this._items.length);
    this._items.push(option);
    this._filteredItems.push(option);
    return option;
  }
  clearSelected(keepDisabled = false) {
    this._selectionModel.clear(keepDisabled);
    this._items.forEach((item) => {
      item.selected = keepDisabled && item.selected && item.disabled;
      item.marked = false;
    });
    if (this._ngSelect.hideSelected) {
      this.resetFilteredItems();
    }
  }
  findByLabel(term) {
    term = stripSpecialChars(term).toLocaleLowerCase();
    return this.filteredItems.find((item) => {
      const label = stripSpecialChars(item.label).toLocaleLowerCase();
      return label.substr(0, term.length) === term;
    });
  }
  filter(term) {
    if (!term) {
      this.resetFilteredItems();
      return;
    }
    this._filteredItems = [];
    term = this._ngSelect.searchFn ? term : stripSpecialChars(term).toLocaleLowerCase();
    const match = this._ngSelect.searchFn || this._defaultSearchFn;
    const hideSelected = this._ngSelect.hideSelected;
    for (const key of Array.from(this._groups.keys())) {
      const matchedItems = [];
      for (const item of this._groups.get(key)) {
        if (hideSelected && (item.parent && item.parent.selected || item.selected)) {
          continue;
        }
        const searchItem = this._ngSelect.searchFn ? item.value : item;
        if (match(term, searchItem)) {
          matchedItems.push(item);
        }
      }
      if (matchedItems.length > 0) {
        const [last] = matchedItems.slice(-1);
        if (last.parent) {
          const head = this._items.find((x) => x === last.parent);
          this._filteredItems.push(head);
        }
        this._filteredItems.push(...matchedItems);
      }
    }
  }
  resetFilteredItems() {
    if (this._filteredItems.length === this._items.length) {
      return;
    }
    if (this._ngSelect.hideSelected && this.selectedItems.length > 0) {
      this._filteredItems = this._items.filter((x) => !x.selected);
    } else {
      this._filteredItems = this._items;
    }
  }
  unmarkItem() {
    this._markedIndex = -1;
  }
  markNextItem() {
    this._stepToItem(1);
  }
  markPreviousItem() {
    this._stepToItem(-1);
  }
  markItem(item) {
    this._markedIndex = this._filteredItems.indexOf(item);
  }
  markSelectedOrDefault(markDefault) {
    if (this._filteredItems.length === 0) {
      return;
    }
    const lastMarkedIndex = this._getLastMarkedIndex();
    if (lastMarkedIndex > -1) {
      this._markedIndex = lastMarkedIndex;
    } else {
      this._markedIndex = markDefault ? this.filteredItems.findIndex((x) => !x.disabled) : -1;
    }
  }
  resolveNested(option, key) {
    if (!isObject2(option)) {
      return option;
    }
    if (key.indexOf(".") === -1) {
      return option[key];
    } else {
      const keys = key.split(".");
      let value = option;
      for (let i = 0, len = keys.length; i < len; ++i) {
        if (value == null) {
          return null;
        }
        value = value[keys[i]];
      }
      return value;
    }
  }
  mapItem(item, index) {
    const label = isDefined(item.$ngOptionLabel) ? item.$ngOptionLabel : this.resolveNested(item, this._ngSelect.bindLabel);
    const value = isDefined(item.$ngOptionValue) ? item.$ngOptionValue : item;
    return {
      index,
      label: isDefined(label) ? label.toString() : "",
      value,
      disabled: item.disabled,
      htmlId: `${this._ngSelect.dropdownId}-${index}`
    };
  }
  mapSelectedItems() {
    const multiple = this._ngSelect.multiple;
    for (const selected of this.selectedItems) {
      const value = this._ngSelect.bindValue ? this.resolveNested(selected.value, this._ngSelect.bindValue) : selected.value;
      const item = isDefined(value) ? this.findItem(value) : null;
      this._selectionModel.unselect(selected, multiple);
      this._selectionModel.select(item || selected, multiple, this._ngSelect.selectableGroupAsModel);
    }
    if (this._ngSelect.hideSelected) {
      this._filteredItems = this.filteredItems.filter((x) => this.selectedItems.indexOf(x) === -1);
    }
  }
  _showSelected(item) {
    this._filteredItems.push(item);
    if (item.parent) {
      const parent = item.parent;
      const parentExists = this._filteredItems.find((x) => x === parent);
      if (!parentExists) {
        this._filteredItems.push(parent);
      }
    } else if (item.children) {
      for (const child of item.children) {
        child.selected = false;
        this._filteredItems.push(child);
      }
    }
    this._filteredItems = [...this._filteredItems.sort((a, b) => a.index - b.index)];
  }
  _hideSelected(item) {
    this._filteredItems = this._filteredItems.filter((x) => x !== item);
    if (item.parent) {
      const children = item.parent.children;
      if (children.every((x) => x.selected)) {
        this._filteredItems = this._filteredItems.filter((x) => x !== item.parent);
      }
    } else if (item.children) {
      this._filteredItems = this.filteredItems.filter((x) => x.parent !== item);
    }
  }
  _defaultSearchFn(search, opt) {
    const label = stripSpecialChars(opt.label).toLocaleLowerCase();
    return label.indexOf(search) > -1;
  }
  _getNextItemIndex(steps) {
    if (steps > 0) {
      return this._markedIndex >= this._filteredItems.length - 1 ? 0 : this._markedIndex + 1;
    }
    return this._markedIndex <= 0 ? this._filteredItems.length - 1 : this._markedIndex - 1;
  }
  _stepToItem(steps) {
    if (this._filteredItems.length === 0 || this._filteredItems.every((x) => x.disabled)) {
      return;
    }
    this._markedIndex = this._getNextItemIndex(steps);
    if (this.markedItem.disabled) {
      this._stepToItem(steps);
    }
  }
  _getLastMarkedIndex() {
    if (this._ngSelect.hideSelected) {
      return -1;
    }
    if (this._markedIndex > -1 && this.markedItem === void 0) {
      return -1;
    }
    const selectedIndex = this._filteredItems.indexOf(this.lastSelectedItem);
    if (this.lastSelectedItem && selectedIndex < 0) {
      return -1;
    }
    return Math.max(this.markedIndex, selectedIndex);
  }
  _groupBy(items, prop) {
    const groups = /* @__PURE__ */ new Map();
    if (items.length === 0) {
      return groups;
    }
    if (Array.isArray(items[0].value[prop])) {
      for (const item of items) {
        const children = (item.value[prop] || []).map((x, index) => this.mapItem(x, index));
        groups.set(item, children);
      }
      return groups;
    }
    const isFnKey = isFunction(this._ngSelect.groupBy);
    const keyFn = (item) => {
      const key = isFnKey ? prop(item.value) : item.value[prop];
      return isDefined(key) ? key : void 0;
    };
    for (const item of items) {
      const key = keyFn(item);
      const group = groups.get(key);
      if (group) {
        group.push(item);
      } else {
        groups.set(key, [item]);
      }
    }
    return groups;
  }
  _flatten(groups) {
    const isGroupByFn = isFunction(this._ngSelect.groupBy);
    const items = [];
    for (const key of Array.from(groups.keys())) {
      let i = items.length;
      if (key === void 0) {
        const withoutGroup = groups.get(void 0) || [];
        items.push(...withoutGroup.map((x) => {
          x.index = i++;
          return x;
        }));
        continue;
      }
      const isObjectKey = isObject2(key);
      const parent = {
        label: isObjectKey ? "" : String(key),
        children: void 0,
        parent: null,
        index: i++,
        disabled: !this._ngSelect.selectableGroup,
        htmlId: newId()
      };
      const groupKey = isGroupByFn ? this._ngSelect.bindLabel : this._ngSelect.groupBy;
      const groupValue = this._ngSelect.groupValue || (() => {
        if (isObjectKey) {
          return key.value;
        }
        return {
          [groupKey]: key
        };
      });
      const children = groups.get(key).map((x) => {
        x.parent = parent;
        x.children = void 0;
        x.index = i++;
        return x;
      });
      parent.children = children;
      parent.value = groupValue(key, children.map((x) => x.value));
      items.push(parent);
      items.push(...children);
    }
    return items;
  }
};
var NgDropdownPanelService = class _NgDropdownPanelService {
  constructor() {
    this._dimensions = {
      itemHeight: 0,
      panelHeight: 0,
      itemsPerViewport: 0
    };
  }
  get dimensions() {
    return this._dimensions;
  }
  calculateItems(scrollPos, itemsLength, buffer) {
    const d = this._dimensions;
    const scrollHeight = d.itemHeight * itemsLength;
    const scrollTop = Math.max(0, scrollPos);
    const indexByScrollTop = scrollTop / scrollHeight * itemsLength;
    let end = Math.min(itemsLength, Math.ceil(indexByScrollTop) + (d.itemsPerViewport + 1));
    const maxStartEnd = end;
    const maxStart = Math.max(0, maxStartEnd - d.itemsPerViewport);
    let start = Math.min(maxStart, Math.floor(indexByScrollTop));
    let topPadding = d.itemHeight * Math.ceil(start) - d.itemHeight * Math.min(start, buffer);
    topPadding = !isNaN(topPadding) ? topPadding : 0;
    start = !isNaN(start) ? start : -1;
    end = !isNaN(end) ? end : -1;
    start -= buffer;
    start = Math.max(0, start);
    end += buffer;
    end = Math.min(itemsLength, end);
    return {
      topPadding,
      scrollHeight,
      start,
      end
    };
  }
  setDimensions(itemHeight, panelHeight) {
    const itemsPerViewport = Math.max(1, Math.floor(panelHeight / itemHeight));
    this._dimensions = {
      itemHeight,
      panelHeight,
      itemsPerViewport
    };
  }
  getScrollTo(itemTop, itemHeight, lastScroll) {
    const {
      panelHeight
    } = this.dimensions;
    const itemBottom = itemTop + itemHeight;
    const top = lastScroll;
    const bottom = top + panelHeight;
    if (panelHeight >= itemBottom && lastScroll === itemTop) {
      return null;
    }
    if (itemBottom > bottom) {
      return top + itemBottom - bottom;
    } else if (itemTop <= top) {
      return itemTop;
    }
    return null;
  }
  static {
    this.ɵfac = function NgDropdownPanelService_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _NgDropdownPanelService)();
    };
  }
  static {
    this.ɵprov = ɵɵdefineInjectable({
      token: _NgDropdownPanelService,
      factory: _NgDropdownPanelService.ɵfac
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(NgDropdownPanelService, [{
    type: Injectable
  }], null, null);
})();
var CSS_POSITIONS = ["top", "right", "bottom", "left"];
var SCROLL_SCHEDULER = typeof requestAnimationFrame !== "undefined" ? animationFrameScheduler : asapScheduler;
var NgDropdownPanelComponent = class _NgDropdownPanelComponent {
  constructor(_renderer, _zone, _panelService, _elementRef, _document) {
    this._renderer = _renderer;
    this._zone = _zone;
    this._panelService = _panelService;
    this._document = _document;
    this.items = [];
    this.position = "auto";
    this.virtualScroll = false;
    this.filterValue = null;
    this.ariaLabelDropdown = input(null);
    this.update = new EventEmitter();
    this.scroll = new EventEmitter();
    this.scrollToEnd = new EventEmitter();
    this.outsideClick = new EventEmitter();
    this._destroy$ = new Subject();
    this._scrollToEndFired = false;
    this._updateScrollHeight = false;
    this._lastScrollPosition = 0;
    this._dropdown = _elementRef.nativeElement;
  }
  get currentPosition() {
    return this._currentPosition;
  }
  get itemsLength() {
    return this._itemsLength;
  }
  set itemsLength(value) {
    if (value !== this._itemsLength) {
      this._itemsLength = value;
      this._onItemsLengthChanged();
    }
  }
  get _startOffset() {
    if (this.markedItem) {
      const {
        itemHeight,
        panelHeight
      } = this._panelService.dimensions;
      const offset = this.markedItem.index * itemHeight;
      return panelHeight > offset ? 0 : offset;
    }
    return 0;
  }
  ngOnInit() {
    this._select = this._dropdown.parentElement;
    this._virtualPadding = this.paddingElementRef.nativeElement;
    this._scrollablePanel = this.scrollElementRef.nativeElement;
    this._contentPanel = this.contentElementRef.nativeElement;
    this._handleScroll();
    this._handleOutsideClick();
    this._appendDropdown();
    this._setupMousedownListener();
  }
  ngOnChanges(changes) {
    if (changes.items) {
      const change = changes.items;
      this._onItemsChange(change.currentValue, change.firstChange);
    }
  }
  ngOnDestroy() {
    this._destroy$.next();
    this._destroy$.complete();
    this._destroy$.unsubscribe();
    if (this.appendTo) {
      this._renderer.removeChild(this._dropdown.parentNode, this._dropdown);
    }
  }
  scrollTo(option, startFromOption = false) {
    if (!option) {
      return;
    }
    const index = this.items.indexOf(option);
    if (index < 0 || index >= this.itemsLength) {
      return;
    }
    let scrollTo;
    if (this.virtualScroll) {
      const itemHeight = this._panelService.dimensions.itemHeight;
      scrollTo = this._panelService.getScrollTo(index * itemHeight, itemHeight, this._lastScrollPosition);
    } else {
      const item = this._dropdown.querySelector(`#${option.htmlId}`);
      const lastScroll = startFromOption ? item.offsetTop : this._lastScrollPosition;
      scrollTo = this._panelService.getScrollTo(item.offsetTop, item.clientHeight, lastScroll);
    }
    if (isDefined(scrollTo)) {
      this._scrollablePanel.scrollTop = scrollTo;
    }
  }
  scrollToTag() {
    const panel = this._scrollablePanel;
    panel.scrollTop = panel.scrollHeight - panel.clientHeight;
  }
  adjustPosition() {
    this._updateYPosition();
  }
  _handleDropdownPosition() {
    this._currentPosition = this._calculateCurrentPosition(this._dropdown);
    if (CSS_POSITIONS.includes(this._currentPosition)) {
      this._updateDropdownClass(this._currentPosition);
    } else {
      this._updateDropdownClass("bottom");
    }
    if (this.appendTo) {
      this._updateYPosition();
    }
    this._dropdown.style.opacity = "1";
  }
  _updateDropdownClass(currentPosition) {
    CSS_POSITIONS.forEach((position) => {
      const REMOVE_CSS_CLASS = `ng-select-${position}`;
      this._renderer.removeClass(this._dropdown, REMOVE_CSS_CLASS);
      this._renderer.removeClass(this._select, REMOVE_CSS_CLASS);
    });
    const ADD_CSS_CLASS = `ng-select-${currentPosition}`;
    this._renderer.addClass(this._dropdown, ADD_CSS_CLASS);
    this._renderer.addClass(this._select, ADD_CSS_CLASS);
  }
  _handleScroll() {
    this._zone.runOutsideAngular(() => {
      fromEvent(this.scrollElementRef.nativeElement, "scroll").pipe(takeUntil(this._destroy$), auditTime(0, SCROLL_SCHEDULER)).subscribe((e) => {
        const path = e.path || e.composedPath && e.composedPath();
        if (!path || path.length === 0 && !e.target) {
          return;
        }
        const scrollTop = !path || path.length === 0 ? e.target.scrollTop : path[0].scrollTop;
        this._onContentScrolled(scrollTop);
      });
    });
  }
  _handleOutsideClick() {
    if (!this._document) {
      return;
    }
    this._zone.runOutsideAngular(() => {
      merge(fromEvent(this._document, "touchstart", {
        capture: true
      }), fromEvent(this._document, "click", {
        capture: true
      })).pipe(takeUntil(this._destroy$)).subscribe(($event) => this._checkToClose($event));
    });
  }
  _checkToClose($event) {
    if (this._select.contains($event.target) || this._dropdown.contains($event.target)) {
      return;
    }
    const path = $event.path || $event.composedPath && $event.composedPath();
    if ($event.target && $event.target.shadowRoot && path && path[0] && this._select.contains(path[0])) {
      return;
    }
    this._zone.run(() => this.outsideClick.emit());
  }
  _onItemsChange(items, firstChange) {
    this.items = items || [];
    this._scrollToEndFired = false;
    this.itemsLength = items.length;
    if (this.virtualScroll) {
      this._updateItemsRange(firstChange);
    } else {
      this._setVirtualHeight();
      this._updateItems(firstChange);
    }
  }
  _updateItems(firstChange) {
    this.update.emit(this.items);
    if (firstChange === false) {
      return;
    }
    this._zone.runOutsideAngular(() => {
      Promise.resolve().then(() => {
        const panelHeight = this._scrollablePanel.clientHeight;
        this._panelService.setDimensions(0, panelHeight);
        this._handleDropdownPosition();
        this.scrollTo(this.markedItem, firstChange);
      });
    });
  }
  _updateItemsRange(firstChange) {
    this._zone.runOutsideAngular(() => {
      this._measureDimensions().then(() => {
        if (firstChange) {
          this._renderItemsRange(this._startOffset);
          this._handleDropdownPosition();
        } else {
          this._renderItemsRange();
        }
      });
    });
  }
  _onContentScrolled(scrollTop) {
    if (this.virtualScroll) {
      this._renderItemsRange(scrollTop);
    }
    this._lastScrollPosition = scrollTop;
    this._fireScrollToEnd(scrollTop);
  }
  _updateVirtualHeight(height) {
    if (this._updateScrollHeight) {
      this._virtualPadding.style.height = `${height}px`;
      this._updateScrollHeight = false;
    }
  }
  _setVirtualHeight() {
    if (!this._virtualPadding) {
      return;
    }
    this._virtualPadding.style.height = `0px`;
  }
  _onItemsLengthChanged() {
    this._updateScrollHeight = true;
  }
  _renderItemsRange(scrollTop = null) {
    if (scrollTop && this._lastScrollPosition === scrollTop) {
      return;
    }
    scrollTop = scrollTop || this._scrollablePanel.scrollTop;
    const range = this._panelService.calculateItems(scrollTop, this.itemsLength, this.bufferAmount);
    this._updateVirtualHeight(range.scrollHeight);
    this._contentPanel.style.transform = `translateY(${range.topPadding}px)`;
    this._zone.run(() => {
      this.update.emit(this.items.slice(range.start, range.end));
      this.scroll.emit({
        start: range.start,
        end: range.end
      });
    });
    if (isDefined(scrollTop) && this._lastScrollPosition === 0) {
      this._scrollablePanel.scrollTop = scrollTop;
      this._lastScrollPosition = scrollTop;
    }
  }
  _measureDimensions() {
    if (this._panelService.dimensions.itemHeight > 0 || this.itemsLength === 0) {
      return Promise.resolve(this._panelService.dimensions);
    }
    const [first2] = this.items;
    this.update.emit([first2]);
    return Promise.resolve().then(() => {
      const option = this._dropdown.querySelector(`#${first2.htmlId}`);
      const optionHeight = option.clientHeight;
      this._virtualPadding.style.height = `${optionHeight * this.itemsLength}px`;
      const panelHeight = this._scrollablePanel.clientHeight;
      this._panelService.setDimensions(optionHeight, panelHeight);
      return this._panelService.dimensions;
    });
  }
  _fireScrollToEnd(scrollTop) {
    if (this._scrollToEndFired || scrollTop === 0) {
      return;
    }
    const padding = this.virtualScroll ? this._virtualPadding : this._contentPanel;
    if (scrollTop + this._dropdown.clientHeight >= padding.clientHeight - 1) {
      this._zone.run(() => this.scrollToEnd.emit());
      this._scrollToEndFired = true;
    }
  }
  _calculateCurrentPosition(dropdownEl) {
    if (this.position !== "auto") {
      return this.position;
    }
    const selectRect = this._select.getBoundingClientRect();
    const scrollTop = document.documentElement.scrollTop || document.body.scrollTop;
    const offsetTop = selectRect.top + window.pageYOffset;
    const height = selectRect.height;
    const dropdownHeight = dropdownEl.getBoundingClientRect().height;
    if (offsetTop + height + dropdownHeight > scrollTop + document.documentElement.clientHeight) {
      return "top";
    } else {
      return "bottom";
    }
  }
  _appendDropdown() {
    if (!this.appendTo) {
      return;
    }
    this._parent = this._dropdown.shadowRoot ? this._dropdown.shadowRoot.querySelector(this.appendTo) : document.querySelector(this.appendTo);
    if (!this._parent) {
      throw new Error(`appendTo selector ${this.appendTo} did not found any parent element`);
    }
    this._updateXPosition();
    this._parent.appendChild(this._dropdown);
  }
  _updateXPosition() {
    const select = this._select.getBoundingClientRect();
    const parent = this._parent.getBoundingClientRect();
    const offsetLeft = select.left - parent.left;
    this._dropdown.style.left = offsetLeft + "px";
    this._dropdown.style.width = select.width + "px";
    this._dropdown.style.minWidth = select.width + "px";
  }
  _updateYPosition() {
    const select = this._select.getBoundingClientRect();
    const parent = this._parent.getBoundingClientRect();
    const delta = select.height;
    if (this._currentPosition === "top") {
      const offsetBottom = parent.bottom - select.bottom;
      this._dropdown.style.bottom = offsetBottom + delta + "px";
      this._dropdown.style.top = "auto";
    } else if (this._currentPosition === "bottom") {
      const offsetTop = select.top - parent.top;
      this._dropdown.style.top = offsetTop + delta + "px";
      this._dropdown.style.bottom = "auto";
    }
  }
  _setupMousedownListener() {
    this._zone.runOutsideAngular(() => {
      fromEvent(this._dropdown, "mousedown").pipe(takeUntil(this._destroy$)).subscribe((event) => {
        const target = event.target;
        if (target.tagName === "INPUT") {
          return;
        }
        event.preventDefault();
      });
    });
  }
  static {
    this.ɵfac = function NgDropdownPanelComponent_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _NgDropdownPanelComponent)(ɵɵdirectiveInject(Renderer2), ɵɵdirectiveInject(NgZone), ɵɵdirectiveInject(NgDropdownPanelService), ɵɵdirectiveInject(ElementRef), ɵɵdirectiveInject(DOCUMENT, 8));
    };
  }
  static {
    this.ɵcmp = ɵɵdefineComponent({
      type: _NgDropdownPanelComponent,
      selectors: [["ng-dropdown-panel"]],
      viewQuery: function NgDropdownPanelComponent_Query(rf, ctx) {
        if (rf & 1) {
          ɵɵviewQuery(_c0, 7, ElementRef);
          ɵɵviewQuery(_c1, 7, ElementRef);
          ɵɵviewQuery(_c2, 7, ElementRef);
        }
        if (rf & 2) {
          let _t;
          ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.contentElementRef = _t.first);
          ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.scrollElementRef = _t.first);
          ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.paddingElementRef = _t.first);
        }
      },
      inputs: {
        items: "items",
        markedItem: "markedItem",
        position: "position",
        appendTo: "appendTo",
        bufferAmount: "bufferAmount",
        virtualScroll: [2, "virtualScroll", "virtualScroll", booleanAttribute],
        headerTemplate: "headerTemplate",
        footerTemplate: "footerTemplate",
        filterValue: "filterValue",
        ariaLabelDropdown: [1, "ariaLabelDropdown"]
      },
      outputs: {
        update: "update",
        scroll: "scroll",
        scrollToEnd: "scrollToEnd",
        outsideClick: "outsideClick"
      },
      features: [ɵɵNgOnChangesFeature],
      ngContentSelectors: _c3,
      decls: 9,
      vars: 7,
      consts: [["scroll", ""], ["padding", ""], ["content", ""], [1, "ng-dropdown-header"], ["role", "listbox", 1, "ng-dropdown-panel-items", "scroll-host"], [1, "ng-dropdown-footer"], [3, "ngTemplateOutlet", "ngTemplateOutletContext"]],
      template: function NgDropdownPanelComponent_Template(rf, ctx) {
        if (rf & 1) {
          ɵɵprojectionDef();
          ɵɵtemplate(0, NgDropdownPanelComponent_Conditional_0_Template, 2, 4, "div", 3);
          ɵɵelementStart(1, "div", 4, 0);
          ɵɵelement(3, "div", null, 1);
          ɵɵelementStart(5, "div", null, 2);
          ɵɵprojection(7);
          ɵɵelementEnd()();
          ɵɵtemplate(8, NgDropdownPanelComponent_Conditional_8_Template, 2, 4, "div", 5);
        }
        if (rf & 2) {
          ɵɵconditional(ctx.headerTemplate ? 0 : -1);
          ɵɵadvance();
          ɵɵattribute("aria-label", ctx.ariaLabelDropdown());
          ɵɵadvance(2);
          ɵɵclassProp("total-padding", ctx.virtualScroll);
          ɵɵadvance(2);
          ɵɵclassProp("scrollable-content", ctx.virtualScroll && ctx.items.length);
          ɵɵadvance(3);
          ɵɵconditional(ctx.footerTemplate ? 8 : -1);
        }
      },
      dependencies: [NgTemplateOutlet],
      encapsulation: 2,
      changeDetection: 0
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(NgDropdownPanelComponent, [{
    type: Component,
    args: [{
      changeDetection: ChangeDetectionStrategy.OnPush,
      encapsulation: ViewEncapsulation.None,
      selector: "ng-dropdown-panel",
      template: `
		@if (headerTemplate) {
			<div class="ng-dropdown-header">
				<ng-container [ngTemplateOutlet]="headerTemplate" [ngTemplateOutletContext]="{ searchTerm: filterValue }" />
			</div>
		}
		<div #scroll role="listbox" class="ng-dropdown-panel-items scroll-host" [attr.aria-label]="ariaLabelDropdown()">
			<div #padding [class.total-padding]="virtualScroll"></div>
			<div #content [class.scrollable-content]="virtualScroll && items.length">
				<ng-content />
			</div>
		</div>
		@if (footerTemplate) {
			<div class="ng-dropdown-footer">
				<ng-container [ngTemplateOutlet]="footerTemplate" [ngTemplateOutletContext]="{ searchTerm: filterValue }" />
			</div>
		}
	`,
      imports: [NgTemplateOutlet]
    }]
  }], () => [{
    type: Renderer2
  }, {
    type: NgZone
  }, {
    type: NgDropdownPanelService
  }, {
    type: ElementRef
  }, {
    type: void 0,
    decorators: [{
      type: Optional
    }, {
      type: Inject,
      args: [DOCUMENT]
    }]
  }], {
    items: [{
      type: Input
    }],
    markedItem: [{
      type: Input
    }],
    position: [{
      type: Input
    }],
    appendTo: [{
      type: Input
    }],
    bufferAmount: [{
      type: Input
    }],
    virtualScroll: [{
      type: Input,
      args: [{
        transform: booleanAttribute
      }]
    }],
    headerTemplate: [{
      type: Input
    }],
    footerTemplate: [{
      type: Input
    }],
    filterValue: [{
      type: Input
    }],
    update: [{
      type: Output
    }],
    scroll: [{
      type: Output
    }],
    scrollToEnd: [{
      type: Output
    }],
    outsideClick: [{
      type: Output
    }],
    contentElementRef: [{
      type: ViewChild,
      args: ["content", {
        read: ElementRef,
        static: true
      }]
    }],
    scrollElementRef: [{
      type: ViewChild,
      args: ["scroll", {
        read: ElementRef,
        static: true
      }]
    }],
    paddingElementRef: [{
      type: ViewChild,
      args: ["padding", {
        read: ElementRef,
        static: true
      }]
    }]
  });
})();
var NgOptionComponent = class _NgOptionComponent {
  constructor(elementRef) {
    this.elementRef = elementRef;
    this.disabled = false;
    this.stateChange$ = new Subject();
  }
  get label() {
    return (this.elementRef.nativeElement.textContent || "").trim();
  }
  ngOnChanges(changes) {
    if (changes.disabled) {
      this.stateChange$.next({
        value: this.value,
        disabled: this.disabled
      });
    }
  }
  ngAfterViewChecked() {
    if (this.label !== this._previousLabel) {
      this._previousLabel = this.label;
      this.stateChange$.next({
        value: this.value,
        disabled: this.disabled,
        label: this.elementRef.nativeElement.innerHTML
      });
    }
  }
  ngOnDestroy() {
    this.stateChange$.complete();
  }
  static {
    this.ɵfac = function NgOptionComponent_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _NgOptionComponent)(ɵɵdirectiveInject(ElementRef));
    };
  }
  static {
    this.ɵcmp = ɵɵdefineComponent({
      type: _NgOptionComponent,
      selectors: [["ng-option"]],
      inputs: {
        value: "value",
        disabled: [2, "disabled", "disabled", booleanAttribute]
      },
      features: [ɵɵNgOnChangesFeature],
      ngContentSelectors: _c3,
      decls: 1,
      vars: 0,
      template: function NgOptionComponent_Template(rf, ctx) {
        if (rf & 1) {
          ɵɵprojectionDef();
          ɵɵprojection(0);
        }
      },
      encapsulation: 2,
      changeDetection: 0
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(NgOptionComponent, [{
    type: Component,
    args: [{
      selector: "ng-option",
      standalone: true,
      changeDetection: ChangeDetectionStrategy.OnPush,
      template: `<ng-content />`
    }]
  }], () => [{
    type: ElementRef
  }], {
    value: [{
      type: Input
    }],
    disabled: [{
      type: Input,
      args: [{
        transform: booleanAttribute
      }]
    }]
  });
})();
var KeyCode;
(function(KeyCode2) {
  KeyCode2["Tab"] = "Tab";
  KeyCode2["Enter"] = "Enter";
  KeyCode2["Esc"] = "Escape";
  KeyCode2["Space"] = " ";
  KeyCode2["ArrowUp"] = "ArrowUp";
  KeyCode2["ArrowDown"] = "ArrowDown";
  KeyCode2["Backspace"] = "Backspace";
})(KeyCode || (KeyCode = {}));
function DefaultSelectionModelFactory() {
  return new DefaultSelectionModel();
}
var DefaultSelectionModel = class {
  constructor() {
    this._selected = [];
  }
  get value() {
    return this._selected;
  }
  select(item, multiple, groupAsModel) {
    item.selected = true;
    if (!item.children || !multiple && groupAsModel) {
      this._selected.push(item);
    }
    if (multiple) {
      if (item.parent) {
        const childrenCount = item.parent.children.length;
        const selectedCount = item.parent.children.filter((x) => x.selected).length;
        item.parent.selected = childrenCount === selectedCount;
      } else if (item.children) {
        this._setChildrenSelectedState(item.children, true);
        this._removeChildren(item);
        if (groupAsModel && this._activeChildren(item)) {
          this._selected = [...this._selected.filter((x) => x.parent !== item), item];
        } else {
          this._selected = [...this._selected, ...item.children.filter((x) => !x.disabled)];
        }
      }
    }
  }
  unselect(item, multiple) {
    this._selected = this._selected.filter((x) => x !== item);
    item.selected = false;
    if (multiple) {
      if (item.parent && item.parent.selected) {
        const children = item.parent.children;
        this._removeParent(item.parent);
        this._removeChildren(item.parent);
        this._selected.push(...children.filter((x) => x !== item && !x.disabled));
        item.parent.selected = false;
      } else if (item.children) {
        this._setChildrenSelectedState(item.children, false);
        this._removeChildren(item);
      }
    }
  }
  clear(keepDisabled) {
    this._selected = keepDisabled ? this._selected.filter((x) => x.disabled) : [];
  }
  _setChildrenSelectedState(children, selected) {
    for (const child of children) {
      if (child.disabled) {
        continue;
      }
      child.selected = selected;
    }
  }
  _removeChildren(parent) {
    this._selected = [...this._selected.filter((x) => x.parent !== parent), ...parent.children.filter((x) => x.parent === parent && x.disabled && x.selected)];
  }
  _removeParent(parent) {
    this._selected = this._selected.filter((x) => x !== parent);
  }
  _activeChildren(item) {
    return item.children.every((x) => !x.disabled || x.selected);
  }
};
var NgSelectConfig = class _NgSelectConfig {
  constructor() {
    this.fixedPlaceholder = false;
    this.notFoundText = "No items found";
    this.typeToSearchText = "Type to search";
    this.addTagText = "Add item";
    this.loadingText = "Loading...";
    this.clearAllText = "Clear all";
    this.disableVirtualScroll = true;
    this.openOnEnter = true;
    this.appearance = "underline";
  }
  static {
    this.ɵfac = function NgSelectConfig_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _NgSelectConfig)();
    };
  }
  static {
    this.ɵprov = ɵɵdefineInjectable({
      token: _NgSelectConfig,
      factory: _NgSelectConfig.ɵfac,
      providedIn: "root"
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(NgSelectConfig, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], null, null);
})();
var ConsoleService = class _ConsoleService {
  warn(message) {
    console.warn(message);
  }
  static {
    this.ɵfac = function ConsoleService_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _ConsoleService)();
    };
  }
  static {
    this.ɵprov = ɵɵdefineInjectable({
      token: _ConsoleService,
      factory: _ConsoleService.ɵfac,
      providedIn: "root"
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(ConsoleService, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], null, null);
})();
var SELECTION_MODEL_FACTORY = new InjectionToken("ng-select-selection-model");
var NgSelectComponent = class _NgSelectComponent {
  constructor(classes, autoFocus, config, newSelectionModel, _elementRef, _cd, _console) {
    this.classes = classes;
    this.autoFocus = autoFocus;
    this.config = config;
    this._cd = _cd;
    this._console = _console;
    this.ariaLabelDropdown = "Options List";
    this.markFirst = true;
    this.fixedPlaceholder = false;
    this.preventToggleOnRightClick = false;
    this.dropdownPosition = "auto";
    this.loading = false;
    this.closeOnSelect = true;
    this.hideSelected = false;
    this.selectOnTab = false;
    this.bufferAmount = 4;
    this.selectableGroup = false;
    this.selectableGroupAsModel = true;
    this.searchFn = null;
    this.trackByFn = null;
    this.clearOnBackspace = true;
    this.labelForId = null;
    this.inputAttrs = {};
    this.tabFocusOnClearButton = input(true, {
      transform: booleanAttribute
    });
    this.readonly = false;
    this.searchWhileComposing = true;
    this.minTermLength = 0;
    this.editableSearchTerm = false;
    this.ngClass = null;
    this.multiple = false;
    this.addTag = false;
    this.searchable = true;
    this.clearable = true;
    this.isOpen = false;
    this.blurEvent = new EventEmitter();
    this.focusEvent = new EventEmitter();
    this.changeEvent = new EventEmitter();
    this.openEvent = new EventEmitter();
    this.closeEvent = new EventEmitter();
    this.searchEvent = new EventEmitter();
    this.clearEvent = new EventEmitter();
    this.addEvent = new EventEmitter();
    this.removeEvent = new EventEmitter();
    this.scroll = new EventEmitter();
    this.scrollToEnd = new EventEmitter();
    this.useDefaultClass = true;
    this.viewPortItems = [];
    this.searchTerm = null;
    this.dropdownId = newId();
    this.escapeHTML = true;
    this._defaultLabel = "label";
    this._pressedKeys = [];
    this._isComposing = false;
    this._destroy$ = new Subject();
    this._keyPress$ = new Subject();
    this._items = [];
    this.keyDownFn = (_) => true;
    this.clearItem = (item) => {
      const option = this.selectedItems.find((x) => x.value === item);
      this.unselect(option);
    };
    this.trackByOption = (_, item) => {
      if (this.trackByFn) {
        return this.trackByFn(item.value);
      }
      return item;
    };
    this._onChange = (_) => {
    };
    this._onTouched = () => {
    };
    this._mergeGlobalConfig(config);
    this.itemsList = new ItemsList(this, newSelectionModel ? newSelectionModel() : DefaultSelectionModelFactory());
    this.element = _elementRef.nativeElement;
  }
  get filtered() {
    return !!this.searchTerm && this.searchable || this._isComposing;
  }
  get single() {
    return !this.multiple;
  }
  get items() {
    return this._items;
  }
  set items(value) {
    this._itemsAreUsed = true;
    this._items = value ?? [];
  }
  get disabled() {
    return this.readonly || this._disabled;
  }
  get compareWith() {
    return this._compareWith;
  }
  set compareWith(fn) {
    if (fn !== void 0 && fn !== null && !isFunction(fn)) {
      throw Error("`compareWith` must be a function.");
    }
    this._compareWith = fn;
  }
  get clearSearchOnAdd() {
    if (isDefined(this._clearSearchOnAdd)) {
      return this._clearSearchOnAdd;
    } else if (isDefined(this.config.clearSearchOnAdd)) {
      return this.config.clearSearchOnAdd;
    }
    return this.closeOnSelect;
  }
  set clearSearchOnAdd(value) {
    this._clearSearchOnAdd = value;
  }
  get deselectOnClick() {
    if (isDefined(this._deselectOnClick)) {
      return this._deselectOnClick;
    } else if (isDefined(this.config.deselectOnClick)) {
      return this.config.deselectOnClick;
    }
    return this.multiple;
  }
  set deselectOnClick(value) {
    this._deselectOnClick = value;
  }
  get selectedItems() {
    return this.itemsList.selectedItems;
  }
  get selectedValues() {
    return this.selectedItems.map((x) => x.value);
  }
  get hasValue() {
    return this.selectedItems.length > 0;
  }
  get currentPanelPosition() {
    if (this.dropdownPanel) {
      return this.dropdownPanel.currentPosition;
    }
    return void 0;
  }
  get showAddTag() {
    if (!this._validTerm) {
      return false;
    }
    const term = this.searchTerm.toLowerCase().trim();
    return this.addTag && !this.itemsList.filteredItems.some((x) => x.label.toLowerCase() === term) && (!this.hideSelected && this.isOpen || !this.selectedItems.some((x) => x.label.toLowerCase() === term)) && !this.loading;
  }
  get _editableSearchTerm() {
    return this.editableSearchTerm && !this.multiple;
  }
  get _isTypeahead() {
    return this.typeahead && this.typeahead.observers.length > 0;
  }
  get _validTerm() {
    const term = this.searchTerm?.trim();
    return term && term.length >= this.minTermLength;
  }
  ngOnInit() {
    this._handleKeyPresses();
    this._setInputAttributes();
  }
  ngOnChanges(changes) {
    if (changes.multiple) {
      this.itemsList.clearSelected();
    }
    if (changes.items) {
      this._setItems(changes.items.currentValue || []);
    }
    if (changes.isOpen) {
      this._manualOpen = isDefined(changes.isOpen.currentValue);
    }
    if (changes.groupBy) {
      if (!changes.items) {
        this._setItems([...this.items]);
      }
    }
    if (changes.inputAttrs) {
      this._setInputAttributes();
    }
  }
  ngAfterViewInit() {
    if (!this._itemsAreUsed) {
      this.escapeHTML = false;
      this._setItemsFromNgOptions();
    }
    if (isDefined(this.autoFocus)) {
      this.focus();
    }
  }
  ngOnDestroy() {
    this._destroy$.next();
    this._destroy$.complete();
  }
  handleKeyDown($event) {
    const keyName = $event.key;
    if (Object.values(KeyCode).includes(keyName)) {
      if (this.keyDownFn($event) === false) {
        return;
      }
      this.handleKeyCode($event);
    } else if (keyName && keyName.length === 1) {
      this._keyPress$.next(keyName.toLocaleLowerCase());
    }
  }
  handleKeyCode($event) {
    const target = $event.target;
    if (this.clearButton && this.clearButton.nativeElement === target) {
      this.handleKeyCodeClear($event);
    } else {
      this.handleKeyCodeInput($event);
    }
  }
  handleKeyCodeInput($event) {
    switch ($event.key) {
      case KeyCode.ArrowDown:
        this._handleArrowDown($event);
        break;
      case KeyCode.ArrowUp:
        this._handleArrowUp($event);
        break;
      case KeyCode.Space:
        this._handleSpace($event);
        break;
      case KeyCode.Enter:
        this._handleEnter($event);
        break;
      case KeyCode.Tab:
        this._handleTab($event);
        break;
      case KeyCode.Esc:
        this.close();
        $event.preventDefault();
        break;
      case KeyCode.Backspace:
        this._handleBackspace();
        break;
    }
  }
  handleKeyCodeClear($event) {
    switch ($event.key) {
      case KeyCode.Enter:
        this.handleClearClick();
        $event.preventDefault();
        break;
    }
  }
  handleMousedown($event) {
    if (this.preventToggleOnRightClick && $event.button === 2) {
      return false;
    }
    const target = $event.target;
    if (target.tagName !== "INPUT") {
      $event.preventDefault();
    }
    if (target.classList.contains("ng-clear-wrapper")) {
      this.handleClearClick();
      return;
    }
    if (target.classList.contains("ng-arrow-wrapper")) {
      this.handleArrowClick();
      return;
    }
    if (target.classList.contains("ng-value-icon")) {
      return;
    }
    if (!this.focused) {
      this.focus();
    }
    if (this.searchable) {
      this.open();
    } else {
      this.toggle();
    }
  }
  handleArrowClick() {
    if (this.isOpen) {
      this.close();
    } else {
      this.open();
    }
  }
  handleClearClick() {
    if (this.hasValue) {
      this.itemsList.clearSelected(true);
      this._updateNgModel();
    }
    this._clearSearch();
    this.focus();
    this.clearEvent.emit();
    this._onSelectionChanged();
  }
  clearModel() {
    if (!this.clearable) {
      return;
    }
    this.itemsList.clearSelected();
    this._updateNgModel();
  }
  writeValue(value) {
    this.itemsList.clearSelected();
    this._handleWriteValue(value);
    this._cd.markForCheck();
  }
  registerOnChange(fn) {
    this._onChange = fn;
  }
  registerOnTouched(fn) {
    this._onTouched = fn;
  }
  setDisabledState(state) {
    this._disabled = state;
    this._cd.markForCheck();
  }
  toggle() {
    if (!this.isOpen) {
      this.open();
    } else {
      this.close();
    }
  }
  open() {
    if (this.disabled || this.isOpen || this._manualOpen) {
      return;
    }
    if (!this._isTypeahead && !this.addTag && this.itemsList.noItemsToSelect) {
      return;
    }
    this.isOpen = true;
    this.itemsList.markSelectedOrDefault(this.markFirst);
    this.openEvent.emit();
    if (!this.searchTerm) {
      this.focus();
    }
    this.detectChanges();
  }
  close() {
    if (!this.isOpen || this._manualOpen) {
      return;
    }
    this.isOpen = false;
    this._isComposing = false;
    if (!this._editableSearchTerm) {
      this._clearSearch();
    } else {
      this.itemsList.resetFilteredItems();
    }
    this.itemsList.unmarkItem();
    this._onTouched();
    this.closeEvent.emit();
    this._cd.markForCheck();
  }
  toggleItem(item) {
    if (!item || item.disabled || this.disabled) {
      return;
    }
    if (this.deselectOnClick && item.selected) {
      this.unselect(item);
    } else {
      this.select(item);
    }
    if (this._editableSearchTerm) {
      this._setSearchTermFromItems();
    }
  }
  select(item) {
    if (!item.selected) {
      this.itemsList.select(item);
      if (this.clearSearchOnAdd && !this._editableSearchTerm) {
        this._clearSearch();
      }
      this._updateNgModel();
      if (this.multiple) {
        this.addEvent.emit(item.value);
      }
    }
    if (this.closeOnSelect || this.itemsList.noItemsToSelect) {
      this.close();
    }
    this._onSelectionChanged();
  }
  focus() {
    this.searchInput.nativeElement.focus();
  }
  blur() {
    this.searchInput.nativeElement.blur();
  }
  unselect(item) {
    if (!item) {
      return;
    }
    this.itemsList.unselect(item);
    this.focus();
    this._updateNgModel();
    this.removeEvent.emit(item.value);
    this._onSelectionChanged();
  }
  selectTag() {
    let tag;
    if (isFunction(this.addTag)) {
      tag = this.addTag(this.searchTerm);
    } else {
      tag = this._primitive ? this.searchTerm : {
        [this.bindLabel]: this.searchTerm
      };
    }
    const handleTag = (item) => this._isTypeahead || !this.isOpen ? this.itemsList.mapItem(item, null) : this.itemsList.addItem(item);
    if (isPromise2(tag)) {
      tag.then((item) => this.select(handleTag(item))).catch(() => {
      });
    } else if (tag) {
      this.select(handleTag(tag));
    }
  }
  showClear() {
    return this.clearable && (this.hasValue || this.searchTerm) && !this.disabled;
  }
  focusOnClear() {
    this.blur();
    if (this.clearButton) {
      this.clearButton.nativeElement.focus();
    }
  }
  showNoItemsFound() {
    const empty = this.itemsList.filteredItems.length === 0;
    return (empty && !this._isTypeahead && !this.loading || empty && this._isTypeahead && this._validTerm && !this.loading) && !this.showAddTag;
  }
  showTypeToSearch() {
    const empty = this.itemsList.filteredItems.length === 0;
    return empty && this._isTypeahead && !this._validTerm && !this.loading;
  }
  onCompositionStart() {
    this._isComposing = true;
  }
  onCompositionEnd(term) {
    this._isComposing = false;
    if (this.searchWhileComposing) {
      return;
    }
    this.filter(term);
  }
  filter(term) {
    if (this._isComposing && !this.searchWhileComposing) {
      return;
    }
    this.searchTerm = term;
    if (this._isTypeahead && (this._validTerm || this.minTermLength === 0)) {
      this.typeahead.next(term);
    }
    if (!this._isTypeahead) {
      this.itemsList.filter(this.searchTerm);
      if (this.isOpen) {
        this.itemsList.markSelectedOrDefault(this.markFirst);
      }
    }
    this.searchEvent.emit({
      term,
      items: this.itemsList.filteredItems.map((x) => x.value)
    });
    this.open();
  }
  onInputFocus($event) {
    if (this.focused) {
      return;
    }
    if (this._editableSearchTerm) {
      this._setSearchTermFromItems();
    }
    this.element.classList.add("ng-select-focused");
    this.focusEvent.emit($event);
    this.focused = true;
  }
  onInputBlur($event) {
    this.element.classList.remove("ng-select-focused");
    this.blurEvent.emit($event);
    if (!this.isOpen && !this.disabled) {
      this._onTouched();
    }
    if (this._editableSearchTerm) {
      this._setSearchTermFromItems();
    }
    this.focused = false;
  }
  onItemHover(item) {
    if (item.disabled) {
      return;
    }
    this.itemsList.markItem(item);
  }
  detectChanges() {
    if (!this._cd.destroyed) {
      this._cd.detectChanges();
    }
  }
  _setSearchTermFromItems() {
    const selected = this.selectedItems?.[0];
    this.searchTerm = selected?.label ?? null;
  }
  _setItems(items) {
    const firstItem = items[0];
    this.bindLabel = this.bindLabel || this._defaultLabel;
    this._primitive = isDefined(firstItem) ? !isObject2(firstItem) : this._primitive || this.bindLabel === this._defaultLabel;
    this.itemsList.setItems(items);
    if (items.length > 0 && this.hasValue) {
      this.itemsList.mapSelectedItems();
    }
    if (this.isOpen && isDefined(this.searchTerm) && !this._isTypeahead) {
      this.itemsList.filter(this.searchTerm);
    }
    if (this._isTypeahead || this.isOpen) {
      this.itemsList.markSelectedOrDefault(this.markFirst);
    }
  }
  _setItemsFromNgOptions() {
    const mapNgOptions = (options) => {
      this.items = options.map((option) => ({
        $ngOptionValue: option.value,
        $ngOptionLabel: option.elementRef.nativeElement.innerHTML,
        disabled: option.disabled
      }));
      this.itemsList.setItems(this.items);
      if (this.hasValue) {
        this.itemsList.mapSelectedItems();
      }
      this.detectChanges();
    };
    const handleOptionChange = () => {
      const changedOrDestroyed = merge(this.ngOptions.changes, this._destroy$);
      merge(...this.ngOptions.map((option) => option.stateChange$)).pipe(takeUntil(changedOrDestroyed)).subscribe((option) => {
        const item = this.itemsList.findItem(option.value);
        item.disabled = option.disabled;
        item.label = option.label || item.label;
        this._cd.detectChanges();
      });
    };
    this.ngOptions.changes.pipe(startWith(this.ngOptions), takeUntil(this._destroy$)).subscribe((options) => {
      this.bindLabel = this._defaultLabel;
      mapNgOptions(options);
      handleOptionChange();
    });
  }
  _isValidWriteValue(value) {
    if (!isDefined(value) || this.multiple && value === "" || Array.isArray(value) && value.length === 0) {
      return false;
    }
    const validateBinding = (item) => {
      if (!isDefined(this.compareWith) && isObject2(item) && this.bindValue) {
        this._console.warn(`Setting object(${JSON.stringify(item)}) as your model with bindValue is not allowed unless [compareWith] is used.`);
        return false;
      }
      return true;
    };
    if (this.multiple) {
      if (!Array.isArray(value)) {
        this._console.warn("Multiple select ngModel should be array.");
        return false;
      }
      return value.every((item) => validateBinding(item));
    } else {
      return validateBinding(value);
    }
  }
  _handleWriteValue(ngModel) {
    if (!this._isValidWriteValue(ngModel)) {
      return;
    }
    const select = (val) => {
      let item = this.itemsList.findItem(val);
      if (item) {
        this.itemsList.select(item);
      } else {
        const isValObject = isObject2(val);
        const isPrimitive = !isValObject && !this.bindValue;
        if (isValObject || isPrimitive) {
          this.itemsList.select(this.itemsList.mapItem(val, null));
        } else if (this.bindValue) {
          item = {
            [this.bindLabel]: null,
            [this.bindValue]: val
          };
          this.itemsList.select(this.itemsList.mapItem(item, null));
        }
      }
    };
    if (this.multiple) {
      ngModel.forEach((item) => select(item));
    } else {
      select(ngModel);
    }
  }
  _handleKeyPresses() {
    if (this.searchable) {
      return;
    }
    this._keyPress$.pipe(takeUntil(this._destroy$), tap((letter) => this._pressedKeys.push(letter)), debounceTime(200), filter(() => this._pressedKeys.length > 0), map(() => this._pressedKeys.join(""))).subscribe((term) => {
      const item = this.itemsList.findByLabel(term);
      if (item) {
        if (this.isOpen) {
          this.itemsList.markItem(item);
          this._scrollToMarked();
          this._cd.markForCheck();
        } else {
          this.select(item);
        }
      }
      this._pressedKeys = [];
    });
  }
  _setInputAttributes() {
    const input2 = this.searchInput.nativeElement;
    const attributes = __spreadValues({
      type: "text",
      autocorrect: "off",
      autocapitalize: "off",
      autocomplete: "off",
      "aria-controls": this.dropdownId
    }, this.inputAttrs);
    for (const key of Object.keys(attributes)) {
      input2.setAttribute(key, attributes[key]);
    }
  }
  _updateNgModel() {
    const model = [];
    for (const item of this.selectedItems) {
      if (this.bindValue) {
        let value = null;
        if (item.children) {
          const groupKey = this.groupValue ? this.bindValue : this.groupBy;
          value = item.value[groupKey || this.groupBy];
        } else {
          value = this.itemsList.resolveNested(item.value, this.bindValue);
        }
        model.push(value);
      } else {
        model.push(item.value);
      }
    }
    const selected = this.selectedItems.map((x) => x.value);
    if (this.multiple) {
      this._onChange(model);
      this.changeEvent.emit(selected);
    } else {
      this._onChange(isDefined(model[0]) ? model[0] : null);
      this.changeEvent.emit(selected[0]);
    }
    this._cd.markForCheck();
  }
  _clearSearch() {
    if (!this.searchTerm) {
      return;
    }
    this._changeSearch(null);
    this.itemsList.resetFilteredItems();
  }
  _changeSearch(searchTerm) {
    this.searchTerm = searchTerm;
    if (this._isTypeahead) {
      this.typeahead.next(searchTerm);
    }
  }
  _scrollToMarked() {
    if (!this.isOpen || !this.dropdownPanel) {
      return;
    }
    this.dropdownPanel.scrollTo(this.itemsList.markedItem);
  }
  _scrollToTag() {
    if (!this.isOpen || !this.dropdownPanel) {
      return;
    }
    this.dropdownPanel.scrollToTag();
  }
  _onSelectionChanged() {
    if (this.isOpen && this.deselectOnClick && this.appendTo) {
      this._cd.detectChanges();
      this.dropdownPanel.adjustPosition();
    }
  }
  _handleTab($event) {
    if (this.isOpen === false) {
      if (this.showClear() && !$event.shiftKey && this.tabFocusOnClearButton()) {
        this.focusOnClear();
        $event.preventDefault();
      } else if (!this.addTag) {
        return;
      }
    }
    if (this.selectOnTab) {
      if (this.itemsList.markedItem) {
        this.toggleItem(this.itemsList.markedItem);
        $event.preventDefault();
      } else if (this.showAddTag) {
        this.selectTag();
        $event.preventDefault();
      } else {
        this.close();
      }
    } else {
      this.close();
    }
  }
  _handleEnter($event) {
    if (this.isOpen || this._manualOpen) {
      if (this.itemsList.markedItem) {
        this.toggleItem(this.itemsList.markedItem);
      } else if (this.showAddTag) {
        this.selectTag();
      }
    } else if (this.openOnEnter) {
      this.open();
    } else {
      return;
    }
    $event.preventDefault();
  }
  _handleSpace($event) {
    if (this.isOpen || this._manualOpen) {
      return;
    }
    this.open();
    $event.preventDefault();
  }
  _handleArrowDown($event) {
    if (this._nextItemIsTag(1)) {
      this.itemsList.unmarkItem();
      this._scrollToTag();
    } else {
      this.itemsList.markNextItem();
      this._scrollToMarked();
    }
    this.open();
    $event.preventDefault();
  }
  _handleArrowUp($event) {
    if (!this.isOpen) {
      return;
    }
    if (this._nextItemIsTag(-1)) {
      this.itemsList.unmarkItem();
      this._scrollToTag();
    } else {
      this.itemsList.markPreviousItem();
      this._scrollToMarked();
    }
    $event.preventDefault();
  }
  _nextItemIsTag(nextStep) {
    const nextIndex = this.itemsList.markedIndex + nextStep;
    return this.addTag && this.searchTerm && this.itemsList.markedItem && (nextIndex < 0 || nextIndex === this.itemsList.filteredItems.length);
  }
  _handleBackspace() {
    if (this.searchTerm || !this.clearable || !this.clearOnBackspace || !this.hasValue) {
      return;
    }
    if (this.multiple) {
      this.unselect(this.itemsList.lastSelectedItem);
    } else {
      this.clearModel();
    }
  }
  _mergeGlobalConfig(config) {
    this.placeholder = this.placeholder || config.placeholder;
    this.fixedPlaceholder = this.fixedPlaceholder || config.fixedPlaceholder;
    this.notFoundText = this.notFoundText || config.notFoundText;
    this.typeToSearchText = this.typeToSearchText || config.typeToSearchText;
    this.addTagText = this.addTagText || config.addTagText;
    this.loadingText = this.loadingText || config.loadingText;
    this.clearAllText = this.clearAllText || config.clearAllText;
    this.virtualScroll = this.getVirtualScroll(config);
    this.openOnEnter = isDefined(this.openOnEnter) ? this.openOnEnter : config.openOnEnter;
    this.appendTo = this.appendTo || config.appendTo;
    this.bindValue = this.bindValue || config.bindValue;
    this.bindLabel = this.bindLabel || config.bindLabel;
    this.appearance = this.appearance || config.appearance;
  }
  /**
   * Gets virtual scroll value from input or from config
   *
   *  @param config NgSelectConfig object
   *
   *  @returns `true` if virtual scroll is enabled, `false` otherwise
   */
  getVirtualScroll(config) {
    return isDefined(this.virtualScroll) ? this.virtualScroll : this.isVirtualScrollDisabled(config);
  }
  /**
   * Gets disableVirtualScroll value from input or from config
   *
   *  @param config NgSelectConfig object
   *
   *  @returns `true` if disableVirtualScroll is enabled, `false` otherwise
   */
  isVirtualScrollDisabled(config) {
    return isDefined(config.disableVirtualScroll) ? !config.disableVirtualScroll : false;
  }
  static {
    this.ɵfac = function NgSelectComponent_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _NgSelectComponent)(ɵɵinjectAttribute("class"), ɵɵinjectAttribute("autofocus"), ɵɵdirectiveInject(NgSelectConfig), ɵɵdirectiveInject(SELECTION_MODEL_FACTORY, 8), ɵɵdirectiveInject(ElementRef), ɵɵdirectiveInject(ChangeDetectorRef), ɵɵdirectiveInject(ConsoleService));
    };
  }
  static {
    this.ɵcmp = ɵɵdefineComponent({
      type: _NgSelectComponent,
      selectors: [["ng-select"]],
      contentQueries: function NgSelectComponent_ContentQueries(rf, ctx, dirIndex) {
        if (rf & 1) {
          ɵɵcontentQuery(dirIndex, NgOptionTemplateDirective, 5, TemplateRef);
          ɵɵcontentQuery(dirIndex, NgOptgroupTemplateDirective, 5, TemplateRef);
          ɵɵcontentQuery(dirIndex, NgLabelTemplateDirective, 5, TemplateRef);
          ɵɵcontentQuery(dirIndex, NgMultiLabelTemplateDirective, 5, TemplateRef);
          ɵɵcontentQuery(dirIndex, NgHeaderTemplateDirective, 5, TemplateRef);
          ɵɵcontentQuery(dirIndex, NgFooterTemplateDirective, 5, TemplateRef);
          ɵɵcontentQuery(dirIndex, NgNotFoundTemplateDirective, 5, TemplateRef);
          ɵɵcontentQuery(dirIndex, NgPlaceholderTemplateDirective, 5, TemplateRef);
          ɵɵcontentQuery(dirIndex, NgTypeToSearchTemplateDirective, 5, TemplateRef);
          ɵɵcontentQuery(dirIndex, NgLoadingTextTemplateDirective, 5, TemplateRef);
          ɵɵcontentQuery(dirIndex, NgTagTemplateDirective, 5, TemplateRef);
          ɵɵcontentQuery(dirIndex, NgLoadingSpinnerTemplateDirective, 5, TemplateRef);
          ɵɵcontentQuery(dirIndex, NgClearButtonTemplateDirective, 5, TemplateRef);
          ɵɵcontentQuery(dirIndex, NgOptionComponent, 5);
        }
        if (rf & 2) {
          let _t;
          ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.optionTemplate = _t.first);
          ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.optgroupTemplate = _t.first);
          ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.labelTemplate = _t.first);
          ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.multiLabelTemplate = _t.first);
          ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.headerTemplate = _t.first);
          ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.footerTemplate = _t.first);
          ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.notFoundTemplate = _t.first);
          ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.placeholderTemplate = _t.first);
          ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.typeToSearchTemplate = _t.first);
          ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.loadingTextTemplate = _t.first);
          ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.tagTemplate = _t.first);
          ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.loadingSpinnerTemplate = _t.first);
          ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.clearButtonTemplate = _t.first);
          ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.ngOptions = _t);
        }
      },
      viewQuery: function NgSelectComponent_Query(rf, ctx) {
        if (rf & 1) {
          ɵɵviewQuery(NgDropdownPanelComponent, 5);
          ɵɵviewQuery(_c5, 7);
          ɵɵviewQuery(_c6, 5);
        }
        if (rf & 2) {
          let _t;
          ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.dropdownPanel = _t.first);
          ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.searchInput = _t.first);
          ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.clearButton = _t.first);
        }
      },
      hostVars: 20,
      hostBindings: function NgSelectComponent_HostBindings(rf, ctx) {
        if (rf & 1) {
          ɵɵlistener("keydown", function NgSelectComponent_keydown_HostBindingHandler($event) {
            return ctx.handleKeyDown($event);
          });
        }
        if (rf & 2) {
          ɵɵclassProp("ng-select-typeahead", ctx.typeahead)("ng-select-multiple", ctx.multiple)("ng-select-taggable", ctx.addTag)("ng-select-searchable", ctx.searchable)("ng-select-clearable", ctx.clearable)("ng-select-opened", ctx.isOpen)("ng-select", ctx.useDefaultClass)("ng-select-filtered", ctx.filtered)("ng-select-single", ctx.single)("ng-select-disabled", ctx.disabled);
        }
      },
      inputs: {
        ariaLabelDropdown: "ariaLabelDropdown",
        bindLabel: "bindLabel",
        bindValue: "bindValue",
        ariaLabel: "ariaLabel",
        markFirst: [2, "markFirst", "markFirst", booleanAttribute],
        placeholder: "placeholder",
        fixedPlaceholder: "fixedPlaceholder",
        notFoundText: "notFoundText",
        typeToSearchText: "typeToSearchText",
        preventToggleOnRightClick: "preventToggleOnRightClick",
        addTagText: "addTagText",
        loadingText: "loadingText",
        clearAllText: "clearAllText",
        appearance: "appearance",
        dropdownPosition: "dropdownPosition",
        appendTo: "appendTo",
        loading: [2, "loading", "loading", booleanAttribute],
        closeOnSelect: [2, "closeOnSelect", "closeOnSelect", booleanAttribute],
        hideSelected: [2, "hideSelected", "hideSelected", booleanAttribute],
        selectOnTab: [2, "selectOnTab", "selectOnTab", booleanAttribute],
        openOnEnter: [2, "openOnEnter", "openOnEnter", booleanAttribute],
        maxSelectedItems: [2, "maxSelectedItems", "maxSelectedItems", numberAttribute],
        groupBy: "groupBy",
        groupValue: "groupValue",
        bufferAmount: [2, "bufferAmount", "bufferAmount", numberAttribute],
        virtualScroll: [2, "virtualScroll", "virtualScroll", booleanAttribute],
        selectableGroup: [2, "selectableGroup", "selectableGroup", booleanAttribute],
        selectableGroupAsModel: [2, "selectableGroupAsModel", "selectableGroupAsModel", booleanAttribute],
        searchFn: "searchFn",
        trackByFn: "trackByFn",
        clearOnBackspace: [2, "clearOnBackspace", "clearOnBackspace", booleanAttribute],
        labelForId: "labelForId",
        inputAttrs: "inputAttrs",
        tabIndex: [2, "tabIndex", "tabIndex", numberAttribute],
        tabFocusOnClearButton: [1, "tabFocusOnClearButton"],
        readonly: [2, "readonly", "readonly", booleanAttribute],
        searchWhileComposing: [2, "searchWhileComposing", "searchWhileComposing", booleanAttribute],
        minTermLength: [2, "minTermLength", "minTermLength", numberAttribute],
        editableSearchTerm: [2, "editableSearchTerm", "editableSearchTerm", booleanAttribute],
        ngClass: "ngClass",
        typeahead: "typeahead",
        multiple: [2, "multiple", "multiple", booleanAttribute],
        addTag: "addTag",
        searchable: [2, "searchable", "searchable", booleanAttribute],
        clearable: [2, "clearable", "clearable", booleanAttribute],
        isOpen: "isOpen",
        items: "items",
        compareWith: "compareWith",
        clearSearchOnAdd: "clearSearchOnAdd",
        deselectOnClick: "deselectOnClick",
        keyDownFn: "keyDownFn"
      },
      outputs: {
        blurEvent: "blur",
        focusEvent: "focus",
        changeEvent: "change",
        openEvent: "open",
        closeEvent: "close",
        searchEvent: "search",
        clearEvent: "clear",
        addEvent: "add",
        removeEvent: "remove",
        scroll: "scroll",
        scrollToEnd: "scrollToEnd"
      },
      features: [ɵɵProvidersFeature([{
        provide: NG_VALUE_ACCESSOR,
        useExisting: forwardRef(() => _NgSelectComponent),
        multi: true
      }, NgDropdownPanelService]), ɵɵNgOnChangesFeature],
      decls: 15,
      vars: 20,
      consts: [["searchInput", ""], ["defaultPlaceholderTemplate", ""], ["defaultLabelTemplate", ""], ["defaultLoadingSpinnerTemplate", ""], ["clearButton", ""], ["defaultOptionTemplate", ""], ["defaultTagTemplate", ""], ["defaultNotFoundTemplate", ""], ["defaultTypeToSearchTemplate", ""], ["defaultLoadingTextTemplate", ""], [1, "ng-select-container", 3, "mousedown"], [1, "ng-value-container"], [3, "ngTemplateOutlet", "ngTemplateOutletContext"], [1, "ng-input"], ["aria-autocomplete", "list", "role", "combobox", 3, "blur", "change", "compositionend", "compositionstart", "focus", "input", "disabled", "readOnly", "value"], [1, "ng-arrow-wrapper"], [1, "ng-arrow"], [1, "ng-dropdown-panel", 3, "virtualScroll", "bufferAmount", "appendTo", "position", "headerTemplate", "footerTemplate", "filterValue", "items", "markedItem", "ng-select-multiple", "ngClass", "id", "ariaLabelDropdown"], ["aria-atomic", "true", "aria-live", "polite", "role", "status", 1, "ng-visually-hidden"], [3, "ngTemplateOutlet"], [1, "ng-placeholder"], [1, "ng-value", 3, "ng-value-disabled"], [1, "ng-value"], ["aria-hidden", "true", 1, "ng-value-icon", "left", 3, "click"], [1, "ng-value-label", 3, "ngItemLabel", "escape"], [1, "ng-spinner-loader"], ["role", "button", "tabindex", "0", 1, "ng-clear-wrapper", 3, "title"], ["aria-hidden", "true", 1, "ng-clear"], [1, "ng-dropdown-panel", 3, "update", "scroll", "scrollToEnd", "outsideClick", "virtualScroll", "bufferAmount", "appendTo", "position", "headerTemplate", "footerTemplate", "filterValue", "items", "markedItem", "ngClass", "id", "ariaLabelDropdown"], [1, "ng-option", 3, "ng-option-disabled", "ng-option-selected", "ng-optgroup", "ng-option", "ng-option-child", "ng-option-marked"], ["role", "option", 1, "ng-option", 3, "ng-option-marked"], [1, "ng-option", 3, "click", "mouseover"], [1, "ng-option-label", 3, "ngItemLabel", "escape"], ["role", "option", 1, "ng-option", 3, "mouseover", "click"], [1, "ng-tag-label"], [1, "ng-option", "ng-option-disabled"]],
      template: function NgSelectComponent_Template(rf, ctx) {
        if (rf & 1) {
          const _r1 = ɵɵgetCurrentView();
          ɵɵelementStart(0, "div", 10);
          ɵɵlistener("mousedown", function NgSelectComponent_Template_div_mousedown_0_listener($event) {
            ɵɵrestoreView(_r1);
            return ɵɵresetView(ctx.handleMousedown($event));
          });
          ɵɵelementStart(1, "div", 11);
          ɵɵtemplate(2, NgSelectComponent_Conditional_2_Template, 3, 1)(3, NgSelectComponent_Conditional_3_Template, 2, 0)(4, NgSelectComponent_Conditional_4_Template, 1, 5, null, 12);
          ɵɵelementStart(5, "div", 13)(6, "input", 14, 0);
          ɵɵlistener("blur", function NgSelectComponent_Template_input_blur_6_listener($event) {
            ɵɵrestoreView(_r1);
            return ɵɵresetView(ctx.onInputBlur($event));
          })("change", function NgSelectComponent_Template_input_change_6_listener($event) {
            ɵɵrestoreView(_r1);
            return ɵɵresetView($event.stopPropagation());
          })("compositionend", function NgSelectComponent_Template_input_compositionend_6_listener() {
            ɵɵrestoreView(_r1);
            const searchInput_r7 = ɵɵreference(7);
            return ɵɵresetView(ctx.onCompositionEnd(searchInput_r7.value));
          })("compositionstart", function NgSelectComponent_Template_input_compositionstart_6_listener() {
            ɵɵrestoreView(_r1);
            return ɵɵresetView(ctx.onCompositionStart());
          })("focus", function NgSelectComponent_Template_input_focus_6_listener($event) {
            ɵɵrestoreView(_r1);
            return ɵɵresetView(ctx.onInputFocus($event));
          })("input", function NgSelectComponent_Template_input_input_6_listener() {
            ɵɵrestoreView(_r1);
            const searchInput_r7 = ɵɵreference(7);
            return ɵɵresetView(ctx.filter(searchInput_r7.value));
          });
          ɵɵelementEnd()()();
          ɵɵtemplate(8, NgSelectComponent_Conditional_8_Template, 3, 1)(9, NgSelectComponent_Conditional_9_Template, 2, 1);
          ɵɵelementStart(10, "span", 15);
          ɵɵelement(11, "span", 16);
          ɵɵelementEnd()();
          ɵɵtemplate(12, NgSelectComponent_Conditional_12_Template, 8, 18, "ng-dropdown-panel", 17);
          ɵɵelementStart(13, "div", 18);
          ɵɵtemplate(14, NgSelectComponent_Conditional_14_Template, 1, 1);
          ɵɵelementEnd();
        }
        if (rf & 2) {
          let tmp_8_0;
          ɵɵclassProp("ng-appearance-outline", ctx.appearance === "outline")("ng-has-value", ctx.hasValue);
          ɵɵadvance(2);
          ɵɵconditional(ctx.selectedItems.length === 0 && !ctx.searchTerm || ctx.fixedPlaceholder ? 2 : -1);
          ɵɵadvance();
          ɵɵconditional((!ctx.multiLabelTemplate || !ctx.multiple) && ctx.selectedItems.length > 0 ? 3 : -1);
          ɵɵadvance();
          ɵɵconditional(ctx.multiple && ctx.multiLabelTemplate && ctx.selectedValues.length > 0 ? 4 : -1);
          ɵɵadvance(2);
          ɵɵproperty("disabled", ctx.disabled)("readOnly", !ctx.searchable || ctx.itemsList.maxItemsSelected)("value", (tmp_8_0 = ctx.searchTerm) !== null && tmp_8_0 !== void 0 ? tmp_8_0 : "");
          ɵɵattribute("aria-activedescendant", ctx.isOpen ? ctx.itemsList == null ? null : ctx.itemsList.markedItem == null ? null : ctx.itemsList.markedItem.htmlId : null)("aria-controls", ctx.isOpen ? ctx.dropdownId : null)("aria-expanded", ctx.isOpen)("aria-label", ctx.ariaLabel)("id", ctx.labelForId)("tabindex", ctx.tabIndex);
          ɵɵadvance(2);
          ɵɵconditional(ctx.loading ? 8 : -1);
          ɵɵadvance();
          ɵɵconditional(ctx.showClear() ? 9 : -1);
          ɵɵadvance(3);
          ɵɵconditional(ctx.isOpen ? 12 : -1);
          ɵɵadvance(2);
          ɵɵconditional(ctx.isOpen && ctx.showNoItemsFound() ? 14 : -1);
        }
      },
      dependencies: [NgTemplateOutlet, NgItemLabelDirective, NgDropdownPanelComponent, NgClass],
      styles: ['@charset "UTF-8";.ng-select{position:relative;display:block;-webkit-box-sizing:border-box;-moz-box-sizing:border-box;box-sizing:border-box}.ng-select div,.ng-select input,.ng-select span{-webkit-box-sizing:border-box;-moz-box-sizing:border-box;box-sizing:border-box}.ng-select [hidden]{display:none}.ng-select.ng-select-searchable .ng-select-container .ng-value-container .ng-input{opacity:1}.ng-select.ng-select-opened .ng-select-container{z-index:1001}.ng-select.ng-select-disabled .ng-select-container .ng-value-container .ng-placeholder,.ng-select.ng-select-disabled .ng-select-container .ng-value-container .ng-value{-webkit-user-select:none;user-select:none;cursor:default}.ng-select.ng-select-disabled .ng-arrow-wrapper{cursor:default}.ng-select.ng-select-filtered .ng-placeholder{display:none}.ng-select .ng-select-container{cursor:default;display:flex;outline:none;overflow:hidden;position:relative;width:100%}.ng-select .ng-select-container .ng-value-container{display:flex;flex:1}.ng-select .ng-select-container .ng-value-container .ng-input{opacity:0}.ng-select .ng-select-container .ng-value-container .ng-input>input{box-sizing:content-box;background:none transparent;border:0 none;box-shadow:none;outline:none;padding:0;cursor:default;width:100%}.ng-select .ng-select-container .ng-value-container .ng-input>input::-ms-clear{display:none}.ng-select .ng-select-container .ng-value-container .ng-input>input[readonly]{-webkit-user-select:unset;user-select:unset;width:0;padding:0}.ng-select.ng-select-single.ng-select-filtered .ng-select-container .ng-value-container .ng-value{visibility:hidden}.ng-select.ng-select-single .ng-select-container .ng-value-container,.ng-select.ng-select-single .ng-select-container .ng-value-container .ng-value{white-space:nowrap;overflow:hidden;text-overflow:ellipsis}.ng-select.ng-select-single .ng-select-container .ng-value-container .ng-value .ng-value-icon{display:none}.ng-select.ng-select-single .ng-select-container .ng-value-container .ng-input{position:absolute;left:0;width:100%}.ng-select.ng-select-multiple.ng-select-disabled>.ng-select-container .ng-value-container .ng-value .ng-value-icon{display:none}.ng-select.ng-select-multiple .ng-select-container .ng-value-container{flex-wrap:wrap}.ng-select.ng-select-multiple .ng-select-container .ng-value-container .ng-placeholder{position:absolute}.ng-select.ng-select-multiple .ng-select-container .ng-value-container .ng-value{white-space:nowrap}.ng-select.ng-select-multiple .ng-select-container .ng-value-container .ng-value.ng-value-disabled .ng-value-icon{display:none}.ng-select.ng-select-multiple .ng-select-container .ng-value-container .ng-value .ng-value-icon{cursor:pointer}.ng-select.ng-select-multiple .ng-select-container .ng-value-container .ng-input{flex:1;z-index:2}.ng-select.ng-select-multiple .ng-select-container .ng-value-container .ng-placeholder{z-index:1}.ng-select .ng-clear-wrapper{cursor:pointer;position:relative;width:17px;-webkit-user-select:none;user-select:none}.ng-select .ng-clear-wrapper .ng-clear{display:inline-block;font-size:18px;line-height:1;pointer-events:none}.ng-select .ng-spinner-loader{border-radius:50%;width:17px;height:17px;margin-right:5px;font-size:10px;position:relative;text-indent:-9999em;border-top:2px solid rgba(66,66,66,.2);border-right:2px solid rgba(66,66,66,.2);border-bottom:2px solid rgba(66,66,66,.2);border-left:2px solid #424242;transform:translateZ(0);animation:load8 .8s infinite linear}.ng-select .ng-spinner-loader:after{border-radius:50%;width:17px;height:17px}@-webkit-keyframes load8{0%{-webkit-transform:rotate(0deg);transform:rotate(0)}to{-webkit-transform:rotate(360deg);transform:rotate(360deg)}}@keyframes load8{0%{-webkit-transform:rotate(0deg);transform:rotate(0)}to{-webkit-transform:rotate(360deg);transform:rotate(360deg)}}.ng-select .ng-arrow-wrapper{cursor:pointer;position:relative;text-align:center;-webkit-user-select:none;user-select:none}.ng-select .ng-arrow-wrapper .ng-arrow{pointer-events:none;display:inline-block;height:0;width:0;position:relative}.ng-dropdown-panel{box-sizing:border-box;position:absolute;opacity:0;width:100%;z-index:1050;-webkit-overflow-scrolling:touch}.ng-dropdown-panel .ng-dropdown-panel-items{display:block;height:auto;-webkit-box-sizing:border-box;-moz-box-sizing:border-box;box-sizing:border-box;max-height:240px;overflow-y:auto}.ng-dropdown-panel .ng-dropdown-panel-items .ng-optgroup{white-space:nowrap;overflow:hidden;text-overflow:ellipsis}.ng-dropdown-panel .ng-dropdown-panel-items .ng-option{box-sizing:border-box;cursor:pointer;display:block;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}.ng-dropdown-panel .ng-dropdown-panel-items .ng-option .ng-option-label:empty:before{content:"\\200b"}.ng-dropdown-panel .ng-dropdown-panel-items .ng-option .highlighted{font-weight:700;text-decoration:underline}.ng-dropdown-panel .ng-dropdown-panel-items .ng-option.disabled{cursor:default}.ng-dropdown-panel .scroll-host{overflow:hidden;overflow-y:auto;position:relative;display:block;-webkit-overflow-scrolling:touch}.ng-dropdown-panel .scrollable-content{top:0;left:0;width:100%;height:100%;position:absolute}.ng-dropdown-panel .total-padding{width:1px;opacity:0}.ng-visually-hidden{position:absolute!important;width:1px;height:1px;margin:-1px;padding:0;overflow:hidden;clip:rect(0 0 0 0);border:0;white-space:nowrap}\n'],
      encapsulation: 2,
      changeDetection: 0
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(NgSelectComponent, [{
    type: Component,
    args: [{
      selector: "ng-select",
      providers: [{
        provide: NG_VALUE_ACCESSOR,
        useExisting: forwardRef(() => NgSelectComponent),
        multi: true
      }, NgDropdownPanelService],
      encapsulation: ViewEncapsulation.None,
      changeDetection: ChangeDetectionStrategy.OnPush,
      imports: [NgTemplateOutlet, NgItemLabelDirective, NgDropdownPanelComponent, NgClass],
      template: `<div
	(mousedown)="handleMousedown($event)"
	[class.ng-appearance-outline]="appearance === 'outline'"
	[class.ng-has-value]="hasValue"
	class="ng-select-container">
	<div class="ng-value-container">
		@if ((selectedItems.length === 0 && !searchTerm) || fixedPlaceholder) {
			<ng-template #defaultPlaceholderTemplate>
				<div class="ng-placeholder">{{ placeholder }}</div>
			</ng-template>
			<ng-template [ngTemplateOutlet]="placeholderTemplate || defaultPlaceholderTemplate"> </ng-template>
		}

		@if ((!multiLabelTemplate || !multiple) && selectedItems.length > 0) {
			@for (item of selectedItems; track trackByOption($index, item)) {
				<div [class.ng-value-disabled]="item.disabled" class="ng-value">
					<ng-template #defaultLabelTemplate>
						<span class="ng-value-icon left" (click)="unselect(item)" aria-hidden="true">×</span>
						<span class="ng-value-label" [ngItemLabel]="item.label" [escape]="escapeHTML"></span>
					</ng-template>
					<ng-template
						[ngTemplateOutlet]="labelTemplate || defaultLabelTemplate"
						[ngTemplateOutletContext]="{ item: item.value, clear: clearItem, label: item.label }">
					</ng-template>
				</div>
			}
		}

		@if (multiple && multiLabelTemplate && selectedValues.length > 0) {
			<ng-template [ngTemplateOutlet]="multiLabelTemplate" [ngTemplateOutletContext]="{ items: selectedValues, clear: clearItem }">
			</ng-template>
		}

		<div class="ng-input">
			<input
				#searchInput
				(blur)="onInputBlur($event)"
				(change)="$event.stopPropagation()"
				(compositionend)="onCompositionEnd(searchInput.value)"
				(compositionstart)="onCompositionStart()"
				(focus)="onInputFocus($event)"
				(input)="filter(searchInput.value)"
				[attr.aria-activedescendant]="isOpen ? itemsList?.markedItem?.htmlId : null"
				[attr.aria-controls]="isOpen ? dropdownId : null"
				[attr.aria-expanded]="isOpen"
				[attr.aria-label]="ariaLabel"
				[attr.id]="labelForId"
				[attr.tabindex]="tabIndex"
				[disabled]="disabled"
				[readOnly]="!searchable || itemsList.maxItemsSelected"
				[value]="searchTerm ?? ''"
				aria-autocomplete="list"
				role="combobox" />
		</div>
	</div>

	@if (loading) {
		<ng-template #defaultLoadingSpinnerTemplate>
			<div class="ng-spinner-loader"></div>
		</ng-template>
		<ng-template [ngTemplateOutlet]="loadingSpinnerTemplate || defaultLoadingSpinnerTemplate"></ng-template>
	}

	@if (showClear()) {
		@if (clearButtonTemplate) {
			<ng-container [ngTemplateOutlet]="clearButtonTemplate"></ng-container>
		} @else {
			<span
				class="ng-clear-wrapper"
				role="button"
				tabindex="0"
				[attr.tabindex]="tabFocusOnClearButton() ? 0 : -1"
				title="{{ clearAllText }}"
				#clearButton>
				<span class="ng-clear" aria-hidden="true">×</span>
			</span>
		}
	}

	<span class="ng-arrow-wrapper">
		<span class="ng-arrow"></span>
	</span>
</div>

@if (isOpen) {
	<ng-dropdown-panel
		class="ng-dropdown-panel"
		[virtualScroll]="virtualScroll"
		[bufferAmount]="bufferAmount"
		[appendTo]="appendTo"
		[position]="dropdownPosition"
		[headerTemplate]="headerTemplate"
		[footerTemplate]="footerTemplate"
		[filterValue]="searchTerm"
		[items]="itemsList.filteredItems"
		[markedItem]="itemsList.markedItem"
		(update)="viewPortItems = $event"
		(scroll)="scroll.emit($event)"
		(scrollToEnd)="scrollToEnd.emit($event)"
		(outsideClick)="close()"
		[class.ng-select-multiple]="multiple"
		[ngClass]="appendTo ? (ngClass ? ngClass : classes) : null"
		[id]="dropdownId"
		[ariaLabelDropdown]="ariaLabelDropdown">
		<ng-container>
			@for (item of viewPortItems; track trackByOption($index, item)) {
				<div
					class="ng-option"
					[attr.role]="item.children ? 'group' : 'option'"
					(click)="toggleItem(item)"
					(mouseover)="onItemHover(item)"
					[class.ng-option-disabled]="item.disabled"
					[class.ng-option-selected]="item.selected"
					[class.ng-optgroup]="item.children"
					[class.ng-option]="!item.children"
					[class.ng-option-child]="!!item.parent"
					[class.ng-option-marked]="item === itemsList.markedItem"
					[attr.aria-selected]="item.selected"
					[attr.id]="item?.htmlId"
					[attr.aria-setsize]="itemsList.filteredItems.length"
					[attr.aria-posinset]="item.index + 1">
					<ng-template #defaultOptionTemplate>
						<span class="ng-option-label" [ngItemLabel]="item.label" [escape]="escapeHTML"></span>
					</ng-template>
					<ng-template
						[ngTemplateOutlet]="
							item.children ? optgroupTemplate || defaultOptionTemplate : optionTemplate || defaultOptionTemplate
						"
						[ngTemplateOutletContext]="{ item: item.value, item$: item, index: item.index, searchTerm: searchTerm }">
					</ng-template>
				</div>
			}
			@if (showAddTag) {
				<div
					class="ng-option"
					[class.ng-option-marked]="!itemsList.markedItem"
					(mouseover)="itemsList.unmarkItem()"
					role="option"
					(click)="selectTag()">
					<ng-template #defaultTagTemplate>
						<span
							><span class="ng-tag-label">{{ addTagText }}</span
							>"{{ searchTerm }}"</span
						>
					</ng-template>
					<ng-template
						[ngTemplateOutlet]="tagTemplate || defaultTagTemplate"
						[ngTemplateOutletContext]="{ searchTerm: searchTerm }">
					</ng-template>
				</div>
			}
		</ng-container>
		@if (showNoItemsFound()) {
			<ng-template #defaultNotFoundTemplate>
				<div class="ng-option ng-option-disabled">{{ notFoundText }}</div>
			</ng-template>
			<ng-template
				[ngTemplateOutlet]="notFoundTemplate || defaultNotFoundTemplate"
				[ngTemplateOutletContext]="{ searchTerm: searchTerm }">
			</ng-template>
		}
		@if (showTypeToSearch()) {
			<ng-template #defaultTypeToSearchTemplate>
				<div class="ng-option ng-option-disabled">{{ typeToSearchText }}</div>
			</ng-template>
			<ng-template [ngTemplateOutlet]="typeToSearchTemplate || defaultTypeToSearchTemplate"></ng-template>
		}
		@if (loading && itemsList.filteredItems.length === 0) {
			<ng-template #defaultLoadingTextTemplate>
				<div class="ng-option ng-option-disabled">{{ loadingText }}</div>
			</ng-template>
			<ng-template
				[ngTemplateOutlet]="loadingTextTemplate || defaultLoadingTextTemplate"
				[ngTemplateOutletContext]="{ searchTerm: searchTerm }">
			</ng-template>
		}
	</ng-dropdown-panel>
}

<!-- Always present aria-live region -->
<div aria-atomic="true" aria-live="polite" class="ng-visually-hidden" role="status">
	@if (isOpen && showNoItemsFound()) {
		{{ notFoundText }}
	}
</div>
`,
      styles: ['@charset "UTF-8";.ng-select{position:relative;display:block;-webkit-box-sizing:border-box;-moz-box-sizing:border-box;box-sizing:border-box}.ng-select div,.ng-select input,.ng-select span{-webkit-box-sizing:border-box;-moz-box-sizing:border-box;box-sizing:border-box}.ng-select [hidden]{display:none}.ng-select.ng-select-searchable .ng-select-container .ng-value-container .ng-input{opacity:1}.ng-select.ng-select-opened .ng-select-container{z-index:1001}.ng-select.ng-select-disabled .ng-select-container .ng-value-container .ng-placeholder,.ng-select.ng-select-disabled .ng-select-container .ng-value-container .ng-value{-webkit-user-select:none;user-select:none;cursor:default}.ng-select.ng-select-disabled .ng-arrow-wrapper{cursor:default}.ng-select.ng-select-filtered .ng-placeholder{display:none}.ng-select .ng-select-container{cursor:default;display:flex;outline:none;overflow:hidden;position:relative;width:100%}.ng-select .ng-select-container .ng-value-container{display:flex;flex:1}.ng-select .ng-select-container .ng-value-container .ng-input{opacity:0}.ng-select .ng-select-container .ng-value-container .ng-input>input{box-sizing:content-box;background:none transparent;border:0 none;box-shadow:none;outline:none;padding:0;cursor:default;width:100%}.ng-select .ng-select-container .ng-value-container .ng-input>input::-ms-clear{display:none}.ng-select .ng-select-container .ng-value-container .ng-input>input[readonly]{-webkit-user-select:unset;user-select:unset;width:0;padding:0}.ng-select.ng-select-single.ng-select-filtered .ng-select-container .ng-value-container .ng-value{visibility:hidden}.ng-select.ng-select-single .ng-select-container .ng-value-container,.ng-select.ng-select-single .ng-select-container .ng-value-container .ng-value{white-space:nowrap;overflow:hidden;text-overflow:ellipsis}.ng-select.ng-select-single .ng-select-container .ng-value-container .ng-value .ng-value-icon{display:none}.ng-select.ng-select-single .ng-select-container .ng-value-container .ng-input{position:absolute;left:0;width:100%}.ng-select.ng-select-multiple.ng-select-disabled>.ng-select-container .ng-value-container .ng-value .ng-value-icon{display:none}.ng-select.ng-select-multiple .ng-select-container .ng-value-container{flex-wrap:wrap}.ng-select.ng-select-multiple .ng-select-container .ng-value-container .ng-placeholder{position:absolute}.ng-select.ng-select-multiple .ng-select-container .ng-value-container .ng-value{white-space:nowrap}.ng-select.ng-select-multiple .ng-select-container .ng-value-container .ng-value.ng-value-disabled .ng-value-icon{display:none}.ng-select.ng-select-multiple .ng-select-container .ng-value-container .ng-value .ng-value-icon{cursor:pointer}.ng-select.ng-select-multiple .ng-select-container .ng-value-container .ng-input{flex:1;z-index:2}.ng-select.ng-select-multiple .ng-select-container .ng-value-container .ng-placeholder{z-index:1}.ng-select .ng-clear-wrapper{cursor:pointer;position:relative;width:17px;-webkit-user-select:none;user-select:none}.ng-select .ng-clear-wrapper .ng-clear{display:inline-block;font-size:18px;line-height:1;pointer-events:none}.ng-select .ng-spinner-loader{border-radius:50%;width:17px;height:17px;margin-right:5px;font-size:10px;position:relative;text-indent:-9999em;border-top:2px solid rgba(66,66,66,.2);border-right:2px solid rgba(66,66,66,.2);border-bottom:2px solid rgba(66,66,66,.2);border-left:2px solid #424242;transform:translateZ(0);animation:load8 .8s infinite linear}.ng-select .ng-spinner-loader:after{border-radius:50%;width:17px;height:17px}@-webkit-keyframes load8{0%{-webkit-transform:rotate(0deg);transform:rotate(0)}to{-webkit-transform:rotate(360deg);transform:rotate(360deg)}}@keyframes load8{0%{-webkit-transform:rotate(0deg);transform:rotate(0)}to{-webkit-transform:rotate(360deg);transform:rotate(360deg)}}.ng-select .ng-arrow-wrapper{cursor:pointer;position:relative;text-align:center;-webkit-user-select:none;user-select:none}.ng-select .ng-arrow-wrapper .ng-arrow{pointer-events:none;display:inline-block;height:0;width:0;position:relative}.ng-dropdown-panel{box-sizing:border-box;position:absolute;opacity:0;width:100%;z-index:1050;-webkit-overflow-scrolling:touch}.ng-dropdown-panel .ng-dropdown-panel-items{display:block;height:auto;-webkit-box-sizing:border-box;-moz-box-sizing:border-box;box-sizing:border-box;max-height:240px;overflow-y:auto}.ng-dropdown-panel .ng-dropdown-panel-items .ng-optgroup{white-space:nowrap;overflow:hidden;text-overflow:ellipsis}.ng-dropdown-panel .ng-dropdown-panel-items .ng-option{box-sizing:border-box;cursor:pointer;display:block;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}.ng-dropdown-panel .ng-dropdown-panel-items .ng-option .ng-option-label:empty:before{content:"\\200b"}.ng-dropdown-panel .ng-dropdown-panel-items .ng-option .highlighted{font-weight:700;text-decoration:underline}.ng-dropdown-panel .ng-dropdown-panel-items .ng-option.disabled{cursor:default}.ng-dropdown-panel .scroll-host{overflow:hidden;overflow-y:auto;position:relative;display:block;-webkit-overflow-scrolling:touch}.ng-dropdown-panel .scrollable-content{top:0;left:0;width:100%;height:100%;position:absolute}.ng-dropdown-panel .total-padding{width:1px;opacity:0}.ng-visually-hidden{position:absolute!important;width:1px;height:1px;margin:-1px;padding:0;overflow:hidden;clip:rect(0 0 0 0);border:0;white-space:nowrap}\n']
    }]
  }], () => [{
    type: void 0,
    decorators: [{
      type: Attribute,
      args: ["class"]
    }]
  }, {
    type: void 0,
    decorators: [{
      type: Attribute,
      args: ["autofocus"]
    }]
  }, {
    type: NgSelectConfig
  }, {
    type: void 0,
    decorators: [{
      type: Inject,
      args: [SELECTION_MODEL_FACTORY]
    }, {
      type: Optional
    }]
  }, {
    type: ElementRef
  }, {
    type: ChangeDetectorRef
  }, {
    type: ConsoleService
  }], {
    ariaLabelDropdown: [{
      type: Input
    }],
    bindLabel: [{
      type: Input
    }],
    bindValue: [{
      type: Input
    }],
    ariaLabel: [{
      type: Input
    }],
    markFirst: [{
      type: Input,
      args: [{
        transform: booleanAttribute
      }]
    }],
    placeholder: [{
      type: Input
    }],
    fixedPlaceholder: [{
      type: Input
    }],
    notFoundText: [{
      type: Input
    }],
    typeToSearchText: [{
      type: Input
    }],
    preventToggleOnRightClick: [{
      type: Input
    }],
    addTagText: [{
      type: Input
    }],
    loadingText: [{
      type: Input
    }],
    clearAllText: [{
      type: Input
    }],
    appearance: [{
      type: Input
    }],
    dropdownPosition: [{
      type: Input
    }],
    appendTo: [{
      type: Input
    }],
    loading: [{
      type: Input,
      args: [{
        transform: booleanAttribute
      }]
    }],
    closeOnSelect: [{
      type: Input,
      args: [{
        transform: booleanAttribute
      }]
    }],
    hideSelected: [{
      type: Input,
      args: [{
        transform: booleanAttribute
      }]
    }],
    selectOnTab: [{
      type: Input,
      args: [{
        transform: booleanAttribute
      }]
    }],
    openOnEnter: [{
      type: Input,
      args: [{
        transform: booleanAttribute
      }]
    }],
    maxSelectedItems: [{
      type: Input,
      args: [{
        transform: numberAttribute
      }]
    }],
    groupBy: [{
      type: Input
    }],
    groupValue: [{
      type: Input
    }],
    bufferAmount: [{
      type: Input,
      args: [{
        transform: numberAttribute
      }]
    }],
    virtualScroll: [{
      type: Input,
      args: [{
        transform: booleanAttribute
      }]
    }],
    selectableGroup: [{
      type: Input,
      args: [{
        transform: booleanAttribute
      }]
    }],
    selectableGroupAsModel: [{
      type: Input,
      args: [{
        transform: booleanAttribute
      }]
    }],
    searchFn: [{
      type: Input
    }],
    trackByFn: [{
      type: Input
    }],
    clearOnBackspace: [{
      type: Input,
      args: [{
        transform: booleanAttribute
      }]
    }],
    labelForId: [{
      type: Input
    }],
    inputAttrs: [{
      type: Input
    }],
    tabIndex: [{
      type: Input,
      args: [{
        transform: numberAttribute
      }]
    }],
    readonly: [{
      type: Input,
      args: [{
        transform: booleanAttribute
      }]
    }],
    searchWhileComposing: [{
      type: Input,
      args: [{
        transform: booleanAttribute
      }]
    }],
    minTermLength: [{
      type: Input,
      args: [{
        transform: numberAttribute
      }]
    }],
    editableSearchTerm: [{
      type: Input,
      args: [{
        transform: booleanAttribute
      }]
    }],
    ngClass: [{
      type: Input
    }],
    typeahead: [{
      type: Input
    }, {
      type: HostBinding,
      args: ["class.ng-select-typeahead"]
    }],
    multiple: [{
      type: Input,
      args: [{
        transform: booleanAttribute
      }]
    }, {
      type: HostBinding,
      args: ["class.ng-select-multiple"]
    }],
    addTag: [{
      type: Input
    }, {
      type: HostBinding,
      args: ["class.ng-select-taggable"]
    }],
    searchable: [{
      type: Input,
      args: [{
        transform: booleanAttribute
      }]
    }, {
      type: HostBinding,
      args: ["class.ng-select-searchable"]
    }],
    clearable: [{
      type: Input,
      args: [{
        transform: booleanAttribute
      }]
    }, {
      type: HostBinding,
      args: ["class.ng-select-clearable"]
    }],
    isOpen: [{
      type: Input
    }, {
      type: HostBinding,
      args: ["class.ng-select-opened"]
    }],
    blurEvent: [{
      type: Output,
      args: ["blur"]
    }],
    focusEvent: [{
      type: Output,
      args: ["focus"]
    }],
    changeEvent: [{
      type: Output,
      args: ["change"]
    }],
    openEvent: [{
      type: Output,
      args: ["open"]
    }],
    closeEvent: [{
      type: Output,
      args: ["close"]
    }],
    searchEvent: [{
      type: Output,
      args: ["search"]
    }],
    clearEvent: [{
      type: Output,
      args: ["clear"]
    }],
    addEvent: [{
      type: Output,
      args: ["add"]
    }],
    removeEvent: [{
      type: Output,
      args: ["remove"]
    }],
    scroll: [{
      type: Output,
      args: ["scroll"]
    }],
    scrollToEnd: [{
      type: Output,
      args: ["scrollToEnd"]
    }],
    optionTemplate: [{
      type: ContentChild,
      args: [NgOptionTemplateDirective, {
        read: TemplateRef
      }]
    }],
    optgroupTemplate: [{
      type: ContentChild,
      args: [NgOptgroupTemplateDirective, {
        read: TemplateRef
      }]
    }],
    labelTemplate: [{
      type: ContentChild,
      args: [NgLabelTemplateDirective, {
        read: TemplateRef
      }]
    }],
    multiLabelTemplate: [{
      type: ContentChild,
      args: [NgMultiLabelTemplateDirective, {
        read: TemplateRef
      }]
    }],
    headerTemplate: [{
      type: ContentChild,
      args: [NgHeaderTemplateDirective, {
        read: TemplateRef
      }]
    }],
    footerTemplate: [{
      type: ContentChild,
      args: [NgFooterTemplateDirective, {
        read: TemplateRef
      }]
    }],
    notFoundTemplate: [{
      type: ContentChild,
      args: [NgNotFoundTemplateDirective, {
        read: TemplateRef
      }]
    }],
    placeholderTemplate: [{
      type: ContentChild,
      args: [NgPlaceholderTemplateDirective, {
        read: TemplateRef
      }]
    }],
    typeToSearchTemplate: [{
      type: ContentChild,
      args: [NgTypeToSearchTemplateDirective, {
        read: TemplateRef
      }]
    }],
    loadingTextTemplate: [{
      type: ContentChild,
      args: [NgLoadingTextTemplateDirective, {
        read: TemplateRef
      }]
    }],
    tagTemplate: [{
      type: ContentChild,
      args: [NgTagTemplateDirective, {
        read: TemplateRef
      }]
    }],
    loadingSpinnerTemplate: [{
      type: ContentChild,
      args: [NgLoadingSpinnerTemplateDirective, {
        read: TemplateRef
      }]
    }],
    clearButtonTemplate: [{
      type: ContentChild,
      args: [NgClearButtonTemplateDirective, {
        read: TemplateRef
      }]
    }],
    dropdownPanel: [{
      type: ViewChild,
      args: [forwardRef(() => NgDropdownPanelComponent)]
    }],
    searchInput: [{
      type: ViewChild,
      args: ["searchInput", {
        static: true
      }]
    }],
    clearButton: [{
      type: ViewChild,
      args: ["clearButton"]
    }],
    ngOptions: [{
      type: ContentChildren,
      args: [NgOptionComponent, {
        descendants: true
      }]
    }],
    useDefaultClass: [{
      type: HostBinding,
      args: ["class.ng-select"]
    }],
    filtered: [{
      type: HostBinding,
      args: ["class.ng-select-filtered"]
    }],
    single: [{
      type: HostBinding,
      args: ["class.ng-select-single"]
    }],
    items: [{
      type: Input
    }],
    disabled: [{
      type: HostBinding,
      args: ["class.ng-select-disabled"]
    }],
    compareWith: [{
      type: Input
    }],
    clearSearchOnAdd: [{
      type: Input
    }],
    deselectOnClick: [{
      type: Input
    }],
    keyDownFn: [{
      type: Input
    }],
    handleKeyDown: [{
      type: HostListener,
      args: ["keydown", ["$event"]]
    }]
  });
})();
var NgSelectModule = class _NgSelectModule {
  static {
    this.ɵfac = function NgSelectModule_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _NgSelectModule)();
    };
  }
  static {
    this.ɵmod = ɵɵdefineNgModule({
      type: _NgSelectModule,
      imports: [NgDropdownPanelComponent, NgOptionComponent, NgSelectComponent, NgOptgroupTemplateDirective, NgOptionTemplateDirective, NgLabelTemplateDirective, NgMultiLabelTemplateDirective, NgHeaderTemplateDirective, NgFooterTemplateDirective, NgPlaceholderTemplateDirective, NgClearButtonTemplateDirective, NgNotFoundTemplateDirective, NgTypeToSearchTemplateDirective, NgLoadingTextTemplateDirective, NgTagTemplateDirective, NgLoadingSpinnerTemplateDirective, NgItemLabelDirective],
      exports: [NgSelectComponent, NgOptionComponent, NgOptgroupTemplateDirective, NgOptionTemplateDirective, NgLabelTemplateDirective, NgMultiLabelTemplateDirective, NgHeaderTemplateDirective, NgFooterTemplateDirective, NgPlaceholderTemplateDirective, NgNotFoundTemplateDirective, NgTypeToSearchTemplateDirective, NgLoadingTextTemplateDirective, NgTagTemplateDirective, NgLoadingSpinnerTemplateDirective, NgClearButtonTemplateDirective]
    });
  }
  static {
    this.ɵinj = ɵɵdefineInjector({
      providers: [{
        provide: SELECTION_MODEL_FACTORY,
        useValue: DefaultSelectionModelFactory
      }]
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(NgSelectModule, [{
    type: NgModule,
    args: [{
      imports: [NgDropdownPanelComponent, NgOptionComponent, NgSelectComponent, NgOptgroupTemplateDirective, NgOptionTemplateDirective, NgLabelTemplateDirective, NgMultiLabelTemplateDirective, NgHeaderTemplateDirective, NgFooterTemplateDirective, NgPlaceholderTemplateDirective, NgClearButtonTemplateDirective, NgNotFoundTemplateDirective, NgTypeToSearchTemplateDirective, NgLoadingTextTemplateDirective, NgTagTemplateDirective, NgLoadingSpinnerTemplateDirective, NgItemLabelDirective],
      exports: [NgSelectComponent, NgOptionComponent, NgOptgroupTemplateDirective, NgOptionTemplateDirective, NgLabelTemplateDirective, NgMultiLabelTemplateDirective, NgHeaderTemplateDirective, NgFooterTemplateDirective, NgPlaceholderTemplateDirective, NgNotFoundTemplateDirective, NgTypeToSearchTemplateDirective, NgLoadingTextTemplateDirective, NgTagTemplateDirective, NgLoadingSpinnerTemplateDirective, NgClearButtonTemplateDirective],
      providers: [{
        provide: SELECTION_MODEL_FACTORY,
        useValue: DefaultSelectionModelFactory
      }]
    }]
  }], null, null);
})();

// node_modules/ngx-infinite-scroll/fesm2022/ngx-infinite-scroll.mjs
function resolveContainerElement(selector, scrollWindow, defaultElement, fromRoot) {
  const hasWindow = window && !!window.document && window.document.documentElement;
  let container = hasWindow && scrollWindow ? window : defaultElement;
  if (selector) {
    const containerIsString = selector && hasWindow && typeof selector === "string";
    container = containerIsString ? findElement(selector, defaultElement.nativeElement, fromRoot) : selector;
    if (!container) {
      throw new Error("ngx-infinite-scroll {resolveContainerElement()}: selector for");
    }
  }
  return container;
}
function findElement(selector, customRoot, fromRoot) {
  const rootEl = fromRoot ? window.document : customRoot;
  return rootEl.querySelector(selector);
}
function inputPropChanged(prop) {
  return prop && !prop.firstChange;
}
function hasWindowDefined() {
  return typeof window !== "undefined";
}
var VerticalProps = {
  clientHeight: "clientHeight",
  offsetHeight: "offsetHeight",
  scrollHeight: "scrollHeight",
  pageYOffset: "pageYOffset",
  offsetTop: "offsetTop",
  scrollTop: "scrollTop",
  top: "top"
};
var HorizontalProps = {
  clientHeight: "clientWidth",
  offsetHeight: "offsetWidth",
  scrollHeight: "scrollWidth",
  pageYOffset: "pageXOffset",
  offsetTop: "offsetLeft",
  scrollTop: "scrollLeft",
  top: "left"
};
var AxisResolver = class {
  constructor(vertical = true) {
    this.vertical = vertical;
    this.propsMap = vertical ? VerticalProps : HorizontalProps;
  }
  clientHeightKey() {
    return this.propsMap.clientHeight;
  }
  offsetHeightKey() {
    return this.propsMap.offsetHeight;
  }
  scrollHeightKey() {
    return this.propsMap.scrollHeight;
  }
  pageYOffsetKey() {
    return this.propsMap.pageYOffset;
  }
  offsetTopKey() {
    return this.propsMap.offsetTop;
  }
  scrollTopKey() {
    return this.propsMap.scrollTop;
  }
  topKey() {
    return this.propsMap.top;
  }
};
function shouldTriggerEvents(alwaysCallback, shouldFireScrollEvent2, isTriggeredCurrentTotal) {
  if (alwaysCallback && shouldFireScrollEvent2) {
    return true;
  }
  if (!isTriggeredCurrentTotal && shouldFireScrollEvent2) {
    return true;
  }
  return false;
}
function createResolver({
  windowElement,
  axis
}) {
  return createResolverWithContainer({
    axis,
    isWindow: isElementWindow(windowElement)
  }, windowElement);
}
function createResolverWithContainer(resolver, windowElement) {
  const container = resolver.isWindow || windowElement && !windowElement.nativeElement ? windowElement : windowElement.nativeElement;
  return __spreadProps(__spreadValues({}, resolver), {
    container
  });
}
function isElementWindow(windowElement) {
  const isWindow = ["Window", "global"].some((obj) => Object.prototype.toString.call(windowElement).includes(obj));
  return isWindow;
}
function getDocumentElement(isContainerWindow, windowElement) {
  return isContainerWindow ? windowElement.document.documentElement : null;
}
function calculatePoints(element, resolver) {
  const height = extractHeightForElement(resolver);
  return resolver.isWindow ? calculatePointsForWindow(height, element, resolver) : calculatePointsForElement(height, element, resolver);
}
function calculatePointsForWindow(height, element, resolver) {
  const {
    axis,
    container,
    isWindow
  } = resolver;
  const {
    offsetHeightKey,
    clientHeightKey
  } = extractHeightPropKeys(axis);
  const scrolled = height + getElementPageYOffset(getDocumentElement(isWindow, container), axis, isWindow);
  const nativeElementHeight = getElementHeight(element.nativeElement, isWindow, offsetHeightKey, clientHeightKey);
  const totalToScroll = getElementOffsetTop(element.nativeElement, axis, isWindow) + nativeElementHeight;
  return {
    height,
    scrolled,
    totalToScroll,
    isWindow
  };
}
function calculatePointsForElement(height, element, resolver) {
  const {
    axis,
    container
  } = resolver;
  const scrolled = container[axis.scrollTopKey()];
  const totalToScroll = container[axis.scrollHeightKey()];
  return {
    height,
    scrolled,
    totalToScroll,
    isWindow: false
  };
}
function extractHeightPropKeys(axis) {
  return {
    offsetHeightKey: axis.offsetHeightKey(),
    clientHeightKey: axis.clientHeightKey()
  };
}
function extractHeightForElement({
  container,
  isWindow,
  axis
}) {
  const {
    offsetHeightKey,
    clientHeightKey
  } = extractHeightPropKeys(axis);
  return getElementHeight(container, isWindow, offsetHeightKey, clientHeightKey);
}
function getElementHeight(elem, isWindow, offsetHeightKey, clientHeightKey) {
  if (isNaN(elem[offsetHeightKey])) {
    const docElem = getDocumentElement(isWindow, elem);
    return docElem ? docElem[clientHeightKey] : 0;
  } else {
    return elem[offsetHeightKey];
  }
}
function getElementOffsetTop(elem, axis, isWindow) {
  const topKey = axis.topKey();
  if (!elem.getBoundingClientRect) {
    return;
  }
  return elem.getBoundingClientRect()[topKey] + getElementPageYOffset(elem, axis, isWindow);
}
function getElementPageYOffset(elem, axis, isWindow) {
  const pageYOffset = axis.pageYOffsetKey();
  const scrollTop = axis.scrollTopKey();
  const offsetTop = axis.offsetTopKey();
  if (isNaN(window.pageYOffset)) {
    return getDocumentElement(isWindow, elem)[scrollTop];
  } else if (elem.ownerDocument) {
    return elem.ownerDocument.defaultView[pageYOffset];
  } else {
    return elem[offsetTop];
  }
}
function shouldFireScrollEvent(container, distance = {
  down: 0,
  up: 0
}, scrollingDown) {
  let remaining;
  let containerBreakpoint;
  if (container.totalToScroll <= 0) {
    return false;
  }
  const scrolledUntilNow = container.isWindow ? container.scrolled : container.height + container.scrolled;
  if (scrollingDown) {
    remaining = (container.totalToScroll - scrolledUntilNow) / container.totalToScroll;
    const distanceDown = distance?.down ? distance.down : 0;
    containerBreakpoint = distanceDown / 10;
  } else {
    const totalHiddenContentHeight = container.scrolled + (container.totalToScroll - scrolledUntilNow);
    remaining = container.scrolled / totalHiddenContentHeight;
    const distanceUp = distance?.up ? distance.up : 0;
    containerBreakpoint = distanceUp / 10;
  }
  const shouldFireEvent = remaining <= containerBreakpoint;
  return shouldFireEvent;
}
function isScrollingDownwards(lastScrollPosition, container) {
  return lastScrollPosition < container.scrolled;
}
function getScrollStats(lastScrollPosition, container, distance) {
  const scrollDown = isScrollingDownwards(lastScrollPosition, container);
  return {
    fire: shouldFireScrollEvent(container, distance, scrollDown),
    scrollDown
  };
}
var ScrollState = class {
  constructor(attrs) {
    this.lastScrollPosition = 0;
    this.lastTotalToScroll = 0;
    this.totalToScroll = 0;
    this.triggered = {
      down: 0,
      up: 0
    };
    Object.assign(this, attrs);
  }
  updateScrollPosition(position) {
    return this.lastScrollPosition = position;
  }
  updateTotalToScroll(totalToScroll) {
    if (this.lastTotalToScroll !== totalToScroll) {
      this.lastTotalToScroll = this.totalToScroll;
      this.totalToScroll = totalToScroll;
    }
  }
  updateScroll(scrolledUntilNow, totalToScroll) {
    this.updateScrollPosition(scrolledUntilNow);
    this.updateTotalToScroll(totalToScroll);
  }
  updateTriggeredFlag(scroll, isScrollingDown) {
    if (isScrollingDown) {
      this.triggered.down = scroll;
    } else {
      this.triggered.up = scroll;
    }
  }
  isTriggeredScroll(totalToScroll, isScrollingDown) {
    return isScrollingDown ? this.triggered.down === totalToScroll : this.triggered.up === totalToScroll;
  }
};
function createScroller(config) {
  const {
    scrollContainer,
    scrollWindow,
    element,
    fromRoot
  } = config;
  const resolver = createResolver({
    axis: new AxisResolver(!config.horizontal),
    windowElement: resolveContainerElement(scrollContainer, scrollWindow, element, fromRoot)
  });
  const scrollState = new ScrollState({
    totalToScroll: calculatePoints(element, resolver).totalToScroll
  });
  const options = {
    container: resolver.container,
    throttle: config.throttle
  };
  const distance = {
    up: config.upDistance,
    down: config.downDistance
  };
  return attachScrollEvent(options).pipe(mergeMap(() => of(calculatePoints(element, resolver))), map((positionStats) => toInfiniteScrollParams(scrollState.lastScrollPosition, positionStats, distance)), tap(({
    stats
  }) => scrollState.updateScroll(stats.scrolled, stats.totalToScroll)), filter(({
    fire,
    scrollDown,
    stats: {
      totalToScroll
    }
  }) => shouldTriggerEvents(config.alwaysCallback, fire, scrollState.isTriggeredScroll(totalToScroll, scrollDown))), tap(({
    scrollDown,
    stats: {
      totalToScroll
    }
  }) => {
    scrollState.updateTriggeredFlag(totalToScroll, scrollDown);
  }), map(toInfiniteScrollAction));
}
function attachScrollEvent(options) {
  let obs = fromEvent(options.container, "scroll");
  if (options.throttle) {
    obs = obs.pipe(throttleTime(options.throttle, void 0, {
      leading: true,
      trailing: true
    }));
  }
  return obs;
}
function toInfiniteScrollParams(lastScrollPosition, stats, distance) {
  const {
    scrollDown,
    fire
  } = getScrollStats(lastScrollPosition, stats, distance);
  return {
    scrollDown,
    fire,
    stats
  };
}
var InfiniteScrollActions = {
  DOWN: "[NGX_ISE] DOWN",
  UP: "[NGX_ISE] UP"
};
function toInfiniteScrollAction(response) {
  const {
    scrollDown,
    stats: {
      scrolled: currentScrollPosition
    }
  } = response;
  return {
    type: scrollDown ? InfiniteScrollActions.DOWN : InfiniteScrollActions.UP,
    payload: {
      currentScrollPosition
    }
  };
}
var InfiniteScrollDirective = class _InfiniteScrollDirective {
  constructor(element, zone) {
    this.element = element;
    this.zone = zone;
    this.scrolled = new EventEmitter();
    this.scrolledUp = new EventEmitter();
    this.infiniteScrollDistance = 2;
    this.infiniteScrollUpDistance = 1.5;
    this.infiniteScrollThrottle = 150;
    this.infiniteScrollDisabled = false;
    this.infiniteScrollContainer = null;
    this.scrollWindow = true;
    this.immediateCheck = false;
    this.horizontal = false;
    this.alwaysCallback = false;
    this.fromRoot = false;
  }
  ngAfterViewInit() {
    if (!this.infiniteScrollDisabled) {
      this.setup();
    }
  }
  ngOnChanges({
    infiniteScrollContainer,
    infiniteScrollDisabled,
    infiniteScrollDistance
  }) {
    const containerChanged = inputPropChanged(infiniteScrollContainer);
    const disabledChanged = inputPropChanged(infiniteScrollDisabled);
    const distanceChanged = inputPropChanged(infiniteScrollDistance);
    const shouldSetup = !disabledChanged && !this.infiniteScrollDisabled || disabledChanged && !infiniteScrollDisabled.currentValue || distanceChanged;
    if (containerChanged || disabledChanged || distanceChanged) {
      this.destroyScroller();
      if (shouldSetup) {
        this.setup();
      }
    }
  }
  ngOnDestroy() {
    this.destroyScroller();
  }
  setup() {
    if (!hasWindowDefined()) {
      return;
    }
    this.zone.runOutsideAngular(() => {
      this.disposeScroller = createScroller({
        fromRoot: this.fromRoot,
        alwaysCallback: this.alwaysCallback,
        disable: this.infiniteScrollDisabled,
        downDistance: this.infiniteScrollDistance,
        element: this.element,
        horizontal: this.horizontal,
        scrollContainer: this.infiniteScrollContainer,
        scrollWindow: this.scrollWindow,
        throttle: this.infiniteScrollThrottle,
        upDistance: this.infiniteScrollUpDistance
      }).subscribe((payload) => this.handleOnScroll(payload));
    });
  }
  handleOnScroll({
    type,
    payload
  }) {
    const emitter = type === InfiniteScrollActions.DOWN ? this.scrolled : this.scrolledUp;
    if (hasObservers(emitter)) {
      this.zone.run(() => emitter.emit(payload));
    }
  }
  destroyScroller() {
    if (this.disposeScroller) {
      this.disposeScroller.unsubscribe();
    }
  }
  static {
    this.ɵfac = function InfiniteScrollDirective_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _InfiniteScrollDirective)(ɵɵdirectiveInject(ElementRef), ɵɵdirectiveInject(NgZone));
    };
  }
  static {
    this.ɵdir = ɵɵdefineDirective({
      type: _InfiniteScrollDirective,
      selectors: [["", "infiniteScroll", ""], ["", "infinite-scroll", ""], ["", "data-infinite-scroll", ""]],
      inputs: {
        infiniteScrollDistance: "infiniteScrollDistance",
        infiniteScrollUpDistance: "infiniteScrollUpDistance",
        infiniteScrollThrottle: "infiniteScrollThrottle",
        infiniteScrollDisabled: "infiniteScrollDisabled",
        infiniteScrollContainer: "infiniteScrollContainer",
        scrollWindow: "scrollWindow",
        immediateCheck: "immediateCheck",
        horizontal: "horizontal",
        alwaysCallback: "alwaysCallback",
        fromRoot: "fromRoot"
      },
      outputs: {
        scrolled: "scrolled",
        scrolledUp: "scrolledUp"
      },
      features: [ɵɵNgOnChangesFeature]
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(InfiniteScrollDirective, [{
    type: Directive,
    args: [{
      selector: "[infiniteScroll], [infinite-scroll], [data-infinite-scroll]"
    }]
  }], () => [{
    type: ElementRef
  }, {
    type: NgZone
  }], {
    scrolled: [{
      type: Output
    }],
    scrolledUp: [{
      type: Output
    }],
    infiniteScrollDistance: [{
      type: Input
    }],
    infiniteScrollUpDistance: [{
      type: Input
    }],
    infiniteScrollThrottle: [{
      type: Input
    }],
    infiniteScrollDisabled: [{
      type: Input
    }],
    infiniteScrollContainer: [{
      type: Input
    }],
    scrollWindow: [{
      type: Input
    }],
    immediateCheck: [{
      type: Input
    }],
    horizontal: [{
      type: Input
    }],
    alwaysCallback: [{
      type: Input
    }],
    fromRoot: [{
      type: Input
    }]
  });
})();
function hasObservers(emitter) {
  return emitter.observed ?? emitter.observers.length > 0;
}
var InfiniteScrollModule = class _InfiniteScrollModule {
  static {
    this.ɵfac = function InfiniteScrollModule_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _InfiniteScrollModule)();
    };
  }
  static {
    this.ɵmod = ɵɵdefineNgModule({
      type: _InfiniteScrollModule,
      imports: [InfiniteScrollDirective],
      exports: [InfiniteScrollDirective]
    });
  }
  static {
    this.ɵinj = ɵɵdefineInjector({});
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(InfiniteScrollModule, [{
    type: NgModule,
    args: [{
      exports: [InfiniteScrollDirective],
      imports: [InfiniteScrollDirective]
    }]
  }], null, null);
})();

// node_modules/@spartacus/storefront/fesm2022/spartacus-storefront.mjs
var _c02 = (a0) => ({
  "anonymous-consent-banner-hidden": a0
});
function AnonymousConsentManagementBannerComponent_ng_container_0_div_3_Template(rf, ctx) {
  if (rf & 1) {
    const _r1 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "div", 4)(1, "div", 5)(2, "div", 6);
    ɵɵtext(3);
    ɵɵpipe(4, "cxTranslate");
    ɵɵelementEnd();
    ɵɵelementStart(5, "div", 7);
    ɵɵtext(6);
    ɵɵpipe(7, "cxTranslate");
    ɵɵelementEnd()();
    ɵɵelementStart(8, "div", 8)(9, "button", 9);
    ɵɵlistener("click", function AnonymousConsentManagementBannerComponent_ng_container_0_div_3_Template_button_click_9_listener() {
      ɵɵrestoreView(_r1);
      const ctx_r1 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r1.viewDetails());
    });
    ɵɵtext(10);
    ɵɵpipe(11, "cxTranslate");
    ɵɵelementEnd();
    ɵɵelementStart(12, "button", 10);
    ɵɵlistener("click", function AnonymousConsentManagementBannerComponent_ng_container_0_div_3_Template_button_click_12_listener() {
      ɵɵrestoreView(_r1);
      const ctx_r1 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r1.allowAll());
    });
    ɵɵtext(13);
    ɵɵpipe(14, "cxTranslate");
    ɵɵelementEnd()()();
  }
  if (rf & 2) {
    ɵɵadvance(3);
    ɵɵtextInterpolate1(" ", ɵɵpipeBind1(4, 4, "anonymousConsents.banner.title"), " ");
    ɵɵadvance(3);
    ɵɵtextInterpolate1(" ", ɵɵpipeBind1(7, 6, "anonymousConsents.banner.description"), " ");
    ɵɵadvance(4);
    ɵɵtextInterpolate1(" ", ɵɵpipeBind1(11, 8, "anonymousConsents.banner.viewDetails"), " ");
    ɵɵadvance(3);
    ɵɵtextInterpolate1(" ", ɵɵpipeBind1(14, 10, "anonymousConsents.banner.allowAll"), " ");
  }
}
function AnonymousConsentManagementBannerComponent_ng_container_0_div_4_Template(rf, ctx) {
  if (rf & 1) {
    const _r3 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "div", 4)(1, "div", 11)(2, "div", 6);
    ɵɵtext(3);
    ɵɵpipe(4, "cxTranslate");
    ɵɵelementEnd();
    ɵɵelementStart(5, "div", 7);
    ɵɵtext(6);
    ɵɵpipe(7, "cxTranslate");
    ɵɵelementEnd()();
    ɵɵelementStart(8, "div", 12)(9, "button", 9);
    ɵɵlistener("click", function AnonymousConsentManagementBannerComponent_ng_container_0_div_4_Template_button_click_9_listener() {
      ɵɵrestoreView(_r3);
      const ctx_r1 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r1.viewDetails());
    });
    ɵɵtext(10);
    ɵɵpipe(11, "cxTranslate");
    ɵɵelementEnd();
    ɵɵelementStart(12, "button", 10);
    ɵɵlistener("click", function AnonymousConsentManagementBannerComponent_ng_container_0_div_4_Template_button_click_12_listener() {
      ɵɵrestoreView(_r3);
      const ctx_r1 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r1.allowAll());
    });
    ɵɵtext(13);
    ɵɵpipe(14, "cxTranslate");
    ɵɵelementEnd()()();
  }
  if (rf & 2) {
    ɵɵadvance(3);
    ɵɵtextInterpolate1(" ", ɵɵpipeBind1(4, 4, "anonymousConsents.banner.title"), " ");
    ɵɵadvance(3);
    ɵɵtextInterpolate1(" ", ɵɵpipeBind1(7, 6, "anonymousConsents.banner.description"), " ");
    ɵɵadvance(4);
    ɵɵtextInterpolate1(" ", ɵɵpipeBind1(11, 8, "anonymousConsents.banner.consentManagement"), " ");
    ɵɵadvance(3);
    ɵɵtextInterpolate1(" ", ɵɵpipeBind1(14, 10, "anonymousConsents.banner.allowAll"), " ");
  }
}
function AnonymousConsentManagementBannerComponent_ng_container_0_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainerStart(0);
    ɵɵelementStart(1, "div", 1)(2, "div", 2);
    ɵɵtemplate(3, AnonymousConsentManagementBannerComponent_ng_container_0_div_3_Template, 15, 12, "div", 3)(4, AnonymousConsentManagementBannerComponent_ng_container_0_div_4_Template, 15, 12, "div", 3);
    ɵɵelementEnd()();
    ɵɵelementContainerEnd();
  }
  if (rf & 2) {
    const bannerVisible_r4 = ctx.ngIf;
    ɵɵadvance();
    ɵɵproperty("ngClass", ɵɵpureFunction1(3, _c02, !bannerVisible_r4));
    ɵɵadvance(2);
    ɵɵproperty("cxFeature", "!a11yHideConsentButtonWhenBannerVisible");
    ɵɵadvance();
    ɵɵproperty("cxFeature", "a11yHideConsentButtonWhenBannerVisible");
  }
}
var _c12 = ["*"];
function IconComponent_ng_container_0_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainerStart(0);
    ɵɵelement(1, "i", 1);
    ɵɵelementContainerEnd();
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    ɵɵadvance();
    ɵɵproperty("outerHTML", ctx_r0.icon, ɵɵsanitizeHtml);
  }
}
function ConsentManagementFormComponent_ng_container_4_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainerStart(0);
    ɵɵtext(1);
    ɵɵelement(2, "br");
    ɵɵelementContainerEnd();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext();
    ɵɵadvance();
    ɵɵtextInterpolate1(" ", ctx_r1.consentTemplate == null ? null : ctx_r1.consentTemplate.name, " ");
  }
}
function ConsentManagementFormComponent_ng_template_7_Template(rf, ctx) {
}
function ConsentManagementFormComponent_ng_template_10_Template(rf, ctx) {
}
function ConsentManagementFormComponent_ng_template_11_ng_container_0_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainerStart(0);
    ɵɵelementStart(1, "abbr", 7);
    ɵɵpipe(2, "cxTranslate");
    ɵɵtext(3, "*");
    ɵɵelementEnd();
    ɵɵelementContainerEnd();
  }
  if (rf & 2) {
    ɵɵadvance();
    ɵɵpropertyInterpolate("title", ɵɵpipeBind1(2, 1, "common.required"));
  }
}
function ConsentManagementFormComponent_ng_template_11_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵtemplate(0, ConsentManagementFormComponent_ng_template_11_ng_container_0_Template, 4, 3, "ng-container", 4);
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext();
    ɵɵproperty("ngIf", ctx_r1.showMandatory);
  }
}
var _c22 = ["messageContainer"];
function MessageComponent_button_8_Template(rf, ctx) {
  if (rf & 1) {
    const _r1 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "button", 11);
    ɵɵlistener("click", function MessageComponent_button_8_Template_button_click_0_listener() {
      ɵɵrestoreView(_r1);
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.showBody = !ctx_r1.showBody);
    });
    ɵɵtext(1);
    ɵɵelement(2, "cx-icon", 12);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext();
    ɵɵattribute("aria-expanded", ctx_r1.showBody)("aria-label", ctx_r1.accordionText);
    ɵɵadvance();
    ɵɵtextInterpolate1(" ", ctx_r1.accordionText, " ");
    ɵɵadvance();
    ɵɵproperty("type", ctx_r1.showBody ? "CARET_UP" : "CARET_DOWN");
  }
}
function MessageComponent_button_9_Template(rf, ctx) {
  if (rf & 1) {
    const _r3 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "button", 13);
    ɵɵlistener("click", function MessageComponent_button_9_Template_button_click_0_listener() {
      ɵɵrestoreView(_r3);
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.buttonAction.emit());
    });
    ɵɵtext(1);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext();
    ɵɵproperty("cxAtMessage", ctx_r1.actionButtonMessage);
    ɵɵadvance();
    ɵɵtextInterpolate1(" ", ctx_r1.actionButtonText, " ");
  }
}
function MessageComponent_button_10_Template(rf, ctx) {
  if (rf & 1) {
    const _r4 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "button", 14);
    ɵɵpipe(1, "cxTranslate");
    ɵɵpipe(2, "cxTranslate");
    ɵɵpipe(3, "cxTranslate");
    ɵɵlistener("click", function MessageComponent_button_10_Template_button_click_0_listener() {
      ɵɵrestoreView(_r4);
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.closeMessage.emit());
    });
    ɵɵelement(4, "cx-icon", 15);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext();
    ɵɵpropertyInterpolate("title", ɵɵpipeBind1(1, 4, "common.close"));
    ɵɵproperty("cxAtMessage", ɵɵpipeBind1(2, 6, "common.close"));
    ɵɵattribute("aria-label", ɵɵpipeBind1(3, 8, "common.close"));
    ɵɵadvance(4);
    ɵɵproperty("type", ctx_r1.iconTypes.CLOSE);
  }
}
function MessageComponent_div_11_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div", 16);
    ɵɵprojection(1);
    ɵɵelementEnd();
  }
}
var _c32 = () => ({
  autofocus: ".cx-message"
});
function AnonymousConsentDialogComponent_div_2_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div");
    ɵɵelement(1, "cx-spinner");
    ɵɵelementEnd();
  }
}
function AnonymousConsentDialogComponent_ng_template_4_div_9_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div", 14);
    ɵɵtext(1);
    ɵɵpipe(2, "cxTranslate");
    ɵɵelement(3, "div", 15);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    ɵɵadvance();
    ɵɵtextInterpolate1(" ", ɵɵpipeBind1(2, 1, "anonymousConsents.dialog.legalDescription"), " ");
  }
}
function AnonymousConsentDialogComponent_ng_template_4_ng_container_10_cx_message_2_Template(rf, ctx) {
  if (rf & 1) {
    const _r4 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "cx-message", 18);
    ɵɵpipe(1, "cxTranslate");
    ɵɵlistener("closeMessage", function AnonymousConsentDialogComponent_ng_template_4_ng_container_10_cx_message_2_Template_cx_message_closeMessage_0_listener() {
      ɵɵrestoreView(_r4);
      const ctx_r2 = ɵɵnextContext(3);
      return ɵɵresetView(ctx_r2.closeMessage());
    });
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const message_r5 = ctx.ngIf;
    ɵɵproperty("text", ɵɵpipeBind1(1, 4, message_r5.key))("type", message_r5.type)("isVisibleCloseButton", true)("cxFocus", ɵɵpureFunction0(6, _c32));
  }
}
function AnonymousConsentDialogComponent_ng_template_4_ng_container_10_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainerStart(0);
    ɵɵelementStart(1, "div", 16);
    ɵɵtemplate(2, AnonymousConsentDialogComponent_ng_template_4_ng_container_10_cx_message_2_Template, 2, 7, "cx-message", 17);
    ɵɵpipe(3, "async");
    ɵɵelementEnd();
    ɵɵelementContainerEnd();
  }
  if (rf & 2) {
    const ctx_r2 = ɵɵnextContext(2);
    ɵɵadvance(2);
    ɵɵproperty("ngIf", ɵɵpipeBind1(3, 1, ctx_r2.message$));
  }
}
function AnonymousConsentDialogComponent_ng_template_4_div_18_ng_container_1_div_1_Template(rf, ctx) {
  if (rf & 1) {
    const _r6 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "div", 22)(1, "cx-consent-management-form", 23);
    ɵɵlistener("consentChanged", function AnonymousConsentDialogComponent_ng_template_4_div_18_ng_container_1_div_1_Template_cx_consent_management_form_consentChanged_1_listener($event) {
      ɵɵrestoreView(_r6);
      const ctx_r2 = ɵɵnextContext(4);
      return ɵɵresetView(ctx_r2.onConsentChange($event));
    });
    ɵɵelementEnd()();
  }
  if (rf & 2) {
    const template_r7 = ctx.$implicit;
    const consents_r8 = ɵɵnextContext().ngIf;
    const ctx_r2 = ɵɵnextContext(3);
    ɵɵadvance();
    ɵɵproperty("consentTemplate", template_r7)("requiredConsents", ctx_r2.requiredConsents)("consent", ctx_r2.getCorrespondingConsent(template_r7, consents_r8));
  }
}
function AnonymousConsentDialogComponent_ng_template_4_div_18_ng_container_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainerStart(0);
    ɵɵtemplate(1, AnonymousConsentDialogComponent_ng_template_4_div_18_ng_container_1_div_1_Template, 2, 3, "div", 21);
    ɵɵelementContainerEnd();
  }
  if (rf & 2) {
    const templates_r9 = ɵɵnextContext().ngIf;
    ɵɵadvance();
    ɵɵproperty("ngForOf", templates_r9);
  }
}
function AnonymousConsentDialogComponent_ng_template_4_div_18_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div", 19);
    ɵɵtemplate(1, AnonymousConsentDialogComponent_ng_template_4_div_18_ng_container_1_Template, 2, 1, "ng-container", 20);
    ɵɵpipe(2, "async");
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r2 = ɵɵnextContext(2);
    ɵɵadvance();
    ɵɵproperty("ngIf", ɵɵpipeBind1(2, 1, ctx_r2.consents$));
  }
}
function AnonymousConsentDialogComponent_ng_template_4_Template(rf, ctx) {
  if (rf & 1) {
    const _r2 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "div", 4)(1, "h3", 5);
    ɵɵtext(2);
    ɵɵpipe(3, "cxTranslate");
    ɵɵelementEnd();
    ɵɵelementStart(4, "button", 6);
    ɵɵpipe(5, "cxTranslate");
    ɵɵpipe(6, "cxTranslate");
    ɵɵlistener("click", function AnonymousConsentDialogComponent_ng_template_4_Template_button_click_4_listener() {
      ɵɵrestoreView(_r2);
      const ctx_r2 = ɵɵnextContext();
      return ɵɵresetView(ctx_r2.close("Cross click"));
    });
    ɵɵelementStart(7, "span", 7);
    ɵɵelement(8, "cx-icon", 8);
    ɵɵelementEnd()()();
    ɵɵtemplate(9, AnonymousConsentDialogComponent_ng_template_4_div_9_Template, 4, 3, "div", 9)(10, AnonymousConsentDialogComponent_ng_template_4_ng_container_10_Template, 4, 3, "ng-container", 10);
    ɵɵelementStart(11, "div", 11)(12, "button", 12);
    ɵɵlistener("click", function AnonymousConsentDialogComponent_ng_template_4_Template_button_click_12_listener() {
      ɵɵrestoreView(_r2);
      const ctx_r2 = ɵɵnextContext();
      return ɵɵresetView(ctx_r2.rejectAll());
    });
    ɵɵtext(13);
    ɵɵpipe(14, "cxTranslate");
    ɵɵelementEnd();
    ɵɵelementStart(15, "button", 12);
    ɵɵlistener("click", function AnonymousConsentDialogComponent_ng_template_4_Template_button_click_15_listener() {
      ɵɵrestoreView(_r2);
      const ctx_r2 = ɵɵnextContext();
      return ɵɵresetView(ctx_r2.allowAll());
    });
    ɵɵtext(16);
    ɵɵpipe(17, "cxTranslate");
    ɵɵelementEnd()();
    ɵɵtemplate(18, AnonymousConsentDialogComponent_ng_template_4_div_18_Template, 3, 3, "div", 13);
    ɵɵpipe(19, "async");
  }
  if (rf & 2) {
    const ctx_r2 = ɵɵnextContext();
    ɵɵadvance(2);
    ɵɵtextInterpolate1(" ", ɵɵpipeBind1(3, 9, "anonymousConsents.dialog.title"), " ");
    ɵɵadvance(2);
    ɵɵpropertyInterpolate("title", ɵɵpipeBind1(5, 11, "common.close"));
    ɵɵattribute("aria-label", ɵɵpipeBind1(6, 13, "common.close"));
    ɵɵadvance(4);
    ɵɵproperty("type", ctx_r2.iconTypes.CLOSE);
    ɵɵadvance();
    ɵɵproperty("ngIf", ctx_r2.showLegalDescription);
    ɵɵadvance();
    ɵɵproperty("cxFeature", "a11yAnonymousConsentMessageInDialog");
    ɵɵadvance(3);
    ɵɵtextInterpolate1(" ", ɵɵpipeBind1(14, 15, "anonymousConsents.dialog.clearAll"), " ");
    ɵɵadvance(3);
    ɵɵtextInterpolate1(" ", ɵɵpipeBind1(17, 17, "anonymousConsents.dialog.selectAll"), " ");
    ɵɵadvance(2);
    ɵɵproperty("ngIf", ɵɵpipeBind1(19, 19, ctx_r2.templates$));
  }
}
var _c42 = ["open"];
function AnonymousConsentOpenDialogComponent_ng_container_0_Template(rf, ctx) {
  if (rf & 1) {
    const _r1 = ɵɵgetCurrentView();
    ɵɵelementContainerStart(0);
    ɵɵelementStart(1, "button", 2, 0);
    ɵɵlistener("click", function AnonymousConsentOpenDialogComponent_ng_container_0_Template_button_click_1_listener() {
      ɵɵrestoreView(_r1);
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.openDialog());
    });
    ɵɵtext(3);
    ɵɵpipe(4, "cxTranslate");
    ɵɵelementEnd();
    ɵɵelementContainerEnd();
  }
  if (rf & 2) {
    ɵɵadvance(3);
    ɵɵtextInterpolate1(" ", ɵɵpipeBind1(4, 1, "anonymousConsents.dialog.title"), " ");
  }
}
function AnonymousConsentOpenDialogComponent_ng_container_1_button_1_Template(rf, ctx) {
  if (rf & 1) {
    const _r3 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "button", 2, 0);
    ɵɵlistener("click", function AnonymousConsentOpenDialogComponent_ng_container_1_button_1_Template_button_click_0_listener() {
      ɵɵrestoreView(_r3);
      const ctx_r1 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r1.openDialog());
    });
    ɵɵtext(2);
    ɵɵpipe(3, "cxTranslate");
    ɵɵelementEnd();
  }
  if (rf & 2) {
    ɵɵadvance(2);
    ɵɵtextInterpolate1(" ", ɵɵpipeBind1(3, 1, "anonymousConsents.dialog.title"), " ");
  }
}
function AnonymousConsentOpenDialogComponent_ng_container_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainerStart(0);
    ɵɵtemplate(1, AnonymousConsentOpenDialogComponent_ng_container_1_button_1_Template, 4, 3, "button", 3);
    ɵɵpipe(2, "async");
    ɵɵelementContainerEnd();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext();
    ɵɵadvance();
    ɵɵproperty("ngIf", !ɵɵpipeBind1(2, 1, ctx_r1.bannerVisible$));
  }
}
var _c52 = (a0) => ({
  components$: a0
});
var _c62 = (a0) => ({
  component: a0
});
function PageSlotComponent_0_ng_template_0_ng_container_0_1_ng_template_0_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainer(0, 4);
  }
  if (rf & 2) {
    const component_r2 = ɵɵnextContext(2).$implicit;
    ɵɵproperty("cxComponentWrapper", component_r2);
  }
}
function PageSlotComponent_0_ng_template_0_ng_container_0_1_Template(rf, ctx) {
  if (rf & 1) {
    const _r1 = ɵɵgetCurrentView();
    ɵɵtemplate(0, PageSlotComponent_0_ng_template_0_ng_container_0_1_ng_template_0_Template, 1, 1, "ng-template", 3);
    ɵɵlistener("loaded", function PageSlotComponent_0_ng_template_0_ng_container_0_1_Template_ng_template_loaded_0_listener($event) {
      ɵɵrestoreView(_r1);
      const ctx_r2 = ɵɵnextContext(4);
      return ɵɵresetView(ctx_r2.isLoaded($event));
    });
  }
  if (rf & 2) {
    const component_r2 = ɵɵnextContext().$implicit;
    const ctx_r2 = ɵɵnextContext(3);
    ɵɵproperty("cxOutlet", component_r2.flexType)("cxOutletContext", ɵɵpureFunction1(3, _c62, component_r2))("cxOutletDefer", ctx_r2.getComponentDeferOptions(component_r2.flexType));
  }
}
function PageSlotComponent_0_ng_template_0_ng_container_0_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainerStart(0);
    ɵɵtemplate(1, PageSlotComponent_0_ng_template_0_ng_container_0_1_Template, 1, 5, null, 0);
    ɵɵelementContainerEnd();
  }
  if (rf & 2) {
    const component_r2 = ctx.$implicit;
    ɵɵadvance();
    ɵɵproperty("ngIf", component_r2.flexType);
  }
}
function PageSlotComponent_0_ng_template_0_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵtemplate(0, PageSlotComponent_0_ng_template_0_ng_container_0_Template, 2, 1, "ng-container", 2);
  }
  if (rf & 2) {
    const ctx_r2 = ɵɵnextContext(2);
    ɵɵproperty("ngForOf", ctx_r2.components);
  }
}
function PageSlotComponent_0_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵtemplate(0, PageSlotComponent_0_ng_template_0_Template, 1, 1, "ng-template", 1);
  }
  if (rf & 2) {
    const ctx_r2 = ɵɵnextContext();
    ɵɵproperty("cxOutlet", ctx_r2.position)("cxOutletContext", ɵɵpureFunction1(2, _c52, ctx_r2.components$));
  }
}
var _c72 = (a0, a1, a2) => ({
  templateName$: a0,
  slots$: a1,
  section$: a2
});
function PageLayoutComponent_0_ng_template_0_cx_page_slot_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "cx-page-slot", 3);
    ɵɵpipe(1, "async");
  }
  if (rf & 2) {
    const slot_r1 = ctx.$implicit;
    const ctx_r1 = ɵɵnextContext(3);
    ɵɵproperty("position", slot_r1)("isPageFold", slot_r1 === ɵɵpipeBind1(1, 2, ctx_r1.pageFoldSlot$));
  }
}
function PageLayoutComponent_0_ng_template_0_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵprojection(0);
    ɵɵtemplate(1, PageLayoutComponent_0_ng_template_0_cx_page_slot_1_Template, 2, 4, "cx-page-slot", 2);
    ɵɵpipe(2, "async");
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext(2);
    ɵɵadvance();
    ɵɵproperty("ngForOf", ɵɵpipeBind1(2, 1, ctx_r1.slots$));
  }
}
function PageLayoutComponent_0_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵtemplate(0, PageLayoutComponent_0_ng_template_0_Template, 3, 3, "ng-template", 1);
  }
  if (rf & 2) {
    const layoutName_r3 = ctx.ngIf;
    const ctx_r1 = ɵɵnextContext();
    ɵɵproperty("cxPageTemplateStyle", layoutName_r3)("cxOutlet", layoutName_r3)("cxOutletContext", ɵɵpureFunction3(3, _c72, ctx_r1.templateName$, ctx_r1.slots$, ctx_r1.section$));
  }
}
function AddToHomeScreenBannerComponent_div_0_Template(rf, ctx) {
  if (rf & 1) {
    const _r1 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "div")(1, "div", 1)(2, "div", 2)(3, "p");
    ɵɵtext(4);
    ɵɵpipe(5, "cxTranslate");
    ɵɵelementEnd();
    ɵɵelementStart(6, "ul")(7, "li");
    ɵɵtext(8);
    ɵɵpipe(9, "cxTranslate");
    ɵɵelementEnd();
    ɵɵelementStart(10, "li");
    ɵɵtext(11);
    ɵɵpipe(12, "cxTranslate");
    ɵɵelementEnd()();
    ɵɵelementStart(13, "button", 3);
    ɵɵlistener("click", function AddToHomeScreenBannerComponent_div_0_Template_button_click_13_listener() {
      ɵɵrestoreView(_r1);
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.prompt());
    });
    ɵɵtext(14);
    ɵɵpipe(15, "cxTranslate");
    ɵɵelementEnd()()()();
  }
  if (rf & 2) {
    ɵɵadvance(4);
    ɵɵtextInterpolate1(" ", ɵɵpipeBind1(5, 4, "pwa.addToHomeScreenDescription"), " ");
    ɵɵadvance(4);
    ɵɵtextInterpolate(ɵɵpipeBind1(9, 6, "pwa.noInstallationNeeded"));
    ɵɵadvance(3);
    ɵɵtextInterpolate(ɵɵpipeBind1(12, 8, "pwa.fastAccessToApplication"));
    ɵɵadvance(3);
    ɵɵtextInterpolate1(" ", ɵɵpipeBind1(15, 10, "pwa.addToHomeScreen"), " ");
  }
}
function AddToHomeScreenBtnComponent_ng_content_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵprojection(0, 0, ["*ngIf", "canPrompt$ | async"]);
  }
}
var _c82 = () => ({
  tab: true
});
function SkipLinkComponent_div_0_button_1_Template(rf, ctx) {
  if (rf & 1) {
    const _r1 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "button", 3);
    ɵɵlistener("click", function SkipLinkComponent_div_0_button_1_Template_button_click_0_listener() {
      const link_r2 = ɵɵrestoreView(_r1).$implicit;
      const ctx_r2 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r2.scrollToTarget(link_r2));
    });
    ɵɵtext(1);
    ɵɵpipe(2, "cxTranslate");
    ɵɵpipe(3, "cxTranslate");
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const link_r2 = ctx.$implicit;
    ɵɵadvance();
    ɵɵtextInterpolate2(" ", ɵɵpipeBind1(2, 2, "skipLink.skipTo"), " ", ɵɵpipeBind1(3, 4, link_r2.i18nKey), " ");
  }
}
function SkipLinkComponent_div_0_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div", 1);
    ɵɵtemplate(1, SkipLinkComponent_div_0_button_1_Template, 4, 6, "button", 2);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const links_r4 = ctx.ngIf;
    ɵɵproperty("cxFocus", ɵɵpureFunction0(2, _c82));
    ɵɵadvance();
    ɵɵproperty("ngForOf", links_r4);
  }
}
var _c92 = (a0, a1) => ({
  item: a0,
  itemIndex: a1
});
var _c10 = (a0, a1) => ({
  currentSlideNumber: a0,
  numberOfSlides: a1
});
function CarouselComponent_ng_container_0_h2_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "h2");
    ɵɵtext(1);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext(2);
    ɵɵadvance();
    ɵɵtextInterpolate(ctx_r0.title);
  }
}
function CarouselComponent_ng_container_0_button_3_Template(rf, ctx) {
  if (rf & 1) {
    const _r2 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "button", 7);
    ɵɵpipe(1, "cxTranslate");
    ɵɵpipe(2, "cxTranslate");
    ɵɵlistener("click", function CarouselComponent_ng_container_0_button_3_Template_button_click_0_listener($event) {
      ɵɵrestoreView(_r2);
      const size_r3 = ɵɵnextContext().ngIf;
      const ctx_r0 = ɵɵnextContext();
      $event.stopPropagation();
      return ɵɵresetView(ctx_r0.activeSlide === 0 ? null : ctx_r0.activeSlide = ctx_r0.activeSlide - size_r3);
    })("keydown.arrowleft", function CarouselComponent_ng_container_0_button_3_Template_button_keydown_arrowleft_0_listener($event) {
      ɵɵrestoreView(_r2);
      const ctx_r0 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r0.shareEvent($event));
    })("keydown.arrowright", function CarouselComponent_ng_container_0_button_3_Template_button_keydown_arrowright_0_listener($event) {
      ɵɵrestoreView(_r2);
      const ctx_r0 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r0.shareEvent($event));
    })("keydown.arrowup", function CarouselComponent_ng_container_0_button_3_Template_button_keydown_arrowup_0_listener($event) {
      ɵɵrestoreView(_r2);
      const ctx_r0 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r0.shareEvent($event));
    })("keydown.arrowdown", function CarouselComponent_ng_container_0_button_3_Template_button_keydown_arrowdown_0_listener($event) {
      ɵɵrestoreView(_r2);
      const ctx_r0 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r0.shareEvent($event));
    });
    ɵɵelement(3, "cx-icon", 8);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext(2);
    ɵɵattribute("aria-disabled", ctx_r0.activeSlide === 0)("aria-label", ɵɵpipeBind1(1, 4, "carousel.previousSlide"))("title", ɵɵpipeBind1(2, 6, "carousel.previousSlide"));
    ɵɵadvance(3);
    ɵɵproperty("type", ctx_r0.previousIcon);
  }
}
function CarouselComponent_ng_container_0_ng_container_5_div_1_ng_container_1_div_1_ng_container_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainer(0);
  }
}
function CarouselComponent_ng_container_0_ng_container_5_div_1_ng_container_1_div_1_Template(rf, ctx) {
  if (rf & 1) {
    const _r4 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "div", 12);
    ɵɵlistener("keydown", function CarouselComponent_ng_container_0_ng_container_5_div_1_ng_container_1_div_1_Template_div_keydown_0_listener($event) {
      ɵɵrestoreView(_r4);
      const size_r3 = ɵɵnextContext(4).ngIf;
      const ctx_r0 = ɵɵnextContext();
      return ɵɵresetView(ctx_r0.onItemKeydown($event, size_r3));
    });
    ɵɵtemplate(1, CarouselComponent_ng_container_0_ng_container_5_div_1_ng_container_1_div_1_ng_container_1_Template, 1, 0, "ng-container", 13);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const data_r5 = ctx.ngIf;
    const j_r6 = ɵɵnextContext().index;
    const i_r7 = ɵɵnextContext(2).index;
    const ctx_r0 = ɵɵnextContext(2);
    ɵɵclassProp("active", i_r7 === ctx_r0.activeSlide);
    ɵɵadvance();
    ɵɵproperty("ngTemplateOutlet", ctx_r0.template)("ngTemplateOutletContext", ɵɵpureFunction2(4, _c92, data_r5, i_r7 + j_r6));
  }
}
function CarouselComponent_ng_container_0_ng_container_5_div_1_ng_container_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainerStart(0);
    ɵɵtemplate(1, CarouselComponent_ng_container_0_ng_container_5_div_1_ng_container_1_div_1_Template, 2, 7, "div", 11);
    ɵɵpipe(2, "async");
    ɵɵelementContainerEnd();
  }
  if (rf & 2) {
    const item_r8 = ctx.$implicit;
    ɵɵadvance();
    ɵɵproperty("ngIf", ɵɵpipeBind1(2, 1, item_r8));
  }
}
function CarouselComponent_ng_container_0_ng_container_5_div_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div", 10);
    ɵɵtemplate(1, CarouselComponent_ng_container_0_ng_container_5_div_1_ng_container_1_Template, 3, 3, "ng-container", 4);
    ɵɵpipe(2, "slice");
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const i_r7 = ɵɵnextContext().index;
    const size_r3 = ɵɵnextContext().ngIf;
    const ctx_r0 = ɵɵnextContext();
    ɵɵclassProp("active", i_r7 === ctx_r0.activeSlide);
    ɵɵadvance();
    ɵɵproperty("ngForOf", ɵɵpipeBind3(2, 4, ctx_r0.items, i_r7, i_r7 + size_r3))("ngForTrackBy", ctx_r0.trackByFn);
  }
}
function CarouselComponent_ng_container_0_ng_container_5_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainerStart(0);
    ɵɵtemplate(1, CarouselComponent_ng_container_0_ng_container_5_div_1_Template, 3, 8, "div", 9);
    ɵɵelementContainerEnd();
  }
  if (rf & 2) {
    const i_r7 = ctx.index;
    const size_r3 = ɵɵnextContext().ngIf;
    ɵɵadvance();
    ɵɵproperty("ngIf", i_r7 % size_r3 === 0);
  }
}
function CarouselComponent_ng_container_0_button_6_Template(rf, ctx) {
  if (rf & 1) {
    const _r9 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "button", 14);
    ɵɵpipe(1, "cxTranslate");
    ɵɵpipe(2, "cxTranslate");
    ɵɵlistener("click", function CarouselComponent_ng_container_0_button_6_Template_button_click_0_listener($event) {
      ɵɵrestoreView(_r9);
      const size_r3 = ɵɵnextContext().ngIf;
      const ctx_r0 = ɵɵnextContext();
      $event.stopPropagation();
      return ɵɵresetView(ctx_r0.activeSlide > ctx_r0.items.length - size_r3 - 1 ? null : ctx_r0.activeSlide = ctx_r0.activeSlide + size_r3);
    })("keydown.arrowleft", function CarouselComponent_ng_container_0_button_6_Template_button_keydown_arrowleft_0_listener($event) {
      ɵɵrestoreView(_r9);
      const ctx_r0 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r0.shareEvent($event));
    })("keydown.arrowright", function CarouselComponent_ng_container_0_button_6_Template_button_keydown_arrowright_0_listener($event) {
      ɵɵrestoreView(_r9);
      const ctx_r0 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r0.shareEvent($event));
    })("keydown.arrowup", function CarouselComponent_ng_container_0_button_6_Template_button_keydown_arrowup_0_listener($event) {
      ɵɵrestoreView(_r9);
      const ctx_r0 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r0.shareEvent($event));
    })("keydown.arrowdown", function CarouselComponent_ng_container_0_button_6_Template_button_keydown_arrowdown_0_listener($event) {
      ɵɵrestoreView(_r9);
      const ctx_r0 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r0.shareEvent($event));
    });
    ɵɵelement(3, "cx-icon", 8);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const size_r3 = ɵɵnextContext().ngIf;
    const ctx_r0 = ɵɵnextContext();
    ɵɵattribute("aria-disabled", ctx_r0.activeSlide > ctx_r0.items.length - size_r3 - 1)("aria-label", ɵɵpipeBind1(1, 4, "carousel.nextSlide"))("title", ɵɵpipeBind1(2, 6, "carousel.nextSlide"));
    ɵɵadvance(3);
    ɵɵproperty("type", ctx_r0.nextIcon);
  }
}
function CarouselComponent_ng_container_0_div_7_ng_container_1_button_1_Template(rf, ctx) {
  if (rf & 1) {
    const _r10 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "button", 18);
    ɵɵpipe(1, "cxTranslate");
    ɵɵpipe(2, "cxTranslate");
    ɵɵlistener("click", function CarouselComponent_ng_container_0_div_7_ng_container_1_button_1_Template_button_click_0_listener($event) {
      ɵɵrestoreView(_r10);
      const i_r11 = ɵɵnextContext().index;
      const ctx_r0 = ɵɵnextContext(3);
      $event.stopPropagation();
      return ɵɵresetView(ctx_r0.activeSlide = i_r11);
    });
    ɵɵelement(3, "cx-icon", 19);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const i_r11 = ɵɵnextContext().index;
    const size_r3 = ɵɵnextContext(2).ngIf;
    const ctx_r0 = ɵɵnextContext();
    ɵɵattribute("aria-current", i_r11 === ctx_r0.activeSlide)("aria-disabled", i_r11 === ctx_r0.activeSlide)("aria-label", ɵɵpipeBind2(1, 5, "carousel.slideNumber", ɵɵpureFunction2(11, _c10, ctx_r0.getSlideNumber(size_r3, i_r11), size_r3)))("title", ɵɵpipeBind2(2, 8, "carousel.slideNumber", ɵɵpureFunction2(14, _c10, ctx_r0.getSlideNumber(size_r3, i_r11), size_r3)));
    ɵɵadvance(3);
    ɵɵproperty("type", ctx_r0.indicatorIcon);
  }
}
function CarouselComponent_ng_container_0_div_7_ng_container_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainerStart(0);
    ɵɵtemplate(1, CarouselComponent_ng_container_0_div_7_ng_container_1_button_1_Template, 4, 17, "button", 17);
    ɵɵelementContainerEnd();
  }
  if (rf & 2) {
    const i_r11 = ctx.index;
    const size_r3 = ɵɵnextContext(2).ngIf;
    ɵɵadvance();
    ɵɵproperty("ngIf", i_r11 % size_r3 === 0);
  }
}
function CarouselComponent_ng_container_0_div_7_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div", 15);
    ɵɵtemplate(1, CarouselComponent_ng_container_0_div_7_ng_container_1_Template, 2, 1, "ng-container", 16);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext(2);
    ɵɵadvance();
    ɵɵproperty("ngForOf", ctx_r0.items);
  }
}
function CarouselComponent_ng_container_0_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainerStart(0);
    ɵɵtemplate(1, CarouselComponent_ng_container_0_h2_1_Template, 2, 1, "h2", 0);
    ɵɵelementStart(2, "div", 1);
    ɵɵtemplate(3, CarouselComponent_ng_container_0_button_3_Template, 4, 8, "button", 2);
    ɵɵelementStart(4, "div", 3);
    ɵɵtemplate(5, CarouselComponent_ng_container_0_ng_container_5_Template, 2, 1, "ng-container", 4);
    ɵɵelementEnd();
    ɵɵtemplate(6, CarouselComponent_ng_container_0_button_6_Template, 4, 8, "button", 5);
    ɵɵelementEnd();
    ɵɵtemplate(7, CarouselComponent_ng_container_0_div_7_Template, 2, 1, "div", 6);
    ɵɵelementContainerEnd();
  }
  if (rf & 2) {
    const size_r3 = ctx.ngIf;
    const ctx_r0 = ɵɵnextContext();
    ɵɵadvance();
    ɵɵproperty("ngIf", ctx_r0.title);
    ɵɵadvance();
    ɵɵproperty("ngClass", "size-" + size_r3);
    ɵɵadvance();
    ɵɵproperty("ngIf", size_r3 < ctx_r0.items.length);
    ɵɵadvance(2);
    ɵɵproperty("ngForOf", ctx_r0.items)("ngForTrackBy", ctx_r0.trackByFn);
    ɵɵadvance();
    ɵɵproperty("ngIf", size_r3 < ctx_r0.items.length);
    ɵɵadvance();
    ɵɵproperty("ngIf", !ctx_r0.hideIndicators && size_r3 < ctx_r0.items.length);
  }
}
var _c11 = (a0, a1, a2) => ({
  flexType: a0,
  typeCode: a1,
  uid: a2
});
function BannerCarouselComponent_cx_carousel_0_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "cx-carousel", 2);
  }
  if (rf & 2) {
    const items_r1 = ctx.ngIf;
    ɵɵnextContext();
    const template_r2 = ɵɵreference(3);
    ɵɵproperty("items", items_r1)("template", template_r2);
  }
}
function BannerCarouselComponent_ng_template_2_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainer(0, 3);
  }
  if (rf & 2) {
    const item_r3 = ctx.item;
    ɵɵproperty("cxComponentWrapper", ɵɵpureFunction3(1, _c11, item_r3.typeCode, item_r3.typeCode, item_r3 == null ? null : item_r3.uid));
  }
}
var _c122 = (a0, a1, a2) => ({
  loading: a0,
  templateList: a1,
  allConsentsLoading: a2
});
function ConsentManagementComponent_ng_container_0_ng_container_1_div_9_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div", 5);
    ɵɵelement(1, "cx-spinner");
    ɵɵelementEnd();
  }
}
function ConsentManagementComponent_ng_container_0_ng_container_1_ng_container_10_cx_consent_management_form_3_Template(rf, ctx) {
  if (rf & 1) {
    const _r4 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "cx-consent-management-form", 8);
    ɵɵlistener("consentChanged", function ConsentManagementComponent_ng_container_0_ng_container_1_ng_container_10_cx_consent_management_form_3_Template_cx_consent_management_form_consentChanged_0_listener($event) {
      ɵɵrestoreView(_r4);
      const ctx_r2 = ɵɵnextContext(4);
      return ɵɵresetView(ctx_r2.onConsentChange($event));
    });
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const consentTemplate_r5 = ctx.$implicit;
    const data_r6 = ɵɵnextContext(3).ngIf;
    const ctx_r2 = ɵɵnextContext();
    ɵɵproperty("consentTemplate", consentTemplate_r5)("requiredConsents", ctx_r2.requiredConsents)("disabled", !!data_r6.loading);
  }
}
function ConsentManagementComponent_ng_container_0_ng_container_1_ng_container_10_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainerStart(0);
    ɵɵelementStart(1, "div", 6)(2, "div", 2);
    ɵɵtemplate(3, ConsentManagementComponent_ng_container_0_ng_container_1_ng_container_10_cx_consent_management_form_3_Template, 1, 3, "cx-consent-management-form", 7);
    ɵɵelementEnd()();
    ɵɵelementContainerEnd();
  }
  if (rf & 2) {
    const templateList_r2 = ɵɵnextContext().ngIf;
    ɵɵadvance(3);
    ɵɵproperty("ngForOf", templateList_r2);
  }
}
function ConsentManagementComponent_ng_container_0_ng_container_1_Template(rf, ctx) {
  if (rf & 1) {
    const _r1 = ɵɵgetCurrentView();
    ɵɵelementContainerStart(0);
    ɵɵelementStart(1, "div", 1)(2, "div", 2)(3, "button", 3);
    ɵɵlistener("click", function ConsentManagementComponent_ng_container_0_ng_container_1_Template_button_click_3_listener() {
      const templateList_r2 = ɵɵrestoreView(_r1).ngIf;
      const ctx_r2 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r2.rejectAll(templateList_r2));
    });
    ɵɵtext(4);
    ɵɵpipe(5, "cxTranslate");
    ɵɵelementEnd();
    ɵɵelementStart(6, "button", 3);
    ɵɵlistener("click", function ConsentManagementComponent_ng_container_0_ng_container_1_Template_button_click_6_listener() {
      const templateList_r2 = ɵɵrestoreView(_r1).ngIf;
      const ctx_r2 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r2.allowAll(templateList_r2));
    });
    ɵɵtext(7);
    ɵɵpipe(8, "cxTranslate");
    ɵɵelementEnd()()();
    ɵɵtemplate(9, ConsentManagementComponent_ng_container_0_ng_container_1_div_9_Template, 2, 0, "div", 4)(10, ConsentManagementComponent_ng_container_0_ng_container_1_ng_container_10_Template, 4, 1, "ng-container", 0);
    ɵɵelementContainerEnd();
  }
  if (rf & 2) {
    const templateList_r2 = ctx.ngIf;
    const data_r6 = ɵɵnextContext().ngIf;
    ɵɵadvance(3);
    ɵɵproperty("disabled", data_r6.loading);
    ɵɵadvance();
    ɵɵtextInterpolate1(" ", ɵɵpipeBind1(5, 6, "consentManagementForm.clearAll"), " ");
    ɵɵadvance(2);
    ɵɵproperty("disabled", data_r6.loading);
    ɵɵadvance();
    ɵɵtextInterpolate1(" ", ɵɵpipeBind1(8, 8, "consentManagementForm.selectAll"), " ");
    ɵɵadvance(2);
    ɵɵproperty("ngIf", data_r6.allConsentsLoading || data_r6.loading && !(templateList_r2 == null ? null : templateList_r2.length));
    ɵɵadvance();
    ɵɵproperty("ngIf", !data_r6.allConsentsLoading);
  }
}
function ConsentManagementComponent_ng_container_0_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainerStart(0);
    ɵɵtemplate(1, ConsentManagementComponent_ng_container_0_ng_container_1_Template, 11, 10, "ng-container", 0);
    ɵɵelementContainerEnd();
  }
  if (rf & 2) {
    const data_r6 = ctx.ngIf;
    ɵɵadvance();
    ɵɵproperty("ngIf", data_r6.templateList);
  }
}
function MyAccountV2ConsentManagementFormComponent_ng_container_6_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainerStart(0);
    ɵɵtext(1);
    ɵɵelementContainerEnd();
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    ɵɵadvance();
    ɵɵtextInterpolate1(" ", ctx_r0.consentTemplate == null ? null : ctx_r0.consentTemplate.name, " ");
  }
}
function MyAccountV2ConsentManagementFormComponent_span_9_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "span", 6);
    ɵɵtext(1);
    ɵɵpipe(2, "cxTranslate");
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    ɵɵadvance();
    ɵɵtextInterpolate2(" ", ɵɵpipeBind1(2, 2, "myAccountV2Consent.dateDescription"), "", ctx_r0.consentApprovedTime, " ");
  }
}
function MyAccountV2ConsentManagementComponent_div_0_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div")(1, "div", 2);
    ɵɵelement(2, "cx-spinner");
    ɵɵelementEnd()();
  }
}
function MyAccountV2ConsentManagementComponent_ng_template_2_ng_container_0_cx_my_account_v2_consent_management_form_13_Template(rf, ctx) {
  if (rf & 1) {
    const _r4 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "cx-my-account-v2-consent-management-form", 10);
    ɵɵlistener("consentChanged", function MyAccountV2ConsentManagementComponent_ng_template_2_ng_container_0_cx_my_account_v2_consent_management_form_13_Template_cx_my_account_v2_consent_management_form_consentChanged_0_listener($event) {
      ɵɵrestoreView(_r4);
      const ctx_r2 = ɵɵnextContext(3);
      return ɵɵresetView(ctx_r2.onConsentChange($event));
    });
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const consentTemplate_r5 = ctx.$implicit;
    const ctx_r2 = ɵɵnextContext(3);
    ɵɵproperty("consentTemplate", consentTemplate_r5)("requiredConsents", ctx_r2.requiredConsents);
  }
}
function MyAccountV2ConsentManagementComponent_ng_template_2_ng_container_0_Template(rf, ctx) {
  if (rf & 1) {
    const _r1 = ɵɵgetCurrentView();
    ɵɵelementContainerStart(0);
    ɵɵelementStart(1, "div", 4)(2, "div", 5)(3, "div", 6);
    ɵɵtext(4);
    ɵɵpipe(5, "cxTranslate");
    ɵɵelementEnd();
    ɵɵelementStart(6, "div", 7)(7, "button", 8);
    ɵɵlistener("click", function MyAccountV2ConsentManagementComponent_ng_template_2_ng_container_0_Template_button_click_7_listener() {
      const templateList_r2 = ɵɵrestoreView(_r1).ngIf;
      const ctx_r2 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r2.rejectAll(templateList_r2));
    });
    ɵɵtext(8);
    ɵɵpipe(9, "cxTranslate");
    ɵɵelementEnd();
    ɵɵelementStart(10, "button", 8);
    ɵɵlistener("click", function MyAccountV2ConsentManagementComponent_ng_template_2_ng_container_0_Template_button_click_10_listener() {
      const templateList_r2 = ɵɵrestoreView(_r1).ngIf;
      const ctx_r2 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r2.allowAll(templateList_r2));
    });
    ɵɵtext(11);
    ɵɵpipe(12, "cxTranslate");
    ɵɵelementEnd()();
    ɵɵtemplate(13, MyAccountV2ConsentManagementComponent_ng_template_2_ng_container_0_cx_my_account_v2_consent_management_form_13_Template, 1, 2, "cx-my-account-v2-consent-management-form", 9);
    ɵɵelementEnd()();
    ɵɵelementContainerEnd();
  }
  if (rf & 2) {
    const templateList_r2 = ctx.ngIf;
    ɵɵadvance(4);
    ɵɵtextInterpolate1(" ", ɵɵpipeBind1(5, 4, "myAccountV2Consent.header"), " ");
    ɵɵadvance(4);
    ɵɵtextInterpolate1(" ", ɵɵpipeBind1(9, 6, "myAccountV2Consent.clearAll"), " ");
    ɵɵadvance(3);
    ɵɵtextInterpolate1(" ", ɵɵpipeBind1(12, 8, "myAccountV2Consent.selectAll"), " ");
    ɵɵadvance(2);
    ɵɵproperty("ngForOf", templateList_r2);
  }
}
function MyAccountV2ConsentManagementComponent_ng_template_2_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵtemplate(0, MyAccountV2ConsentManagementComponent_ng_template_2_ng_container_0_Template, 14, 10, "ng-container", 3);
    ɵɵpipe(1, "async");
  }
  if (rf & 2) {
    const ctx_r2 = ɵɵnextContext();
    ɵɵproperty("ngIf", ɵɵpipeBind1(1, 1, ctx_r2.templateList$));
  }
}
function PopoverComponent_button_3_Template(rf, ctx) {
  if (rf & 1) {
    const _r1 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "button", 5);
    ɵɵpipe(1, "cxTranslate");
    ɵɵpipe(2, "cxTranslate");
    ɵɵlistener("keydown.enter", function PopoverComponent_button_3_Template_button_keydown_enter_0_listener($event) {
      ɵɵrestoreView(_r1);
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.close($event));
    })("keydown.space", function PopoverComponent_button_3_Template_button_keydown_space_0_listener($event) {
      ɵɵrestoreView(_r1);
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.close($event));
    })("click", function PopoverComponent_button_3_Template_button_click_0_listener($event) {
      ɵɵrestoreView(_r1);
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.close($event));
    });
    ɵɵelement(3, "cx-icon", 6);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext();
    ɵɵpropertyInterpolate("title", ɵɵpipeBind1(1, 3, "common.close"));
    ɵɵattribute("aria-label", ɵɵpipeBind1(2, 5, "common.close"));
    ɵɵadvance(3);
    ɵɵproperty("type", ctx_r1.iconTypes.CLOSE);
  }
}
function PopoverComponent_ng_container_4_ng_container_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainer(0);
  }
}
function PopoverComponent_ng_container_4_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainerStart(0);
    ɵɵtemplate(1, PopoverComponent_ng_container_4_ng_container_1_Template, 1, 0, "ng-container", 7);
    ɵɵelementContainerEnd();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext();
    ɵɵadvance();
    ɵɵproperty("ngTemplateOutlet", ctx_r1.content);
  }
}
function PopoverComponent_span_5_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "span");
    ɵɵtext(1);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext();
    ɵɵadvance();
    ɵɵtextInterpolate(ctx_r1.content);
  }
}
var _c13 = (a0) => [a0];
var _c14 = () => ({
  placement: "auto",
  appendToBody: true,
  displayCloseButton: true
});
function TruncateTextPopoverComponent_ng_template_5_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵtext(0);
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    ɵɵtextInterpolate1(" ", ctx_r0.content, " ");
  }
}
function TruncateTextPopoverComponent_button_7_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "button", 3);
    ɵɵtext(1);
    ɵɵpipe(2, "cxTranslate");
    ɵɵelementEnd();
  }
  if (rf & 2) {
    ɵɵnextContext();
    const fullText_r2 = ɵɵreference(6);
    ɵɵproperty("cxPopover", fullText_r2)("cxPopoverOptions", ɵɵpureFunction0(5, _c14));
    ɵɵadvance();
    ɵɵtextInterpolate1(" ", ɵɵpipeBind1(2, 3, "common.more"), " ");
  }
}
function GenericLinkComponent_ng_container_0_ng_container_2_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainer(0);
  }
}
function GenericLinkComponent_ng_container_0_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainerStart(0);
    ɵɵelementStart(1, "a", 3);
    ɵɵtemplate(2, GenericLinkComponent_ng_container_0_ng_container_2_Template, 1, 0, "ng-container", 4);
    ɵɵelementEnd();
    ɵɵelementContainerEnd();
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    const content_r2 = ɵɵreference(4);
    ɵɵadvance();
    ɵɵproperty("href", ctx_r0.url, ɵɵsanitizeUrl)("tabindex", ctx_r0.tabindex);
    ɵɵattribute("target", ctx_r0.target)("rel", ctx_r0.rel)("id", ctx_r0.id)("class", ctx_r0.class)("style", ctx_r0.style, ɵɵsanitizeStyle)("title", ctx_r0.title)("aria-label", ctx_r0.ariaLabel ? ctx_r0.ariaLabel : null);
    ɵɵadvance();
    ɵɵproperty("ngTemplateOutlet", content_r2);
  }
}
function GenericLinkComponent_ng_template_1_ng_container_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainer(0);
  }
}
function GenericLinkComponent_ng_template_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "a", 5);
    ɵɵtemplate(1, GenericLinkComponent_ng_template_1_ng_container_1_Template, 1, 0, "ng-container", 4);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    const content_r2 = ɵɵreference(4);
    ɵɵproperty("routerLink", ctx_r0.routerUrl)("queryParams", ctx_r0.queryParams)("fragment", ctx_r0.fragment)("target", ctx_r0.target || void 0)("tabindex", ctx_r0.tabindex);
    ɵɵattribute("id", ctx_r0.id)("class", ctx_r0.class)("style", ctx_r0.style, ɵɵsanitizeStyle)("title", ctx_r0.title)("aria-label", ctx_r0.ariaLabel ? ctx_r0.ariaLabel : null);
    ɵɵadvance();
    ɵɵproperty("ngTemplateOutlet", content_r2);
  }
}
function GenericLinkComponent_ng_template_3_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵprojection(0);
  }
}
var _c15 = [[["", "label_container_bottom", ""]]];
var _c16 = ["[label_container_bottom]"];
var _c17 = (a0, a1, a2, a3) => ({
  content: a0,
  truncateText: a1,
  charactersLimit: a2,
  class: "cx-card-label",
  text: a3
});
var _c18 = (a0, a1, a2, a3) => ({
  content: a0,
  truncateText: a1,
  charactersLimit: a2,
  class: "cx-card-paragraph-text",
  text: a3
});
var _c19 = () => ({
  autofocus: true
});
function CardComponent_div_0_div_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div", 15);
    ɵɵtext(1);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext(2);
    ɵɵproperty("id", ctx_r0.getLabel("content-header"));
    ɵɵadvance();
    ɵɵtextInterpolate1(" ", ctx_r0.content.header, " ");
  }
}
function CardComponent_div_0_div_3_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div", 16);
    ɵɵtext(1);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext(2);
    ɵɵadvance();
    ɵɵtextInterpolate1(" ", ctx_r0.content.deleteMsg, " ");
  }
}
function CardComponent_div_0_span_4_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "span", 17);
    ɵɵtext(1);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext(2);
    ɵɵproperty("id", ctx_r0.getLabel("content-title"));
    ɵɵadvance();
    ɵɵtextInterpolate1(" ", ctx_r0.content.title, " ");
  }
}
function CardComponent_div_0_div_7_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div", 18);
    ɵɵtext(1);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext(2);
    ɵɵadvance();
    ɵɵtextInterpolate1(" ", ctx_r0.content.textBold, " ");
  }
}
function CardComponent_div_0_div_8_ng_container_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainer(0);
  }
}
function CardComponent_div_0_div_8_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div");
    ɵɵtemplate(1, CardComponent_div_0_div_8_ng_container_1_Template, 1, 0, "ng-container", 19);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const line_r2 = ctx.$implicit;
    const ctx_r0 = ɵɵnextContext(2);
    const showText_r3 = ɵɵreference(2);
    ɵɵadvance();
    ɵɵproperty("ngTemplateOutlet", showText_r3)("ngTemplateOutletContext", ɵɵpureFunction4(2, _c17, ctx_r0.content, ctx_r0.truncateText, ctx_r0.charactersLimit, line_r2));
  }
}
function CardComponent_div_0_div_9_div_3_ng_container_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainer(0);
  }
}
function CardComponent_div_0_div_9_div_3_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div");
    ɵɵtemplate(1, CardComponent_div_0_div_9_div_3_ng_container_1_Template, 1, 0, "ng-container", 19);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const text_r4 = ctx.$implicit;
    const ctx_r0 = ɵɵnextContext(3);
    const showText_r3 = ɵɵreference(2);
    ɵɵadvance();
    ɵɵproperty("ngTemplateOutlet", showText_r3)("ngTemplateOutletContext", ɵɵpureFunction4(2, _c18, ctx_r0.content, ctx_r0.truncateParagraphText, ctx_r0.charactersLimit, text_r4));
  }
}
function CardComponent_div_0_div_9_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div", 20)(1, "div", 21);
    ɵɵtext(2);
    ɵɵelementEnd();
    ɵɵtemplate(3, CardComponent_div_0_div_9_div_3_Template, 2, 7, "div", 10);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const item_r5 = ctx.$implicit;
    ɵɵadvance(2);
    ɵɵtextInterpolate(item_r5.title);
    ɵɵadvance();
    ɵɵproperty("ngForOf", item_r5.text);
  }
}
function CardComponent_div_0_div_11_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div", 22);
    ɵɵpipe(1, "cxTranslate");
    ɵɵpipe(2, "cxTranslate");
    ɵɵelement(3, "cx-icon", 23);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    let tmp_3_0;
    let tmp_4_0;
    const ctx_r0 = ɵɵnextContext(2);
    ɵɵattribute("aria-label", ɵɵpipeBind1(1, 3, (tmp_3_0 = ctx_r0.content.imgLabel) !== null && tmp_3_0 !== void 0 ? tmp_3_0 : "paymentCard.credit"))("title", ɵɵpipeBind1(2, 5, (tmp_4_0 = ctx_r0.content.imgLabel) !== null && tmp_4_0 !== void 0 ? tmp_4_0 : "paymentCard.credit"));
    ɵɵadvance(3);
    ɵɵproperty("type", ctx_r0.content.img);
  }
}
function CardComponent_div_0_div_12_div_1_Template(rf, ctx) {
  if (rf & 1) {
    const _r6 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "div", 26)(1, "div", 27)(2, "button", 28);
    ɵɵlistener("click", function CardComponent_div_0_div_12_div_1_Template_button_click_2_listener() {
      ɵɵrestoreView(_r6);
      const ctx_r0 = ɵɵnextContext(3);
      return ɵɵresetView(ctx_r0.delete());
    });
    ɵɵtext(3);
    ɵɵpipe(4, "cxTranslate");
    ɵɵelementEnd()();
    ɵɵelementStart(5, "div", 29)(6, "button", 30);
    ɵɵpipe(7, "cxTranslate");
    ɵɵlistener("click", function CardComponent_div_0_div_12_div_1_Template_button_click_6_listener() {
      ɵɵrestoreView(_r6);
      const ctx_r0 = ɵɵnextContext(3);
      return ɵɵresetView(ctx_r0.cancelEdit());
    });
    ɵɵtext(8);
    ɵɵpipe(9, "cxTranslate");
    ɵɵelementEnd()()();
  }
  if (rf & 2) {
    ɵɵadvance();
    ɵɵproperty("cxFocus", ɵɵpureFunction0(10, _c19));
    ɵɵadvance(2);
    ɵɵtextInterpolate1(" ", ɵɵpipeBind1(4, 4, "common.delete"), " ");
    ɵɵadvance(3);
    ɵɵproperty("cxAtMessage", ɵɵpipeBind1(7, 6, "assistiveMessage.actionCancelled"));
    ɵɵadvance(2);
    ɵɵtextInterpolate1(" ", ɵɵpipeBind1(9, 8, "common.cancel"), " ");
  }
}
function CardComponent_div_0_div_12_div_2_Template(rf, ctx) {
  if (rf & 1) {
    const _r7 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "div", 26)(1, "div", 29)(2, "button", 30);
    ɵɵpipe(3, "cxTranslate");
    ɵɵlistener("click", function CardComponent_div_0_div_12_div_2_Template_button_click_2_listener() {
      ɵɵrestoreView(_r7);
      const ctx_r0 = ɵɵnextContext(3);
      return ɵɵresetView(ctx_r0.cancelEdit());
    });
    ɵɵtext(4);
    ɵɵpipe(5, "cxTranslate");
    ɵɵelementEnd()();
    ɵɵelementStart(6, "div", 27)(7, "button", 28);
    ɵɵlistener("click", function CardComponent_div_0_div_12_div_2_Template_button_click_7_listener() {
      ɵɵrestoreView(_r7);
      const ctx_r0 = ɵɵnextContext(3);
      return ɵɵresetView(ctx_r0.delete());
    });
    ɵɵtext(8);
    ɵɵpipe(9, "cxTranslate");
    ɵɵelementEnd()()();
  }
  if (rf & 2) {
    ɵɵadvance(2);
    ɵɵproperty("cxAtMessage", ɵɵpipeBind1(3, 4, "assistiveMessage.actionCancelled"));
    ɵɵadvance(2);
    ɵɵtextInterpolate1(" ", ɵɵpipeBind1(5, 6, "common.cancel"), " ");
    ɵɵadvance(2);
    ɵɵproperty("cxFocus", ɵɵpureFunction0(10, _c19));
    ɵɵadvance(2);
    ɵɵtextInterpolate1(" ", ɵɵpipeBind1(9, 8, "common.delete"), " ");
  }
}
function CardComponent_div_0_div_12_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div", 24);
    ɵɵtemplate(1, CardComponent_div_0_div_12_div_1_Template, 10, 11, "div", 25)(2, CardComponent_div_0_div_12_div_2_Template, 10, 11, "div", 25);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    ɵɵadvance();
    ɵɵproperty("cxFeature", "a11yDeleteButton2First");
    ɵɵadvance();
    ɵɵproperty("cxFeature", "!a11yDeleteButton2First");
  }
}
function CardComponent_div_0_div_13_ng_container_1_div_1_ng_container_2_button_1_Template(rf, ctx) {
  if (rf & 1) {
    const _r8 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "button", 37);
    ɵɵlistener("click", function CardComponent_div_0_div_13_ng_container_1_div_1_ng_container_2_button_1_Template_button_click_0_listener() {
      ɵɵrestoreView(_r8);
      const ctx_r0 = ɵɵnextContext(6);
      return ɵɵresetView(ctx_r0.delete());
    });
    ɵɵtext(1);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const action_r9 = ɵɵnextContext(2).$implicit;
    ɵɵadvance();
    ɵɵtextInterpolate1(" ", action_r9.name, " ");
  }
}
function CardComponent_div_0_div_13_ng_container_1_div_1_ng_container_2_button_2_Template(rf, ctx) {
  if (rf & 1) {
    const _r10 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "button", 37);
    ɵɵlistener("click", function CardComponent_div_0_div_13_ng_container_1_div_1_ng_container_2_button_2_Template_button_click_0_listener() {
      ɵɵrestoreView(_r10);
      const ctx_r0 = ɵɵnextContext(6);
      return ɵɵresetView(ctx_r0.setDefault());
    });
    ɵɵtext(1);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const action_r9 = ɵɵnextContext(2).$implicit;
    ɵɵadvance();
    ɵɵtextInterpolate1(" ", action_r9.name, " ");
  }
}
function CardComponent_div_0_div_13_ng_container_1_div_1_ng_container_2_button_3_Template(rf, ctx) {
  if (rf & 1) {
    const _r11 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "button", 37);
    ɵɵlistener("click", function CardComponent_div_0_div_13_ng_container_1_div_1_ng_container_2_button_3_Template_button_click_0_listener() {
      ɵɵrestoreView(_r11);
      const ctx_r0 = ɵɵnextContext(6);
      return ɵɵresetView(ctx_r0.send());
    });
    ɵɵtext(1);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const action_r9 = ɵɵnextContext(2).$implicit;
    const ctx_r0 = ɵɵnextContext(4);
    ɵɵattribute("aria-describedby", ctx_r0.index >= 0 && ctx_r0.content.header && !ctx_r0.editMode ? "content-header-" + ctx_r0.index : null);
    ɵɵadvance();
    ɵɵtextInterpolate1(" ", action_r9.name, " ");
  }
}
function CardComponent_div_0_div_13_ng_container_1_div_1_ng_container_2_button_4_Template(rf, ctx) {
  if (rf & 1) {
    const _r12 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "button", 37);
    ɵɵlistener("click", function CardComponent_div_0_div_13_ng_container_1_div_1_ng_container_2_button_4_Template_button_click_0_listener() {
      ɵɵrestoreView(_r12);
      const ctx_r0 = ɵɵnextContext(6);
      return ɵɵresetView(ctx_r0.edit());
    });
    ɵɵtext(1);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const action_r9 = ɵɵnextContext(2).$implicit;
    ɵɵadvance();
    ɵɵtextInterpolate1(" ", action_r9.name, " ");
  }
}
function CardComponent_div_0_div_13_ng_container_1_div_1_ng_container_2_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainerStart(0, 35);
    ɵɵtemplate(1, CardComponent_div_0_div_13_ng_container_1_div_1_ng_container_2_button_1_Template, 2, 1, "button", 36)(2, CardComponent_div_0_div_13_ng_container_1_div_1_ng_container_2_button_2_Template, 2, 1, "button", 36)(3, CardComponent_div_0_div_13_ng_container_1_div_1_ng_container_2_button_3_Template, 2, 2, "button", 36)(4, CardComponent_div_0_div_13_ng_container_1_div_1_ng_container_2_button_4_Template, 2, 1, "button", 36);
    ɵɵelementContainerEnd();
  }
  if (rf & 2) {
    const action_r9 = ɵɵnextContext().$implicit;
    ɵɵproperty("ngSwitch", action_r9.event);
    ɵɵadvance();
    ɵɵproperty("ngSwitchCase", "delete");
    ɵɵadvance();
    ɵɵproperty("ngSwitchCase", "default");
    ɵɵadvance();
    ɵɵproperty("ngSwitchCase", "send");
    ɵɵadvance();
    ɵɵproperty("ngSwitchCase", "edit");
  }
}
function CardComponent_div_0_div_13_ng_container_1_div_1_a_3_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "a", 38);
    ɵɵtext(1);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const action_r9 = ɵɵnextContext().$implicit;
    ɵɵpropertyInterpolate("href", action_r9.link, ɵɵsanitizeUrl);
    ɵɵadvance();
    ɵɵtextInterpolate1(" ", action_r9.name, " ");
  }
}
function CardComponent_div_0_div_13_ng_container_1_div_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div")(1, "div");
    ɵɵtemplate(2, CardComponent_div_0_div_13_ng_container_1_div_1_ng_container_2_Template, 5, 5, "ng-container", 33)(3, CardComponent_div_0_div_13_ng_container_1_div_1_a_3_Template, 2, 2, "a", 34);
    ɵɵelementEnd()();
  }
  if (rf & 2) {
    const action_r9 = ctx.$implicit;
    const ctx_r0 = ɵɵnextContext(4);
    ɵɵadvance(2);
    ɵɵproperty("ngIf", ctx_r0.isCardAction(action_r9));
    ɵɵadvance();
    ɵɵproperty("ngIf", ctx_r0.isCardLinkAction(action_r9));
  }
}
function CardComponent_div_0_div_13_ng_container_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainerStart(0);
    ɵɵtemplate(1, CardComponent_div_0_div_13_ng_container_1_div_1_Template, 4, 2, "div", 10);
    ɵɵelementContainerEnd();
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext(3);
    ɵɵadvance();
    ɵɵproperty("ngForOf", ctx_r0.content.actions);
  }
}
function CardComponent_div_0_div_13_ng_container_2_div_1_ng_container_2_button_1_Template(rf, ctx) {
  if (rf & 1) {
    const _r13 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "button", 43);
    ɵɵlistener("click", function CardComponent_div_0_div_13_ng_container_2_div_1_ng_container_2_button_1_Template_button_click_0_listener() {
      ɵɵrestoreView(_r13);
      const ctx_r0 = ɵɵnextContext(6);
      return ɵɵresetView(ctx_r0.delete());
    });
    ɵɵtext(1);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const action_r14 = ɵɵnextContext(2).$implicit;
    ɵɵadvance();
    ɵɵtextInterpolate1(" ", action_r14.name, " ");
  }
}
function CardComponent_div_0_div_13_ng_container_2_div_1_ng_container_2_button_2_Template(rf, ctx) {
  if (rf & 1) {
    const _r15 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "button", 43);
    ɵɵlistener("click", function CardComponent_div_0_div_13_ng_container_2_div_1_ng_container_2_button_2_Template_button_click_0_listener() {
      ɵɵrestoreView(_r15);
      const ctx_r0 = ɵɵnextContext(6);
      return ɵɵresetView(ctx_r0.setDefault());
    });
    ɵɵtext(1);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const action_r14 = ɵɵnextContext(2).$implicit;
    ɵɵadvance();
    ɵɵtextInterpolate1(" ", action_r14.name, " ");
  }
}
function CardComponent_div_0_div_13_ng_container_2_div_1_ng_container_2_button_3_Template(rf, ctx) {
  if (rf & 1) {
    const _r16 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "button", 43);
    ɵɵlistener("click", function CardComponent_div_0_div_13_ng_container_2_div_1_ng_container_2_button_3_Template_button_click_0_listener() {
      ɵɵrestoreView(_r16);
      const ctx_r0 = ɵɵnextContext(6);
      return ɵɵresetView(ctx_r0.send());
    });
    ɵɵtext(1);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const action_r14 = ɵɵnextContext(2).$implicit;
    const ctx_r0 = ɵɵnextContext(4);
    ɵɵattribute("aria-describedby", ctx_r0.index >= 0 && ctx_r0.content.header && !ctx_r0.editMode ? "content-header-" + ctx_r0.index : null);
    ɵɵadvance();
    ɵɵtextInterpolate1(" ", action_r14.name, " ");
  }
}
function CardComponent_div_0_div_13_ng_container_2_div_1_ng_container_2_button_4_Template(rf, ctx) {
  if (rf & 1) {
    const _r17 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "button", 43);
    ɵɵlistener("click", function CardComponent_div_0_div_13_ng_container_2_div_1_ng_container_2_button_4_Template_button_click_0_listener() {
      ɵɵrestoreView(_r17);
      const ctx_r0 = ɵɵnextContext(6);
      return ɵɵresetView(ctx_r0.edit());
    });
    ɵɵtext(1);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const action_r14 = ɵɵnextContext(2).$implicit;
    ɵɵadvance();
    ɵɵtextInterpolate1(" ", action_r14.name, " ");
  }
}
function CardComponent_div_0_div_13_ng_container_2_div_1_ng_container_2_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainerStart(0, 35);
    ɵɵtemplate(1, CardComponent_div_0_div_13_ng_container_2_div_1_ng_container_2_button_1_Template, 2, 1, "button", 42)(2, CardComponent_div_0_div_13_ng_container_2_div_1_ng_container_2_button_2_Template, 2, 1, "button", 42)(3, CardComponent_div_0_div_13_ng_container_2_div_1_ng_container_2_button_3_Template, 2, 2, "button", 42)(4, CardComponent_div_0_div_13_ng_container_2_div_1_ng_container_2_button_4_Template, 2, 1, "button", 42);
    ɵɵelementContainerEnd();
  }
  if (rf & 2) {
    const action_r14 = ɵɵnextContext().$implicit;
    ɵɵproperty("ngSwitch", action_r14.event);
    ɵɵadvance();
    ɵɵproperty("ngSwitchCase", "delete");
    ɵɵadvance();
    ɵɵproperty("ngSwitchCase", "default");
    ɵɵadvance();
    ɵɵproperty("ngSwitchCase", "send");
    ɵɵadvance();
    ɵɵproperty("ngSwitchCase", "edit");
  }
}
function CardComponent_div_0_div_13_ng_container_2_div_1_cx_generic_link_3_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "cx-generic-link", 44);
    ɵɵpipe(1, "cxTranslate");
    ɵɵtext(2);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    let tmp_9_0;
    const action_r14 = ɵɵnextContext().$implicit;
    ɵɵproperty("url", action_r14.link)("ariaLabel", action_r14.ariaLabel ? ɵɵpipeBind1(1, 4, action_r14.ariaLabel) : void 0)("target", (tmp_9_0 = action_r14.target) !== null && tmp_9_0 !== void 0 ? tmp_9_0 : null);
    ɵɵadvance(2);
    ɵɵtextInterpolate(action_r14.name);
  }
}
function CardComponent_div_0_div_13_ng_container_2_div_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div", 40)(1, "div");
    ɵɵtemplate(2, CardComponent_div_0_div_13_ng_container_2_div_1_ng_container_2_Template, 5, 5, "ng-container", 33)(3, CardComponent_div_0_div_13_ng_container_2_div_1_cx_generic_link_3_Template, 3, 6, "cx-generic-link", 41);
    ɵɵelementEnd()();
  }
  if (rf & 2) {
    const action_r14 = ctx.$implicit;
    const ctx_r0 = ɵɵnextContext(4);
    ɵɵadvance(2);
    ɵɵproperty("ngIf", ctx_r0.isCardAction(action_r14));
    ɵɵadvance();
    ɵɵproperty("ngIf", ctx_r0.isCardLinkAction(action_r14));
  }
}
function CardComponent_div_0_div_13_ng_container_2_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainerStart(0);
    ɵɵtemplate(1, CardComponent_div_0_div_13_ng_container_2_div_1_Template, 4, 2, "div", 39);
    ɵɵelementContainerEnd();
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext(3);
    ɵɵadvance();
    ɵɵproperty("ngForOf", ctx_r0.content.actions);
  }
}
function CardComponent_div_0_div_13_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div", 31);
    ɵɵtemplate(1, CardComponent_div_0_div_13_ng_container_1_Template, 2, 1, "ng-container", 32)(2, CardComponent_div_0_div_13_ng_container_2_Template, 2, 1, "ng-container", 32);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    ɵɵadvance();
    ɵɵproperty("cxFeature", "!a11yImproveButtonsInCardComponent");
    ɵɵadvance();
    ɵɵproperty("cxFeature", "a11yImproveButtonsInCardComponent");
  }
}
function CardComponent_div_0_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div", 2);
    ɵɵtemplate(1, CardComponent_div_0_div_1_Template, 2, 2, "div", 3);
    ɵɵelementStart(2, "div", 4);
    ɵɵtemplate(3, CardComponent_div_0_div_3_Template, 2, 1, "div", 5)(4, CardComponent_div_0_span_4_Template, 2, 2, "span", 6);
    ɵɵelementStart(5, "div", 7)(6, "div", 8);
    ɵɵtemplate(7, CardComponent_div_0_div_7_Template, 2, 1, "div", 9)(8, CardComponent_div_0_div_8_Template, 2, 7, "div", 10)(9, CardComponent_div_0_div_9_Template, 4, 2, "div", 11);
    ɵɵprojection(10);
    ɵɵelementEnd();
    ɵɵtemplate(11, CardComponent_div_0_div_11_Template, 4, 7, "div", 12);
    ɵɵelementEnd();
    ɵɵtemplate(12, CardComponent_div_0_div_12_Template, 3, 2, "div", 13)(13, CardComponent_div_0_div_13_Template, 3, 2, "div", 14);
    ɵɵelementEnd()();
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    ɵɵclassProp("cx-card-border", ctx_r0.border)("cx-card-fit-to-container", ctx_r0.fitToContainer);
    ɵɵproperty("tabindex", ctx_r0.border ? 0 : -1);
    ɵɵattribute("role", ctx_r0.role)("aria-labelledby", ctx_r0.isHeaderVisible ? ctx_r0.getLabel("content-header") : null)("aria-describedby", ctx_r0.ariaDescribedBy);
    ɵɵadvance();
    ɵɵproperty("ngIf", ctx_r0.isHeaderVisible);
    ɵɵadvance();
    ɵɵclassProp("cx-card-delete", ctx_r0.editMode);
    ɵɵadvance();
    ɵɵproperty("ngIf", ctx_r0.editMode);
    ɵɵadvance();
    ɵɵproperty("ngIf", ctx_r0.content.title);
    ɵɵadvance();
    ɵɵproperty("id", ctx_r0.getLabel("cx-card-container"));
    ɵɵadvance(2);
    ɵɵproperty("ngIf", ctx_r0.content.textBold);
    ɵɵadvance();
    ɵɵproperty("ngForOf", ctx_r0.content.text);
    ɵɵadvance();
    ɵɵproperty("ngForOf", ctx_r0.content.paragraphs)("ngForTrackBy", ctx_r0.trackByIndex);
    ɵɵadvance(2);
    ɵɵproperty("ngIf", ctx_r0.content.img);
    ɵɵadvance();
    ɵɵproperty("ngIf", ctx_r0.editMode);
    ɵɵadvance();
    ɵɵproperty("ngIf", ctx_r0.content.actions && !ctx_r0.editMode);
  }
}
function CardComponent_ng_template_1_div_0_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div", 47);
    ɵɵtext(1);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r17 = ɵɵnextContext();
    const content_r19 = ctx_r17.content;
    const class_r20 = ctx_r17.class;
    const text_r21 = ctx_r17.text;
    ɵɵclassMap(class_r20);
    ɵɵproperty("ngClass", content_r19.customClass);
    ɵɵadvance();
    ɵɵtextInterpolate1(" ", text_r21, " ");
  }
}
function CardComponent_ng_template_1_cx_truncate_text_popover_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "cx-truncate-text-popover", 48);
  }
  if (rf & 2) {
    const ctx_r17 = ɵɵnextContext();
    const content_r19 = ctx_r17.content;
    const charactersLimit_r22 = ctx_r17.charactersLimit;
    const class_r20 = ctx_r17.class;
    const text_r21 = ctx_r17.text;
    ɵɵproperty("charactersLimit", charactersLimit_r22)("content", text_r21)("customClass", content_r19.customClass || class_r20);
  }
}
function CardComponent_ng_template_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵtemplate(0, CardComponent_ng_template_1_div_0_Template, 2, 4, "div", 45)(1, CardComponent_ng_template_1_cx_truncate_text_popover_1_Template, 1, 3, "cx-truncate-text-popover", 46);
  }
  if (rf & 2) {
    const truncateText_r23 = ctx.truncateText;
    ɵɵproperty("ngIf", !truncateText_r23);
    ɵɵadvance();
    ɵɵproperty("ngIf", truncateText_r23);
  }
}
function MediaComponent_ng_container_0_ng_container_1_picture_1_source_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "source", 7);
  }
  if (rf & 2) {
    const source_r2 = ctx.$implicit;
    ɵɵproperty("media", source_r2.media)("srcset", source_r2.srcset);
    ɵɵattribute("width", source_r2.width)("height", source_r2.height);
  }
}
function MediaComponent_ng_container_0_ng_container_1_picture_1_Template(rf, ctx) {
  if (rf & 1) {
    const _r1 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "picture");
    ɵɵtemplate(1, MediaComponent_ng_container_0_ng_container_1_picture_1_source_1_Template, 1, 4, "source", 5);
    ɵɵelementStart(2, "img", 6);
    ɵɵlistener("load", function MediaComponent_ng_container_0_ng_container_1_picture_1_Template_img_load_2_listener() {
      ɵɵrestoreView(_r1);
      const ctx_r2 = ɵɵnextContext(3);
      return ɵɵresetView(ctx_r2.loadHandler());
    })("error", function MediaComponent_ng_container_0_ng_container_1_picture_1_Template_img_error_2_listener() {
      ɵɵrestoreView(_r1);
      const ctx_r2 = ɵɵnextContext(3);
      return ɵɵresetView(ctx_r2.errorHandler());
    });
    ɵɵelementEnd()();
  }
  if (rf & 2) {
    const ctx_r2 = ɵɵnextContext(3);
    ɵɵadvance();
    ɵɵproperty("ngForOf", ctx_r2.media.sources)("ngForTrackBy", ctx_r2.trackByMedia);
    ɵɵadvance();
    ɵɵproperty("loading", ctx_r2.effectiveLoadingStrategy)("alt", ctx_r2.media.alt)("title", ctx_r2.media.alt)("src", ctx_r2.media.src, ɵɵsanitizeUrl);
    ɵɵattribute("fetchpriority", ctx_r2.fetchPriority)("role", ctx_r2.media.role)("width", ctx_r2.media.width)("height", ctx_r2.media.height);
  }
}
function MediaComponent_ng_container_0_ng_container_1_ng_template_2_img_0_Template(rf, ctx) {
  if (rf & 1) {
    const _r4 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "img", 9);
    ɵɵlistener("load", function MediaComponent_ng_container_0_ng_container_1_ng_template_2_img_0_Template_img_load_0_listener() {
      ɵɵrestoreView(_r4);
      const ctx_r2 = ɵɵnextContext(4);
      return ɵɵresetView(ctx_r2.loadHandler());
    })("error", function MediaComponent_ng_container_0_ng_container_1_ng_template_2_img_0_Template_img_error_0_listener() {
      ɵɵrestoreView(_r4);
      const ctx_r2 = ɵɵnextContext(4);
      return ɵɵresetView(ctx_r2.errorHandler());
    });
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r2 = ɵɵnextContext(4);
    ɵɵproperty("loading", ctx_r2.effectiveLoadingStrategy)("alt", ctx_r2.media.alt)("title", ctx_r2.media.alt)("src", ctx_r2.media.src, ɵɵsanitizeUrl)("srcset", ctx_r2.media.srcset || ctx_r2.media.src);
    ɵɵattribute("fetchpriority", ctx_r2.fetchPriority)("role", ctx_r2.media.role)("width", ctx_r2.media.width)("height", ctx_r2.media.height)("sizes", ctx_r2.sizesForImgElement);
  }
}
function MediaComponent_ng_container_0_ng_container_1_ng_template_2_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵtemplate(0, MediaComponent_ng_container_0_ng_container_1_ng_template_2_img_0_Template, 1, 10, "img", 8);
  }
  if (rf & 2) {
    const ctx_r2 = ɵɵnextContext(3);
    ɵɵproperty("ngIf", ctx_r2.media.src);
  }
}
function MediaComponent_ng_container_0_ng_container_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainerStart(0);
    ɵɵtemplate(1, MediaComponent_ng_container_0_ng_container_1_picture_1_Template, 3, 10, "picture", 4)(2, MediaComponent_ng_container_0_ng_container_1_ng_template_2_Template, 1, 1, "ng-template", null, 0, ɵɵtemplateRefExtractor);
    ɵɵelementContainerEnd();
  }
  if (rf & 2) {
    const imageElementTmp_r5 = ɵɵreference(3);
    const ctx_r2 = ɵɵnextContext(2);
    ɵɵadvance();
    ɵɵproperty("ngIf", !!(ctx_r2.media == null ? null : ctx_r2.media.sources == null ? null : ctx_r2.media.sources.length) && ctx_r2.elementType !== "img")("ngIfElse", imageElementTmp_r5);
  }
}
function MediaComponent_ng_container_0_ng_container_2_picture_1_source_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "source", 11);
  }
  if (rf & 2) {
    const source_r7 = ctx.$implicit;
    ɵɵproperty("srcset", source_r7.srcset)("media", source_r7.media);
  }
}
function MediaComponent_ng_container_0_ng_container_2_picture_1_Template(rf, ctx) {
  if (rf & 1) {
    const _r6 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "picture");
    ɵɵtemplate(1, MediaComponent_ng_container_0_ng_container_2_picture_1_source_1_Template, 1, 2, "source", 10);
    ɵɵpipe(2, "cxMediaSources");
    ɵɵelementStart(3, "img", 6);
    ɵɵlistener("load", function MediaComponent_ng_container_0_ng_container_2_picture_1_Template_img_load_3_listener() {
      ɵɵrestoreView(_r6);
      const ctx_r2 = ɵɵnextContext(3);
      return ɵɵresetView(ctx_r2.loadHandler());
    })("error", function MediaComponent_ng_container_0_ng_container_2_picture_1_Template_img_error_3_listener() {
      ɵɵrestoreView(_r6);
      const ctx_r2 = ɵɵnextContext(3);
      return ɵɵresetView(ctx_r2.errorHandler());
    });
    ɵɵelementEnd()();
  }
  if (rf & 2) {
    const ctx_r2 = ɵɵnextContext(3);
    ɵɵadvance();
    ɵɵproperty("ngForOf", ɵɵpipeBind1(2, 8, ctx_r2.media.srcset))("ngForTrackBy", ctx_r2.trackByMedia);
    ɵɵadvance(2);
    ɵɵproperty("loading", ctx_r2.effectiveLoadingStrategy)("alt", ctx_r2.media.alt)("title", ctx_r2.media.alt)("src", ctx_r2.media.src, ɵɵsanitizeUrl);
    ɵɵattribute("fetchpriority", ctx_r2.fetchPriority)("role", ctx_r2.media.role);
  }
}
function MediaComponent_ng_container_0_ng_container_2_ng_template_2_img_0_Template(rf, ctx) {
  if (rf & 1) {
    const _r8 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "img", 9);
    ɵɵlistener("load", function MediaComponent_ng_container_0_ng_container_2_ng_template_2_img_0_Template_img_load_0_listener() {
      ɵɵrestoreView(_r8);
      const ctx_r2 = ɵɵnextContext(4);
      return ɵɵresetView(ctx_r2.loadHandler());
    })("error", function MediaComponent_ng_container_0_ng_container_2_ng_template_2_img_0_Template_img_error_0_listener() {
      ɵɵrestoreView(_r8);
      const ctx_r2 = ɵɵnextContext(4);
      return ɵɵresetView(ctx_r2.errorHandler());
    });
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r2 = ɵɵnextContext(4);
    ɵɵproperty("loading", ctx_r2.effectiveLoadingStrategy)("alt", ctx_r2.media.alt)("title", ctx_r2.media.alt)("src", ctx_r2.media.src, ɵɵsanitizeUrl)("srcset", (ctx_r2.media == null ? null : ctx_r2.media.srcset) || ctx_r2.media.src);
    ɵɵattribute("fetchpriority", ctx_r2.fetchPriority)("role", ctx_r2.media.role);
  }
}
function MediaComponent_ng_container_0_ng_container_2_ng_template_2_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵtemplate(0, MediaComponent_ng_container_0_ng_container_2_ng_template_2_img_0_Template, 1, 7, "img", 8);
  }
  if (rf & 2) {
    const ctx_r2 = ɵɵnextContext(3);
    ɵɵproperty("ngIf", ctx_r2.media.src);
  }
}
function MediaComponent_ng_container_0_ng_container_2_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainerStart(0);
    ɵɵtemplate(1, MediaComponent_ng_container_0_ng_container_2_picture_1_Template, 4, 10, "picture", 4)(2, MediaComponent_ng_container_0_ng_container_2_ng_template_2_Template, 1, 1, "ng-template", null, 1, ɵɵtemplateRefExtractor);
    ɵɵelementContainerEnd();
  }
  if (rf & 2) {
    const legacyImgTmp_r9 = ɵɵreference(3);
    const ctx_r2 = ɵɵnextContext(2);
    ɵɵadvance();
    ɵɵproperty("ngIf", ctx_r2.media.srcset && !ctx_r2.isLegacy)("ngIfElse", legacyImgTmp_r9);
  }
}
function MediaComponent_ng_container_0_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainerStart(0);
    ɵɵtemplate(1, MediaComponent_ng_container_0_ng_container_1_Template, 4, 2, "ng-container", 3)(2, MediaComponent_ng_container_0_ng_container_2_Template, 4, 2, "ng-container", 3);
    ɵɵelementContainerEnd();
  }
  if (rf & 2) {
    ɵɵadvance();
    ɵɵproperty("cxFeature", "useExtendedMediaComponentConfiguration");
    ɵɵadvance();
    ɵɵproperty("cxFeature", "!useExtendedMediaComponentConfiguration");
  }
}
function CarouselScrollingComponent_ng_container_0_h2_2_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "h2");
    ɵɵtext(1);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext(2);
    ɵɵadvance();
    ɵɵtextInterpolate(ctx_r1.title);
  }
}
function CarouselScrollingComponent_ng_container_0_ng_container_14_div_1_ng_container_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainer(0);
  }
}
function CarouselScrollingComponent_ng_container_0_ng_container_14_div_1_Template(rf, ctx) {
  if (rf & 1) {
    const _r4 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "div", 16);
    ɵɵlistener("keydown", function CarouselScrollingComponent_ng_container_0_ng_container_14_div_1_Template_div_keydown_0_listener($event) {
      ɵɵrestoreView(_r4);
      const ctx_r1 = ɵɵnextContext(3);
      return ɵɵresetView(ctx_r1.onItemKeydown($event));
    })("focusin", function CarouselScrollingComponent_ng_container_0_ng_container_14_div_1_Template_div_focusin_0_listener($event) {
      ɵɵrestoreView(_r4);
      const ctx_r1 = ɵɵnextContext(3);
      return ɵɵresetView(ctx_r1.onItemFocusIn($event));
    });
    ɵɵtemplate(1, CarouselScrollingComponent_ng_container_0_ng_container_14_div_1_ng_container_1_Template, 1, 0, "ng-container", 17);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const data_r5 = ctx.ngIf;
    const i_r6 = ɵɵnextContext().index;
    const ctx_r1 = ɵɵnextContext(2);
    ɵɵadvance();
    ɵɵproperty("ngTemplateOutlet", ctx_r1.template)("ngTemplateOutletContext", ɵɵpureFunction2(2, _c92, data_r5, i_r6));
  }
}
function CarouselScrollingComponent_ng_container_0_ng_container_14_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainerStart(0);
    ɵɵtemplate(1, CarouselScrollingComponent_ng_container_0_ng_container_14_div_1_Template, 2, 5, "div", 15);
    ɵɵpipe(2, "async");
    ɵɵelementContainerEnd();
  }
  if (rf & 2) {
    const item_r7 = ctx.$implicit;
    ɵɵadvance();
    ɵɵproperty("ngIf", ɵɵpipeBind1(2, 1, item_r7));
  }
}
function CarouselScrollingComponent_ng_container_0_Template(rf, ctx) {
  if (rf & 1) {
    const _r1 = ɵɵgetCurrentView();
    ɵɵelementContainerStart(0, 5, 0);
    ɵɵtemplate(2, CarouselScrollingComponent_ng_container_0_h2_2_Template, 2, 1, "h2", 6);
    ɵɵelementStart(3, "div", 7)(4, "button", 8);
    ɵɵpipe(5, "async");
    ɵɵpipe(6, "async");
    ɵɵpipe(7, "cxTranslate");
    ɵɵpipe(8, "cxTranslate");
    ɵɵlistener("click", function CarouselScrollingComponent_ng_container_0_Template_button_click_4_listener($event) {
      ɵɵrestoreView(_r1);
      const cxHorizontalScrollingPosition_r3 = ɵɵreference(1);
      $event.stopPropagation();
      return ɵɵresetView(cxHorizontalScrollingPosition_r3.scrollBackward());
    });
    ɵɵelement(9, "cx-icon", 9);
    ɵɵelementEnd();
    ɵɵelementStart(10, "div", 10, 1);
    ɵɵelement(12, "span", 11, 2);
    ɵɵtemplate(14, CarouselScrollingComponent_ng_container_0_ng_container_14_Template, 3, 3, "ng-container", 12);
    ɵɵelement(15, "span", 13, 3);
    ɵɵelementEnd();
    ɵɵelementStart(17, "button", 14);
    ɵɵpipe(18, "async");
    ɵɵpipe(19, "async");
    ɵɵpipe(20, "cxTranslate");
    ɵɵpipe(21, "cxTranslate");
    ɵɵlistener("click", function CarouselScrollingComponent_ng_container_0_Template_button_click_17_listener($event) {
      ɵɵrestoreView(_r1);
      const cxHorizontalScrollingPosition_r3 = ɵɵreference(1);
      $event.stopPropagation();
      return ɵɵresetView(cxHorizontalScrollingPosition_r3.scrollForward());
    });
    ɵɵelement(22, "cx-icon", 9);
    ɵɵelementEnd()();
    ɵɵelementContainerEnd();
  }
  if (rf & 2) {
    const cxHorizontalScrollingPosition_r3 = ɵɵreference(1);
    const scrollingArea_r8 = ɵɵreference(11);
    const scrollingAreaStart_r9 = ɵɵreference(13);
    const scrollingAreaEnd_r10 = ɵɵreference(16);
    const ctx_r1 = ɵɵnextContext();
    ɵɵproperty("scrollingArea", scrollingArea_r8)("scrollingAreaStart", scrollingAreaStart_r9)("scrollingAreaEnd", scrollingAreaEnd_r10);
    ɵɵadvance(2);
    ɵɵproperty("ngIf", ctx_r1.title);
    ɵɵadvance(2);
    ɵɵclassProp("scroll-possible", ɵɵpipeBind1(5, 18, cxHorizontalScrollingPosition_r3.isScrollNeeded$));
    ɵɵattribute("aria-disabled", ɵɵpipeBind1(6, 20, cxHorizontalScrollingPosition_r3.isScrollStart$))("aria-label", ɵɵpipeBind1(7, 22, "carousel.scrollBackward"))("title", ɵɵpipeBind1(8, 24, "carousel.scrollBackward"));
    ɵɵadvance(5);
    ɵɵproperty("type", ctx_r1.previousIcon);
    ɵɵadvance(5);
    ɵɵproperty("ngForOf", ctx_r1.items)("ngForTrackBy", ctx_r1.trackByFn);
    ɵɵadvance(3);
    ɵɵclassProp("scroll-possible", ɵɵpipeBind1(18, 26, cxHorizontalScrollingPosition_r3.isScrollNeeded$));
    ɵɵattribute("aria-disabled", ɵɵpipeBind1(19, 28, cxHorizontalScrollingPosition_r3.isScrollEnd$))("aria-label", ɵɵpipeBind1(20, 30, "carousel.scrollForward"))("title", ɵɵpipeBind1(21, 32, "carousel.scrollForward"));
    ɵɵadvance(5);
    ɵɵproperty("type", ctx_r1.nextIcon);
  }
}
var _c20 = (a0, a1) => [a0, a1];
function FormErrorsComponent_ng_container_0_ng_container_1_p_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "p");
    ɵɵtext(1);
    ɵɵpipe(2, "cxTranslate");
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const error_r1 = ctx.$implicit;
    const ctx_r1 = ɵɵnextContext(3);
    ɵɵadvance();
    ɵɵtextInterpolate1(" ", ɵɵpipeBind2(2, 1, ɵɵpureFunction2(4, _c20, ctx_r1.prefix + "." + error_r1[0], ctx_r1.fallbackPrefix + "." + error_r1[0]), ctx_r1.getTranslationParams(error_r1[1])), " ");
  }
}
function FormErrorsComponent_ng_container_0_ng_container_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainerStart(0);
    ɵɵtemplate(1, FormErrorsComponent_ng_container_0_ng_container_1_p_1_Template, 3, 7, "p", 1);
    ɵɵelementContainerEnd();
  }
  if (rf & 2) {
    const errorDetails_r3 = ɵɵnextContext().ngIf;
    ɵɵadvance();
    ɵɵproperty("ngForOf", errorDetails_r3);
  }
}
function FormErrorsComponent_ng_container_0_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainerStart(0);
    ɵɵtemplate(1, FormErrorsComponent_ng_container_0_ng_container_1_Template, 2, 1, "ng-container", 0);
    ɵɵelementContainerEnd();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext();
    ɵɵadvance();
    ɵɵproperty("ngIf", !ctx_r1.hidden);
  }
}
var _c21 = (a0, a1, a2) => ({
  label: a0,
  max: a1,
  min: a2
});
var _c222 = ["fileInput"];
function FileUploadComponent_button_2_Template(rf, ctx) {
  if (rf & 1) {
    const _r2 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "button", 5);
    ɵɵpipe(1, "cxTranslate");
    ɵɵlistener("click", function FileUploadComponent_button_2_Template_button_click_0_listener() {
      ɵɵrestoreView(_r2);
      ɵɵnextContext();
      const fileInput_r3 = ɵɵreference(1);
      return ɵɵresetView(fileInput_r3.click());
    });
    ɵɵprojection(2);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    ɵɵattribute("aria-label", ɵɵpipeBind1(1, 1, "common.selectFile"));
  }
}
function FileUploadComponent_button_3_ng_container_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainer(0);
  }
}
function FileUploadComponent_button_3_Template(rf, ctx) {
  if (rf & 1) {
    const _r4 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "button", 6);
    ɵɵlistener("click", function FileUploadComponent_button_3_Template_button_click_0_listener() {
      ɵɵrestoreView(_r4);
      ɵɵnextContext();
      const fileInput_r3 = ɵɵreference(1);
      return ɵɵresetView(fileInput_r3.click());
    });
    ɵɵtemplate(1, FileUploadComponent_button_3_ng_container_1_Template, 1, 0, "ng-container", 7);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r4 = ɵɵnextContext();
    ɵɵadvance();
    ɵɵproperty("ngTemplateOutlet", ctx_r4.customButton);
  }
}
function FileUploadComponent_p_4_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "p");
    ɵɵtext(1);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const file_r6 = ctx.$implicit;
    ɵɵadvance();
    ɵɵtextInterpolate1(" ", file_r6 == null ? null : file_r6.name, "\n");
  }
}
var _c23 = ["qty"];
var _c24 = () => ({
  key: "qty"
});
var _c25 = () => ({
  key: "decrement"
});
var _c26 = () => ({
  key: "increment"
});
function ItemCounterComponent_button_0_Template(rf, ctx) {
  if (rf & 1) {
    const _r2 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "button", 6);
    ɵɵpipe(1, "cxTranslate");
    ɵɵpipe(2, "cxTranslate");
    ɵɵlistener("click", function ItemCounterComponent_button_0_Template_button_click_0_listener() {
      ɵɵrestoreView(_r2);
      const ctx_r2 = ɵɵnextContext();
      return ɵɵresetView(ctx_r2.decrement());
    });
    ɵɵtext(3, " -\n");
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r2 = ɵɵnextContext();
    ɵɵpropertyInterpolate("title", ɵɵpipeBind1(1, 5, "itemCounter.removeOne"));
    ɵɵproperty("tabindex", ctx_r2.control.disabled || ctx_r2.control.value <= ctx_r2.min ? -1 : 0)("cxFocus", ɵɵpureFunction0(9, _c25));
    ɵɵattribute("aria-disabled", ctx_r2.control.disabled || ctx_r2.control.value <= ctx_r2.min)("aria-label", ɵɵpipeBind1(2, 7, "itemCounter.removeOne"));
  }
}
function ItemCounterComponent_button_1_Template(rf, ctx) {
  if (rf & 1) {
    const _r4 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "button", 7);
    ɵɵpipe(1, "cxTranslate");
    ɵɵpipe(2, "cxTranslate");
    ɵɵlistener("click", function ItemCounterComponent_button_1_Template_button_click_0_listener() {
      ɵɵrestoreView(_r4);
      const ctx_r2 = ɵɵnextContext();
      return ɵɵresetView(ctx_r2.decrement());
    });
    ɵɵtext(3, " -\n");
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r2 = ɵɵnextContext();
    ɵɵpropertyInterpolate("title", ɵɵpipeBind1(1, 5, "itemCounter.removeOne"));
    ɵɵproperty("disabled", ctx_r2.control.disabled || ctx_r2.control.value <= ctx_r2.min)("tabindex", ctx_r2.control.disabled || ctx_r2.control.value <= ctx_r2.min ? -1 : 0)("cxFocus", ɵɵpureFunction0(9, _c25));
    ɵɵattribute("aria-label", ɵɵpipeBind1(2, 7, "itemCounter.removeOne"));
  }
}
function ItemCounterComponent_button_5_Template(rf, ctx) {
  if (rf & 1) {
    const _r5 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "button", 8);
    ɵɵpipe(1, "cxTranslate");
    ɵɵpipe(2, "cxTranslate");
    ɵɵlistener("click", function ItemCounterComponent_button_5_Template_button_click_0_listener() {
      ɵɵrestoreView(_r5);
      const ctx_r2 = ɵɵnextContext();
      return ɵɵresetView(ctx_r2.increment());
    });
    ɵɵtext(3, " +\n");
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r2 = ɵɵnextContext();
    ɵɵpropertyInterpolate("title", ɵɵpipeBind1(1, 4, "itemCounter.addOneMore"));
    ɵɵproperty("cxFocus", ɵɵpureFunction0(8, _c26));
    ɵɵattribute("aria-disabled", ctx_r2.control.disabled || ctx_r2.control.value >= ctx_r2.max)("aria-label", ɵɵpipeBind1(2, 6, "itemCounter.addOneMore"));
  }
}
function ItemCounterComponent_button_6_Template(rf, ctx) {
  if (rf & 1) {
    const _r6 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "button", 9);
    ɵɵpipe(1, "cxTranslate");
    ɵɵpipe(2, "cxTranslate");
    ɵɵlistener("click", function ItemCounterComponent_button_6_Template_button_click_0_listener() {
      ɵɵrestoreView(_r6);
      const ctx_r2 = ɵɵnextContext();
      return ɵɵresetView(ctx_r2.increment());
    });
    ɵɵtext(3, " +\n");
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r2 = ɵɵnextContext();
    ɵɵpropertyInterpolate("title", ɵɵpipeBind1(1, 4, "itemCounter.addOneMore"));
    ɵɵproperty("disabled", ctx_r2.control.disabled || ctx_r2.control.value >= ctx_r2.max)("cxFocus", ɵɵpureFunction0(8, _c26));
    ɵɵattribute("aria-label", ɵɵpipeBind1(2, 6, "itemCounter.addOneMore"));
  }
}
function GlobalMessageComponent_div_0_div_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div", 6)(1, "span");
    ɵɵtext(2);
    ɵɵpipe(3, "cxTranslate");
    ɵɵelementEnd()();
  }
  if (rf & 2) {
    const assistiveMsg_r1 = ctx.$implicit;
    ɵɵadvance(2);
    ɵɵtextInterpolate(ɵɵpipeBind1(3, 1, assistiveMsg_r1));
  }
}
function GlobalMessageComponent_div_0_div_2_Template(rf, ctx) {
  if (rf & 1) {
    const _r2 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "div", 7)(1, "span", 8);
    ɵɵelement(2, "cx-icon", 9);
    ɵɵelementEnd();
    ɵɵelementStart(3, "span");
    ɵɵtext(4);
    ɵɵpipe(5, "cxTranslate");
    ɵɵelementEnd();
    ɵɵelementStart(6, "button", 10);
    ɵɵpipe(7, "cxTranslate");
    ɵɵlistener("click", function GlobalMessageComponent_div_0_div_2_Template_button_click_6_listener() {
      const i_r3 = ɵɵrestoreView(_r2).index;
      const ctx_r3 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r3.clear(ctx_r3.messageType.MSG_TYPE_CONFIRMATION, i_r3));
    });
    ɵɵelement(8, "cx-icon", 9);
    ɵɵelementEnd()();
  }
  if (rf & 2) {
    const confMsg_r5 = ctx.$implicit;
    const ctx_r3 = ɵɵnextContext(2);
    ɵɵadvance(2);
    ɵɵproperty("type", ctx_r3.iconTypes.SUCCESS);
    ɵɵadvance(2);
    ɵɵtextInterpolate(ɵɵpipeBind1(5, 4, confMsg_r5));
    ɵɵadvance(2);
    ɵɵpropertyInterpolate("title", ɵɵpipeBind1(7, 6, "common.close"));
    ɵɵadvance(2);
    ɵɵproperty("type", ctx_r3.iconTypes.CLOSE);
  }
}
function GlobalMessageComponent_div_0_div_3_Template(rf, ctx) {
  if (rf & 1) {
    const _r6 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "div", 11)(1, "span", 8);
    ɵɵelement(2, "cx-icon", 9);
    ɵɵelementEnd();
    ɵɵelementStart(3, "span");
    ɵɵtext(4);
    ɵɵpipe(5, "cxTranslate");
    ɵɵelementEnd();
    ɵɵelementStart(6, "button", 10);
    ɵɵpipe(7, "cxTranslate");
    ɵɵlistener("click", function GlobalMessageComponent_div_0_div_3_Template_button_click_6_listener() {
      const i_r7 = ɵɵrestoreView(_r6).index;
      const ctx_r3 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r3.clear(ctx_r3.messageType.MSG_TYPE_INFO, i_r7));
    });
    ɵɵelement(8, "cx-icon", 9);
    ɵɵelementEnd()();
  }
  if (rf & 2) {
    const infoMsg_r8 = ctx.$implicit;
    const ctx_r3 = ɵɵnextContext(2);
    ɵɵadvance(2);
    ɵɵproperty("type", ctx_r3.iconTypes.INFO);
    ɵɵadvance(2);
    ɵɵtextInterpolate(ɵɵpipeBind1(5, 4, infoMsg_r8));
    ɵɵadvance(2);
    ɵɵpropertyInterpolate("title", ɵɵpipeBind1(7, 6, "common.close"));
    ɵɵadvance(2);
    ɵɵproperty("type", ctx_r3.iconTypes.CLOSE);
  }
}
function GlobalMessageComponent_div_0_div_4_Template(rf, ctx) {
  if (rf & 1) {
    const _r9 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "div", 12)(1, "span", 8);
    ɵɵelement(2, "cx-icon", 9);
    ɵɵelementEnd();
    ɵɵelementStart(3, "span");
    ɵɵtext(4);
    ɵɵpipe(5, "cxTranslate");
    ɵɵelementEnd();
    ɵɵelementStart(6, "button", 10);
    ɵɵpipe(7, "cxTranslate");
    ɵɵlistener("click", function GlobalMessageComponent_div_0_div_4_Template_button_click_6_listener() {
      const i_r10 = ɵɵrestoreView(_r9).index;
      const ctx_r3 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r3.clear(ctx_r3.messageType.MSG_TYPE_WARNING, i_r10));
    });
    ɵɵelement(8, "cx-icon", 9);
    ɵɵelementEnd()();
  }
  if (rf & 2) {
    const infoMsg_r11 = ctx.$implicit;
    const ctx_r3 = ɵɵnextContext(2);
    ɵɵadvance(2);
    ɵɵproperty("type", ctx_r3.iconTypes.WARNING);
    ɵɵadvance(2);
    ɵɵtextInterpolate(ɵɵpipeBind1(5, 4, infoMsg_r11));
    ɵɵadvance(2);
    ɵɵpropertyInterpolate("title", ɵɵpipeBind1(7, 6, "common.close"));
    ɵɵadvance(2);
    ɵɵproperty("type", ctx_r3.iconTypes.CLOSE);
  }
}
function GlobalMessageComponent_div_0_div_5_Template(rf, ctx) {
  if (rf & 1) {
    const _r12 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "div", 13)(1, "span", 8);
    ɵɵelement(2, "cx-icon", 9);
    ɵɵelementEnd();
    ɵɵelementStart(3, "span");
    ɵɵtext(4);
    ɵɵpipe(5, "cxTranslate");
    ɵɵelementEnd();
    ɵɵelementStart(6, "button", 10);
    ɵɵpipe(7, "cxTranslate");
    ɵɵlistener("click", function GlobalMessageComponent_div_0_div_5_Template_button_click_6_listener() {
      const i_r13 = ɵɵrestoreView(_r12).index;
      const ctx_r3 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r3.clear(ctx_r3.messageType.MSG_TYPE_ERROR, i_r13));
    });
    ɵɵelement(8, "cx-icon", 9);
    ɵɵelementEnd()();
  }
  if (rf & 2) {
    const errorMsg_r14 = ctx.$implicit;
    const ctx_r3 = ɵɵnextContext(2);
    ɵɵadvance(2);
    ɵɵproperty("type", ctx_r3.iconTypes.ERROR);
    ɵɵadvance(2);
    ɵɵtextInterpolate(ɵɵpipeBind1(5, 4, errorMsg_r14));
    ɵɵadvance(2);
    ɵɵpropertyInterpolate("title", ɵɵpipeBind1(7, 6, "common.close"));
    ɵɵadvance(2);
    ɵɵproperty("type", ctx_r3.iconTypes.CLOSE);
  }
}
function GlobalMessageComponent_div_0_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div");
    ɵɵtemplate(1, GlobalMessageComponent_div_0_div_1_Template, 4, 3, "div", 1)(2, GlobalMessageComponent_div_0_div_2_Template, 9, 8, "div", 2)(3, GlobalMessageComponent_div_0_div_3_Template, 9, 8, "div", 3)(4, GlobalMessageComponent_div_0_div_4_Template, 9, 8, "div", 4)(5, GlobalMessageComponent_div_0_div_5_Template, 9, 8, "div", 5);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const messages_r15 = ctx.ngIf;
    const ctx_r3 = ɵɵnextContext();
    ɵɵadvance();
    ɵɵproperty("ngForOf", messages_r15[ctx_r3.messageType.MSG_TYPE_ASSISTIVE]);
    ɵɵadvance();
    ɵɵproperty("ngForOf", messages_r15[ctx_r3.messageType.MSG_TYPE_CONFIRMATION]);
    ɵɵadvance();
    ɵɵproperty("ngForOf", messages_r15[ctx_r3.messageType.MSG_TYPE_INFO]);
    ɵɵadvance();
    ɵɵproperty("ngForOf", messages_r15[ctx_r3.messageType.MSG_TYPE_WARNING]);
    ɵɵadvance();
    ɵɵproperty("ngForOf", messages_r15[ctx_r3.messageType.MSG_TYPE_ERROR]);
  }
}
var _c27 = () => ({
  disableMouseFocus: true
});
function StorefrontComponent_ng_template_0_ng_template_0_Template(rf, ctx) {
  if (rf & 1) {
    const _r1 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "header", 4);
    ɵɵpipe(1, "async");
    ɵɵlistener("keydown.escape", function StorefrontComponent_ng_template_0_ng_template_0_Template_header_keydown_escape_0_listener() {
      ɵɵrestoreView(_r1);
      const ctx_r1 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r1.collapseMenu());
    })("click", function StorefrontComponent_ng_template_0_ng_template_0_Template_header_click_0_listener($event) {
      ɵɵrestoreView(_r1);
      const ctx_r1 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r1.collapseMenuIfClickOutside($event));
    });
    ɵɵelement(2, "cx-page-layout", 5)(3, "cx-page-layout", 6);
    ɵɵelementEnd();
    ɵɵelement(4, "cx-page-slot", 7)(5, "cx-global-message", 8);
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext(2);
    ɵɵclassProp("is-expanded", ɵɵpipeBind1(1, 4, ctx_r1.isExpanded$));
    ɵɵproperty("cxFocus", ctx_r1.focusConfig);
    ɵɵadvance(2);
    ɵɵproperty("cxSkipFocus", ctx_r1.skipFocusConfig);
  }
}
function StorefrontComponent_ng_template_0_ng_template_3_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "footer", 9);
    ɵɵelement(1, "cx-page-layout", 10);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    ɵɵproperty("cxFocus", ɵɵpureFunction0(1, _c27));
  }
}
function StorefrontComponent_ng_template_0_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵtemplate(0, StorefrontComponent_ng_template_0_ng_template_0_Template, 6, 6, "ng-template", 1);
    ɵɵelementStart(1, "main", 2);
    ɵɵelement(2, "router-outlet");
    ɵɵelementEnd();
    ɵɵtemplate(3, StorefrontComponent_ng_template_0_ng_template_3_Template, 2, 2, "ng-template", 3);
  }
  if (rf & 2) {
    ɵɵadvance();
    ɵɵproperty("cxFocus", ɵɵpureFunction0(1, _c27));
  }
}
var _c28 = (a0, a1) => ({
  ariaLabel: a0,
  ariaControls: a1
});
function SortingComponent_ng_option_2_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "ng-option", 2);
    ɵɵtext(1);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const sort_r1 = ctx.$implicit;
    const ctx_r1 = ɵɵnextContext();
    ɵɵproperty("value", sort_r1.code);
    ɵɵadvance();
    ɵɵtextInterpolate(sort_r1.name ? sort_r1.name : ctx_r1.sortLabels && sort_r1.code ? ctx_r1.sortLabels[sort_r1.code] : "");
  }
}
var _c29 = (a0) => ({
  key: a0
});
function PaginationComponent_a_0_Template(rf, ctx) {
  if (rf & 1) {
    const _r1 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "a", 1);
    ɵɵpipe(1, "async");
    ɵɵpipe(2, "async");
    ɵɵlistener("click", function PaginationComponent_a_0_Template_a_click_0_listener() {
      const item_r2 = ɵɵrestoreView(_r1).$implicit;
      const ctx_r2 = ɵɵnextContext();
      return ɵɵresetView(ctx_r2.pageChange(item_r2));
    });
    ɵɵtext(3);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const item_r2 = ctx.$implicit;
    const i_r4 = ctx.index;
    const ctx_r2 = ɵɵnextContext();
    ɵɵclassMap(item_r2.type);
    ɵɵclassProp("disabled", ctx_r2.isInactive(item_r2))("current", ctx_r2.isCurrent(item_r2));
    ɵɵproperty("routerLink", ctx_r2.pageRoute)("queryParams", ctx_r2.getQueryParams(item_r2))("tabIndex", ctx_r2.isInactive(item_r2) ? -1 : 0)("cxFocus", ɵɵpureFunction1(19, _c29, ctx_r2.paginationID + i_r4.toString()));
    ɵɵattribute("aria-current", ctx_r2.isCurrent(item_r2))("aria-disabled", ctx_r2.isInactive(item_r2))("aria-label", ɵɵpipeBind1(1, 15, ctx_r2.getAriaLabel$(item_r2)))("title", ɵɵpipeBind1(2, 17, ctx_r2.getAriaLabel$(item_r2)));
    ɵɵadvance(3);
    ɵɵtextInterpolate1(" ", item_r2.label, "\n");
  }
}
function TotalComponent_ng_container_0_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainerStart(0);
    ɵɵelementStart(1, "div");
    ɵɵtext(2);
    ɵɵpipe(3, "cxTranslate");
    ɵɵelementEnd();
    ɵɵelement(4, "div", 1);
    ɵɵelementStart(5, "div");
    ɵɵtext(6);
    ɵɵpipe(7, "cxTranslate");
    ɵɵelementEnd();
    ɵɵelementContainerEnd();
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    ɵɵadvance(2);
    ɵɵtextInterpolate2("", ctx_r0.totalResults, " ", ɵɵpipeBind1(3, 6, "common.results"), "");
    ɵɵadvance(4);
    ɵɵtextInterpolate4(" ", ctx_r0.fromItem, "-", ctx_r0.toItem, " ", ɵɵpipeBind1(7, 8, "common.of"), " ", ctx_r0.totalResults, " ");
  }
}
function ProgressButtonComponent_div_2_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div", 3)(1, "div", 4);
    ɵɵtext(2);
    ɵɵpipe(3, "cxTranslate");
    ɵɵelementEnd()();
  }
  if (rf & 2) {
    ɵɵadvance(2);
    ɵɵtextInterpolate(ɵɵpipeBind1(3, 1, "spinner.loading"));
  }
}
function PromotionsComponent_div_0_ng_container_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainerStart(0);
    ɵɵelementStart(1, "p");
    ɵɵtext(2);
    ɵɵelementEnd();
    ɵɵelementContainerEnd();
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext(2);
    ɵɵadvance(2);
    ɵɵtextInterpolate(ctx_r0.promotions[0].description);
  }
}
function PromotionsComponent_div_0_ng_container_2_li_2_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "li");
    ɵɵtext(1);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const promotion_r2 = ctx.$implicit;
    ɵɵadvance();
    ɵɵtextInterpolate1(" ", promotion_r2.description, " ");
  }
}
function PromotionsComponent_div_0_ng_container_2_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainerStart(0);
    ɵɵelementStart(1, "ul");
    ɵɵtemplate(2, PromotionsComponent_div_0_ng_container_2_li_2_Template, 2, 1, "li", 3);
    ɵɵelementEnd();
    ɵɵelementContainerEnd();
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext(2);
    ɵɵadvance(2);
    ɵɵproperty("ngForOf", ctx_r0.promotions);
  }
}
function PromotionsComponent_div_0_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div", 1);
    ɵɵtemplate(1, PromotionsComponent_div_0_ng_container_1_Template, 3, 1, "ng-container", 2)(2, PromotionsComponent_div_0_ng_container_2_Template, 3, 1, "ng-container", 2);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    ɵɵadvance();
    ɵɵproperty("ngIf", ctx_r0.promotions.length === 1);
    ɵɵadvance();
    ɵɵproperty("ngIf", ctx_r0.promotions.length > 1);
  }
}
function SiteContextSelectorComponent_label_0_option_5_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "option", 4);
    ɵɵpipe(1, "async");
    ɵɵpipe(2, "async");
    ɵɵtext(3);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const item_r3 = ctx.$implicit;
    const i_r4 = ctx.index;
    const items_r5 = ɵɵnextContext().ngIf;
    const ctx_r1 = ɵɵnextContext();
    ɵɵpropertyInterpolate("value", item_r3.isocode);
    ɵɵproperty("selected", ɵɵpipeBind1(1, 4, ctx_r1.activeItem$) === item_r3.isocode);
    ɵɵattribute("aria-label", ɵɵpipeBind1(2, 6, ctx_r1.ariaLabel$(item_r3.label, i_r4, items_r5.length)));
    ɵɵadvance(3);
    ɵɵtextInterpolate1(" ", item_r3.label, " ");
  }
}
function SiteContextSelectorComponent_label_0_Template(rf, ctx) {
  if (rf & 1) {
    const _r1 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "label")(1, "span");
    ɵɵtext(2);
    ɵɵpipe(3, "async");
    ɵɵelementEnd();
    ɵɵelementStart(4, "select", 1);
    ɵɵlistener("change", function SiteContextSelectorComponent_label_0_Template_select_change_4_listener($event) {
      ɵɵrestoreView(_r1);
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.active = $event.target.value);
    });
    ɵɵtemplate(5, SiteContextSelectorComponent_label_0_option_5_Template, 4, 8, "option", 2);
    ɵɵelementEnd();
    ɵɵelement(6, "cx-icon", 3);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const items_r5 = ctx.ngIf;
    const ctx_r1 = ɵɵnextContext();
    ɵɵadvance(2);
    ɵɵtextInterpolate1("", ɵɵpipeBind1(3, 3, ctx_r1.label$), ":");
    ɵɵadvance(3);
    ɵɵproperty("ngForOf", items_r5);
    ɵɵadvance();
    ɵɵproperty("type", ctx_r1.iconTypes.CARET_DOWN);
  }
}
function SiteThemeSwitcherComponent_label_0_option_5_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "option", 4);
    ɵɵpipe(1, "async");
    ɵɵpipe(2, "async");
    ɵɵtext(3);
    ɵɵpipe(4, "cxTranslate");
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const theme_r3 = ctx.$implicit;
    const i_r4 = ctx.index;
    const themes_r5 = ɵɵnextContext().ngIf;
    const ctx_r1 = ɵɵnextContext();
    ɵɵpropertyInterpolate("value", theme_r3.className);
    ɵɵproperty("selected", ɵɵpipeBind1(1, 4, ctx_r1.activeItem$) === theme_r3.className);
    ɵɵattribute("aria-label", ɵɵpipeBind1(2, 6, ctx_r1.ariaLabel$(theme_r3, i_r4, themes_r5.length)));
    ɵɵadvance(3);
    ɵɵtextInterpolate1(" ", theme_r3.i18nNameKey ? ɵɵpipeBind1(4, 8, theme_r3.i18nNameKey) : theme_r3.className, " ");
  }
}
function SiteThemeSwitcherComponent_label_0_Template(rf, ctx) {
  if (rf & 1) {
    const _r1 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "label")(1, "span");
    ɵɵtext(2);
    ɵɵpipe(3, "cxTranslate");
    ɵɵelementEnd();
    ɵɵelementStart(4, "select", 1);
    ɵɵlistener("change", function SiteThemeSwitcherComponent_label_0_Template_select_change_4_listener($event) {
      ɵɵrestoreView(_r1);
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.activeItem = $event.target.value);
    });
    ɵɵtemplate(5, SiteThemeSwitcherComponent_label_0_option_5_Template, 5, 10, "option", 2);
    ɵɵelementEnd();
    ɵɵelement(6, "cx-icon", 3);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const themes_r5 = ctx.ngIf;
    const ctx_r1 = ɵɵnextContext();
    ɵɵadvance(2);
    ɵɵtextInterpolate1(" ", ɵɵpipeBind1(3, 3, "siteThemeSwitcher.theme"), ":");
    ɵɵadvance(3);
    ɵɵproperty("ngForOf", themes_r5);
    ɵɵadvance();
    ɵɵproperty("type", ctx_r1.iconTypes.CARET_DOWN);
  }
}
var _c30 = (a0) => ({
  rating: a0
});
var _c31 = () => [1, 2, 3, 4, 5];
var _c322 = (a0) => ({
  count: a0
});
function StarRatingComponent_div_0_ng_container_3_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainer(0);
  }
}
function StarRatingComponent_div_0_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div", 2);
    ɵɵpipe(1, "cxTranslate");
    ɵɵpipe(2, "cxTranslate");
    ɵɵtemplate(3, StarRatingComponent_div_0_ng_container_3_Template, 1, 0, "ng-container", 3);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    const starRating_r2 = ɵɵreference(2);
    ɵɵproperty("title", ɵɵpipeBind2(1, 3, "productReview.ratedOutOf", ɵɵpureFunction1(9, _c30, ctx_r0.rating.toFixed(1))));
    ɵɵattribute("aria-label", ɵɵpipeBind2(2, 6, "productReview.ratedOutOf", ɵɵpureFunction1(11, _c30, ctx_r0.rating.toFixed(1))));
    ɵɵadvance(3);
    ɵɵproperty("ngTemplateOutlet", starRating_r2);
  }
}
function StarRatingComponent_ng_template_1_cx_icon_0_Template(rf, ctx) {
  if (rf & 1) {
    const _r3 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "cx-icon", 5);
    ɵɵpipe(1, "cxTranslate");
    ɵɵlistener("mouseover", function StarRatingComponent_ng_template_1_cx_icon_0_Template_cx_icon_mouseover_0_listener() {
      const i_r4 = ɵɵrestoreView(_r3).$implicit;
      const ctx_r0 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r0.setRate(i_r4));
    })("click", function StarRatingComponent_ng_template_1_cx_icon_0_Template_cx_icon_click_0_listener() {
      const i_r4 = ɵɵrestoreView(_r3).$implicit;
      const ctx_r0 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r0.saveRate(i_r4));
    })("keydown.space", function StarRatingComponent_ng_template_1_cx_icon_0_Template_cx_icon_keydown_space_0_listener() {
      const i_r4 = ɵɵrestoreView(_r3).$implicit;
      const ctx_r0 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r0.saveRate(i_r4));
    });
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const i_r4 = ctx.$implicit;
    const ctx_r0 = ɵɵnextContext(2);
    ɵɵproperty("type", ctx_r0.icon);
    ɵɵattribute("aria-current", !ctx_r0.disabled && i_r4 === ctx_r0.rating)("tabindex", ctx_r0.disabled ? null : 0)("role", ctx_r0.disabled ? null : "button")("aria-label", !ctx_r0.disabled ? ɵɵpipeBind2(1, 6, "productReview.addRate", ɵɵpureFunction1(9, _c322, i_r4)) : null)("aria-hidden", ctx_r0.disabled);
  }
}
function StarRatingComponent_ng_template_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵtemplate(0, StarRatingComponent_ng_template_1_cx_icon_0_Template, 2, 11, "cx-icon", 4);
  }
  if (rf & 2) {
    ɵɵproperty("ngForOf", ɵɵpureFunction0(1, _c31));
  }
}
function TableComponent_table_0_ng_container_1_tbody_1_tr_1_ng_template_2_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵtext(0);
  }
  if (rf & 2) {
    const cell_r4 = ɵɵnextContext().$implicit;
    ɵɵtextInterpolate1(" ", cell_r4, " ");
  }
}
function TableComponent_table_0_ng_container_1_tbody_1_tr_1_ng_template_4_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵtext(0);
  }
  if (rf & 2) {
    const cell_r4 = ɵɵnextContext().$implicit;
    const item_r2 = ɵɵnextContext().$implicit;
    ɵɵtextInterpolate1(" ", item_r2[cell_r4], " ");
  }
}
function TableComponent_table_0_ng_container_1_tbody_1_tr_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "tr")(1, "th");
    ɵɵtemplate(2, TableComponent_table_0_ng_container_1_tbody_1_tr_1_ng_template_2_Template, 1, 1, "ng-template", 4);
    ɵɵelementEnd();
    ɵɵelementStart(3, "td");
    ɵɵtemplate(4, TableComponent_table_0_ng_container_1_tbody_1_tr_1_ng_template_4_Template, 1, 1, "ng-template", 4);
    ɵɵelementEnd()();
  }
  if (rf & 2) {
    const cell_r4 = ctx.$implicit;
    const item_r2 = ɵɵnextContext().$implicit;
    const ctx_r2 = ɵɵnextContext(3);
    ɵɵclassMap(cell_r4);
    ɵɵadvance(2);
    ɵɵproperty("cxOutlet", ctx_r2.getHeaderOutletRef(cell_r4))("cxOutletContext", ctx_r2.getHeaderOutletContext(cell_r4));
    ɵɵadvance(2);
    ɵɵproperty("cxOutlet", ctx_r2.getDataOutletRef(cell_r4))("cxOutletContext", ctx_r2.getDataOutletContext(cell_r4, item_r2));
  }
}
function TableComponent_table_0_ng_container_1_tbody_1_Template(rf, ctx) {
  if (rf & 1) {
    const _r1 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "tbody", 2);
    ɵɵlistener("click", function TableComponent_table_0_ng_container_1_tbody_1_Template_tbody_click_0_listener() {
      const item_r2 = ɵɵrestoreView(_r1).$implicit;
      const ctx_r2 = ɵɵnextContext(3);
      return ɵɵresetView(ctx_r2.launchItem(item_r2));
    });
    ɵɵtemplate(1, TableComponent_table_0_ng_container_1_tbody_1_tr_1_Template, 5, 6, "tr", 3);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const item_r2 = ctx.$implicit;
    const ctx_r2 = ɵɵnextContext(3);
    ɵɵclassProp("is-current", ctx_r2.isCurrentItem(item_r2));
    ɵɵadvance();
    ɵɵproperty("ngForOf", ctx_r2.structure.cells);
  }
}
function TableComponent_table_0_ng_container_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainerStart(0);
    ɵɵtemplate(1, TableComponent_table_0_ng_container_1_tbody_1_Template, 2, 3, "tbody", 1);
    ɵɵelementContainerEnd();
  }
  if (rf & 2) {
    const ctx_r2 = ɵɵnextContext(2);
    ɵɵadvance();
    ɵɵproperty("ngForOf", ctx_r2.data)("ngForTrackBy", ctx_r2.trackData);
  }
}
function TableComponent_table_0_ng_container_2_th_3_ng_template_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵtext(0);
  }
  if (rf & 2) {
    const cell_r5 = ɵɵnextContext().$implicit;
    ɵɵtextInterpolate1(" ", cell_r5, " ");
  }
}
function TableComponent_table_0_ng_container_2_th_3_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "th", 6);
    ɵɵtemplate(1, TableComponent_table_0_ng_container_2_th_3_ng_template_1_Template, 1, 1, "ng-template", 4);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const cell_r5 = ctx.$implicit;
    const ctx_r2 = ɵɵnextContext(3);
    ɵɵclassMap(cell_r5);
    ɵɵadvance();
    ɵɵproperty("cxOutlet", ctx_r2.getHeaderOutletRef(cell_r5))("cxOutletContext", ctx_r2.getHeaderOutletContext(cell_r5));
  }
}
function TableComponent_table_0_ng_container_2_tr_4_td_1_ng_template_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵtext(0);
  }
  if (rf & 2) {
    const cell_r8 = ɵɵnextContext().$implicit;
    const item_r7 = ɵɵnextContext().$implicit;
    ɵɵtextInterpolate1(" ", item_r7[cell_r8], " ");
  }
}
function TableComponent_table_0_ng_container_2_tr_4_td_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "td");
    ɵɵtemplate(1, TableComponent_table_0_ng_container_2_tr_4_td_1_ng_template_1_Template, 1, 1, "ng-template", 4);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const cell_r8 = ctx.$implicit;
    const item_r7 = ɵɵnextContext().$implicit;
    const ctx_r2 = ɵɵnextContext(3);
    ɵɵclassMap(cell_r8);
    ɵɵadvance();
    ɵɵproperty("cxOutlet", ctx_r2.getDataOutletRef(cell_r8))("cxOutletContext", ctx_r2.getDataOutletContext(cell_r8, item_r7));
  }
}
function TableComponent_table_0_ng_container_2_tr_4_Template(rf, ctx) {
  if (rf & 1) {
    const _r6 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "tr", 2);
    ɵɵlistener("click", function TableComponent_table_0_ng_container_2_tr_4_Template_tr_click_0_listener() {
      const item_r7 = ɵɵrestoreView(_r6).$implicit;
      const ctx_r2 = ɵɵnextContext(3);
      return ɵɵresetView(ctx_r2.launchItem(item_r7));
    });
    ɵɵtemplate(1, TableComponent_table_0_ng_container_2_tr_4_td_1_Template, 2, 4, "td", 3);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const item_r7 = ctx.$implicit;
    const ctx_r2 = ɵɵnextContext(3);
    ɵɵclassProp("is-current", ctx_r2.isCurrentItem(item_r7));
    ɵɵadvance();
    ɵɵproperty("ngForOf", ctx_r2.structure.cells);
  }
}
function TableComponent_table_0_ng_container_2_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainerStart(0);
    ɵɵelementStart(1, "thead")(2, "tr");
    ɵɵtemplate(3, TableComponent_table_0_ng_container_2_th_3_Template, 2, 4, "th", 5);
    ɵɵelementEnd()();
    ɵɵtemplate(4, TableComponent_table_0_ng_container_2_tr_4_Template, 2, 3, "tr", 1);
    ɵɵelementContainerEnd();
  }
  if (rf & 2) {
    const ctx_r2 = ɵɵnextContext(2);
    ɵɵadvance(3);
    ɵɵproperty("ngForOf", ctx_r2.structure.cells);
    ɵɵadvance();
    ɵɵproperty("ngForOf", ctx_r2.data)("ngForTrackBy", ctx_r2.trackData);
  }
}
function TableComponent_table_0_ng_container_3_tr_1_ng_template_2_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵtext(0);
  }
  if (rf & 2) {
    const cell_r9 = ɵɵnextContext().$implicit;
    ɵɵtextInterpolate1(" ", cell_r9, " ");
  }
}
function TableComponent_table_0_ng_container_3_tr_1_td_3_ng_template_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵtext(0);
  }
  if (rf & 2) {
    const item_r11 = ɵɵnextContext().$implicit;
    const cell_r9 = ɵɵnextContext().$implicit;
    ɵɵtextInterpolate1(" ", item_r11[cell_r9], " ");
  }
}
function TableComponent_table_0_ng_container_3_tr_1_td_3_Template(rf, ctx) {
  if (rf & 1) {
    const _r10 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "td", 2);
    ɵɵlistener("click", function TableComponent_table_0_ng_container_3_tr_1_td_3_Template_td_click_0_listener() {
      const item_r11 = ɵɵrestoreView(_r10).$implicit;
      const ctx_r2 = ɵɵnextContext(4);
      return ɵɵresetView(ctx_r2.launchItem(item_r11));
    });
    ɵɵtemplate(1, TableComponent_table_0_ng_container_3_tr_1_td_3_ng_template_1_Template, 1, 1, "ng-template", 4);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const item_r11 = ctx.$implicit;
    const cell_r9 = ɵɵnextContext().$implicit;
    const ctx_r2 = ɵɵnextContext(3);
    ɵɵclassProp("is-current", ctx_r2.isCurrentItem(item_r11));
    ɵɵadvance();
    ɵɵproperty("cxOutlet", ctx_r2.getDataOutletRef(cell_r9))("cxOutletContext", ctx_r2.getDataOutletContext(cell_r9, item_r11));
  }
}
function TableComponent_table_0_ng_container_3_tr_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "tr")(1, "th", 6);
    ɵɵtemplate(2, TableComponent_table_0_ng_container_3_tr_1_ng_template_2_Template, 1, 1, "ng-template", 4);
    ɵɵelementEnd();
    ɵɵtemplate(3, TableComponent_table_0_ng_container_3_tr_1_td_3_Template, 2, 4, "td", 1);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const cell_r9 = ctx.$implicit;
    const ctx_r2 = ɵɵnextContext(3);
    ɵɵclassMap(cell_r9);
    ɵɵadvance(2);
    ɵɵproperty("cxOutlet", ctx_r2.getHeaderOutletRef(cell_r9))("cxOutletContext", ctx_r2.getHeaderOutletContext(cell_r9));
    ɵɵadvance();
    ɵɵproperty("ngForOf", ctx_r2.data)("ngForTrackBy", ctx_r2.trackData);
  }
}
function TableComponent_table_0_ng_container_3_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainerStart(0);
    ɵɵtemplate(1, TableComponent_table_0_ng_container_3_tr_1_Template, 4, 6, "tr", 3);
    ɵɵelementContainerEnd();
  }
  if (rf & 2) {
    const ctx_r2 = ɵɵnextContext(2);
    ɵɵadvance();
    ɵɵproperty("ngForOf", ctx_r2.structure.cells);
  }
}
function TableComponent_table_0_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "table");
    ɵɵtemplate(1, TableComponent_table_0_ng_container_1_Template, 2, 2, "ng-container", 0)(2, TableComponent_table_0_ng_container_2_Template, 5, 3, "ng-container", 0)(3, TableComponent_table_0_ng_container_3_Template, 2, 1, "ng-container", 0);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r2 = ɵɵnextContext();
    ɵɵadvance();
    ɵɵproperty("ngIf", ctx_r2.verticalStackedLayout);
    ɵɵadvance();
    ɵɵproperty("ngIf", ctx_r2.verticalLayout);
    ɵɵadvance();
    ɵɵproperty("ngIf", ctx_r2.horizontalLayout);
  }
}
var _c33 = ["captcha"];
var _c34 = (a0) => ({
  "right-align": a0
});
function AvatarComponent_div_0_span_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "span");
    ɵɵtext(1);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext(2);
    ɵɵadvance();
    ɵɵtextInterpolate1(" ", ctx_r0.getInitials((ctx_r0.message == null ? null : ctx_r0.message.author) || ""), " ");
  }
}
function AvatarComponent_div_0_cx_icon_2_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "cx-icon", 4);
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext(2);
    ɵɵproperty("type", ctx_r0.iconTypes.USER);
  }
}
function AvatarComponent_div_0_cx_icon_3_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "cx-icon", 4);
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext(2);
    ɵɵproperty("type", ctx_r0.iconTypes.HEADSET);
  }
}
function AvatarComponent_div_0_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div", 1);
    ɵɵtemplate(1, AvatarComponent_div_0_span_1_Template, 2, 1, "span", 2)(2, AvatarComponent_div_0_cx_icon_2_Template, 1, 1, "cx-icon", 3)(3, AvatarComponent_div_0_cx_icon_3_Template, 1, 1, "cx-icon", 3);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    ɵɵproperty("ngClass", ɵɵpureFunction1(4, _c34, ctx_r0.message.rightAlign));
    ɵɵadvance();
    ɵɵproperty("ngIf", !ctx_r0.message.rightAlign && (ctx_r0.message == null ? null : ctx_r0.message.author));
    ɵɵadvance();
    ɵɵproperty("ngIf", !ctx_r0.message.rightAlign && !(ctx_r0.message == null ? null : ctx_r0.message.author));
    ɵɵadvance();
    ɵɵproperty("ngIf", ctx_r0.message.rightAlign);
  }
}
var _c35 = (a0) => ({
  "cx-message-display": a0
});
var _c36 = (a0, a1, a2) => ({
  author: a0,
  text: a1,
  date: a2
});
var _c37 = () => [];
function MessagingComponent_ng_container_0_ng_container_5_a_10_Template(rf, ctx) {
  if (rf & 1) {
    const _r3 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "a", 11);
    ɵɵlistener("click", function MessagingComponent_ng_container_0_ng_container_5_a_10_Template_a_click_0_listener() {
      ɵɵrestoreView(_r3);
      const message_r4 = ɵɵnextContext().$implicit;
      const ctx_r1 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r1.onItemClicked(message_r4.item));
    });
    ɵɵtext(1);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const message_r4 = ɵɵnextContext().$implicit;
    ɵɵadvance();
    ɵɵtextInterpolate1("", message_r4.item.name, ":");
  }
}
function MessagingComponent_ng_container_0_ng_container_5_a_12_Template(rf, ctx) {
  if (rf & 1) {
    const _r5 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "a", 12);
    ɵɵlistener("click", function MessagingComponent_ng_container_0_ng_container_5_a_12_Template_a_click_0_listener() {
      const attachment_r6 = ɵɵrestoreView(_r5).$implicit;
      const message_r4 = ɵɵnextContext().$implicit;
      const ctx_r1 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r1.triggerDownload(message_r4.code, attachment_r6.id, attachment_r6.filename));
    });
    ɵɵelement(1, "cx-icon", 13);
    ɵɵtext(2);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const attachment_r6 = ctx.$implicit;
    const ctx_r1 = ɵɵnextContext(3);
    ɵɵadvance();
    ɵɵproperty("type", ctx_r1.iconTypes.ATTACHMENT);
    ɵɵadvance();
    ɵɵtextInterpolate1(" ", attachment_r6.filename, " ");
  }
}
function MessagingComponent_ng_container_0_ng_container_5_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainerStart(0);
    ɵɵelementStart(1, "div", 6);
    ɵɵelement(2, "cx-avatar", 7);
    ɵɵelementStart(3, "div")(4, "label");
    ɵɵtext(5);
    ɵɵpipe(6, "cxDate");
    ɵɵelementEnd();
    ɵɵelementStart(7, "div", 8);
    ɵɵpipe(8, "cxDate");
    ɵɵpipe(9, "cxTranslate");
    ɵɵtemplate(10, MessagingComponent_ng_container_0_ng_container_5_a_10_Template, 2, 1, "a", 9);
    ɵɵtext(11);
    ɵɵelementEnd();
    ɵɵtemplate(12, MessagingComponent_ng_container_0_ng_container_5_a_12_Template, 3, 2, "a", 10);
    ɵɵelementEnd()();
    ɵɵelementContainerEnd();
  }
  if (rf & 2) {
    let tmp_5_0;
    let tmp_7_0;
    let tmp_8_0;
    let tmp_10_0;
    let tmp_13_0;
    const message_r4 = ctx.$implicit;
    const i_r7 = ctx.index;
    const ctx_r1 = ɵɵnextContext(2);
    ɵɵadvance();
    ɵɵproperty("ngClass", ɵɵpureFunction1(18, _c35, (tmp_5_0 = message_r4.rightAlign) !== null && tmp_5_0 !== void 0 ? tmp_5_0 : false));
    ɵɵadvance();
    ɵɵproperty("message", message_r4);
    ɵɵadvance(3);
    ɵɵtextInterpolate((tmp_7_0 = ɵɵpipeBind2(6, 9, message_r4.createdAt, ctx_r1.messagingConfigs == null ? null : ctx_r1.messagingConfigs.dateFormat)) !== null && tmp_7_0 !== void 0 ? tmp_7_0 : ctx_r1.dateFormat);
    ɵɵadvance(2);
    ɵɵproperty("ngClass", ((tmp_8_0 = message_r4.rightAlign) !== null && tmp_8_0 !== void 0 ? tmp_8_0 : false) ? "cx-message-right-align-text" : "cx-message-left-align-text")("tabindex", i_r7 ? -1 : 0);
    ɵɵattribute("aria-label", ɵɵpipeBind2(9, 15, "chatMessaging.informationLabel", ɵɵpureFunction3(20, _c36, message_r4.author, ctx_r1.getMessageText(message_r4), (tmp_10_0 = ɵɵpipeBind2(8, 12, message_r4.createdAt, ctx_r1.messagingConfigs == null ? null : ctx_r1.messagingConfigs.dateFormat)) !== null && tmp_10_0 !== void 0 ? tmp_10_0 : ctx_r1.dateFormat)));
    ɵɵadvance(3);
    ɵɵproperty("ngIf", message_r4.item);
    ɵɵadvance();
    ɵɵtextInterpolate1(" ", message_r4.text, " ");
    ɵɵadvance();
    ɵɵproperty("ngForOf", (tmp_13_0 = message_r4.attachments) !== null && tmp_13_0 !== void 0 ? tmp_13_0 : ɵɵpureFunction0(24, _c37));
  }
}
function MessagingComponent_ng_container_0_div_6_select_6_option_2_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "option", 28);
    ɵɵtext(1);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const item_r9 = ctx.$implicit;
    ɵɵpropertyInterpolate("value", item_r9.id);
    ɵɵadvance();
    ɵɵtextInterpolate1(" ", item_r9.name, " ");
  }
}
function MessagingComponent_ng_container_0_div_6_select_6_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "select", 26);
    ɵɵpipe(1, "cxTranslate");
    ɵɵtemplate(2, MessagingComponent_ng_container_0_div_6_select_6_option_2_Template, 2, 2, "option", 27);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const itemList_r10 = ctx.ngIf;
    ɵɵattribute("aria-label", ɵɵpipeBind1(1, 2, "chatMessaging.a11y.itemListBoxLabel"));
    ɵɵadvance(2);
    ɵɵproperty("ngForOf", itemList_r10);
  }
}
function MessagingComponent_ng_container_0_div_6_span_11_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "span", 29);
    ɵɵtext(1);
    ɵɵpipe(2, "cxTranslate");
    ɵɵelementEnd();
  }
  if (rf & 2) {
    ɵɵadvance();
    ɵɵtextInterpolate1(" ", ɵɵpipeBind1(2, 1, "chatMessaging.characterLimitAlert"), " ");
  }
}
function MessagingComponent_ng_container_0_div_6_cx_file_upload_16_ng_template_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "cx-icon", 13);
    ɵɵelementStart(1, "span", 31);
    ɵɵtext(2);
    ɵɵpipe(3, "cxTranslate");
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext(4);
    ɵɵproperty("type", ctx_r1.iconTypes.UPLOAD);
    ɵɵadvance(2);
    ɵɵtextInterpolate1("", ɵɵpipeBind1(3, 2, "chatMessaging.uploadFile"), " ");
  }
}
function MessagingComponent_ng_container_0_div_6_cx_file_upload_16_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "cx-file-upload", 30);
    ɵɵtemplate(1, MessagingComponent_ng_container_0_div_6_cx_file_upload_16_ng_template_1_Template, 4, 4, "ng-template");
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext(3);
    ɵɵproperty("formControl", ctx_r1.form.get("file"))("accept", ctx_r1.allowedTypes);
  }
}
function MessagingComponent_ng_container_0_div_6_Template(rf, ctx) {
  if (rf & 1) {
    const _r8 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "div", 14)(1, "div", 15)(2, "label", 16)(3, "span");
    ɵɵtext(4);
    ɵɵpipe(5, "cxTranslate");
    ɵɵtemplate(6, MessagingComponent_ng_container_0_div_6_select_6_Template, 3, 4, "select", 17);
    ɵɵpipe(7, "async");
    ɵɵelementEnd();
    ɵɵelementStart(8, "div", 18)(9, "input", 19);
    ɵɵpipe(10, "cxTranslate");
    ɵɵlistener("keydown.enter", function MessagingComponent_ng_container_0_div_6_Template_input_keydown_enter_9_listener() {
      ɵɵrestoreView(_r8);
      const ctx_r1 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r1.onSend());
    });
    ɵɵelementEnd();
    ɵɵtemplate(11, MessagingComponent_ng_container_0_div_6_span_11_Template, 3, 3, "span", 20);
    ɵɵelementStart(12, "button", 21);
    ɵɵlistener("click", function MessagingComponent_ng_container_0_div_6_Template_button_click_12_listener() {
      ɵɵrestoreView(_r8);
      const ctx_r1 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r1.onSend());
    });
    ɵɵtext(13);
    ɵɵpipe(14, "cxTranslate");
    ɵɵelementEnd()()();
    ɵɵelementStart(15, "div", 22);
    ɵɵtemplate(16, MessagingComponent_ng_container_0_div_6_cx_file_upload_16_Template, 2, 2, "cx-file-upload", 23);
    ɵɵelementStart(17, "p", 24);
    ɵɵtext(18);
    ɵɵpipe(19, "cxTranslate");
    ɵɵelementEnd()();
    ɵɵelement(20, "cx-form-errors", 25);
    ɵɵelementEnd()();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext(2);
    ɵɵproperty("formGroup", ctx_r1.form);
    ɵɵadvance(4);
    ɵɵtextInterpolate1(" ", ɵɵpipeBind1(5, 11, "chatMessaging.addNewMessage"), " ");
    ɵɵadvance(2);
    ɵɵproperty("ngIf", ɵɵpipeBind1(7, 13, ctx_r1.messagingConfigs == null ? null : ctx_r1.messagingConfigs.itemList$));
    ɵɵadvance(3);
    ɵɵpropertyInterpolate("placeholder", (ctx_r1.messagingConfigs == null ? null : ctx_r1.messagingConfigs.newMessagePlaceHolder) || ɵɵpipeBind1(10, 15, "chatMessaging.addMessagePlaceHolder"));
    ɵɵproperty("maxLength", (ctx_r1.messagingConfigs == null ? null : ctx_r1.messagingConfigs.charactersLimit) || ctx_r1.MAX_INPUT_CHARACTERS);
    ɵɵadvance(2);
    ɵɵproperty("ngIf", ctx_r1.inputCharacterLeft === 0);
    ɵɵadvance();
    ɵɵproperty("ngClass", !!(ctx_r1.messagingConfigs == null ? null : ctx_r1.messagingConfigs.sendBtnIsNotPrimary) ? "btn-secondary" : "btn-primary");
    ɵɵadvance();
    ɵɵtextInterpolate1(" ", ɵɵpipeBind1(14, 17, "chatMessaging.send"), " ");
    ɵɵadvance(3);
    ɵɵproperty("ngIf", ctx_r1.messagingConfigs == null ? null : ctx_r1.messagingConfigs.enableFileUploadOption);
    ɵɵadvance(2);
    ɵɵtextInterpolate1(" ", ɵɵpipeBind2(19, 19, "chatMessaging.charactersLeft", ɵɵpureFunction1(22, _c322, ctx_r1.inputCharacterLeft)), " ");
    ɵɵadvance(2);
    ɵɵproperty("control", ctx_r1.form.get("file"));
  }
}
function MessagingComponent_ng_container_0_Template(rf, ctx) {
  if (rf & 1) {
    const _r1 = ɵɵgetCurrentView();
    ɵɵelementContainerStart(0);
    ɵɵelementStart(1, "div", 1);
    ɵɵelement(2, "div", 2);
    ɵɵelementStart(3, "div", 3);
    ɵɵpipe(4, "cxTranslate");
    ɵɵlistener("keydown.arrowdown", function MessagingComponent_ng_container_0_Template_div_keydown_arrowdown_3_listener($event) {
      ɵɵrestoreView(_r1);
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.focusNextChild($event));
    })("keydown.arrowup", function MessagingComponent_ng_container_0_Template_div_keydown_arrowup_3_listener($event) {
      ɵɵrestoreView(_r1);
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.focusPreviousChild($event));
    });
    ɵɵtemplate(5, MessagingComponent_ng_container_0_ng_container_5_Template, 13, 25, "ng-container", 4);
    ɵɵelementEnd()();
    ɵɵtemplate(6, MessagingComponent_ng_container_0_div_6_Template, 21, 24, "div", 5);
    ɵɵpipe(7, "async");
    ɵɵelementContainerEnd();
  }
  if (rf & 2) {
    const messageEvents_r11 = ctx.ngIf;
    const ctx_r1 = ɵɵnextContext();
    ɵɵadvance(3);
    ɵɵattribute("aria-label", ɵɵpipeBind1(4, 3, "chatMessaging.messages"));
    ɵɵadvance(2);
    ɵɵproperty("ngForOf", messageEvents_r11);
    ɵɵadvance();
    ɵɵproperty("ngIf", ɵɵpipeBind1(7, 5, ctx_r1.messagingConfigs == null ? null : ctx_r1.messagingConfigs.displayAddMessageSection));
  }
}
function ReadMoreComponent_ng_container_0_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainerStart(0);
    ɵɵtext(1);
    ɵɵelementContainerEnd();
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    ɵɵadvance();
    ɵɵtextInterpolate1(" ", ctx_r0.text.slice(0, ctx_r0.maxLength), "\n");
  }
}
function ReadMoreComponent_ng_container_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainerStart(0);
    ɵɵtext(1);
    ɵɵelementContainerEnd();
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    ɵɵadvance();
    ɵɵtextInterpolate1(" ", ctx_r0.text, "\n");
  }
}
function ReadMoreComponent_button_2_Template(rf, ctx) {
  if (rf & 1) {
    const _r2 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "button", 2);
    ɵɵlistener("click", function ReadMoreComponent_button_2_Template_button_click_0_listener() {
      ɵɵrestoreView(_r2);
      const ctx_r0 = ɵɵnextContext();
      return ɵɵresetView(ctx_r0.isCollapsed = !ctx_r0.isCollapsed);
    });
    ɵɵtext(1);
    ɵɵpipe(2, "cxTranslate");
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    ɵɵadvance();
    ɵɵtextInterpolate1(" ", ɵɵpipeBind1(2, 1, ctx_r0.isCollapsed ? ctx_r0.readMoreI18nKey : ctx_r0.readLessI18nKey), "\n");
  }
}
function BannerComponent_ng_container_0_ng_container_1_ng_container_1_p_2_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "p", 7);
  }
  if (rf & 2) {
    const data_r1 = ɵɵnextContext(2).ngIf;
    ɵɵproperty("innerHTML", data_r1.headline, ɵɵsanitizeHtml);
  }
}
function BannerComponent_ng_container_0_ng_container_1_ng_container_1_cx_media_3_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "cx-media", 8);
    ɵɵpipe(1, "async");
  }
  if (rf & 2) {
    const data_r1 = ɵɵnextContext(2).ngIf;
    const lcpContext_r2 = ɵɵnextContext().$implicit;
    const ctx_r2 = ɵɵnextContext();
    ɵɵproperty("container", ctx_r2.getImage(data_r1))("elementType", "picture")("fetchPriority", ɵɵpipeBind1(1, 3, lcpContext_r2.fetchPriority$));
  }
}
function BannerComponent_ng_container_0_ng_container_1_ng_container_1_cx_media_4_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "cx-media", 9);
    ɵɵpipe(1, "async");
  }
  if (rf & 2) {
    const data_r1 = ɵɵnextContext(2).ngIf;
    const lcpContext_r2 = ɵɵnextContext().$implicit;
    const ctx_r2 = ɵɵnextContext();
    ɵɵproperty("container", ctx_r2.getImage(data_r1))("fetchPriority", ɵɵpipeBind1(1, 2, lcpContext_r2.fetchPriority$));
  }
}
function BannerComponent_ng_container_0_ng_container_1_ng_container_1_p_5_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "p", 10);
  }
  if (rf & 2) {
    const data_r1 = ɵɵnextContext(2).ngIf;
    ɵɵproperty("innerHTML", data_r1.content, ɵɵsanitizeHtml);
  }
}
function BannerComponent_ng_container_0_ng_container_1_ng_container_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainerStart(0);
    ɵɵelement(1, "cx-generic-link", 2);
    ɵɵtemplate(2, BannerComponent_ng_container_0_ng_container_1_ng_container_1_p_2_Template, 1, 1, "p", 3)(3, BannerComponent_ng_container_0_ng_container_1_ng_container_1_cx_media_3_Template, 2, 5, "cx-media", 4)(4, BannerComponent_ng_container_0_ng_container_1_ng_container_1_cx_media_4_Template, 2, 4, "cx-media", 5)(5, BannerComponent_ng_container_0_ng_container_1_ng_container_1_p_5_Template, 1, 1, "p", 6);
    ɵɵelementContainerEnd();
  }
  if (rf & 2) {
    const data_r1 = ɵɵnextContext().ngIf;
    const ctx_r2 = ɵɵnextContext(2);
    ɵɵadvance();
    ɵɵproperty("url", ctx_r2.routerLink)("target", ctx_r2.getTarget(data_r1))("ariaLabel", ctx_r2.getLinkAriaLabel(data_r1));
    ɵɵattribute("title", !data_r1.headline ? ctx_r2.getImageAltText(data_r1) : null);
    ɵɵadvance();
    ɵɵproperty("ngIf", data_r1.headline);
    ɵɵadvance();
    ɵɵproperty("cxFeature", "useExtendedMediaComponentConfiguration");
    ɵɵadvance();
    ɵɵproperty("cxFeature", "!useExtendedMediaComponentConfiguration");
    ɵɵadvance();
    ɵɵproperty("ngIf", data_r1.content);
  }
}
function BannerComponent_ng_container_0_ng_container_1_ng_container_2_p_2_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "p", 7);
  }
  if (rf & 2) {
    const data_r1 = ɵɵnextContext(2).ngIf;
    ɵɵproperty("innerHTML", data_r1.headline, ɵɵsanitizeHtml);
  }
}
function BannerComponent_ng_container_0_ng_container_1_ng_container_2_cx_media_3_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "cx-media", 8);
    ɵɵpipe(1, "async");
  }
  if (rf & 2) {
    const data_r1 = ɵɵnextContext(2).ngIf;
    const lcpContext_r2 = ɵɵnextContext().$implicit;
    const ctx_r2 = ɵɵnextContext();
    ɵɵproperty("container", ctx_r2.getImage(data_r1))("elementType", "picture")("fetchPriority", ɵɵpipeBind1(1, 3, lcpContext_r2.fetchPriority$));
  }
}
function BannerComponent_ng_container_0_ng_container_1_ng_container_2_cx_media_4_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "cx-media", 9);
    ɵɵpipe(1, "async");
  }
  if (rf & 2) {
    const data_r1 = ɵɵnextContext(2).ngIf;
    const lcpContext_r2 = ɵɵnextContext().$implicit;
    const ctx_r2 = ɵɵnextContext();
    ɵɵproperty("container", ctx_r2.getImage(data_r1))("fetchPriority", ɵɵpipeBind1(1, 2, lcpContext_r2.fetchPriority$));
  }
}
function BannerComponent_ng_container_0_ng_container_1_ng_container_2_p_5_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "p", 10);
  }
  if (rf & 2) {
    const data_r1 = ɵɵnextContext(2).ngIf;
    ɵɵproperty("innerHTML", data_r1.content, ɵɵsanitizeHtml);
  }
}
function BannerComponent_ng_container_0_ng_container_1_ng_container_2_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainerStart(0);
    ɵɵelementStart(1, "div", 11);
    ɵɵtemplate(2, BannerComponent_ng_container_0_ng_container_1_ng_container_2_p_2_Template, 1, 1, "p", 3)(3, BannerComponent_ng_container_0_ng_container_1_ng_container_2_cx_media_3_Template, 2, 5, "cx-media", 4)(4, BannerComponent_ng_container_0_ng_container_1_ng_container_2_cx_media_4_Template, 2, 4, "cx-media", 5)(5, BannerComponent_ng_container_0_ng_container_1_ng_container_2_p_5_Template, 1, 1, "p", 6);
    ɵɵelementEnd();
    ɵɵelementContainerEnd();
  }
  if (rf & 2) {
    const data_r1 = ɵɵnextContext().ngIf;
    ɵɵadvance(2);
    ɵɵproperty("ngIf", data_r1.headline);
    ɵɵadvance();
    ɵɵproperty("cxFeature", "useExtendedMediaComponentConfiguration");
    ɵɵadvance();
    ɵɵproperty("cxFeature", "!useExtendedMediaComponentConfiguration");
    ɵɵadvance();
    ɵɵproperty("ngIf", data_r1.content);
  }
}
function BannerComponent_ng_container_0_ng_container_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainerStart(0);
    ɵɵtemplate(1, BannerComponent_ng_container_0_ng_container_1_ng_container_1_Template, 6, 8, "ng-container", 1)(2, BannerComponent_ng_container_0_ng_container_1_ng_container_2_Template, 6, 4, "ng-container", 1);
    ɵɵelementContainerEnd();
  }
  if (rf & 2) {
    const ctx_r2 = ɵɵnextContext(2);
    ɵɵadvance();
    ɵɵproperty("ngIf", ctx_r2.routerLink);
    ɵɵadvance();
    ɵɵproperty("ngIf", !ctx_r2.routerLink);
  }
}
function BannerComponent_ng_container_0_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainerStart(0);
    ɵɵtemplate(1, BannerComponent_ng_container_0_ng_container_1_Template, 3, 2, "ng-container", 1);
    ɵɵpipe(2, "async");
    ɵɵelementContainerEnd();
  }
  if (rf & 2) {
    const ctx_r2 = ɵɵnextContext();
    ɵɵadvance();
    ɵɵproperty("ngIf", ɵɵpipeBind1(2, 1, ctx_r2.data$));
  }
}
function LinkComponent_cx_generic_link_0_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "cx-generic-link", 1);
    ɵɵtext(1);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    let tmp_3_0;
    const data_r1 = ctx.ngIf;
    const ctx_r1 = ɵɵnextContext();
    ɵɵstyleMap(data_r1.styleAttributes);
    ɵɵproperty("url", (tmp_3_0 = data_r1.url) !== null && tmp_3_0 !== void 0 ? tmp_3_0 : "")("target", ctx_r1.getTarget(data_r1));
    ɵɵadvance();
    ɵɵtextInterpolate(data_r1.linkName);
  }
}
function ParagraphComponent_div_0_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "div", 1);
    ɵɵpipe(1, "cxSupplementHashAnchors");
  }
  if (rf & 2) {
    const data_r1 = ctx.ngIf;
    const ctx_r1 = ɵɵnextContext();
    ɵɵproperty("innerHTML", ctx_r1.bypassSecurityTrustHtml(ɵɵpipeBind1(1, 1, data_r1.content)), ɵɵsanitizeHtml);
  }
}
function PDFComponent_ng_container_0_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainerStart(0);
    ɵɵelementStart(1, "div", 1)(2, "a", 2)(3, "span");
    ɵɵtext(4);
    ɵɵpipe(5, "cxTranslate");
    ɵɵelementEnd();
    ɵɵelementStart(6, "span", 3);
    ɵɵelement(7, "cx-icon", 4);
    ɵɵelementEnd()()();
    ɵɵelementContainerEnd();
  }
  if (rf & 2) {
    const data_r1 = ctx.ngIf;
    const ctx_r1 = ɵɵnextContext();
    ɵɵadvance(2);
    ɵɵproperty("href", ctx_r1.url, ɵɵsanitizeUrl);
    ɵɵadvance(2);
    ɵɵtextInterpolate(ctx_r1.addPdfExtension((data_r1 == null ? null : data_r1.title) || (data_r1 == null ? null : data_r1.pdfFile == null ? null : data_r1.pdfFile.altText) || ɵɵpipeBind1(5, 3, "pdf.defaultTitle")));
    ɵɵadvance(3);
    ɵɵproperty("type", "PDF_FILE");
  }
}
var _c38 = (a0) => ({
  focusOnEscape: a0
});
function TabPanelComponent_ng_container_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainer(0, 2);
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    ɵɵproperty("ngTemplateOutlet", ctx_r0.tab.content);
  }
}
var _c39 = ["tabHeader"];
var _c40 = () => ({});
var _c41 = (a0) => ({
  tabNum: a0
});
function TabComponent_ng_container_0_ng_container_3_span_5_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "span", 7);
    ɵɵtext(1);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const i_r2 = ɵɵnextContext().index;
    const ctx_r3 = ɵɵnextContext(2);
    ɵɵadvance();
    ɵɵtextInterpolate1(" ", ctx_r3.isOpen(i_r2) ? "–" : "+", " ");
  }
}
function TabComponent_ng_container_0_ng_container_3_ng_container_6_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainer(0, 8);
  }
  if (rf & 2) {
    const i_r2 = ɵɵnextContext().index;
    ɵɵnextContext();
    const tabPanel_r5 = ɵɵreference(6);
    ɵɵproperty("ngTemplateOutlet", tabPanel_r5)("ngTemplateOutletContext", ɵɵpureFunction1(2, _c41, i_r2));
  }
}
function TabComponent_ng_container_0_ng_container_3_Template(rf, ctx) {
  if (rf & 1) {
    const _r1 = ɵɵgetCurrentView();
    ɵɵelementContainerStart(0);
    ɵɵelementStart(1, "button", 4, 1);
    ɵɵlistener("click", function TabComponent_ng_container_0_ng_container_3_Template_button_click_1_listener() {
      const i_r2 = ɵɵrestoreView(_r1).index;
      const mode_r3 = ɵɵnextContext().ngIf;
      const ctx_r3 = ɵɵnextContext();
      return ɵɵresetView(ctx_r3.select(i_r2, mode_r3));
    })("keydown", function TabComponent_ng_container_0_ng_container_3_Template_button_keydown_1_listener($event) {
      const i_r2 = ɵɵrestoreView(_r1).index;
      const mode_r3 = ɵɵnextContext().ngIf;
      const ctx_r3 = ɵɵnextContext();
      return ɵɵresetView(ctx_r3.handleKeydownEvent(i_r2, ctx_r3.tabs, mode_r3, $event));
    });
    ɵɵtext(3);
    ɵɵpipe(4, "cxTranslate");
    ɵɵtemplate(5, TabComponent_ng_container_0_ng_container_3_span_5_Template, 2, 1, "span", 5);
    ɵɵelementEnd();
    ɵɵtemplate(6, TabComponent_ng_container_0_ng_container_3_ng_container_6_Template, 1, 4, "ng-container", 6);
    ɵɵelementContainerEnd();
  }
  if (rf & 2) {
    const tab_r6 = ctx.$implicit;
    const i_r2 = ctx.index;
    const mode_r3 = ɵɵnextContext().ngIf;
    const ctx_r3 = ɵɵnextContext();
    ɵɵadvance();
    ɵɵclassProp("active", ctx_r3.isOpen(i_r2));
    ɵɵproperty("disabled", ctx_r3.disabled);
    ɵɵattribute("role", mode_r3 === ctx_r3.TAB_MODE.ACCORDIAN ? "button" : "tab")("aria-selected", mode_r3 === ctx_r3.TAB_MODE.ACCORDIAN ? null : ctx_r3.isOpen(i_r2))("aria-expanded", mode_r3 === ctx_r3.TAB_MODE.TAB ? null : ctx_r3.isOpen(i_r2))("aria-controls", ctx_r3.isOpen(i_r2) ? "section-" + i_r2 : null)("tabindex", ctx_r3.getTabIndex(i_r2, mode_r3))("id", tab_r6.id)("title", ctx_r3.getTitle(mode_r3, i_r2));
    ɵɵadvance(2);
    ɵɵtextInterpolate1(" ", tab_r6.headerKey ? ɵɵpipeBind2(4, 13, tab_r6.headerKey, tab_r6.headerParams || ɵɵpureFunction0(16, _c40)) : tab_r6.header, " ");
    ɵɵadvance(2);
    ɵɵproperty("ngIf", mode_r3 === ctx_r3.TAB_MODE.ACCORDIAN);
    ɵɵadvance();
    ɵɵproperty("ngIf", mode_r3 === ctx_r3.TAB_MODE.ACCORDIAN);
  }
}
function TabComponent_ng_container_0_ng_container_4_ng_container_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainer(0, 8);
  }
  if (rf & 2) {
    const i_r7 = ctx.index;
    ɵɵnextContext(2);
    const tabPanel_r5 = ɵɵreference(6);
    ɵɵproperty("ngTemplateOutlet", tabPanel_r5)("ngTemplateOutletContext", ɵɵpureFunction1(2, _c41, i_r7));
  }
}
function TabComponent_ng_container_0_ng_container_4_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainerStart(0);
    ɵɵtemplate(1, TabComponent_ng_container_0_ng_container_4_ng_container_1_Template, 1, 4, "ng-container", 9);
    ɵɵelementContainerEnd();
  }
  if (rf & 2) {
    const ctx_r3 = ɵɵnextContext(2);
    ɵɵadvance();
    ɵɵproperty("ngForOf", ctx_r3.tabs);
  }
}
function TabComponent_ng_container_0_ng_template_5_cx_tab_panel_0_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "cx-tab-panel", 11);
  }
  if (rf & 2) {
    const tabNum_r8 = ɵɵnextContext().tabNum;
    const mode_r3 = ɵɵnextContext().ngIf;
    const ctx_r3 = ɵɵnextContext();
    ɵɵproperty("tab", ctx_r3.tabs[tabNum_r8])("mode", mode_r3);
  }
}
function TabComponent_ng_container_0_ng_template_5_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵtemplate(0, TabComponent_ng_container_0_ng_template_5_cx_tab_panel_0_Template, 1, 2, "cx-tab-panel", 10);
  }
  if (rf & 2) {
    const tabNum_r8 = ctx.tabNum;
    const ctx_r3 = ɵɵnextContext(2);
    ɵɵproperty("ngIf", ctx_r3.isOpen(tabNum_r8));
  }
}
function TabComponent_ng_container_0_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainerStart(0);
    ɵɵelementStart(1, "div");
    ɵɵpipe(2, "cxTranslate");
    ɵɵtemplate(3, TabComponent_ng_container_0_ng_container_3_Template, 7, 17, "ng-container", 3);
    ɵɵelementEnd();
    ɵɵtemplate(4, TabComponent_ng_container_0_ng_container_4_Template, 2, 1, "ng-container", 2)(5, TabComponent_ng_container_0_ng_template_5_Template, 1, 1, "ng-template", null, 0, ɵɵtemplateRefExtractor);
    ɵɵelementContainerEnd();
  }
  if (rf & 2) {
    const mode_r3 = ctx.ngIf;
    const ctx_r3 = ɵɵnextContext();
    ɵɵadvance();
    ɵɵclassMap(mode_r3.toLowerCase());
    ɵɵattribute("role", mode_r3 === ctx_r3.TAB_MODE.ACCORDIAN ? null : "tablist")("aria-label", ɵɵpipeBind1(2, 6, ctx_r3.config.label));
    ɵɵadvance(2);
    ɵɵproperty("ngForOf", ctx_r3.tabs);
    ɵɵadvance();
    ɵɵproperty("ngIf", mode_r3 === ctx_r3.TAB_MODE.TAB);
  }
}
var _c422 = ["tabRef"];
function TabParagraphContainerComponent_ng_container_0_4_ng_template_0_Template(rf, ctx) {
  if (rf & 1) {
    const _r1 = ɵɵgetCurrentView();
    ɵɵelementContainerStart(0, 5);
    ɵɵlistener("cxComponentRef", function TabParagraphContainerComponent_ng_container_0_4_ng_template_0_Template_ng_container_cxComponentRef_0_listener($event) {
      ɵɵrestoreView(_r1);
      const component_r2 = ɵɵnextContext().$implicit;
      const ctx_r2 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r2.tabCompLoaded($event, component_r2.uid));
    });
    ɵɵelementContainerEnd();
  }
  if (rf & 2) {
    const component_r2 = ɵɵnextContext().$implicit;
    ɵɵproperty("cxComponentWrapper", component_r2);
  }
}
function TabParagraphContainerComponent_ng_container_0_4_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵtemplate(0, TabParagraphContainerComponent_ng_container_0_4_ng_template_0_Template, 1, 1, "ng-template", 4, 0, ɵɵtemplateRefExtractor);
  }
  if (rf & 2) {
    const component_r2 = ctx.$implicit;
    ɵɵproperty("cxOutlet", component_r2.flexType);
  }
}
function TabParagraphContainerComponent_ng_container_0_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainerStart(0);
    ɵɵelementStart(1, "cx-tab", 2);
    ɵɵpipe(2, "async");
    ɵɵpipe(3, "async");
    ɵɵtemplate(4, TabParagraphContainerComponent_ng_container_0_4_Template, 2, 1, null, 3);
    ɵɵelementEnd();
    ɵɵelementContainerEnd();
  }
  if (rf & 2) {
    const components_r4 = ctx.ngIf;
    const ctx_r2 = ɵɵnextContext();
    ɵɵadvance();
    ɵɵproperty("tabs", ɵɵpipeBind1(2, 3, ctx_r2.tabs$))("config", ɵɵpipeBind1(3, 5, ctx_r2.tabConfig$));
    ɵɵadvance(3);
    ɵɵproperty("ngForOf", components_r4);
  }
}
function VideoComponent_div_0_a_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "a", 5);
    ɵɵtext(1);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const data_r1 = ɵɵnextContext().ngIf;
    const ctx_r1 = ɵɵnextContext();
    ɵɵproperty("routerLink", ctx_r1.routerLink);
    ɵɵadvance();
    ɵɵtextInterpolate(data_r1.overlayTitle);
  }
}
function VideoComponent_div_0_video_2_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "video", 6);
    ɵɵpipe(1, "cxTranslate");
  }
  if (rf & 2) {
    const data_r1 = ɵɵnextContext().ngIf;
    const ctx_r1 = ɵɵnextContext();
    ɵɵstyleProp("height", data_r1.videoContainerHeight, "px");
    ɵɵproperty("poster", ctx_r1.thumbnail == null ? null : ctx_r1.thumbnail.src, ɵɵsanitizeUrl)("loop", ctx_r1.loop)("autoplay", ctx_r1.autoPlay)("muted", ctx_r1.mute)("src", ctx_r1.source, ɵɵsanitizeUrl);
    ɵɵattribute("aria-label", ɵɵpipeBind1(1, 8, "player.label"));
  }
}
function VideoComponent_div_0_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div", 2);
    ɵɵtemplate(1, VideoComponent_div_0_a_1_Template, 2, 2, "a", 3)(2, VideoComponent_div_0_video_2_Template, 2, 10, "video", 4);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const data_r1 = ctx.ngIf;
    const ctx_r1 = ɵɵnextContext();
    ɵɵadvance();
    ɵɵproperty("ngIf", data_r1.overlayTitle);
    ɵɵadvance();
    ɵɵproperty("ngIf", ctx_r1.source);
  }
}
function VideoComponent_ng_template_2_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div", 7);
    ɵɵelement(1, "cx-spinner");
    ɵɵelementEnd();
  }
}
var _c43 = ["element"];
var _c44 = () => ["/my-account/notification-preference"];
function MyCouponsComponent_ng_container_1_ng_container_1_ng_container_5_div_12_Template(rf, ctx) {
  if (rf & 1) {
    const _r3 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "div", 21)(1, "cx-coupon-card", 22);
    ɵɵlistener("notificationChanged", function MyCouponsComponent_ng_container_1_ng_container_1_ng_container_5_div_12_Template_cx_coupon_card_notificationChanged_1_listener($event) {
      ɵɵrestoreView(_r3);
      const ctx_r1 = ɵɵnextContext(4);
      return ɵɵresetView(ctx_r1.notificationChange($event));
    });
    ɵɵelementEnd()();
  }
  if (rf & 2) {
    const coupon_r4 = ctx.$implicit;
    const ctx_r1 = ɵɵnextContext(4);
    ɵɵadvance();
    ɵɵproperty("coupon", coupon_r4)("couponSubscriptionLoading$", ctx_r1.couponSubscriptionLoading$);
  }
}
function MyCouponsComponent_ng_container_1_ng_container_1_ng_container_5_Template(rf, ctx) {
  if (rf & 1) {
    const _r1 = ɵɵgetCurrentView();
    ɵɵelementContainerStart(0);
    ɵɵelementStart(1, "div", 6)(2, "label", 7)(3, "span");
    ɵɵtext(4);
    ɵɵpipe(5, "cxTranslate");
    ɵɵelementEnd();
    ɵɵelementStart(6, "cx-sorting", 8);
    ɵɵpipe(7, "async");
    ɵɵpipe(8, "cxTranslate");
    ɵɵlistener("sortListEvent", function MyCouponsComponent_ng_container_1_ng_container_1_ng_container_5_Template_cx_sorting_sortListEvent_6_listener($event) {
      ɵɵrestoreView(_r1);
      const ctx_r1 = ɵɵnextContext(3);
      return ɵɵresetView(ctx_r1.sortChange($event));
    });
    ɵɵelementEnd()();
    ɵɵelementStart(9, "div", 9)(10, "cx-pagination", 10);
    ɵɵlistener("viewPageEvent", function MyCouponsComponent_ng_container_1_ng_container_1_ng_container_5_Template_cx_pagination_viewPageEvent_10_listener($event) {
      ɵɵrestoreView(_r1);
      const ctx_r1 = ɵɵnextContext(3);
      return ɵɵresetView(ctx_r1.pageChange($event));
    });
    ɵɵelementEnd()()();
    ɵɵelementStart(11, "div", 11);
    ɵɵtemplate(12, MyCouponsComponent_ng_container_1_ng_container_1_ng_container_5_div_12_Template, 2, 2, "div", 12);
    ɵɵelementEnd();
    ɵɵelementStart(13, "div", 13)(14, "label", 14)(15, "span");
    ɵɵtext(16);
    ɵɵpipe(17, "cxTranslate");
    ɵɵelementEnd();
    ɵɵelementStart(18, "cx-sorting", 15);
    ɵɵpipe(19, "cxTranslate");
    ɵɵpipe(20, "async");
    ɵɵpipe(21, "cxTranslate");
    ɵɵlistener("sortListEvent", function MyCouponsComponent_ng_container_1_ng_container_1_ng_container_5_Template_cx_sorting_sortListEvent_18_listener($event) {
      ɵɵrestoreView(_r1);
      const ctx_r1 = ɵɵnextContext(3);
      return ɵɵresetView(ctx_r1.sortChange($event));
    });
    ɵɵelementEnd()();
    ɵɵelementStart(22, "div", 16)(23, "cx-pagination", 17);
    ɵɵlistener("viewPageEvent", function MyCouponsComponent_ng_container_1_ng_container_1_ng_container_5_Template_cx_pagination_viewPageEvent_23_listener($event) {
      ɵɵrestoreView(_r1);
      const ctx_r1 = ɵɵnextContext(3);
      return ɵɵresetView(ctx_r1.pageChange($event));
    });
    ɵɵelementEnd()()();
    ɵɵelementStart(24, "div", 18)(25, "span");
    ɵɵelement(26, "cx-icon", 19);
    ɵɵtext(27);
    ɵɵpipe(28, "cxTranslate");
    ɵɵelementStart(29, "a", 20);
    ɵɵtext(30);
    ɵɵpipe(31, "cxTranslate");
    ɵɵelementEnd();
    ɵɵtext(32);
    ɵɵpipe(33, "cxTranslate");
    ɵɵelementEnd()();
    ɵɵelementContainerEnd();
  }
  if (rf & 2) {
    const couponResult_r5 = ɵɵnextContext().ngIf;
    const ctx_r1 = ɵɵnextContext(2);
    ɵɵadvance(4);
    ɵɵtextInterpolate(ɵɵpipeBind1(5, 19, "myCoupons.sortBy"));
    ɵɵadvance(2);
    ɵɵproperty("sortOptions", ctx_r1.sortOptions)("sortLabels", ɵɵpipeBind1(7, 21, ctx_r1.sortLabels))("selectedOption", ctx_r1.sort)("ariaLabel", ɵɵpipeBind1(8, 23, "myCoupons.sortCoupons"));
    ɵɵadvance(4);
    ɵɵproperty("pagination", ctx_r1.pagination);
    ɵɵadvance(2);
    ɵɵproperty("ngForOf", couponResult_r5.coupons);
    ɵɵadvance(4);
    ɵɵtextInterpolate(ɵɵpipeBind1(17, 25, "myCoupons.sortBy"));
    ɵɵadvance(2);
    ɵɵpropertyInterpolate("placeholder", ɵɵpipeBind1(19, 27, "myCoupons.sortBy"));
    ɵɵproperty("sortOptions", ctx_r1.sortOptions)("sortLabels", ɵɵpipeBind1(20, 29, ctx_r1.sortLabels))("selectedOption", ctx_r1.sort)("ariaLabel", ɵɵpipeBind1(21, 31, "myCoupons.sortCoupons"));
    ɵɵadvance(5);
    ɵɵproperty("pagination", ctx_r1.pagination);
    ɵɵadvance(3);
    ɵɵproperty("type", ctx_r1.iconTypes.INFO);
    ɵɵadvance();
    ɵɵtextInterpolate1(" ", ɵɵpipeBind1(28, 33, "myCoupons.notesPreffix"), "");
    ɵɵadvance(2);
    ɵɵproperty("routerLink", ɵɵpureFunction0(39, _c44));
    ɵɵadvance();
    ɵɵtextInterpolate(ɵɵpipeBind1(31, 35, "myCoupons.notesLink"));
    ɵɵadvance(2);
    ɵɵtextInterpolate(ɵɵpipeBind1(33, 37, "myCoupons.notesSuffix"));
  }
}
function MyCouponsComponent_ng_container_1_ng_container_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainerStart(0);
    ɵɵelementStart(1, "div", 5)(2, "h2");
    ɵɵtext(3);
    ɵɵpipe(4, "cxTranslate");
    ɵɵelementEnd()();
    ɵɵtemplate(5, MyCouponsComponent_ng_container_1_ng_container_1_ng_container_5_Template, 34, 40, "ng-container", 3);
    ɵɵelementContainerEnd();
  }
  if (rf & 2) {
    const couponResult_r5 = ctx.ngIf;
    ɵɵnextContext();
    const noCoupons_r6 = ɵɵreference(4);
    ɵɵadvance(3);
    ɵɵtextInterpolate1(" ", ɵɵpipeBind1(4, 3, "myCoupons.myCoupons"), " ");
    ɵɵadvance(2);
    ɵɵproperty("ngIf", couponResult_r5.pagination && couponResult_r5.pagination.totalCount && couponResult_r5.pagination.totalCount > 0)("ngIfElse", noCoupons_r6);
  }
}
function MyCouponsComponent_ng_container_1_ng_template_3_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "section")(1, "p", 23);
    ɵɵtext(2);
    ɵɵpipe(3, "cxTranslate");
    ɵɵelementEnd()();
  }
  if (rf & 2) {
    ɵɵadvance(2);
    ɵɵtextInterpolate1(" ", ɵɵpipeBind1(3, 1, "myCoupons.noCouponsMessage"), " ");
  }
}
function MyCouponsComponent_ng_container_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainerStart(0);
    ɵɵtemplate(1, MyCouponsComponent_ng_container_1_ng_container_1_Template, 6, 5, "ng-container", 4);
    ɵɵpipe(2, "async");
    ɵɵtemplate(3, MyCouponsComponent_ng_container_1_ng_template_3_Template, 4, 3, "ng-template", null, 1, ɵɵtemplateRefExtractor);
    ɵɵelementContainerEnd();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext();
    ɵɵadvance();
    ɵɵproperty("ngIf", ɵɵpipeBind1(2, 1, ctx_r1.couponResult$));
  }
}
function MyCouponsComponent_ng_template_3_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div", 24);
    ɵɵelement(1, "cx-spinner");
    ɵɵelementEnd();
  }
}
var _c45 = (a0) => ({
  label: a0
});
var _c46 = (a0) => ({
  cxRoute: "product",
  params: a0
});
var _c47 = (a0) => ({
  code: a0
});
var _c48 = (a0) => ({
  expirationDate: a0
});
function MyInterestsComponent_div_0_div_5_ng_container_1_thead_16_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "thead", 21)(1, "th", 22);
    ɵɵtext(2);
    ɵɵpipe(3, "cxTranslate");
    ɵɵelementEnd();
    ɵɵelementStart(4, "th", 22);
    ɵɵtext(5);
    ɵɵpipe(6, "cxTranslate");
    ɵɵelementEnd();
    ɵɵelementStart(7, "th", 22);
    ɵɵtext(8);
    ɵɵpipe(9, "cxTranslate");
    ɵɵelementEnd();
    ɵɵelementStart(10, "th", 23);
    ɵɵtext(11);
    ɵɵpipe(12, "cxTranslate");
    ɵɵelementEnd()();
  }
  if (rf & 2) {
    ɵɵadvance(2);
    ɵɵtextInterpolate1(" ", ɵɵpipeBind1(3, 4, "myInterests.item"), " ");
    ɵɵadvance(3);
    ɵɵtextInterpolate1(" ", ɵɵpipeBind1(6, 6, "myInterests.price"), " ");
    ɵɵadvance(3);
    ɵɵtextInterpolate1(" ", ɵɵpipeBind1(9, 8, "myInterests.notifications"), " ");
    ɵɵadvance(3);
    ɵɵtextInterpolate1(" ", ɵɵpipeBind1(12, 10, "myInterests.actions"), " ");
  }
}
function MyInterestsComponent_div_0_div_5_ng_container_1_thead_17_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "thead", 21)(1, "tr")(2, "th");
    ɵɵtext(3);
    ɵɵpipe(4, "cxTranslate");
    ɵɵelementEnd();
    ɵɵelementStart(5, "th");
    ɵɵtext(6);
    ɵɵpipe(7, "cxTranslate");
    ɵɵelementEnd();
    ɵɵelementStart(8, "th");
    ɵɵtext(9);
    ɵɵpipe(10, "cxTranslate");
    ɵɵelementEnd();
    ɵɵelementStart(11, "th");
    ɵɵtext(12);
    ɵɵpipe(13, "cxTranslate");
    ɵɵelementEnd()()();
  }
  if (rf & 2) {
    ɵɵadvance(3);
    ɵɵtextInterpolate1(" ", ɵɵpipeBind1(4, 4, "myInterests.item"), " ");
    ɵɵadvance(3);
    ɵɵtextInterpolate1(" ", ɵɵpipeBind1(7, 6, "myInterests.price"), " ");
    ɵɵadvance(3);
    ɵɵtextInterpolate1(" ", ɵɵpipeBind1(10, 8, "myInterests.notifications"), " ");
    ɵɵadvance(3);
    ɵɵtextInterpolate1(" ", ɵɵpipeBind1(13, 10, "myInterests.actions"), " ");
  }
}
function MyInterestsComponent_div_0_div_5_ng_container_1_tr_19_ng_container_1_div_10_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div", 43)(1, "a", 44);
    ɵɵpipe(2, "cxUrl");
    ɵɵtext(3);
    ɵɵelementEnd()();
  }
  if (rf & 2) {
    const product_r4 = ɵɵnextContext().ngIf;
    ɵɵadvance();
    ɵɵproperty("routerLink", ɵɵpipeBind1(2, 2, ɵɵpureFunction1(4, _c46, product_r4)));
    ɵɵadvance(2);
    ɵɵtextInterpolate1(" ", product_r4.name, " ");
  }
}
function MyInterestsComponent_div_0_div_5_ng_container_1_tr_19_ng_container_1_div_11_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div", 45)(1, "span");
    ɵɵtext(2);
    ɵɵpipe(3, "cxTranslate");
    ɵɵelementEnd()();
  }
  if (rf & 2) {
    const product_r4 = ɵɵnextContext().ngIf;
    ɵɵadvance(2);
    ɵɵtextInterpolate(ɵɵpipeBind2(3, 1, "myInterests.productId", ɵɵpureFunction1(4, _c47, product_r4.code)));
  }
}
function MyInterestsComponent_div_0_div_5_ng_container_1_tr_19_ng_container_1_ng_container_12_div_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div", 47)(1, "div", 48);
    ɵɵtext(2);
    ɵɵelementEnd();
    ɵɵelementStart(3, "div", 49);
    ɵɵtext(4);
    ɵɵelementEnd()();
  }
  if (rf & 2) {
    const variant_r5 = ctx.$implicit;
    ɵɵadvance(2);
    ɵɵtextInterpolate1(" ", variant_r5.name, " ");
    ɵɵadvance(2);
    ɵɵtextInterpolate1(" ", variant_r5.value, " ");
  }
}
function MyInterestsComponent_div_0_div_5_ng_container_1_tr_19_ng_container_1_ng_container_12_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainerStart(0);
    ɵɵtemplate(1, MyInterestsComponent_div_0_div_5_ng_container_1_tr_19_ng_container_1_ng_container_12_div_1_Template, 5, 2, "div", 46);
    ɵɵelementContainerEnd();
  }
  if (rf & 2) {
    const baseOptions_r6 = ctx.$implicit;
    ɵɵadvance();
    ɵɵproperty("ngForOf", baseOptions_r6.selected == null ? null : baseOptions_r6.selected.variantOptionQualifiers);
  }
}
function MyInterestsComponent_div_0_div_5_ng_container_1_tr_19_ng_container_1_div_13_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div", 47)(1, "div", 50);
    ɵɵtext(2);
    ɵɵpipe(3, "cxTranslate");
    ɵɵelementEnd()();
  }
  if (rf & 2) {
    ɵɵadvance(2);
    ɵɵtextInterpolate1(" ", ɵɵpipeBind1(3, 1, "myInterests.outOfStock"), " ");
  }
}
function MyInterestsComponent_div_0_div_5_ng_container_1_tr_19_ng_container_1_div_26_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div", 51)(1, "span", 52);
    ɵɵtext(2);
    ɵɵpipe(3, "cxTranslate");
    ɵɵelementEnd();
    ɵɵelementStart(4, "span", 53);
    ɵɵtext(5);
    ɵɵpipe(6, "cxDate");
    ɵɵpipe(7, "cxTranslate");
    ɵɵelementEnd()();
  }
  if (rf & 2) {
    const interestEntry_r7 = ctx.$implicit;
    ɵɵadvance(2);
    ɵɵtextInterpolate1(" ", ɵɵpipeBind1(3, 2, "myInterests." + interestEntry_r7.interestType), " ");
    ɵɵadvance(3);
    ɵɵtextInterpolate1(" ", ɵɵpipeBind2(7, 6, "myInterests.expirationDate", ɵɵpureFunction1(9, _c48, ɵɵpipeBind1(6, 4, interestEntry_r7.expirationDate))), " ");
  }
}
function MyInterestsComponent_div_0_div_5_ng_container_1_tr_19_ng_container_1_Template(rf, ctx) {
  if (rf & 1) {
    const _r3 = ɵɵgetCurrentView();
    ɵɵelementContainerStart(0);
    ɵɵelementStart(1, "td", 26)(2, "div", 27)(3, "div", 28)(4, "a", 29);
    ɵɵpipe(5, "cxUrl");
    ɵɵelement(6, "cx-media", 30);
    ɵɵelementEnd()();
    ɵɵelementStart(7, "div", 31)(8, "div", 32)(9, "div");
    ɵɵtemplate(10, MyInterestsComponent_div_0_div_5_ng_container_1_tr_19_ng_container_1_div_10_Template, 4, 6, "div", 33)(11, MyInterestsComponent_div_0_div_5_ng_container_1_tr_19_ng_container_1_div_11_Template, 4, 6, "div", 34)(12, MyInterestsComponent_div_0_div_5_ng_container_1_tr_19_ng_container_1_ng_container_12_Template, 2, 1, "ng-container", 35)(13, MyInterestsComponent_div_0_div_5_ng_container_1_tr_19_ng_container_1_div_13_Template, 4, 3, "div", 36);
    ɵɵelementEnd()()()()();
    ɵɵelementStart(14, "td", 26)(15, "div", 37)(16, "div", 38);
    ɵɵtext(17);
    ɵɵpipe(18, "cxTranslate");
    ɵɵelementEnd();
    ɵɵelementStart(19, "span");
    ɵɵtext(20);
    ɵɵelementEnd()()();
    ɵɵelementStart(21, "td", 26)(22, "div", 39)(23, "div", 38);
    ɵɵtext(24);
    ɵɵpipe(25, "cxTranslate");
    ɵɵelementEnd();
    ɵɵtemplate(26, MyInterestsComponent_div_0_div_5_ng_container_1_tr_19_ng_container_1_div_26_Template, 8, 11, "div", 40);
    ɵɵelementEnd()();
    ɵɵelementStart(27, "td", 26)(28, "div", 41)(29, "button", 42);
    ɵɵpipe(30, "cxTranslate");
    ɵɵpipe(31, "async");
    ɵɵlistener("click", function MyInterestsComponent_div_0_div_5_ng_container_1_tr_19_ng_container_1_Template_button_click_29_listener() {
      ɵɵrestoreView(_r3);
      const interest_r8 = ɵɵnextContext().$implicit;
      const ctx_r1 = ɵɵnextContext(4);
      return ɵɵresetView(ctx_r1.removeInterest(interest_r8));
    });
    ɵɵtext(32);
    ɵɵpipe(33, "cxTranslate");
    ɵɵelementEnd()()();
    ɵɵelementContainerEnd();
  }
  if (rf & 2) {
    const product_r4 = ctx.ngIf;
    const interest_r8 = ɵɵnextContext().$implicit;
    const ctx_r1 = ɵɵnextContext(4);
    ɵɵadvance(4);
    ɵɵproperty("routerLink", ɵɵpipeBind1(5, 13, ɵɵpureFunction1(25, _c46, product_r4)));
    ɵɵadvance(2);
    ɵɵproperty("container", product_r4.images == null ? null : product_r4.images.PRIMARY);
    ɵɵadvance(4);
    ɵɵproperty("ngIf", product_r4.name);
    ɵɵadvance();
    ɵɵproperty("ngIf", product_r4.code);
    ɵɵadvance();
    ɵɵproperty("ngForOf", product_r4.baseOptions);
    ɵɵadvance();
    ɵɵproperty("ngIf", (product_r4.stock == null ? null : product_r4.stock.stockLevelStatus) === "outOfStock");
    ɵɵadvance(4);
    ɵɵtextInterpolate1(" ", ɵɵpipeBind1(18, 15, "myInterests.price"), " ");
    ɵɵadvance(3);
    ɵɵtextInterpolate(product_r4.price == null ? null : product_r4.price.formattedValue);
    ɵɵadvance(4);
    ɵɵtextInterpolate1(" ", ɵɵpipeBind1(25, 17, "myInterests.notifications"), " ");
    ɵɵadvance(2);
    ɵɵproperty("ngForOf", interest_r8.productInterestEntry);
    ɵɵadvance(3);
    ɵɵproperty("cxAtMessage", ɵɵpipeBind1(30, 19, "myInterests.itemRemoved"))("disabled", ɵɵpipeBind1(31, 21, ctx_r1.isRemoveDisabled$));
    ɵɵadvance(3);
    ɵɵtextInterpolate1(" ", ɵɵpipeBind1(33, 23, "myInterests.remove"), " ");
  }
}
function MyInterestsComponent_div_0_div_5_ng_container_1_tr_19_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "tr", 24);
    ɵɵtemplate(1, MyInterestsComponent_div_0_div_5_ng_container_1_tr_19_ng_container_1_Template, 34, 27, "ng-container", 25);
    ɵɵpipe(2, "async");
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const interest_r8 = ctx.$implicit;
    ɵɵadvance();
    ɵɵproperty("ngIf", ɵɵpipeBind1(2, 1, interest_r8.product$));
  }
}
function MyInterestsComponent_div_0_div_5_ng_container_1_Template(rf, ctx) {
  if (rf & 1) {
    const _r1 = ɵɵgetCurrentView();
    ɵɵelementContainerStart(0);
    ɵɵelementStart(1, "div", 8)(2, "label", 9)(3, "span");
    ɵɵtext(4);
    ɵɵpipe(5, "cxTranslate");
    ɵɵelementEnd();
    ɵɵelementStart(6, "cx-sorting", 10);
    ɵɵpipe(7, "cxTranslate");
    ɵɵpipe(8, "async");
    ɵɵpipe(9, "cxTranslate");
    ɵɵlistener("sortListEvent", function MyInterestsComponent_div_0_div_5_ng_container_1_Template_cx_sorting_sortListEvent_6_listener($event) {
      ɵɵrestoreView(_r1);
      const ctx_r1 = ɵɵnextContext(3);
      return ɵɵresetView(ctx_r1.sortChange($event));
    });
    ɵɵelementEnd()();
    ɵɵelementStart(10, "div", 11)(11, "cx-pagination", 12);
    ɵɵlistener("viewPageEvent", function MyInterestsComponent_div_0_div_5_ng_container_1_Template_cx_pagination_viewPageEvent_11_listener($event) {
      ɵɵrestoreView(_r1);
      const ctx_r1 = ɵɵnextContext(3);
      return ɵɵresetView(ctx_r1.pageChange($event));
    });
    ɵɵelementEnd()()();
    ɵɵelementStart(12, "table", 13)(13, "caption", 14);
    ɵɵtext(14);
    ɵɵpipe(15, "cxTranslate");
    ɵɵelementEnd();
    ɵɵtemplate(16, MyInterestsComponent_div_0_div_5_ng_container_1_thead_16_Template, 13, 12, "thead", 15)(17, MyInterestsComponent_div_0_div_5_ng_container_1_thead_17_Template, 14, 12, "thead", 15);
    ɵɵelementStart(18, "tbody");
    ɵɵtemplate(19, MyInterestsComponent_div_0_div_5_ng_container_1_tr_19_Template, 3, 3, "tr", 16);
    ɵɵelementEnd()();
    ɵɵelementStart(20, "div", 17)(21, "label", 18)(22, "span");
    ɵɵtext(23);
    ɵɵpipe(24, "cxTranslate");
    ɵɵelementEnd();
    ɵɵelementStart(25, "cx-sorting", 10);
    ɵɵpipe(26, "cxTranslate");
    ɵɵpipe(27, "async");
    ɵɵpipe(28, "cxTranslate");
    ɵɵlistener("sortListEvent", function MyInterestsComponent_div_0_div_5_ng_container_1_Template_cx_sorting_sortListEvent_25_listener($event) {
      ɵɵrestoreView(_r1);
      const ctx_r1 = ɵɵnextContext(3);
      return ɵɵresetView(ctx_r1.sortChange($event));
    });
    ɵɵelementEnd()();
    ɵɵelementStart(29, "div", 19)(30, "cx-pagination", 20);
    ɵɵlistener("viewPageEvent", function MyInterestsComponent_div_0_div_5_ng_container_1_Template_cx_pagination_viewPageEvent_30_listener($event) {
      ɵɵrestoreView(_r1);
      const ctx_r1 = ɵɵnextContext(3);
      return ɵɵresetView(ctx_r1.pageChange($event));
    });
    ɵɵelementEnd()()();
    ɵɵelementContainerEnd();
  }
  if (rf & 2) {
    const interests_r9 = ɵɵnextContext(2).ngIf;
    const ctx_r1 = ɵɵnextContext();
    ɵɵadvance(4);
    ɵɵtextInterpolate(ɵɵpipeBind1(5, 18, "myInterests.sortBy"));
    ɵɵadvance(2);
    ɵɵpropertyInterpolate("placeholder", ɵɵpipeBind1(7, 20, "myInterests.sortBy"));
    ɵɵproperty("sortOptions", ctx_r1.sortOptions)("sortLabels", ɵɵpipeBind1(8, 22, ctx_r1.sortLabels))("selectedOption", ctx_r1.sort)("ariaLabel", ɵɵpipeBind1(9, 24, "myInterests.sortInterests"));
    ɵɵadvance(5);
    ɵɵproperty("pagination", ctx_r1.pagination);
    ɵɵadvance(3);
    ɵɵtextInterpolate1(" ", ɵɵpipeBind1(15, 26, "myInterests.caption"), " ");
    ɵɵadvance(2);
    ɵɵproperty("cxFeature", "!a11yTableHeaderReadout");
    ɵɵadvance();
    ɵɵproperty("cxFeature", "a11yTableHeaderReadout");
    ɵɵadvance(2);
    ɵɵproperty("ngForOf", interests_r9.results);
    ɵɵadvance(4);
    ɵɵtextInterpolate(ɵɵpipeBind1(24, 28, "myInterests.sortBy"));
    ɵɵadvance(2);
    ɵɵpropertyInterpolate("placeholder", ɵɵpipeBind1(26, 30, "myInterests.sortBy"));
    ɵɵproperty("sortOptions", ctx_r1.sortOptions)("sortLabels", ɵɵpipeBind1(27, 32, ctx_r1.sortLabels))("selectedOption", ctx_r1.sort)("ariaLabel", ɵɵpipeBind1(28, 34, "myInterests.sortInterests"));
    ɵɵadvance(5);
    ɵɵproperty("pagination", ctx_r1.pagination);
  }
}
function MyInterestsComponent_div_0_div_5_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div", 6);
    ɵɵtemplate(1, MyInterestsComponent_div_0_div_5_ng_container_1_Template, 31, 36, "ng-container", 7);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const interests_r9 = ɵɵnextContext().ngIf;
    ɵɵnextContext();
    const noInterest_r10 = ɵɵreference(3);
    ɵɵadvance();
    ɵɵproperty("ngIf", interests_r9.pagination && interests_r9.pagination.totalCount && interests_r9.pagination.totalCount > 0)("ngIfElse", noInterest_r10);
  }
}
function MyInterestsComponent_div_0_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div", 3)(1, "div", 4)(2, "h2");
    ɵɵtext(3);
    ɵɵpipe(4, "cxTranslate");
    ɵɵelementEnd()();
    ɵɵtemplate(5, MyInterestsComponent_div_0_div_5_Template, 2, 2, "div", 5);
    ɵɵpipe(6, "async");
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext();
    const loading_r11 = ɵɵreference(5);
    ɵɵadvance(3);
    ɵɵtextInterpolate(ɵɵpipeBind1(4, 3, "myInterests.header"));
    ɵɵadvance(2);
    ɵɵproperty("ngIf", !ɵɵpipeBind1(6, 5, ctx_r1.getInterestsloading$))("ngIfElse", loading_r11);
  }
}
function MyInterestsComponent_ng_template_2_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div", 54);
    ɵɵtext(1);
    ɵɵpipe(2, "cxTranslate");
    ɵɵelementEnd();
  }
  if (rf & 2) {
    ɵɵadvance();
    ɵɵtextInterpolate1(" ", ɵɵpipeBind1(2, 1, "myInterests.noInterests"), " ");
  }
}
function MyInterestsComponent_ng_template_4_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div", 55);
    ɵɵelement(1, "cx-spinner");
    ɵɵelementEnd();
  }
}
function NotificationPreferenceComponent_ng_container_0_div_1_ng_container_7_div_1_Template(rf, ctx) {
  if (rf & 1) {
    const _r1 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "div", 9)(1, "input", 10);
    ɵɵpipe(2, "async");
    ɵɵlistener("change", function NotificationPreferenceComponent_ng_container_0_div_1_ng_container_7_div_1_Template_input_change_1_listener() {
      ɵɵrestoreView(_r1);
      const preference_r2 = ɵɵnextContext().$implicit;
      const ctx_r2 = ɵɵnextContext(3);
      return ɵɵresetView(ctx_r2.updatePreference(preference_r2));
    });
    ɵɵelementEnd();
    ɵɵelementStart(3, "label", 11);
    ɵɵtext(4);
    ɵɵpipe(5, "cxTranslate");
    ɵɵelementEnd()();
  }
  if (rf & 2) {
    const preference_r2 = ɵɵnextContext().$implicit;
    const ctx_r2 = ɵɵnextContext(3);
    ɵɵadvance();
    ɵɵpropertyInterpolate("id", preference_r2.channel);
    ɵɵproperty("checked", preference_r2.enabled)("disabled", ɵɵpipeBind1(2, 6, ctx_r2.isLoading$));
    ɵɵadvance(2);
    ɵɵpropertyInterpolate("for", preference_r2.channel);
    ɵɵadvance();
    ɵɵtextInterpolate2(" ", ɵɵpipeBind1(5, 8, "notificationPreference." + preference_r2.channel), " ", preference_r2.value, " ");
  }
}
function NotificationPreferenceComponent_ng_container_0_div_1_ng_container_7_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainerStart(0);
    ɵɵtemplate(1, NotificationPreferenceComponent_ng_container_0_div_1_ng_container_7_div_1_Template, 6, 10, "div", 8);
    ɵɵelementContainerEnd();
  }
  if (rf & 2) {
    const preference_r2 = ctx.$implicit;
    ɵɵadvance();
    ɵɵproperty("ngIf", preference_r2.visible);
  }
}
function NotificationPreferenceComponent_ng_container_0_div_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div")(1, "div", 3)(2, "div", 4)(3, "fieldset")(4, "legend", 5);
    ɵɵtext(5);
    ɵɵpipe(6, "cxTranslate");
    ɵɵelementEnd();
    ɵɵtemplate(7, NotificationPreferenceComponent_ng_container_0_div_1_ng_container_7_Template, 2, 1, "ng-container", 6);
    ɵɵelementEnd();
    ɵɵelementStart(8, "p", 7)(9, "strong");
    ɵɵtext(10);
    ɵɵpipe(11, "cxTranslate");
    ɵɵelementEnd();
    ɵɵtext(12);
    ɵɵpipe(13, "cxTranslate");
    ɵɵelementEnd()()()();
  }
  if (rf & 2) {
    const preferences_r4 = ɵɵnextContext().ngIf;
    ɵɵadvance(5);
    ɵɵtextInterpolate1(" ", ɵɵpipeBind1(6, 4, "notificationPreference.message"), " ");
    ɵɵadvance(2);
    ɵɵproperty("ngForOf", preferences_r4);
    ɵɵadvance(3);
    ɵɵtextInterpolate(ɵɵpipeBind1(11, 6, "notificationPreference.note"));
    ɵɵadvance(2);
    ɵɵtextInterpolate1("", ɵɵpipeBind1(13, 8, "notificationPreference.noteMessage"), " ");
  }
}
function NotificationPreferenceComponent_ng_container_0_ng_template_2_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div", 12);
    ɵɵelement(1, "cx-spinner");
    ɵɵelementEnd();
  }
}
function NotificationPreferenceComponent_ng_container_0_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainerStart(0);
    ɵɵtemplate(1, NotificationPreferenceComponent_ng_container_0_div_1_Template, 14, 10, "div", 2)(2, NotificationPreferenceComponent_ng_container_0_ng_template_2_Template, 2, 0, "ng-template", null, 0, ɵɵtemplateRefExtractor);
    ɵɵelementContainerEnd();
  }
  if (rf & 2) {
    const preferences_r4 = ctx.ngIf;
    const loading_r5 = ɵɵreference(3);
    ɵɵadvance();
    ɵɵproperty("ngIf", preferences_r4.length > 0)("ngIfElse", loading_r5);
  }
}
function BreadcrumbComponent_li_4_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "li");
    ɵɵelement(1, "a", 2);
    ɵɵpipe(2, "cxTranslate");
    ɵɵpipe(3, "cxTranslate");
    ɵɵpipe(4, "cxTranslate");
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const crumb_r1 = ctx.$implicit;
    const crumbList_r2 = ctx.ngForOf;
    const i_r3 = ctx.index;
    const last_r4 = ctx.last;
    ɵɵadvance();
    ɵɵproperty("routerLink", crumb_r1.link)("innerHTML", crumb_r1.label, ɵɵsanitizeHtml);
    ɵɵattribute("aria-label", last_r4 ? ɵɵpipeBind1(2, 3, "common.breadcrumbsCurrentPage") + " " + crumb_r1.label + (i_r3 + 1) + ɵɵpipeBind1(3, 5, "common.of") + crumbList_r2.length : crumb_r1.label + (i_r3 + 1) + ɵɵpipeBind1(4, 7, "common.of") + crumbList_r2.length);
  }
}
var _c49 = (a0) => ({
  node: a0,
  depth: 0
});
var _c50 = (a0, a1, a2) => ({
  title: a0,
  position: a1,
  listLength: a2
});
var _c51 = (a0) => ({
  title: a0
});
var _c522 = (a0, a1, a2, a3) => ({
  node: a0,
  depth: a1,
  listLength: a2,
  position: a3
});
function NavigationUIComponent_li_2_Template(rf, ctx) {
  if (rf & 1) {
    const _r1 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "li", 6)(1, "button", 7);
    ɵɵlistener("click", function NavigationUIComponent_li_2_Template_button_click_1_listener() {
      ɵɵrestoreView(_r1);
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.back());
    });
    ɵɵelement(2, "cx-icon", 8);
    ɵɵtext(3);
    ɵɵpipe(4, "cxTranslate");
    ɵɵelementEnd()();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext();
    ɵɵadvance(2);
    ɵɵproperty("type", ctx_r1.iconType.CARET_LEFT);
    ɵɵadvance();
    ɵɵtextInterpolate1(" ", ɵɵpipeBind1(4, 2, "common.back"), " ");
  }
}
function NavigationUIComponent_ng_container_3_ng_container_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainer(0);
  }
}
function NavigationUIComponent_ng_container_3_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainerStart(0);
    ɵɵtemplate(1, NavigationUIComponent_ng_container_3_ng_container_1_Template, 1, 0, "ng-container", 9);
    ɵɵelementContainerEnd();
  }
  if (rf & 2) {
    const child_r3 = ctx.$implicit;
    ɵɵnextContext();
    const nav_r4 = ɵɵreference(5);
    ɵɵadvance();
    ɵɵproperty("ngTemplateOutlet", nav_r4)("ngTemplateOutletContext", ɵɵpureFunction1(2, _c49, child_r3));
  }
}
function NavigationUIComponent_ng_template_4_cx_generic_link_1_Template(rf, ctx) {
  if (rf & 1) {
    const _r5 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "cx-generic-link", 12);
    ɵɵpipe(1, "cxTranslate");
    ɵɵlistener("click", function NavigationUIComponent_ng_template_4_cx_generic_link_1_Template_cx_generic_link_click_0_listener() {
      ɵɵrestoreView(_r5);
      const node_r6 = ɵɵnextContext().node;
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.closeIfClickedTheSameLink(node_r6));
    })("focus", function NavigationUIComponent_ng_template_4_cx_generic_link_1_Template_cx_generic_link_focus_0_listener() {
      ɵɵrestoreView(_r5);
      const depth_r7 = ɵɵnextContext().depth;
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(depth_r7 || ctx_r1.reinitializeMenu());
    })("keydown.space", function NavigationUIComponent_ng_template_4_cx_generic_link_1_Template_cx_generic_link_keydown_space_0_listener($event) {
      ɵɵrestoreView(_r5);
      const ctx_r1 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r1.toggleOpen($event));
    })("keydown.esc", function NavigationUIComponent_ng_template_4_cx_generic_link_1_Template_cx_generic_link_keydown_esc_0_listener() {
      ɵɵrestoreView(_r5);
      const ctx_r1 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r1.back());
    });
    ɵɵtext(2);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r7 = ɵɵnextContext();
    const node_r6 = ctx_r7.node;
    const depth_r7 = ctx_r7.depth;
    const listLength_r9 = ctx_r7.listLength;
    const position_r10 = ctx_r7.position;
    const ctx_r1 = ɵɵnextContext();
    ɵɵstyleMap(node_r6.styleAttributes);
    ɵɵclassMap(node_r6.styleClasses);
    ɵɵproperty("ariaLabel", ɵɵpipeBind2(1, 9, "navigation.linkItemInList", ɵɵpureFunction3(12, _c50, node_r6.title, position_r10, listLength_r9)))("url", node_r6.url)("target", node_r6.target)("tabindex", ctx_r1.getTabIndex(node_r6, depth_r7));
    ɵɵadvance(2);
    ɵɵtextInterpolate1(" ", node_r6.title, " ");
  }
}
function NavigationUIComponent_ng_template_4_ng_template_2_ng_container_0_cx_generic_link_1_Template(rf, ctx) {
  if (rf & 1) {
    const _r11 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "cx-generic-link", 16);
    ɵɵlistener("click", function NavigationUIComponent_ng_template_4_ng_template_2_ng_container_0_cx_generic_link_1_Template_cx_generic_link_click_0_listener() {
      ɵɵrestoreView(_r11);
      const node_r6 = ɵɵnextContext(3).node;
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.closeIfClickedTheSameLink(node_r6));
    });
    ɵɵtext(1);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const node_r6 = ɵɵnextContext(3).node;
    ɵɵproperty("url", node_r6.url)("target", node_r6.target);
    ɵɵadvance();
    ɵɵtextInterpolate1(" ", node_r6.title, " ");
  }
}
function NavigationUIComponent_ng_template_4_ng_template_2_ng_container_0_ng_container_2_ng_container_3_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainerStart(0);
    ɵɵtext(1);
    ɵɵelementContainerEnd();
  }
  if (rf & 2) {
    const node_r6 = ɵɵnextContext(4).node;
    ɵɵadvance();
    ɵɵtextInterpolate1(" ", node_r6.title, " ");
  }
}
function NavigationUIComponent_ng_template_4_ng_template_2_ng_container_0_ng_container_2_Template(rf, ctx) {
  if (rf & 1) {
    const _r12 = ɵɵgetCurrentView();
    ɵɵelementContainerStart(0);
    ɵɵelementStart(1, "button", 17);
    ɵɵpipe(2, "async");
    ɵɵlistener("click", function NavigationUIComponent_ng_template_4_ng_template_2_ng_container_0_ng_container_2_Template_button_click_1_listener($event) {
      ɵɵrestoreView(_r12);
      const ctx_r1 = ɵɵnextContext(4);
      return ɵɵresetView(ctx_r1.toggleOpen($event));
    })("mouseenter", function NavigationUIComponent_ng_template_4_ng_template_2_ng_container_0_ng_container_2_Template_button_mouseenter_1_listener($event) {
      ɵɵrestoreView(_r12);
      const ctx_r1 = ɵɵnextContext(4);
      return ɵɵresetView(ctx_r1.onMouseEnter($event));
    })("keydown.space", function NavigationUIComponent_ng_template_4_ng_template_2_ng_container_0_ng_container_2_Template_button_keydown_space_1_listener($event) {
      ɵɵrestoreView(_r12);
      const ctx_r1 = ɵɵnextContext(4);
      return ɵɵresetView(ctx_r1.onSpace($event));
    })("keydown.enter", function NavigationUIComponent_ng_template_4_ng_template_2_ng_container_0_ng_container_2_Template_button_keydown_enter_1_listener($event) {
      ɵɵrestoreView(_r12);
      const ctx_r1 = ɵɵnextContext(4);
      return ɵɵresetView(ctx_r1.onSpace($event));
    })("keydown.esc", function NavigationUIComponent_ng_template_4_ng_template_2_ng_container_0_ng_container_2_Template_button_keydown_esc_1_listener() {
      ɵɵrestoreView(_r12);
      const ctx_r1 = ɵɵnextContext(4);
      return ɵɵresetView(ctx_r1.back());
    })("keydown.arrowDown", function NavigationUIComponent_ng_template_4_ng_template_2_ng_container_0_ng_container_2_Template_button_keydown_arrowDown_1_listener($event) {
      ɵɵrestoreView(_r12);
      const ctx_r1 = ɵɵnextContext(4);
      return ɵɵresetView(ctx_r1.focusOnNode($event));
    })("focus", function NavigationUIComponent_ng_template_4_ng_template_2_ng_container_0_ng_container_2_Template_button_focus_1_listener() {
      ɵɵrestoreView(_r12);
      const depth_r7 = ɵɵnextContext(3).depth;
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(depth_r7 || ctx_r1.reinitializeMenu());
    });
    ɵɵtemplate(3, NavigationUIComponent_ng_template_4_ng_template_2_ng_container_0_ng_container_2_ng_container_3_Template, 2, 1, "ng-container", 18);
    ɵɵelement(4, "cx-icon", 8);
    ɵɵelementEnd();
    ɵɵelementContainerEnd();
  }
  if (rf & 2) {
    const ctx_r7 = ɵɵnextContext(3);
    const node_r6 = ctx_r7.node;
    const depth_r7 = ctx_r7.depth;
    const ctx_r1 = ɵɵnextContext();
    ɵɵadvance();
    ɵɵattribute("role", ɵɵpipeBind1(2, 7, ctx_r1.isDesktop$) && depth_r7 ? "heading" : "button")("aria-haspopup", true)("aria-expanded", false)("aria-controls", node_r6.title)("aria-describedby", "greeting");
    ɵɵadvance(2);
    ɵɵproperty("ngIf", !node_r6.url);
    ɵɵadvance();
    ɵɵproperty("type", ctx_r1.iconType.CARET_DOWN);
  }
}
function NavigationUIComponent_ng_template_4_ng_template_2_ng_container_0_ng_container_3_h4_1_Template(rf, ctx) {
  if (rf & 1) {
    const _r13 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "h4", 20);
    ɵɵlistener("keydown.arrowDown", function NavigationUIComponent_ng_template_4_ng_template_2_ng_container_0_ng_container_3_h4_1_Template_h4_keydown_arrowDown_0_listener($event) {
      ɵɵrestoreView(_r13);
      const ctx_r1 = ɵɵnextContext(5);
      return ɵɵresetView(ctx_r1.focusOnNode($event));
    })("keydown.space", function NavigationUIComponent_ng_template_4_ng_template_2_ng_container_0_ng_container_3_h4_1_Template_h4_keydown_space_0_listener($event) {
      ɵɵrestoreView(_r13);
      const ctx_r1 = ɵɵnextContext(5);
      return ɵɵresetView(ctx_r1.onSpace($event));
    });
    ɵɵtext(1);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const node_r6 = ɵɵnextContext(4).node;
    ɵɵadvance();
    ɵɵtextInterpolate1(" ", node_r6.title, " ");
  }
}
function NavigationUIComponent_ng_template_4_ng_template_2_ng_container_0_ng_container_3_ng_template_3_ng_container_2_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainerStart(0);
    ɵɵtext(1);
    ɵɵelementContainerEnd();
  }
  if (rf & 2) {
    const node_r6 = ɵɵnextContext(5).node;
    ɵɵadvance();
    ɵɵtextInterpolate1(" ", node_r6.title, " ");
  }
}
function NavigationUIComponent_ng_template_4_ng_template_2_ng_container_0_ng_container_3_ng_template_3_Template(rf, ctx) {
  if (rf & 1) {
    const _r14 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "button", 21);
    ɵɵpipe(1, "cxTranslate");
    ɵɵlistener("click", function NavigationUIComponent_ng_template_4_ng_template_2_ng_container_0_ng_container_3_ng_template_3_Template_button_click_0_listener($event) {
      ɵɵrestoreView(_r14);
      const ctx_r1 = ɵɵnextContext(5);
      return ɵɵresetView(ctx_r1.toggleOpen($event));
    })("mouseenter", function NavigationUIComponent_ng_template_4_ng_template_2_ng_container_0_ng_container_3_ng_template_3_Template_button_mouseenter_0_listener($event) {
      ɵɵrestoreView(_r14);
      const ctx_r1 = ɵɵnextContext(5);
      return ɵɵresetView(ctx_r1.onMouseEnter($event));
    })("keydown.space", function NavigationUIComponent_ng_template_4_ng_template_2_ng_container_0_ng_container_3_ng_template_3_Template_button_keydown_space_0_listener($event) {
      ɵɵrestoreView(_r14);
      const ctx_r1 = ɵɵnextContext(5);
      return ɵɵresetView(ctx_r1.onSpace($event));
    })("keydown.enter", function NavigationUIComponent_ng_template_4_ng_template_2_ng_container_0_ng_container_3_ng_template_3_Template_button_keydown_enter_0_listener($event) {
      ɵɵrestoreView(_r14);
      const ctx_r1 = ɵɵnextContext(5);
      return ɵɵresetView(ctx_r1.onSpace($event));
    })("keydown.esc", function NavigationUIComponent_ng_template_4_ng_template_2_ng_container_0_ng_container_3_ng_template_3_Template_button_keydown_esc_0_listener() {
      ɵɵrestoreView(_r14);
      const ctx_r1 = ɵɵnextContext(5);
      return ɵɵresetView(ctx_r1.back());
    })("keydown.arrowDown", function NavigationUIComponent_ng_template_4_ng_template_2_ng_container_0_ng_container_3_ng_template_3_Template_button_keydown_arrowDown_0_listener($event) {
      ɵɵrestoreView(_r14);
      const ctx_r1 = ɵɵnextContext(5);
      return ɵɵresetView(ctx_r1.focusOnNode($event));
    })("focus", function NavigationUIComponent_ng_template_4_ng_template_2_ng_container_0_ng_container_3_ng_template_3_Template_button_focus_0_listener() {
      ɵɵrestoreView(_r14);
      const depth_r7 = ɵɵnextContext(4).depth;
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(depth_r7 || ctx_r1.reinitializeMenu());
    });
    ɵɵtemplate(2, NavigationUIComponent_ng_template_4_ng_template_2_ng_container_0_ng_container_3_ng_template_3_ng_container_2_Template, 2, 1, "ng-container", 18);
    ɵɵelement(3, "cx-icon", 8);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const node_r6 = ɵɵnextContext(4).node;
    const ctx_r1 = ɵɵnextContext();
    ɵɵattribute("aria-haspopup", true)("aria-expanded", false)("aria-label", node_r6.url ? node_r6.title : null)("aria-controls", ctx_r1.transformIntoValidID(node_r6.title))("title", ɵɵpipeBind2(1, 7, "navigation.menuButonTitle", ɵɵpureFunction1(10, _c51, node_r6.title)));
    ɵɵadvance(2);
    ɵɵproperty("ngIf", !node_r6.url);
    ɵɵadvance();
    ɵɵproperty("type", ctx_r1.iconType.CARET_DOWN);
  }
}
function NavigationUIComponent_ng_template_4_ng_template_2_ng_container_0_ng_container_3_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainerStart(0);
    ɵɵtemplate(1, NavigationUIComponent_ng_template_4_ng_template_2_ng_container_0_ng_container_3_h4_1_Template, 2, 1, "h4", 19);
    ɵɵpipe(2, "async");
    ɵɵtemplate(3, NavigationUIComponent_ng_template_4_ng_template_2_ng_container_0_ng_container_3_ng_template_3_Template, 4, 12, "ng-template", null, 3, ɵɵtemplateRefExtractor);
    ɵɵelementContainerEnd();
  }
  if (rf & 2) {
    const dropdownHeader_r15 = ɵɵreference(4);
    const depth_r7 = ɵɵnextContext(3).depth;
    const ctx_r1 = ɵɵnextContext();
    ɵɵadvance();
    ɵɵproperty("ngIf", ɵɵpipeBind1(2, 2, ctx_r1.isDesktop$) && depth_r7)("ngIfElse", dropdownHeader_r15);
  }
}
function NavigationUIComponent_ng_template_4_ng_template_2_ng_container_0_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainerStart(0);
    ɵɵtemplate(1, NavigationUIComponent_ng_template_4_ng_template_2_ng_container_0_cx_generic_link_1_Template, 2, 3, "cx-generic-link", 14)(2, NavigationUIComponent_ng_template_4_ng_template_2_ng_container_0_ng_container_2_Template, 5, 9, "ng-container", 15)(3, NavigationUIComponent_ng_template_4_ng_template_2_ng_container_0_ng_container_3_Template, 5, 4, "ng-container", 15);
    ɵɵelementContainerEnd();
  }
  if (rf & 2) {
    const node_r6 = ɵɵnextContext(2).node;
    ɵɵadvance();
    ɵɵproperty("ngIf", node_r6.url);
    ɵɵadvance();
    ɵɵproperty("cxFeature", "!a11yNavigationButtonsAriaFixes");
    ɵɵadvance();
    ɵɵproperty("cxFeature", "a11yNavigationButtonsAriaFixes");
  }
}
function NavigationUIComponent_ng_template_4_ng_template_2_ng_template_1_span_0_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "span", 23);
    ɵɵtext(1);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const node_r6 = ɵɵnextContext(3).node;
    const ctx_r1 = ɵɵnextContext();
    ɵɵattribute("tabindex", ctx_r1.focusableNodeTitles ? 0 : -1);
    ɵɵadvance();
    ɵɵtextInterpolate1(" ", node_r6.title, " ");
  }
}
function NavigationUIComponent_ng_template_4_ng_template_2_ng_template_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵtemplate(0, NavigationUIComponent_ng_template_4_ng_template_2_ng_template_1_span_0_Template, 2, 2, "span", 22);
  }
  if (rf & 2) {
    const node_r6 = ɵɵnextContext(2).node;
    ɵɵproperty("ngIf", node_r6.title);
  }
}
function NavigationUIComponent_ng_template_4_ng_template_2_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵtemplate(0, NavigationUIComponent_ng_template_4_ng_template_2_ng_container_0_Template, 4, 3, "ng-container", 13)(1, NavigationUIComponent_ng_template_4_ng_template_2_ng_template_1_Template, 1, 1, "ng-template", null, 2, ɵɵtemplateRefExtractor);
  }
  if (rf & 2) {
    const title_r16 = ɵɵreference(2);
    const node_r6 = ɵɵnextContext().node;
    const ctx_r1 = ɵɵnextContext();
    ɵɵproperty("ngIf", ctx_r1.flyout && (node_r6.children == null ? null : node_r6.children.length) > 0)("ngIfElse", title_r16);
  }
}
function NavigationUIComponent_ng_template_4_div_4_ng_container_2_ng_container_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainer(0);
  }
}
function NavigationUIComponent_ng_template_4_div_4_ng_container_2_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainerStart(0);
    ɵɵtemplate(1, NavigationUIComponent_ng_template_4_div_4_ng_container_2_ng_container_1_Template, 1, 0, "ng-container", 9);
    ɵɵelementContainerEnd();
  }
  if (rf & 2) {
    const child_r17 = ctx.$implicit;
    const i_r18 = ctx.index;
    const ctx_r7 = ɵɵnextContext(2);
    const node_r6 = ctx_r7.node;
    const depth_r7 = ctx_r7.depth;
    ɵɵnextContext();
    const nav_r4 = ɵɵreference(5);
    ɵɵadvance();
    ɵɵproperty("ngTemplateOutlet", nav_r4)("ngTemplateOutletContext", ɵɵpureFunction4(2, _c522, child_r17, depth_r7 + 1, node_r6.children.length, i_r18 + 1));
  }
}
function NavigationUIComponent_ng_template_4_div_4_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div", 24)(1, "ul", 25);
    ɵɵtemplate(2, NavigationUIComponent_ng_template_4_div_4_ng_container_2_Template, 2, 7, "ng-container", 5);
    ɵɵelementEnd()();
  }
  if (rf & 2) {
    const node_r6 = ɵɵnextContext().node;
    const ctx_r1 = ɵɵnextContext();
    ɵɵproperty("id", ctx_r1.transformIntoValidID(node_r6.title));
    ɵɵadvance();
    ɵɵattribute("depth", ctx_r1.getTotalDepth(node_r6))("wrap-after", node_r6.children.length > ctx_r1.wrapAfter ? ctx_r1.wrapAfter : null)("columns", ctx_r1.getColumnCount(node_r6.children.length))("aria-label", node_r6.title);
    ɵɵadvance();
    ɵɵproperty("ngForOf", node_r6.children);
  }
}
function NavigationUIComponent_ng_template_4_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "li");
    ɵɵtemplate(1, NavigationUIComponent_ng_template_4_cx_generic_link_1_Template, 3, 16, "cx-generic-link", 10)(2, NavigationUIComponent_ng_template_4_ng_template_2_Template, 3, 2, "ng-template", null, 1, ɵɵtemplateRefExtractor)(4, NavigationUIComponent_ng_template_4_div_4_Template, 3, 6, "div", 11);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const node_r6 = ctx.node;
    const depth_r7 = ctx.depth;
    const heading_r19 = ɵɵreference(3);
    const ctx_r1 = ɵɵnextContext();
    ɵɵattribute("role", !ctx_r1.flyout && !depth_r7 ? "presentation" : "listitem");
    ɵɵadvance();
    ɵɵproperty("ngIf", node_r6.url && (!node_r6.children || (node_r6.children == null ? null : node_r6.children.length) === 0))("ngIfElse", heading_r19);
    ɵɵadvance(3);
    ɵɵproperty("ngIf", node_r6.children && node_r6.children.length > 0);
  }
}
function CategoryNavigationComponent_cx_navigation_ui_0_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "cx-navigation-ui", 1);
    ɵɵpipe(1, "async");
    ɵɵpipe(2, "cxTranslate");
  }
  if (rf & 2) {
    let tmp_3_0;
    let tmp_4_0;
    const data_r1 = ctx.ngIf;
    const ctx_r1 = ɵɵnextContext();
    ɵɵproperty("node", ɵɵpipeBind1(1, 5, ctx_r1.node$))("ngClass", (tmp_3_0 = data_r1.styleClass) !== null && tmp_3_0 !== void 0 ? tmp_3_0 : "")("wrapAfter", +((tmp_4_0 = data_r1.wrapAfter) !== null && tmp_4_0 !== void 0 ? tmp_4_0 : ""))("resetMenuOnClose", data_r1.resetMenuOnClose)("navAriaLabel", ɵɵpipeBind1(2, 7, "navigation.categoryNavLabel"));
  }
}
function FooterNavigationComponent_cx_navigation_ui_0_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "cx-navigation-ui", 1);
    ɵɵpipe(1, "async");
    ɵɵpipe(2, "cxTranslate");
  }
  if (rf & 2) {
    let tmp_4_0;
    const node_r1 = ctx.ngIf;
    const ctx_r1 = ɵɵnextContext();
    ɵɵproperty("node", node_r1)("flyout", false)("ngClass", (tmp_4_0 = ɵɵpipeBind1(1, 4, ctx_r1.styleClass$)) !== null && tmp_4_0 !== void 0 ? tmp_4_0 : "")("navAriaLabel", ɵɵpipeBind1(2, 6, "navigation.footerNavLabel"));
  }
}
var _c53 = ["searchInput"];
var _c54 = ["searchButton"];
var _c55 = (a0) => ({
  query: a0
});
var _c56 = (a0) => ({
  cxRoute: "search",
  params: a0
});
var _c57 = (a0) => ({
  maxTrendingSearches: a0
});
var _c58 = (a0, a1, a2) => ({
  search: a0,
  searchBoxActive: a1,
  maxRecentSearches: a2
});
function SearchBoxComponent_div_0_Template(rf, ctx) {
  if (rf & 1) {
    const _r1 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "div", 6);
    ɵɵpipe(1, "cxTranslate");
    ɵɵelementStart(2, "label", 7)(3, "input", 8, 0);
    ɵɵpipe(5, "cxTranslate");
    ɵɵpipe(6, "async");
    ɵɵpipe(7, "cxTranslate");
    ɵɵlistener("click", function SearchBoxComponent_div_0_Template_input_click_3_listener() {
      ɵɵrestoreView(_r1);
      const searchInput_r2 = ɵɵreference(4);
      const ctx_r2 = ɵɵnextContext();
      return ɵɵresetView(ctx_r2.search(searchInput_r2.value));
    })("input", function SearchBoxComponent_div_0_Template_input_input_3_listener() {
      ɵɵrestoreView(_r1);
      const searchInput_r2 = ɵɵreference(4);
      const ctx_r2 = ɵɵnextContext();
      return ɵɵresetView(ctx_r2.search(searchInput_r2.value));
    })("blur", function SearchBoxComponent_div_0_Template_input_blur_3_listener() {
      ɵɵrestoreView(_r1);
      const ctx_r2 = ɵɵnextContext();
      return ɵɵresetView(ctx_r2.close());
    })("keydown.tab", function SearchBoxComponent_div_0_Template_input_keydown_tab_3_listener() {
      ɵɵrestoreView(_r1);
      const ctx_r2 = ɵɵnextContext();
      return ɵɵresetView(ctx_r2.close());
    })("keydown.escape", function SearchBoxComponent_div_0_Template_input_keydown_escape_3_listener() {
      ɵɵrestoreView(_r1);
      const ctx_r2 = ɵɵnextContext();
      return ɵɵresetView(ctx_r2.close());
    })("keydown.enter", function SearchBoxComponent_div_0_Template_input_keydown_enter_3_listener() {
      ɵɵrestoreView(_r1);
      const searchInput_r2 = ɵɵreference(4);
      const ctx_r2 = ɵɵnextContext();
      ctx_r2.close(true);
      ctx_r2.launchSearchResult(searchInput_r2.value);
      return ɵɵresetView(ctx_r2.updateChosenWord(searchInput_r2.value));
    })("keydown.arrowup", function SearchBoxComponent_div_0_Template_input_keydown_arrowup_3_listener($event) {
      ɵɵrestoreView(_r1);
      const ctx_r2 = ɵɵnextContext();
      return ɵɵresetView(ctx_r2.focusPreviousChild($event));
    })("keydown.arrowdown", function SearchBoxComponent_div_0_Template_input_keydown_arrowdown_3_listener($event) {
      ɵɵrestoreView(_r1);
      const ctx_r2 = ɵɵnextContext();
      return ɵɵresetView(ctx_r2.focusNextChild($event));
    });
    ɵɵelementEnd();
    ɵɵelementStart(8, "button", 9);
    ɵɵpipe(9, "cxTranslate");
    ɵɵpipe(10, "cxTranslate");
    ɵɵlistener("click", function SearchBoxComponent_div_0_Template_button_click_8_listener() {
      ɵɵrestoreView(_r1);
      const searchInput_r2 = ɵɵreference(4);
      const ctx_r2 = ɵɵnextContext();
      return ɵɵresetView(ctx_r2.clear(searchInput_r2));
    });
    ɵɵelement(11, "cx-icon", 10);
    ɵɵelementEnd();
    ɵɵelementStart(12, "div", 11);
    ɵɵpipe(13, "cxTranslate");
    ɵɵelement(14, "cx-icon", 10);
    ɵɵelementEnd();
    ɵɵelementStart(15, "button", 12, 1);
    ɵɵpipe(17, "cxTranslate");
    ɵɵpipe(18, "cxTranslate");
    ɵɵlistener("click", function SearchBoxComponent_div_0_Template_button_click_15_listener() {
      ɵɵrestoreView(_r1);
      const ctx_r2 = ɵɵnextContext();
      return ɵɵresetView(ctx_r2.open());
    });
    ɵɵelement(19, "cx-icon", 10);
    ɵɵelementEnd()()();
  }
  if (rf & 2) {
    const searchInput_r2 = ɵɵreference(4);
    const ctx_r2 = ɵɵnextContext();
    ɵɵattribute("aria-label", ɵɵpipeBind1(1, 15, "searchBox.productSearch"));
    ɵɵadvance(2);
    ɵɵclassProp("dirty", !!searchInput_r2.value);
    ɵɵadvance();
    ɵɵpropertyInterpolate("value", ctx_r2.chosenWord);
    ɵɵproperty("placeholder", ɵɵpipeBind1(5, 17, "searchBox.placeholder"));
    ɵɵattribute("tabindex", ctx_r2.getTabIndex(ɵɵpipeBind1(6, 19, ctx_r2.isMobile)))("aria-label", ɵɵpipeBind1(7, 21, "searchBox.placeholder"));
    ɵɵadvance(5);
    ɵɵproperty("title", ɵɵpipeBind1(9, 23, "common.reset"));
    ɵɵattribute("aria-label", ɵɵpipeBind1(10, 25, "common.reset"));
    ɵɵadvance(3);
    ɵɵproperty("type", ctx_r2.iconTypes.RESET);
    ɵɵadvance();
    ɵɵproperty("title", ɵɵpipeBind1(13, 27, "common.search"));
    ɵɵadvance(2);
    ɵɵproperty("type", ctx_r2.iconTypes.SEARCH);
    ɵɵadvance();
    ɵɵproperty("title", ɵɵpipeBind1(17, 29, "common.search"));
    ɵɵattribute("aria-label", ɵɵpipeBind1(18, 31, "common.search"));
    ɵɵadvance(4);
    ɵɵproperty("type", ctx_r2.iconTypes.SEARCH);
  }
}
function SearchBoxComponent_div_1_Template(rf, ctx) {
  if (rf & 1) {
    const _r4 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "div", 13);
    ɵɵpipe(1, "cxTranslate");
    ɵɵelementStart(2, "label", 14)(3, "span", 15);
    ɵɵtext(4);
    ɵɵpipe(5, "cxTranslate");
    ɵɵelementEnd();
    ɵɵelementStart(6, "div", 16)(7, "input", 8, 0);
    ɵɵpipe(9, "cxTranslate");
    ɵɵpipe(10, "async");
    ɵɵlistener("click", function SearchBoxComponent_div_1_Template_input_click_7_listener() {
      ɵɵrestoreView(_r4);
      const ctx_r2 = ɵɵnextContext();
      return ɵɵresetView(ctx_r2.open());
    })("input", function SearchBoxComponent_div_1_Template_input_input_7_listener() {
      ɵɵrestoreView(_r4);
      const searchInput_r5 = ɵɵreference(8);
      const ctx_r2 = ɵɵnextContext();
      return ɵɵresetView(ctx_r2.search(searchInput_r5.value));
    })("blur", function SearchBoxComponent_div_1_Template_input_blur_7_listener() {
      ɵɵrestoreView(_r4);
      const ctx_r2 = ɵɵnextContext();
      return ɵɵresetView(ctx_r2.close());
    })("keydown.tab", function SearchBoxComponent_div_1_Template_input_keydown_tab_7_listener() {
      ɵɵrestoreView(_r4);
      const ctx_r2 = ɵɵnextContext();
      return ɵɵresetView(ctx_r2.close());
    })("keydown.escape", function SearchBoxComponent_div_1_Template_input_keydown_escape_7_listener() {
      ɵɵrestoreView(_r4);
      const ctx_r2 = ɵɵnextContext();
      return ɵɵresetView(ctx_r2.close());
    })("keydown.enter", function SearchBoxComponent_div_1_Template_input_keydown_enter_7_listener() {
      ɵɵrestoreView(_r4);
      const searchInput_r5 = ɵɵreference(8);
      const ctx_r2 = ɵɵnextContext();
      ctx_r2.close(true);
      ctx_r2.launchSearchResult(searchInput_r5.value);
      return ɵɵresetView(ctx_r2.updateChosenWord(searchInput_r5.value));
    })("keydown.arrowup", function SearchBoxComponent_div_1_Template_input_keydown_arrowup_7_listener($event) {
      ɵɵrestoreView(_r4);
      const ctx_r2 = ɵɵnextContext();
      return ɵɵresetView(ctx_r2.focusPreviousChild($event));
    })("keydown.arrowdown", function SearchBoxComponent_div_1_Template_input_keydown_arrowdown_7_listener($event) {
      ɵɵrestoreView(_r4);
      const ctx_r2 = ɵɵnextContext();
      return ɵɵresetView(ctx_r2.focusNextChild($event));
    });
    ɵɵelementEnd();
    ɵɵelementStart(11, "button", 9);
    ɵɵpipe(12, "cxTranslate");
    ɵɵpipe(13, "cxTranslate");
    ɵɵlistener("click", function SearchBoxComponent_div_1_Template_button_click_11_listener() {
      ɵɵrestoreView(_r4);
      const searchInput_r5 = ɵɵreference(8);
      const ctx_r2 = ɵɵnextContext();
      return ɵɵresetView(ctx_r2.clear(searchInput_r5));
    });
    ɵɵelement(14, "cx-icon", 10);
    ɵɵelementEnd();
    ɵɵelementStart(15, "div", 11);
    ɵɵpipe(16, "cxTranslate");
    ɵɵelement(17, "cx-icon", 10);
    ɵɵelementEnd()();
    ɵɵelementStart(18, "button", 12, 1);
    ɵɵpipe(20, "cxTranslate");
    ɵɵpipe(21, "cxTranslate");
    ɵɵlistener("click", function SearchBoxComponent_div_1_Template_button_click_18_listener() {
      ɵɵrestoreView(_r4);
      const ctx_r2 = ɵɵnextContext();
      return ɵɵresetView(ctx_r2.open());
    });
    ɵɵelement(22, "cx-icon", 10);
    ɵɵelementEnd()()();
  }
  if (rf & 2) {
    const searchInput_r5 = ɵɵreference(8);
    const ctx_r2 = ɵɵnextContext();
    ɵɵattribute("aria-label", ɵɵpipeBind1(1, 15, "searchBox.productSearch"));
    ɵɵadvance(2);
    ɵɵclassProp("dirty", !!(searchInput_r5 == null ? null : searchInput_r5.value));
    ɵɵadvance(2);
    ɵɵtextInterpolate(ɵɵpipeBind1(5, 17, "common.search"));
    ɵɵadvance(3);
    ɵɵpropertyInterpolate("value", ctx_r2.chosenWord);
    ɵɵproperty("placeholder", ɵɵpipeBind1(9, 19, "searchBox.placeholder"));
    ɵɵattribute("tabindex", ctx_r2.getTabIndex(ɵɵpipeBind1(10, 21, ctx_r2.isMobile)));
    ɵɵadvance(4);
    ɵɵproperty("title", ɵɵpipeBind1(12, 23, "common.reset"));
    ɵɵattribute("aria-label", ɵɵpipeBind1(13, 25, "common.reset"));
    ɵɵadvance(3);
    ɵɵproperty("type", ctx_r2.iconTypes.RESET);
    ɵɵadvance();
    ɵɵproperty("title", ɵɵpipeBind1(16, 27, "common.search"));
    ɵɵadvance(2);
    ɵɵproperty("type", ctx_r2.iconTypes.SEARCH);
    ɵɵadvance();
    ɵɵproperty("title", ɵɵpipeBind1(20, 29, "common.search"));
    ɵɵattribute("aria-label", ɵɵpipeBind1(21, 31, "common.search"));
    ɵɵadvance(4);
    ɵɵproperty("type", ctx_r2.iconTypes.SEARCH);
  }
}
function SearchBoxComponent_div_2_button_1_Template(rf, ctx) {
  if (rf & 1) {
    const _r7 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "button", 28);
    ɵɵlistener("keydown.arrowdown", function SearchBoxComponent_div_2_button_1_Template_button_keydown_arrowdown_0_listener($event) {
      ɵɵrestoreView(_r7);
      const ctx_r2 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r2.focusPreviousGroup($event));
    })("click", function SearchBoxComponent_div_2_button_1_Template_button_click_0_listener() {
      ɵɵrestoreView(_r7);
      const ctx_r2 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r2.close());
    });
    ɵɵtext(1);
    ɵɵpipe(2, "cxTranslate");
    ɵɵelementEnd();
  }
  if (rf & 2) {
    ɵɵadvance();
    ɵɵtextInterpolate1(" ", ɵɵpipeBind1(2, 1, "searchBox.closeSearchPanel"), " ");
  }
}
function SearchBoxComponent_div_2_h3_2_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "h3", 29);
  }
  if (rf & 2) {
    const result_r8 = ɵɵnextContext().ngIf;
    ɵɵproperty("innerHTML", result_r8.message, ɵɵsanitizeHtml);
  }
}
function SearchBoxComponent_div_2_div_3_ng_container_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainerStart(0);
    ɵɵelementStart(1, "h3");
    ɵɵtext(2);
    ɵɵpipe(3, "cxTranslate");
    ɵɵelementEnd();
    ɵɵelementContainerEnd();
  }
  if (rf & 2) {
    ɵɵadvance(2);
    ɵɵtextInterpolate1(" ", ɵɵpipeBind1(3, 1, "searchBox.suggestions"), " ");
  }
}
function SearchBoxComponent_div_2_div_3_li_4_Template(rf, ctx) {
  if (rf & 1) {
    const _r9 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "li", 33)(1, "a", 34);
    ɵɵpipe(2, "cxHighlight");
    ɵɵpipe(3, "cxUrl");
    ɵɵlistener("keydown.arrowup", function SearchBoxComponent_div_2_div_3_li_4_Template_a_keydown_arrowup_1_listener($event) {
      ɵɵrestoreView(_r9);
      const ctx_r2 = ɵɵnextContext(3);
      return ɵɵresetView(ctx_r2.focusPreviousChild($event));
    })("keydown.arrowdown", function SearchBoxComponent_div_2_div_3_li_4_Template_a_keydown_arrowdown_1_listener($event) {
      ɵɵrestoreView(_r9);
      const ctx_r2 = ɵɵnextContext(3);
      return ɵɵresetView(ctx_r2.focusNextChild($event));
    })("keydown.arrowleft", function SearchBoxComponent_div_2_div_3_li_4_Template_a_keydown_arrowleft_1_listener($event) {
      ɵɵrestoreView(_r9);
      const ctx_r2 = ɵɵnextContext(3);
      return ɵɵresetView(ctx_r2.focusPreviousGroup($event));
    })("keydown.arrowright", function SearchBoxComponent_div_2_div_3_li_4_Template_a_keydown_arrowright_1_listener($event) {
      ɵɵrestoreView(_r9);
      const ctx_r2 = ɵɵnextContext(3);
      return ɵɵresetView(ctx_r2.focusNextGroup($event));
    })("keydown.enter", function SearchBoxComponent_div_2_div_3_li_4_Template_a_keydown_enter_1_listener() {
      ɵɵrestoreView(_r9);
      const ctx_r2 = ɵɵnextContext(3);
      return ɵɵresetView(ctx_r2.close(true));
    })("keydown.escape", function SearchBoxComponent_div_2_div_3_li_4_Template_a_keydown_escape_1_listener() {
      ɵɵrestoreView(_r9);
      const ctx_r2 = ɵɵnextContext(3);
      return ɵɵresetView(ctx_r2.close(true));
    })("blur", function SearchBoxComponent_div_2_div_3_li_4_Template_a_blur_1_listener() {
      ɵɵrestoreView(_r9);
      const ctx_r2 = ɵɵnextContext(3);
      return ɵɵresetView(ctx_r2.close());
    })("mousedown", function SearchBoxComponent_div_2_div_3_li_4_Template_a_mousedown_1_listener($event) {
      ɵɵrestoreView(_r9);
      const ctx_r2 = ɵɵnextContext(3);
      return ɵɵresetView(ctx_r2.preventDefault($event));
    })("click", function SearchBoxComponent_div_2_div_3_li_4_Template_a_click_1_listener() {
      let tmp_6_0;
      const suggestion_r10 = ɵɵrestoreView(_r9).$implicit;
      const result_r8 = ɵɵnextContext(2).ngIf;
      const ctx_r2 = ɵɵnextContext();
      ctx_r2.dispatchSuggestionEvent({
        freeText: ctx_r2.searchInputEl == null ? null : ctx_r2.searchInputEl.nativeElement == null ? null : ctx_r2.searchInputEl.nativeElement.value,
        selectedSuggestion: suggestion_r10,
        searchSuggestions: (tmp_6_0 = result_r8.suggestions) !== null && tmp_6_0 !== void 0 ? tmp_6_0 : []
      });
      return ɵɵresetView(ctx_r2.updateChosenWord(suggestion_r10));
    });
    ɵɵelementEnd()();
  }
  if (rf & 2) {
    const suggestion_r10 = ctx.$implicit;
    const ctx_r2 = ɵɵnextContext(3);
    ɵɵadvance();
    ɵɵproperty("innerHTML", ɵɵpipeBind2(2, 2, suggestion_r10, ctx_r2.searchInputEl == null ? null : ctx_r2.searchInputEl.nativeElement == null ? null : ctx_r2.searchInputEl.nativeElement.value), ɵɵsanitizeHtml)("routerLink", ɵɵpipeBind1(3, 5, ɵɵpureFunction1(9, _c56, ɵɵpureFunction1(7, _c55, suggestion_r10))));
  }
}
function SearchBoxComponent_div_2_div_3_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div", 30);
    ɵɵtemplate(1, SearchBoxComponent_div_2_div_3_ng_container_1_Template, 4, 3, "ng-container", 22);
    ɵɵelementStart(2, "ul", 31);
    ɵɵpipe(3, "cxTranslate");
    ɵɵtemplate(4, SearchBoxComponent_div_2_div_3_li_4_Template, 4, 11, "li", 32);
    ɵɵelementEnd()();
  }
  if (rf & 2) {
    const result_r8 = ɵɵnextContext().ngIf;
    const ctx_r2 = ɵɵnextContext();
    ɵɵadvance();
    ɵɵproperty("ngIf", ctx_r2.isEnabledFeature(ctx_r2.searchBoxFeatures.TRENDING_SEARCHES_FEATURE) || ctx_r2.isEnabledFeature(ctx_r2.searchBoxFeatures.SEARCH_BOX_V2));
    ɵɵadvance();
    ɵɵclassProp("no-headers", !ctx_r2.isEnabledFeature(ctx_r2.searchBoxFeatures.TRENDING_SEARCHES_FEATURE) && !ctx_r2.isEnabledFeature(ctx_r2.searchBoxFeatures.SEARCH_BOX_V2));
    ɵɵattribute("aria-label", ɵɵpipeBind1(3, 5, "searchBox.ariaLabelSuggestions"));
    ɵɵadvance(2);
    ɵɵproperty("ngForOf", result_r8.suggestions);
  }
}
function SearchBoxComponent_div_2_ng_container_5_ng_container_1_ng_template_1_Template(rf, ctx) {
}
function SearchBoxComponent_div_2_ng_container_5_ng_container_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainerStart(0);
    ɵɵtemplate(1, SearchBoxComponent_div_2_ng_container_5_ng_container_1_ng_template_1_Template, 0, 0, "ng-template", 36);
    ɵɵelementContainerEnd();
  }
  if (rf & 2) {
    const ctx_r2 = ɵɵnextContext(3);
    ɵɵadvance();
    ɵɵproperty("cxOutlet", ctx_r2.searchBoxOutlets.TRENDING_SEARCHES)("cxOutletContext", ɵɵpureFunction1(2, _c57, ctx_r2.config.maxTrendingSearches));
  }
}
function SearchBoxComponent_div_2_ng_container_5_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainerStart(0);
    ɵɵtemplate(1, SearchBoxComponent_div_2_ng_container_5_ng_container_1_Template, 2, 4, "ng-container", 35);
    ɵɵelementContainerEnd();
  }
  if (rf & 2) {
    const ctx_r2 = ɵɵnextContext(2);
    ɵɵadvance();
    ɵɵproperty("cxFeature", ctx_r2.searchBoxFeatures.TRENDING_SEARCHES_FEATURE);
  }
}
function SearchBoxComponent_div_2_6_ng_template_0_Template(rf, ctx) {
}
function SearchBoxComponent_div_2_6_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵtemplate(0, SearchBoxComponent_div_2_6_ng_template_0_Template, 0, 0, "ng-template", 36);
  }
  if (rf & 2) {
    let tmp_4_0;
    const ctx_r2 = ɵɵnextContext(2);
    ɵɵproperty("cxOutlet", ctx_r2.searchBoxOutlets.RECENT_SEARCHES)("cxOutletContext", ɵɵpureFunction3(2, _c58, (tmp_4_0 = ctx_r2.searchInputEl == null ? null : ctx_r2.searchInputEl.nativeElement == null ? null : ctx_r2.searchInputEl.nativeElement.value) !== null && tmp_4_0 !== void 0 ? tmp_4_0 : "", ctx_r2.searchBoxActive, ctx_r2.config.maxRecentSearches));
  }
}
function SearchBoxComponent_div_2_ng_container_8_h3_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "h3");
    ɵɵtext(1);
    ɵɵpipe(2, "cxTranslate");
    ɵɵelementEnd();
  }
  if (rf & 2) {
    ɵɵadvance();
    ɵɵtextInterpolate1(" ", ɵɵpipeBind1(2, 1, "searchBox.products"), " ");
  }
}
function SearchBoxComponent_div_2_ng_container_8_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainerStart(0);
    ɵɵtemplate(1, SearchBoxComponent_div_2_ng_container_8_h3_1_Template, 3, 3, "h3", 22);
    ɵɵelementContainerEnd();
  }
  if (rf & 2) {
    const result_r8 = ɵɵnextContext().ngIf;
    ɵɵadvance();
    ɵɵproperty("ngIf", result_r8.products == null ? null : result_r8.products.length);
  }
}
function SearchBoxComponent_div_2_ul_9_li_2_cx_media_3_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "cx-media", 42);
  }
  if (rf & 2) {
    const product_r12 = ɵɵnextContext().$implicit;
    ɵɵproperty("container", product_r12.images == null ? null : product_r12.images.PRIMARY);
  }
}
function SearchBoxComponent_div_2_ul_9_li_2_Template(rf, ctx) {
  if (rf & 1) {
    const _r11 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "li", 33)(1, "a", 38);
    ɵɵpipe(2, "cxUrl");
    ɵɵlistener("keydown.arrowup", function SearchBoxComponent_div_2_ul_9_li_2_Template_a_keydown_arrowup_1_listener($event) {
      ɵɵrestoreView(_r11);
      const ctx_r2 = ɵɵnextContext(3);
      return ɵɵresetView(ctx_r2.focusPreviousChild($event));
    })("keydown.arrowdown", function SearchBoxComponent_div_2_ul_9_li_2_Template_a_keydown_arrowdown_1_listener($event) {
      ɵɵrestoreView(_r11);
      const ctx_r2 = ɵɵnextContext(3);
      return ɵɵresetView(ctx_r2.focusNextChild($event));
    })("keydown.arrowleft", function SearchBoxComponent_div_2_ul_9_li_2_Template_a_keydown_arrowleft_1_listener($event) {
      ɵɵrestoreView(_r11);
      const ctx_r2 = ɵɵnextContext(3);
      return ɵɵresetView(ctx_r2.focusPreviousGroup($event));
    })("keydown.arrowright", function SearchBoxComponent_div_2_ul_9_li_2_Template_a_keydown_arrowright_1_listener($event) {
      ɵɵrestoreView(_r11);
      const ctx_r2 = ɵɵnextContext(3);
      return ɵɵresetView(ctx_r2.focusNextGroup($event));
    })("keydown.enter", function SearchBoxComponent_div_2_ul_9_li_2_Template_a_keydown_enter_1_listener() {
      ɵɵrestoreView(_r11);
      const ctx_r2 = ɵɵnextContext(3);
      return ɵɵresetView(ctx_r2.close(true));
    })("keydown.escape", function SearchBoxComponent_div_2_ul_9_li_2_Template_a_keydown_escape_1_listener() {
      ɵɵrestoreView(_r11);
      const ctx_r2 = ɵɵnextContext(3);
      return ɵɵresetView(ctx_r2.close(true));
    })("blur", function SearchBoxComponent_div_2_ul_9_li_2_Template_a_blur_1_listener() {
      ɵɵrestoreView(_r11);
      const ctx_r2 = ɵɵnextContext(3);
      return ɵɵresetView(ctx_r2.close());
    })("mousedown", function SearchBoxComponent_div_2_ul_9_li_2_Template_a_mousedown_1_listener($event) {
      ɵɵrestoreView(_r11);
      const ctx_r2 = ɵɵnextContext(3);
      return ɵɵresetView(ctx_r2.preventDefault($event));
    })("click", function SearchBoxComponent_div_2_ul_9_li_2_Template_a_click_1_listener() {
      const product_r12 = ɵɵrestoreView(_r11).$implicit;
      const ctx_r2 = ɵɵnextContext(3);
      return ɵɵresetView(ctx_r2.dispatchProductEvent({
        freeText: ctx_r2.searchInputEl == null ? null : ctx_r2.searchInputEl.nativeElement == null ? null : ctx_r2.searchInputEl.nativeElement.value,
        productCode: product_r12.code
      }));
    });
    ɵɵtemplate(3, SearchBoxComponent_div_2_ul_9_li_2_cx_media_3_Template, 1, 1, "cx-media", 39);
    ɵɵelement(4, "div", 40);
    ɵɵelementStart(5, "span", 41);
    ɵɵtext(6);
    ɵɵelementEnd()()();
  }
  if (rf & 2) {
    const product_r12 = ctx.$implicit;
    const ctx_r2 = ɵɵnextContext(3);
    ɵɵadvance();
    ɵɵclassProp("has-media", ctx_r2.config.displayProductImages);
    ɵɵproperty("routerLink", ɵɵpipeBind1(2, 6, ɵɵpureFunction1(8, _c46, product_r12)));
    ɵɵadvance(2);
    ɵɵproperty("ngIf", ctx_r2.config.displayProductImages);
    ɵɵadvance();
    ɵɵproperty("innerHTML", product_r12.nameHtml, ɵɵsanitizeHtml);
    ɵɵadvance(2);
    ɵɵtextInterpolate(product_r12.price == null ? null : product_r12.price.formattedValue);
  }
}
function SearchBoxComponent_div_2_ul_9_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "ul", 37);
    ɵɵpipe(1, "cxTranslate");
    ɵɵtemplate(2, SearchBoxComponent_div_2_ul_9_li_2_Template, 7, 10, "li", 32);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const result_r8 = ɵɵnextContext().ngIf;
    const ctx_r2 = ɵɵnextContext();
    ɵɵclassProp("hidden", ctx_r2.hasSearchBoxV2);
    ɵɵattribute("aria-label", ɵɵpipeBind1(1, 4, "searchBox.ariaLabelProducts"));
    ɵɵadvance(2);
    ɵɵproperty("ngForOf", result_r8.products);
  }
}
function SearchBoxComponent_div_2_ng_container_10_ng_template_3_Template(rf, ctx) {
  if (rf & 1) {
    const _r14 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "a", 44);
    ɵɵpipe(1, "cxUrl");
    ɵɵlistener("keydown.enter", function SearchBoxComponent_div_2_ng_container_10_ng_template_3_Template_a_keydown_enter_0_listener() {
      ɵɵrestoreView(_r14);
      const ctx_r2 = ɵɵnextContext(3);
      return ɵɵresetView(ctx_r2.close(true));
    })("keydown.escape", function SearchBoxComponent_div_2_ng_container_10_ng_template_3_Template_a_keydown_escape_0_listener() {
      ɵɵrestoreView(_r14);
      const ctx_r2 = ɵɵnextContext(3);
      return ɵɵresetView(ctx_r2.close(true));
    })("keydown.arrowleft", function SearchBoxComponent_div_2_ng_container_10_ng_template_3_Template_a_keydown_arrowleft_0_listener($event) {
      ɵɵrestoreView(_r14);
      const ctx_r2 = ɵɵnextContext(3);
      return ɵɵresetView(ctx_r2.carouselEventPropagator($event));
    })("keydown.arrowright", function SearchBoxComponent_div_2_ng_container_10_ng_template_3_Template_a_keydown_arrowright_0_listener($event) {
      ɵɵrestoreView(_r14);
      const ctx_r2 = ɵɵnextContext(3);
      return ɵɵresetView(ctx_r2.carouselEventPropagator($event));
    })("keydown.arrowup", function SearchBoxComponent_div_2_ng_container_10_ng_template_3_Template_a_keydown_arrowup_0_listener($event) {
      ɵɵrestoreView(_r14);
      const ctx_r2 = ɵɵnextContext(3);
      return ɵɵresetView(ctx_r2.carouselEventPropagator($event));
    })("mousedown", function SearchBoxComponent_div_2_ng_container_10_ng_template_3_Template_a_mousedown_0_listener($event) {
      ɵɵrestoreView(_r14);
      const ctx_r2 = ɵɵnextContext(3);
      return ɵɵresetView(ctx_r2.preventDefault($event));
    })("click", function SearchBoxComponent_div_2_ng_container_10_ng_template_3_Template_a_click_0_listener() {
      const product_r15 = ɵɵrestoreView(_r14).item;
      const ctx_r2 = ɵɵnextContext(3);
      return ɵɵresetView(ctx_r2.dispatchProductEvent({
        freeText: ctx_r2.searchInputEl == null ? null : ctx_r2.searchInputEl.nativeElement == null ? null : ctx_r2.searchInputEl.nativeElement.value,
        productCode: product_r15.code
      }));
    });
    ɵɵelement(2, "cx-media", 45)(3, "h3", 46);
    ɵɵelementStart(4, "div", 41);
    ɵɵtext(5);
    ɵɵelementEnd()();
    ɵɵelementStart(6, "div", 47);
    ɵɵelementContainer(7, 48);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    let tmp_10_0;
    const product_r15 = ctx.item;
    const ctx_r2 = ɵɵnextContext(3);
    ɵɵclassProp("has-media", ctx_r2.config.displayProductImages);
    ɵɵproperty("routerLink", ɵɵpipeBind1(1, 7, ɵɵpureFunction1(9, _c46, product_r15)));
    ɵɵadvance(2);
    ɵɵproperty("container", product_r15.images == null ? null : product_r15.images.PRIMARY)("alt", (tmp_10_0 = product_r15.name) !== null && tmp_10_0 !== void 0 ? tmp_10_0 : "");
    ɵɵadvance();
    ɵɵproperty("innerHtml", product_r15.nameHtml, ɵɵsanitizeHtml);
    ɵɵadvance(2);
    ɵɵtextInterpolate1(" ", product_r15.price == null ? null : product_r15.price.formattedValue, " ");
  }
}
function SearchBoxComponent_div_2_ng_container_10_Template(rf, ctx) {
  if (rf & 1) {
    const _r13 = ɵɵgetCurrentView();
    ɵɵelementContainerStart(0);
    ɵɵelementStart(1, "cx-carousel", 43);
    ɵɵpipe(2, "cxTranslate");
    ɵɵlistener("keybordEvent", function SearchBoxComponent_div_2_ng_container_10_Template_cx_carousel_keybordEvent_1_listener($event) {
      ɵɵrestoreView(_r13);
      const ctx_r2 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r2.carouselEventPropagator($event));
    });
    ɵɵelementEnd();
    ɵɵtemplate(3, SearchBoxComponent_div_2_ng_container_10_ng_template_3_Template, 8, 11, "ng-template", null, 2, ɵɵtemplateRefExtractor);
    ɵɵelementContainerEnd();
  }
  if (rf & 2) {
    const items_r16 = ctx.ngIf;
    const carouselItem_r17 = ɵɵreference(4);
    ɵɵadvance();
    ɵɵproperty("items", items_r16)("template", carouselItem_r17);
    ɵɵattribute("aria-label", ɵɵpipeBind1(2, 3, "searchBox.ariaLabelProducts"));
  }
}
function SearchBoxComponent_div_2_div_12_h3_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "h3", 51);
  }
  if (rf & 2) {
    const result_r8 = ɵɵnextContext(2).ngIf;
    ɵɵproperty("innerHTML", result_r8.message, ɵɵsanitizeHtml);
  }
}
function SearchBoxComponent_div_2_div_12_ng_container_2_h3_4_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "h3", 56);
    ɵɵtext(1);
    ɵɵpipe(2, "cxTranslate");
    ɵɵelementEnd();
  }
  if (rf & 2) {
    ɵɵadvance();
    ɵɵtextInterpolate1(" ", ɵɵpipeBind1(2, 1, "searchBox.trendingSearches"), " ");
  }
}
function SearchBoxComponent_div_2_div_12_ng_container_2_h3_8_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "h3", 57);
    ɵɵtext(1);
    ɵɵpipe(2, "cxTranslate");
    ɵɵelementEnd();
  }
  if (rf & 2) {
    ɵɵadvance();
    ɵɵtextInterpolate1(" ", ɵɵpipeBind1(2, 1, "searchBox.products"), " ");
  }
}
function SearchBoxComponent_div_2_div_12_ng_container_2_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainerStart(0);
    ɵɵelementStart(1, "h3", 52);
    ɵɵtext(2);
    ɵɵpipe(3, "cxTranslate");
    ɵɵelementEnd();
    ɵɵtemplate(4, SearchBoxComponent_div_2_div_12_ng_container_2_h3_4_Template, 3, 3, "h3", 53);
    ɵɵelementStart(5, "h3", 54);
    ɵɵtext(6);
    ɵɵpipe(7, "cxTranslate");
    ɵɵelementEnd();
    ɵɵtemplate(8, SearchBoxComponent_div_2_div_12_ng_container_2_h3_8_Template, 3, 3, "h3", 55);
    ɵɵelementContainerEnd();
  }
  if (rf & 2) {
    const result_r8 = ɵɵnextContext(2).ngIf;
    const ctx_r2 = ɵɵnextContext();
    ɵɵadvance(2);
    ɵɵtextInterpolate1(" ", ɵɵpipeBind1(3, 4, "searchBox.suggestions"), " ");
    ɵɵadvance(2);
    ɵɵproperty("ngIf", (ctx_r2.searchInputEl == null ? null : ctx_r2.searchInputEl.nativeElement == null ? null : ctx_r2.searchInputEl.nativeElement.value == null ? null : ctx_r2.searchInputEl.nativeElement.value.length) === 0);
    ɵɵadvance(2);
    ɵɵtextInterpolate1(" ", ɵɵpipeBind1(7, 6, "searchBox.recentSearches"), " ");
    ɵɵadvance(2);
    ɵɵproperty("ngIf", result_r8.products == null ? null : result_r8.products.length);
  }
}
function SearchBoxComponent_div_2_div_12_Template(rf, ctx) {
  if (rf & 1) {
    const _r18 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "div", 49);
    ɵɵtemplate(1, SearchBoxComponent_div_2_div_12_h3_1_Template, 1, 1, "h3", 50)(2, SearchBoxComponent_div_2_div_12_ng_container_2_Template, 9, 8, "ng-container", 22);
    ɵɵelementStart(3, "button", 28);
    ɵɵlistener("keydown.arrowdown", function SearchBoxComponent_div_2_div_12_Template_button_keydown_arrowdown_3_listener($event) {
      ɵɵrestoreView(_r18);
      const ctx_r2 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r2.focusPreviousGroup($event));
    })("click", function SearchBoxComponent_div_2_div_12_Template_button_click_3_listener() {
      ɵɵrestoreView(_r18);
      const ctx_r2 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r2.close());
    });
    ɵɵtext(4);
    ɵɵpipe(5, "cxTranslate");
    ɵɵelementEnd()();
  }
  if (rf & 2) {
    const result_r8 = ɵɵnextContext().ngIf;
    const ctx_r2 = ɵɵnextContext();
    ɵɵclassProp("d-none", !ctx_r2.hasSearchBoxV2)("header-panel-divider", !result_r8.message);
    ɵɵadvance();
    ɵɵproperty("ngIf", result_r8.message);
    ɵɵadvance();
    ɵɵproperty("ngIf", !result_r8.message);
    ɵɵadvance(2);
    ɵɵtextInterpolate1(" ", ɵɵpipeBind1(5, 7, "searchBox.closeSearchPanel"), " ");
  }
}
function SearchBoxComponent_div_2_div_16_ng_container_4_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainerStart(0);
    ɵɵtext(1);
    ɵɵpipe(2, "cxTranslate");
    ɵɵelementContainerEnd();
  }
  if (rf & 2) {
    ɵɵadvance();
    ɵɵtextInterpolate1(" ", ɵɵpipeBind1(2, 1, "searchBox.initialDescription"), " ");
  }
}
function SearchBoxComponent_div_2_div_16_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div", 58);
    ɵɵtext(1);
    ɵɵpipe(2, "cxTranslate");
    ɵɵpipe(3, "cxTranslate");
    ɵɵtemplate(4, SearchBoxComponent_div_2_div_16_ng_container_4_Template, 3, 3, "ng-container", 35);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const result_r8 = ɵɵnextContext().ngIf;
    ɵɵadvance();
    ɵɵtextInterpolate2(" ", ɵɵpipeBind2(2, 3, "searchBox.suggestionsResult", ɵɵpureFunction1(9, _c322, result_r8.suggestions == null ? null : result_r8.suggestions.length)), " ", ɵɵpipeBind2(3, 6, "searchBox.productsResult", ɵɵpureFunction1(11, _c322, result_r8.products == null ? null : result_r8.products.length)), " ");
    ɵɵadvance(3);
    ɵɵproperty("cxFeature", "!a11ySearchboxAssistiveMessage");
  }
}
function SearchBoxComponent_div_2_Template(rf, ctx) {
  if (rf & 1) {
    const _r6 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "div", 17);
    ɵɵlistener("click", function SearchBoxComponent_div_2_Template_div_click_0_listener() {
      ɵɵrestoreView(_r6);
      const ctx_r2 = ɵɵnextContext();
      return ɵɵresetView(ctx_r2.close(true));
    });
    ɵɵtemplate(1, SearchBoxComponent_div_2_button_1_Template, 3, 3, "button", 18)(2, SearchBoxComponent_div_2_h3_2_Template, 1, 1, "h3", 19)(3, SearchBoxComponent_div_2_div_3_Template, 5, 7, "div", 20);
    ɵɵelementStart(4, "div", 21);
    ɵɵtemplate(5, SearchBoxComponent_div_2_ng_container_5_Template, 2, 1, "ng-container", 22);
    ɵɵelementEnd();
    ɵɵtemplate(6, SearchBoxComponent_div_2_6_Template, 1, 6, null, 22);
    ɵɵelementStart(7, "div", 23);
    ɵɵtemplate(8, SearchBoxComponent_div_2_ng_container_8_Template, 2, 1, "ng-container", 22)(9, SearchBoxComponent_div_2_ul_9_Template, 3, 6, "ul", 24)(10, SearchBoxComponent_div_2_ng_container_10_Template, 5, 5, "ng-container", 22);
    ɵɵpipe(11, "async");
    ɵɵelementEnd();
    ɵɵtemplate(12, SearchBoxComponent_div_2_div_12_Template, 6, 9, "div", 25);
    ɵɵelementStart(13, "span", 26);
    ɵɵtext(14);
    ɵɵpipe(15, "cxTranslate");
    ɵɵelementEnd();
    ɵɵtemplate(16, SearchBoxComponent_div_2_div_16_Template, 5, 13, "div", 27);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    let tmp_5_0;
    const result_r8 = ctx.ngIf;
    const ctx_r2 = ɵɵnextContext();
    ɵɵclassProp("no-headers", !ctx_r2.isEnabledFeature(ctx_r2.searchBoxFeatures.TRENDING_SEARCHES_FEATURE) && !ctx_r2.isEnabledFeature(ctx_r2.searchBoxFeatures.SEARCH_BOX_V2));
    ɵɵadvance();
    ɵɵproperty("ngIf", ctx_r2.isEnabledFeature(ctx_r2.searchBoxFeatures.SEARCH_BOX_V2));
    ɵɵadvance();
    ɵɵproperty("ngIf", result_r8.message);
    ɵɵadvance();
    ɵɵproperty("ngIf", ((tmp_5_0 = ctx_r2.searchInputEl == null ? null : ctx_r2.searchInputEl.nativeElement == null ? null : ctx_r2.searchInputEl.nativeElement.value) !== null && tmp_5_0 !== void 0 ? tmp_5_0 : "").length > 0);
    ɵɵadvance();
    ɵɵclassProp("d-block", (ctx_r2.searchInputEl == null ? null : ctx_r2.searchInputEl.nativeElement == null ? null : ctx_r2.searchInputEl.nativeElement.value == null ? null : ctx_r2.searchInputEl.nativeElement.value.length) === 0);
    ɵɵadvance();
    ɵɵproperty("ngIf", ctx_r2.searchBoxFeatures.TRENDING_SEARCHES_FEATURE);
    ɵɵadvance();
    ɵɵproperty("ngIf", ctx_r2.config.recentSearches);
    ɵɵadvance(2);
    ɵɵproperty("ngIf", ctx_r2.isEnabledFeature(ctx_r2.searchBoxFeatures.TRENDING_SEARCHES_FEATURE) || ctx_r2.isEnabledFeature(ctx_r2.searchBoxFeatures.SEARCH_BOX_V2));
    ɵɵadvance();
    ɵɵproperty("ngIf", result_r8.products);
    ɵɵadvance();
    ɵɵproperty("ngIf", ɵɵpipeBind1(11, 15, ctx_r2.items$));
    ɵɵadvance(2);
    ɵɵproperty("ngIf", ctx_r2.isEnabledFeature(ctx_r2.searchBoxFeatures.SEARCH_BOX_V2));
    ɵɵadvance(2);
    ɵɵtextInterpolate1(" ", ɵɵpipeBind1(15, 17, "searchBox.initialDescription"), " ");
    ɵɵadvance(2);
    ɵɵproperty("ngIf", (result_r8.suggestions == null ? null : result_r8.suggestions.length) || (result_r8.products == null ? null : result_r8.products.length));
  }
}
var _c59 = ["button"];
function MyAccountV2NavigationComponent_cx_navigation_ui_0_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "cx-navigation-ui", 1);
    ɵɵpipe(1, "async");
    ɵɵpipe(2, "async");
  }
  if (rf & 2) {
    let tmp_4_0;
    const node_r1 = ctx.ngIf;
    const ctx_r1 = ɵɵnextContext();
    ɵɵproperty("node", node_r1)("flyout", false)("ngClass", (tmp_4_0 = ɵɵpipeBind1(1, 5, ctx_r1.styleClass$)) !== null && tmp_4_0 !== void 0 ? tmp_4_0 : "")("navAriaLabel", ɵɵpipeBind1(2, 7, ctx_r1.name$))("focusableNodeTitles", true);
  }
}
function MyAccountV2NotificationPreferenceComponent_ng_container_0_div_1_ng_container_10_label_1_Template(rf, ctx) {
  if (rf & 1) {
    const _r1 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "label", 10)(1, "input", 11);
    ɵɵpipe(2, "async");
    ɵɵlistener("change", function MyAccountV2NotificationPreferenceComponent_ng_container_0_div_1_ng_container_10_label_1_Template_input_change_1_listener() {
      ɵɵrestoreView(_r1);
      const preference_r2 = ɵɵnextContext().$implicit;
      const ctx_r2 = ɵɵnextContext(3);
      return ɵɵresetView(ctx_r2.updatePreference(preference_r2));
    });
    ɵɵelementEnd();
    ɵɵelementStart(3, "span", 12);
    ɵɵtext(4);
    ɵɵpipe(5, "cxTranslate");
    ɵɵelementEnd()();
  }
  if (rf & 2) {
    const preference_r2 = ɵɵnextContext().$implicit;
    const ctx_r2 = ɵɵnextContext(3);
    ɵɵadvance();
    ɵɵproperty("checked", preference_r2.enabled)("disabled", ɵɵpipeBind1(2, 4, ctx_r2.isLoading$));
    ɵɵadvance(3);
    ɵɵtextInterpolate2(" ", ɵɵpipeBind1(5, 6, "myAccountV2NotificationPreference." + preference_r2.channel), " ", preference_r2.value, " ");
  }
}
function MyAccountV2NotificationPreferenceComponent_ng_container_0_div_1_ng_container_10_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainerStart(0);
    ɵɵtemplate(1, MyAccountV2NotificationPreferenceComponent_ng_container_0_div_1_ng_container_10_label_1_Template, 6, 8, "label", 9);
    ɵɵelementContainerEnd();
  }
  if (rf & 2) {
    const preference_r2 = ctx.$implicit;
    ɵɵadvance();
    ɵɵproperty("ngIf", preference_r2.visible);
  }
}
function MyAccountV2NotificationPreferenceComponent_ng_container_0_div_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div")(1, "div", 3)(2, "div", 4)(3, "div", 5);
    ɵɵtext(4);
    ɵɵpipe(5, "cxTranslate");
    ɵɵelementEnd();
    ɵɵelementStart(6, "div", 6);
    ɵɵtext(7);
    ɵɵpipe(8, "cxTranslate");
    ɵɵelementEnd();
    ɵɵelementStart(9, "div", 7);
    ɵɵtemplate(10, MyAccountV2NotificationPreferenceComponent_ng_container_0_div_1_ng_container_10_Template, 2, 1, "ng-container", 8);
    ɵɵelementEnd();
    ɵɵelementStart(11, "label")(12, "strong");
    ɵɵtext(13);
    ɵɵpipe(14, "cxTranslate");
    ɵɵelementEnd();
    ɵɵtext(15);
    ɵɵpipe(16, "cxTranslate");
    ɵɵelementEnd()()()();
  }
  if (rf & 2) {
    const preferences_r4 = ɵɵnextContext().ngIf;
    ɵɵadvance(4);
    ɵɵtextInterpolate1(" ", ɵɵpipeBind1(5, 5, "myAccountV2NotificationPreference.header"), " ");
    ɵɵadvance(3);
    ɵɵtextInterpolate1(" ", ɵɵpipeBind1(8, 7, "myAccountV2NotificationPreference.message"), " ");
    ɵɵadvance(3);
    ɵɵproperty("ngForOf", preferences_r4);
    ɵɵadvance(3);
    ɵɵtextInterpolate(ɵɵpipeBind1(14, 9, "myAccountV2NotificationPreference.note"));
    ɵɵadvance(2);
    ɵɵtextInterpolate1("", ɵɵpipeBind1(16, 11, "myAccountV2NotificationPreference.noteMessage"), " ");
  }
}
function MyAccountV2NotificationPreferenceComponent_ng_container_0_ng_template_2_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div", 13);
    ɵɵelement(1, "cx-spinner");
    ɵɵelementEnd();
  }
}
function MyAccountV2NotificationPreferenceComponent_ng_container_0_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainerStart(0);
    ɵɵtemplate(1, MyAccountV2NotificationPreferenceComponent_ng_container_0_div_1_Template, 17, 13, "div", 2)(2, MyAccountV2NotificationPreferenceComponent_ng_container_0_ng_template_2_Template, 2, 0, "ng-template", null, 0, ɵɵtemplateRefExtractor);
    ɵɵelementContainerEnd();
  }
  if (rf & 2) {
    const preferences_r4 = ctx.ngIf;
    const loading_r5 = ɵɵreference(3);
    ɵɵadvance();
    ɵɵproperty("ngIf", preferences_r4.length > 0)("ngIfElse", loading_r5);
  }
}
function PaymentMethodsComponent_ng_container_0_div_11_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div");
    ɵɵelement(1, "cx-spinner");
    ɵɵelementEnd();
  }
}
function PaymentMethodsComponent_ng_container_0_ng_template_13_div_1_Template(rf, ctx) {
  if (rf & 1) {
    const _r1 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "div", 9)(1, "div", 10)(2, "cx-card", 11);
    ɵɵpipe(3, "async");
    ɵɵlistener("deleteCard", function PaymentMethodsComponent_ng_container_0_ng_template_13_div_1_Template_cx_card_deleteCard_2_listener() {
      const paymentMethod_r2 = ɵɵrestoreView(_r1).$implicit;
      const ctx_r2 = ɵɵnextContext(3);
      return ɵɵresetView(ctx_r2.deletePaymentMethod(paymentMethod_r2));
    })("setDefaultCard", function PaymentMethodsComponent_ng_container_0_ng_template_13_div_1_Template_cx_card_setDefaultCard_2_listener() {
      const paymentMethod_r2 = ɵɵrestoreView(_r1).$implicit;
      const ctx_r2 = ɵɵnextContext(3);
      return ɵɵresetView(ctx_r2.setDefaultPaymentMethod(paymentMethod_r2));
    })("editCard", function PaymentMethodsComponent_ng_container_0_ng_template_13_div_1_Template_cx_card_editCard_2_listener() {
      const paymentMethod_r2 = ɵɵrestoreView(_r1).$implicit;
      const ctx_r2 = ɵɵnextContext(3);
      return ɵɵresetView(ctx_r2.setEdit(paymentMethod_r2));
    })("cancelCard", function PaymentMethodsComponent_ng_container_0_ng_template_13_div_1_Template_cx_card_cancelCard_2_listener() {
      ɵɵrestoreView(_r1);
      const ctx_r2 = ɵɵnextContext(3);
      return ɵɵresetView(ctx_r2.cancelCard());
    });
    ɵɵelementEnd()()();
  }
  if (rf & 2) {
    const paymentMethod_r2 = ctx.$implicit;
    const i_r4 = ctx.index;
    const ctx_r2 = ɵɵnextContext(3);
    ɵɵadvance(2);
    ɵɵproperty("index", i_r4)("border", true)("fitToContainer", true)("content", ɵɵpipeBind1(3, 5, ctx_r2.getCardContent(paymentMethod_r2)))("editMode", ctx_r2.editCard === paymentMethod_r2.id);
  }
}
function PaymentMethodsComponent_ng_container_0_ng_template_13_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div", 7);
    ɵɵtemplate(1, PaymentMethodsComponent_ng_container_0_ng_template_13_div_1_Template, 4, 7, "div", 8);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const paymentMethods_r5 = ɵɵnextContext().ngIf;
    ɵɵadvance();
    ɵɵproperty("ngForOf", paymentMethods_r5);
  }
}
function PaymentMethodsComponent_ng_container_0_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainerStart(0);
    ɵɵelement(1, "cx-form-required-legend");
    ɵɵelementStart(2, "div", 2)(3, "div", 3)(4, "h2");
    ɵɵtext(5);
    ɵɵpipe(6, "cxTranslate");
    ɵɵelementEnd()();
    ɵɵelementStart(7, "div", 4)(8, "div", 5);
    ɵɵtext(9);
    ɵɵpipe(10, "cxTranslate");
    ɵɵelementEnd();
    ɵɵtemplate(11, PaymentMethodsComponent_ng_container_0_div_11_Template, 2, 0, "div", 6);
    ɵɵpipe(12, "async");
    ɵɵtemplate(13, PaymentMethodsComponent_ng_container_0_ng_template_13_Template, 2, 1, "ng-template", null, 0, ɵɵtemplateRefExtractor);
    ɵɵelementEnd()();
    ɵɵelementContainerEnd();
  }
  if (rf & 2) {
    const cards_r6 = ɵɵreference(14);
    const ctx_r2 = ɵɵnextContext();
    ɵɵadvance(5);
    ɵɵtextInterpolate1(" ", ɵɵpipeBind1(6, 4, "paymentMethods.paymentMethods"), " ");
    ɵɵadvance(4);
    ɵɵtextInterpolate1(" ", ɵɵpipeBind1(10, 6, "paymentMethods.newPaymentMethodsAreAddedDuringCheckout"), " ");
    ɵɵadvance(2);
    ɵɵproperty("ngIf", ɵɵpipeBind1(12, 8, ctx_r2.loading$))("ngIfElse", cards_r6);
  }
}
function ProductViewComponent_cx_icon_5_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "cx-icon", 2);
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    ɵɵproperty("type", ctx_r0.iconTypes.GRID);
  }
}
function ProductViewComponent_cx_icon_6_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "cx-icon", 2);
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    ɵɵproperty("type", ctx_r0.iconTypes.LIST);
  }
}
var _c60 = (a0) => ({
  product: a0
});
function ProductListItemComponent_cx_media_5_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "cx-media", 11);
    ɵɵpipe(1, "async");
  }
  if (rf & 2) {
    const lcpContext_r1 = ctx.$implicit;
    const ctx_r1 = ɵɵnextContext();
    ɵɵproperty("container", ctx_r1.product.images == null ? null : ctx_r1.product.images.PRIMARY)("alt", ctx_r1.product.name || ctx_r1.product.summary)("fetchPriority", ctx_r1.itemIndex === 0 ? ɵɵpipeBind1(1, 3, lcpContext_r1.fetchPriority$) : void 0);
  }
}
function ProductListItemComponent_ng_template_10_cx_star_rating_0_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "cx-star-rating", 15);
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext(2);
    ɵɵproperty("rating", ctx_r1.product == null ? null : ctx_r1.product.averageRating);
  }
}
function ProductListItemComponent_ng_template_10_div_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div", 16);
    ɵɵtext(1);
    ɵɵpipe(2, "cxTranslate");
    ɵɵelementEnd();
  }
  if (rf & 2) {
    ɵɵadvance();
    ɵɵtextInterpolate1(" ", ɵɵpipeBind1(2, 1, "productDetails.noReviews"), " ");
  }
}
function ProductListItemComponent_ng_template_10_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵtemplate(0, ProductListItemComponent_ng_template_10_cx_star_rating_0_Template, 1, 1, "cx-star-rating", 12)(1, ProductListItemComponent_ng_template_10_div_1_Template, 3, 3, "div", 13);
    ɵɵelementStart(2, "div", 14);
    ɵɵpipe(3, "cxTranslate");
    ɵɵtext(4);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext();
    ɵɵproperty("ngIf", ctx_r1.product.averageRating);
    ɵɵadvance();
    ɵɵproperty("ngIf", !ctx_r1.product.averageRating);
    ɵɵadvance();
    ɵɵattribute("aria-label", ɵɵpipeBind1(3, 4, "productDetails.productPrice"));
    ɵɵadvance(2);
    ɵɵtextInterpolate1(" ", ctx_r1.product.price == null ? null : ctx_r1.product.price.formattedValue, " ");
  }
}
function ProductListItemComponent_ng_template_15_ng_container_0_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainerStart(0);
    ɵɵelementContainer(1, 18);
    ɵɵelementContainerEnd();
  }
}
function ProductListItemComponent_ng_template_15_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵtemplate(0, ProductListItemComponent_ng_template_15_ng_container_0_Template, 2, 0, "ng-container", 17);
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext();
    ɵɵproperty("ngIf", !ctx_r1.hideAddToCartButton);
  }
}
function ProductGridItemComponent_cx_media_2_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "cx-media", 4);
    ɵɵpipe(1, "async");
  }
  if (rf & 2) {
    const lcpContext_r1 = ctx.$implicit;
    const ctx_r1 = ɵɵnextContext();
    ɵɵproperty("container", ctx_r1.product.images == null ? null : ctx_r1.product.images.PRIMARY)("alt", ctx_r1.product.name || ctx_r1.product.summary)("fetchPriority", ctx_r1.itemIndex === 0 ? ɵɵpipeBind1(1, 3, lcpContext_r1.fetchPriority$) : void 0);
  }
}
function ProductGridItemComponent_ng_template_5_cx_star_rating_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "cx-star-rating", 10);
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext(2);
    ɵɵproperty("rating", ctx_r1.product == null ? null : ctx_r1.product.averageRating);
  }
}
function ProductGridItemComponent_ng_template_5_div_2_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div");
    ɵɵtext(1);
    ɵɵpipe(2, "cxTranslate");
    ɵɵelementEnd();
  }
  if (rf & 2) {
    ɵɵadvance();
    ɵɵtextInterpolate1(" ", ɵɵpipeBind1(2, 1, "productDetails.noReviews"), " ");
  }
}
function ProductGridItemComponent_ng_template_5_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div", 5);
    ɵɵtemplate(1, ProductGridItemComponent_ng_template_5_cx_star_rating_1_Template, 1, 1, "cx-star-rating", 6)(2, ProductGridItemComponent_ng_template_5_div_2_Template, 3, 3, "div", 7);
    ɵɵelementEnd();
    ɵɵelementStart(3, "div", 8)(4, "div", 9);
    ɵɵpipe(5, "cxTranslate");
    ɵɵtext(6);
    ɵɵelementEnd()();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext();
    ɵɵadvance();
    ɵɵproperty("ngIf", ctx_r1.product.averageRating);
    ɵɵadvance();
    ɵɵproperty("ngIf", !ctx_r1.product.averageRating);
    ɵɵadvance(2);
    ɵɵattribute("aria-label", ɵɵpipeBind1(5, 4, "productDetails.productPrice"));
    ɵɵadvance(2);
    ɵɵtextInterpolate1(" ", ctx_r1.product.price == null ? null : ctx_r1.product.price.formattedValue, " ");
  }
}
function ProductGridItemComponent_ng_template_6_ng_container_0_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainerStart(0);
    ɵɵelementContainer(1, 11);
    ɵɵelementContainerEnd();
  }
}
function ProductGridItemComponent_ng_template_6_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵtemplate(0, ProductGridItemComponent_ng_template_6_ng_container_0_Template, 2, 0, "ng-container", 7);
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext();
    ɵɵproperty("ngIf", !ctx_r1.hideAddToCartButton);
  }
}
function ProductScrollComponent_ng_container_0_cx_product_grid_item_3_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "cx-product-grid-item", 8);
  }
  if (rf & 2) {
    const product_r3 = ctx.$implicit;
    const i_r4 = ctx.index;
    ɵɵproperty("product", product_r3)("itemIndex", i_r4);
  }
}
function ProductScrollComponent_ng_container_0_div_5_Template(rf, ctx) {
  if (rf & 1) {
    const _r5 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "div", 9);
    ɵɵlistener("click", function ProductScrollComponent_ng_container_0_div_5_Template_div_click_0_listener() {
      ɵɵrestoreView(_r5);
      const ctx_r1 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r1.scrollToTop());
    });
    ɵɵtext(1);
    ɵɵpipe(2, "cxTranslate");
    ɵɵelementEnd();
  }
  if (rf & 2) {
    ɵɵadvance();
    ɵɵtextInterpolate1(" ", ɵɵpipeBind1(2, 1, "productList.backToTopBtn"), " ");
  }
}
function ProductScrollComponent_ng_container_0_div_6_Template(rf, ctx) {
  if (rf & 1) {
    const _r6 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "div", 10);
    ɵɵlistener("click", function ProductScrollComponent_ng_container_0_div_6_Template_div_click_0_listener() {
      let tmp_3_0;
      ɵɵrestoreView(_r6);
      const ctx_r1 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r1.loadNextPage(((tmp_3_0 = ctx_r1.model == null ? null : ctx_r1.model.pagination == null ? null : ctx_r1.model.pagination.currentPage) !== null && tmp_3_0 !== void 0 ? tmp_3_0 : 0) + 1));
    });
    ɵɵtext(1);
    ɵɵpipe(2, "cxTranslate");
    ɵɵelementEnd();
  }
  if (rf & 2) {
    ɵɵadvance();
    ɵɵtextInterpolate1(" ", ɵɵpipeBind1(2, 1, "productList.showMoreBtn"), " ");
  }
}
function ProductScrollComponent_ng_container_0_div_7_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div", 11);
    ɵɵelement(1, "cx-spinner");
    ɵɵelementEnd();
  }
}
function ProductScrollComponent_ng_container_0_Template(rf, ctx) {
  if (rf & 1) {
    const _r1 = ɵɵgetCurrentView();
    ɵɵelementContainerStart(0);
    ɵɵelementStart(1, "div", 1);
    ɵɵlistener("scrolled", function ProductScrollComponent_ng_container_0_Template_div_scrolled_1_listener() {
      let tmp_2_0;
      ɵɵrestoreView(_r1);
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.scrollPage(((tmp_2_0 = ctx_r1.model == null ? null : ctx_r1.model.pagination == null ? null : ctx_r1.model.pagination.currentPage) !== null && tmp_2_0 !== void 0 ? tmp_2_0 : 0) + 1));
    });
    ɵɵelementStart(2, "div", 2);
    ɵɵtemplate(3, ProductScrollComponent_ng_container_0_cx_product_grid_item_3_Template, 1, 2, "cx-product-grid-item", 3);
    ɵɵelementEnd();
    ɵɵelementStart(4, "div", 4);
    ɵɵtemplate(5, ProductScrollComponent_ng_container_0_div_5_Template, 3, 3, "div", 5)(6, ProductScrollComponent_ng_container_0_div_6_Template, 3, 3, "div", 6);
    ɵɵelementEnd();
    ɵɵtemplate(7, ProductScrollComponent_ng_container_0_div_7_Template, 2, 0, "div", 7);
    ɵɵelementEnd();
    ɵɵelementContainerEnd();
  }
  if (rf & 2) {
    let tmp_5_0;
    let tmp_6_0;
    const ctx_r1 = ɵɵnextContext();
    ɵɵadvance();
    ɵɵproperty("infiniteScrollDistance", 5)("infiniteScrollThrottle", 50)("infiniteScrollDisabled", ctx_r1.isMaxProducts || ctx_r1.isLastPage || ctx_r1.isEmpty);
    ɵɵadvance(2);
    ɵɵproperty("ngForOf", ctx_r1.model == null ? null : ctx_r1.model.products);
    ɵɵadvance();
    ɵɵproperty("className", !ctx_r1.isLastPage && ((tmp_5_0 = ctx_r1.model == null ? null : ctx_r1.model.pagination == null ? null : ctx_r1.model.pagination.currentPage) !== null && tmp_5_0 !== void 0 ? tmp_5_0 : 0) > 0 ? "cx-double-btn-container grid-btn-padding" : "cx-single-btn-container grid-btn-padding");
    ɵɵadvance();
    ɵɵproperty("ngIf", (ctx_r1.isMaxProducts || ctx_r1.isLastPage) && ((tmp_6_0 = ctx_r1.model == null ? null : ctx_r1.model.pagination == null ? null : ctx_r1.model.pagination.currentPage) !== null && tmp_6_0 !== void 0 ? tmp_6_0 : 0) > 0);
    ɵɵadvance();
    ɵɵproperty("ngIf", ctx_r1.isMaxProducts && !ctx_r1.isLastPage);
    ɵɵadvance();
    ɵɵproperty("ngIf", ctx_r1.appendProducts);
  }
}
function ProductScrollComponent_ng_container_1_cx_product_list_item_2_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "cx-product-list-item", 13);
  }
  if (rf & 2) {
    const product_r8 = ctx.$implicit;
    const i_r9 = ctx.index;
    ɵɵproperty("product", product_r8)("itemIndex", i_r9);
  }
}
function ProductScrollComponent_ng_container_1_div_4_Template(rf, ctx) {
  if (rf & 1) {
    const _r10 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "div", 9);
    ɵɵlistener("click", function ProductScrollComponent_ng_container_1_div_4_Template_div_click_0_listener() {
      ɵɵrestoreView(_r10);
      const ctx_r1 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r1.scrollToTop());
    });
    ɵɵtext(1);
    ɵɵpipe(2, "cxTranslate");
    ɵɵelementEnd();
  }
  if (rf & 2) {
    ɵɵadvance();
    ɵɵtextInterpolate1(" ", ɵɵpipeBind1(2, 1, "productList.backToTopBtn"), " ");
  }
}
function ProductScrollComponent_ng_container_1_div_5_Template(rf, ctx) {
  if (rf & 1) {
    const _r11 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "div", 10);
    ɵɵlistener("click", function ProductScrollComponent_ng_container_1_div_5_Template_div_click_0_listener() {
      let tmp_3_0;
      ɵɵrestoreView(_r11);
      const ctx_r1 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r1.loadNextPage(((tmp_3_0 = ctx_r1.model == null ? null : ctx_r1.model.pagination == null ? null : ctx_r1.model.pagination.currentPage) !== null && tmp_3_0 !== void 0 ? tmp_3_0 : 0) + 1));
    });
    ɵɵtext(1);
    ɵɵpipe(2, "cxTranslate");
    ɵɵelementEnd();
  }
  if (rf & 2) {
    ɵɵadvance();
    ɵɵtextInterpolate1(" ", ɵɵpipeBind1(2, 1, "productList.showMoreBtn"), " ");
  }
}
function ProductScrollComponent_ng_container_1_div_6_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div", 11);
    ɵɵelement(1, "cx-spinner");
    ɵɵelementEnd();
  }
}
function ProductScrollComponent_ng_container_1_Template(rf, ctx) {
  if (rf & 1) {
    const _r7 = ɵɵgetCurrentView();
    ɵɵelementContainerStart(0);
    ɵɵelementStart(1, "div", 1);
    ɵɵlistener("scrolled", function ProductScrollComponent_ng_container_1_Template_div_scrolled_1_listener() {
      let tmp_2_0;
      ɵɵrestoreView(_r7);
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.scrollPage(((tmp_2_0 = ctx_r1.model == null ? null : ctx_r1.model.pagination == null ? null : ctx_r1.model.pagination.currentPage) !== null && tmp_2_0 !== void 0 ? tmp_2_0 : 0) + 1));
    });
    ɵɵtemplate(2, ProductScrollComponent_ng_container_1_cx_product_list_item_2_Template, 1, 2, "cx-product-list-item", 12);
    ɵɵelementStart(3, "div", 4);
    ɵɵtemplate(4, ProductScrollComponent_ng_container_1_div_4_Template, 3, 3, "div", 5)(5, ProductScrollComponent_ng_container_1_div_5_Template, 3, 3, "div", 6);
    ɵɵelementEnd();
    ɵɵtemplate(6, ProductScrollComponent_ng_container_1_div_6_Template, 2, 0, "div", 7);
    ɵɵelementEnd();
    ɵɵelementContainerEnd();
  }
  if (rf & 2) {
    let tmp_5_0;
    let tmp_6_0;
    const ctx_r1 = ɵɵnextContext();
    ɵɵadvance();
    ɵɵproperty("infiniteScrollDistance", 3)("infiniteScrollThrottle", 50)("infiniteScrollDisabled", ctx_r1.isMaxProducts || ctx_r1.isLastPage || ctx_r1.isEmpty);
    ɵɵadvance();
    ɵɵproperty("ngForOf", ctx_r1.model == null ? null : ctx_r1.model.products);
    ɵɵadvance();
    ɵɵproperty("className", !ctx_r1.isLastPage && ((tmp_5_0 = ctx_r1.model == null ? null : ctx_r1.model.pagination == null ? null : ctx_r1.model.pagination.currentPage) !== null && tmp_5_0 !== void 0 ? tmp_5_0 : 0) > 0 ? "cx-double-btn-container" : "cx-single-btn-container");
    ɵɵadvance();
    ɵɵproperty("ngIf", (ctx_r1.isMaxProducts || ctx_r1.isLastPage) && ((tmp_6_0 = ctx_r1.model == null ? null : ctx_r1.model.pagination == null ? null : ctx_r1.model.pagination.currentPage) !== null && tmp_6_0 !== void 0 ? tmp_6_0 : 0) > 0);
    ɵɵadvance();
    ɵɵproperty("ngIf", ctx_r1.isMaxProducts && !ctx_r1.isLastPage);
    ɵɵadvance();
    ɵɵproperty("ngIf", ctx_r1.appendProducts);
  }
}
function ProductListComponent_div_0_div_5_div_10_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div", 17)(1, "div", 18);
    ɵɵpipe(2, "cxTranslate");
    ɵɵelement(3, "cx-pagination", 19);
    ɵɵelementEnd()();
  }
  if (rf & 2) {
    const model_r3 = ɵɵnextContext(2).ngIf;
    ɵɵadvance();
    ɵɵattribute("aria-label", ɵɵpipeBind1(2, 3, "productList.productSearchPagination"));
    ɵɵadvance(2);
    ɵɵproperty("pagination", model_r3.pagination)("defaultPage", 0);
  }
}
function ProductListComponent_div_0_div_5_ng_container_14_ng_container_1_cx_product_grid_item_2_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "cx-product-grid-item", 22);
  }
  if (rf & 2) {
    const product_r4 = ctx.$implicit;
    const i_r5 = ctx.index;
    ɵɵproperty("product", product_r4)("itemIndex", i_r5);
  }
}
function ProductListComponent_div_0_div_5_ng_container_14_ng_container_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainerStart(0);
    ɵɵelementStart(1, "div", 5);
    ɵɵtemplate(2, ProductListComponent_div_0_div_5_ng_container_14_ng_container_1_cx_product_grid_item_2_Template, 1, 2, "cx-product-grid-item", 21);
    ɵɵelementEnd();
    ɵɵelementContainerEnd();
  }
  if (rf & 2) {
    const model_r3 = ɵɵnextContext(3).ngIf;
    ɵɵadvance(2);
    ɵɵproperty("ngForOf", model_r3 == null ? null : model_r3.products);
  }
}
function ProductListComponent_div_0_div_5_ng_container_14_ng_container_2_cx_product_list_item_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "cx-product-list-item", 24);
  }
  if (rf & 2) {
    const product_r6 = ctx.$implicit;
    const i_r7 = ctx.index;
    ɵɵproperty("product", product_r6)("itemIndex", i_r7);
  }
}
function ProductListComponent_div_0_div_5_ng_container_14_ng_container_2_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainerStart(0);
    ɵɵtemplate(1, ProductListComponent_div_0_div_5_ng_container_14_ng_container_2_cx_product_list_item_1_Template, 1, 2, "cx-product-list-item", 23);
    ɵɵelementContainerEnd();
  }
  if (rf & 2) {
    const model_r3 = ɵɵnextContext(3).ngIf;
    ɵɵadvance();
    ɵɵproperty("ngForOf", model_r3 == null ? null : model_r3.products);
  }
}
function ProductListComponent_div_0_div_5_ng_container_14_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainerStart(0);
    ɵɵtemplate(1, ProductListComponent_div_0_div_5_ng_container_14_ng_container_1_Template, 3, 1, "ng-container", 20)(2, ProductListComponent_div_0_div_5_ng_container_14_ng_container_2_Template, 2, 1, "ng-container", 20);
    ɵɵelementContainerEnd();
  }
  if (rf & 2) {
    const viewMode_r8 = ɵɵnextContext().ngIf;
    const ctx_r1 = ɵɵnextContext(2);
    ɵɵadvance();
    ɵɵproperty("ngIf", viewMode_r8 === ctx_r1.ViewModes.Grid);
    ɵɵadvance();
    ɵɵproperty("ngIf", viewMode_r8 === ctx_r1.ViewModes.List);
  }
}
function ProductListComponent_div_0_div_5_ng_template_15_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "cx-product-scroll", 25);
  }
  if (rf & 2) {
    const viewMode_r8 = ɵɵnextContext().ngIf;
    const model_r3 = ɵɵnextContext().ngIf;
    const ctx_r1 = ɵɵnextContext();
    ɵɵproperty("scrollConfig", ctx_r1.scrollConfig)("model", model_r3)("inputViewMode", viewMode_r8);
  }
}
function ProductListComponent_div_0_div_5_div_26_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div", 17);
    ɵɵpipe(1, "cxTranslate");
    ɵɵelementStart(2, "div", 18);
    ɵɵelement(3, "cx-pagination", 26);
    ɵɵelementEnd()();
  }
  if (rf & 2) {
    const model_r3 = ɵɵnextContext(2).ngIf;
    ɵɵattribute("aria-label", ɵɵpipeBind1(1, 3, "productList.productSearchPagination"));
    ɵɵadvance(3);
    ɵɵproperty("pagination", model_r3.pagination)("defaultPage", 0);
  }
}
function ProductListComponent_div_0_div_5_Template(rf, ctx) {
  if (rf & 1) {
    const _r1 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "div", 7)(1, "div", 8)(2, "div", 5)(3, "label", 9)(4, "span");
    ɵɵtext(5);
    ɵɵpipe(6, "cxTranslate");
    ɵɵelementEnd();
    ɵɵelementStart(7, "cx-sorting", 10);
    ɵɵpipe(8, "cxTranslate");
    ɵɵpipe(9, "cxTranslate");
    ɵɵlistener("sortListEvent", function ProductListComponent_div_0_div_5_Template_cx_sorting_sortListEvent_7_listener($event) {
      ɵɵrestoreView(_r1);
      const ctx_r1 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r1.sortList($event));
    });
    ɵɵelementEnd()();
    ɵɵtemplate(10, ProductListComponent_div_0_div_5_div_10_Template, 4, 5, "div", 11);
    ɵɵelementStart(11, "div", 12)(12, "cx-product-view", 13);
    ɵɵlistener("modeChange", function ProductListComponent_div_0_div_5_Template_cx_product_view_modeChange_12_listener($event) {
      ɵɵrestoreView(_r1);
      const ctx_r1 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r1.setViewMode($event));
    });
    ɵɵelementEnd()()()();
    ɵɵelementStart(13, "div", 14);
    ɵɵtemplate(14, ProductListComponent_div_0_div_5_ng_container_14_Template, 3, 2, "ng-container", 15)(15, ProductListComponent_div_0_div_5_ng_template_15_Template, 1, 3, "ng-template", null, 0, ɵɵtemplateRefExtractor);
    ɵɵelementEnd();
    ɵɵelementStart(17, "div", 16)(18, "div", 5)(19, "label", 9)(20, "span");
    ɵɵtext(21);
    ɵɵpipe(22, "cxTranslate");
    ɵɵelementEnd();
    ɵɵelementStart(23, "cx-sorting", 10);
    ɵɵpipe(24, "cxTranslate");
    ɵɵpipe(25, "cxTranslate");
    ɵɵlistener("sortListEvent", function ProductListComponent_div_0_div_5_Template_cx_sorting_sortListEvent_23_listener($event) {
      ɵɵrestoreView(_r1);
      const ctx_r1 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r1.sortList($event));
    });
    ɵɵelementEnd()();
    ɵɵtemplate(26, ProductListComponent_div_0_div_5_div_26_Template, 4, 5, "div", 11);
    ɵɵelementStart(27, "div", 12)(28, "cx-product-view", 13);
    ɵɵlistener("modeChange", function ProductListComponent_div_0_div_5_Template_cx_product_view_modeChange_28_listener($event) {
      ɵɵrestoreView(_r1);
      const ctx_r1 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r1.setViewMode($event));
    });
    ɵɵelementEnd()()()()();
  }
  if (rf & 2) {
    const viewMode_r8 = ctx.ngIf;
    const infiniteScroll_r9 = ɵɵreference(16);
    const model_r3 = ɵɵnextContext().ngIf;
    const ctx_r1 = ɵɵnextContext();
    ɵɵadvance(5);
    ɵɵtextInterpolate(ɵɵpipeBind1(6, 16, "productList.sortBy"));
    ɵɵadvance(2);
    ɵɵpropertyInterpolate("placeholder", ɵɵpipeBind1(8, 18, "productList.sortBy"));
    ɵɵproperty("sortOptions", model_r3.sorts)("selectedOption", model_r3.pagination == null ? null : model_r3.pagination.sort)("ariaLabel", ɵɵpipeBind1(9, 20, "productList.sortResults"));
    ɵɵadvance(3);
    ɵɵproperty("ngIf", !ctx_r1.isInfiniteScroll);
    ɵɵadvance(2);
    ɵɵproperty("mode", viewMode_r8);
    ɵɵadvance(2);
    ɵɵproperty("ngIf", !ctx_r1.isInfiniteScroll)("ngIfElse", infiniteScroll_r9);
    ɵɵadvance(7);
    ɵɵtextInterpolate(ɵɵpipeBind1(22, 22, "productList.sortBy"));
    ɵɵadvance(2);
    ɵɵpropertyInterpolate("placeholder", ɵɵpipeBind1(24, 24, "productList.sortBy"));
    ɵɵproperty("sortOptions", model_r3.sorts)("selectedOption", model_r3.pagination == null ? null : model_r3.pagination.sort)("ariaLabel", ɵɵpipeBind1(25, 26, "productList.sortResults"));
    ɵɵadvance(3);
    ɵɵproperty("ngIf", !ctx_r1.isInfiniteScroll);
    ɵɵadvance(2);
    ɵɵproperty("mode", viewMode_r8);
  }
}
function ProductListComponent_div_0_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div", 2)(1, "section", 3);
    ɵɵpipe(2, "cxTranslate");
    ɵɵelementStart(3, "div", 4)(4, "div", 5);
    ɵɵtemplate(5, ProductListComponent_div_0_div_5_Template, 29, 28, "div", 6);
    ɵɵpipe(6, "async");
    ɵɵelementEnd()()()();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext();
    ɵɵadvance();
    ɵɵattribute("aria-label", ɵɵpipeBind1(2, 2, "productList.productListResults"));
    ɵɵadvance(4);
    ɵɵproperty("ngIf", ɵɵpipeBind1(6, 4, ctx_r1.viewMode$));
  }
}
var _c61 = (a0) => ({
  filter: a0
});
function ActiveFacetsComponent_ng_container_0_div_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div", 3);
    ɵɵtext(1);
    ɵɵpipe(2, "cxTranslate");
    ɵɵelementEnd();
  }
  if (rf & 2) {
    ɵɵadvance();
    ɵɵtextInterpolate1(" ", ɵɵpipeBind1(2, 1, "productList.appliedFilter"), " ");
  }
}
function ActiveFacetsComponent_ng_container_0_a_2_Template(rf, ctx) {
  if (rf & 1) {
    const _r1 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "a", 4);
    ɵɵpipe(1, "cxTranslate");
    ɵɵpipe(2, "cxTranslate");
    ɵɵlistener("keydown.space", function ActiveFacetsComponent_ng_container_0_a_2_Template_a_keydown_space_0_listener($event) {
      ɵɵrestoreView(_r1);
      const ctx_r1 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r1.removeFilterWithSpacebar($event));
    });
    ɵɵelementStart(3, "span");
    ɵɵtext(4);
    ɵɵelementEnd();
    ɵɵelement(5, "cx-icon", 5);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const facet_r3 = ctx.$implicit;
    const facetList_r4 = ɵɵnextContext().ngIf;
    const ctx_r1 = ɵɵnextContext();
    ɵɵproperty("queryParams", ctx_r1.getLinkParams(facet_r3))("cxFocus", ɵɵpureFunction1(12, _c29, ctx_r1.getFocusKey(facetList_r4, facet_r3)));
    ɵɵattribute("aria-label", ɵɵpipeBind2(1, 6, "productList.activeFilter", ɵɵpureFunction1(14, _c61, facet_r3.facetValueName)))("title", ɵɵpipeBind2(2, 9, "productList.activeFilter", ɵɵpureFunction1(16, _c61, facet_r3.facetValueName)));
    ɵɵadvance(4);
    ɵɵtextInterpolate(facet_r3.facetValueName);
    ɵɵadvance();
    ɵɵproperty("type", ctx_r1.closeIcon);
  }
}
function ActiveFacetsComponent_ng_container_0_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainerStart(0);
    ɵɵtemplate(1, ActiveFacetsComponent_ng_container_0_div_1_Template, 3, 3, "div", 1)(2, ActiveFacetsComponent_ng_container_0_a_2_Template, 6, 18, "a", 2);
    ɵɵelementContainerEnd();
  }
  if (rf & 2) {
    const facetList_r4 = ctx.ngIf;
    ɵɵadvance();
    ɵɵproperty("ngIf", facetList_r4.activeFacets && facetList_r4.activeFacets.length > 0);
    ɵɵadvance();
    ɵɵproperty("ngForOf", facetList_r4 == null ? null : facetList_r4.activeFacets);
  }
}
var _c622 = ["facetHeader"];
var _c63 = ["facetValue"];
var _c64 = (a0, a1) => ({
  name: a0,
  count: a1
});
var _c65 = () => ({
  key: "moreorless"
});
function FacetComponent_ng_container_0_a_2_Template(rf, ctx) {
  if (rf & 1) {
    const _r1 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "a", 6, 0);
    ɵɵpipe(2, "cxTranslate");
    ɵɵlistener("keydown", function FacetComponent_ng_container_0_a_2_Template_a_keydown_0_listener($event) {
      ɵɵrestoreView(_r1);
      const ctx_r1 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r1.onKeydown($event));
    })("keydown.space", function FacetComponent_ng_container_0_a_2_Template_a_keydown_space_0_listener($event) {
      ɵɵrestoreView(_r1);
      const ctx_r1 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r1.openLink($event));
    });
    ɵɵelementStart(3, "span", 7);
    ɵɵtext(4);
    ɵɵelementStart(5, "span", 8);
    ɵɵtext(6);
    ɵɵelementEnd()()();
  }
  if (rf & 2) {
    const value_r3 = ctx.$implicit;
    const ctx_r1 = ɵɵnextContext(2);
    ɵɵclassProp("selected", value_r3.selected);
    ɵɵproperty("queryParams", ctx_r1.getLinkParams(value_r3))("cxFocus", ɵɵpureFunction1(12, _c29, value_r3.name));
    ɵɵattribute("role", ctx_r1.facet.multiSelect ? "checkbox" : null)("aria-checked", ctx_r1.facet.multiSelect ? value_r3.selected : null)("aria-label", ɵɵpipeBind2(2, 9, "productFacetNavigation.ariaLabelItemsAvailable", ɵɵpureFunction2(14, _c64, value_r3.name, value_r3.count)));
    ɵɵadvance(4);
    ɵɵtextInterpolate(value_r3.name);
    ɵɵadvance(2);
    ɵɵtextInterpolate(value_r3.count);
  }
}
function FacetComponent_ng_container_0_a_5_Template(rf, ctx) {
  if (rf & 1) {
    const _r4 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "a", 6, 0);
    ɵɵlistener("keydown", function FacetComponent_ng_container_0_a_5_Template_a_keydown_0_listener($event) {
      ɵɵrestoreView(_r4);
      const ctx_r1 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r1.onKeydown($event));
    })("keydown.space", function FacetComponent_ng_container_0_a_5_Template_a_keydown_space_0_listener($event) {
      ɵɵrestoreView(_r4);
      const ctx_r1 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r1.openLink($event));
    });
    ɵɵelementStart(2, "span");
    ɵɵtext(3);
    ɵɵelementStart(4, "span", 9);
    ɵɵtext(5);
    ɵɵelementEnd()()();
  }
  if (rf & 2) {
    const value_r5 = ctx.$implicit;
    const ctx_r1 = ɵɵnextContext(2);
    ɵɵclassProp("selected", value_r5.selected);
    ɵɵproperty("queryParams", ctx_r1.getLinkParams(value_r5))("cxFocus", ɵɵpureFunction1(6, _c29, value_r5.name));
    ɵɵadvance(3);
    ɵɵtextInterpolate(value_r5.name);
    ɵɵadvance(2);
    ɵɵtextInterpolate(value_r5.count);
  }
}
function FacetComponent_ng_container_0_button_7_Template(rf, ctx) {
  if (rf & 1) {
    const _r6 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "button", 10);
    ɵɵpipe(1, "cxTranslate");
    ɵɵpipe(2, "cxTranslate");
    ɵɵlistener("click", function FacetComponent_ng_container_0_button_7_Template_button_click_0_listener() {
      ɵɵrestoreView(_r6);
      const ctx_r1 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r1.decreaseVisibleValues());
    });
    ɵɵtext(3);
    ɵɵpipe(4, "cxTranslate");
    ɵɵelementEnd();
  }
  if (rf & 2) {
    ɵɵproperty("cxAtMessage", ɵɵpipeBind1(1, 4, "productFacetNavigation.decreaseOptionsVisibility"))("cxFocus", ɵɵpureFunction0(10, _c65));
    ɵɵattribute("aria-label", ɵɵpipeBind1(2, 6, "productFacetNavigation.ariaLabelShowLess"));
    ɵɵadvance(3);
    ɵɵtextInterpolate1(" ", ɵɵpipeBind1(4, 8, "productList.showLess"), " ");
  }
}
function FacetComponent_ng_container_0_button_8_Template(rf, ctx) {
  if (rf & 1) {
    const _r7 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "button", 11);
    ɵɵpipe(1, "cxTranslate");
    ɵɵpipe(2, "cxTranslate");
    ɵɵlistener("click", function FacetComponent_ng_container_0_button_8_Template_button_click_0_listener() {
      ɵɵrestoreView(_r7);
      const ctx_r1 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r1.increaseVisibleValues());
    });
    ɵɵtext(3);
    ɵɵpipe(4, "cxTranslate");
    ɵɵelementEnd();
  }
  if (rf & 2) {
    ɵɵproperty("cxAtMessage", ɵɵpipeBind1(1, 4, "productFacetNavigation.increaseOptionsVisibility"))("cxFocus", ɵɵpureFunction0(10, _c65));
    ɵɵattribute("aria-label", ɵɵpipeBind1(2, 6, "productFacetNavigation.ariaLabelShowMore"));
    ɵɵadvance(3);
    ɵɵtextInterpolate1(" ", ɵɵpipeBind1(4, 8, "productList.showMore"), " ");
  }
}
function FacetComponent_ng_container_0_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainerStart(0);
    ɵɵelementStart(1, "div");
    ɵɵtemplate(2, FacetComponent_ng_container_0_a_2_Template, 7, 17, "a", 2);
    ɵɵpipe(3, "slice");
    ɵɵelementStart(4, "div", 3);
    ɵɵtemplate(5, FacetComponent_ng_container_0_a_5_Template, 6, 8, "a", 2);
    ɵɵpipe(6, "slice");
    ɵɵtemplate(7, FacetComponent_ng_container_0_button_7_Template, 5, 11, "button", 4)(8, FacetComponent_ng_container_0_button_8_Template, 5, 11, "button", 5);
    ɵɵelementEnd()();
    ɵɵelementContainerEnd();
  }
  if (rf & 2) {
    let tmp_3_0;
    const state_r8 = ctx.ngIf;
    const ctx_r1 = ɵɵnextContext();
    ɵɵadvance(2);
    ɵɵproperty("ngForOf", ɵɵpipeBind3(3, 4, ctx_r1.facet.values, 0, state_r8.topVisible));
    ɵɵadvance(3);
    ɵɵproperty("ngForOf", ɵɵpipeBind3(6, 8, ctx_r1.facet.values, (tmp_3_0 = state_r8.topVisible) !== null && tmp_3_0 !== void 0 ? tmp_3_0 : 0, state_r8.maxVisible));
    ɵɵadvance(2);
    ɵɵproperty("ngIf", state_r8.maxVisible && state_r8.topVisible && state_r8.maxVisible > state_r8.topVisible);
    ɵɵadvance();
    ɵɵproperty("ngIf", state_r8.maxVisible && ctx_r1.facet.values && state_r8.maxVisible > 0 && state_r8.maxVisible < ctx_r1.facet.values.length);
  }
}
var _c66 = ["backToResultsBtn"];
var _c67 = ["facetsRef"];
function FacetListComponent_section_0_11_ng_template_0_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "cx-facet", 10);
    ɵɵpipe(1, "cxTranslate");
  }
  if (rf & 2) {
    const facet_r3 = ɵɵnextContext().$implicit;
    ɵɵproperty("facet", facet_r3);
    ɵɵattribute("aria-label", ɵɵpipeBind2(1, 2, "productFacetNavigation.ariaLabelItemsAvailable", ɵɵpureFunction2(5, _c64, facet_r3.name, facet_r3 == null ? null : facet_r3.values == null ? null : facet_r3.values.length)));
  }
}
function FacetListComponent_section_0_11_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵtemplate(0, FacetListComponent_section_0_11_ng_template_0_Template, 2, 8, "ng-template", null, 0, ɵɵtemplateRefExtractor);
  }
}
function FacetListComponent_section_0_div_12_Template(rf, ctx) {
  if (rf & 1) {
    const _r4 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "div", 11)(1, "button", 12, 1);
    ɵɵlistener("click", function FacetListComponent_section_0_div_12_Template_button_click_1_listener() {
      ɵɵrestoreView(_r4);
      const ctx_r1 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r1.close());
    });
    ɵɵtext(3);
    ɵɵpipe(4, "cxTranslate");
    ɵɵelementEnd()();
  }
  if (rf & 2) {
    ɵɵadvance(3);
    ɵɵtextInterpolate1(" ", ɵɵpipeBind1(4, 1, "productFacetNavigation.backToResults"), " ");
  }
}
function FacetListComponent_section_0_Template(rf, ctx) {
  if (rf & 1) {
    const _r1 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "section", 3);
    ɵɵpipe(1, "cxTranslate");
    ɵɵlistener("esc", function FacetListComponent_section_0_Template_section_esc_0_listener($event) {
      ɵɵrestoreView(_r1);
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.close($event));
    })("click", function FacetListComponent_section_0_Template_section_click_0_listener($event) {
      ɵɵrestoreView(_r1);
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.block($event));
    });
    ɵɵelementStart(2, "div", 4)(3, "h4");
    ɵɵtext(4);
    ɵɵpipe(5, "cxTranslate");
    ɵɵelementEnd();
    ɵɵelementStart(6, "button", 5);
    ɵɵpipe(7, "cxTranslate");
    ɵɵpipe(8, "cxTranslate");
    ɵɵlistener("click", function FacetListComponent_section_0_Template_button_click_6_listener() {
      ɵɵrestoreView(_r1);
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.close());
    });
    ɵɵelement(9, "cx-icon", 6);
    ɵɵelementEnd()();
    ɵɵelement(10, "cx-tab", 7);
    ɵɵtemplate(11, FacetListComponent_section_0_11_Template, 2, 0, null, 8)(12, FacetListComponent_section_0_div_12_Template, 5, 3, "div", 9);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const facets_r5 = ctx.ngIf;
    const ctx_r1 = ɵɵnextContext();
    ɵɵproperty("cxFocus", ctx_r1.isDialog ? ctx_r1.dialogFocusConfig : ɵɵpureFunction0(19, _c40))("tabindex", -1);
    ɵɵattribute("aria-label", ɵɵpipeBind1(1, 11, "productFacetNavigation.filterBy.facet"));
    ɵɵadvance(4);
    ɵɵtextInterpolate(ɵɵpipeBind1(5, 13, "productList.filterBy.label"));
    ɵɵadvance(2);
    ɵɵpropertyInterpolate("title", ɵɵpipeBind1(7, 15, "common.close"));
    ɵɵattribute("aria-label", ɵɵpipeBind1(8, 17, "common.close"));
    ɵɵadvance(3);
    ɵɵproperty("type", ctx_r1.iconTypes.CLOSE);
    ɵɵadvance();
    ɵɵproperty("tabs$", ctx_r1.tabs$)("config", ctx_r1.tabConfig);
    ɵɵadvance();
    ɵɵproperty("ngForOf", facets_r5);
    ɵɵadvance();
    ɵɵproperty("ngIf", ctx_r1.isDialog);
  }
}
var _c68 = ["trigger"];
function ProductFacetNavigationComponent_cx_facet_list_6_Template(rf, ctx) {
  if (rf & 1) {
    const _r2 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "cx-facet-list", 4);
    ɵɵpipe(1, "async");
    ɵɵlistener("closeList", function ProductFacetNavigationComponent_cx_facet_list_6_Template_cx_facet_list_closeList_0_listener() {
      ɵɵrestoreView(_r2);
      const ctx_r2 = ɵɵnextContext();
      return ɵɵresetView(ctx_r2.close());
    });
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r2 = ɵɵnextContext();
    ɵɵclassProp("active", ɵɵpipeBind1(1, 5, ctx_r2.isActive$))("dialog", ctx_r2.hasTrigger);
    ɵɵproperty("isDialog", ctx_r2.hasTrigger);
  }
}
function ProductCarouselItemComponent_ng_container_2_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainerStart(0);
    ɵɵelement(1, "cx-media", 6);
    ɵɵpipe(2, "async");
    ɵɵelementContainerEnd();
  }
  if (rf & 2) {
    let tmp_3_0;
    const lcpContext_r1 = ctx.$implicit;
    const ctx_r1 = ɵɵnextContext();
    ɵɵadvance();
    ɵɵproperty("container", ctx_r1.item.images == null ? null : ctx_r1.item.images.PRIMARY)("alt", (tmp_3_0 = ctx_r1.item.name) !== null && tmp_3_0 !== void 0 ? tmp_3_0 : "")("fetchPriority", ctx_r1.itemIndex === 0 ? ɵɵpipeBind1(2, 3, lcpContext_r1.fetchPriority$) : void 0);
  }
}
function ProductCarouselComponent_ng_container_0_cx_carousel_scrolling_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "cx-carousel-scrolling", 3);
    ɵɵpipe(1, "async");
    ɵɵpipe(2, "async");
    ɵɵpipe(3, "cxTranslate");
  }
  if (rf & 2) {
    const items_r1 = ctx.ngIf;
    const ctx_r1 = ɵɵnextContext(2);
    const carouselItem_r3 = ɵɵreference(3);
    ɵɵproperty("items", items_r1)("title", ɵɵpipeBind1(1, 5, ctx_r1.title$))("template", carouselItem_r3)("trackByFn", ctx_r1.trackByFn);
    ɵɵattribute("aria-label", ɵɵpipeBind2(3, 9, "productCarousel.carouselLabel", ɵɵpureFunction1(12, _c51, ɵɵpipeBind1(2, 7, ctx_r1.title$))));
  }
}
function ProductCarouselComponent_ng_container_0_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainerStart(0);
    ɵɵtemplate(1, ProductCarouselComponent_ng_container_0_cx_carousel_scrolling_1_Template, 4, 14, "cx-carousel-scrolling", 2);
    ɵɵpipe(2, "async");
    ɵɵelementContainerEnd();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext();
    ɵɵadvance();
    ɵɵproperty("ngIf", ɵɵpipeBind1(2, 1, ctx_r1.items$));
  }
}
function ProductCarouselComponent_ng_container_1_cx_carousel_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "cx-carousel", 5);
    ɵɵpipe(1, "async");
    ɵɵpipe(2, "async");
    ɵɵpipe(3, "cxTranslate");
  }
  if (rf & 2) {
    const items_r4 = ctx.ngIf;
    const ctx_r1 = ɵɵnextContext(2);
    const carouselItem_r3 = ɵɵreference(3);
    ɵɵproperty("items", items_r4)("title", ɵɵpipeBind1(1, 5, ctx_r1.title$))("template", carouselItem_r3)("trackByFn", ctx_r1.trackByFn);
    ɵɵattribute("aria-label", ɵɵpipeBind2(3, 9, "productCarousel.carouselLabel", ɵɵpureFunction1(12, _c51, ɵɵpipeBind1(2, 7, ctx_r1.title$))));
  }
}
function ProductCarouselComponent_ng_container_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainerStart(0);
    ɵɵtemplate(1, ProductCarouselComponent_ng_container_1_cx_carousel_1_Template, 4, 14, "cx-carousel", 4);
    ɵɵpipe(2, "async");
    ɵɵelementContainerEnd();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext();
    ɵɵadvance();
    ɵɵproperty("ngIf", ɵɵpipeBind1(2, 1, ctx_r1.items$));
  }
}
function ProductCarouselComponent_ng_template_2_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "cx-product-carousel-item", 6);
  }
  if (rf & 2) {
    const item_r5 = ctx.item;
    const itemIndex_r6 = ctx.itemIndex;
    ɵɵproperty("item", item_r5)("itemIndex", itemIndex_r6);
  }
}
function ProductReferencesComponent_ng_container_0_ng_container_1_cx_carousel_scrolling_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "cx-carousel-scrolling", 4);
    ɵɵpipe(1, "async");
  }
  if (rf & 2) {
    const items_r1 = ɵɵnextContext().ngIf;
    const ctx_r1 = ɵɵnextContext(2);
    const carouselItem_r3 = ɵɵreference(3);
    ɵɵproperty("title", ɵɵpipeBind1(1, 4, ctx_r1.title$))("items", items_r1)("template", carouselItem_r3)("trackByFn", ctx_r1.trackByFn);
  }
}
function ProductReferencesComponent_ng_container_0_ng_container_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainerStart(0);
    ɵɵtemplate(1, ProductReferencesComponent_ng_container_0_ng_container_1_cx_carousel_scrolling_1_Template, 2, 6, "cx-carousel-scrolling", 3);
    ɵɵelementContainerEnd();
  }
  if (rf & 2) {
    const items_r1 = ctx.ngIf;
    ɵɵadvance();
    ɵɵproperty("ngIf", items_r1.length);
  }
}
function ProductReferencesComponent_ng_container_0_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainerStart(0);
    ɵɵtemplate(1, ProductReferencesComponent_ng_container_0_ng_container_1_Template, 2, 1, "ng-container", 2);
    ɵɵpipe(2, "async");
    ɵɵelementContainerEnd();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext();
    ɵɵadvance();
    ɵɵproperty("ngIf", ɵɵpipeBind1(2, 1, ctx_r1.items$));
  }
}
function ProductReferencesComponent_ng_container_1_ng_container_1_cx_carousel_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "cx-carousel", 4);
    ɵɵpipe(1, "async");
  }
  if (rf & 2) {
    const items_r4 = ctx.ngIf;
    const ctx_r1 = ɵɵnextContext(3);
    const carouselItem_r3 = ɵɵreference(3);
    ɵɵproperty("title", ɵɵpipeBind1(1, 4, ctx_r1.title$))("items", items_r4)("template", carouselItem_r3)("trackByFn", ctx_r1.trackByFn);
  }
}
function ProductReferencesComponent_ng_container_1_ng_container_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainerStart(0);
    ɵɵtemplate(1, ProductReferencesComponent_ng_container_1_ng_container_1_cx_carousel_1_Template, 2, 6, "cx-carousel", 3);
    ɵɵpipe(2, "async");
    ɵɵelementContainerEnd();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext(2);
    ɵɵadvance();
    ɵɵproperty("ngIf", ɵɵpipeBind1(2, 1, ctx_r1.items$));
  }
}
function ProductReferencesComponent_ng_container_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainerStart(0);
    ɵɵtemplate(1, ProductReferencesComponent_ng_container_1_ng_container_1_Template, 3, 3, "ng-container", 2);
    ɵɵpipe(2, "async");
    ɵɵelementContainerEnd();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext();
    ɵɵadvance();
    ɵɵproperty("ngIf", ɵɵpipeBind1(2, 1, ctx_r1.items$));
  }
}
function ProductReferencesComponent_ng_template_2_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "a", 5);
    ɵɵpipe(1, "cxUrl");
    ɵɵelement(2, "cx-media", 6);
    ɵɵelementStart(3, "h4");
    ɵɵtext(4);
    ɵɵelementEnd();
    ɵɵelementStart(5, "div", 7);
    ɵɵtext(6);
    ɵɵelementEnd()();
  }
  if (rf & 2) {
    const item_r5 = ctx.item;
    ɵɵproperty("routerLink", ɵɵpipeBind1(1, 4, ɵɵpureFunction1(6, _c46, item_r5)));
    ɵɵadvance(2);
    ɵɵproperty("container", item_r5.images == null ? null : item_r5.images.PRIMARY);
    ɵɵadvance(2);
    ɵɵtextInterpolate(item_r5.name);
    ɵɵadvance(2);
    ɵɵtextInterpolate(item_r5.price == null ? null : item_r5.price.formattedValue);
  }
}
function ProductImagesComponent_ng_container_0_cx_media_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "cx-media", 3);
    ɵɵpipe(1, "async");
  }
  if (rf & 2) {
    const lcpContext_r1 = ctx.$implicit;
    const main_r2 = ɵɵnextContext().ngIf;
    ɵɵproperty("container", main_r2)("fetchPriority", ɵɵpipeBind1(1, 2, lcpContext_r1.fetchPriority$));
  }
}
function ProductImagesComponent_ng_container_0_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainerStart(0);
    ɵɵtemplate(1, ProductImagesComponent_ng_container_0_cx_media_1_Template, 2, 4, "cx-media", 2);
    ɵɵelementContainerEnd();
  }
}
function ProductImagesComponent_ng_container_2_ng_container_1_cx_carousel_scrolling_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "cx-carousel-scrolling", 6);
  }
  if (rf & 2) {
    const thumbs_r3 = ɵɵnextContext(2).ngIf;
    const ctx_r3 = ɵɵnextContext();
    const thumb_r5 = ɵɵreference(5);
    ɵɵproperty("items", thumbs_r3)("template", thumb_r5)("trackByFn", ctx_r3.trackByFn);
  }
}
function ProductImagesComponent_ng_container_2_ng_container_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainerStart(0);
    ɵɵtemplate(1, ProductImagesComponent_ng_container_2_ng_container_1_cx_carousel_scrolling_1_Template, 1, 3, "cx-carousel-scrolling", 5);
    ɵɵelementContainerEnd();
  }
  if (rf & 2) {
    const thumbs_r3 = ɵɵnextContext().ngIf;
    ɵɵadvance();
    ɵɵproperty("ngIf", thumbs_r3.length);
  }
}
function ProductImagesComponent_ng_container_2_ng_container_2_cx_carousel_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "cx-carousel", 8);
  }
  if (rf & 2) {
    const thumbs_r3 = ɵɵnextContext(2).ngIf;
    ɵɵnextContext();
    const thumb_r5 = ɵɵreference(5);
    ɵɵproperty("items", thumbs_r3)("hideIndicators", false)("template", thumb_r5);
  }
}
function ProductImagesComponent_ng_container_2_ng_container_2_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainerStart(0);
    ɵɵtemplate(1, ProductImagesComponent_ng_container_2_ng_container_2_cx_carousel_1_Template, 1, 3, "cx-carousel", 7);
    ɵɵelementContainerEnd();
  }
  if (rf & 2) {
    const thumbs_r3 = ɵɵnextContext().ngIf;
    ɵɵadvance();
    ɵɵproperty("ngIf", thumbs_r3.length);
  }
}
function ProductImagesComponent_ng_container_2_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainerStart(0);
    ɵɵtemplate(1, ProductImagesComponent_ng_container_2_ng_container_1_Template, 2, 1, "ng-container", 4)(2, ProductImagesComponent_ng_container_2_ng_container_2_Template, 2, 1, "ng-container", 4);
    ɵɵelementContainerEnd();
  }
  if (rf & 2) {
    ɵɵadvance();
    ɵɵproperty("cxFeature", "productCarouselScrolling");
    ɵɵadvance();
    ɵɵproperty("cxFeature", "!productCarouselScrolling");
  }
}
function ProductImagesComponent_ng_template_4_Template(rf, ctx) {
  if (rf & 1) {
    const _r6 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "cx-media", 9);
    ɵɵpipe(1, "async");
    ɵɵlistener("focus", function ProductImagesComponent_ng_template_4_Template_cx_media_focus_0_listener() {
      const item_r7 = ɵɵrestoreView(_r6).item;
      const ctx_r3 = ɵɵnextContext();
      return ɵɵresetView(ctx_r3.openImage(item_r7.container));
    });
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const item_r7 = ctx.item;
    const ctx_r3 = ɵɵnextContext();
    ɵɵclassProp("is-active", ɵɵpipeBind1(1, 3, ctx_r3.isActive(item_r7.container)));
    ɵɵproperty("container", item_r7.container);
  }
}
var _c69 = (a0, a1) => ({
  rating: a0,
  count: a1
});
function ProductIntroComponent_ng_container_0_div_1_ng_container_4_button_1_Template(rf, ctx) {
  if (rf & 1) {
    const _r1 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "button", 8);
    ɵɵpipe(1, "number");
    ɵɵpipe(2, "cxTranslate");
    ɵɵlistener("click", function ProductIntroComponent_ng_container_0_div_1_ng_container_4_button_1_Template_button_click_0_listener() {
      ɵɵrestoreView(_r1);
      const ctx_r1 = ɵɵnextContext(4);
      return ɵɵresetView(ctx_r1.showReviews());
    });
    ɵɵtext(3);
    ɵɵpipe(4, "cxTranslate");
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const product_r3 = ɵɵnextContext(3).ngIf;
    ɵɵattribute("aria-label", ɵɵpipeBind2(2, 5, "productSummary.showReviewsDetailed", ɵɵpureFunction2(10, _c69, ɵɵpipeBind2(1, 2, product_r3 == null ? null : product_r3.averageRating, "1.0-1"), product_r3 == null ? null : product_r3.numberOfReviews)));
    ɵɵadvance(3);
    ɵɵtextInterpolate1(" ", ɵɵpipeBind1(4, 8, "productSummary.showReviews"), " ");
  }
}
function ProductIntroComponent_ng_container_0_div_1_ng_container_4_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainerStart(0);
    ɵɵtemplate(1, ProductIntroComponent_ng_container_0_div_1_ng_container_4_button_1_Template, 5, 13, "button", 7);
    ɵɵelementContainerEnd();
  }
  if (rf & 2) {
    const product_r3 = ɵɵnextContext(2).ngIf;
    ɵɵadvance();
    ɵɵproperty("ngIf", product_r3 == null ? null : product_r3.averageRating);
  }
}
function ProductIntroComponent_ng_container_0_div_1_ng_container_5_button_1_Template(rf, ctx) {
  if (rf & 1) {
    const _r4 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "button", 10);
    ɵɵpipe(1, "number");
    ɵɵpipe(2, "cxTranslate");
    ɵɵlistener("click", function ProductIntroComponent_ng_container_0_div_1_ng_container_5_button_1_Template_button_click_0_listener() {
      ɵɵrestoreView(_r4);
      const ctx_r1 = ɵɵnextContext(4);
      return ɵɵresetView(ctx_r1.showReviews());
    });
    ɵɵtext(3);
    ɵɵpipe(4, "cxTranslate");
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const product_r3 = ɵɵnextContext(3).ngIf;
    ɵɵattribute("aria-label", ɵɵpipeBind2(2, 5, "productSummary.showReviewsDetailed", ɵɵpureFunction2(10, _c69, ɵɵpipeBind2(1, 2, product_r3 == null ? null : product_r3.averageRating, "1.0-1"), product_r3 == null ? null : product_r3.numberOfReviews)));
    ɵɵadvance(3);
    ɵɵtextInterpolate1(" ", ɵɵpipeBind1(4, 8, "productSummary.showReviews"), " ");
  }
}
function ProductIntroComponent_ng_container_0_div_1_ng_container_5_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainerStart(0);
    ɵɵtemplate(1, ProductIntroComponent_ng_container_0_div_1_ng_container_5_button_1_Template, 5, 13, "button", 9);
    ɵɵpipe(2, "async");
    ɵɵelementContainerEnd();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext(3);
    ɵɵadvance();
    ɵɵproperty("ngIf", ɵɵpipeBind1(2, 1, ctx_r1.areReviewsAvailable$));
  }
}
function ProductIntroComponent_ng_container_0_div_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div", 3);
    ɵɵelement(1, "cx-star-rating", 4);
    ɵɵelementStart(2, "div", 5);
    ɵɵtext(3);
    ɵɵelementEnd();
    ɵɵtemplate(4, ProductIntroComponent_ng_container_0_div_1_ng_container_4_Template, 2, 1, "ng-container", 6)(5, ProductIntroComponent_ng_container_0_div_1_ng_container_5_Template, 3, 3, "ng-container", 6);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    let tmp_3_0;
    const product_r3 = ɵɵnextContext().ngIf;
    ɵɵadvance();
    ɵɵproperty("rating", (tmp_3_0 = product_r3 == null ? null : product_r3.averageRating) !== null && tmp_3_0 !== void 0 ? tmp_3_0 : 0);
    ɵɵadvance(2);
    ɵɵtextInterpolate1("(", product_r3 == null ? null : product_r3.numberOfReviews, ")");
    ɵɵadvance();
    ɵɵproperty("cxFeature", "a11yLinkBtnsToTertiaryBtns");
    ɵɵadvance();
    ɵɵproperty("cxFeature", "!a11yLinkBtnsToTertiaryBtns");
  }
}
function ProductIntroComponent_ng_container_0_div_2_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div", 3);
    ɵɵtext(1);
    ɵɵpipe(2, "cxTranslate");
    ɵɵelementEnd();
  }
  if (rf & 2) {
    ɵɵadvance();
    ɵɵtextInterpolate1(" ", ɵɵpipeBind1(2, 1, "productDetails.noReviews"), " ");
  }
}
function ProductIntroComponent_ng_container_0_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainerStart(0);
    ɵɵtemplate(1, ProductIntroComponent_ng_container_0_div_1_Template, 6, 4, "div", 1)(2, ProductIntroComponent_ng_container_0_div_2_Template, 3, 3, "div", 1);
    ɵɵelementStart(3, "div", 2);
    ɵɵtext(4);
    ɵɵpipe(5, "cxTranslate");
    ɵɵelementEnd();
    ɵɵelementContainerEnd();
  }
  if (rf & 2) {
    const product_r3 = ctx.ngIf;
    ɵɵadvance();
    ɵɵproperty("ngIf", product_r3 == null ? null : product_r3.averageRating);
    ɵɵadvance();
    ɵɵproperty("ngIf", !(product_r3 == null ? null : product_r3.averageRating));
    ɵɵadvance(2);
    ɵɵtextInterpolate2(" ", ɵɵpipeBind1(5, 4, "productSummary.id"), " ", product_r3 == null ? null : product_r3.code, " ");
  }
}
function ProductSummaryComponent_ng_container_0_ng_container_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainerStart(0);
    ɵɵelement(1, "cx-promotions", 2);
    ɵɵelementContainerEnd();
  }
  if (rf & 2) {
    const product_r1 = ɵɵnextContext().ngIf;
    ɵɵadvance();
    ɵɵproperty("promotions", product_r1.potentialPromotions);
  }
}
function ProductSummaryComponent_ng_container_0_ng_template_2_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div", 3);
    ɵɵpipe(1, "cxTranslate");
    ɵɵtext(2);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const product_r1 = ɵɵnextContext().ngIf;
    ɵɵattribute("aria-label", ɵɵpipeBind1(1, 2, "productSummary.newItemPrice"));
    ɵɵadvance(2);
    ɵɵtextInterpolate1(" ", product_r1 == null ? null : product_r1.price == null ? null : product_r1.price.formattedValue, " ");
  }
}
function ProductSummaryComponent_ng_container_0_ng_template_3_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "p", 4);
  }
  if (rf & 2) {
    const product_r1 = ɵɵnextContext().ngIf;
    ɵɵproperty("innerHTML", product_r1 == null ? null : product_r1.summary, ɵɵsanitizeHtml);
  }
}
function ProductSummaryComponent_ng_container_0_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainerStart(0);
    ɵɵtemplate(1, ProductSummaryComponent_ng_container_0_ng_container_1_Template, 2, 1, "ng-container", 0)(2, ProductSummaryComponent_ng_container_0_ng_template_2_Template, 3, 4, "ng-template", 1)(3, ProductSummaryComponent_ng_container_0_ng_template_3_Template, 1, 1, "ng-template", 1);
    ɵɵelementContainerEnd();
  }
  if (rf & 2) {
    const product_r1 = ctx.ngIf;
    const ctx_r1 = ɵɵnextContext();
    ɵɵadvance();
    ɵɵproperty("ngIf", product_r1.potentialPromotions);
    ɵɵadvance();
    ɵɵproperty("cxOutlet", ctx_r1.outlets.PRICE)("cxOutletContext", ɵɵpureFunction1(5, _c60, product_r1));
    ɵɵadvance();
    ɵɵproperty("cxOutlet", ctx_r1.outlets.SUMMARY)("cxOutletContext", ɵɵpureFunction1(7, _c60, product_r1));
  }
}
function ProductAttributesComponent_ng_container_0_table_5_tr_4_li_5_span_2_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "span");
    ɵɵtext(1);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const feature_r1 = ɵɵnextContext(2).$implicit;
    ɵɵadvance();
    ɵɵtextInterpolate1(" ", feature_r1.featureUnit == null ? null : feature_r1.featureUnit.symbol, " ");
  }
}
function ProductAttributesComponent_ng_container_0_table_5_tr_4_li_5_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "li");
    ɵɵtext(1);
    ɵɵtemplate(2, ProductAttributesComponent_ng_container_0_table_5_tr_4_li_5_span_2_Template, 2, 1, "span", 0);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const featureValue_r2 = ctx.$implicit;
    const feature_r1 = ɵɵnextContext().$implicit;
    ɵɵadvance();
    ɵɵtextInterpolate1(" ", featureValue_r2 == null ? null : featureValue_r2.value, " ");
    ɵɵadvance();
    ɵɵproperty("ngIf", feature_r1.featureUnit && feature_r1.featureUnit.symbol && feature_r1.featureUnit.symbol.length > 0 && (feature_r1.featureUnit == null ? null : feature_r1.featureUnit.unitType) !== "300");
  }
}
function ProductAttributesComponent_ng_container_0_table_5_tr_4_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "tr")(1, "td");
    ɵɵtext(2);
    ɵɵelementEnd();
    ɵɵelementStart(3, "td")(4, "ul");
    ɵɵtemplate(5, ProductAttributesComponent_ng_container_0_table_5_tr_4_li_5_Template, 3, 2, "li", 2);
    ɵɵelementEnd()()();
  }
  if (rf & 2) {
    const feature_r1 = ctx.$implicit;
    ɵɵadvance(2);
    ɵɵtextInterpolate(feature_r1.name);
    ɵɵadvance(3);
    ɵɵproperty("ngForOf", feature_r1 == null ? null : feature_r1.featureValues);
  }
}
function ProductAttributesComponent_ng_container_0_table_5_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "table")(1, "th")(2, "h2");
    ɵɵtext(3);
    ɵɵelementEnd()();
    ɵɵtemplate(4, ProductAttributesComponent_ng_container_0_table_5_tr_4_Template, 6, 2, "tr", 2);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const class_r3 = ctx.$implicit;
    ɵɵadvance(3);
    ɵɵtextInterpolate(class_r3.name);
    ɵɵadvance();
    ɵɵproperty("ngForOf", class_r3.features);
  }
}
function ProductAttributesComponent_ng_container_0_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainerStart(0);
    ɵɵelementStart(1, "div", 1)(2, "h2");
    ɵɵtext(3);
    ɵɵpipe(4, "cxTranslate");
    ɵɵelementEnd();
    ɵɵtemplate(5, ProductAttributesComponent_ng_container_0_table_5_Template, 5, 2, "table", 2);
    ɵɵelementEnd();
    ɵɵelementContainerEnd();
  }
  if (rf & 2) {
    const product_r4 = ctx.ngIf;
    ɵɵadvance(3);
    ɵɵtextInterpolate(ɵɵpipeBind1(4, 2, "productDetails.specification"));
    ɵɵadvance(2);
    ɵɵproperty("ngForOf", product_r4 == null ? null : product_r4.classifications);
  }
}
function ProductDetailsTabComponent_ng_container_0_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainerStart(0);
    ɵɵelement(1, "div", 1);
    ɵɵelementContainerEnd();
  }
  if (rf & 2) {
    const product_r1 = ctx.ngIf;
    ɵɵadvance();
    ɵɵproperty("innerHTML", product_r1 == null ? null : product_r1.description, ɵɵsanitizeHtml);
  }
}
function ProductDetailsTabComponent_ng_container_2_ng_container_1_ng_container_1_ng_template_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div", 4);
    ɵɵelementContainer(1, 5);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const child_r2 = ɵɵnextContext(2).$implicit;
    ɵɵadvance();
    ɵɵproperty("cxComponentWrapper", child_r2);
  }
}
function ProductDetailsTabComponent_ng_container_2_ng_container_1_ng_container_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainerStart(0);
    ɵɵtemplate(1, ProductDetailsTabComponent_ng_container_2_ng_container_1_ng_container_1_ng_template_1_Template, 2, 1, "ng-template", 3);
    ɵɵelementContainerEnd();
  }
  if (rf & 2) {
    const child_r2 = ɵɵnextContext().$implicit;
    ɵɵadvance();
    ɵɵproperty("cxOutlet", child_r2.flexType)("cxOutletContext", ɵɵpureFunction0(2, _c40));
  }
}
function ProductDetailsTabComponent_ng_container_2_ng_container_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainerStart(0);
    ɵɵtemplate(1, ProductDetailsTabComponent_ng_container_2_ng_container_1_ng_container_1_Template, 2, 3, "ng-container", 0);
    ɵɵelementContainerEnd();
  }
  if (rf & 2) {
    const child_r2 = ctx.$implicit;
    ɵɵadvance();
    ɵɵproperty("ngIf", child_r2);
  }
}
function ProductDetailsTabComponent_ng_container_2_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainerStart(0);
    ɵɵtemplate(1, ProductDetailsTabComponent_ng_container_2_ng_container_1_Template, 2, 1, "ng-container", 2);
    ɵɵelementContainerEnd();
  }
  if (rf & 2) {
    const children_r3 = ctx.ngIf;
    ɵɵadvance();
    ɵɵproperty("ngForOf", children_r3);
  }
}
var _c70 = ["titleInput"];
var _c71 = ["writeReviewButton"];
function ProductReviewsComponent_div_0_ng_container_4_cx_star_rating_9_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "cx-star-rating", 10);
  }
  if (rf & 2) {
    const product_r3 = ɵɵnextContext(2).ngIf;
    ɵɵproperty("rating", product_r3.averageRating);
  }
}
function ProductReviewsComponent_div_0_ng_container_4_div_10_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div", 11);
    ɵɵtext(1);
    ɵɵpipe(2, "cxTranslate");
    ɵɵelementEnd();
  }
  if (rf & 2) {
    ɵɵadvance();
    ɵɵtextInterpolate1(" ", ɵɵpipeBind1(2, 1, "productDetails.noReviews"), " ");
  }
}
function ProductReviewsComponent_div_0_ng_container_4_ng_container_11_ng_container_1_div_1_div_9_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div", 21);
    ɵɵtext(1);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const review_r4 = ɵɵnextContext().$implicit;
    ɵɵadvance();
    ɵɵtextInterpolate1(" ", review_r4.comment, " ");
  }
}
function ProductReviewsComponent_div_0_ng_container_4_ng_container_11_ng_container_1_div_1_cx_read_more_10_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "cx-read-more", 22);
  }
  if (rf & 2) {
    const review_r4 = ɵɵnextContext().$implicit;
    ɵɵproperty("text", review_r4.comment);
  }
}
function ProductReviewsComponent_div_0_ng_container_4_ng_container_11_ng_container_1_div_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div", 14)(1, "div", 15);
    ɵɵtext(2);
    ɵɵelementEnd();
    ɵɵelement(3, "cx-star-rating", 16);
    ɵɵelementStart(4, "div", 17);
    ɵɵtext(5);
    ɵɵelementEnd();
    ɵɵelementStart(6, "div", 18);
    ɵɵtext(7);
    ɵɵpipe(8, "cxDate");
    ɵɵelementEnd();
    ɵɵtemplate(9, ProductReviewsComponent_div_0_ng_container_4_ng_container_11_ng_container_1_div_1_div_9_Template, 2, 1, "div", 19)(10, ProductReviewsComponent_div_0_ng_container_4_ng_container_11_ng_container_1_div_1_cx_read_more_10_Template, 1, 1, "cx-read-more", 20);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    let tmp_11_0;
    const review_r4 = ctx.$implicit;
    ɵɵadvance(2);
    ɵɵtextInterpolate(review_r4.headline);
    ɵɵadvance();
    ɵɵproperty("rating", (tmp_11_0 = review_r4.rating) !== null && tmp_11_0 !== void 0 ? tmp_11_0 : 0);
    ɵɵadvance(2);
    ɵɵtextInterpolate1(" ", review_r4.alias ? review_r4.alias : review_r4.principal == null ? null : review_r4.principal.name, " ");
    ɵɵadvance(2);
    ɵɵtextInterpolate(ɵɵpipeBind1(8, 6, review_r4.date));
    ɵɵadvance(2);
    ɵɵproperty("cxFeature", "!readMoreDirective");
    ɵɵadvance();
    ɵɵproperty("cxFeature", "readMoreDirective");
  }
}
function ProductReviewsComponent_div_0_ng_container_4_ng_container_11_ng_container_1_div_3_Template(rf, ctx) {
  if (rf & 1) {
    const _r5 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "div")(1, "button", 23);
    ɵɵlistener("click", function ProductReviewsComponent_div_0_ng_container_4_ng_container_11_ng_container_1_div_3_Template_button_click_1_listener() {
      ɵɵrestoreView(_r5);
      const reviews_r6 = ɵɵnextContext().ngIf;
      const ctx_r1 = ɵɵnextContext(4);
      return ɵɵresetView(ctx_r1.maxListItems === ctx_r1.initialMaxListItems ? ctx_r1.maxListItems = reviews_r6.length : ctx_r1.maxListItems = ctx_r1.initialMaxListItems);
    });
    ɵɵtext(2);
    ɵɵpipe(3, "cxTranslate");
    ɵɵelementEnd()();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext(5);
    ɵɵadvance(2);
    ɵɵtextInterpolate1(" ", ɵɵpipeBind1(3, 1, ctx_r1.maxListItems === ctx_r1.initialMaxListItems ? "productReview.more" : "productReview.less"), " ");
  }
}
function ProductReviewsComponent_div_0_ng_container_4_ng_container_11_ng_container_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainerStart(0);
    ɵɵtemplate(1, ProductReviewsComponent_div_0_ng_container_4_ng_container_11_ng_container_1_div_1_Template, 11, 8, "div", 13);
    ɵɵpipe(2, "slice");
    ɵɵtemplate(3, ProductReviewsComponent_div_0_ng_container_4_ng_container_11_ng_container_1_div_3_Template, 4, 3, "div", 12);
    ɵɵelementContainerEnd();
  }
  if (rf & 2) {
    const reviews_r6 = ctx.ngIf;
    const ctx_r1 = ɵɵnextContext(4);
    ɵɵadvance();
    ɵɵproperty("ngForOf", ɵɵpipeBind3(2, 2, reviews_r6, 0, ctx_r1.maxListItems));
    ɵɵadvance(2);
    ɵɵproperty("ngIf", reviews_r6.length > ctx_r1.initialMaxListItems);
  }
}
function ProductReviewsComponent_div_0_ng_container_4_ng_container_11_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainerStart(0);
    ɵɵtemplate(1, ProductReviewsComponent_div_0_ng_container_4_ng_container_11_ng_container_1_Template, 4, 6, "ng-container", 12);
    ɵɵpipe(2, "async");
    ɵɵelementContainerEnd();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext(3);
    ɵɵadvance();
    ɵɵproperty("ngIf", ɵɵpipeBind1(2, 1, ctx_r1.reviews$));
  }
}
function ProductReviewsComponent_div_0_ng_container_4_Template(rf, ctx) {
  if (rf & 1) {
    const _r1 = ɵɵgetCurrentView();
    ɵɵelementContainerStart(0);
    ɵɵelementStart(1, "div", 6)(2, "h3");
    ɵɵtext(3);
    ɵɵpipe(4, "cxTranslate");
    ɵɵelementEnd();
    ɵɵelementStart(5, "button", 7, 1);
    ɵɵlistener("click", function ProductReviewsComponent_div_0_ng_container_4_Template_button_click_5_listener() {
      ɵɵrestoreView(_r1);
      const ctx_r1 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r1.initiateWriteReview());
    });
    ɵɵtext(7);
    ɵɵpipe(8, "cxTranslate");
    ɵɵelementEnd();
    ɵɵtemplate(9, ProductReviewsComponent_div_0_ng_container_4_cx_star_rating_9_Template, 1, 1, "cx-star-rating", 8)(10, ProductReviewsComponent_div_0_ng_container_4_div_10_Template, 3, 3, "div", 9);
    ɵɵelementEnd();
    ɵɵtemplate(11, ProductReviewsComponent_div_0_ng_container_4_ng_container_11_Template, 3, 3, "ng-container", 5);
    ɵɵelementContainerEnd();
  }
  if (rf & 2) {
    const product_r3 = ɵɵnextContext().ngIf;
    const writeReview_r7 = ɵɵreference(6);
    const ctx_r1 = ɵɵnextContext();
    ɵɵadvance(3);
    ɵɵtextInterpolate(ɵɵpipeBind1(4, 6, "productReview.overallRating"));
    ɵɵadvance(4);
    ɵɵtextInterpolate1(" ", ɵɵpipeBind1(8, 8, "productReview.writeReview"), " ");
    ɵɵadvance(2);
    ɵɵproperty("ngIf", product_r3.averageRating);
    ɵɵadvance();
    ɵɵproperty("ngIf", !product_r3.averageRating);
    ɵɵadvance();
    ɵɵproperty("ngIf", !ctx_r1.isWritingReview)("ngIfElse", writeReview_r7);
  }
}
function ProductReviewsComponent_div_0_ng_template_5_input_8_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "input", 45, 2);
  }
  if (rf & 2) {
    let tmp_6_0;
    const ctx_r1 = ɵɵnextContext(3);
    ɵɵattribute("aria-invalid", ((tmp_6_0 = ctx_r1.reviewForm.get("title")) == null ? null : tmp_6_0.touched) && ((tmp_6_0 = ctx_r1.reviewForm.get("title")) == null ? null : tmp_6_0.invalid))("aria-errormessage", "titleError");
  }
}
function ProductReviewsComponent_div_0_ng_template_5_input_9_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "input", 46, 2);
  }
  if (rf & 2) {
    let tmp_7_0;
    const ctx_r1 = ɵɵnextContext(3);
    ɵɵproperty("maxLength", ctx_r1.maxLengthReviewTitle);
    ɵɵattribute("aria-invalid", ((tmp_7_0 = ctx_r1.reviewForm.get("title")) == null ? null : tmp_7_0.touched) && ((tmp_7_0 = ctx_r1.reviewForm.get("title")) == null ? null : tmp_7_0.invalid))("aria-errormessage", "titleError");
  }
}
function ProductReviewsComponent_div_0_ng_template_5_textarea_18_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "textarea", 47);
  }
  if (rf & 2) {
    let tmp_5_0;
    const ctx_r1 = ɵɵnextContext(3);
    ɵɵattribute("aria-invalid", ((tmp_5_0 = ctx_r1.reviewForm.get("comment")) == null ? null : tmp_5_0.touched) && ((tmp_5_0 = ctx_r1.reviewForm.get("comment")) == null ? null : tmp_5_0.invalid))("aria-errormessage", "commentError");
  }
}
function ProductReviewsComponent_div_0_ng_template_5_textarea_19_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "textarea", 48);
    ɵɵpipe(1, "cxTranslate");
  }
  if (rf & 2) {
    let tmp_7_0;
    const ctx_r1 = ɵɵnextContext(3);
    ɵɵproperty("placeholder", ɵɵpipeBind1(1, 4, "productReview.commentPlaceholder"))("maxLength", ctx_r1.maxLengthReviewComment);
    ɵɵattribute("aria-invalid", ((tmp_7_0 = ctx_r1.reviewForm.get("comment")) == null ? null : tmp_7_0.touched) && ((tmp_7_0 = ctx_r1.reviewForm.get("comment")) == null ? null : tmp_7_0.invalid))("aria-errormessage", "commentError");
  }
}
function ProductReviewsComponent_div_0_ng_template_5_input_38_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "input", 49);
  }
}
function ProductReviewsComponent_div_0_ng_template_5_input_39_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "input", 50);
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext(3);
    ɵɵproperty("maxLength", ctx_r1.maxLengthReviewerName);
  }
}
function ProductReviewsComponent_div_0_ng_template_5_cx_form_errors_40_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "cx-form-errors", 29);
    ɵɵpipe(1, "cxTranslate");
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext(3);
    ɵɵproperty("translationParams", ɵɵpureFunction1(4, _c45, ɵɵpipeBind1(1, 2, "productReview.reviewerName")))("control", ctx_r1.reviewForm.get("reviewerName"));
  }
}
function ProductReviewsComponent_div_0_ng_template_5_cx_form_errors_41_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "cx-form-errors", 51);
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext(3);
    ɵɵproperty("control", ctx_r1.reviewForm.get("reviewerName"));
  }
}
function ProductReviewsComponent_div_0_ng_template_5_p_42_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "p", 52);
    ɵɵtext(1);
    ɵɵpipe(2, "cxTranslate");
    ɵɵelementEnd();
  }
  if (rf & 2) {
    let tmp_5_0;
    const ctx_r1 = ɵɵnextContext(3);
    ɵɵadvance();
    ɵɵtextInterpolate1(" ", ɵɵpipeBind2(2, 1, "productReview.charactersLeft", ɵɵpureFunction1(4, _c322, ctx_r1.maxLengthReviewerName - ((tmp_5_0 = (tmp_5_0 = ctx_r1.reviewForm.get("reviewerName")) == null ? null : tmp_5_0.value == null ? null : tmp_5_0.value.length) !== null && tmp_5_0 !== void 0 ? tmp_5_0 : 0))), " ");
  }
}
function ProductReviewsComponent_div_0_ng_template_5_Template(rf, ctx) {
  if (rf & 1) {
    const _r8 = ɵɵgetCurrentView();
    ɵɵelement(0, "cx-form-required-legend");
    ɵɵelementStart(1, "form", 24);
    ɵɵlistener("ngSubmit", function ProductReviewsComponent_div_0_ng_template_5_Template_form_ngSubmit_1_listener() {
      ɵɵrestoreView(_r8);
      const product_r3 = ɵɵnextContext().ngIf;
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.submitReview(product_r3));
    });
    ɵɵelementStart(2, "div", 25)(3, "label")(4, "span", 26);
    ɵɵtext(5);
    ɵɵpipe(6, "cxTranslate");
    ɵɵelement(7, "cx-form-required-asterisks");
    ɵɵelementEnd();
    ɵɵtemplate(8, ProductReviewsComponent_div_0_ng_template_5_input_8_Template, 2, 2, "input", 27)(9, ProductReviewsComponent_div_0_ng_template_5_input_9_Template, 2, 3, "input", 28);
    ɵɵelement(10, "cx-form-errors", 29);
    ɵɵpipe(11, "cxTranslate");
    ɵɵelementEnd()();
    ɵɵelementStart(12, "div", 25)(13, "label")(14, "span", 26);
    ɵɵtext(15);
    ɵɵpipe(16, "cxTranslate");
    ɵɵelement(17, "cx-form-required-asterisks");
    ɵɵelementEnd();
    ɵɵtemplate(18, ProductReviewsComponent_div_0_ng_template_5_textarea_18_Template, 1, 2, "textarea", 30)(19, ProductReviewsComponent_div_0_ng_template_5_textarea_19_Template, 2, 6, "textarea", 31);
    ɵɵelement(20, "cx-form-errors", 32);
    ɵɵpipe(21, "cxTranslate");
    ɵɵelementEnd()();
    ɵɵelementStart(22, "div", 25)(23, "label")(24, "span", 26);
    ɵɵtext(25);
    ɵɵpipe(26, "cxTranslate");
    ɵɵelement(27, "cx-form-required-asterisks");
    ɵɵelementEnd();
    ɵɵelement(28, "input", 33);
    ɵɵelementStart(29, "cx-star-rating", 34);
    ɵɵpipe(30, "cxTranslate");
    ɵɵlistener("change", function ProductReviewsComponent_div_0_ng_template_5_Template_cx_star_rating_change_29_listener($event) {
      ɵɵrestoreView(_r8);
      const ctx_r1 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r1.setRating($event));
    });
    ɵɵelementEnd();
    ɵɵelement(31, "cx-form-errors", 35);
    ɵɵpipe(32, "cxTranslate");
    ɵɵelementEnd()();
    ɵɵelementStart(33, "div", 25)(34, "label")(35, "span", 26);
    ɵɵtext(36);
    ɵɵpipe(37, "cxTranslate");
    ɵɵelementEnd();
    ɵɵtemplate(38, ProductReviewsComponent_div_0_ng_template_5_input_38_Template, 1, 0, "input", 36)(39, ProductReviewsComponent_div_0_ng_template_5_input_39_Template, 1, 1, "input", 37)(40, ProductReviewsComponent_div_0_ng_template_5_cx_form_errors_40_Template, 2, 6, "cx-form-errors", 38)(41, ProductReviewsComponent_div_0_ng_template_5_cx_form_errors_41_Template, 1, 1, "cx-form-errors", 39)(42, ProductReviewsComponent_div_0_ng_template_5_p_42_Template, 3, 6, "p", 40);
    ɵɵelementEnd()();
    ɵɵelementStart(43, "div", 41)(44, "div", 42)(45, "button", 43);
    ɵɵlistener("click", function ProductReviewsComponent_div_0_ng_template_5_Template_button_click_45_listener() {
      ɵɵrestoreView(_r8);
      const ctx_r1 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r1.cancelWriteReview());
    });
    ɵɵtext(46);
    ɵɵpipe(47, "cxTranslate");
    ɵɵelementEnd()();
    ɵɵelementStart(48, "div", 42)(49, "button", 44);
    ɵɵtext(50);
    ɵɵpipe(51, "cxTranslate");
    ɵɵelementEnd()()()();
  }
  if (rf & 2) {
    let tmp_16_0;
    const ctx_r1 = ɵɵnextContext(2);
    ɵɵadvance();
    ɵɵproperty("formGroup", ctx_r1.reviewForm);
    ɵɵadvance(4);
    ɵɵtextInterpolate1("", ɵɵpipeBind1(6, 26, "productReview.reviewTitle"), " ");
    ɵɵadvance(3);
    ɵɵproperty("cxFeature", "!productReviewCharactersLeft");
    ɵɵadvance();
    ɵɵproperty("cxFeature", "productReviewCharactersLeft");
    ɵɵadvance();
    ɵɵproperty("translationParams", ɵɵpureFunction1(46, _c45, ɵɵpipeBind1(11, 28, "productReview.reviewTitle")))("control", ctx_r1.reviewForm.get("title"));
    ɵɵadvance(5);
    ɵɵtextInterpolate1("", ɵɵpipeBind1(16, 30, "productReview.writeYourComments"), " ");
    ɵɵadvance(3);
    ɵɵproperty("cxFeature", "!productReviewCharactersLeft");
    ɵɵadvance();
    ɵɵproperty("cxFeature", "productReviewCharactersLeft");
    ɵɵadvance();
    ɵɵproperty("translationParams", ɵɵpureFunction1(48, _c45, ɵɵpipeBind1(21, 32, "productReview.writeYourComments")))("control", ctx_r1.reviewForm.get("comment"));
    ɵɵadvance(5);
    ɵɵtextInterpolate1("", ɵɵpipeBind1(26, 34, "productReview.rating"), " ");
    ɵɵadvance(3);
    ɵɵattribute("aria-invalid", ((tmp_16_0 = ctx_r1.reviewForm.get("rating")) == null ? null : tmp_16_0.touched) && ((tmp_16_0 = ctx_r1.reviewForm.get("rating")) == null ? null : tmp_16_0.invalid))("aria-errormessage", "ratingError");
    ɵɵadvance();
    ɵɵproperty("disabled", false);
    ɵɵattribute("aria-label", ɵɵpipeBind1(30, 36, "productReview.ratingRequired"));
    ɵɵadvance(2);
    ɵɵproperty("translationParams", ɵɵpureFunction1(50, _c45, ɵɵpipeBind1(32, 38, "productReview.rating")))("control", ctx_r1.reviewForm.get("rating"));
    ɵɵadvance(5);
    ɵɵtextInterpolate1("", ɵɵpipeBind1(37, 40, "productReview.reviewerName"), " ");
    ɵɵadvance(2);
    ɵɵproperty("cxFeature", "!productReviewCharactersLeft");
    ɵɵadvance();
    ɵɵproperty("cxFeature", "productReviewCharactersLeft");
    ɵɵadvance();
    ɵɵproperty("cxFeature", "formErrorsDescriptiveMessages");
    ɵɵadvance();
    ɵɵproperty("cxFeature", "!formErrorsDescriptiveMessages");
    ɵɵadvance();
    ɵɵproperty("cxFeature", "productReviewCharactersLeft");
    ɵɵadvance(4);
    ɵɵtextInterpolate1(" ", ɵɵpipeBind1(47, 42, "common.cancel"), " ");
    ɵɵadvance(4);
    ɵɵtextInterpolate1(" ", ɵɵpipeBind1(51, 44, "common.submit"), " ");
  }
}
function ProductReviewsComponent_div_0_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div", 4)(1, "h2");
    ɵɵtext(2);
    ɵɵpipe(3, "cxTranslate");
    ɵɵelementEnd();
    ɵɵtemplate(4, ProductReviewsComponent_div_0_ng_container_4_Template, 12, 10, "ng-container", 5)(5, ProductReviewsComponent_div_0_ng_template_5_Template, 52, 52, "ng-template", null, 0, ɵɵtemplateRefExtractor);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const product_r3 = ctx.ngIf;
    const writeReview_r7 = ɵɵreference(6);
    const ctx_r1 = ɵɵnextContext();
    ɵɵadvance(2);
    ɵɵtextInterpolate2(" ", ɵɵpipeBind1(3, 4, "productDetails.reviews"), " (", product_r3.numberOfReviews, ") ");
    ɵɵadvance(2);
    ɵɵproperty("ngIf", !ctx_r1.isWritingReview)("ngIfElse", writeReview_r7);
  }
}
var _c722 = () => ({
  cxRoute: "notificationPreference"
});
var _c73 = () => ({
  cxRoute: "myInterests"
});
function StockNotificationDialogComponent_ng_container_10_p_6_span_3_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "span");
    ɵɵtext(1);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const preference_r3 = ɵɵnextContext().$implicit;
    ɵɵadvance();
    ɵɵtextInterpolate(": " + preference_r3.value);
  }
}
function StockNotificationDialogComponent_ng_container_10_p_6_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "p", 18)(1, "span");
    ɵɵtext(2);
    ɵɵelementEnd();
    ɵɵtemplate(3, StockNotificationDialogComponent_ng_container_10_p_6_span_3_Template, 2, 1, "span", 19);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const preference_r3 = ctx.$implicit;
    ɵɵadvance(2);
    ɵɵtextInterpolate(preference_r3.channel);
    ɵɵadvance();
    ɵɵproperty("ngIf", preference_r3.value);
  }
}
function StockNotificationDialogComponent_ng_container_10_Template(rf, ctx) {
  if (rf & 1) {
    const _r2 = ɵɵgetCurrentView();
    ɵɵelementContainerStart(0);
    ɵɵelementStart(1, "div", 8)(2, "div", 9)(3, "p", 10);
    ɵɵtext(4);
    ɵɵpipe(5, "cxTranslate");
    ɵɵelementEnd();
    ɵɵtemplate(6, StockNotificationDialogComponent_ng_container_10_p_6_Template, 4, 2, "p", 11);
    ɵɵelementStart(7, "p", 10);
    ɵɵtext(8);
    ɵɵpipe(9, "cxTranslate");
    ɵɵelementEnd();
    ɵɵelementStart(10, "p", 10);
    ɵɵtext(11);
    ɵɵpipe(12, "cxTranslate");
    ɵɵelementStart(13, "a", 12);
    ɵɵpipe(14, "cxUrl");
    ɵɵlistener("click", function StockNotificationDialogComponent_ng_container_10_Template_a_click_13_listener() {
      ɵɵrestoreView(_r2);
      const ctx_r3 = ɵɵnextContext();
      return ɵɵresetView(ctx_r3.close("Link clicked"));
    });
    ɵɵtext(15);
    ɵɵpipe(16, "cxTranslate");
    ɵɵelementEnd();
    ɵɵtext(17);
    ɵɵpipe(18, "cxTranslate");
    ɵɵelementEnd();
    ɵɵelementStart(19, "p", 10);
    ɵɵtext(20);
    ɵɵpipe(21, "cxTranslate");
    ɵɵelementStart(22, "a", 13);
    ɵɵpipe(23, "cxUrl");
    ɵɵlistener("click", function StockNotificationDialogComponent_ng_container_10_Template_a_click_22_listener() {
      ɵɵrestoreView(_r2);
      const ctx_r3 = ɵɵnextContext();
      return ɵɵresetView(ctx_r3.close("Link clicked"));
    });
    ɵɵtext(24);
    ɵɵpipe(25, "cxTranslate");
    ɵɵelementEnd();
    ɵɵtext(26);
    ɵɵpipe(27, "cxTranslate");
    ɵɵelementEnd()()();
    ɵɵelementStart(28, "div", 14)(29, "div", 15)(30, "div", 16)(31, "button", 17);
    ɵɵlistener("click", function StockNotificationDialogComponent_ng_container_10_Template_button_click_31_listener() {
      ɵɵrestoreView(_r2);
      const ctx_r3 = ɵɵnextContext();
      return ɵɵresetView(ctx_r3.close("Button clicked"));
    });
    ɵɵtext(32);
    ɵɵpipe(33, "cxTranslate");
    ɵɵelementEnd()()()();
    ɵɵelementContainerEnd();
  }
  if (rf & 2) {
    const ctx_r3 = ɵɵnextContext();
    ɵɵadvance(4);
    ɵɵtextInterpolate1(" ", ɵɵpipeBind1(5, 12, "stockNotification.subscriptionDialog.notifiedPrefix"), " ");
    ɵɵadvance(2);
    ɵɵproperty("ngForOf", ctx_r3.enabledPrefs);
    ɵɵadvance(2);
    ɵɵtextInterpolate1(" ", ɵɵpipeBind1(9, 14, "stockNotification.subscriptionDialog.notifiedSuffix"), " ");
    ɵɵadvance(3);
    ɵɵtextInterpolate1(" ", ɵɵpipeBind1(12, 16, "stockNotification.subscriptionDialog.manageChannelsPrefix"), " ");
    ɵɵadvance(2);
    ɵɵproperty("routerLink", ɵɵpipeBind1(14, 18, ɵɵpureFunction0(34, _c722)));
    ɵɵadvance(2);
    ɵɵtextInterpolate1(" ", ɵɵpipeBind1(16, 20, "stockNotification.subscriptionDialog.manageChannelsLink"), "");
    ɵɵadvance(2);
    ɵɵtextInterpolate1(" ", ɵɵpipeBind1(18, 22, "stockNotification.subscriptionDialog.manageChannelsSuffix"), " ");
    ɵɵadvance(3);
    ɵɵtextInterpolate1(" ", ɵɵpipeBind1(21, 24, "stockNotification.subscriptionDialog.manageSubscriptionsPrefix"), " ");
    ɵɵadvance(2);
    ɵɵproperty("routerLink", ɵɵpipeBind1(23, 26, ɵɵpureFunction0(35, _c73)));
    ɵɵadvance(2);
    ɵɵtextInterpolate1(" ", ɵɵpipeBind1(25, 28, "stockNotification.subscriptionDialog.manageSubscriptionsLink"), "");
    ɵɵadvance(2);
    ɵɵtextInterpolate1(" ", ɵɵpipeBind1(27, 30, "stockNotification.subscriptionDialog.manageSubscriptionsSuffix"), " ");
    ɵɵadvance(6);
    ɵɵtextInterpolate1(" ", ɵɵpipeBind1(33, 32, "stockNotification.subscriptionDialog.okBtn"), " ");
  }
}
function StockNotificationDialogComponent_ng_template_12_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div", 8)(1, "p");
    ɵɵtext(2);
    ɵɵpipe(3, "cxTranslate");
    ɵɵelementEnd();
    ɵɵelementStart(4, "div", 20)(5, "div", 21);
    ɵɵelement(6, "cx-spinner");
    ɵɵelementEnd()()();
  }
  if (rf & 2) {
    ɵɵadvance(2);
    ɵɵtextInterpolate1(" ", ɵɵpipeBind1(3, 1, "stockNotification.subscriptionDialog.subscribing"), " ");
  }
}
var _c74 = () => ({
  cxRoute: "login"
});
function StockNotificationComponent_ng_container_0_ng_container_1_ng_container_1_Template(rf, ctx) {
  if (rf & 1) {
    const _r1 = ɵɵgetCurrentView();
    ɵɵelementContainerStart(0);
    ɵɵelementStart(1, "div", 6)(2, "p");
    ɵɵtext(3);
    ɵɵpipe(4, "cxTranslate");
    ɵɵelementEnd()();
    ɵɵelementStart(5, "button", 7);
    ɵɵlistener("click", function StockNotificationComponent_ng_container_0_ng_container_1_ng_container_1_Template_button_click_5_listener() {
      ɵɵrestoreView(_r1);
      const ctx_r1 = ɵɵnextContext(3);
      return ɵɵresetView(ctx_r1.subscribe());
    });
    ɵɵtext(6);
    ɵɵpipe(7, "cxTranslate");
    ɵɵelementEnd();
    ɵɵelementContainerEnd();
  }
  if (rf & 2) {
    ɵɵadvance(3);
    ɵɵtextInterpolate(ɵɵpipeBind1(4, 2, "stockNotification.getNotified"));
    ɵɵadvance(3);
    ɵɵtextInterpolate1(" ", ɵɵpipeBind1(7, 4, "stockNotification.notifyMe"), " ");
  }
}
function StockNotificationComponent_ng_container_0_ng_container_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainerStart(0);
    ɵɵtemplate(1, StockNotificationComponent_ng_container_0_ng_container_1_ng_container_1_Template, 8, 6, "ng-container", 5);
    ɵɵpipe(2, "async");
    ɵɵelementContainerEnd();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext(2);
    const disableNotify_r3 = ɵɵreference(3);
    ɵɵadvance();
    ɵɵproperty("ngIf", ɵɵpipeBind1(2, 2, ctx_r1.prefsEnabled$))("ngIfElse", disableNotify_r3);
  }
}
function StockNotificationComponent_ng_container_0_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainerStart(0);
    ɵɵtemplate(1, StockNotificationComponent_ng_container_0_ng_container_1_Template, 3, 4, "ng-container", 5);
    ɵɵpipe(2, "async");
    ɵɵelementContainerEnd();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext();
    const stopNotify_r4 = ɵɵreference(5);
    ɵɵadvance();
    ɵɵproperty("ngIf", !ɵɵpipeBind1(2, 2, ctx_r1.hasProductInterests$))("ngIfElse", stopNotify_r4);
  }
}
function StockNotificationComponent_ng_template_2_ng_container_2_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainerStart(0);
    ɵɵelementStart(1, "a", 10);
    ɵɵpipe(2, "cxUrl");
    ɵɵtext(3);
    ɵɵpipe(4, "cxTranslate");
    ɵɵelementEnd();
    ɵɵtext(5);
    ɵɵpipe(6, "cxTranslate");
    ɵɵelement(7, "br");
    ɵɵelementContainerEnd();
  }
  if (rf & 2) {
    ɵɵadvance();
    ɵɵproperty("routerLink", ɵɵpipeBind1(2, 3, ɵɵpureFunction0(9, _c74)));
    ɵɵadvance(2);
    ɵɵtextInterpolate1(" ", ɵɵpipeBind1(4, 5, "miniLogin.signInRegister"), "");
    ɵɵadvance(2);
    ɵɵtextInterpolate1(" ", ɵɵpipeBind1(6, 7, "stockNotification.getNotifySuffix"), "");
  }
}
function StockNotificationComponent_ng_template_2_ng_template_3_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵtext(0);
    ɵɵpipe(1, "cxTranslate");
    ɵɵelement(2, "br");
    ɵɵtext(3);
    ɵɵpipe(4, "cxTranslate");
    ɵɵelementStart(5, "a", 10);
    ɵɵpipe(6, "cxUrl");
    ɵɵtext(7);
    ɵɵpipe(8, "cxTranslate");
    ɵɵelementEnd();
    ɵɵtext(9);
    ɵɵpipe(10, "cxTranslate");
  }
  if (rf & 2) {
    ɵɵtextInterpolate1(" ", ɵɵpipeBind1(1, 5, "stockNotification.getNotify"), "");
    ɵɵadvance(3);
    ɵɵtextInterpolate1(" ", ɵɵpipeBind1(4, 7, "stockNotification.activateChannelsPrefix"), " ");
    ɵɵadvance(2);
    ɵɵproperty("routerLink", ɵɵpipeBind1(6, 9, ɵɵpureFunction0(15, _c722)));
    ɵɵadvance(2);
    ɵɵtextInterpolate(ɵɵpipeBind1(8, 11, "stockNotification.channelsLink"));
    ɵɵadvance(2);
    ɵɵtextInterpolate1("", ɵɵpipeBind1(10, 13, "stockNotification.activateChannelsSuffix"), " ");
  }
}
function StockNotificationComponent_ng_template_2_Template(rf, ctx) {
  if (rf & 1) {
    const _r5 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "div", 8)(1, "p");
    ɵɵtemplate(2, StockNotificationComponent_ng_template_2_ng_container_2_Template, 8, 10, "ng-container", 5)(3, StockNotificationComponent_ng_template_2_ng_template_3_Template, 11, 16, "ng-template", null, 3, ɵɵtemplateRefExtractor);
    ɵɵelementEnd()();
    ɵɵelementStart(5, "button", 9);
    ɵɵlistener("click", function StockNotificationComponent_ng_template_2_Template_button_click_5_listener() {
      ɵɵrestoreView(_r5);
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.subscribe());
    });
    ɵɵtext(6);
    ɵɵpipe(7, "cxTranslate");
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const loggedIn_r6 = ɵɵreference(4);
    const ctx_r1 = ɵɵnextContext();
    ɵɵadvance(2);
    ɵɵproperty("ngIf", ctx_r1.anonymous)("ngIfElse", loggedIn_r6);
    ɵɵadvance(3);
    ɵɵattribute("disabled", ctx_r1.anonymous ? true : null);
    ɵɵadvance();
    ɵɵtextInterpolate1(" ", ɵɵpipeBind1(7, 4, "stockNotification.notifyMe"), " ");
  }
}
function StockNotificationComponent_ng_template_4_ng_container_0_Template(rf, ctx) {
  if (rf & 1) {
    const _r7 = ɵɵgetCurrentView();
    ɵɵelementContainerStart(0);
    ɵɵelementStart(1, "div", 6)(2, "p");
    ɵɵtext(3);
    ɵɵpipe(4, "cxTranslate");
    ɵɵelementEnd()();
    ɵɵelementStart(5, "button", 11);
    ɵɵlistener("click", function StockNotificationComponent_ng_template_4_ng_container_0_Template_button_click_5_listener() {
      ɵɵrestoreView(_r7);
      const ctx_r1 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r1.unsubscribe());
    });
    ɵɵtext(6);
    ɵɵpipe(7, "cxTranslate");
    ɵɵelementEnd();
    ɵɵelementContainerEnd();
  }
  if (rf & 2) {
    ɵɵadvance(3);
    ɵɵtextInterpolate(ɵɵpipeBind1(4, 2, "stockNotification.notified"));
    ɵɵadvance(3);
    ɵɵtextInterpolate1(" ", ɵɵpipeBind1(7, 4, "stockNotification.stopNotify"), " ");
  }
}
function StockNotificationComponent_ng_template_4_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵtemplate(0, StockNotificationComponent_ng_template_4_ng_container_0_Template, 8, 6, "ng-container", 5);
    ɵɵpipe(1, "async");
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext();
    const loading_r8 = ɵɵreference(7);
    ɵɵproperty("ngIf", !ɵɵpipeBind1(1, 2, ctx_r1.isRemoveInterestLoading$))("ngIfElse", loading_r8);
  }
}
function StockNotificationComponent_ng_template_6_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div", 12)(1, "div", 13)(2, "div", 14);
    ɵɵelement(3, "cx-spinner");
    ɵɵelementEnd()()();
  }
}
var FOCUS_ATTR = "data-cx-focus";
var FOCUS_GROUP_ATTR = "data-cx-focus-group";
var MOVE_FOCUS;
(function(MOVE_FOCUS2) {
  MOVE_FOCUS2[MOVE_FOCUS2["NEXT"] = 1] = "NEXT";
  MOVE_FOCUS2[MOVE_FOCUS2["PREV"] = -1] = "PREV";
})(MOVE_FOCUS || (MOVE_FOCUS = {}));
var TrapFocus;
(function(TrapFocus2) {
  TrapFocus2["start"] = "start";
  TrapFocus2["end"] = "end";
  TrapFocus2["both"] = "both";
})(TrapFocus || (TrapFocus = {}));
var BaseFocusService = class _BaseFocusService {
  static {
    this.ɵfac = function BaseFocusService_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _BaseFocusService)();
    };
  }
  static {
    this.ɵprov = ɵɵdefineInjectable({
      token: _BaseFocusService,
      factory: _BaseFocusService.ɵfac,
      providedIn: "root"
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(BaseFocusService, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], null, null);
})();
var BaseFocusDirective = class _BaseFocusDirective {
  constructor(elementRef, service) {
    this.elementRef = elementRef;
    this.service = service;
    this.defaultConfig = {};
  }
  ngOnInit() {
    this.setDefaultConfiguration();
    this.requiredTabindex = -1;
  }
  // empty, but sub classes might have an implementation
  /* eslint @angular-eslint/no-empty-lifecycle-method: 1 */
  ngOnChanges(_changes) {
  }
  /**
   * Override the (input) config if it undefined or an empty string, with the
   * `defaultConfig`. The `defaultConfig` might be specified for each directive
   * differently. If a specific directive is used (i.e. `cxAutoFocus`), the
   * specific (inherited) defaultConfig will be used.
   */
  setDefaultConfiguration() {
    if ((!this.config || this.config === "") && this.defaultConfig) {
      this.config = this.defaultConfig;
    }
  }
  /**
   * Helper method to return the host element for the directive
   * given by the `elementRef`.
   */
  get host() {
    return this.elementRef.nativeElement;
  }
  /**
   * Force a tabindex on the host element if it is _required_ to make the element
   * focusable. If the element is focusable by nature or by a given tabindex, the
   * `tabindex` is not applied.
   *
   * Buttons, active links, etc. do no need an explicit tabindex to receive focus.
   */
  set requiredTabindex(tabindex) {
    if (this.requiresExplicitTabIndex) {
      this.tabindex = tabindex;
    }
  }
  /**
   * Returns true if the host element does not have a tabindex defined
   * and it also doesn't get focus by browsers nature (i.e. button or
   * active link).
   */
  get requiresExplicitTabIndex() {
    return this.tabindex === void 0 && ["button", "input", "select", "textarea"].indexOf(this.host.tagName.toLowerCase()) === -1 && !(this.host.tagName === "A" && (this.host.hasAttribute("href") || this.host.hasAttribute("routerlink") || this.host.getAttribute("ng-reflect-router-link")));
  }
  static {
    this.ɵfac = function BaseFocusDirective_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _BaseFocusDirective)(ɵɵdirectiveInject(ElementRef), ɵɵdirectiveInject(BaseFocusService));
    };
  }
  static {
    this.ɵdir = ɵɵdefineDirective({
      type: _BaseFocusDirective,
      hostVars: 1,
      hostBindings: function BaseFocusDirective_HostBindings(rf, ctx) {
        if (rf & 2) {
          ɵɵattribute("tabindex", ctx.tabindex);
        }
      },
      inputs: {
        tabindex: "tabindex"
      },
      features: [ɵɵNgOnChangesFeature]
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(BaseFocusDirective, [{
    type: Directive
  }], () => [{
    type: ElementRef
  }, {
    type: BaseFocusService
  }], {
    tabindex: [{
      type: Input
    }, {
      type: HostBinding,
      args: ["attr.tabindex"]
    }]
  });
})();
var VisibleFocusDirective = class _VisibleFocusDirective extends BaseFocusDirective {
  // TODO: (CXSPA-5912) Remove usless constructor next major release
  constructor(elementRef, service) {
    super(elementRef, service);
    this.elementRef = elementRef;
    this.service = service;
    this.defaultConfig = {
      disableMouseFocus: true
    };
    this.mouseFocus = false;
  }
  handleMousedown() {
    if (this.shouldFocusVisible) {
      this.mouseFocus = true;
    }
  }
  handleKeydown(event) {
    if (this.shouldFocusVisible) {
      this.mouseFocus = !this.isNavigating(event);
    }
  }
  /**
   * Indicates whether the configurations setup to disable visual focus.
   */
  get shouldFocusVisible() {
    return this.config?.disableMouseFocus;
  }
  /**
   * Indicates whether the event is used to navigate the storefront. Some keyboard events
   * are used by mouse users to fill a form or interact with the OS or browser.
   */
  isNavigating(event) {
    if (event.metaKey) {
      return false;
    }
    if (event.code === "Tab") {
      return true;
    }
    if (["INPUT", "TEXTAREA"].includes(event.target.tagName)) {
      return false;
    }
    return true;
  }
  static {
    this.ɵfac = function VisibleFocusDirective_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _VisibleFocusDirective)(ɵɵdirectiveInject(ElementRef), ɵɵdirectiveInject(BaseFocusService));
    };
  }
  static {
    this.ɵdir = ɵɵdefineDirective({
      type: _VisibleFocusDirective,
      hostVars: 2,
      hostBindings: function VisibleFocusDirective_HostBindings(rf, ctx) {
        if (rf & 1) {
          ɵɵlistener("mousedown", function VisibleFocusDirective_mousedown_HostBindingHandler() {
            return ctx.handleMousedown();
          })("keydown", function VisibleFocusDirective_keydown_HostBindingHandler($event) {
            return ctx.handleKeydown($event);
          });
        }
        if (rf & 2) {
          ɵɵclassProp("mouse-focus", ctx.mouseFocus);
        }
      },
      features: [ɵɵInheritDefinitionFeature]
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(VisibleFocusDirective, [{
    type: Directive
  }], () => [{
    type: ElementRef
  }, {
    type: BaseFocusService
  }], {
    mouseFocus: [{
      type: HostBinding,
      args: ["class.mouse-focus"]
    }],
    handleMousedown: [{
      type: HostListener,
      args: ["mousedown"]
    }],
    handleKeydown: [{
      type: HostListener,
      args: ["keydown", ["$event"]]
    }]
  });
})();
var BlockFocusDirective = class _BlockFocusDirective extends VisibleFocusDirective {
  constructor(elementRef, service) {
    super(elementRef, service);
    this.elementRef = elementRef;
    this.service = service;
    this.defaultConfig = {
      block: true
    };
    this.config = {};
  }
  ngOnInit() {
    super.ngOnInit();
    if (this.config.block) {
      this.tabindex = -1;
    }
  }
  static {
    this.ɵfac = function BlockFocusDirective_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _BlockFocusDirective)(ɵɵdirectiveInject(ElementRef), ɵɵdirectiveInject(BaseFocusService));
    };
  }
  static {
    this.ɵdir = ɵɵdefineDirective({
      type: _BlockFocusDirective,
      features: [ɵɵInheritDefinitionFeature]
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(BlockFocusDirective, [{
    type: Directive
  }], () => [{
    type: ElementRef
  }, {
    type: BaseFocusService
  }], null);
})();
var GLOBAL_GROUP = "_g_";
var PersistFocusService = class _PersistFocusService extends BaseFocusService {
  constructor() {
    super(...arguments);
    this.focus = /* @__PURE__ */ new Map();
  }
  get(group) {
    return this.focus.get(group || GLOBAL_GROUP);
  }
  /**
   * Persist the keyboard focus state for the given key. The focus is stored globally
   * or for the given group.
   */
  set(key, group) {
    if (key) {
      this.focus.set(group || GLOBAL_GROUP, key);
    }
  }
  /**
   * Clears the persisted keyboard focus state globally or for the given group.
   */
  clear(group) {
    this.focus.delete(group || GLOBAL_GROUP);
  }
  /**
   * Returns the group for the host element based on the configured group or
   * by the `data-cx-focus-group` attribute stored on the host.
   */
  getPersistenceGroup(host, config) {
    return config?.group ? config.group : host?.getAttribute(FOCUS_GROUP_ATTR);
  }
  static {
    this.ɵfac = /* @__PURE__ */ (() => {
      let ɵPersistFocusService_BaseFactory;
      return function PersistFocusService_Factory(__ngFactoryType__) {
        return (ɵPersistFocusService_BaseFactory || (ɵPersistFocusService_BaseFactory = ɵɵgetInheritedFactory(_PersistFocusService)))(__ngFactoryType__ || _PersistFocusService);
      };
    })();
  }
  static {
    this.ɵprov = ɵɵdefineInjectable({
      token: _PersistFocusService,
      factory: _PersistFocusService.ɵfac,
      providedIn: "root"
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PersistFocusService, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], null, null);
})();
var PersistFocusDirective = class _PersistFocusDirective extends BlockFocusDirective {
  handleFocus(event) {
    this.service.set(this.key, this.group);
    event?.preventDefault();
    event?.stopPropagation();
  }
  constructor(elementRef, service) {
    super(elementRef, service);
    this.elementRef = elementRef;
    this.service = service;
    this.defaultConfig = {};
    this.config = {};
  }
  ngOnInit() {
    super.ngOnInit();
    this.attr = this.key ? this.key : void 0;
  }
  setDefaultConfiguration() {
    if (typeof this.config === "string" && this.config !== "") {
      this.config = {
        key: this.config
      };
    }
    super.setDefaultConfiguration();
  }
  /**
   * Focus the element explicitly if it was focused before.
   */
  ngAfterViewInit() {
    if (this.isPersisted) {
      this.host.focus({
        preventScroll: true
      });
    }
  }
  get isPersisted() {
    return !!this.key && this.service.get(this.group) === this.key;
  }
  /**
   * Returns the key for the host element, which is used to persist the
   * focus state. This is useful in cases where the DOM is rebuild.
   */
  get key() {
    return this.config?.key;
  }
  /**
   * returns the persistence group (if any) for the focusable elements.
   */
  get group() {
    return this.service.getPersistenceGroup(this.host, this.config);
  }
  static {
    this.ɵfac = function PersistFocusDirective_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _PersistFocusDirective)(ɵɵdirectiveInject(ElementRef), ɵɵdirectiveInject(PersistFocusService));
    };
  }
  static {
    this.ɵdir = ɵɵdefineDirective({
      type: _PersistFocusDirective,
      hostVars: 1,
      hostBindings: function PersistFocusDirective_HostBindings(rf, ctx) {
        if (rf & 1) {
          ɵɵlistener("focus", function PersistFocusDirective_focus_HostBindingHandler($event) {
            return ctx.handleFocus($event);
          });
        }
        if (rf & 2) {
          ɵɵattribute("data-cx-focus", ctx.attr);
        }
      },
      features: [ɵɵInheritDefinitionFeature]
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PersistFocusDirective, [{
    type: Directive
  }], () => [{
    type: ElementRef
  }, {
    type: PersistFocusService
  }], {
    attr: [{
      type: HostBinding,
      args: [`attr.${FOCUS_ATTR}`]
    }],
    handleFocus: [{
      type: HostListener,
      args: ["focus", ["$event"]]
    }]
  });
})();
var SelectFocusUtility = class _SelectFocusUtility {
  constructor() {
    this.focusableSelectors = ["a[href]", "button", "[tabindex]", "input", "select", "textarea"];
    this.focusableSelectorSuffix = ":not([disabled]):not([hidden]):not([aria-hidden])";
  }
  query(host, selector) {
    if (!selector || selector === "") {
      return [];
    }
    return Array.from(host?.querySelectorAll(selector));
  }
  findFirstFocusable(host, config = {
    autofocus: true
  }) {
    const selector = typeof config?.autofocus === "string" ? config.autofocus : "[autofocus]";
    return this.query(host, selector).find((el) => !this.isHidden(el)) || this.findFocusable(host).find((el) => Boolean(el));
  }
  /**
   * returns all focusable child elements of the host element. The element selectors
   * are build from the `focusableSelectors`.
   *
   * @param host the `HTMLElement` used to query focusable elements
   * @param locked indicates whether inactive (`tabindex="-1"`) focusable elements should be returned
   * @param invisible indicates whether hidden focusable elements should be returned
   */
  findFocusable(host, locked = false, invisible = false) {
    let suffix = this.focusableSelectorSuffix;
    if (!locked) {
      suffix += `:not([tabindex='-1'])`;
    }
    const selector = this.focusableSelectors.map((s) => s += suffix).join(",");
    return this.query(host, selector).filter((el) => !invisible ? !this.isHidden(el) : Boolean(el));
  }
  /**
   * Indicates whether the element is hidden by CSS. There are various CSS rules and
   * HTML structures which can lead to an hidden or invisible element. An `offsetParent`
   * of null indicates that the element or any of it's decendants is hidden (`display:none`).
   *
   * Oother techniques use the visibility (`visibility: hidden`), opacity (`opacity`) or
   * phyisical location on the element itself or any of it's anchestor elements. Those
   * technique require to work with the _computed styles_, which will cause a performance
   * downgrade. We don't do this in the standard implementaton.
   */
  isHidden(el) {
    return el.offsetParent === null;
  }
  static {
    this.ɵfac = function SelectFocusUtility_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _SelectFocusUtility)();
    };
  }
  static {
    this.ɵprov = ɵɵdefineInjectable({
      token: _SelectFocusUtility,
      factory: _SelectFocusUtility.ɵfac,
      providedIn: "root"
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(SelectFocusUtility, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], null, null);
})();
var EscapeFocusService = class _EscapeFocusService extends PersistFocusService {
  constructor(selectFocusUtil) {
    super();
    this.selectFocusUtil = selectFocusUtil;
  }
  shouldFocus(config) {
    return !!config?.focusOnEscape;
  }
  handleEscape(host, config, event) {
    if (this.shouldFocus(config)) {
      if (host !== event.target) {
        host.focus({
          preventScroll: true
        });
        event.preventDefault();
        event.stopPropagation();
      } else {
        if (config?.focusOnDoubleEscape) {
          this.selectFocusUtil.findFirstFocusable(host, {
            autofocus: true
          })?.focus();
        }
      }
    }
  }
  static {
    this.ɵfac = function EscapeFocusService_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _EscapeFocusService)(ɵɵinject(SelectFocusUtility));
    };
  }
  static {
    this.ɵprov = ɵɵdefineInjectable({
      token: _EscapeFocusService,
      factory: _EscapeFocusService.ɵfac,
      providedIn: "root"
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(EscapeFocusService, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], () => [{
    type: SelectFocusUtility
  }], null);
})();
var EscapeFocusDirective = class _EscapeFocusDirective extends PersistFocusDirective {
  /**
   * Handles the escape key event.
   * @param event the native keyboard event which contains the escape keydown event
   */
  handleEscape(event) {
    if (this.service.shouldFocus(this.config)) {
      this.service.handleEscape(this.host, this.config, event);
    }
    this.esc.emit(this.service.shouldFocus(this.config));
  }
  constructor(elementRef, service) {
    super(elementRef, service);
    this.elementRef = elementRef;
    this.service = service;
    this.defaultConfig = {
      focusOnEscape: true
    };
    this.esc = new EventEmitter();
  }
  ngOnInit() {
    if (this.service.shouldFocus(this.config)) {
      this.requiredTabindex = -1;
    }
    super.ngOnInit();
  }
  static {
    this.ɵfac = function EscapeFocusDirective_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _EscapeFocusDirective)(ɵɵdirectiveInject(ElementRef), ɵɵdirectiveInject(EscapeFocusService));
    };
  }
  static {
    this.ɵdir = ɵɵdefineDirective({
      type: _EscapeFocusDirective,
      hostBindings: function EscapeFocusDirective_HostBindings(rf, ctx) {
        if (rf & 1) {
          ɵɵlistener("keydown.escape", function EscapeFocusDirective_keydown_escape_HostBindingHandler($event) {
            return ctx.handleEscape($event);
          });
        }
      },
      outputs: {
        esc: "esc"
      },
      features: [ɵɵInheritDefinitionFeature]
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(EscapeFocusDirective, [{
    type: Directive
  }], () => [{
    type: ElementRef
  }, {
    type: EscapeFocusService
  }], {
    esc: [{
      type: Output
    }],
    handleEscape: [{
      type: HostListener,
      args: ["keydown.escape", ["$event"]]
    }]
  });
})();
var AutoFocusService = class _AutoFocusService extends EscapeFocusService {
  /**
   * Returns the first focusable child element of the host element.
   */
  findFirstFocusable(host, config = {
    autofocus: true
  }) {
    if (config?.autofocus === ":host") {
      return host;
    } else if (this.hasPersistedFocus(host, config)) {
      return this.getPersisted(host, this.getPersistenceGroup(host, config));
    } else {
      return this.selectFocusUtil.findFirstFocusable(host, config) || host;
    }
  }
  /**
   * Indicates whether any of the focusable child elements is focused.
   */
  hasPersistedFocus(host, config) {
    return !!this.getPersisted(host, this.getPersistenceGroup(host, config));
  }
  /**
   * Returns the element that has a persisted focus state.
   *
   * @param host the `HTMLElement` used to query for focusable children
   * @param group the optional group for the persistent state, to separate different focus
   *   groups and remain the persistence
   */
  getPersisted(host, group) {
    if (!this.get(group)) {
      return;
    }
    const focussed = Array.from(host?.querySelectorAll(`[${FOCUS_ATTR}='${this.get(group)}']`));
    return focussed.length > 0 ? focussed[0] : void 0;
  }
  static {
    this.ɵfac = /* @__PURE__ */ (() => {
      let ɵAutoFocusService_BaseFactory;
      return function AutoFocusService_Factory(__ngFactoryType__) {
        return (ɵAutoFocusService_BaseFactory || (ɵAutoFocusService_BaseFactory = ɵɵgetInheritedFactory(_AutoFocusService)))(__ngFactoryType__ || _AutoFocusService);
      };
    })();
  }
  static {
    this.ɵprov = ɵɵdefineInjectable({
      token: _AutoFocusService,
      factory: _AutoFocusService.ɵfac,
      providedIn: "root"
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(AutoFocusService, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], null, null);
})();
var AutoFocusDirective = class _AutoFocusDirective extends EscapeFocusDirective {
  constructor(elementRef, service) {
    super(elementRef, service);
    this.elementRef = elementRef;
    this.service = service;
    this.defaultConfig = {
      autofocus: true
    };
  }
  /**
   * Focus the element explicitly if it was focussed before.
   */
  ngAfterViewInit() {
    if (this.shouldAutofocus) {
      this.handleFocus();
    }
    if (!this.shouldAutofocus || this.hasPersistedFocus) {
      super.ngAfterViewInit();
    }
  }
  ngOnChanges(changes) {
    if (!!changes.config?.currentValue?.refreshFocus) {
      if (!this.config.autofocus) {
        this.config.autofocus = true;
      }
      this.handleFocus();
    }
    super.ngOnChanges(changes);
  }
  /**
   * Mimic the focus without setting the actual focus on the host. The first
   * focusable child element will be focussed.
   */
  handleFocus(event) {
    if (this.shouldAutofocus) {
      if (!event?.target || event.target === this.host) {
        this.firstFocusable?.focus();
      } else {
        event.target.focus();
      }
    }
    super.handleFocus(event);
  }
  /**
   * Helper function to get the first focusable child element
   */
  get hasPersistedFocus() {
    return this.service.hasPersistedFocus(this.host, this.config);
  }
  /**
   * Helper function to indicate whether we should use autofocus for the
   * child elements.
   */
  get shouldAutofocus() {
    return !!this.config?.autofocus;
  }
  /**
   * Helper function to get the first focusable child element.
   *
   * We keep this private to not pollute the API.
   */
  get firstFocusable() {
    return this.service.findFirstFocusable(this.host, this.config);
  }
  static {
    this.ɵfac = function AutoFocusDirective_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _AutoFocusDirective)(ɵɵdirectiveInject(ElementRef), ɵɵdirectiveInject(AutoFocusService));
    };
  }
  static {
    this.ɵdir = ɵɵdefineDirective({
      type: _AutoFocusDirective,
      features: [ɵɵInheritDefinitionFeature, ɵɵNgOnChangesFeature]
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(AutoFocusDirective, [{
    type: Directive
  }], () => [{
    type: ElementRef
  }, {
    type: AutoFocusService
  }], null);
})();
var TabFocusService = class _TabFocusService extends AutoFocusService {
  /**
   * Moves to the next (or previous) tab.
   */
  moveTab(host, config, increment, event) {
    if (config?.tab) {
      const next = config.tab === "scroll" ? this.findNextScrollable(host, config, increment) : this.findNext(host, config, increment);
      next?.focus();
      event.preventDefault();
      event.stopPropagation();
    }
  }
  /**
   * builds out virtual slides out of the full scrollable area, to allow
   * for maximum flexibility for the underlying layout without using hardcoded
   * slide sizes.
   */
  findNextScrollable(host, config, increment) {
    const active = this.getActiveChild(host, config);
    if (!active) {
      return;
    }
    const virtualSlideCount = Math.round(host.scrollWidth / host.clientWidth);
    const currentVirtualSlide = Math.round(active.offsetLeft / (host.scrollWidth / virtualSlideCount));
    let nextVirtualSlide = currentVirtualSlide + increment;
    if (increment === MOVE_FOCUS.NEXT && nextVirtualSlide >= virtualSlideCount) {
      nextVirtualSlide = 0;
    }
    if (increment === MOVE_FOCUS.PREV && nextVirtualSlide < 0) {
      nextVirtualSlide = virtualSlideCount - 1;
    }
    const firstItemOnNextSlide = this.getChildren(host, config)?.find((tab) => tab.offsetLeft >= host.scrollWidth / virtualSlideCount * nextVirtualSlide);
    return firstItemOnNextSlide;
  }
  findNext(host, config, increment) {
    const childs = this.getChildren(host, config);
    let activeIndex = childs?.findIndex((c) => c === this.getActiveChild(host, config));
    if (!activeIndex || activeIndex === -1) {
      activeIndex = 0;
    }
    activeIndex += increment;
    if (increment === MOVE_FOCUS.NEXT && activeIndex >= childs?.length) {
      activeIndex = childs.length - 1;
    }
    if (increment === MOVE_FOCUS.PREV && activeIndex < 0) {
      activeIndex = 0;
    }
    return childs ? childs[activeIndex] : void 0;
  }
  /**
   * Returns the active focusable child element. If there's no active
   * focusable child element, the first focusable child is returned.
   */
  getActiveChild(host, config) {
    const persisted = this.getPersisted(host, config?.group);
    if (persisted) {
      return persisted;
    }
    const children = this.getChildren(host, config);
    let index = children.findIndex((tab) => this.isActive(tab));
    if (!index || index === -1) {
      index = 0;
    }
    return children[index];
  }
  getChildren(host, config) {
    if (typeof config.tab === "string" && config.tab !== "scroll") {
      return this.selectFocusUtil.query(host, config.tab);
    } else {
      return this.findFocusable(host, true);
    }
  }
  /**
   * Returns all focusable child elements of the host element.
   *
   * @param host The host element is used to query child focusable elements.
   * @param locked Indicates if locked elements (tabindex=-1) should be returned, defaults to false.
   * @param invisible Indicates if invisible child elements should be returned, defaults to false.
   */
  findFocusable(host, locked = false, invisible = false) {
    return this.selectFocusUtil.findFocusable(host, locked, invisible);
  }
  isActive(el) {
    const child = document.activeElement;
    const selector = child?.tagName;
    return el === child || !!selector && !!Array.from(el.querySelectorAll(selector)).find((e) => e === child);
  }
  static {
    this.ɵfac = /* @__PURE__ */ (() => {
      let ɵTabFocusService_BaseFactory;
      return function TabFocusService_Factory(__ngFactoryType__) {
        return (ɵTabFocusService_BaseFactory || (ɵTabFocusService_BaseFactory = ɵɵgetInheritedFactory(_TabFocusService)))(__ngFactoryType__ || _TabFocusService);
      };
    })();
  }
  static {
    this.ɵprov = ɵɵdefineInjectable({
      token: _TabFocusService,
      factory: _TabFocusService.ɵfac,
      providedIn: "root"
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(TabFocusService, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], null, null);
})();
var TabFocusDirective = class _TabFocusDirective extends AutoFocusDirective {
  handleNextTab(event) {
    if (this.config?.tab) {
      this.service.moveTab(this.host, this.config, MOVE_FOCUS.NEXT, event);
    }
  }
  handlePreviousTab(event) {
    if (this.config?.tab) {
      this.service.moveTab(this.host, this.config, MOVE_FOCUS.PREV, event);
    }
  }
  constructor(elementRef, service) {
    super(elementRef, service);
    this.elementRef = elementRef;
    this.service = service;
    this.defaultConfig = {
      tab: true
    };
    this.config = {};
  }
  static {
    this.ɵfac = function TabFocusDirective_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _TabFocusDirective)(ɵɵdirectiveInject(ElementRef), ɵɵdirectiveInject(TabFocusService));
    };
  }
  static {
    this.ɵdir = ɵɵdefineDirective({
      type: _TabFocusDirective,
      hostBindings: function TabFocusDirective_HostBindings(rf, ctx) {
        if (rf & 1) {
          ɵɵlistener("keydown.arrowRight", function TabFocusDirective_keydown_arrowRight_HostBindingHandler($event) {
            return ctx.handleNextTab($event);
          })("keydown.arrowLeft", function TabFocusDirective_keydown_arrowLeft_HostBindingHandler($event) {
            return ctx.handlePreviousTab($event);
          });
        }
      },
      features: [ɵɵInheritDefinitionFeature]
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(TabFocusDirective, [{
    type: Directive
  }], () => [{
    type: ElementRef
  }, {
    type: TabFocusService
  }], {
    handleNextTab: [{
      type: HostListener,
      args: ["keydown.arrowRight", ["$event"]]
    }],
    handlePreviousTab: [{
      type: HostListener,
      args: ["keydown.arrowLeft", ["$event"]]
    }]
  });
})();
var TrapFocusService = class _TrapFocusService extends TabFocusService {
  /**
   * Indicates whether any of the child elements of the host are focusable.
   *
   * @param host `HTMLElement` that is used to query the focusable elements.
   */
  hasFocusableChildren(host) {
    return this.findFocusable(host).length > 0;
  }
  /**
   * Focus the next or previous element of all available focusable elements.
   * The focus is _trapped_ in case there's no next or previous available element.
   * The focus will automatically move the start or end of the list.
   */
  moveFocus(host, config, increment, event) {
    const focusable = this.findFocusable(host);
    let index = focusable.findIndex((v) => v === event.target) + increment;
    const trap = config.trap || config.trapTabOnly;
    const shouldMoveFocus = index >= 0 && index < focusable.length || index < 0 && this.getTrapStart(trap) || index >= focusable.length && this.getTrapEnd(trap);
    if (shouldMoveFocus) {
      if (index >= focusable.length) {
        index = 0;
      }
      if (index < 0) {
        index = focusable.length - 1;
      }
      event.preventDefault();
      event.stopPropagation();
      const el = focusable[index];
      el.focus();
    }
  }
  getTrapStart(trap) {
    return trap === true || trap === TrapFocus.start;
  }
  getTrapEnd(trap) {
    return trap === true || trap === TrapFocus.end;
  }
  static {
    this.ɵfac = /* @__PURE__ */ (() => {
      let ɵTrapFocusService_BaseFactory;
      return function TrapFocusService_Factory(__ngFactoryType__) {
        return (ɵTrapFocusService_BaseFactory || (ɵTrapFocusService_BaseFactory = ɵɵgetInheritedFactory(_TrapFocusService)))(__ngFactoryType__ || _TrapFocusService);
      };
    })();
  }
  static {
    this.ɵprov = ɵɵdefineInjectable({
      token: _TrapFocusService,
      factory: _TrapFocusService.ɵfac,
      providedIn: "root"
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(TrapFocusService, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], null, null);
})();
var TrapFocusDirective = class _TrapFocusDirective extends TabFocusDirective {
  constructor(elementRef, service) {
    super(elementRef, service);
    this.elementRef = elementRef;
    this.service = service;
    this.defaultConfig = {
      trap: true
    };
    this.config = {};
    this.handleTrapDown = (event) => {
      const isTabPressed = event.key === "Tab";
      if (!!this.config.trap || this.config.trapTabOnly && isTabPressed) {
        this.moveFocus(event, MOVE_FOCUS.NEXT);
      }
    };
    this.handleTrapUp = (event) => {
      const isTabPressed = event.key === "Tab";
      if (!!this.config.trap || this.config.trapTabOnly && isTabPressed) {
        this.moveFocus(event, MOVE_FOCUS.PREV);
      }
    };
  }
  /**
   * Moves the focus of the element reference up or down, depending on the increment.
   * The focus of the element is trapped to avoid it from going out of the group.
   *
   * @param event UIEvent that is used to get the target element. The event is blocked
   *   from standard execution and further bubbling.
   * @param increment indicates whether the next or previous is focussed.
   */
  moveFocus(event, increment) {
    if (this.service.hasFocusableChildren(this.host)) {
      this.service.moveFocus(this.host, this.config, increment, event);
    }
  }
  static {
    this.ɵfac = function TrapFocusDirective_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _TrapFocusDirective)(ɵɵdirectiveInject(ElementRef), ɵɵdirectiveInject(TrapFocusService));
    };
  }
  static {
    this.ɵdir = ɵɵdefineDirective({
      type: _TrapFocusDirective,
      hostBindings: function TrapFocusDirective_HostBindings(rf, ctx) {
        if (rf & 1) {
          ɵɵlistener("keydown.arrowdown", function TrapFocusDirective_keydown_arrowdown_HostBindingHandler($event) {
            return ctx.handleTrapDown($event);
          })("keydown.tab", function TrapFocusDirective_keydown_tab_HostBindingHandler($event) {
            return ctx.handleTrapDown($event);
          })("keydown.arrowup", function TrapFocusDirective_keydown_arrowup_HostBindingHandler($event) {
            return ctx.handleTrapUp($event);
          })("keydown.shift.tab", function TrapFocusDirective_keydown_shift_tab_HostBindingHandler($event) {
            return ctx.handleTrapUp($event);
          });
        }
      },
      features: [ɵɵInheritDefinitionFeature]
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(TrapFocusDirective, [{
    type: Directive
  }], () => [{
    type: ElementRef
  }, {
    type: TrapFocusService
  }], {
    handleTrapDown: [{
      type: HostListener,
      args: ["keydown.arrowdown", ["$event"]]
    }, {
      type: HostListener,
      args: ["keydown.tab", ["$event"]]
    }],
    handleTrapUp: [{
      type: HostListener,
      args: ["keydown.arrowup", ["$event"]]
    }, {
      type: HostListener,
      args: ["keydown.shift.tab", ["$event"]]
    }]
  });
})();
var LockFocusService = class _LockFocusService extends TrapFocusService {
  static {
    this.ɵfac = /* @__PURE__ */ (() => {
      let ɵLockFocusService_BaseFactory;
      return function LockFocusService_Factory(__ngFactoryType__) {
        return (ɵLockFocusService_BaseFactory || (ɵLockFocusService_BaseFactory = ɵɵgetInheritedFactory(_LockFocusService)))(__ngFactoryType__ || _LockFocusService);
      };
    })();
  }
  static {
    this.ɵprov = ɵɵdefineInjectable({
      token: _LockFocusService,
      factory: _LockFocusService.ɵfac,
      providedIn: "root"
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(LockFocusService, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], null, null);
})();
var UNLOCK_HIDDEN_ELEMENTS = true;
var LockFocusDirective = class _LockFocusDirective extends TrapFocusDirective {
  /**
   * When the user selects enter or space, the focusable childs are
   * unlocked, which means that the tabindex is set to 0.
   */
  handleEnter(event) {
    if (this.shouldLock && this.host === event.target) {
      this.unlockFocus(event);
      event.preventDefault();
      event.stopPropagation();
    }
  }
  /**
   * In case any of the children elements is touched by the mouse,
   * we unlock the group to not break the mouse-experience.
   */
  handleClick(event) {
    if (this.shouldLock && this.isLocked) {
      this.unlockFocus(event);
      event.stopPropagation();
    }
  }
  constructor(elementRef, service, renderer) {
    super(elementRef, service);
    this.elementRef = elementRef;
    this.service = service;
    this.renderer = renderer;
    this.defaultConfig = {
      lock: true
    };
    this.config = {};
    this.unlock = new EventEmitter();
  }
  lockFocus() {
    this.addTabindexToChildren(-1);
  }
  unlockFocus(event) {
    this.unlock.emit(true);
    this.addTabindexToChildren(0);
    if (event?.target === this.host) {
      setTimeout(() => {
        super.handleFocus(event);
      }, 100);
    }
  }
  ngOnInit() {
    super.ngOnInit();
    this.shouldLock = this.config?.lock;
    if (this.shouldLock) {
      this.tabindex = 0;
      if (!this.config.hasOwnProperty("autofocus")) {
        this.config.autofocus = true;
      }
      if (!this.config.hasOwnProperty("focusOnEscape")) {
        this.config.focusOnEscape = !(this.config?.focusOnEscape === false);
      }
    }
  }
  ngAfterViewInit() {
    if (this.shouldLock) {
      if (!!this.group) {
        const group = this.group;
        this.service.findFocusable(this.host).forEach((el) => (
          // we must do this in after view init as
          this.renderer.setAttribute(el, FOCUS_GROUP_ATTR, group)
        ));
      }
      if (this.shouldAutofocus) {
        this.handleFocus();
      }
    }
    super.ngAfterViewInit();
  }
  handleFocus(event) {
    if (this.shouldLock) {
      if (this.shouldUnlockAfterAutofocus(event)) {
        setTimeout(() => this.unlockFocus(event));
      } else {
        setTimeout(() => this.lockFocus());
        event?.stopPropagation();
        return;
      }
    }
    super.handleFocus(event);
  }
  handleEscape(event) {
    if (this.shouldLock) {
      this.service.clear(this.config.group);
    }
    super.handleEscape(event);
  }
  /**
   * When the handleFocus is called without an actual event, it's coming from Autofocus.
   * In this case we unlock the focusable children in case there's a focusable child that
   * was unlocked before.
   *
   * We keep this private to not polute the API.
   */
  shouldUnlockAfterAutofocus(event) {
    return !event && this.service.hasPersistedFocus(this.host, this.config);
  }
  /**
   * Add the tabindex attribute to the focusable children elements
   */
  addTabindexToChildren(i = 0) {
    if (this.shouldLock) {
      this.isLocked = i === -1;
      if (!(this.hasFocusableChildren && i === 0) || i === 0) {
        this.focusable.forEach((el) => this.renderer.setAttribute(el, "tabindex", i.toString()));
      }
    }
  }
  /**
   * Utility method, returns all focusable children for the host element.
   *
   * We keep this private to not polute the API.
   */
  get hasFocusableChildren() {
    return this.service.hasFocusableChildren(this.host);
  }
  /**
   * Returns the focusable children of the host element. If the host element
   * is configured to be locked, the query is restricted to child elements
   * with a tabindex !== `-1`.
   *
   * We keep this private to not polute the API.
   */
  get focusable() {
    return this.service.findFocusable(this.host, this.shouldLock, UNLOCK_HIDDEN_ELEMENTS);
  }
  static {
    this.ɵfac = function LockFocusDirective_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _LockFocusDirective)(ɵɵdirectiveInject(ElementRef), ɵɵdirectiveInject(LockFocusService), ɵɵdirectiveInject(Renderer2));
    };
  }
  static {
    this.ɵdir = ɵɵdefineDirective({
      type: _LockFocusDirective,
      hostVars: 4,
      hostBindings: function LockFocusDirective_HostBindings(rf, ctx) {
        if (rf & 1) {
          ɵɵlistener("keydown.enter", function LockFocusDirective_keydown_enter_HostBindingHandler($event) {
            return ctx.handleEnter($event);
          })("keydown.space", function LockFocusDirective_keydown_space_HostBindingHandler($event) {
            return ctx.handleEnter($event);
          })("click", function LockFocusDirective_click_HostBindingHandler($event) {
            return ctx.handleClick($event);
          });
        }
        if (rf & 2) {
          ɵɵclassProp("focus-lock", ctx.shouldLock)("is-locked", ctx.isLocked);
        }
      },
      outputs: {
        unlock: "unlock"
      },
      features: [ɵɵInheritDefinitionFeature]
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(LockFocusDirective, [{
    type: Directive
  }], () => [{
    type: ElementRef
  }, {
    type: LockFocusService
  }, {
    type: Renderer2
  }], {
    shouldLock: [{
      type: HostBinding,
      args: ["class.focus-lock"]
    }],
    isLocked: [{
      type: HostBinding,
      args: ["class.is-locked"]
    }],
    unlock: [{
      type: Output
    }],
    handleEnter: [{
      type: HostListener,
      args: ["keydown.enter", ["$event"]]
    }, {
      type: HostListener,
      args: ["keydown.space", ["$event"]]
    }],
    handleClick: [{
      type: HostListener,
      args: ["click", ["$event"]]
    }]
  });
})();
var KeyboardFocusService = class _KeyboardFocusService extends LockFocusService {
  static {
    this.ɵfac = /* @__PURE__ */ (() => {
      let ɵKeyboardFocusService_BaseFactory;
      return function KeyboardFocusService_Factory(__ngFactoryType__) {
        return (ɵKeyboardFocusService_BaseFactory || (ɵKeyboardFocusService_BaseFactory = ɵɵgetInheritedFactory(_KeyboardFocusService)))(__ngFactoryType__ || _KeyboardFocusService);
      };
    })();
  }
  static {
    this.ɵprov = ɵɵdefineInjectable({
      token: _KeyboardFocusService,
      factory: _KeyboardFocusService.ɵfac,
      providedIn: "root"
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(KeyboardFocusService, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], null, null);
})();
var FocusDirective = class _FocusDirective extends LockFocusDirective {
  constructor(elementRef, service, renderer) {
    super(elementRef, service, renderer);
    this.elementRef = elementRef;
    this.service = service;
    this.renderer = renderer;
    this.defaultConfig = {};
    this.config = {};
  }
  static {
    this.ɵfac = function FocusDirective_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _FocusDirective)(ɵɵdirectiveInject(ElementRef), ɵɵdirectiveInject(KeyboardFocusService), ɵɵdirectiveInject(Renderer2));
    };
  }
  static {
    this.ɵdir = ɵɵdefineDirective({
      type: _FocusDirective,
      selectors: [["", "cxFocus", ""]],
      inputs: {
        config: [0, "cxFocus", "config"]
      },
      standalone: false,
      features: [ɵɵInheritDefinitionFeature]
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(FocusDirective, [{
    type: Directive,
    args: [{
      selector: "[cxFocus]",
      standalone: false
    }]
  }], () => [{
    type: ElementRef
  }, {
    type: KeyboardFocusService
  }, {
    type: Renderer2
  }], {
    config: [{
      type: Input,
      args: ["cxFocus"]
    }]
  });
})();
var SkipFocusDirective = class _SkipFocusDirective {
  constructor() {
    this.config = {
      isEnabled: false
    };
    this.elementRef = inject(ElementRef);
    this.winRef = inject(WindowRef);
    this.renderer = inject(Renderer2);
  }
  ngOnChanges() {
    this.excludeFromFocus(this.config.isEnabled, this.config.activeElementSelectors);
  }
  excludeFromFocus(isEnabled, skipSelectors = []) {
    if (!this.winRef.isBrowser()) {
      return;
    }
    const tabindex = isEnabled ? "-1" : "0";
    const focusableElementsSelector = 'a[href], button:not([disabled]), textarea:not([disabled]), input:not([disabled]), select:not([disabled]), [tabindex]:not([tabindex="-1"]';
    const focusableElements = this.elementRef.nativeElement.querySelectorAll(focusableElementsSelector);
    Array.from(focusableElements || []).forEach((el) => {
      const element = el;
      const shouldSkip = skipSelectors.some((selector) => {
        return element.matches(selector);
      });
      if (!shouldSkip && this.isElementVisible(element)) {
        this.renderer.setAttribute(element, "tabindex", tabindex);
      }
    });
  }
  isElementVisible(element) {
    const style = this.winRef.nativeWindow?.getComputedStyle(element);
    return style?.visibility !== "hidden" && style?.display !== "none" && element.offsetWidth > 0 && element.offsetHeight > 0;
  }
  static {
    this.ɵfac = function SkipFocusDirective_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _SkipFocusDirective)();
    };
  }
  static {
    this.ɵdir = ɵɵdefineDirective({
      type: _SkipFocusDirective,
      selectors: [["", "cxSkipFocus", ""]],
      inputs: {
        config: [0, "cxSkipFocus", "config"]
      },
      standalone: false,
      features: [ɵɵNgOnChangesFeature]
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(SkipFocusDirective, [{
    type: Directive,
    args: [{
      selector: "[cxSkipFocus]",
      standalone: false
    }]
  }], null, {
    config: [{
      type: Input,
      args: ["cxSkipFocus"]
    }]
  });
})();
var directives$1 = [
  // PersistFocusDirective,
  // VisibleFocusDirective,
  // BlockFocusDirective,
  // AutoFocusDirective,
  // EscapeFocusDirective,
  // LockFocusDirective,
  // TrapFocusDirective,
  // TabFocusDirective,
  FocusDirective,
  SkipFocusDirective
];
var KeyboardFocusModule = class _KeyboardFocusModule {
  static {
    this.ɵfac = function KeyboardFocusModule_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _KeyboardFocusModule)();
    };
  }
  static {
    this.ɵmod = ɵɵdefineNgModule({
      type: _KeyboardFocusModule,
      declarations: [
        // PersistFocusDirective,
        // VisibleFocusDirective,
        // BlockFocusDirective,
        // AutoFocusDirective,
        // EscapeFocusDirective,
        // LockFocusDirective,
        // TrapFocusDirective,
        // TabFocusDirective,
        FocusDirective,
        SkipFocusDirective
      ],
      imports: [CommonModule],
      exports: [
        // PersistFocusDirective,
        // VisibleFocusDirective,
        // BlockFocusDirective,
        // AutoFocusDirective,
        // EscapeFocusDirective,
        // LockFocusDirective,
        // TrapFocusDirective,
        // TabFocusDirective,
        FocusDirective,
        SkipFocusDirective
      ]
    });
  }
  static {
    this.ɵinj = ɵɵdefineInjector({
      imports: [CommonModule]
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(KeyboardFocusModule, [{
    type: NgModule,
    args: [{
      imports: [CommonModule],
      declarations: [...directives$1],
      exports: [...directives$1]
    }]
  }], null, null);
})();
var MockKeyboardFocusDirective = class _MockKeyboardFocusDirective {
  constructor() {
    this.config = {};
  }
  static {
    this.ɵfac = function MockKeyboardFocusDirective_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _MockKeyboardFocusDirective)();
    };
  }
  static {
    this.ɵdir = ɵɵdefineDirective({
      type: _MockKeyboardFocusDirective,
      selectors: [["", "cxFocus", ""]],
      inputs: {
        config: [0, "cxFocus", "config"]
      },
      standalone: false
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(MockKeyboardFocusDirective, [{
    type: Directive,
    args: [{
      selector: "[cxFocus]",
      standalone: false
    }]
  }], null, {
    config: [{
      type: Input,
      args: ["cxFocus"]
    }]
  });
})();
var KeyboardFocusTestingModule = class _KeyboardFocusTestingModule {
  static {
    this.ɵfac = function KeyboardFocusTestingModule_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _KeyboardFocusTestingModule)();
    };
  }
  static {
    this.ɵmod = ɵɵdefineNgModule({
      type: _KeyboardFocusTestingModule,
      declarations: [MockKeyboardFocusDirective],
      exports: [MockKeyboardFocusDirective]
    });
  }
  static {
    this.ɵinj = ɵɵdefineInjector({});
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(KeyboardFocusTestingModule, [{
    type: NgModule,
    args: [{
      declarations: [MockKeyboardFocusDirective],
      exports: [MockKeyboardFocusDirective]
    }]
  }], null, null);
})();
var disableTabbingForTick = (elements) => {
  for (const element of elements) {
    element.tabIndex = -1;
  }
  requestAnimationFrame(() => {
    for (const element of elements) {
      element.tabIndex = 0;
    }
  });
};
var DIALOG_TYPE;
(function(DIALOG_TYPE2) {
  DIALOG_TYPE2["POPOVER"] = "POPOVER";
  DIALOG_TYPE2["POPOVER_CENTER"] = "POPOVER_CENTER";
  DIALOG_TYPE2["POPOVER_CENTER_BACKDROP"] = "POPOVER_CENTER_BACKDROP";
  DIALOG_TYPE2["DIALOG"] = "DIALOG";
  DIALOG_TYPE2["SIDEBAR_START"] = "SIDEBAR_START";
  DIALOG_TYPE2["SIDEBAR_END"] = "SIDEBAR_END";
})(DIALOG_TYPE || (DIALOG_TYPE = {}));
var LAUNCH_CALLER;
(function(LAUNCH_CALLER2) {
  LAUNCH_CALLER2["ASM"] = "ASM";
  LAUNCH_CALLER2["SKIP_LINKS"] = "SKIP_LINKS";
  LAUNCH_CALLER2["ANONYMOUS_CONSENT"] = "ANONYMOUS_CONSENT";
  LAUNCH_CALLER2["REPLENISHMENT_ORDER"] = "REPLENISHMENT_ORDER";
  LAUNCH_CALLER2["PLACE_ORDER_SPINNER"] = "PLACE_ORDER_SPINNER";
  LAUNCH_CALLER2["SUGGESTED_ADDRESSES"] = "SUGGESTED_ADDRESSES";
  LAUNCH_CALLER2["COUPON"] = "COUPON";
  LAUNCH_CALLER2["CLAIM_DIALOG"] = "CLAIM_DIALOG";
  LAUNCH_CALLER2["STOCK_NOTIFICATION"] = "STOCK_NOTIFICATION";
})(LAUNCH_CALLER || (LAUNCH_CALLER = {}));
var LaunchRenderStrategy = class LaunchRenderStrategy2 {
  constructor(document2, rendererFactory) {
    this.document = document2;
    this.rendererFactory = rendererFactory;
    this.renderedCallers = [];
    this.dialogClasses = ["d-block", "fade", "modal", "show"];
    this.popoverClasses = ["cx-dialog-popover"];
    this.popoverCenterClasses = ["cx-dialog-popover-center"];
    this.popoverCenterBackdropClasses = ["cx-dialog-popover-center-backdrop"];
    this.sidebarEndClasses = ["cx-sidebar-end"];
    this.sidebarStartClasses = ["cx-sidebar-start"];
    this.renderer = rendererFactory.createRenderer(null, null);
  }
  /**
   * Determines if element should render
   *
   * @param caller
   * @param config
   */
  shouldRender(caller, config) {
    return Boolean(config.component) && (this.renderedCallers.some((el) => el.caller === caller) ? !!config.multi : true);
  }
  applyClasses(component, dialogType) {
    let classes = [];
    switch (dialogType) {
      case DIALOG_TYPE.DIALOG:
        classes = this.dialogClasses;
        this.renderer.addClass(this.document.body, "modal-open");
        break;
      case DIALOG_TYPE.POPOVER:
        classes = this.popoverClasses;
        break;
      case DIALOG_TYPE.POPOVER_CENTER:
        classes = this.popoverCenterClasses;
        break;
      case DIALOG_TYPE.POPOVER_CENTER_BACKDROP:
        classes = this.popoverCenterBackdropClasses;
        break;
      case DIALOG_TYPE.SIDEBAR_END:
        classes = this.sidebarEndClasses;
        break;
      case DIALOG_TYPE.SIDEBAR_START:
        classes = this.sidebarStartClasses;
        break;
    }
    for (const newClass of classes) {
      this.renderer.addClass(component.location.nativeElement, newClass);
    }
  }
  /**
   * Method to call when rendered element is destroyed
   * The element will be removed from the list of rendered elements
   *
   * @param caller
   * @param _config optional parameters used in children strategies
   */
  remove(caller, config) {
    this.renderedCallers = this.renderedCallers.filter((el) => el.caller !== caller);
    if (config?.dialogType === DIALOG_TYPE.DIALOG) {
      this.renderer.removeClass(this.document.body, "modal-open");
    }
  }
  getPriority() {
    return Priority.LOW;
  }
};
LaunchRenderStrategy = __decorate([__param(0, Inject(DOCUMENT))], LaunchRenderStrategy);
var BREAKPOINT;
(function(BREAKPOINT2) {
  BREAKPOINT2["xs"] = "xs";
  BREAKPOINT2["sm"] = "sm";
  BREAKPOINT2["md"] = "md";
  BREAKPOINT2["lg"] = "lg";
  BREAKPOINT2["xl"] = "xl";
})(BREAKPOINT || (BREAKPOINT = {}));
var LayoutConfig = class _LayoutConfig {
  static {
    this.ɵfac = function LayoutConfig_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _LayoutConfig)();
    };
  }
  static {
    this.ɵprov = ɵɵdefineInjectable({
      token: _LayoutConfig,
      factory: function LayoutConfig_Factory(__ngFactoryType__) {
        let __ngConditionalFactory__ = null;
        if (__ngFactoryType__) {
          __ngConditionalFactory__ = new (__ngFactoryType__ || _LayoutConfig)();
        } else {
          __ngConditionalFactory__ = ɵɵinject(Config);
        }
        return __ngConditionalFactory__;
      },
      providedIn: "root"
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(LayoutConfig, [{
    type: Injectable,
    args: [{
      providedIn: "root",
      useExisting: Config
    }]
  }], null, null);
})();
var LaunchDialogService = class _LaunchDialogService {
  get data$() {
    return this._dataSubject.asObservable();
  }
  constructor(renderStrategies, layoutConfig2) {
    this.renderStrategies = renderStrategies;
    this.layoutConfig = layoutConfig2;
    this._dialogClose = new BehaviorSubject(void 0);
    this._dataSubject = new BehaviorSubject(void 0);
    this.logger = inject(LoggerService);
    this.renderStrategies = this.renderStrategies || [];
  }
  /**
   * Open the dialog
   *
   * @param caller LAUNCH_CALLER
   * @param openElement button's Element ref
   * @param vcr View Container Ref of the container for inline rendering
   * @param data optional data which could be passed to dialog
   */
  openDialog(caller, openElement, vcr, data) {
    const component = this.launch(caller, vcr, data);
    if (component) {
      return combineLatest([component, this.dialogClose]).pipe(filter(([, close]) => close !== void 0), tap(([comp]) => {
        openElement?.nativeElement.focus();
        this.clear(caller);
        comp?.destroy();
      }), map(([comp]) => comp));
    }
  }
  /**
   * Render the element based on the strategy from the launch configuration
   *
   * @param caller LAUNCH_CALLER
   * @param vcr View Container Ref of the container for inline rendering
   */
  launch(caller, vcr, data) {
    const config = this.findConfiguration(caller);
    if (config) {
      const renderer = this.getStrategy(config);
      if (renderer) {
        this._dialogClose.next(void 0);
        this._dataSubject.next(data);
        return renderer.render(config, caller, vcr);
      }
    } else if (isDevMode()) {
      this.logger.warn("No configuration provided for caller " + caller);
    }
  }
  /**
   * Opens dialog and subscribe in the service. Should be used if the trigger component might get destroyed while the component is open.
   *
   * @param caller Launch Caller
   * @param openElement Element to open
   * @param data Data to provide to the rendered element
   */
  openDialogAndSubscribe(caller, openElement, data) {
    this.openDialog(caller, openElement, void 0, data)?.pipe(take(1)).subscribe();
  }
  /**
   * Util method to remove element from rendered elements list
   *
   * @param caller LAUNCH_CALLER
   */
  clear(caller) {
    const config = this.findConfiguration(caller);
    if (config) {
      const renderer = this.getStrategy(config);
      if (renderer) {
        renderer.remove(caller, config);
      }
    }
  }
  get dialogClose() {
    return this._dialogClose.asObservable();
  }
  closeDialog(reason) {
    this._dialogClose.next(reason);
  }
  /**
   * Returns the configuration for the caller
   *
   * @param caller LAUNCH_CALLER
   */
  findConfiguration(caller) {
    if (this.layoutConfig?.launch) {
      return this.layoutConfig.launch[caller];
    }
    return void 0;
  }
  /**
   * Returns the render strategy based on the configuration
   *
   * @param config Configuration for launch
   */
  getStrategy(config) {
    return resolveApplicable(this.renderStrategies, [config]);
  }
  static {
    this.ɵfac = function LaunchDialogService_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _LaunchDialogService)(ɵɵinject(LaunchRenderStrategy), ɵɵinject(LayoutConfig));
    };
  }
  static {
    this.ɵprov = ɵɵdefineInjectable({
      token: _LaunchDialogService,
      factory: _LaunchDialogService.ɵfac,
      providedIn: "root"
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(LaunchDialogService, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], () => [{
    type: void 0,
    decorators: [{
      type: Inject,
      args: [LaunchRenderStrategy]
    }]
  }, {
    type: LayoutConfig
  }], null);
})();
var AnonymousConsentManagementBannerComponent = class _AnonymousConsentManagementBannerComponent {
  constructor(anonymousConsentsService, vcr, launchDialogService) {
    this.anonymousConsentsService = anonymousConsentsService;
    this.vcr = vcr;
    this.launchDialogService = launchDialogService;
    this.subscriptions = new Subscription();
    this.bannerVisible$ = this.anonymousConsentsService.isBannerVisible();
    useFeatureStyles("a11yScrollToTopPositioning");
  }
  viewDetails() {
    this.hideBanner();
    const dialog = this.launchDialogService.openDialog(LAUNCH_CALLER.ANONYMOUS_CONSENT, void 0, this.vcr);
    if (dialog) {
      this.subscriptions.add(dialog.subscribe());
    }
  }
  allowAll() {
    this.subscriptions.add(this.anonymousConsentsService.giveAllConsents().pipe(tap(() => this.hideBanner())).subscribe());
  }
  hideBanner() {
    this.anonymousConsentsService.toggleBannerDismissed(true);
  }
  ngOnDestroy() {
    this.subscriptions.unsubscribe();
  }
  static {
    this.ɵfac = function AnonymousConsentManagementBannerComponent_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _AnonymousConsentManagementBannerComponent)(ɵɵdirectiveInject(AnonymousConsentsService), ɵɵdirectiveInject(ViewContainerRef), ɵɵdirectiveInject(LaunchDialogService));
    };
  }
  static {
    this.ɵcmp = ɵɵdefineComponent({
      type: _AnonymousConsentManagementBannerComponent,
      selectors: [["cx-anonymous-consent-management-banner"]],
      standalone: false,
      decls: 2,
      vars: 3,
      consts: [[4, "ngIf"], [1, "anonymous-consent-banner", 3, "ngClass"], [1, "container"], ["class", "row", 4, "cxFeature"], [1, "row"], [1, "col-lg-7", "col-xs-12"], [1, "cx-banner-title"], [1, "cx-banner-description"], [1, "col-lg-5", "col-xs-12", "cx-banner-buttons"], [1, "btn", "btn-secondary", 3, "click"], [1, "btn", "btn-primary", 3, "click"], [1, "col-xl-7", "col-lg-6", "col-xs-12"], [1, "col-xl-5", "col-lg-6", "col-xs-12", "cx-banner-buttons"]],
      template: function AnonymousConsentManagementBannerComponent_Template(rf, ctx) {
        if (rf & 1) {
          ɵɵtemplate(0, AnonymousConsentManagementBannerComponent_ng_container_0_Template, 5, 5, "ng-container", 0);
          ɵɵpipe(1, "async");
        }
        if (rf & 2) {
          ɵɵproperty("ngIf", ɵɵpipeBind1(1, 1, ctx.bannerVisible$));
        }
      },
      dependencies: [NgClass, NgIf, FeatureDirective, AsyncPipe, TranslatePipe],
      encapsulation: 2
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(AnonymousConsentManagementBannerComponent, [{
    type: Component,
    args: [{
      selector: "cx-anonymous-consent-management-banner",
      standalone: false,
      template: `<ng-container *ngIf="bannerVisible$ | async as bannerVisible">
  <div
    [ngClass]="{ 'anonymous-consent-banner-hidden': !bannerVisible }"
    class="anonymous-consent-banner"
  >
    <div class="container">
      <div class="row" *cxFeature="'!a11yHideConsentButtonWhenBannerVisible'">
        <div class="col-lg-7 col-xs-12">
          <div class="cx-banner-title">
            {{ 'anonymousConsents.banner.title' | cxTranslate }}
          </div>
          <div class="cx-banner-description">
            {{ 'anonymousConsents.banner.description' | cxTranslate }}
          </div>
        </div>

        <div class="col-lg-5 col-xs-12 cx-banner-buttons">
          <button class="btn btn-secondary" (click)="viewDetails()">
            {{ 'anonymousConsents.banner.viewDetails' | cxTranslate }}
          </button>
          <button class="btn btn-primary" (click)="allowAll()">
            {{ 'anonymousConsents.banner.allowAll' | cxTranslate }}
          </button>
        </div>
      </div>
      <div class="row" *cxFeature="'a11yHideConsentButtonWhenBannerVisible'">
        <div class="col-xl-7 col-lg-6 col-xs-12">
          <div class="cx-banner-title">
            {{ 'anonymousConsents.banner.title' | cxTranslate }}
          </div>
          <div class="cx-banner-description">
            {{ 'anonymousConsents.banner.description' | cxTranslate }}
          </div>
        </div>

        <div class="col-xl-5 col-lg-6 col-xs-12 cx-banner-buttons">
          <button class="btn btn-secondary" (click)="viewDetails()">
            {{ 'anonymousConsents.banner.consentManagement' | cxTranslate }}
          </button>
          <button class="btn btn-primary" (click)="allowAll()">
            {{ 'anonymousConsents.banner.allowAll' | cxTranslate }}
          </button>
        </div>
      </div>
    </div>
  </div>
</ng-container>
`
    }]
  }], () => [{
    type: AnonymousConsentsService
  }, {
    type: ViewContainerRef
  }, {
    type: LaunchDialogService
  }], null);
})();
var InlineRenderStrategy = class _InlineRenderStrategy extends LaunchRenderStrategy {
  constructor(document2, rendererFactory, componentFactoryResolver) {
    super(document2, rendererFactory);
    this.document = document2;
    this.rendererFactory = rendererFactory;
    this.componentFactoryResolver = componentFactoryResolver;
    this.logger = inject(LoggerService);
  }
  /**
   * Renders the component from the configuration in the view container ref
   *
   * @param config
   * @param caller
   * @param vcr
   */
  render(config, caller, vcr) {
    if (vcr && this.shouldRender(caller, config)) {
      const template = this.componentFactoryResolver.resolveComponentFactory(config.component);
      const component = vcr.createComponent(template);
      if (config?.dialogType) {
        this.applyClasses(component, config?.dialogType);
      }
      this.renderedCallers.push({
        caller,
        element: vcr.element,
        component
      });
      return of(component);
    } else if (isDevMode()) {
      if (!vcr) {
        this.logger.warn(`No view container ref provided for ${caller}`);
      } else {
        this.logger.warn(`Element for ${caller} already rendered. To allow multi rendering add property multi: true.`);
      }
    }
  }
  hasMatch(config) {
    return Boolean(config.inline);
  }
  static {
    this.ɵfac = function InlineRenderStrategy_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _InlineRenderStrategy)(ɵɵinject(DOCUMENT), ɵɵinject(RendererFactory2), ɵɵinject(ComponentFactoryResolver$1));
    };
  }
  static {
    this.ɵprov = ɵɵdefineInjectable({
      token: _InlineRenderStrategy,
      factory: _InlineRenderStrategy.ɵfac,
      providedIn: "root"
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(InlineRenderStrategy, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], () => [{
    type: void 0,
    decorators: [{
      type: Inject,
      args: [DOCUMENT]
    }]
  }, {
    type: RendererFactory2
  }, {
    type: ComponentFactoryResolver$1
  }], null);
})();
var InlineRootRenderStrategy = class _InlineRootRenderStrategy extends LaunchRenderStrategy {
  constructor(document2, rendererFactory, componentFactoryResolver, injector) {
    super(document2, rendererFactory);
    this.document = document2;
    this.rendererFactory = rendererFactory;
    this.componentFactoryResolver = componentFactoryResolver;
    this.injector = injector;
  }
  get hostComponent() {
    return this.injector.get(ApplicationRef)?.components?.[0];
  }
  render(config, caller) {
    if (this.shouldRender(caller, config)) {
      const componentFactory = this.componentFactoryResolver.resolveComponentFactory(config.component);
      const contentInjector = Injector.create({
        providers: []
      });
      const componentRef = componentFactory.create(contentInjector);
      this.injector.get(ApplicationRef)?.attachView(componentRef.hostView);
      this.renderer.appendChild(this.hostComponent?.location.nativeElement, componentRef.location.nativeElement);
      if (config?.dialogType) {
        this.applyClasses(componentRef, config?.dialogType);
      }
      this.renderedCallers.push({
        caller,
        component: componentRef
      });
      return of(componentRef);
    }
  }
  hasMatch(config) {
    return Boolean(config.inlineRoot);
  }
  static {
    this.ɵfac = function InlineRootRenderStrategy_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _InlineRootRenderStrategy)(ɵɵinject(DOCUMENT), ɵɵinject(RendererFactory2), ɵɵinject(ComponentFactoryResolver$1), ɵɵinject(Injector));
    };
  }
  static {
    this.ɵprov = ɵɵdefineInjectable({
      token: _InlineRootRenderStrategy,
      factory: _InlineRootRenderStrategy.ɵfac,
      providedIn: "root"
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(InlineRootRenderStrategy, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], () => [{
    type: void 0,
    decorators: [{
      type: Inject,
      args: [DOCUMENT]
    }]
  }, {
    type: RendererFactory2
  }, {
    type: ComponentFactoryResolver$1
  }, {
    type: Injector
  }], null);
})();
var OutletPosition;
(function(OutletPosition2) {
  OutletPosition2["REPLACE"] = "replace";
  OutletPosition2["BEFORE"] = "before";
  OutletPosition2["AFTER"] = "after";
})(OutletPosition || (OutletPosition = {}));
var AVOID_STACKED_OUTLETS = false;
var USE_STACKED_OUTLETS = true;
var OutletContextData = class {
};
var OutletService = class _OutletService {
  constructor() {
    this.templatesRefs = {
      [OutletPosition.BEFORE]: /* @__PURE__ */ new Map(),
      [OutletPosition.REPLACE]: /* @__PURE__ */ new Map(),
      [OutletPosition.AFTER]: /* @__PURE__ */ new Map()
    };
  }
  /**
   * @param templateOrFactory A `ComponentFactory` that inserts a component dynamically.
   */
  add(outlet, templateOrFactory, position = OutletPosition.REPLACE) {
    const store = this.templatesRefs[position];
    if (store) {
      const existing = store.get(outlet) || [];
      const newValue = existing.concat([templateOrFactory]);
      store.set(outlet, newValue);
    }
  }
  /**
   *
   * Returns a single object or multiple objects for the given outlet reference,
   * depending on the `stacked` argument.
   *
   * @param outlet The outlet reference
   * @param position the outlet position, `OutletPosition.before`, `OutletPosition.AFTER` or `OutletPosition.REPLACE`
   * @param stacked Indicates whether an array of outlet components is returned
   */
  get(outlet, position = OutletPosition.REPLACE, stacked = AVOID_STACKED_OUTLETS) {
    const store = this.templatesRefs[position] || this.templatesRefs[OutletPosition.REPLACE];
    const templateRef = store.get(outlet);
    if (templateRef && !stacked) {
      return templateRef[0];
    }
    return templateRef;
  }
  remove(outlet, position = OutletPosition.REPLACE, value) {
    const store = this.templatesRefs[position] || this.templatesRefs[OutletPosition.REPLACE];
    this.removeValueOrAll(store, outlet, value);
  }
  removeValueOrAll(store, outlet, value) {
    if (!value && store.has(outlet)) {
      store.delete(outlet);
    } else if (value && store.has(outlet)) {
      let existing = store.get(outlet);
      existing = existing?.filter((val) => val !== value);
      if (existing) {
        store.set(outlet, existing);
      }
    }
  }
  static {
    this.ɵfac = function OutletService_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _OutletService)();
    };
  }
  static {
    this.ɵprov = ɵɵdefineInjectable({
      token: _OutletService,
      factory: _OutletService.ɵfac,
      providedIn: "root"
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(OutletService, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], null, null);
})();
var OutletRefDirective = class _OutletRefDirective {
  constructor(tpl, outletService) {
    this.tpl = tpl;
    this.outletService = outletService;
  }
  ngOnInit() {
    this.outletService.add(this.cxOutletRef, this.tpl, this.cxOutletPos);
  }
  ngOnDestroy() {
    this.outletService.remove(this.cxOutletRef, this.cxOutletPos, this.tpl);
  }
  static {
    this.ɵfac = function OutletRefDirective_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _OutletRefDirective)(ɵɵdirectiveInject(TemplateRef), ɵɵdirectiveInject(OutletService));
    };
  }
  static {
    this.ɵdir = ɵɵdefineDirective({
      type: _OutletRefDirective,
      selectors: [["", "cxOutletRef", ""]],
      inputs: {
        cxOutletRef: "cxOutletRef",
        cxOutletPos: "cxOutletPos"
      },
      standalone: false
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(OutletRefDirective, [{
    type: Directive,
    args: [{
      selector: "[cxOutletRef]",
      standalone: false
    }]
  }], () => [{
    type: TemplateRef
  }, {
    type: OutletService
  }], {
    cxOutletRef: [{
      type: Input
    }],
    cxOutletPos: [{
      type: Input
    }]
  });
})();
var OutletRefModule = class _OutletRefModule {
  static {
    this.ɵfac = function OutletRefModule_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _OutletRefModule)();
    };
  }
  static {
    this.ɵmod = ɵɵdefineNgModule({
      type: _OutletRefModule,
      declarations: [OutletRefDirective],
      imports: [CommonModule],
      exports: [OutletRefDirective]
    });
  }
  static {
    this.ɵinj = ɵɵdefineInjector({
      imports: [CommonModule]
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(OutletRefModule, [{
    type: NgModule,
    args: [{
      imports: [CommonModule],
      declarations: [OutletRefDirective],
      exports: [OutletRefDirective]
    }]
  }], null, null);
})();
var OutletRendererService = class _OutletRendererService {
  constructor() {
    this.outletRefs = new BehaviorSubject(/* @__PURE__ */ new Map());
  }
  /**
   * Dynamically render the templates in the specified array
   *
   * @param outlet
   */
  render(outlet) {
    if (this.outletRefs.value.size !== 0) {
      this.outletRefs.value.get(outlet)?.render();
    }
  }
  /**
   * Register outlet to be available to render dynamically
   *
   * @param cxOutlet
   * @param context
   */
  register(cxOutlet, context) {
    this.outletRefs.next(this.outletRefs.value.set(cxOutlet, context));
  }
  /**
   * Returns map of outlets
   *
   */
  getOutletRef(outlet) {
    return this.outletRefs.asObservable().pipe(map((val) => val.get(outlet)), filter(isNotNullable));
  }
  static {
    this.ɵfac = function OutletRendererService_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _OutletRendererService)();
    };
  }
  static {
    this.ɵprov = ɵɵdefineInjectable({
      token: _OutletRendererService,
      factory: _OutletRendererService.ɵfac,
      providedIn: "root"
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(OutletRendererService, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], null, null);
})();
var IntersectionService = class _IntersectionService {
  constructor(config) {
    this.config = config;
  }
  /**
   * Returns an Observable that emits only once a boolean value whenever
   * the given element has shown in the view port.
   *
   * The returned observable will only emit the first value. The
   * observable must be cleaned up either way, since the value might never emit; it
   * depends on whether the element appears in the view port.
   *
   * @param element - HTML element
   * @param options - Allows to specify an optional root margin, in order to fire before the element shows up in the viewport
   * @param intersectingCondition - Allows to specify an intersecting condition.
   * If this parameter is not set, then the transition state of the element will be verified whenever the element intersects the view port.
   * @returns Element intersects?
   */
  isIntersected(element, options, intersectingCondition) {
    return this.intersects(element, options, intersectingCondition).pipe(first((v) => v === true));
  }
  /**
   * Returns an observable that emits for every change of intersection of a given element.
   *
   * @param element - HTML element
   * @param options - Allows to specify an optional root margin, in order to fire before the element shows up in the viewport
   * @param intersectingCondition - Allows to specify an intersecting condition.
   * If this parameter is not set, then the transition state of the element will be verified whenever the element intersects the view port.
   * @returns Element intersects?
   */
  isIntersecting(element, options, intersectingCondition) {
    return this.intersects(element, options, intersectingCondition);
  }
  /**
   * Indicates whenever the element intersects the view port. An optional margin
   * is used to intersects before the element shows up in the viewport.
   * A value is emitted each time the element intersects.
   */
  intersects(element, options = {}, intersectingCondition) {
    return this.createIntersectionObservable(element, options).pipe(mergeMap((entries) => entries), map((entry) => intersectingCondition ? intersectingCondition(entry) : entry.isIntersecting), distinctUntilChanged());
  }
  createIntersectionObservable(element, options) {
    return new Observable((observer) => {
      const rootMargin = this.getRootMargin(options);
      const intersectOptions = {
        rootMargin,
        threshold: options.threshold
      };
      const intersectionObserver = new IntersectionObserver((entries) => {
        observer.next(entries);
      }, intersectOptions);
      intersectionObserver.observe(element);
      return () => {
        intersectionObserver.disconnect();
      };
    });
  }
  getRootMargin(options = {}) {
    if (options.rootMargin) {
      return options.rootMargin;
    }
    const layoutConfig2 = this.config;
    if (layoutConfig2.deferredLoading && layoutConfig2.deferredLoading.intersectionMargin) {
      return layoutConfig2.deferredLoading.intersectionMargin;
    }
  }
  static {
    this.ɵfac = function IntersectionService_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _IntersectionService)(ɵɵinject(LayoutConfig));
    };
  }
  static {
    this.ɵprov = ɵɵdefineInjectable({
      token: _IntersectionService,
      factory: _IntersectionService.ɵfac,
      providedIn: "root"
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(IntersectionService, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], () => [{
    type: LayoutConfig
  }], null);
})();
var DeferLoaderService = class _DeferLoaderService {
  constructor(platformId, config, intersectionService) {
    this.platformId = platformId;
    this.config = config;
    this.intersectionService = intersectionService;
    this.globalLoadStrategy = config.deferredLoading?.strategy ?? DeferLoadingStrategy.INSTANT;
  }
  /**
   * Defer loading till the element intersects the viewport.
   *
   * We evaluate whether we instantly load the element for different reasons:
   * - we run in SSR mode
   * - there's no global strategy given
   * - the global loading strategy is set to INSTANT loading,
   *   and the loading strategy in the given is not set to DEFER
   * - the loading strategy in the given options is set to INSTANT
   */
  load(element, options) {
    if (this.shouldLoadInstantly((options || {}).deferLoading)) {
      return of(true);
    } else {
      return this.intersectionService.isIntersected(element, options);
    }
  }
  shouldLoadInstantly(elementLoadingStrategy) {
    return isPlatformServer(this.platformId) || elementLoadingStrategy === DeferLoadingStrategy.INSTANT || elementLoadingStrategy !== DeferLoadingStrategy.DEFER && this.globalLoadStrategy === DeferLoadingStrategy.INSTANT;
  }
  static {
    this.ɵfac = function DeferLoaderService_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _DeferLoaderService)(ɵɵinject(PLATFORM_ID), ɵɵinject(LayoutConfig), ɵɵinject(IntersectionService));
    };
  }
  static {
    this.ɵprov = ɵɵdefineInjectable({
      token: _DeferLoaderService,
      factory: _DeferLoaderService.ɵfac,
      providedIn: "root"
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(DeferLoaderService, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], () => [{
    type: Object,
    decorators: [{
      type: Inject,
      args: [PLATFORM_ID]
    }]
  }, {
    type: LayoutConfig
  }, {
    type: IntersectionService
  }], null);
})();
var OutletDirective = class _OutletDirective {
  constructor(vcr, templateRef, outletService, deferLoaderService, outletRendererService) {
    this.vcr = vcr;
    this.templateRef = templateRef;
    this.outletService = outletService;
    this.deferLoaderService = deferLoaderService;
    this.outletRendererService = outletRendererService;
    this.renderedTemplate = [];
    this.renderedComponents = /* @__PURE__ */ new Map();
    this.outletContext$ = new ReplaySubject(1);
    this.loaded = new EventEmitter(true);
    this.cxComponentRefChange = new EventEmitter();
    this.subscription = new Subscription();
  }
  /**
   * Renders view for outlet or defers it, depending on the input `cxOutletDefer`
   */
  render() {
    this.vcr.clear();
    this.renderedTemplate = [];
    this.renderedComponents.clear();
    this.subscription.unsubscribe();
    this.subscription = new Subscription();
    if (this.cxOutletDefer) {
      this.deferLoading();
    } else {
      this.build();
    }
  }
  ngOnChanges(changes) {
    if (changes.cxOutlet) {
      this.render();
      this.outletRendererService.register(this.cxOutlet, this);
    }
    if (changes.cxOutletContext) {
      this.outletContext$.next(this.cxOutletContext);
    }
  }
  deferLoading() {
    this.loaded.emit(false);
    const hostElement = this.getHostElement(this.vcr.element.nativeElement);
    this.subscription.add(this.deferLoaderService.load(hostElement, this.cxOutletDefer).subscribe(() => {
      this.build();
      this.loaded.emit(true);
    }));
  }
  /**
   * Renders view for outlet
   */
  build() {
    this.buildOutlet(OutletPosition.BEFORE);
    this.buildOutlet(OutletPosition.REPLACE);
    this.buildOutlet(OutletPosition.AFTER);
  }
  /**
   * Renders view in a given position for outlet
   */
  buildOutlet(position) {
    let templates = this.outletService.get(this.cxOutlet, position, USE_STACKED_OUTLETS);
    templates = templates?.filter((el) => !this.renderedTemplate.includes(el));
    if (!templates && position === OutletPosition.REPLACE) {
      templates = [this.templateRef];
    }
    if (!Array.isArray(templates)) {
      templates = [templates];
    }
    const components = [];
    templates.forEach((obj) => {
      const component = this.create(obj, position);
      if (component) {
        components.push(component);
      }
    });
    this.renderedComponents.set(position, components);
  }
  /**
   * Renders view based on the given template or component factory
   */
  create(tmplOrFactory, position) {
    this.renderedTemplate.push(tmplOrFactory);
    if (tmplOrFactory instanceof ComponentFactory$1) {
      const component = this.vcr.createComponent(tmplOrFactory, void 0, this.getComponentInjector(position));
      this.cxComponentRefChange.emit(component);
      return component;
    } else if (tmplOrFactory instanceof TemplateRef) {
      const view = this.vcr.createEmbeddedView(tmplOrFactory, {
        $implicit: this.cxOutletContext
      });
      view.markForCheck();
      this.cxComponentRefChange.emit(view);
      return view;
    }
  }
  /**
   * Returns injector with OutletContextData that can be injected to the component
   * rendered in the outlet
   */
  getComponentInjector(position) {
    const contextData = {
      reference: this.cxOutlet,
      position,
      context: this.cxOutletContext,
      context$: this.outletContext$.asObservable()
    };
    return Injector.create({
      providers: [{
        provide: OutletContextData,
        useValue: contextData
      }],
      parent: this.vcr.injector
    });
  }
  /**
   * Returns the closest `HtmlElement`, by iterating over the
   * parent nodes of the given element.
   *
   * We avoid traversing the parent _elements_, as this is blocking
   * ie11 implementations. One of the spare exclusions we make to not
   * supporting ie11.
   *
   * @param element
   */
  getHostElement(element) {
    if (element instanceof HTMLElement) {
      return element;
    }
    return this.getHostElement(element.parentNode);
  }
  ngOnDestroy() {
    this.subscription.unsubscribe();
    this.outletContext$.complete();
  }
  static {
    this.ɵfac = function OutletDirective_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _OutletDirective)(ɵɵdirectiveInject(ViewContainerRef), ɵɵdirectiveInject(TemplateRef), ɵɵdirectiveInject(OutletService), ɵɵdirectiveInject(DeferLoaderService), ɵɵdirectiveInject(OutletRendererService));
    };
  }
  static {
    this.ɵdir = ɵɵdefineDirective({
      type: _OutletDirective,
      selectors: [["", "cxOutlet", ""]],
      inputs: {
        cxOutlet: "cxOutlet",
        cxOutletContext: "cxOutletContext",
        cxOutletDefer: "cxOutletDefer",
        cxComponentRef: "cxComponentRef"
      },
      outputs: {
        loaded: "loaded",
        cxComponentRefChange: "cxComponentRefChange"
      },
      standalone: false,
      features: [ɵɵNgOnChangesFeature]
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(OutletDirective, [{
    type: Directive,
    args: [{
      selector: "[cxOutlet]",
      standalone: false
    }]
  }], () => [{
    type: ViewContainerRef
  }, {
    type: TemplateRef
  }, {
    type: OutletService
  }, {
    type: DeferLoaderService
  }, {
    type: OutletRendererService
  }], {
    cxOutlet: [{
      type: Input
    }],
    cxOutletContext: [{
      type: Input
    }],
    cxOutletDefer: [{
      type: Input
    }],
    loaded: [{
      type: Output
    }],
    cxComponentRef: [{
      type: Input
    }],
    cxComponentRefChange: [{
      type: Output
    }]
  });
})();
var PROVIDE_OUTLET_OPTIONS = new InjectionToken("PROVIDE_OUTLET_OPTIONS");
function provideOutlet(options) {
  return {
    provide: PROVIDE_OUTLET_OPTIONS,
    useValue: options,
    multi: true
  };
}
function registerOutletsFactory(providedOutletOptions, componentFactoryResolver, outletService) {
  const result = () => {
    (providedOutletOptions ?? []).forEach((options) => {
      const factory = componentFactoryResolver.resolveComponentFactory(options.component);
      outletService.add(options.id, factory, options.position ?? OutletPosition.AFTER);
    });
  };
  return result;
}
var OutletModule = class _OutletModule {
  static forRoot() {
    return {
      ngModule: _OutletModule,
      providers: [{
        provide: APP_INITIALIZER,
        useFactory: registerOutletsFactory,
        deps: [[new Optional(), PROVIDE_OUTLET_OPTIONS], ComponentFactoryResolver$1, OutletService],
        multi: true
      }]
    };
  }
  static forChild() {
    return {
      ngModule: _OutletModule,
      providers: [{
        provide: MODULE_INITIALIZER,
        useFactory: registerOutletsFactory,
        deps: [[new Optional(), PROVIDE_OUTLET_OPTIONS], ComponentFactoryResolver$1, OutletService],
        multi: true
      }]
    };
  }
  static {
    this.ɵfac = function OutletModule_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _OutletModule)();
    };
  }
  static {
    this.ɵmod = ɵɵdefineNgModule({
      type: _OutletModule,
      declarations: [OutletDirective],
      imports: [CommonModule],
      exports: [OutletDirective]
    });
  }
  static {
    this.ɵinj = ɵɵdefineInjector({
      imports: [CommonModule]
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(OutletModule, [{
    type: NgModule,
    args: [{
      imports: [CommonModule],
      declarations: [OutletDirective],
      exports: [OutletDirective]
    }]
  }], null, null);
})();
var OutletRenderStrategy = class _OutletRenderStrategy extends LaunchRenderStrategy {
  constructor(document2, rendererFactory, outletService, componentFactoryResolver, outletRendererService) {
    super(document2, rendererFactory);
    this.document = document2;
    this.rendererFactory = rendererFactory;
    this.outletService = outletService;
    this.componentFactoryResolver = componentFactoryResolver;
    this.outletRendererService = outletRendererService;
  }
  /**
   * Renders the element in the configured outlet
   *
   * @param config
   * @param caller
   * @param vcr
   */
  render(config, caller) {
    if (this.shouldRender(caller, config)) {
      const template = this.componentFactoryResolver.resolveComponentFactory(config.component);
      this.outletService.add(config.outlet, template, config.position ? config.position : OutletPosition.BEFORE);
      this.outletRendererService.render(config.outlet);
      this.renderedCallers.push({
        caller
      });
      return this.outletRendererService.getOutletRef(config.outlet).pipe(map((outletDirective) => {
        const components = outletDirective.renderedComponents.get(config.position ? config.position : OutletPosition.BEFORE);
        return components.reverse().find((component) => component.componentType === template.componentType);
      }), tap((component) => {
        if (config?.dialogType && component) {
          this.applyClasses(component, config?.dialogType);
        }
      }));
    }
  }
  hasMatch(config) {
    return Boolean(config.outlet);
  }
  remove(caller, config) {
    const template = this.componentFactoryResolver.resolveComponentFactory(config.component);
    this.outletService.remove(config.outlet, config.position ? config.position : OutletPosition.BEFORE, template);
    super.remove(caller, config);
  }
  static {
    this.ɵfac = function OutletRenderStrategy_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _OutletRenderStrategy)(ɵɵinject(DOCUMENT), ɵɵinject(RendererFactory2), ɵɵinject(OutletService), ɵɵinject(ComponentFactoryResolver$1), ɵɵinject(OutletRendererService));
    };
  }
  static {
    this.ɵprov = ɵɵdefineInjectable({
      token: _OutletRenderStrategy,
      factory: _OutletRenderStrategy.ɵfac,
      providedIn: "root"
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(OutletRenderStrategy, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], () => [{
    type: void 0,
    decorators: [{
      type: Inject,
      args: [DOCUMENT]
    }]
  }, {
    type: RendererFactory2
  }, {
    type: OutletService
  }, {
    type: ComponentFactoryResolver$1
  }, {
    type: OutletRendererService
  }], null);
})();
var RoutingRenderStrategy = class _RoutingRenderStrategy extends LaunchRenderStrategy {
  constructor(document2, rendererFactory, routingService) {
    super(document2, rendererFactory);
    this.document = document2;
    this.rendererFactory = rendererFactory;
    this.routingService = routingService;
  }
  /**
   * Navigates to the route configured for the caller
   */
  render(config, _caller) {
    this.routingService.go(config);
  }
  hasMatch(config) {
    return Boolean(config.cxRoute);
  }
  static {
    this.ɵfac = function RoutingRenderStrategy_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _RoutingRenderStrategy)(ɵɵinject(DOCUMENT), ɵɵinject(RendererFactory2), ɵɵinject(RoutingService));
    };
  }
  static {
    this.ɵprov = ɵɵdefineInjectable({
      token: _RoutingRenderStrategy,
      factory: _RoutingRenderStrategy.ɵfac,
      providedIn: "root"
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(RoutingRenderStrategy, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], () => [{
    type: void 0,
    decorators: [{
      type: Inject,
      args: [DOCUMENT]
    }]
  }, {
    type: RendererFactory2
  }, {
    type: RoutingService
  }], null);
})();
var LaunchDialogModule = class _LaunchDialogModule {
  static forRoot() {
    return {
      ngModule: _LaunchDialogModule,
      providers: [{
        provide: LayoutConfig,
        useExisting: Config
      }]
    };
  }
  static {
    this.ɵfac = function LaunchDialogModule_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _LaunchDialogModule)();
    };
  }
  static {
    this.ɵmod = ɵɵdefineNgModule({
      type: _LaunchDialogModule
    });
  }
  static {
    this.ɵinj = ɵɵdefineInjector({
      providers: [{
        provide: LaunchRenderStrategy,
        useExisting: OutletRenderStrategy,
        multi: true
      }, {
        provide: LaunchRenderStrategy,
        useExisting: InlineRenderStrategy,
        multi: true
      }, {
        provide: LaunchRenderStrategy,
        useExisting: RoutingRenderStrategy,
        multi: true
      }, {
        provide: LaunchRenderStrategy,
        useExisting: InlineRootRenderStrategy,
        multi: true
      }]
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(LaunchDialogModule, [{
    type: NgModule,
    args: [{
      providers: [{
        provide: LaunchRenderStrategy,
        useExisting: OutletRenderStrategy,
        multi: true
      }, {
        provide: LaunchRenderStrategy,
        useExisting: InlineRenderStrategy,
        multi: true
      }, {
        provide: LaunchRenderStrategy,
        useExisting: RoutingRenderStrategy,
        multi: true
      }, {
        provide: LaunchRenderStrategy,
        useExisting: InlineRootRenderStrategy,
        multi: true
      }]
    }]
  }], null, null);
})();
var ICON_TYPE;
(function(ICON_TYPE2) {
  ICON_TYPE2["ACTIVE"] = "ACTIVE";
  ICON_TYPE2["ADDRESS_BOOK"] = "ADDRESS_BOOK";
  ICON_TYPE2["AMEX"] = "AMEX";
  ICON_TYPE2["CARET_DOWN"] = "CARET_DOWN";
  ICON_TYPE2["CARET_LEFT"] = "CARET_LEFT";
  ICON_TYPE2["CARET_RIGHT"] = "CARET_RIGHT";
  ICON_TYPE2["CARET_UP"] = "CARET_UP";
  ICON_TYPE2["CART"] = "CART";
  ICON_TYPE2["CART_PLUS"] = "CART_PLUS";
  ICON_TYPE2["CART_ARROW_DOWN"] = "CART_ARROW_DOWN";
  ICON_TYPE2["CHECK"] = "CHECK";
  ICON_TYPE2["CIRCLE"] = "CIRCLE";
  ICON_TYPE2["CLIPBOARD_LIST"] = "CLIPBOARD_LIST";
  ICON_TYPE2["CLOCK"] = "CLOCK";
  ICON_TYPE2["CLOSE"] = "CLOSE";
  ICON_TYPE2["COLLAPSE"] = "COLLAPSE";
  ICON_TYPE2["CREDIT_CARD"] = "CREDIT_CARD";
  ICON_TYPE2["DINERS_CLUB"] = "DINERS_CLUB";
  ICON_TYPE2["DOWNLOAD"] = "DOWNLOAD";
  ICON_TYPE2["EMPTY_HEART"] = "EMPTY_HEART";
  ICON_TYPE2["ERROR"] = "ERROR";
  ICON_TYPE2["EXPAND"] = "EXPAND";
  ICON_TYPE2["EXPAND_ARROWS"] = "EXPAND_ARROWS";
  ICON_TYPE2["EYE"] = "EYE";
  ICON_TYPE2["EYE_SLASH"] = "EYE_SLASH";
  ICON_TYPE2["FILE"] = "FILE";
  ICON_TYPE2["FILTER"] = "FILTER";
  ICON_TYPE2["GRID"] = "GRID";
  ICON_TYPE2["HEART"] = "HEART";
  ICON_TYPE2["INFO"] = "INFO";
  ICON_TYPE2["LINK_OUT"] = "LINK_OUT";
  ICON_TYPE2["LIST"] = "LIST";
  ICON_TYPE2["MASTER_CARD"] = "MASTER_CARD";
  ICON_TYPE2["OFF"] = "OFF";
  ICON_TYPE2["ON"] = "ON";
  ICON_TYPE2["ORDER"] = "ORDER";
  ICON_TYPE2["PENCIL"] = "PENCIL";
  ICON_TYPE2["RESET"] = "RESET";
  ICON_TYPE2["REPEAT"] = "REPEAT";
  ICON_TYPE2["SEARCH"] = "SEARCH";
  ICON_TYPE2["SEARCH_PLUS"] = "SEARCH_PLUS";
  ICON_TYPE2["SEARCH_MINUS"] = "SEARCH_MINUS";
  ICON_TYPE2["SORT"] = "SORT";
  ICON_TYPE2["SORT_AMOUNT_DOWN"] = "SORT_AMOUNT_DOWN";
  ICON_TYPE2["SORT_AMOUNT_UP"] = "SORT_AMOUNT_UP";
  ICON_TYPE2["SORT_DOWN"] = "SORT_DOWN";
  ICON_TYPE2["STAR"] = "STAR";
  ICON_TYPE2["SUCCESS"] = "SUCCESS";
  ICON_TYPE2["TRASH"] = "TRASH";
  ICON_TYPE2["USER_FRIENDS"] = "USER_FRIENDS";
  ICON_TYPE2["VISA"] = "VISA";
  ICON_TYPE2["WARNING"] = "WARNING";
  ICON_TYPE2["HEADSET"] = "HEADSET";
  ICON_TYPE2["ATTACHMENT"] = "ATTACHMENT";
  ICON_TYPE2["UPLOAD"] = "UPLOAD";
  ICON_TYPE2["USER"] = "USER";
  ICON_TYPE2["USER_PLUS"] = "USER_PLUS";
  ICON_TYPE2["ARROW_LEFT"] = "ARROW_LEFT";
  ICON_TYPE2["ARROW_RIGHT"] = "ARROW_RIGHT";
  ICON_TYPE2["ARROW_DOWN"] = "ARROW_DOWN";
  ICON_TYPE2["ARROW_UP"] = "ARROW_UP";
  ICON_TYPE2["C360_CIRCLE_USER"] = "C360_CIRCLE_USER";
  ICON_TYPE2["C360_INVOICE"] = "C360_INVOICE";
  ICON_TYPE2["HELP"] = "HELP";
})(ICON_TYPE || (ICON_TYPE = {}));
var IconConfig = class _IconConfig {
  static {
    this.ɵfac = function IconConfig_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _IconConfig)();
    };
  }
  static {
    this.ɵprov = ɵɵdefineInjectable({
      token: _IconConfig,
      factory: function IconConfig_Factory(__ngFactoryType__) {
        let __ngConditionalFactory__ = null;
        if (__ngFactoryType__) {
          __ngConditionalFactory__ = new (__ngFactoryType__ || _IconConfig)();
        } else {
          __ngConditionalFactory__ = ɵɵinject(Config);
        }
        return __ngConditionalFactory__;
      },
      providedIn: "root"
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(IconConfig, [{
    type: Injectable,
    args: [{
      providedIn: "root",
      useExisting: Config
    }]
  }], null, null);
})();
var IconResourceType;
(function(IconResourceType2) {
  IconResourceType2["SVG"] = "svg";
  IconResourceType2["LINK"] = "link";
  IconResourceType2["TEXT"] = "text";
})(IconResourceType || (IconResourceType = {}));
var fontawesomeIconConfig = {
  icon: {
    symbols: {
      ACTIVE: "fas fa-check",
      ADDRESS_BOOK: "far fa-address-book",
      AMEX: "fab fa-cc-amex",
      CARET_DOWN: "fas fa-angle-down",
      CARET_LEFT: "fas fa-angle-left",
      CARET_RIGHT: "fas fa-angle-right",
      CARET_UP: "fas fa-angle-up",
      CART: "fas fa-shopping-cart",
      CART_PLUS: "fas fa-cart-plus",
      CART_ARROW_DOWN: "fas fa-cart-arrow-down",
      CHECK: "fas fa-check",
      CIRCLE: "fas fa-circle",
      CLIPBOARD_LIST: "fas fa-clipboard-list",
      CLOCK: "far fa-clock",
      CLOSE: "fas fa-times",
      COLLAPSE: "fas fa-minus",
      CREDIT_CARD: "fas fa-credit-card",
      DINERS_CLUB: "fab fa-cc-diners-club",
      DOWNLOAD: "fas fa-download",
      EMPTY_HEART: "far fa-heart",
      ERROR: "fas fa-exclamation-circle",
      EXPAND_ARROWS: "fas fa-expand",
      EXPAND: "fas fa-plus",
      EYE_SLASH: "fas fa-eye-slash",
      EYE: "fas fa-eye",
      FILE: "fas fa-file",
      FILTER: "fas fa-filter",
      GRID: "fas fa-th-large",
      HEART: "fas fa-heart",
      INFO: "fas fa-info-circle",
      LINK_OUT: "fas fa-external-link-alt",
      LIST: "fas fa-bars",
      MASTER_CARD: "fab fa-cc-mastercard",
      OFF: "fas fa-toggle-off",
      ON: "fas fa-toggle-on",
      ORDER: "fas fa-clipboard-list",
      PDF_FILE: "fas fa-file-pdf",
      PENCIL: "fas fa-pencil-alt",
      REPEAT: "fas fa-retweet",
      RESET: "fas fa-times-circle",
      SEARCH: "fas fa-search",
      SEARCH_PLUS: "fas fa-search-plus",
      SEARCH_MINUS: "fas fa-search-minus",
      SORT_AMOUNT_DOWN: "fas fa-sort-amount-down",
      SORT_AMOUNT_UP: "fas fa-sort-amount-up",
      SORT_DOWN: "fas fa-sort-down",
      SORT: "fas fa-sort",
      STAR: "fas fa-star",
      SUCCESS: "fas fa-check-circle",
      TRASH: "fas fa-trash",
      USER_FRIENDS: "fas fa-user-friends",
      VISA: "fab fa-cc-visa",
      WARNING: "fas fa-exclamation-triangle",
      HEADSET: "fas fa-headset",
      ATTACHMENT: "fas fa-paperclip",
      UPLOAD: "fas fa-upload",
      USER: "fas fa-user",
      USER_PLUS: "fas fa-user-plus",
      ARROW_LEFT: "fas fa-arrow-left",
      ARROW_RIGHT: "fas  fa-arrow-right",
      ARROW_DOWN: "fas  fa-arrow-down",
      ARROW_UP: "fas  fa-arrow-up",
      C360_INVOICE: "fas fa-file-invoice",
      C360_CIRCLE_USER: "fas fa-circle-user",
      HELP: "far fa-circle-question"
    },
    resources: [{
      type: IconResourceType.LINK,
      url: ""
    }]
  }
};
var IconLoaderService = class _IconLoaderService {
  constructor(winRef, iconConfig, sanitizer) {
    this.winRef = winRef;
    this.iconConfig = iconConfig;
    this.sanitizer = sanitizer;
    this.loadedResources = [];
  }
  /**
   * Returns an html fragment which can be added to the DOM in a safe way.
   */
  getHtml(type) {
    if (this.isResourceType(type, IconResourceType.SVG)) {
      const url = this.sanitizer.sanitize(SecurityContext.URL, this.getSvgPath(type) || null);
      if (url) {
        const useElement = this.winRef.document.createElement("use");
        useElement.setAttribute("xlink:href", url);
        const svgElement = this.winRef.document.createElement("svg");
        svgElement.appendChild(useElement);
        return this.sanitizer.bypassSecurityTrustHtml(svgElement.outerHTML);
      }
    }
    if (this.isResourceType(type, IconResourceType.TEXT)) {
      const symbol = this.getSymbol(type);
      if (symbol) {
        const helperDiv = this.winRef.document.createElement("div");
        helperDiv.textContent = symbol;
        return this.sanitizer.bypassSecurityTrustHtml(helperDiv.innerHTML);
      }
    }
  }
  /**
   * Return the direction for which the icon should mirror (ltr vs rtl). The icon direction
   * is configurable, but optional, as only a few icons should be flipped for rtl direction.
   */
  getFlipDirection(type) {
    return this.config?.flipDirection?.[type];
  }
  /**
   *
   * Returns the symbol class(es) for the icon type.
   */
  getStyleClasses(iconType) {
    return this.getSymbol(iconType) || "";
  }
  /**
   * Indicates whether the given `ICON_TYPE` is configured for
   * the given `IconResourceType`.
   */
  isResourceType(iconType, resourceType) {
    return this.config?.resources !== void 0 && !!this.config.resources.find((res) => res.types && res.type === resourceType && res.types.includes(iconType));
  }
  /**
   * Returns the path to the svg link. The link supports path names
   * as well, if the config a[[s been setup to support a svg file path.
   * Additionally, the icon prefix will be taken into account to prefix the
   * icon IDs in the SVG.
   */
  getSvgPath(iconType) {
    const svgResource = this.config?.resources?.find((res) => res.type === IconResourceType.SVG && res.types && res.types.includes(iconType));
    if (svgResource) {
      return svgResource.url ? `${svgResource.url}#${this.getSymbol(iconType)}` : `#${this.getSymbol(iconType)}`;
    }
  }
  /**
   * Loads the resource url (if any) for the given icon.
   * The icon will only be loaded once.
   *
   * NOTE: this is not working when the shadow is used as there's
   * no head element available and the link must be loaded for every
   * web component.
   */
  addLinkResource(iconType) {
    const resource = this.findResource(iconType, IconResourceType.LINK);
    if (resource?.url && !this.loadedResources.includes(resource.url)) {
      this.loadedResources.push(resource.url);
      const sanitizedUrl = this.sanitizer.sanitize(SecurityContext.URL, resource.url);
      if (sanitizedUrl) {
        const head = this.winRef.document.getElementsByTagName("head")[0];
        const link = this.winRef.document.createElement("link");
        link.rel = "stylesheet";
        link.type = "text/css";
        link.href = sanitizedUrl;
        head.appendChild(link);
      }
    }
  }
  findResource(iconType, resourceType) {
    if (!this.config?.resources) {
      return;
    }
    let resource = this.config.resources.find((res) => res.type === resourceType && res.types && res.types.includes(iconType));
    if (!resource) {
      resource = this.config.resources.find((res) => res.type === resourceType && !res.types || res.types && res.types.length === 0);
    }
    return resource;
  }
  getSymbol(iconType) {
    if (this.config && this.config.symbols && this.config.symbols[iconType]) {
      return this.config.symbols[iconType];
    }
  }
  get config() {
    return this.iconConfig.icon;
  }
  static {
    this.ɵfac = function IconLoaderService_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _IconLoaderService)(ɵɵinject(WindowRef), ɵɵinject(IconConfig), ɵɵinject(DomSanitizer));
    };
  }
  static {
    this.ɵprov = ɵɵdefineInjectable({
      token: _IconLoaderService,
      factory: _IconLoaderService.ɵfac,
      providedIn: "root"
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(IconLoaderService, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], () => [{
    type: WindowRef
  }, {
    type: IconConfig
  }, {
    type: DomSanitizer
  }], null);
})();
var DirectionMode;
(function(DirectionMode2) {
  DirectionMode2["LTR"] = "ltr";
  DirectionMode2["RTL"] = "rtl";
})(DirectionMode || (DirectionMode = {}));
var IconComponent = class _IconComponent {
  /**
   * The cxIcon directive is bound to the icon type. You can feed the `ICON_TYPE` to
   * accomplish a configurable button in the UI.
   */
  set cxIcon(type) {
    this.setIcon(type);
  }
  /**
   * The type input parameter is bound to the icon type. You can feed the `ICON_TYPE` to
   * accomplish a configurable button in the UI.
   */
  set type(type) {
    this.setIcon(type);
  }
  constructor(iconLoader, elementRef, renderer) {
    this.iconLoader = iconLoader;
    this.elementRef = elementRef;
    this.renderer = renderer;
  }
  setIcon(type) {
    if (!type || type === "") {
      return;
    }
    this.icon = this.iconLoader.getHtml(type);
    this.addStyleClasses(type);
    this.iconLoader.addLinkResource(type);
    this.flipIcon(type);
  }
  /**
   * The icons supports flipping for some icons to support rtl and ltr directions.
   */
  flipIcon(type) {
    if (!this.iconLoader.getFlipDirection) {
      return;
    }
    const iconDirection = this.iconLoader.getFlipDirection(type);
    this.flipAtLtr = iconDirection === DirectionMode.LTR;
    this.flipAtRtl = iconDirection === DirectionMode.RTL;
  }
  /**
   * Adds the style classes and the link resource (if available).
   */
  addStyleClasses(type) {
    this.renderer.addClass(this.host, "cx-icon");
    this.styleClasses?.forEach((cls) => this.renderer.removeClass(this.host, cls));
    this.styleClasses = this.iconLoader.getStyleClasses(type)?.split(" ");
    this.styleClasses?.forEach((cls) => {
      if (cls !== "") {
        this.renderer.addClass(this.host, cls);
      }
    });
  }
  get host() {
    return this.elementRef.nativeElement;
  }
  static {
    this.ɵfac = function IconComponent_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _IconComponent)(ɵɵdirectiveInject(IconLoaderService), ɵɵdirectiveInject(ElementRef), ɵɵdirectiveInject(Renderer2));
    };
  }
  static {
    this.ɵcmp = ɵɵdefineComponent({
      type: _IconComponent,
      selectors: [["cx-icon"], ["", "cxIcon", ""]],
      hostVars: 4,
      hostBindings: function IconComponent_HostBindings(rf, ctx) {
        if (rf & 2) {
          ɵɵclassProp("flip-at-rtl", ctx.flipAtRtl)("flip-at-ltr", ctx.flipAtLtr);
        }
      },
      inputs: {
        cxIcon: "cxIcon",
        type: "type"
      },
      standalone: false,
      ngContentSelectors: _c12,
      decls: 2,
      vars: 1,
      consts: [[4, "ngIf"], [3, "outerHTML"]],
      template: function IconComponent_Template(rf, ctx) {
        if (rf & 1) {
          ɵɵprojectionDef();
          ɵɵtemplate(0, IconComponent_ng_container_0_Template, 2, 1, "ng-container", 0);
          ɵɵprojection(1);
        }
        if (rf & 2) {
          ɵɵproperty("ngIf", ctx.icon);
        }
      },
      dependencies: [NgIf],
      encapsulation: 2
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(IconComponent, [{
    type: Component,
    args: [{
      selector: "cx-icon,[cxIcon]",
      standalone: false,
      template: '<ng-container *ngIf="icon">\n  <i [outerHTML]="icon"></i>\n</ng-container>\n<ng-content></ng-content>\n'
    }]
  }], () => [{
    type: IconLoaderService
  }, {
    type: ElementRef
  }, {
    type: Renderer2
  }], {
    cxIcon: [{
      type: Input
    }],
    type: [{
      type: Input
    }],
    flipAtRtl: [{
      type: HostBinding,
      args: ["class.flip-at-rtl"]
    }],
    flipAtLtr: [{
      type: HostBinding,
      args: ["class.flip-at-ltr"]
    }]
  });
})();
var defaultIconConfig = {
  icon: {
    flipDirection: {
      CARET_RIGHT: DirectionMode.RTL,
      CARET_LEFT: DirectionMode.RTL
    }
  }
};
var IconModule = class _IconModule {
  static {
    this.ɵfac = function IconModule_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _IconModule)();
    };
  }
  static {
    this.ɵmod = ɵɵdefineNgModule({
      type: _IconModule,
      declarations: [IconComponent],
      imports: [CommonModule],
      exports: [IconComponent]
    });
  }
  static {
    this.ɵinj = ɵɵdefineInjector({
      providers: [
        provideDefaultConfig(defaultIconConfig),
        // TODO: move the opinionated fontawesome config to a recipe
        provideDefaultConfig(fontawesomeIconConfig)
      ],
      imports: [CommonModule]
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(IconModule, [{
    type: NgModule,
    args: [{
      declarations: [IconComponent],
      imports: [CommonModule],
      providers: [
        provideDefaultConfig(defaultIconConfig),
        // TODO: move the opinionated fontawesome config to a recipe
        provideDefaultConfig(fontawesomeIconConfig)
      ],
      exports: [IconComponent]
    }]
  }], null, null);
})();
var MockIconComponent = class _MockIconComponent {
  static {
    this.ɵfac = function MockIconComponent_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _MockIconComponent)();
    };
  }
  static {
    this.ɵcmp = ɵɵdefineComponent({
      type: _MockIconComponent,
      selectors: [["cx-icon"], ["", "cxIcon", ""]],
      inputs: {
        cxIcon: "cxIcon",
        type: "type"
      },
      standalone: false,
      decls: 1,
      vars: 1,
      template: function MockIconComponent_Template(rf, ctx) {
        if (rf & 1) {
          ɵɵtext(0);
        }
        if (rf & 2) {
          ɵɵtextInterpolate(ctx.type || ctx.cxIcon);
        }
      },
      encapsulation: 2
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(MockIconComponent, [{
    type: Component,
    args: [{
      selector: "cx-icon,[cxIcon]",
      template: `{{ type || cxIcon }}`,
      standalone: false
    }]
  }], null, {
    cxIcon: [{
      type: Input
    }],
    type: [{
      type: Input
    }]
  });
})();
var mockComponents = [MockIconComponent];
var MockIconLoaderService = class {
  getHtml() {
  }
  getStyleClasses() {
  }
  addStyleClasses() {
  }
  addLinkResource() {
  }
};
var IconTestingModule = class _IconTestingModule {
  static {
    this.ɵfac = function IconTestingModule_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _IconTestingModule)();
    };
  }
  static {
    this.ɵmod = ɵɵdefineNgModule({
      type: _IconTestingModule,
      declarations: [MockIconComponent],
      exports: [MockIconComponent]
    });
  }
  static {
    this.ɵinj = ɵɵdefineInjector({
      providers: [{
        provide: IconLoaderService,
        useClass: MockIconLoaderService
      }]
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(IconTestingModule, [{
    type: NgModule,
    args: [{
      declarations: mockComponents,
      exports: mockComponents,
      providers: [{
        provide: IconLoaderService,
        useClass: MockIconLoaderService
      }]
    }]
  }], null, null);
})();
var SpinnerComponent = class _SpinnerComponent {
  constructor() {
  }
  static {
    this.ɵfac = function SpinnerComponent_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _SpinnerComponent)();
    };
  }
  static {
    this.ɵcmp = ɵɵdefineComponent({
      type: _SpinnerComponent,
      selectors: [["cx-spinner"]],
      standalone: false,
      decls: 4,
      vars: 3,
      consts: [["role", "status", 1, "loader-container"], [1, "loader"]],
      template: function SpinnerComponent_Template(rf, ctx) {
        if (rf & 1) {
          ɵɵelementStart(0, "div", 0)(1, "div", 1);
          ɵɵtext(2);
          ɵɵpipe(3, "cxTranslate");
          ɵɵelementEnd()();
        }
        if (rf & 2) {
          ɵɵadvance(2);
          ɵɵtextInterpolate(ɵɵpipeBind1(3, 1, "spinner.loading"));
        }
      },
      dependencies: [TranslatePipe],
      encapsulation: 2
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(SpinnerComponent, [{
    type: Component,
    args: [{
      selector: "cx-spinner",
      standalone: false,
      template: `<div class="loader-container" role="status">
  <div class="loader">{{ 'spinner.loading' | cxTranslate }}</div>
</div>
`
    }]
  }], () => [], null);
})();
var ConsentManagementFormComponent = class _ConsentManagementFormComponent {
  constructor() {
    this.consentGiven = false;
    this.requiredConsents = [];
    this.disabled = false;
    this.showMandatory = false;
    this.consentChanged = new EventEmitter();
    this.featureConfigService = inject(FeatureConfigService, {
      optional: true
    });
  }
  ngOnInit() {
    this.updateConsentGiven();
  }
  ngOnChanges(changes) {
    if (this.featureConfigService?.isEnabled("updateConsentGivenInOnChanges") && (changes.consent || changes.consentTemplate)) {
      this.updateConsentGiven();
    }
  }
  onConsentChange() {
    this.consentGiven = !this.consentGiven;
    this.consentChanged.emit({
      given: this.consentGiven,
      template: this.consentTemplate
    });
  }
  isRequired(templateId) {
    return templateId ? this.requiredConsents.includes(templateId) : false;
  }
  updateConsentGiven() {
    if (this.consent) {
      this.consentGiven = Boolean(this.consent.consentState === ANONYMOUS_CONSENT_STATUS.GIVEN);
    } else {
      if (this.consentTemplate && this.consentTemplate.currentConsent) {
        if (this.consentTemplate.currentConsent.consentWithdrawnDate) {
          this.consentGiven = false;
        } else if (this.consentTemplate.currentConsent.consentGivenDate) {
          this.consentGiven = true;
        }
      }
    }
  }
  static {
    this.ɵfac = function ConsentManagementFormComponent_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _ConsentManagementFormComponent)();
    };
  }
  static {
    this.ɵcmp = ɵɵdefineComponent({
      type: _ConsentManagementFormComponent,
      selectors: [["cx-consent-management-form"]],
      inputs: {
        consentTemplate: "consentTemplate",
        requiredConsents: "requiredConsents",
        consent: "consent",
        disabled: "disabled",
        showMandatory: "showMandatory"
      },
      outputs: {
        consentChanged: "consentChanged"
      },
      standalone: false,
      features: [ɵɵNgOnChangesFeature],
      decls: 13,
      vars: 8,
      consts: [["requiredAsterisk", ""], [1, "form-check"], ["type", "checkbox", 1, "form-check-input", 3, "change", "checked", "name", "disabled"], [1, "form-check-label", "cx-be-bold"], [4, "ngIf"], [1, "form-check-label"], [3, "ngTemplateOutlet"], ["aria-hidden", "true", 1, "text-decoration-none", "required-asterisk", 3, "title"]],
      template: function ConsentManagementFormComponent_Template(rf, ctx) {
        if (rf & 1) {
          const _r1 = ɵɵgetCurrentView();
          ɵɵelementStart(0, "div", 1)(1, "label")(2, "input", 2);
          ɵɵlistener("change", function ConsentManagementFormComponent_Template_input_change_2_listener() {
            ɵɵrestoreView(_r1);
            return ɵɵresetView(ctx.onConsentChange());
          });
          ɵɵelementEnd();
          ɵɵelementStart(3, "span", 3);
          ɵɵtemplate(4, ConsentManagementFormComponent_ng_container_4_Template, 3, 1, "ng-container", 4);
          ɵɵelementEnd();
          ɵɵelementStart(5, "span", 5);
          ɵɵtext(6);
          ɵɵtemplate(7, ConsentManagementFormComponent_ng_template_7_Template, 0, 0, "ng-template", 6);
          ɵɵelementEnd()();
          ɵɵelementStart(8, "span", 5);
          ɵɵtext(9);
          ɵɵtemplate(10, ConsentManagementFormComponent_ng_template_10_Template, 0, 0, "ng-template", 6);
          ɵɵelementEnd()();
          ɵɵtemplate(11, ConsentManagementFormComponent_ng_template_11_Template, 1, 1, "ng-template", null, 0, ɵɵtemplateRefExtractor);
        }
        if (rf & 2) {
          const requiredAsterisk_r3 = ɵɵreference(12);
          ɵɵadvance(2);
          ɵɵproperty("checked", ctx.consentGiven)("name", ctx.consentTemplate.id)("disabled", ctx.disabled || ctx.isRequired(ctx.consentTemplate == null ? null : ctx.consentTemplate.id));
          ɵɵadvance(2);
          ɵɵproperty("ngIf", ctx.consentTemplate == null ? null : ctx.consentTemplate.name);
          ɵɵadvance(2);
          ɵɵtextInterpolate1(" ", ctx.consentTemplate == null ? null : ctx.consentTemplate.description, " ");
          ɵɵadvance();
          ɵɵproperty("ngTemplateOutlet", requiredAsterisk_r3);
          ɵɵadvance(2);
          ɵɵtextInterpolate1(" ", ctx.consentTemplate == null ? null : ctx.consentTemplate.description, " ");
          ɵɵadvance();
          ɵɵproperty("ngTemplateOutlet", requiredAsterisk_r3);
        }
      },
      dependencies: [NgIf, NgTemplateOutlet, TranslatePipe],
      encapsulation: 2
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(ConsentManagementFormComponent, [{
    type: Component,
    args: [{
      selector: "cx-consent-management-form",
      standalone: false,
      template: `<div class="form-check">
  <label>
    <input
      type="checkbox"
      class="form-check-input"
      (change)="onConsentChange()"
      [checked]="consentGiven"
      [name]="consentTemplate.id"
      [disabled]="disabled || isRequired(consentTemplate?.id)"
    />
    <span class="form-check-label cx-be-bold">
      <ng-container *ngIf="consentTemplate?.name">
        {{ consentTemplate?.name }}
        <br />
      </ng-container>
    </span>
    <span class="form-check-label">
      {{ consentTemplate?.description }}
      <ng-template [ngTemplateOutlet]="requiredAsterisk"></ng-template>
    </span>
  </label>
  <span class="form-check-label">
    {{ consentTemplate?.description }}
    <ng-template [ngTemplateOutlet]="requiredAsterisk"></ng-template>
  </span>
</div>

<ng-template #requiredAsterisk>
  <ng-container *ngIf="showMandatory">
    <abbr
      aria-hidden="true"
      class="text-decoration-none required-asterisk"
      title="{{ 'common.required' | cxTranslate }}"
      >*</abbr
    >
  </ng-container>
</ng-template>
`
    }]
  }], () => [], {
    consentTemplate: [{
      type: Input
    }],
    requiredConsents: [{
      type: Input
    }],
    consent: [{
      type: Input
    }],
    disabled: [{
      type: Input
    }],
    showMandatory: [{
      type: Input
    }],
    consentChanged: [{
      type: Output
    }]
  });
})();
var AtMessageDirective = class _AtMessageDirective {
  constructor(elementRef, templateRef, globalMessageService) {
    this.elementRef = elementRef;
    this.templateRef = templateRef;
    this.globalMessageService = globalMessageService;
  }
  get host() {
    return !!this.templateRef ? this.templateRef.elementRef.nativeElement.parentElement : this.elementRef.nativeElement;
  }
  /**
   * Emit assistive global meesage to improve screen reader vocalization.
   * @param event
   */
  handleClick(event) {
    event?.preventDefault();
    if (event?.target === this.host && this.cxAtMessage) {
      const message = Array.isArray(this.cxAtMessage) ? this.cxAtMessage.join("\n") : this.cxAtMessage;
      this.globalMessageService.get().pipe(take(1)).subscribe((globalMessageEntities) => {
        if (globalMessageEntities[GlobalMessageType.MSG_TYPE_ASSISTIVE]) {
          this.globalMessageService.remove(GlobalMessageType.MSG_TYPE_ASSISTIVE);
        }
        this.globalMessageService.add(message, GlobalMessageType.MSG_TYPE_ASSISTIVE);
      });
    }
  }
  static {
    this.ɵfac = function AtMessageDirective_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _AtMessageDirective)(ɵɵdirectiveInject(ElementRef), ɵɵdirectiveInject(TemplateRef, 8), ɵɵdirectiveInject(GlobalMessageService));
    };
  }
  static {
    this.ɵdir = ɵɵdefineDirective({
      type: _AtMessageDirective,
      selectors: [["", "cxAtMessage", ""]],
      hostBindings: function AtMessageDirective_HostBindings(rf, ctx) {
        if (rf & 1) {
          ɵɵlistener("click", function AtMessageDirective_click_HostBindingHandler($event) {
            return ctx.handleClick($event);
          });
        }
      },
      inputs: {
        cxAtMessage: "cxAtMessage"
      },
      standalone: false
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(AtMessageDirective, [{
    type: Directive,
    args: [{
      selector: "[cxAtMessage]",
      standalone: false
    }]
  }], () => [{
    type: ElementRef
  }, {
    type: TemplateRef,
    decorators: [{
      type: Optional
    }]
  }, {
    type: GlobalMessageService
  }], {
    cxAtMessage: [{
      type: Input
    }],
    handleClick: [{
      type: HostListener,
      args: ["click", ["$event"]]
    }]
  });
})();
var MessageComponent = class _MessageComponent {
  constructor() {
    this.showBody = false;
    this.isVisibleCloseButton = true;
    this.closeMessage = new EventEmitter();
    this.buttonAction = new EventEmitter();
    this.iconTypes = ICON_TYPE;
  }
  get getCssClassesForMessage() {
    return {
      "cx-message-success": this.type === GlobalMessageType.MSG_TYPE_CONFIRMATION,
      "cx-message-info": this.type === GlobalMessageType.MSG_TYPE_INFO,
      "cx-message-warning": this.type === GlobalMessageType.MSG_TYPE_WARNING,
      "cx-message-danger": this.type === GlobalMessageType.MSG_TYPE_ERROR
    };
  }
  get getIconType() {
    switch (this.type) {
      case GlobalMessageType.MSG_TYPE_WARNING:
        return ICON_TYPE.WARNING;
      case GlobalMessageType.MSG_TYPE_ERROR:
        return ICON_TYPE.ERROR;
      case GlobalMessageType.MSG_TYPE_INFO:
        return ICON_TYPE.INFO;
      default:
        return ICON_TYPE.SUCCESS;
    }
  }
  ngAfterViewInit() {
    if (this.accordionText || this.actionButtonText) {
      this.messageContainer?.nativeElement.focus();
    }
  }
  static {
    this.ɵfac = function MessageComponent_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _MessageComponent)();
    };
  }
  static {
    this.ɵcmp = ɵɵdefineComponent({
      type: _MessageComponent,
      selectors: [["cx-message"]],
      viewQuery: function MessageComponent_Query(rf, ctx) {
        if (rf & 1) {
          ɵɵviewQuery(_c22, 5);
        }
        if (rf & 2) {
          let _t;
          ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.messageContainer = _t.first);
        }
      },
      inputs: {
        text: "text",
        actionButtonText: "actionButtonText",
        actionButtonMessage: "actionButtonMessage",
        accordionText: "accordionText",
        showBody: "showBody",
        isVisibleCloseButton: "isVisibleCloseButton",
        type: "type"
      },
      outputs: {
        closeMessage: "closeMessage",
        buttonAction: "buttonAction"
      },
      standalone: false,
      ngContentSelectors: _c12,
      decls: 12,
      vars: 7,
      consts: [["messageContainer", ""], ["tabindex", "0", 1, "cx-message", 3, "ngClass"], [1, "cx-message-content"], ["role", "presentation", 1, "cx-message-header"], ["role", "presentation", 1, "cx-message-icon"], ["role", "presentation", 3, "type"], [1, "cx-message-text"], ["class", "cx-message-accordion-button link cx-action-link", "type", "button", 3, "click", 4, "ngIf"], ["class", "btn btn-link cx-action-link", "type", "button", 3, "cxAtMessage", "click", 4, "ngIf"], ["class", "close", "type", "button", 3, "cxAtMessage", "title", "click", 4, "ngIf"], ["class", "cx-message-body", 4, "ngIf"], ["type", "button", 1, "cx-message-accordion-button", "link", "cx-action-link", 3, "click"], [1, "cx-message-accordion-icon", 3, "type"], ["type", "button", 1, "btn", "btn-link", "cx-action-link", 3, "click", "cxAtMessage"], ["type", "button", 1, "close", 3, "click", "cxAtMessage", "title"], [3, "type"], [1, "cx-message-body"]],
      template: function MessageComponent_Template(rf, ctx) {
        if (rf & 1) {
          ɵɵprojectionDef();
          ɵɵelementStart(0, "div", 1, 0)(2, "div", 2)(3, "div", 3)(4, "span", 4);
          ɵɵelement(5, "cx-icon", 5);
          ɵɵelementEnd();
          ɵɵelementStart(6, "span", 6);
          ɵɵtext(7);
          ɵɵelementEnd();
          ɵɵtemplate(8, MessageComponent_button_8_Template, 3, 4, "button", 7)(9, MessageComponent_button_9_Template, 2, 2, "button", 8)(10, MessageComponent_button_10_Template, 5, 10, "button", 9);
          ɵɵelementEnd();
          ɵɵtemplate(11, MessageComponent_div_11_Template, 2, 0, "div", 10);
          ɵɵelementEnd()();
        }
        if (rf & 2) {
          ɵɵproperty("ngClass", ctx.getCssClassesForMessage);
          ɵɵadvance(5);
          ɵɵproperty("type", ctx.getIconType);
          ɵɵadvance(2);
          ɵɵtextInterpolate1(" ", ctx.text, " ");
          ɵɵadvance();
          ɵɵproperty("ngIf", ctx.accordionText);
          ɵɵadvance();
          ɵɵproperty("ngIf", ctx.actionButtonText);
          ɵɵadvance();
          ɵɵproperty("ngIf", ctx.isVisibleCloseButton);
          ɵɵadvance();
          ɵɵproperty("ngIf", ctx.showBody || !ctx.accordionText);
        }
      },
      dependencies: [AtMessageDirective, NgClass, NgIf, IconComponent, TranslatePipe],
      encapsulation: 2
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(MessageComponent, [{
    type: Component,
    args: [{
      selector: "cx-message",
      standalone: false,
      template: `<div
  #messageContainer
  [ngClass]="getCssClassesForMessage"
  class="cx-message"
  tabindex="0"
>
  <div class="cx-message-content">
    <div role="presentation" class="cx-message-header">
      <span role="presentation" class="cx-message-icon">
        <cx-icon role="presentation" [type]="getIconType"></cx-icon>
      </span>

      <span class="cx-message-text">
        {{ text }}
      </span>

      <button
        *ngIf="accordionText"
        (click)="showBody = !showBody"
        [attr.aria-expanded]="showBody"
        [attr.aria-label]="accordionText"
        class="cx-message-accordion-button link cx-action-link"
        type="button"
      >
        {{ accordionText }}
        <cx-icon
          [type]="showBody ? 'CARET_UP' : 'CARET_DOWN'"
          class="cx-message-accordion-icon"
        ></cx-icon>
      </button>

      <button
        *ngIf="actionButtonText"
        (click)="buttonAction.emit()"
        [cxAtMessage]="actionButtonMessage"
        class="btn btn-link cx-action-link"
        type="button"
      >
        {{ actionButtonText }}
      </button>

      <button
        *ngIf="isVisibleCloseButton"
        (click)="closeMessage.emit()"
        [attr.aria-label]="'common.close' | cxTranslate"
        [cxAtMessage]="'common.close' | cxTranslate"
        title="{{ 'common.close' | cxTranslate }}"
        class="close"
        type="button"
      >
        <cx-icon [type]="iconTypes.CLOSE"></cx-icon>
      </button>
    </div>

    <div *ngIf="showBody || !accordionText" class="cx-message-body">
      <ng-content></ng-content>
    </div>
  </div>
</div>
`
    }]
  }], () => [], {
    text: [{
      type: Input
    }],
    actionButtonText: [{
      type: Input
    }],
    actionButtonMessage: [{
      type: Input
    }],
    accordionText: [{
      type: Input
    }],
    showBody: [{
      type: Input
    }],
    isVisibleCloseButton: [{
      type: Input
    }],
    type: [{
      type: Input
    }],
    closeMessage: [{
      type: Output
    }],
    buttonAction: [{
      type: Output
    }],
    messageContainer: [{
      type: ViewChild,
      args: ["messageContainer"]
    }]
  });
})();
var AnonymousConsentDialogComponent = class _AnonymousConsentDialogComponent {
  handleClick(event) {
    if (event.target.tagName === this.el.nativeElement.tagName) {
      this.close("Cross click");
    }
  }
  constructor(config, anonymousConsentsService, el, launchDialogService) {
    this.config = config;
    this.anonymousConsentsService = anonymousConsentsService;
    this.el = el;
    this.launchDialogService = launchDialogService;
    this.subscriptions = new Subscription();
    this.featureConfigService = inject(FeatureConfigService);
    this.showLegalDescription = true;
    this.iconTypes = ICON_TYPE;
    this.requiredConsents = [];
    this.focusConfig = {
      trap: true,
      block: true,
      autofocus: 'input[type="checkbox"]',
      focusOnEscape: true
    };
    this.globalMessageService = inject(GlobalMessageService, {
      optional: true
    });
    this.globalMessageType = GlobalMessageType;
    this.message$ = new Subject();
    if (this.config.anonymousConsents) {
      this.showLegalDescription = this.config.anonymousConsents.showLegalDescriptionInDialog;
      if (this.config.anonymousConsents.requiredConsents) {
        this.requiredConsents = this.config.anonymousConsents.requiredConsents;
      }
    }
    useFeatureStyles("a11yAnonymousConsentMessageInDialog");
  }
  ngOnInit() {
    this.templates$ = this.anonymousConsentsService.getTemplates();
    this.consents$ = this.anonymousConsentsService.getConsents();
    this.loading$ = this.anonymousConsentsService.getLoadTemplatesLoading();
  }
  close(reason) {
    this.launchDialogService.closeDialog(reason);
  }
  rejectAll() {
    this.subscriptions.add(combineLatest([this.templates$, this.consents$]).pipe(take(1), distinctUntilChanged(), tap(([templates, consents]) => templates.forEach((template) => {
      const consent = this.getCorrespondingConsent(template, consents);
      if (consent && this.anonymousConsentsService.isConsentGiven(consent)) {
        if (this.isRequiredConsent(template)) {
          return;
        }
        if (template.id) {
          this.anonymousConsentsService.withdrawConsent(template.id);
        }
      }
    }))).subscribe(() => this.onConsentWithdrawnSuccess()));
    if (!this.featureConfigService.isEnabled("a11yAnonymousConsentMessageInDialog")) {
      this.close("rejectAll");
    }
  }
  allowAll() {
    this.subscriptions.add(combineLatest([this.templates$, this.consents$]).pipe(take(1), distinctUntilChanged(), tap(([templates, consents]) => templates.forEach((template) => {
      const consent = this.getCorrespondingConsent(template, consents);
      if (consent && (consent.consentState == null || this.anonymousConsentsService.isConsentWithdrawn(consent))) {
        if (this.isRequiredConsent(template)) {
          return;
        }
        if (template.id) {
          this.anonymousConsentsService.giveConsent(template.id);
        }
      }
    }))).subscribe(() => this.onConsentGivenSuccess()));
    if (!this.featureConfigService.isEnabled("a11yAnonymousConsentMessageInDialog")) {
      this.close("allowAll");
    }
  }
  isRequiredConsent(template) {
    return Boolean(template.id && this.config.anonymousConsents?.requiredConsents && this.config.anonymousConsents.requiredConsents.includes(template.id));
  }
  onConsentChange({
    given,
    template
  }) {
    if (template.id) {
      if (given) {
        this.anonymousConsentsService.giveConsent(template.id);
        this.onConsentGivenSuccess();
      } else {
        this.anonymousConsentsService.withdrawConsent(template.id);
        this.onConsentWithdrawnSuccess();
      }
    }
  }
  getCorrespondingConsent(template, consents = []) {
    for (const consent of consents) {
      if (template.id === consent.templateCode) {
        return consent;
      }
    }
    return null;
  }
  onConsentGivenSuccess() {
    if (this.featureConfigService.isEnabled("a11yAnonymousConsentMessageInDialog")) {
      this.message$.next({
        type: GlobalMessageType.MSG_TYPE_CONFIRMATION,
        key: "consentManagementForm.message.success.given"
      });
    } else {
      this.globalMessageService?.add({
        key: "consentManagementForm.message.success.given"
      }, GlobalMessageType.MSG_TYPE_CONFIRMATION);
    }
  }
  onConsentWithdrawnSuccess() {
    if (this.featureConfigService.isEnabled("a11yAnonymousConsentMessageInDialog")) {
      this.message$.next({
        type: GlobalMessageType.MSG_TYPE_CONFIRMATION,
        key: "consentManagementForm.message.success.withdrawn"
      });
    } else {
      this.globalMessageService?.add({
        key: "consentManagementForm.message.success.withdrawn"
      }, GlobalMessageType.MSG_TYPE_CONFIRMATION);
    }
  }
  closeMessage() {
    this.message$.next(null);
  }
  ngOnDestroy() {
    this.subscriptions.unsubscribe();
  }
  static {
    this.ɵfac = function AnonymousConsentDialogComponent_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _AnonymousConsentDialogComponent)(ɵɵdirectiveInject(AnonymousConsentsConfig), ɵɵdirectiveInject(AnonymousConsentsService), ɵɵdirectiveInject(ElementRef), ɵɵdirectiveInject(LaunchDialogService));
    };
  }
  static {
    this.ɵcmp = ɵɵdefineComponent({
      type: _AnonymousConsentDialogComponent,
      selectors: [["cx-anonymous-consent-dialog"]],
      hostBindings: function AnonymousConsentDialogComponent_HostBindings(rf, ctx) {
        if (rf & 1) {
          ɵɵlistener("click", function AnonymousConsentDialogComponent_click_HostBindingHandler($event) {
            return ctx.handleClick($event);
          });
        }
      },
      standalone: false,
      decls: 6,
      vars: 5,
      consts: [["dialogBody", ""], ["role", "dialog", "aria-labelledby", "dialogTitle", 1, "modal-dialog", "modal-dialog-centered", "modal-lg", "cx-anonymous-consent-dialog", 3, "esc", "cxFocus"], [1, "modal-content", "cx-dialog-content"], [4, "ngIf", "ngIfElse"], [1, "modal-header", "cx-dialog-header"], ["id", "dialogTitle"], ["type", "button", 1, "close", 3, "click", "title"], ["aria-hidden", "true"], [3, "type"], ["class", "cx-dialog-description", 4, "ngIf"], [4, "cxFeature"], [1, "cx-dialog-buttons"], [1, "btn", "btn-link", "cx-action-link", 3, "click"], ["class", "modal-body cx-dialog-body", 4, "ngIf"], [1, "cx-dialog-description"], [1, "cx-dialog-separator", "col-sm-12", "d-xs-block", "d-sm-block", "d-md-none"], ["aria-live", "assertive", "aria-atomic", "true", 1, "cx-dialog-message"], [3, "text", "type", "isVisibleCloseButton", "cxFocus", "closeMessage", 4, "ngIf"], [3, "closeMessage", "text", "type", "isVisibleCloseButton", "cxFocus"], [1, "modal-body", "cx-dialog-body"], [4, "ngIf"], ["class", "cx-dialog-row col-sm-12 col-md-6", 4, "ngFor", "ngForOf"], [1, "cx-dialog-row", "col-sm-12", "col-md-6"], [3, "consentChanged", "consentTemplate", "requiredConsents", "consent"]],
      template: function AnonymousConsentDialogComponent_Template(rf, ctx) {
        if (rf & 1) {
          const _r1 = ɵɵgetCurrentView();
          ɵɵelementStart(0, "div", 1);
          ɵɵlistener("esc", function AnonymousConsentDialogComponent_Template_div_esc_0_listener() {
            ɵɵrestoreView(_r1);
            return ɵɵresetView(ctx.close("Escape clicked"));
          });
          ɵɵelementStart(1, "div", 2);
          ɵɵtemplate(2, AnonymousConsentDialogComponent_div_2_Template, 2, 0, "div", 3);
          ɵɵpipe(3, "async");
          ɵɵtemplate(4, AnonymousConsentDialogComponent_ng_template_4_Template, 20, 21, "ng-template", null, 0, ɵɵtemplateRefExtractor);
          ɵɵelementEnd()();
        }
        if (rf & 2) {
          const dialogBody_r10 = ɵɵreference(5);
          ɵɵproperty("cxFocus", ctx.focusConfig);
          ɵɵadvance(2);
          ɵɵproperty("ngIf", ɵɵpipeBind1(3, 3, ctx.loading$))("ngIfElse", dialogBody_r10);
        }
      },
      dependencies: [NgForOf, NgIf, IconComponent, SpinnerComponent, ConsentManagementFormComponent, FocusDirective, FeatureDirective, MessageComponent, AsyncPipe, TranslatePipe],
      encapsulation: 2
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(AnonymousConsentDialogComponent, [{
    type: Component,
    args: [{
      selector: "cx-anonymous-consent-dialog",
      standalone: false,
      template: `<div
  class="modal-dialog modal-dialog-centered modal-lg cx-anonymous-consent-dialog"
  role="dialog"
  [cxFocus]="focusConfig"
  (esc)="close('Escape clicked')"
  aria-labelledby="dialogTitle"
>
  <div class="modal-content cx-dialog-content">
    <div *ngIf="loading$ | async; else dialogBody">
      <cx-spinner></cx-spinner>
    </div>
    <ng-template #dialogBody>
      <div class="modal-header cx-dialog-header">
        <h3 id="dialogTitle">
          {{ 'anonymousConsents.dialog.title' | cxTranslate }}
        </h3>
        <button
          type="button"
          class="close"
          title="{{ 'common.close' | cxTranslate }}"
          [attr.aria-label]="'common.close' | cxTranslate"
          (click)="close('Cross click')"
        >
          <span aria-hidden="true">
            <cx-icon [type]="iconTypes.CLOSE"></cx-icon>
          </span>
        </button>
      </div>
      <!-- Separator -->
      <div class="cx-dialog-description" *ngIf="showLegalDescription">
        {{ 'anonymousConsents.dialog.legalDescription' | cxTranslate }}
        <div
          class="cx-dialog-separator col-sm-12 d-xs-block d-sm-block d-md-none"
        ></div>
      </div>

      <ng-container *cxFeature="'a11yAnonymousConsentMessageInDialog'">
        <div class="cx-dialog-message" aria-live="assertive" aria-atomic="true">
          <cx-message
            *ngIf="message$ | async as message"
            [text]="message.key | cxTranslate"
            [type]="message.type"
            [isVisibleCloseButton]="true"
            (closeMessage)="closeMessage()"
            [cxFocus]="{ autofocus: '.cx-message' }"
          ></cx-message>
        </div>
      </ng-container>
      <!-- Actions -->
      <div class="cx-dialog-buttons">
        <button class="btn btn-link cx-action-link" (click)="rejectAll()">
          {{ 'anonymousConsents.dialog.clearAll' | cxTranslate }}
        </button>
        <button class="btn btn-link cx-action-link" (click)="allowAll()">
          {{ 'anonymousConsents.dialog.selectAll' | cxTranslate }}
        </button>
      </div>
      <!-- Modal Body -->
      <div
        class="modal-body cx-dialog-body"
        *ngIf="templates$ | async as templates"
      >
        <ng-container *ngIf="consents$ | async as consents">
          <div
            class="cx-dialog-row col-sm-12 col-md-6"
            *ngFor="let template of templates"
          >
            <cx-consent-management-form
              [consentTemplate]="template"
              [requiredConsents]="requiredConsents"
              [consent]="getCorrespondingConsent(template, consents)"
              (consentChanged)="onConsentChange($event)"
            ></cx-consent-management-form>
          </div>
        </ng-container>
      </div>
    </ng-template>
  </div>
</div>
`
    }]
  }], () => [{
    type: AnonymousConsentsConfig
  }, {
    type: AnonymousConsentsService
  }, {
    type: ElementRef
  }, {
    type: LaunchDialogService
  }], {
    globalMessageService: [{
      type: Optional
    }],
    handleClick: [{
      type: HostListener,
      args: ["click", ["$event"]]
    }]
  });
})();
var defaultAnonymousConsentLayoutConfig = {
  launch: {
    ANONYMOUS_CONSENT: {
      inlineRoot: true,
      component: AnonymousConsentDialogComponent,
      dialogType: DIALOG_TYPE.DIALOG
    }
  }
};
var AnonymousConsentOpenDialogComponent = class _AnonymousConsentOpenDialogComponent {
  constructor(vcr, anonymousConsentsService, launchDialogService) {
    this.vcr = vcr;
    this.anonymousConsentsService = anonymousConsentsService;
    this.launchDialogService = launchDialogService;
    this.bannerVisible$ = this.anonymousConsentsService.isBannerVisible();
    useFeatureStyles("a11yHideConsentButtonWhenBannerVisible");
  }
  openDialog() {
    const dialog = this.launchDialogService.openDialog(LAUNCH_CALLER.ANONYMOUS_CONSENT, this.openElement, this.vcr);
    if (dialog) {
      dialog.pipe(take(1)).subscribe();
    }
  }
  static {
    this.ɵfac = function AnonymousConsentOpenDialogComponent_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _AnonymousConsentOpenDialogComponent)(ɵɵdirectiveInject(ViewContainerRef), ɵɵdirectiveInject(AnonymousConsentsService), ɵɵdirectiveInject(LaunchDialogService));
    };
  }
  static {
    this.ɵcmp = ɵɵdefineComponent({
      type: _AnonymousConsentOpenDialogComponent,
      selectors: [["cx-anonymous-consent-open-dialog"]],
      viewQuery: function AnonymousConsentOpenDialogComponent_Query(rf, ctx) {
        if (rf & 1) {
          ɵɵviewQuery(_c42, 5);
        }
        if (rf & 2) {
          let _t;
          ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.openElement = _t.first);
        }
      },
      standalone: false,
      decls: 2,
      vars: 2,
      consts: [["open", ""], [4, "cxFeature"], [1, "btn", "btn-link", 3, "click"], ["class", "btn btn-link", 3, "click", 4, "ngIf"]],
      template: function AnonymousConsentOpenDialogComponent_Template(rf, ctx) {
        if (rf & 1) {
          ɵɵtemplate(0, AnonymousConsentOpenDialogComponent_ng_container_0_Template, 5, 3, "ng-container", 1)(1, AnonymousConsentOpenDialogComponent_ng_container_1_Template, 3, 3, "ng-container", 1);
        }
        if (rf & 2) {
          ɵɵproperty("cxFeature", "!a11yHideConsentButtonWhenBannerVisible");
          ɵɵadvance();
          ɵɵproperty("cxFeature", "a11yHideConsentButtonWhenBannerVisible");
        }
      },
      dependencies: [NgIf, FeatureDirective, AsyncPipe, TranslatePipe],
      encapsulation: 2
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(AnonymousConsentOpenDialogComponent, [{
    type: Component,
    args: [{
      selector: "cx-anonymous-consent-open-dialog",
      standalone: false,
      template: `<ng-container *cxFeature="'!a11yHideConsentButtonWhenBannerVisible'">
  <button #open class="btn btn-link" (click)="openDialog()">
    {{ 'anonymousConsents.dialog.title' | cxTranslate }}
  </button>
</ng-container>

<ng-container *cxFeature="'a11yHideConsentButtonWhenBannerVisible'">
  <button
    *ngIf="!(bannerVisible$ | async)"
    #open
    class="btn btn-link"
    (click)="openDialog()"
  >
    {{ 'anonymousConsents.dialog.title' | cxTranslate }}
  </button>
</ng-container>
`
    }]
  }], () => [{
    type: ViewContainerRef
  }, {
    type: AnonymousConsentsService
  }, {
    type: LaunchDialogService
  }], {
    openElement: [{
      type: ViewChild,
      args: ["open"]
    }]
  });
})();
var AnonymousConsentManagementBannerModule = class _AnonymousConsentManagementBannerModule {
  static {
    this.ɵfac = function AnonymousConsentManagementBannerModule_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _AnonymousConsentManagementBannerModule)();
    };
  }
  static {
    this.ɵmod = ɵɵdefineNgModule({
      type: _AnonymousConsentManagementBannerModule,
      declarations: [AnonymousConsentManagementBannerComponent, AnonymousConsentOpenDialogComponent],
      imports: [CommonModule, I18nModule, KeyboardFocusModule, FeaturesConfigModule],
      exports: [AnonymousConsentManagementBannerComponent, AnonymousConsentOpenDialogComponent]
    });
  }
  static {
    this.ɵinj = ɵɵdefineInjector({
      providers: [provideDefaultConfig(defaultAnonymousConsentLayoutConfig), provideDefaultConfig({
        cmsComponents: {
          AnonymousConsentManagementBannerComponent: {
            component: AnonymousConsentManagementBannerComponent,
            deferLoading: DeferLoadingStrategy.INSTANT
          },
          AnonymousConsentOpenDialogComponent: {
            component: AnonymousConsentOpenDialogComponent
          }
        }
      })],
      imports: [CommonModule, I18nModule, KeyboardFocusModule, FeaturesConfigModule]
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(AnonymousConsentManagementBannerModule, [{
    type: NgModule,
    args: [{
      imports: [CommonModule, I18nModule, KeyboardFocusModule, FeaturesConfigModule],
      providers: [provideDefaultConfig(defaultAnonymousConsentLayoutConfig), provideDefaultConfig({
        cmsComponents: {
          AnonymousConsentManagementBannerComponent: {
            component: AnonymousConsentManagementBannerComponent,
            deferLoading: DeferLoadingStrategy.INSTANT
          },
          AnonymousConsentOpenDialogComponent: {
            component: AnonymousConsentOpenDialogComponent
          }
        }
      })],
      declarations: [AnonymousConsentManagementBannerComponent, AnonymousConsentOpenDialogComponent],
      exports: [AnonymousConsentManagementBannerComponent, AnonymousConsentOpenDialogComponent]
    }]
  }], null, null);
})();
var defaultLcpCmsComponentsConfig = {
  lcpCmsComponents: {
    idMarker: "__cxLCP__",
    ids: []
  }
};
var CmsLcpModule = class _CmsLcpModule {
  static forRoot() {
    return {
      ngModule: _CmsLcpModule,
      providers: [provideDefaultConfig(defaultLcpCmsComponentsConfig)]
    };
  }
  static {
    this.ɵfac = function CmsLcpModule_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _CmsLcpModule)();
    };
  }
  static {
    this.ɵmod = ɵɵdefineNgModule({
      type: _CmsLcpModule
    });
  }
  static {
    this.ɵinj = ɵɵdefineInjector({});
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(CmsLcpModule, [{
    type: NgModule,
    args: [{}]
  }], null, null);
})();
var LcpPresence;
(function(LcpPresence2) {
  LcpPresence2["HAS_LCP"] = "HAS_LCP";
  LcpPresence2["NO_LCP"] = "NO_LCP";
})(LcpPresence || (LcpPresence = {}));
var LcpCmsComponentsConfig = class _LcpCmsComponentsConfig {
  static {
    this.ɵfac = function LcpCmsComponentsConfig_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _LcpCmsComponentsConfig)();
    };
  }
  static {
    this.ɵprov = ɵɵdefineInjectable({
      token: _LcpCmsComponentsConfig,
      factory: function LcpCmsComponentsConfig_Factory(__ngFactoryType__) {
        let __ngConditionalFactory__ = null;
        if (__ngFactoryType__) {
          __ngConditionalFactory__ = new (__ngFactoryType__ || _LcpCmsComponentsConfig)();
        } else {
          __ngConditionalFactory__ = ɵɵinject(Config);
        }
        return __ngConditionalFactory__;
      },
      providedIn: "root"
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(LcpCmsComponentsConfig, [{
    type: Injectable,
    args: [{
      providedIn: "root",
      useExisting: Config
    }]
  }], null, null);
})();
var CmsLcpService = class _CmsLcpService {
  constructor() {
    this.config = inject(LcpCmsComponentsConfig);
  }
  /**
   * Tells whether the given CMS component is marked as containing
   * the LCP (Largest Contentful Paint) element.
   *
   * The list of CMS component IDs can be specified in 2 ways:
   * - statically in the Spartacus configuration `config.lcpCmsComponents.ids`
   * - and dynamically configuring a special marker `config.lcpCmsComponentIdMarker`
   *    (i.e. when the CMS component ID contains a specific marker, for example "__cxLCP__").
   *
   * If a certain component is shared across multiple pages, but it's the LCP only on some of them,
   * you can extend this service to implement custom logic. The following is an example of marking
   * a 'SharedBanner` component as LCP only on the homepage, but not on other pages:
   * ```typescript
   * export class CustomCmsLcpService extends CmsLcpService {
   *   routingService = inject(RoutingService);
   *   getLcpPresence(
   *     componentData: ContentSlotComponentData
   *   ): Observable<LcpPresence> {
   *     return this.routingService.getRouterState().pipe(
   *       switchMap((routerState) => {
   *         const semanticRoute = routerState?.state?.semanticRoute;
   *         if (componentData?.uid === 'SharedBanner') {
   *           return of(
   *             semanticRoute === 'home' ? LcpPresence.HAS_LCP : LcpPresence.NO_LCP
   *           );
   *         }
   *         return super.getLcpPresence(componentData);
   *       })
   *     );
   *   }
   * }
   * ```
   */
  getLcpPresence(componentData) {
    const idMarker = this.config?.lcpCmsComponents?.idMarker;
    const ids = this.config?.lcpCmsComponents?.ids || [];
    if (idMarker && componentData?.uid?.includes(idMarker)) {
      return of(LcpPresence.HAS_LCP);
    }
    if (ids?.length && componentData?.uid && ids.includes(componentData?.uid)) {
      return of(LcpPresence.HAS_LCP);
    }
    return of(LcpPresence.NO_LCP);
  }
  static {
    this.ɵfac = function CmsLcpService_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _CmsLcpService)();
    };
  }
  static {
    this.ɵprov = ɵɵdefineInjectable({
      token: _CmsLcpService,
      factory: _CmsLcpService.ɵfac,
      providedIn: "root"
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(CmsLcpService, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], null, null);
})();
var DEFAULT_LCP_PRESENCE = of(LcpPresence.NO_LCP);
var LCP_PRESENCE = new InjectionToken("LCP_PRESENCE", {
  providedIn: "root",
  factory: () => DEFAULT_LCP_PRESENCE
});
var CmsComponentData = class {
};
var provideLcpPresenceForCmsComponent = () => {
  return {
    provide: LCP_PRESENCE,
    useFactory: () => {
      const cmsComponentData = inject(CmsComponentData);
      const cmsLcpService = inject(CmsLcpService);
      return cmsComponentData.data$.pipe(switchMap((componentData) => cmsLcpService.getLcpPresence(componentData)), distinctUntilChanged(), shareReplay({
        bufferSize: 1,
        refCount: true
      }));
    }
  };
};
var GuardsComposer = class _GuardsComposer {
  /**
   * Executes guards, returning an Observable that:
   * - emits `true` if all those guards pass (emit `true`)
   * - emits `false` or `UrlTree` immediately if any those guards fails (returns `false` or `UrlTree`)
   *
   * This is in line with the default Angular behavior.
   */
  canActivate(guards, route, state) {
    if (guards.length) {
      const canActivateObservables = guards.map((guard) => this.canActivateGuard(guard, route, state));
      return this.composeCanActivateObservables(canActivateObservables);
    }
    return of(true);
  }
  /**
   * Executes a guard's `canActivate` method with `route` and `state`,
   * returning its result as an Observable.
   *
   * Converts non-Observable results (Promise or static value) into Observable.
   */
  canActivateGuard(guard, route, state) {
    if (this.isCanActivate(guard)) {
      return wrapIntoObservable(guard.canActivate(route, state)).pipe(first());
    } else {
      throw new Error("Invalid CanActivate guard");
    }
  }
  /**
   * Composes the given observables into a single observable that emits a boolean or an UrlTree.
   *
   * The returned composed observable:
   * - emits `true` after all the given observables emit `true`.
   * - emits `false` or `UrlTree` as soon as any of the given observables emit such a value (`false` or `UrlTree`).
   */
  composeCanActivateObservables(canActivateObservables) {
    return concat(...canActivateObservables).pipe(skipWhile((canActivate) => canActivate === true), endWith(true), first());
  }
  /**
   * Tells whether the given object has a `canActivate` method.
   */
  isCanActivate(guard) {
    return guard && typeof guard.canActivate === "function";
  }
  static {
    this.ɵfac = function GuardsComposer_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _GuardsComposer)();
    };
  }
  static {
    this.ɵprov = ɵɵdefineInjectable({
      token: _GuardsComposer,
      factory: _GuardsComposer.ɵfac,
      providedIn: "root"
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(GuardsComposer, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], null, null);
})();
var BEFORE_CMS_PAGE_GUARD = new InjectionToken("BEFORE_CMS_PAGE_GUARD", {
  providedIn: "root",
  factory: () => []
});
var BeforeCmsPageGuardService = class _BeforeCmsPageGuardService {
  constructor() {
    this.guards = inject(BEFORE_CMS_PAGE_GUARD);
    this.guardsComposer = inject(GuardsComposer);
  }
  /**
   * Executes guards from `BEFORE_CMS_PAGE_GUARD` token, returning an Observable that:
   * - emits `true` if all those guards pass (emit `true`)
   * - emits `false` or `UrlTree` immediately if any those guards fails (returns `false` or `UrlTree`)
   */
  canActivate(route, state) {
    return this.guardsComposer.canActivate(this.guards, route, state);
  }
  static {
    this.ɵfac = function BeforeCmsPageGuardService_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _BeforeCmsPageGuardService)();
    };
  }
  static {
    this.ɵprov = ɵɵdefineInjectable({
      token: _BeforeCmsPageGuardService,
      factory: _BeforeCmsPageGuardService.ɵfac,
      providedIn: "root"
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(BeforeCmsPageGuardService, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], null, null);
})();
var PAGE_LAYOUT_HANDLER = new InjectionToken("PageLayoutHandler");
var BreakpointService = class _BreakpointService {
  constructor(winRef, layoutConfig2, platform) {
    this.winRef = winRef;
    this.layoutConfig = layoutConfig2;
    this.platform = platform;
    this.breakpoint$ = isPlatformBrowser(this.platform) ? this.winRef.resize$.pipe(map((event) => this.getBreakpoint(event.target.innerWidth)), distinctUntilChanged(), shareReplay({
      refCount: true,
      bufferSize: 1
    })) : of(this.fallbackBreakpoint);
  }
  /**
   * Returns the breakpoints for the storefront layout.
   *
   * The breakpoints are driven by the `LayoutConfig.breakpoints` and sorted based on
   * the given screen size.
   */
  get breakpoints() {
    if (!this._breakpoints) {
      this._breakpoints = this.resolveBreakpointsFromConfig();
    }
    return this._breakpoints;
  }
  /**
   * Returns the _maximum_ size for the breakpoint, given by the `LayoutConfig.breakpoints`
   * configuration.
   */
  getSize(breakpoint) {
    return this.getMaxSize(breakpoint) ?? // if there's no direct max value or explicit max value
    // we must derive the max value from the previous min
    this.getMinSize(this.breakpoints?.[this.breakpoints.indexOf(breakpoint) + 1]);
  }
  /**
   * Indicates whether the current screen size is smaller than the maximum size of the
   * given breakpoint.
   *
   * If the given breakpoint is `BREAKPOINT.md`, the method returns `true` when the
   * window innerWidth is smaller than the configured size of `BREAKPOINT.md`.
   */
  isDown(breakpoint) {
    return this.breakpoint$.pipe(map((br) => this.breakpoints.slice(0, this.breakpoints.indexOf(breakpoint) + 1).includes(br)));
  }
  /**
   * Indicates whether the current screen size is larger than the minimum size of the
   * given breakpoint.
   *
   * If the given breakpoint is `BREAKPOINT.md`, the method returns `true` when the
   * window innerWidth is larger than the configured size of `BREAKPOINT.sm`.
   */
  isUp(breakpoint) {
    return this.breakpoint$.pipe(map((br) => this.breakpoints.slice(this.breakpoints.indexOf(breakpoint)).includes(br)));
  }
  /**
   * Indicates whether the given breakpoint fits in the current screen size.
   */
  isEqual(breakpoint) {
    return this.breakpoint$.pipe(map((br) => br === breakpoint));
  }
  /**
   * Returns the fallback breakpoint in case no breakpoint can be resolved. This is
   * typically the case when we're on SSR without an actual window.
   *
   * Returns the smallest screen size (mobile first).
   */
  get fallbackBreakpoint() {
    return this.breakpoints?.[0];
  }
  /**
   * Resolves the breakpoints and sorts them according to the configured size.
   *
   * The sort order is by small to large screens.
   */
  resolveBreakpointsFromConfig() {
    const sortByScreenSize = (next, prev) => {
      const nextMinSize = this.getMinSize(next);
      const maxNext = Math.max(nextMinSize ? nextMinSize + 1 : 0, this.getMaxSize(next) || 0);
      const preMinSize = this.getMinSize(prev);
      const maxPrev = Math.max(preMinSize ? preMinSize + 1 : 0, this.getMaxSize(prev) || 0);
      return maxNext < maxPrev ? -1 : 0;
    };
    return Object.keys(this.config).sort(sortByScreenSize);
  }
  /**
   * Returns the _maximum_ size for the breakpoint, given by the
   * `LayoutConfig.breakpoints` configuration. We will try to resolve the
   * max size form the current breakpoint, but if this is not available, we
   * resolve it form the next breakpoint
   */
  getMaxSize(breakpoint) {
    const breakpointConfig = this.config[breakpoint];
    if (!breakpointConfig) {
      return null;
    }
    if (typeof breakpointConfig === "number") {
      return breakpointConfig;
    } else if (breakpointConfig.max) {
      return breakpointConfig.max;
    } else {
      return null;
    }
  }
  getMinSize(breakpoint) {
    return this.config[breakpoint]?.min ?? null;
  }
  /**
   * Returns a `BREAKPOINT` for the given window size.
   *
   * This method tries to match the closest breakpoint for the given
   * window size. We'll fallback to the `largest` size in case the window
   * is greater than the largest configurable breakpoint.
   *
   * The windowWidth should be smaller than the maximum size of any of the
   * screen sizes defined in the `LayoutConfig.breakpoints`.
   */
  getBreakpoint(windowWidth) {
    return this.breakpoints.find((br) => {
      const size = this.getSize(br);
      return size !== null && windowWidth < size;
    }) ?? this.breakpoints?.[this.breakpoints.length - 1];
  }
  /**
   * Helper method to return the breakpoint configuration.
   */
  get config() {
    return this.layoutConfig?.breakpoints || {};
  }
  static {
    this.ɵfac = function BreakpointService_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _BreakpointService)(ɵɵinject(WindowRef), ɵɵinject(LayoutConfig), ɵɵinject(PLATFORM_ID));
    };
  }
  static {
    this.ɵprov = ɵɵdefineInjectable({
      token: _BreakpointService,
      factory: _BreakpointService.ɵfac,
      providedIn: "root"
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(BreakpointService, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], () => [{
    type: WindowRef
  }, {
    type: LayoutConfig
  }, {
    type: void 0,
    decorators: [{
      type: Inject,
      args: [PLATFORM_ID]
    }]
  }], null);
})();
var PageLayoutService = class _PageLayoutService {
  constructor(cms, config, breakpointService, unifiedInjector) {
    this.cms = cms;
    this.config = config;
    this.breakpointService = breakpointService;
    this.unifiedInjector = unifiedInjector;
    this.subscription = new Subscription();
    this.logger = inject(LoggerService);
    this.warnLogMessages = {};
    this.logSlots = {};
    this.subscription.add(this.unifiedInjector.getMulti(PAGE_LAYOUT_HANDLER).subscribe((handlers) => this.handlers = handlers));
  }
  getSlots(section) {
    return combineLatest([this.page$, this.breakpointService.breakpoint$]).pipe(map(([page, breakpoint]) => {
      const pageTemplate = page.template;
      const slots = this.resolveSlots(page, section, breakpoint);
      return {
        slots,
        pageTemplate,
        breakpoint
      };
    }), switchMap(({
      slots,
      pageTemplate,
      breakpoint
    }) => {
      let result = of(slots);
      for (const handler of this.handlers || []) {
        result = handler.handle(result, pageTemplate, section, breakpoint);
      }
      return result;
    }), distinctUntilChanged((a, b) => {
      if (a.length !== b.length) {
        return false;
      }
      for (let i = 0; i < a.length; i++) {
        if (a[i] !== b[i]) {
          return false;
        }
      }
      return true;
    }));
  }
  /**
   * Returns an observable with the last page slot above-the-fold
   * for the given pageTemplate / breakpoint.
   *
   * The page fold is configurable in the `LayoutConfig` for each page layout.
   */
  getPageFoldSlot(pageTemplate) {
    return this.breakpointService.breakpoint$.pipe(map((breakpoint) => {
      if (!this.config.layoutSlots) {
        return void 0;
      }
      const pageTemplateConfig = this.config.layoutSlots[pageTemplate];
      const config = this.getResponsiveSlotConfig(pageTemplateConfig, "pageFold", breakpoint);
      return config ? config.pageFold : void 0;
    }));
  }
  resolveSlots(page, section, breakpoint) {
    const config = this.getSlotConfig(page.template ?? "", "slots", section, breakpoint);
    if (config && config.slots) {
      const pageSlots = page.slots ? Object.keys(page.slots) : [];
      return config.slots.filter((slot) => pageSlots.includes(slot));
    } else if (!section) {
      this.logMissingLayoutConfig(page);
      return page.slots ? Object.keys(page.slots) : [];
    } else {
      this.logMissingLayoutConfig(page, section);
      return [];
    }
  }
  get page$() {
    return this.cms.getCurrentPage().pipe(filter((page) => !!page));
  }
  get templateName$() {
    return this.page$.pipe(map((page) => page.template), filter(isNotUndefined));
  }
  /**
   * load slots from the layout configuration. The breakpoint is used
   * to load a specific configuration for the given breakpoint. If there's
   * no configuration available for the given breakpoint the default slot
   * configuration is returned.
   */
  getSlotConfig(templateUid, configAttribute, section, breakpoint) {
    if (!this.config.layoutSlots) {
      return void 0;
    }
    const pageTemplateConfig = this.config.layoutSlots[templateUid];
    if (section) {
      return this.getSlotConfigForSection(templateUid, configAttribute, section, breakpoint);
    }
    if (pageTemplateConfig) {
      return this.getResponsiveSlotConfig(pageTemplateConfig, configAttribute, breakpoint);
    }
  }
  getSlotConfigForSection(templateUid, configAttribute, section, breakpoint) {
    const pageTemplateConfig = this.config.layoutSlots?.[templateUid];
    if (!pageTemplateConfig || !section) {
      return void 0;
    }
    const sectionConfig = pageTemplateConfig[section] ? pageTemplateConfig[section] : this.config.layoutSlots?.[section];
    if (!sectionConfig) {
      return void 0;
    }
    const responsiveConfig = this.getResponsiveSlotConfig(sectionConfig, configAttribute, breakpoint);
    if (responsiveConfig.hasOwnProperty(configAttribute)) {
      return responsiveConfig;
    } else if (pageTemplateConfig[section].hasOwnProperty(configAttribute)) {
      return pageTemplateConfig[section];
    } else if (this.config.layoutSlots?.[section]) {
      return this.config.layoutSlots[section];
    }
  }
  /**
   * Returns a list of slots for a breakpoint specific configuration
   * If there's no specific configuration for the breakpoint,
   * the closest available configuration will be returned.
   */
  getResponsiveSlotConfig(layoutSlotConfig, configAttribute, breakpoint) {
    let slotConfig = layoutSlotConfig;
    if (!layoutSlotConfig || !breakpoint) {
      return slotConfig;
    }
    if (layoutSlotConfig[breakpoint] && layoutSlotConfig[breakpoint].hasOwnProperty(configAttribute)) {
      return layoutSlotConfig[breakpoint];
    }
    const all = this.breakpointService.breakpoints;
    for (const br of all.slice(0, all.indexOf(breakpoint))) {
      if (layoutSlotConfig[br] && layoutSlotConfig[br].hasOwnProperty(configAttribute)) {
        slotConfig = layoutSlotConfig[br];
      }
    }
    return slotConfig;
  }
  /**
   * In order to help developers, we print some detailed log information in
   * case there's no layout configuration available for the given page template
   * or section. Additionally, the slot positions are printed in the console
   * in a format that can be copied / paste to the configuration.
   */
  logMissingLayoutConfig(page, section) {
    if (!isDevMode()) {
      return;
    }
    if (page.template && !this.logSlots[page.template]) {
      this.logger.info(`Available CMS page slots: '${(page.slots ? Object.keys(page.slots) : []).join(`','`)}'`);
      this.logSlots[page.template] = true;
    }
    const cacheKey = section || page.template;
    if (cacheKey && !this.warnLogMessages[cacheKey]) {
      this.logger.warn(`No layout config found for ${cacheKey}, you can configure a 'LayoutConfig' to control the rendering of page slots.`);
      this.warnLogMessages[cacheKey] = true;
    }
  }
  ngOnDestroy() {
    this.subscription.unsubscribe();
  }
  static {
    this.ɵfac = function PageLayoutService_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _PageLayoutService)(ɵɵinject(CmsService), ɵɵinject(LayoutConfig), ɵɵinject(BreakpointService), ɵɵinject(UnifiedInjector));
    };
  }
  static {
    this.ɵprov = ɵɵdefineInjectable({
      token: _PageLayoutService,
      factory: _PageLayoutService.ɵfac,
      providedIn: "root"
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PageLayoutService, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], () => [{
    type: CmsService
  }, {
    type: LayoutConfig
  }, {
    type: BreakpointService
  }, {
    type: UnifiedInjector
  }], null);
})();
var CmsFeaturesService = class _CmsFeaturesService {
  constructor(configInitializer, featureModules) {
    this.configInitializer = configInitializer;
    this.featureModules = featureModules;
    this.componentFeatureMap = /* @__PURE__ */ new Map();
    this.featureInstances = /* @__PURE__ */ new Map();
    this.initFeatureMap();
  }
  initFeatureMap() {
    this.configInitializer.getStable("featureModules").subscribe((config) => {
      this.featureModulesConfig = config.featureModules ?? {};
      for (const [featureName, featureConfig] of Object.entries(this.featureModulesConfig)) {
        if (typeof featureConfig !== "string" && featureConfig?.module && featureConfig?.cmsComponents?.length) {
          for (const component of featureConfig.cmsComponents) {
            this.componentFeatureMap.set(component, featureName);
          }
        }
      }
    });
  }
  /**
   * Check if there is feature module configuration that covers specified
   * component type
   */
  hasFeatureFor(componentType) {
    return this.componentFeatureMap.has(componentType);
  }
  /**
   * Return full CmsComponent mapping defined in feature module
   */
  getCmsMapping(componentType) {
    const feature = this.componentFeatureMap.get(componentType);
    if (!feature) {
      return of(void 0);
    }
    return this.resolveFeatureInstance(feature).pipe(map((featureInstance) => featureInstance.componentsMappings?.[componentType]));
  }
  /**
   * Resolves feature module for provided component type
   *
   * @param componentType
   */
  getModule(componentType) {
    const feature = this.componentFeatureMap.get(componentType);
    if (!feature) {
      return void 0;
    }
    let module;
    this.featureInstances.get(feature)?.subscribe((featureInstance) => {
      module = featureInstance.moduleRef;
    }).unsubscribe();
    return module;
  }
  /**
   * Resolve feature based on feature name, if feature was not yet resolved
   *
   * It will first resolve all module dependencies if defined
   */
  resolveFeatureInstance(featureName) {
    return defer(() => {
      if (!this.featureInstances.has(featureName)) {
        this.featureInstances.set(featureName, this.featureModules.resolveFeature(featureName).pipe(map((moduleRef) => this.createFeatureInstance(moduleRef, featureName)), shareReplay()));
      }
      return this.featureInstances.get(featureName) ?? EMPTY;
    });
  }
  /**
   * Create feature instance from feature's moduleRef
   */
  createFeatureInstance(moduleRef, feature) {
    const featureConfig = this.featureModulesConfig[feature];
    const featureInstance = {
      moduleRef,
      componentsMappings: {}
    };
    const resolvedConfiguration = this.resolveFeatureConfiguration(moduleRef.injector);
    for (const componentType of featureConfig.cmsComponents ?? []) {
      featureInstance.componentsMappings[componentType] = resolvedConfiguration.cmsComponents?.[componentType] ?? {};
    }
    return featureInstance;
  }
  /**
   * Returns configuration provided in feature module
   */
  resolveFeatureConfiguration(featureInjector) {
    const featureConfigChunks = featureInjector.get(ConfigChunk, [], {
      self: true
    });
    const featureDefaultConfigChunks = featureInjector.get(DefaultConfigChunk, [], {
      self: true
    });
    return deepMerge({}, ...featureDefaultConfigChunks ?? [], ...featureConfigChunks ?? []);
  }
  static {
    this.ɵfac = function CmsFeaturesService_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _CmsFeaturesService)(ɵɵinject(ConfigInitializerService), ɵɵinject(FeatureModulesService));
    };
  }
  static {
    this.ɵprov = ɵɵdefineInjectable({
      token: _CmsFeaturesService,
      factory: _CmsFeaturesService.ɵfac,
      providedIn: "root"
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(CmsFeaturesService, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], () => [{
    type: ConfigInitializerService
  }, {
    type: FeatureModulesService
  }], null);
})();
var CmsComponentsService = class _CmsComponentsService {
  constructor(config, platformId, featureModules, configInitializer) {
    this.config = config;
    this.platformId = platformId;
    this.featureModules = featureModules;
    this.configInitializer = configInitializer;
    this.missingComponents = [];
    this.mappings = {};
    this.mappingResolvers = /* @__PURE__ */ new Map();
    this.logger = inject(LoggerService);
    this.configInitializer.getStable("cmsComponents").subscribe((cmsConfig) => {
      this.staticCmsConfig = __spreadValues({}, cmsConfig.cmsComponents);
    });
  }
  /**
   * Should be called to make sure all component mappings are determined,
   * especially lazy loaded ones.
   *
   * It's recommended way to make sure all other methods of CmsComponentService
   * will be able to work synchronously for asked component types and avoid risk
   * of potential errors that could be thrown otherwise.
   */
  determineMappings(componentTypes) {
    return defer(() => {
      const featureResolvers = [];
      for (const componentType of componentTypes) {
        if (!this.mappings[componentType]) {
          const staticConfig = (this.staticCmsConfig ?? this.config.cmsComponents)?.[componentType];
          if (this.featureModules.hasFeatureFor(componentType)) {
            featureResolvers.push(
              // we delegate populating this.mappings to feature resolver
              this.getFeatureMappingResolver(componentType, staticConfig)
            );
          } else {
            if (staticConfig) {
              this.mappings[componentType] = staticConfig;
            }
          }
        }
      }
      if (featureResolvers.length) {
        return forkJoin(featureResolvers).pipe(map(() => componentTypes));
      } else {
        return of(componentTypes);
      }
    });
  }
  getFeatureMappingResolver(componentType, staticConfig) {
    if (!this.mappingResolvers.has(componentType)) {
      const mappingResolver$ = this.featureModules.getCmsMapping(componentType).pipe(filter(isNotUndefined), tap((featureComponentMapping) => {
        this.mappings[componentType] = deepMerge({}, featureComponentMapping, staticConfig);
        this.mappingResolvers.delete(componentType);
      }), share());
      this.mappingResolvers.set(componentType, mappingResolver$);
    }
    return this.mappingResolvers.get(componentType);
  }
  /**
   * Returns the feature module for a cms component.
   * It will only work for cms components provided by feature modules.
   *
   * @param componentType
   */
  getModule(componentType) {
    if (this.featureModules.hasFeatureFor(componentType)) {
      return this.featureModules.getModule(componentType);
    }
  }
  /**
   * Return collection of component mapping configuration for specified list of
   * component types.
   *
   * If component mapping can't be determined synchronously, for example, lazy
   * loaded one, it will throw an error.
   *
   * To make sure component mapping is available, determineMappings()
   * should be called and completed first.
   */
  getMapping(componentType) {
    const componentConfig = this.mappings[componentType] ?? (this.staticCmsConfig ?? this.config.cmsComponents)?.[componentType];
    if (isDevMode() && !componentConfig) {
      if (!this.missingComponents.includes(componentType)) {
        this.missingComponents.push(componentType);
        this.logger.warn(`No component implementation found for the CMS component type '${componentType}'.
`, `Make sure you implement a component and register it in the mapper.`);
      }
    }
    return componentConfig;
  }
  /**
   * Checks, if component should be rendered as some components
   * could be disabled for server side renderings
   */
  shouldRender(componentType) {
    const isSSR = isPlatformServer(this.platformId);
    return !(isSSR && this.getMapping(componentType)?.disableSSR);
  }
  /**
   * Return DeferLoadingStrategy for component type.
   */
  getDeferLoadingStrategy(componentType) {
    return (this.staticCmsConfig ?? this.config.cmsComponents)?.[componentType]?.deferLoading;
  }
  /**
   * Get cms driven child routes for components
   */
  getChildRoutes(componentTypes) {
    const configs = [];
    for (const componentType of componentTypes) {
      if (this.shouldRender(componentType)) {
        configs.push(this.getMapping(componentType)?.childRoutes ?? []);
      }
    }
    return this.standardizeChildRoutes(configs);
  }
  /**
   * Returns the static data for the component type.
   */
  getStaticData(componentType) {
    return this.getMapping(componentType)?.data;
  }
  /**
   * Standardizes the format of `childRoutes` config.
   *
   * Some `childRoutes` configs are simple arrays of Routes (without the notion of the parent route).
   * But some configs can be an object with children routes and their parent defined in separate property.
   */
  standardizeChildRoutes(childRoutesConfigs) {
    const result = {
      children: []
    };
    (childRoutesConfigs || []).forEach((config) => {
      if (Array.isArray(config)) {
        result.children?.push(...config);
      } else {
        result.children?.push(...config.children || []);
        if (config.parent) {
          result.parent = config.parent;
        }
      }
    });
    return result;
  }
  /**
   * Get cms driven guards for components
   */
  getGuards(componentTypes) {
    const guards = /* @__PURE__ */ new Set();
    for (const componentType of componentTypes) {
      this.getMapping(componentType)?.guards?.forEach((guard) => guards.add(guard));
    }
    return Array.from(guards);
  }
  /**
   * Get i18n keys associated with components
   */
  getI18nKeys(componentTypes) {
    const i18nKeys = /* @__PURE__ */ new Set();
    for (const componentType of componentTypes) {
      if (this.shouldRender(componentType)) {
        this.getMapping(componentType)?.i18nKeys?.forEach((key) => i18nKeys.add(key));
      }
    }
    return Array.from(i18nKeys);
  }
  static {
    this.ɵfac = function CmsComponentsService_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _CmsComponentsService)(ɵɵinject(CmsConfig), ɵɵinject(PLATFORM_ID), ɵɵinject(CmsFeaturesService), ɵɵinject(ConfigInitializerService));
    };
  }
  static {
    this.ɵprov = ɵɵdefineInjectable({
      token: _CmsComponentsService,
      factory: _CmsComponentsService.ɵfac,
      providedIn: "root"
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(CmsComponentsService, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], () => [{
    type: CmsConfig
  }, {
    type: Object,
    decorators: [{
      type: Inject,
      args: [PLATFORM_ID]
    }]
  }, {
    type: CmsFeaturesService
  }, {
    type: ConfigInitializerService
  }], null);
})();
var PageSlotService = class _PageSlotService {
  constructor(cmsComponentsService, platformId, document2) {
    this.cmsComponentsService = cmsComponentsService;
    this.platformId = platformId;
    this.document = document2;
    this.resolvePrerenderedSlots();
  }
  /**
   * Finds all slots visible in the SSR pre-rendered DOM
   */
  resolvePrerenderedSlots() {
    if (isPlatformBrowser(this.platformId)) {
      this.prerenderedSlots = Array.from(this.document.querySelectorAll("cx-page-slot")).filter((el) => el.getBoundingClientRect().top < this.document.documentElement.clientHeight).map((el) => el.getAttribute("position"));
    }
  }
  /**
   * Indicates if certain slot should be rendered instantly.
   *
   * It's especially useful when transitioning from SSR to CSR application,
   * where we don't want to apply deferring logic to slots that are visible
   * to avoid unnecessary flickering.
   */
  shouldNotDefer(slot) {
    if (this.prerenderedSlots?.includes(slot)) {
      this.prerenderedSlots.splice(this.prerenderedSlots.indexOf(slot), 1);
      return true;
    }
    return false;
  }
  /**
   * Returns the defer options for the given component. If the wrapping
   * page slot is prerendered, we would ignore the defer options altogether.
   */
  getComponentDeferOptions(slot, componentType) {
    if (slot && this.shouldNotDefer(slot)) {
      return {
        deferLoading: DeferLoadingStrategy.INSTANT
      };
    }
    const deferLoading = this.cmsComponentsService.getDeferLoadingStrategy(componentType);
    return {
      deferLoading
    };
  }
  static {
    this.ɵfac = function PageSlotService_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _PageSlotService)(ɵɵinject(CmsComponentsService), ɵɵinject(PLATFORM_ID), ɵɵinject(DOCUMENT));
    };
  }
  static {
    this.ɵprov = ɵɵdefineInjectable({
      token: _PageSlotService,
      factory: _PageSlotService.ɵfac,
      providedIn: "root"
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PageSlotService, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], () => [{
    type: CmsComponentsService
  }, {
    type: void 0,
    decorators: [{
      type: Inject,
      args: [PLATFORM_ID]
    }]
  }, {
    type: Document,
    decorators: [{
      type: Inject,
      args: [DOCUMENT]
    }]
  }], null);
})();
var ComponentEvent = class extends CxEvent {
};
var ComponentCreateEvent = class extends ComponentEvent {
  static {
    this.type = "ComponentCreate";
  }
};
var ComponentDestroyEvent = class extends ComponentEvent {
  static {
    this.type = "ComponentDestroy";
  }
};
var ComponentHandler = class {
};
var ComponentHandlerService = class _ComponentHandlerService {
  constructor(handlers) {
    this.handlers = handlers;
    this.logger = inject(LoggerService);
    this.invalidMappings = /* @__PURE__ */ new Set();
  }
  /**
   * Get best matching component handler
   *
   * @param componentMapping
   */
  resolve(componentMapping) {
    const handler = resolveApplicable(this.handlers, [componentMapping]);
    if (isDevMode() && !handler) {
      if (!this.invalidMappings.has(componentMapping)) {
        this.invalidMappings.add(componentMapping);
        this.logger.warn("Can't resolve handler for component mapping: ", componentMapping);
      }
    }
    return handler;
  }
  /**
   * Get launcher for specified component mapping
   *
   * @param componentMapping
   * @param viewContainerRef
   * @param elementInjector
   */
  getLauncher(componentMapping, viewContainerRef, elementInjector, module) {
    return this.resolve(componentMapping)?.launcher(componentMapping, viewContainerRef, elementInjector, module);
  }
  static {
    this.ɵfac = function ComponentHandlerService_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _ComponentHandlerService)(ɵɵinject(ComponentHandler, 8));
    };
  }
  static {
    this.ɵprov = ɵɵdefineInjectable({
      token: _ComponentHandlerService,
      factory: _ComponentHandlerService.ɵfac,
      providedIn: "root"
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(ComponentHandlerService, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], () => [{
    type: void 0,
    decorators: [{
      type: Optional
    }, {
      type: Inject,
      args: [ComponentHandler]
    }]
  }], null);
})();
var ComponentDataProvider = class _ComponentDataProvider {
  constructor(componentsService, cmsService) {
    this.componentsService = componentsService;
    this.cmsService = cmsService;
  }
  /**
   * Return the component data for a component given by the `uid`.
   *
   * If the `type` is provided, static component data (if available) is
   * merged into the component data. The static data is complemented and
   * overridden with data retrieved from the cms service.
   */
  get(uid, type) {
    return defer(() => {
      let staticComponentData;
      if (type) {
        staticComponentData = this.componentsService.getStaticData(type);
      }
      if (uid) {
        if (staticComponentData) {
          return this.cmsService.getComponentData(uid).pipe(map((data) => __spreadValues(__spreadValues({}, staticComponentData), data)), startWith(staticComponentData));
        } else {
          return this.cmsService.getComponentData(uid);
        }
      } else {
        return staticComponentData ? of(staticComponentData) : EMPTY;
      }
    });
  }
  static {
    this.ɵfac = function ComponentDataProvider_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _ComponentDataProvider)(ɵɵinject(CmsComponentsService), ɵɵinject(CmsService));
    };
  }
  static {
    this.ɵprov = ɵɵdefineInjectable({
      token: _ComponentDataProvider,
      factory: _ComponentDataProvider.ɵfac,
      providedIn: "root"
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(ComponentDataProvider, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], () => [{
    type: CmsComponentsService
  }, {
    type: CmsService
  }], null);
})();
var CmsInjectorService = class _CmsInjectorService {
  constructor(cmsComponentsService, injector) {
    this.cmsComponentsService = cmsComponentsService;
    this.injector = injector;
  }
  getInjector(type, uid, parentInjector) {
    const configProviders = this.cmsComponentsService.getMapping(type)?.providers ?? [];
    return Injector.create({
      providers: [{
        provide: CmsComponentData,
        useFactory: (dataProvider) => ({
          uid,
          data$: dataProvider.get(uid, type)
        }),
        deps: [ComponentDataProvider]
      }, provideLcpPresenceForCmsComponent(), ...configProviders],
      parent: parentInjector ?? this.injector
    });
  }
  static {
    this.ɵfac = function CmsInjectorService_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _CmsInjectorService)(ɵɵinject(CmsComponentsService), ɵɵinject(Injector));
    };
  }
  static {
    this.ɵprov = ɵɵdefineInjectable({
      token: _CmsInjectorService,
      factory: _CmsInjectorService.ɵfac,
      providedIn: "root"
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(CmsInjectorService, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], () => [{
    type: CmsComponentsService
  }, {
    type: Injector
  }], null);
})();
var ComponentWrapperDirective = class _ComponentWrapperDirective {
  constructor(vcr, cmsComponentsService, injector, dynamicAttributeService, renderer, componentHandler, cmsInjector, eventService) {
    this.vcr = vcr;
    this.cmsComponentsService = cmsComponentsService;
    this.injector = injector;
    this.dynamicAttributeService = dynamicAttributeService;
    this.renderer = renderer;
    this.componentHandler = componentHandler;
    this.cmsInjector = cmsInjector;
    this.eventService = eventService;
    this.cxComponentRef = new EventEmitter();
  }
  ngOnInit() {
    this.cmsComponentsService.determineMappings([this.cxComponentWrapper.flexType ?? ""]).subscribe(() => {
      if (this.cmsComponentsService.shouldRender(this.cxComponentWrapper.flexType ?? "")) {
        this.launchComponent();
      }
    });
  }
  launchComponent() {
    const componentMapping = this.cmsComponentsService.getMapping(this.cxComponentWrapper.flexType ?? "");
    if (!componentMapping) {
      return;
    }
    this.launcherResource = this.componentHandler.getLauncher(componentMapping, this.vcr, this.cmsInjector.getInjector(this.cxComponentWrapper.flexType ?? "", this.cxComponentWrapper.uid ?? "", this.injector), this.cmsComponentsService.getModule(this.cxComponentWrapper.flexType ?? ""))?.pipe(filter(isNotUndefined), tap(({
      elementRef,
      componentRef
    }) => {
      this.cmpRef = componentRef;
      this.cxComponentRef.emit(componentRef);
      this.dispatchEvent(ComponentCreateEvent, elementRef);
      this.decorate(elementRef);
      this.injector.get(ChangeDetectorRef).markForCheck();
    }), finalize(() => this.dispatchEvent(ComponentDestroyEvent))).subscribe();
  }
  /**
   * Dispatch the component event.
   *
   * The event is dispatched during creation and removal of the component.
   */
  dispatchEvent(event, elementRef) {
    const payload = {
      typeCode: this.cxComponentWrapper.typeCode,
      id: this.cxComponentWrapper.uid
    };
    if (event === ComponentCreateEvent) {
      payload.host = elementRef?.nativeElement;
    }
    this.eventService.dispatch(payload, event);
  }
  decorate(elementRef) {
    this.dynamicAttributeService.addAttributesToComponent(elementRef.nativeElement, this.renderer, this.cxComponentWrapper);
  }
  ngOnDestroy() {
    if (this.launcherResource) {
      this.launcherResource.unsubscribe();
    }
  }
  static {
    this.ɵfac = function ComponentWrapperDirective_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _ComponentWrapperDirective)(ɵɵdirectiveInject(ViewContainerRef), ɵɵdirectiveInject(CmsComponentsService), ɵɵdirectiveInject(Injector), ɵɵdirectiveInject(DynamicAttributeService), ɵɵdirectiveInject(Renderer2), ɵɵdirectiveInject(ComponentHandlerService), ɵɵdirectiveInject(CmsInjectorService), ɵɵdirectiveInject(EventService));
    };
  }
  static {
    this.ɵdir = ɵɵdefineDirective({
      type: _ComponentWrapperDirective,
      selectors: [["", "cxComponentWrapper", ""]],
      inputs: {
        cxComponentWrapper: "cxComponentWrapper"
      },
      outputs: {
        cxComponentRef: "cxComponentRef"
      },
      standalone: false
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(ComponentWrapperDirective, [{
    type: Directive,
    args: [{
      selector: "[cxComponentWrapper]",
      standalone: false
    }]
  }], () => [{
    type: ViewContainerRef
  }, {
    type: CmsComponentsService
  }, {
    type: Injector
  }, {
    type: DynamicAttributeService
  }, {
    type: Renderer2
  }, {
    type: ComponentHandlerService
  }, {
    type: CmsInjectorService
  }, {
    type: EventService
  }], {
    cxComponentWrapper: [{
      type: Input
    }],
    cxComponentRef: [{
      type: Output
    }]
  });
})();
var PageSlotComponent = class _PageSlotComponent {
  /**
   * The position represents the unique key for a page slot on a single page, but can
   * be reused cross pages.
   *
   * The position is used to find the CMS components for the page slot. It is also
   * added as an additional CSS class so that layout can be applied.
   */
  set position(value) {
    this.position$.next(value);
  }
  get position() {
    return this.position$.value;
  }
  constructor(cmsService, dynamicAttributeService, renderer, elementRef, cd, pageSlotService) {
    this.cmsService = cmsService;
    this.dynamicAttributeService = dynamicAttributeService;
    this.renderer = renderer;
    this.elementRef = elementRef;
    this.cd = cd;
    this.pageSlotService = pageSlotService;
    this.isPageFold = false;
    this.isPending = true;
    this.hasComponents = false;
    this.position$ = new BehaviorSubject(void 0);
    this.slot$ = this.position$.pipe(filter(isNotUndefined), switchMap((position) => this.cmsService.getContentSlot(position)), distinctUntilChanged(this.isDistinct));
    this.components$ = this.slot$.pipe(map((slot) => slot?.components ?? []));
    this.subscription = new Subscription();
    this.pendingComponentCount = 0;
  }
  ngOnInit() {
    this.subscription.add(this.slot$.pipe(tap((slot) => this.decorate(slot))).subscribe((value) => {
      this.components = value?.components || [];
      this.cd.markForCheck();
    }));
  }
  decorate(slot) {
    let cls = this.class || "";
    if (this.lastPosition && cls.indexOf(this.lastPosition) > -1) {
      cls = cls.replace(this.lastPosition, "");
    }
    if (this.position$.value) {
      cls += ` ${this.position$.value}`;
      this.lastPosition = this.position$.value;
    }
    this.pending = slot?.components?.length || 0;
    this.hasComponents = slot?.components ? slot?.components?.length > 0 : false;
    if (cls && cls !== this.class) {
      this.class = cls;
    }
    if (slot) {
      this.dynamicAttributeService.addAttributesToSlot(this.elementRef.nativeElement, this.renderer, slot);
    }
  }
  /**
   * Sets the pending count for the page slot components. Once all pending components are
   * loaded, the `isPending` flag is updated, so that the associated class can be updated
   */
  set pending(count) {
    this.pendingComponentCount = count;
    this.isPending = this.pendingComponentCount > 0;
  }
  get pending() {
    return this.pendingComponentCount;
  }
  /*
   * Is triggered when a component is added to the view. This is used to
   * update the pending count
   */
  isLoaded(loadState) {
    if (loadState) {
      this.pending--;
      this.cd.markForCheck();
    }
  }
  /**
   * The `DeferLoadingStrategy` indicates whether the component should be
   * rendered instantly or whether it should be deferred.
   */
  getComponentDeferOptions(componentType) {
    return this.pageSlotService.getComponentDeferOptions(this.position, componentType);
  }
  isDistinct(old, current) {
    return Boolean(current.components && old.components && old.components.length === current.components.length && !old.components.find((el, index) => el.uid !== current.components?.[index].uid));
  }
  ngOnDestroy() {
    this.subscription?.unsubscribe();
  }
  static {
    this.ɵfac = function PageSlotComponent_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _PageSlotComponent)(ɵɵdirectiveInject(CmsService), ɵɵdirectiveInject(DynamicAttributeService), ɵɵdirectiveInject(Renderer2), ɵɵdirectiveInject(ElementRef), ɵɵdirectiveInject(ChangeDetectorRef), ɵɵdirectiveInject(PageSlotService));
    };
  }
  static {
    this.ɵcmp = ɵɵdefineComponent({
      type: _PageSlotComponent,
      selectors: [["cx-page-slot"], ["", "cx-page-slot", ""]],
      hostVars: 9,
      hostBindings: function PageSlotComponent_HostBindings(rf, ctx) {
        if (rf & 2) {
          ɵɵattribute("position", ctx.position);
          ɵɵclassMap(ctx.class);
          ɵɵclassProp("page-fold", ctx.isPageFold)("cx-pending", ctx.isPending)("has-components", ctx.hasComponents);
        }
      },
      inputs: {
        position: "position",
        class: "class",
        isPageFold: "isPageFold",
        hasComponents: "hasComponents"
      },
      standalone: false,
      decls: 1,
      vars: 1,
      consts: [[4, "ngIf"], [3, "cxOutlet", "cxOutletContext"], [4, "ngFor", "ngForOf"], [3, "loaded", "cxOutlet", "cxOutletContext", "cxOutletDefer"], [3, "cxComponentWrapper"]],
      template: function PageSlotComponent_Template(rf, ctx) {
        if (rf & 1) {
          ɵɵtemplate(0, PageSlotComponent_0_Template, 1, 4, null, 0);
        }
        if (rf & 2) {
          ɵɵproperty("ngIf", ctx.position);
        }
      },
      dependencies: [NgForOf, NgIf, OutletDirective, ComponentWrapperDirective],
      encapsulation: 2,
      changeDetection: 0
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PageSlotComponent, [{
    type: Component,
    args: [{
      selector: "cx-page-slot,[cx-page-slot]",
      changeDetection: ChangeDetectionStrategy.OnPush,
      standalone: false,
      template: '<ng-template\n  *ngIf="position"\n  [cxOutlet]="position"\n  [cxOutletContext]="{ components$: components$ }"\n>\n  <ng-container *ngFor="let component of components">\n    <ng-template\n      *ngIf="component.flexType"\n      [cxOutlet]="component.flexType"\n      [cxOutletContext]="{ component: component }"\n      [cxOutletDefer]="getComponentDeferOptions(component.flexType)"\n      (loaded)="isLoaded($event)"\n    >\n      <ng-container [cxComponentWrapper]="component"></ng-container>\n    </ng-template>\n  </ng-container>\n</ng-template>\n'
    }]
  }], () => [{
    type: CmsService
  }, {
    type: DynamicAttributeService
  }, {
    type: Renderer2
  }, {
    type: ElementRef
  }, {
    type: ChangeDetectorRef
  }, {
    type: PageSlotService
  }], {
    position: [{
      type: HostBinding,
      args: ["attr.position"]
    }, {
      type: Input
    }],
    class: [{
      type: Input
    }, {
      type: HostBinding
    }],
    isPageFold: [{
      type: HostBinding,
      args: ["class.page-fold"]
    }, {
      type: Input
    }],
    isPending: [{
      type: HostBinding,
      args: ["class.cx-pending"]
    }],
    hasComponents: [{
      type: HostBinding,
      args: ["class.has-components"]
    }, {
      type: Input
    }]
  });
})();
var PageTemplateDirective = class _PageTemplateDirective {
  /**
   * Adds a style class to the host element based on the cms page template, unless
   * the class is given as an input.
   *
   * The host element is either the actual host, or the parent element in case this
   * is used inside an `ng-template`.
   */
  set setTemplate(template) {
    if (template && template !== "") {
      this.useTemplateFromInput = true;
      this.addStyleClass(template);
    } else if (this.useTemplateFromInput) {
      this.clear();
    }
  }
  constructor(pageLayoutService, elementRef, templateRef, cd) {
    this.pageLayoutService = pageLayoutService;
    this.elementRef = elementRef;
    this.templateRef = templateRef;
    this.cd = cd;
    this.subscription = new Subscription();
  }
  ngOnInit() {
    if (!this.useTemplateFromInput) {
      this.subscription.add(this.pageLayoutService.templateName$.subscribe((template) => this.addStyleClass(template)));
    }
  }
  /**
   * Adds the page template as a style class to the given element. If any
   * page template was added before, we clean it up.
   *
   * We'll not use hostBinding for the style class, as it will potential drop
   * an existing class name on the host. This is why we need to work with
   * the lower level change detection api.
   */
  addStyleClass(template, el) {
    this.clear(el);
    if (template) {
      this.currentTemplate = template;
      (el ?? this.host).classList.add(this.currentTemplate);
      this.cd.markForCheck();
    }
  }
  /**
   * Cleans up the class host binding, if a template class was assigned before.
   */
  clear(el) {
    if (this.currentTemplate) {
      (el ?? this.host).classList?.remove(this.currentTemplate);
      this.cd.markForCheck();
    }
  }
  /**
   * Returns the host element (`HTMLElement`).
   *
   * If the directive is used on an `ng-template`, we take the parent element,
   * to ensure that we're not ending up with a comment.
   */
  get host() {
    return !!this.templateRef ? this.templateRef.elementRef.nativeElement.parentElement : this.elementRef.nativeElement;
  }
  ngOnDestroy() {
    this.subscription.unsubscribe();
  }
  static {
    this.ɵfac = function PageTemplateDirective_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _PageTemplateDirective)(ɵɵdirectiveInject(PageLayoutService), ɵɵdirectiveInject(ElementRef), ɵɵdirectiveInject(TemplateRef, 8), ɵɵdirectiveInject(ChangeDetectorRef));
    };
  }
  static {
    this.ɵdir = ɵɵdefineDirective({
      type: _PageTemplateDirective,
      selectors: [["", "cxPageTemplateStyle", ""]],
      inputs: {
        setTemplate: [0, "cxPageTemplateStyle", "setTemplate"]
      },
      standalone: false
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PageTemplateDirective, [{
    type: Directive,
    args: [{
      selector: "[cxPageTemplateStyle]",
      standalone: false
    }]
  }], () => [{
    type: PageLayoutService
  }, {
    type: ElementRef
  }, {
    type: TemplateRef,
    decorators: [{
      type: Optional
    }]
  }, {
    type: ChangeDetectorRef
  }], {
    setTemplate: [{
      type: Input,
      args: ["cxPageTemplateStyle"]
    }]
  });
})();
var PageLayoutComponent = class _PageLayoutComponent {
  set section(value) {
    this.section$.next(value);
  }
  constructor(pageLayoutService) {
    this.pageLayoutService = pageLayoutService;
    this.section$ = new BehaviorSubject(void 0);
    this.templateName$ = this.pageLayoutService.templateName$;
    this.layoutName$ = this.section$.pipe(switchMap((section) => section ? of(section) : this.templateName$));
    this.slots$ = this.section$.pipe(switchMap((section) => this.pageLayoutService.getSlots(section)));
    this.pageFoldSlot$ = this.templateName$.pipe(switchMap((templateName) => this.pageLayoutService.getPageFoldSlot(templateName)), distinctUntilChanged());
    useFeatureStyles("disableCxPageSlotMarginAnimation");
  }
  static {
    this.ɵfac = function PageLayoutComponent_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _PageLayoutComponent)(ɵɵdirectiveInject(PageLayoutService));
    };
  }
  static {
    this.ɵcmp = ɵɵdefineComponent({
      type: _PageLayoutComponent,
      selectors: [["cx-page-layout"]],
      inputs: {
        section: "section"
      },
      standalone: false,
      ngContentSelectors: _c12,
      decls: 2,
      vars: 3,
      consts: [[4, "ngIf"], [3, "cxPageTemplateStyle", "cxOutlet", "cxOutletContext"], [3, "position", "isPageFold", 4, "ngFor", "ngForOf"], [3, "position", "isPageFold"]],
      template: function PageLayoutComponent_Template(rf, ctx) {
        if (rf & 1) {
          ɵɵprojectionDef();
          ɵɵtemplate(0, PageLayoutComponent_0_Template, 1, 7, null, 0);
          ɵɵpipe(1, "async");
        }
        if (rf & 2) {
          ɵɵproperty("ngIf", ɵɵpipeBind1(1, 1, ctx.layoutName$));
        }
      },
      dependencies: [NgForOf, NgIf, OutletDirective, PageSlotComponent, PageTemplateDirective, AsyncPipe],
      encapsulation: 2,
      changeDetection: 0
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PageLayoutComponent, [{
    type: Component,
    args: [{
      selector: "cx-page-layout",
      changeDetection: ChangeDetectionStrategy.OnPush,
      standalone: false,
      template: '<ng-template\n  *ngIf="layoutName$ | async as layoutName"\n  [cxPageTemplateStyle]="layoutName"\n  [cxOutlet]="layoutName"\n  [cxOutletContext]="{\n    templateName$: templateName$,\n    slots$: slots$,\n    section$: section$,\n  }"\n>\n  <ng-content></ng-content>\n\n  <cx-page-slot\n    *ngFor="let slot of slots$ | async"\n    [position]="slot"\n    [isPageFold]="slot === (pageFoldSlot$ | async)"\n  ></cx-page-slot>\n</ng-template>\n'
    }]
  }], () => [{
    type: PageLayoutService
  }], {
    section: [{
      type: Input
    }]
  });
})();
var CmsGuardsService = class _CmsGuardsService {
  constructor(cmsComponentsService, unifiedInjector) {
    this.cmsComponentsService = cmsComponentsService;
    this.unifiedInjector = unifiedInjector;
    this.featureConfigService = inject(FeatureConfigService);
    this.guardsComposer = inject(GuardsComposer);
  }
  /**
   * Executes all guards for the given `componentTypes` and returns an Observable that:
   * - emits `true` if all those guards pass (emit `true`)
   * - emits `false` or `UrlTree` immediately if any those guards fails (returns `false` or `UrlTree`)
   */
  cmsPageCanActivate(componentTypes, route, state) {
    const guards = this.cmsComponentsService.getGuards(componentTypes);
    const guardsInstances = guards.map((guardClass) => getLastValueSync(this.unifiedInjector.get(guardClass))).filter(isCanActivate);
    return this.guardsComposer.canActivate(guardsInstances, route, state);
  }
  /**
   * Executes a guard's `canActivate` method with `route` and `state`,
   * returning its result as an Observable.
   *
   * Converts non-Observable results (Promise or static value) into Observable.
   *
   * NOTE: It injects the guard on demand from the {@link UnifiedInjector}
   *
   * @deprecated since 2211.24 - instead use the util {@link GuardsComposer}
   */
  canActivateGuard(guardClass, route, state) {
    const guard = getLastValueSync(this.unifiedInjector.get(guardClass));
    if (isCanActivate(guard)) {
      return wrapIntoObservable(guard.canActivate(route, state)).pipe(first());
    } else {
      throw new Error("Invalid CanActivate guard in cmsMapping");
    }
  }
  static {
    this.ɵfac = function CmsGuardsService_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _CmsGuardsService)(ɵɵinject(CmsComponentsService), ɵɵinject(UnifiedInjector));
    };
  }
  static {
    this.ɵprov = ɵɵdefineInjectable({
      token: _CmsGuardsService,
      factory: _CmsGuardsService.ɵfac,
      providedIn: "root"
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(CmsGuardsService, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], () => [{
    type: CmsComponentsService
  }, {
    type: UnifiedInjector
  }], null);
})();
function isCanActivate(guard) {
  return guard && typeof guard.canActivate === "function";
}
var CmsRoutesImplService = class _CmsRoutesImplService {
  constructor(router, cmsComponentsService, cmsGuardsService) {
    this.router = router;
    this.cmsComponentsService = cmsComponentsService;
    this.cmsGuardsService = cmsGuardsService;
  }
  cmsRouteExists(url) {
    const isCmsDrivenRoute = url.startsWith("/");
    if (!isCmsDrivenRoute) {
      return false;
    }
    const routePath = url.substring(1);
    return isCmsDrivenRoute && !!this.router.config.find((route) => route.data && route.data.cxCmsRouteContext && route.path === routePath);
  }
  /**
   * Contains Cms driven routing logic intended for use use in guards, especially in canActivate method.
   *
   * Will return true, when logic wont have to modify routing (so canActivate could be easily resolved to true)
   * or will return false, when routing configuration was updated and redirection to newly generated route was initiated.
   *
   * @param pageContext
   * @param currentUrl
   */
  handleCmsRoutesInGuard(pageContext, componentTypes, currentUrl, currentPageLabel) {
    if (this.cmsRouteExists(currentPageLabel)) {
      return true;
    }
    const childRoutesConfig = this.cmsComponentsService.getChildRoutes(componentTypes);
    if (childRoutesConfig?.children?.length) {
      if (this.updateRouting(pageContext, currentPageLabel, childRoutesConfig)) {
        this.router.navigateByUrl(currentUrl);
        return false;
      }
    }
    return true;
  }
  updateRouting(pageContext, pageLabel, childRoutesConfig) {
    if (pageContext.type === PageType.CONTENT_PAGE && pageLabel.startsWith("/") && pageLabel.length > 1) {
      const children = this.wrapCmsGuardsRecursively(childRoutesConfig.children ?? []);
      const newRoute = {
        path: pageLabel.substring(1),
        component: PageLayoutComponent,
        children,
        data: deepMerge({}, childRoutesConfig?.parent?.data ?? {}, {
          cxCmsRouteContext: {
            type: pageContext.type,
            id: pageLabel
          }
        })
      };
      this.router.resetConfig([newRoute, ...this.router.config]);
      return true;
    }
    return false;
  }
  /**
   * Traverses recursively the given Routes and wraps each `canActivate`
   * guard of each Route with a special `CanActivateFn` function.
   *
   * This special wrapper function allows for resolving
   * those guards by the Angular Router using the `UnifiedInjector`
   * instead of only root injector.
   *
   * This allows Angular Router to inject the guards (and their dependencies)
   * even when they are provided only in a child injector of a lazy-loaded module.
   */
  wrapCmsGuardsRecursively(routes) {
    return routes.map((route) => {
      if (route.children) {
        route.children = this.wrapCmsGuardsRecursively(route.children);
      }
      if (route?.canActivate?.length) {
        const canActivate = route.canActivate.map((guard) => this.wrapCmsGuard(guard));
        return __spreadProps(__spreadValues({}, route), {
          canActivate
        });
      }
      return route;
    });
  }
  /**
   * Returns a wrapper function `CanActivateFn` (https://angular.io/api/router/CanActivateFn)
   * that injects the given guard instance and runs its method `.canActivate()`.
   *
   * It allows to inject the guard's instance (and it's dependencies)
   * even if it's 'provided only in a child injector of a lazy-loaded module.
   */
  wrapCmsGuard(guardClass) {
    return (route, state) => {
      return this.cmsGuardsService.canActivateGuard(guardClass, route, state);
    };
  }
  static {
    this.ɵfac = function CmsRoutesImplService_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _CmsRoutesImplService)(ɵɵinject(Router), ɵɵinject(CmsComponentsService), ɵɵinject(CmsGuardsService));
    };
  }
  static {
    this.ɵprov = ɵɵdefineInjectable({
      token: _CmsRoutesImplService,
      factory: _CmsRoutesImplService.ɵfac,
      providedIn: "root"
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(CmsRoutesImplService, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], () => [{
    type: Router
  }, {
    type: CmsComponentsService
  }, {
    type: CmsGuardsService
  }], null);
})();
var CmsRoutesService = class _CmsRoutesService {
  static {
    this.ɵfac = function CmsRoutesService_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _CmsRoutesService)();
    };
  }
  static {
    this.ɵprov = ɵɵdefineInjectable({
      token: _CmsRoutesService,
      factory: function CmsRoutesService_Factory(__ngFactoryType__) {
        let __ngConditionalFactory__ = null;
        if (__ngFactoryType__) {
          __ngConditionalFactory__ = new (__ngFactoryType__ || _CmsRoutesService)();
        } else {
          __ngConditionalFactory__ = ɵɵinject(CmsRoutesImplService);
        }
        return __ngConditionalFactory__;
      },
      providedIn: "root"
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(CmsRoutesService, [{
    type: Injectable,
    args: [{
      providedIn: "root",
      useExisting: CmsRoutesImplService
    }]
  }], null, null);
})();
var CmsI18nService = class _CmsI18nService {
  constructor(cmsComponentsService, translation, translationChunk) {
    this.cmsComponentsService = cmsComponentsService;
    this.translation = translation;
    this.translationChunk = translationChunk;
  }
  loadForComponents(componentTypes) {
    const i18nKeys = this.cmsComponentsService.getI18nKeys(componentTypes);
    const i18nChunks = /* @__PURE__ */ new Set();
    for (const key of i18nKeys) {
      i18nChunks.add(this.translationChunk.getChunkNameForKey(key));
    }
    this.translation.loadChunks(Array.from(i18nChunks));
  }
  static {
    this.ɵfac = function CmsI18nService_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _CmsI18nService)(ɵɵinject(CmsComponentsService), ɵɵinject(TranslationService), ɵɵinject(TranslationChunkService));
    };
  }
  static {
    this.ɵprov = ɵɵdefineInjectable({
      token: _CmsI18nService,
      factory: _CmsI18nService.ɵfac,
      providedIn: "root"
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(CmsI18nService, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], () => [{
    type: CmsComponentsService
  }, {
    type: TranslationService
  }, {
    type: TranslationChunkService
  }], null);
})();
var CmsPageGuardService = class _CmsPageGuardService {
  constructor(semanticPathService, cmsService, cmsRoutes, cmsI18n, cmsGuards, cmsComponentsService, routing) {
    this.semanticPathService = semanticPathService;
    this.cmsService = cmsService;
    this.cmsRoutes = cmsRoutes;
    this.cmsI18n = cmsI18n;
    this.cmsGuards = cmsGuards;
    this.cmsComponentsService = cmsComponentsService;
    this.routing = routing;
  }
  /**
   * Takes CMS components types in the current CMS page, triggers (configurable) side effects and returns a boolean - whether the route can be activated.
   *
   * Based on `cmsComponents` config for the components in the page:
   * - Evaluates components' guards; if one of them emits false or UrlTree - the route cannot be activated or redirects to the given UrlTree, respectively.
   * - If all components' guards emitted true, then the route can be activated
   * - Then we trigger loading of configured i18n chunks in parallel
   * - And we register the configured children routes of cms components
   *
   * @param pageContext current cms page context
   * @param pageData cms page data
   * @param route activated route snapshot
   * @param state router state snapshot
   *
   * @returns boolean observable - whether the route can be activated
   */
  canActivatePage(pageContext, pageData, route, state) {
    return this.cmsService.getPageComponentTypes(pageContext).pipe(take(1), switchMap((componentTypes) => this.cmsComponentsService.determineMappings(componentTypes)), switchMap((componentTypes) => this.cmsGuards.cmsPageCanActivate(componentTypes, route, state).pipe(withLatestFrom(of(componentTypes)))), tap(([canActivate, componentTypes]) => {
      if (canActivate === true) {
        this.cmsI18n.loadForComponents(componentTypes);
      }
    }), map(([canActivate, componentTypes]) => {
      const pageLabel = pageData.label || pageContext.id;
      if (canActivate === true && !route?.data?.cxCmsRouteContext) {
        return this.cmsRoutes.handleCmsRoutesInGuard(pageContext, componentTypes, state.url, pageLabel);
      }
      return canActivate;
    }));
  }
  /**
   * Activates the "NOT FOUND" cms page.
   *
   * It loads cms page data for the "NOT FOUND" page and puts it in the state of the the requested page label.
   * Then it processes its CMS components with the method `canActivatePage()` of this service. For more, see its docs.
   */
  canActivateNotFoundPage(pageContext, route, state) {
    const notFoundLabel = this.semanticPathService.get("notFound");
    if (!notFoundLabel) {
      return of(false);
    }
    const notFoundCmsPageContext = {
      type: PageType.CONTENT_PAGE,
      id: notFoundLabel
    };
    return this.cmsService.getPage(notFoundCmsPageContext).pipe(switchMap((notFoundPage) => {
      if (notFoundPage) {
        return this.cmsService.getPageIndex(notFoundCmsPageContext).pipe(tap((notFoundIndex) => {
          this.cmsService.setPageFailIndex(pageContext, notFoundIndex);
          this.routing.changeNextPageContext(notFoundCmsPageContext);
        }), switchMap((notFoundIndex) => this.cmsService.getPageIndex(pageContext).pipe(
          // we have to wait for page index update
          filter((index) => index === notFoundIndex)
        )), switchMap(() => this.canActivatePage(pageContext, notFoundPage, route, state)));
      }
      return of(false);
    }));
  }
  static {
    this.ɵfac = function CmsPageGuardService_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _CmsPageGuardService)(ɵɵinject(SemanticPathService), ɵɵinject(CmsService), ɵɵinject(CmsRoutesService), ɵɵinject(CmsI18nService), ɵɵinject(CmsGuardsService), ɵɵinject(CmsComponentsService), ɵɵinject(RoutingService));
    };
  }
  static {
    this.ɵprov = ɵɵdefineInjectable({
      token: _CmsPageGuardService,
      factory: _CmsPageGuardService.ɵfac,
      providedIn: "root"
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(CmsPageGuardService, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], () => [{
    type: SemanticPathService
  }, {
    type: CmsService
  }, {
    type: CmsRoutesService
  }, {
    type: CmsI18nService
  }, {
    type: CmsGuardsService
  }, {
    type: CmsComponentsService
  }, {
    type: RoutingService
  }], null);
})();
var CmsPageGuard = class _CmsPageGuard {
  static {
    this.guardName = "CmsPageGuard";
  }
  constructor(routingService, cmsService, protectedRoutesGuard, service, routingConfig) {
    this.routingService = routingService;
    this.cmsService = cmsService;
    this.protectedRoutesGuard = protectedRoutesGuard;
    this.service = service;
    this.routingConfig = routingConfig;
    this.beforeCmsPageGuardService = inject(BeforeCmsPageGuardService);
  }
  /**
   * Tries to load the CMS page data for the anticipated route and returns:
   * - `true` - if it can be activated
   * - `false` - if it cannot be activated
   * - `UrlTree` - if user should be redirected to a given `UrlTree`
   *
   * If the route can be activated, it fires additional calculations on the CMS components present on this CMS page,
   * based on their configuration (`cmsComponents` config).
   *
   * For more, see docs of the `CmsPageGuardService.canActivatePage`.
   */
  canActivate(route, state) {
    return this.beforeCmsPageGuardService.canActivate(route, state).pipe(switchMap((canActivate) => canActivate === true ? this.routingService.getNextPageContext().pipe(filter(isNotUndefined), take(1), switchMap((pageContext) => this.cmsService.getPage(pageContext, this.shouldReload()).pipe(first(), switchMap((pageData) => pageData ? this.service.canActivatePage(pageContext, pageData, route, state) : this.service.canActivateNotFoundPage(pageContext, route, state))))) : of(canActivate)));
  }
  /**
   * Returns whether we should reload the CMS page data, even when it was loaded before.
   */
  shouldReload() {
    return this.routingConfig.getLoadStrategy() !== RouteLoadStrategy.ONCE;
  }
  static {
    this.ɵfac = function CmsPageGuard_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _CmsPageGuard)(ɵɵinject(RoutingService), ɵɵinject(CmsService), ɵɵinject(ProtectedRoutesGuard), ɵɵinject(CmsPageGuardService), ɵɵinject(RoutingConfigService));
    };
  }
  static {
    this.ɵprov = ɵɵdefineInjectable({
      token: _CmsPageGuard,
      factory: _CmsPageGuard.ɵfac,
      providedIn: "root"
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(CmsPageGuard, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], () => [{
    type: RoutingService
  }, {
    type: CmsService
  }, {
    type: ProtectedRoutesGuard
  }, {
    type: CmsPageGuardService
  }, {
    type: RoutingConfigService
  }], null);
})();
var DefaultComponentHandler = class _DefaultComponentHandler {
  hasMatch(componentMapping) {
    return typeof componentMapping.component === "function";
  }
  getPriority() {
    return Priority.FALLBACK;
  }
  launcher(componentMapping, viewContainerRef, elementInjector, module) {
    return new Observable((subscriber) => {
      let componentRef;
      const injector = elementInjector ?? viewContainerRef.injector;
      const dispose = () => {
        if (componentRef) {
          componentRef.destroy();
        }
      };
      const factory = this.getComponentFactory(injector, componentMapping.component);
      if (factory) {
        componentRef = viewContainerRef.createComponent(factory, void 0, injector, void 0, module);
        subscriber.next({
          elementRef: componentRef.location,
          componentRef
        });
      }
      return dispose;
    });
  }
  getComponentFactory(injector, component) {
    if (!component) {
      return null;
    }
    const factory = injector.get(ComponentFactoryResolver$1).resolveComponentFactory(component);
    return factory;
  }
  static {
    this.ɵfac = function DefaultComponentHandler_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _DefaultComponentHandler)();
    };
  }
  static {
    this.ɵprov = ɵɵdefineInjectable({
      token: _DefaultComponentHandler,
      factory: _DefaultComponentHandler.ɵfac,
      providedIn: "root"
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(DefaultComponentHandler, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], null, null);
})();
var LazyComponentHandler = class _LazyComponentHandler {
  constructor(defaultHandler) {
    this.defaultHandler = defaultHandler;
  }
  /**
   * We want to mach dynamic import signature () => import('')
   */
  hasMatch(componentMapping) {
    return typeof componentMapping.component === "function" && this.isNotClass(componentMapping.component);
  }
  isNotClass(symbol) {
    const signature = symbol.toString().substring(0, 20).replace(" ", "");
    return signature.startsWith("function()") || signature.startsWith("()=>");
  }
  getPriority() {
    return Priority.LOW;
  }
  launcher(componentMapping, viewContainerRef, elementInjector, module) {
    return from(componentMapping.component()).pipe(switchMap((component) => this.defaultHandler.launcher(__spreadProps(__spreadValues({}, componentMapping), {
      component
    }), viewContainerRef, elementInjector, module)));
  }
  static {
    this.ɵfac = function LazyComponentHandler_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _LazyComponentHandler)(ɵɵinject(DefaultComponentHandler));
    };
  }
  static {
    this.ɵprov = ɵɵdefineInjectable({
      token: _LazyComponentHandler,
      factory: _LazyComponentHandler.ɵfac,
      providedIn: "root"
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(LazyComponentHandler, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], () => [{
    type: DefaultComponentHandler
  }], null);
})();
var InnerComponentsHostDirective = class _InnerComponentsHostDirective {
  constructor(data, vcr, cmsComponentsService, injector, dynamicAttributeService, renderer, componentHandler, cmsInjector, eventService) {
    this.data = data;
    this.vcr = vcr;
    this.cmsComponentsService = cmsComponentsService;
    this.injector = injector;
    this.dynamicAttributeService = dynamicAttributeService;
    this.renderer = renderer;
    this.componentHandler = componentHandler;
    this.cmsInjector = cmsInjector;
    this.eventService = eventService;
    this.innerComponents$ = this.data.data$.pipe(map((data2) => data2?.composition?.inner ?? []), distinctUntilChanged());
    this.componentWrappers = [];
  }
  ngOnInit() {
    this.subscription = this.innerComponents$.subscribe((x) => {
      this.renderComponents(x);
    });
  }
  renderComponents(components) {
    this.clearComponents();
    components.forEach((component) => this.renderComponent(component));
  }
  renderComponent(component) {
    const componentWrapper = new ComponentWrapperDirective(this.vcr, this.cmsComponentsService, this.injector, this.dynamicAttributeService, this.renderer, this.componentHandler, this.cmsInjector, this.eventService);
    componentWrapper.cxComponentWrapper = {
      flexType: component,
      uid: ""
    };
    componentWrapper.ngOnInit();
    this.componentWrappers.push(componentWrapper);
  }
  clearComponents() {
    this.componentWrappers.forEach((wrapper) => wrapper.ngOnDestroy());
    this.componentWrappers = [];
  }
  ngOnDestroy() {
    this.subscription?.unsubscribe();
    this.clearComponents();
  }
  static {
    this.ɵfac = function InnerComponentsHostDirective_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _InnerComponentsHostDirective)(ɵɵdirectiveInject(CmsComponentData), ɵɵdirectiveInject(ViewContainerRef), ɵɵdirectiveInject(CmsComponentsService), ɵɵdirectiveInject(Injector), ɵɵdirectiveInject(DynamicAttributeService), ɵɵdirectiveInject(Renderer2), ɵɵdirectiveInject(ComponentHandlerService), ɵɵdirectiveInject(CmsInjectorService), ɵɵdirectiveInject(EventService));
    };
  }
  static {
    this.ɵdir = ɵɵdefineDirective({
      type: _InnerComponentsHostDirective,
      selectors: [["", "cxInnerComponentsHost", ""]],
      standalone: false
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(InnerComponentsHostDirective, [{
    type: Directive,
    args: [{
      selector: "[cxInnerComponentsHost]",
      standalone: false
    }]
  }], () => [{
    type: CmsComponentData
  }, {
    type: ViewContainerRef
  }, {
    type: CmsComponentsService
  }, {
    type: Injector
  }, {
    type: DynamicAttributeService
  }, {
    type: Renderer2
  }, {
    type: ComponentHandlerService
  }, {
    type: CmsInjectorService
  }, {
    type: EventService
  }], null);
})();
var PageComponentModule = class _PageComponentModule {
  static forRoot() {
    return {
      ngModule: _PageComponentModule,
      providers: [{
        provide: ComponentHandler,
        useExisting: DefaultComponentHandler,
        multi: true
      }, {
        provide: ComponentHandler,
        useExisting: LazyComponentHandler,
        multi: true
      }]
    };
  }
  static {
    this.ɵfac = function PageComponentModule_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _PageComponentModule)();
    };
  }
  static {
    this.ɵmod = ɵɵdefineNgModule({
      type: _PageComponentModule,
      declarations: [ComponentWrapperDirective, InnerComponentsHostDirective],
      imports: [CommonModule],
      exports: [ComponentWrapperDirective, InnerComponentsHostDirective]
    });
  }
  static {
    this.ɵinj = ɵɵdefineInjector({
      imports: [CommonModule]
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PageComponentModule, [{
    type: NgModule,
    args: [{
      imports: [CommonModule],
      declarations: [ComponentWrapperDirective, InnerComponentsHostDirective],
      exports: [ComponentWrapperDirective, InnerComponentsHostDirective]
    }]
  }], null, null);
})();
var PageSlotModule = class _PageSlotModule {
  // instantiate PageSlotService ASAP, so it can examine SSR pre-rendered DOM
  constructor(_pageSlot) {
  }
  static {
    this.ɵfac = function PageSlotModule_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _PageSlotModule)(ɵɵinject(PageSlotService));
    };
  }
  static {
    this.ɵmod = ɵɵdefineNgModule({
      type: _PageSlotModule,
      declarations: [PageSlotComponent],
      imports: [CommonModule, OutletModule, PageComponentModule],
      exports: [PageSlotComponent]
    });
  }
  static {
    this.ɵinj = ɵɵdefineInjector({
      imports: [CommonModule, OutletModule, PageComponentModule]
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PageSlotModule, [{
    type: NgModule,
    args: [{
      imports: [CommonModule, OutletModule, PageComponentModule],
      declarations: [PageSlotComponent],
      exports: [PageSlotComponent]
    }]
  }], () => [{
    type: PageSlotService
  }], null);
})();
var PageLayoutModule = class _PageLayoutModule {
  static {
    this.ɵfac = function PageLayoutModule_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _PageLayoutModule)();
    };
  }
  static {
    this.ɵmod = ɵɵdefineNgModule({
      type: _PageLayoutModule,
      declarations: [PageLayoutComponent, PageTemplateDirective],
      imports: [CommonModule, OutletModule, PageSlotModule],
      exports: [PageLayoutComponent, PageTemplateDirective]
    });
  }
  static {
    this.ɵinj = ɵɵdefineInjector({
      imports: [CommonModule, OutletModule, PageSlotModule]
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PageLayoutModule, [{
    type: NgModule,
    args: [{
      imports: [CommonModule, OutletModule, PageSlotModule],
      declarations: [PageLayoutComponent, PageTemplateDirective],
      exports: [PageLayoutComponent, PageTemplateDirective]
    }]
  }], null, null);
})();
var PWAModuleConfig = class _PWAModuleConfig {
  static {
    this.ɵfac = function PWAModuleConfig_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _PWAModuleConfig)();
    };
  }
  static {
    this.ɵprov = ɵɵdefineInjectable({
      token: _PWAModuleConfig,
      factory: function PWAModuleConfig_Factory(__ngFactoryType__) {
        let __ngConditionalFactory__ = null;
        if (__ngFactoryType__) {
          __ngConditionalFactory__ = new (__ngFactoryType__ || _PWAModuleConfig)();
        } else {
          __ngConditionalFactory__ = ɵɵinject(Config);
        }
        return __ngConditionalFactory__;
      },
      providedIn: "root"
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PWAModuleConfig, [{
    type: Injectable,
    args: [{
      providedIn: "root",
      useExisting: Config
    }]
  }], null, null);
})();
var defaultPWAModuleConfig = {
  pwa: {
    enabled: false,
    addToHomeScreen: false
  }
};
var AddToHomeScreenService = class _AddToHomeScreenService {
  constructor(config, globalMessageService, winRef) {
    this.config = config;
    this.globalMessageService = globalMessageService;
    this.winRef = winRef;
    this.canPrompt = new BehaviorSubject(false);
    this.canPrompt$ = this.canPrompt.asObservable();
    if (this.config.pwa?.addToHomeScreen) {
      this.init();
    }
  }
  init() {
    if (this.winRef.nativeWindow) {
      this.winRef.nativeWindow.addEventListener("beforeinstallprompt", (event) => {
        event.preventDefault();
        this.deferredEvent = event;
        this.enableAddToHomeScreen();
      });
      this.winRef.nativeWindow.addEventListener("appinstalled", () => {
        this.globalMessageService.add({
          key: "pwa.addedToHomeScreen"
        }, GlobalMessageType.MSG_TYPE_CONFIRMATION);
        this.disableAddToHomeScreen();
        this.deferredEvent = null;
      });
    }
  }
  enableAddToHomeScreen() {
    this.canPrompt.next(true);
  }
  disableAddToHomeScreen() {
    this.canPrompt.next(false);
  }
  firePrompt() {
    if (this.deferredEvent) {
      this.deferredEvent.prompt();
    }
  }
  static {
    this.ɵfac = function AddToHomeScreenService_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _AddToHomeScreenService)(ɵɵinject(PWAModuleConfig), ɵɵinject(GlobalMessageService), ɵɵinject(WindowRef));
    };
  }
  static {
    this.ɵprov = ɵɵdefineInjectable({
      token: _AddToHomeScreenService,
      factory: _AddToHomeScreenService.ɵfac,
      providedIn: "root"
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(AddToHomeScreenService, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], () => [{
    type: PWAModuleConfig
  }, {
    type: GlobalMessageService
  }, {
    type: WindowRef
  }], null);
})();
var AddToHomeScreenComponent = class _AddToHomeScreenComponent {
  constructor(addToHomeScreenService) {
    this.addToHomeScreenService = addToHomeScreenService;
  }
  ngOnInit() {
    this.canPrompt$ = this.addToHomeScreenService.canPrompt$;
  }
  prompt() {
    this.addToHomeScreenService.firePrompt();
  }
  static {
    this.ɵfac = function AddToHomeScreenComponent_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _AddToHomeScreenComponent)(ɵɵdirectiveInject(AddToHomeScreenService));
    };
  }
  static {
    this.ɵdir = ɵɵdefineDirective({
      type: _AddToHomeScreenComponent
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(AddToHomeScreenComponent, [{
    type: Directive
  }], () => [{
    type: AddToHomeScreenService
  }], null);
})();
var AddToHomeScreenBannerComponent = class _AddToHomeScreenBannerComponent extends AddToHomeScreenComponent {
  constructor(addToHomeScreenService) {
    super(addToHomeScreenService);
    this.addToHomeScreenService = addToHomeScreenService;
  }
  static {
    this.ɵfac = function AddToHomeScreenBannerComponent_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _AddToHomeScreenBannerComponent)(ɵɵdirectiveInject(AddToHomeScreenService));
    };
  }
  static {
    this.ɵcmp = ɵɵdefineComponent({
      type: _AddToHomeScreenBannerComponent,
      selectors: [["cx-add-to-home-screen-banner"]],
      standalone: false,
      features: [ɵɵInheritDefinitionFeature],
      decls: 2,
      vars: 3,
      consts: [[4, "ngIf"], [1, "cx-add-to-home-screen-banner"], [1, "cx-add-to-home-screen-banner-inner"], [1, "btn", "btn-primary", 3, "click"]],
      template: function AddToHomeScreenBannerComponent_Template(rf, ctx) {
        if (rf & 1) {
          ɵɵtemplate(0, AddToHomeScreenBannerComponent_div_0_Template, 16, 12, "div", 0);
          ɵɵpipe(1, "async");
        }
        if (rf & 2) {
          ɵɵproperty("ngIf", ɵɵpipeBind1(1, 1, ctx.canPrompt$));
        }
      },
      dependencies: [NgIf, AsyncPipe, TranslatePipe],
      encapsulation: 2
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(AddToHomeScreenBannerComponent, [{
    type: Component,
    args: [{
      selector: "cx-add-to-home-screen-banner",
      standalone: false,
      template: `<div *ngIf="canPrompt$ | async">
  <div class="cx-add-to-home-screen-banner">
    <div class="cx-add-to-home-screen-banner-inner">
      <p>
        {{ 'pwa.addToHomeScreenDescription' | cxTranslate }}
      </p>
      <ul>
        <li>{{ 'pwa.noInstallationNeeded' | cxTranslate }}</li>
        <li>{{ 'pwa.fastAccessToApplication' | cxTranslate }}</li>
      </ul>
      <button (click)="prompt()" class="btn btn-primary">
        {{ 'pwa.addToHomeScreen' | cxTranslate }}
      </button>
    </div>
  </div>
</div>
`
    }]
  }], () => [{
    type: AddToHomeScreenService
  }], null);
})();
var AddToHomeScreenBtnComponent = class _AddToHomeScreenBtnComponent extends AddToHomeScreenComponent {
  constructor(addToHomeScreenService) {
    super(addToHomeScreenService);
    this.addToHomeScreenService = addToHomeScreenService;
  }
  static {
    this.ɵfac = function AddToHomeScreenBtnComponent_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _AddToHomeScreenBtnComponent)(ɵɵdirectiveInject(AddToHomeScreenService));
    };
  }
  static {
    this.ɵcmp = ɵɵdefineComponent({
      type: _AddToHomeScreenBtnComponent,
      selectors: [["cx-add-to-home-screen-btn"]],
      standalone: false,
      features: [ɵɵInheritDefinitionFeature],
      ngContentSelectors: _c12,
      decls: 3,
      vars: 3,
      consts: [[3, "click"], [4, "ngIf"]],
      template: function AddToHomeScreenBtnComponent_Template(rf, ctx) {
        if (rf & 1) {
          ɵɵprojectionDef();
          ɵɵelementStart(0, "span", 0);
          ɵɵlistener("click", function AddToHomeScreenBtnComponent_Template_span_click_0_listener() {
            return ctx.prompt();
          });
          ɵɵtemplate(1, AddToHomeScreenBtnComponent_ng_content_1_Template, 1, 0, "ng-content", 1);
          ɵɵpipe(2, "async");
          ɵɵelementEnd();
        }
        if (rf & 2) {
          ɵɵadvance();
          ɵɵproperty("ngIf", ɵɵpipeBind1(2, 1, ctx.canPrompt$));
        }
      },
      dependencies: [NgIf, AsyncPipe],
      encapsulation: 2
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(AddToHomeScreenBtnComponent, [{
    type: Component,
    args: [{
      selector: "cx-add-to-home-screen-btn",
      standalone: false,
      template: '<span (click)="prompt()">\n  <ng-content *ngIf="canPrompt$ | async"></ng-content>\n</span>\n'
    }]
  }], () => [{
    type: AddToHomeScreenService
  }], null);
})();
function pwaConfigurationFactory(pwaConfig) {
  return {
    enabled: !isDevMode() && pwaConfig.pwa?.enabled || false
  };
}
function pwaFactory(addToHomeScreenService) {
  const result = () => addToHomeScreenService;
  return result;
}
var PwaModule = class _PwaModule {
  static {
    this.ɵfac = function PwaModule_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _PwaModule)();
    };
  }
  static {
    this.ɵmod = ɵɵdefineNgModule({
      type: _PwaModule,
      declarations: [AddToHomeScreenBtnComponent, AddToHomeScreenBannerComponent],
      imports: [CommonModule, ServiceWorkerModule, I18nModule],
      exports: [AddToHomeScreenBtnComponent, AddToHomeScreenBannerComponent]
    });
  }
  static {
    this.ɵinj = ɵɵdefineInjector({
      providers: [provideDefaultConfig(defaultPWAModuleConfig), {
        provide: SwRegistrationOptions,
        useFactory: pwaConfigurationFactory,
        deps: [Config]
      }, {
        provide: APP_INITIALIZER,
        useFactory: pwaFactory,
        deps: [AddToHomeScreenService],
        multi: true
      }],
      imports: [CommonModule, ServiceWorkerModule.register("ngsw-worker.js"), I18nModule]
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PwaModule, [{
    type: NgModule,
    args: [{
      imports: [CommonModule, ServiceWorkerModule.register("ngsw-worker.js"), I18nModule],
      providers: [provideDefaultConfig(defaultPWAModuleConfig), {
        provide: SwRegistrationOptions,
        useFactory: pwaConfigurationFactory,
        deps: [Config]
      }, {
        provide: APP_INITIALIZER,
        useFactory: pwaFactory,
        deps: [AddToHomeScreenService],
        multi: true
      }],
      declarations: [AddToHomeScreenBtnComponent, AddToHomeScreenBannerComponent],
      exports: [AddToHomeScreenBtnComponent, AddToHomeScreenBannerComponent]
    }]
  }], null, null);
})();
var cmsRoute = {
  path: "**",
  canActivate: [CmsPageGuard],
  component: PageLayoutComponent
};
function addCmsRoute(injector) {
  const result = () => {
    const router = injector.get(Router);
    router.config.push(cmsRoute);
  };
  return result;
}
var CmsRouteModule = class _CmsRouteModule {
  static {
    this.ɵfac = function CmsRouteModule_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _CmsRouteModule)();
    };
  }
  static {
    this.ɵmod = ɵɵdefineNgModule({
      type: _CmsRouteModule
    });
  }
  static {
    this.ɵinj = ɵɵdefineInjector({
      providers: [{
        provide: APP_INITIALIZER,
        multi: true,
        deps: [Injector],
        useFactory: addCmsRoute
      }]
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(CmsRouteModule, [{
    type: NgModule,
    args: [{
      providers: [{
        provide: APP_INITIALIZER,
        multi: true,
        deps: [Injector],
        useFactory: addCmsRoute
      }]
    }]
  }], null, null);
})();
var defaultRoutesConfigFactory = () => {
  const featureToggles = inject(FeatureToggles);
  const enableProductPageRouteAllowsNoProductName = featureToggles.defaultProductPageRouteAllowsNoProductName;
  const routingConfig = {
    routing: {
      routes: {
        home: {
          paths: [""]
        },
        notFound: {
          paths: ["not-found"]
        },
        // semantic links for login related pages
        login: {
          /*
           * New auth flow requires 2 paths for login trigger and login form
           * where we are redirected from oauth server.
           * Legacy path will stay, new one is updated.           *
           */
          paths: [featureToggles.authorizationCodeFlowByDefault ? "sign-in" : "login"],
          protected: false,
          authFlow: true
        },
        verifyToken: {
          paths: ["login/verify-token"],
          protected: false,
          authFlow: true
        },
        verifyTokenForRegistration: {
          paths: ["register/verify-token"],
          protected: false,
          authFlow: true
        },
        register: {
          paths: ["login/register"],
          protected: false,
          authFlow: true
        },
        forgotPassword: {
          paths: ["login/forgot-password"],
          protected: false,
          authFlow: true
        },
        resetPassword: {
          paths: ["login/pw/change"],
          protected: false,
          authFlow: true
        },
        logout: {
          paths: ["logout"],
          protected: false,
          authFlow: true
        },
        // plp routes
        search: {
          paths: ["search/:query"]
        },
        category: {
          paths: ["category/:categoryCode"],
          paramsMapping: {
            categoryCode: "code"
          }
        },
        brand: {
          paths: ["Brands/:brandName/c/:brandCode"]
        },
        // pdp routes
        product: {
          paths: enableProductPageRouteAllowsNoProductName ? ["product/:productCode/:name", "product/:productCode"] : ["product/:productCode/:name"],
          paramsMapping: {
            productCode: "code"
          }
        },
        termsAndConditions: {
          paths: ["terms-and-conditions"]
        },
        coupons: {
          paths: ["my-account/coupons"]
        },
        couponClaim: {
          paths: ["my-account/coupon/claim/:couponCode"],
          paramsMapping: {
            couponCode: "code"
          }
        },
        myInterests: {
          paths: ["my-account/my-interests"]
        },
        notificationPreference: {
          paths: ["my-account/notification-preference"]
        }
      }
    }
  };
  if (featureToggles.authorizationCodeFlowByDefault) {
    routingConfig.routing.routes["loginForm"] = {
      paths: ["login"],
      protected: false,
      authFlow: true
    };
  }
  return routingConfig;
};
var RoutingModule2 = class _RoutingModule {
  static forRoot() {
    return {
      ngModule: _RoutingModule,
      providers: [provideDefaultConfigFactory(defaultRoutesConfigFactory), {
        provide: BEFORE_CMS_PAGE_GUARD,
        useExisting: ProtectedRoutesGuard,
        multi: true
      }]
    };
  }
  static {
    this.ɵfac = function RoutingModule_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _RoutingModule)();
    };
  }
  static {
    this.ɵmod = ɵɵdefineNgModule({
      type: _RoutingModule,
      imports: [RoutingModule, CmsRouteModule]
    });
  }
  static {
    this.ɵinj = ɵɵdefineInjector({
      imports: [RoutingModule.forRoot(), CmsRouteModule]
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(RoutingModule2, [{
    type: NgModule,
    args: [{
      imports: [RoutingModule.forRoot(), CmsRouteModule]
    }]
  }], null, null);
})();
function getSuffixUrlMatcher({
  marker,
  paramName,
  precedingParamName
}) {
  precedingParamName = precedingParamName || "param";
  const matcher = function suffixUrlMatcher(segments) {
    const markerIndex = findLastIndex(segments, ({
      path
    }) => path === marker);
    const isMarkerLastSegment = markerIndex === segments.length - 1;
    if (markerIndex === -1 || isMarkerLastSegment) {
      return null;
    }
    const paramIndex = markerIndex + 1;
    const posParams = {
      [paramName]: segments[paramIndex]
    };
    for (let i = 0; i < markerIndex; i++) {
      posParams[`${precedingParamName}${i}`] = segments[i];
    }
    return {
      consumed: segments.slice(0, paramIndex + 1),
      posParams
    };
  };
  if (isDevMode()) {
    matcher["_suffixRouteConfig"] = {
      marker,
      paramName,
      precedingParamName
    };
  }
  return matcher;
}
function findLastIndex(elements, predicate) {
  for (let index = elements.length - 1; index >= 0; index--) {
    if (predicate(elements[index])) {
      return index;
    }
  }
  return -1;
}
var PageMetaLinkService = class _PageMetaLinkService {
  constructor(winRef, rendererFactory) {
    this.winRef = winRef;
    this.rendererFactory = rendererFactory;
    this.document = inject(DOCUMENT);
  }
  /**
   * Adds a canonical link element to the document head.
   *
   * If an id is provided, the link will be updated.
   * If no url is provided, the link element will be deleted.
   */
  setCanonicalLink(url) {
    let link = this.winRef.document.querySelector('link[rel="canonical"]');
    if (!url) {
      link?.remove();
      return;
    }
    if (!link) {
      link = this.renderer.createElement("link");
      link.rel = "canonical";
      link.href = url;
      this.renderer.appendChild(this.winRef.document.head, link);
    } else {
      link?.setAttribute("href", url);
    }
  }
  addPreconnectLink(url) {
    const existing = this.document.head.querySelector(`link[rel="preconnect"][href="${url}"]`);
    if (existing) {
      return;
    }
    const preconnect = this.renderer.createElement("link");
    this.renderer.setAttribute(preconnect, "rel", "preconnect");
    this.renderer.setAttribute(preconnect, "href", url);
    this.document.head.insertBefore(preconnect, this.document.head.firstChild);
  }
  get renderer() {
    return this.rendererFactory.createRenderer(null, null);
  }
  static {
    this.ɵfac = function PageMetaLinkService_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _PageMetaLinkService)(ɵɵinject(WindowRef), ɵɵinject(RendererFactory2));
    };
  }
  static {
    this.ɵprov = ɵɵdefineInjectable({
      token: _PageMetaLinkService,
      factory: _PageMetaLinkService.ɵfac,
      providedIn: "root"
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PageMetaLinkService, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], () => [{
    type: WindowRef
  }, {
    type: RendererFactory2
  }], null);
})();
var SeoMetaService = class _SeoMetaService {
  constructor(ngTitle, ngMeta, pageMetaService, pageMetaLinkService) {
    this.ngTitle = ngTitle;
    this.ngMeta = ngMeta;
    this.pageMetaService = pageMetaService;
    this.pageMetaLinkService = pageMetaLinkService;
  }
  init() {
    this.subscription = this.pageMetaService.getMeta().pipe(filter(isNotNullable)).subscribe((meta) => this.meta = meta);
  }
  set meta(meta) {
    this.title = meta.title;
    this.description = meta.description;
    this.image = meta.image;
    this.robots = meta.robots;
    this.canonicalUrl = meta.canonicalUrl;
  }
  set title(title) {
    this.ngTitle.setTitle(title || "");
  }
  set description(value) {
    if (value) {
      this.addTag({
        name: "description",
        content: value || ""
      });
    } else {
      this.ngMeta.removeTag('name="description"');
    }
  }
  set image(imageUrl) {
    if (imageUrl) {
      this.addTag({
        name: "og:image",
        content: imageUrl
      });
    } else {
      this.ngMeta.removeTag('name="og:image"');
    }
  }
  set robots(value) {
    if (value && value.length > 0) {
      this.addTag({
        name: "robots",
        content: value.join(", ")
      });
    }
  }
  /**
   * Add the canonical Url to the head of the page.
   *
   * If the canonical url already exists the link is removed. This is quite
   * unlikely though, since canonical links are (typically) only added in SSR.
   */
  set canonicalUrl(url) {
    this.pageMetaLinkService?.setCanonicalLink(url);
  }
  addTag(meta) {
    if (meta.content) {
      this.ngMeta.updateTag(meta);
    }
  }
  ngOnDestroy() {
    if (this.subscription) {
      this.subscription.unsubscribe();
    }
  }
  static {
    this.ɵfac = function SeoMetaService_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _SeoMetaService)(ɵɵinject(Title), ɵɵinject(Meta), ɵɵinject(PageMetaService), ɵɵinject(PageMetaLinkService));
    };
  }
  static {
    this.ɵprov = ɵɵdefineInjectable({
      token: _SeoMetaService,
      factory: _SeoMetaService.ɵfac,
      providedIn: "root"
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(SeoMetaService, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], () => [{
    type: Title
  }, {
    type: Meta
  }, {
    type: PageMetaService
  }, {
    type: PageMetaLinkService
  }], null);
})();
var ImageLoadingStrategy;
(function(ImageLoadingStrategy2) {
  ImageLoadingStrategy2["EAGER"] = "eager";
  ImageLoadingStrategy2["LAZY"] = "lazy";
})(ImageLoadingStrategy || (ImageLoadingStrategy = {}));
var ImageFetchPriority;
(function(ImageFetchPriority2) {
  ImageFetchPriority2["HIGH"] = "high";
  ImageFetchPriority2["LOW"] = "low";
  ImageFetchPriority2["AUTO"] = "auto";
})(ImageFetchPriority || (ImageFetchPriority = {}));
var MediaService = class _MediaService {
  constructor(config) {
    this.config = config;
    this.featureConfigService = inject(FeatureConfigService);
  }
  getMediaBasedOnHTMLElementType(elementType, mediaContainer, format, alt, role) {
    const shouldGetMediaForPictureElement = this.featureConfigService.isEnabled("useExtendedMediaComponentConfiguration") && elementType !== "img";
    return shouldGetMediaForPictureElement ? this.getMediaForPictureElement(mediaContainer, format, alt, role) : this.getMedia(mediaContainer, format, alt, role);
  }
  /**
   * Returns a `Media` object with the main media (`src`) and various media (`src`)
   * for specific formats.
   *
   * This method is used for creating `Media` object that is used in `img` HTML element
   */
  getMedia(mediaContainer, format, alt, role) {
    if (!mediaContainer) {
      return;
    }
    const commonMediaProperties = this.getCommonMediaObject(mediaContainer, format, alt, role);
    return __spreadProps(__spreadValues({}, commonMediaProperties), {
      srcset: this.resolveSrcSet(mediaContainer, format)
    });
  }
  /**
   * Returns a `Media` object with the main media (`src`) and various sources
   * for specific formats for HTML `<picture>` element.
   */
  getMediaForPictureElement(mediaContainer, format, alt, role) {
    if (!mediaContainer) {
      return;
    }
    const commonMediaProperties = this.getCommonMediaObject(mediaContainer, format, alt, role);
    return __spreadProps(__spreadValues({}, commonMediaProperties), {
      sources: this.resolveSources(mediaContainer, format)
    });
  }
  /**
   * Generates attributes common for `<img>` ang `<picture>`.
   */
  getCommonMediaObject(mediaContainer, format, alt, role) {
    const mainMedia = mediaContainer.url ? mediaContainer : this.resolveMedia(mediaContainer, format);
    return __spreadValues({
      src: this.resolveAbsoluteUrl(mainMedia?.url ?? ""),
      alt: alt ?? mainMedia?.altText,
      role: role ?? mainMedia?.role
    }, this.getWidthAndHeight(mediaContainer, format));
  }
  /**
   * Returns width and height for Image.
   *
   * Width and height are not coming from CMS so it should be
   * manually added to the Container | Image object
   */
  getWidthAndHeight(mediaContainer, format) {
    const mainMedia = mediaContainer.url ? mediaContainer : this.resolveMedia(mediaContainer, format);
    return {
      width: mainMedia?.width,
      height: mainMedia?.height
    };
  }
  /**
   * Reads the loading strategy from the `MediaConfig`.
   *
   * Defaults to `ImageLoadingStrategy.EAGER`.
   */
  get loadingStrategy() {
    const fallbackStrategy = this.featureConfigService.isEnabled("lazyLoadImagesByDefault") ? ImageLoadingStrategy.LAZY : ImageLoadingStrategy.EAGER;
    return this.config?.imageLoadingStrategy ?? fallbackStrategy;
  }
  /**
   * Creates the media formats in a logical sorted order. The map contains the
   * format key and the format size information. We do this only once for performance
   * benefits.
   */
  get sortedFormats() {
    const mediaFormats = this.config?.mediaFormats;
    if (!this._sortedFormats && mediaFormats) {
      this._sortedFormats = Object.keys(mediaFormats).map((key) => ({
        code: key,
        size: mediaFormats[key]
      })).sort((a, b) => a.size.width && b.size.width && a.size.width > b.size.width ? 1 : -1);
    }
    return this._sortedFormats ?? [];
  }
  /**
   * Creates the media formats in a logical sorted order. The map contains the
   * format key and the format media query information. We do this only once for performance
   * benefits.
   */
  get sortedPictureFormats() {
    const pictureElementMediaFormats = this.config?.media?.pictureElementFormats;
    const pictureFormatsOrder = this.config?.media?.pictureFormatsOrder;
    if (!pictureElementMediaFormats) {
      return [];
    }
    if (!this._sortedPictureFormats && pictureElementMediaFormats) {
      this._sortedPictureFormats = Object.keys(pictureElementMediaFormats).map((key) => ({
        code: key,
        mediaQuery: pictureElementMediaFormats[key]?.mediaQueries || ""
      }));
      if (pictureFormatsOrder) {
        this._sortedPictureFormats.sort((a, b) => {
          const orderA = pictureFormatsOrder.indexOf(a.code);
          const orderB = pictureFormatsOrder.indexOf(b.code);
          return (orderA !== -1 ? orderA : Infinity) - (orderB !== -1 ? orderB : Infinity);
        });
      }
    }
    return this._sortedPictureFormats ?? [];
  }
  /**
   * Creates the media formats in a reversed sorted order.
   */
  get reversedFormats() {
    if (!this._reversedFormats) {
      this._reversedFormats = this.sortedFormats.slice().reverse();
    }
    return this._reversedFormats;
  }
  /**
   * Resolves the right media for the given format. The fo
   */
  resolveMedia(media, format) {
    return media[this.resolveFormat(media, format)];
  }
  /**
   * Validates the format against the given mediaContainer. If there is no format available,
   * or if the mediaContainer doesn't contain a media for the given media, the most optimal
   * format is resolved. If even that is not possible, the first format is returned.
   */
  resolveFormat(mediaContainer, format) {
    if (format && mediaContainer[format]) {
      return format;
    }
    return this.resolveBestFormat(mediaContainer) || Object.keys(mediaContainer)[0];
  }
  /**
   * Returns the media format code with the best size.
   */
  resolveBestFormat(media) {
    return this.reversedFormats.find((format) => media.hasOwnProperty(format.code))?.code;
  }
  /**
   * Returns a set of media for the available media formats. Additionally, the configured media
   * format width is added to the srcset, so that browsers can select the appropriate media.
   *
   * The optional maxFormat indicates that only sources till a certain format should be added
   * to the srcset.
   */
  resolveSrcSet(media, maxFormat) {
    if (!media) {
      return void 0;
    }
    const formats = this.getFormatsUpToMaxFormat(this.sortedFormats, maxFormat);
    const srcset = formats.reduce((set, format) => {
      const image = media[format.code];
      if (!!image) {
        if (set) {
          set += ", ";
        }
        set += `${this.resolveAbsoluteUrl(image.url ?? "")} ${format.size.width}w`;
      }
      return set;
    }, "");
    return srcset === "" ? void 0 : srcset;
  }
  /**
   * Resolves the sources for a picture element based on the provided media container and maximum format.
   *
   * This method generates an array of picture element attributes (`srcset` and `media`) by filtering
   * the sorted picture formats up to the specified maximum format. It then maps the corresponding
   * media sources from the provided media container.
   *
   * The method will return an array of picture element attributes suitable for use
   * in a `<picture>` element, or `undefined` if no media is provided.
   */
  resolveSources(media, maxFormat) {
    if (!media) {
      return void 0;
    }
    const pictureFormats = this.getFormatsUpToMaxFormat(this.sortedPictureFormats, maxFormat);
    return pictureFormats.reduce((sources, format) => {
      const image = media[format.code];
      if (image?.url) {
        sources.push(__spreadValues({
          srcset: this.resolveAbsoluteUrl(image.url),
          media: format.mediaQuery
        }, this.getWidthAndHeight(image, maxFormat)));
      }
      return sources;
    }, []);
  }
  /**
   * Retrieves a list of formats up to and including the specified max format.
   *
   * @template T - A type that extends an object containing a `code` property of type `string`.
   * @param {T[]} formats - An array of format objects, each containing at least a `code` property.
   * @param {string} [maxFormat] - The maximum format code to include in the returned list.
   * @returns {T[]} An array of formats up to and including the specified max format. If `maxFormat` is not found, returns the entire list.
   *
   * This method filters the provided list of formats to include only those up to and including
   * the specified max format. If the `maxFormat` is not found, the entire list of formats is returned.
   */
  getFormatsUpToMaxFormat(formats, maxFormat) {
    let pictureFormats = formats;
    const maxIndex = pictureFormats.findIndex((f) => f.code === maxFormat);
    if (maxIndex > -1) {
      pictureFormats = pictureFormats.slice(0, maxIndex + 1);
    }
    return pictureFormats;
  }
  /**
   * Resolves the absolute URL for the given url. In most cases, this URL represents
   * the relative URL on the backend. In that case, we prefix the url with the baseUrl.
   *
   * When we have receive an absolute URL, we return the URL as-is. An absolute URL might also
   * start with double slash, which is used to resolve media cross from http and https.
   */
  resolveAbsoluteUrl(url) {
    return !url || url.startsWith("http") || url.startsWith("//") ? url : this.getBaseUrl() + url;
  }
  /**
   * The base URL is either driven by a specific `backend.media.baseUrl`, or by the
   * `backend.occ.baseUrl`.
   *
   * The `backend.media.baseUrl` can be used to load media from a different location.
   *
   * In Commerce Cloud, a different location could mean a different "aspect".
   *
   * Defaults to empty string in case no config is provided.
   */
  getBaseUrl() {
    return this.config.backend?.media?.baseUrl ?? this.config.backend?.occ?.baseUrl ?? "";
  }
  static {
    this.ɵfac = function MediaService_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _MediaService)(ɵɵinject(Config));
    };
  }
  static {
    this.ɵprov = ɵɵdefineInjectable({
      token: _MediaService,
      factory: _MediaService.ɵfac,
      providedIn: "root"
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(MediaService, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], () => [{
    type: Config
  }], null);
})();
var MediaPreconnectService = class _MediaPreconnectService {
  constructor() {
    this.pageMetaLinkService = inject(PageMetaLinkService);
    this.mediaService = inject(MediaService);
    this.featureToggles = inject(FeatureToggles);
    this.windowRef = inject(WindowRef);
  }
  addPreconnectLink() {
    if (!this.featureToggles.createMediaPreconnectLink) {
      return;
    }
    const url = this.mediaService.getBaseUrl();
    let domain;
    try {
      domain = new URL(url).origin;
    } catch {
      domain = void 0;
    }
    if (domain && domain !== this.windowRef.location.origin) {
      this.pageMetaLinkService.addPreconnectLink(url);
    }
  }
  static {
    this.ɵfac = function MediaPreconnectService_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _MediaPreconnectService)();
    };
  }
  static {
    this.ɵprov = ɵɵdefineInjectable({
      token: _MediaPreconnectService,
      factory: _MediaPreconnectService.ɵfac,
      providedIn: "root"
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(MediaPreconnectService, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], null, null);
})();
var defaultSeoConfig = {
  seo: {
    structuredData: {
      disableInDevMode: true
    }
  }
};
var SeoConfig = class _SeoConfig {
  static {
    this.ɵfac = function SeoConfig_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _SeoConfig)();
    };
  }
  static {
    this.ɵprov = ɵɵdefineInjectable({
      token: _SeoConfig,
      factory: function SeoConfig_Factory(__ngFactoryType__) {
        let __ngConditionalFactory__ = null;
        if (__ngFactoryType__) {
          __ngConditionalFactory__ = new (__ngFactoryType__ || _SeoConfig)();
        } else {
          __ngConditionalFactory__ = ɵɵinject(Config);
        }
        return __ngConditionalFactory__;
      },
      providedIn: "root"
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(SeoConfig, [{
    type: Injectable,
    args: [{
      providedIn: "root",
      useExisting: Config
    }]
  }], null, null);
})();
var htmlLangProvider = {
  provide: APP_INITIALIZER,
  multi: true,
  useFactory: setHtmlLangAttribute,
  deps: [WindowRef, LanguageService]
};
function setHtmlLangAttribute(winRef, languageService) {
  const result = () => {
    languageService.getActive().subscribe((lang) => {
      winRef.document.documentElement.lang = lang.replace(/_/g, "-");
    });
  };
  return result;
}
var JsonLdScriptFactory = class _JsonLdScriptFactory {
  constructor(platformId, winRef, rendererFactory, config) {
    this.platformId = platformId;
    this.winRef = winRef;
    this.rendererFactory = rendererFactory;
    this.config = config;
    this.renderer = this.rendererFactory.createRenderer(null, null);
  }
  build(schema) {
    if (schema && this.isJsonLdRequired()) {
      this.getJsonLdScriptElement().textContent = this.escapeHtml(schema);
    }
  }
  /**
   * Indicates whether json ld data should be generated.
   *
   * This is only required on the server, but can be enabled in dev mode.
   */
  isJsonLdRequired() {
    return !isPlatformBrowser(this.platformId) || isDevMode() && !this.config.seo?.structuredData?.disableInDevMode;
  }
  /**
   * Creates a json-ld script element. The element is created one, and appended
   * to the html body element.
   *
   * ```html
   * <script id="json-ld" type="application/ld+json">
   * <\/script>
   * ```
   */
  getJsonLdScriptElement() {
    const id = "json-ld";
    let scriptElement = this.winRef.document.getElementById(id);
    if (!scriptElement) {
      const script = this.renderer.createElement("script");
      script.id = id;
      script.type = "application/ld+json";
      this.renderer.appendChild(this.winRef.document.body, script);
      scriptElement = script;
    }
    return scriptElement;
  }
  /**
   * Secure the given json-ld schema by encoding html characters (aka escaping), eg: <script> becomes &lt;script&gt;
   *
   * The given schema is not trusted, as malicious code could be injected (XSS)
   * into the json-ld script.
   */
  escapeHtml(schema) {
    const div = this.renderer.createElement("div");
    div.textContent = JSON.stringify(schema);
    return div.innerHTML;
  }
  static {
    this.ɵfac = function JsonLdScriptFactory_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _JsonLdScriptFactory)(ɵɵinject(PLATFORM_ID), ɵɵinject(WindowRef), ɵɵinject(RendererFactory2), ɵɵinject(SeoConfig));
    };
  }
  static {
    this.ɵprov = ɵɵdefineInjectable({
      token: _JsonLdScriptFactory,
      factory: _JsonLdScriptFactory.ɵfac,
      providedIn: "root"
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(JsonLdScriptFactory, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], () => [{
    type: void 0,
    decorators: [{
      type: Inject,
      args: [PLATFORM_ID]
    }]
  }, {
    type: WindowRef
  }, {
    type: RendererFactory2
  }, {
    type: SeoConfig
  }], null);
})();
var JsonLdDirective = class _JsonLdDirective {
  /**
   * Writes the schema data to a json-ld script element.
   */
  set cxJsonLd(schema) {
    this.generateJsonLdScript(schema);
  }
  constructor(renderer, jsonLdScriptFactory, element) {
    this.renderer = renderer;
    this.jsonLdScriptFactory = jsonLdScriptFactory;
    this.element = element;
  }
  /**
   * attach the json-ld script tag to DOM with the schema data secured by encoding html tags (aka escaping)
   */
  generateJsonLdScript(schema) {
    if (schema && this.jsonLdScriptFactory.isJsonLdRequired()) {
      const script = this.renderer.createElement("script");
      script.type = "application/ld+json";
      script.textContent = this.jsonLdScriptFactory.escapeHtml(schema);
      this.renderer.appendChild(this.element.nativeElement, script);
    }
  }
  static {
    this.ɵfac = function JsonLdDirective_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _JsonLdDirective)(ɵɵdirectiveInject(Renderer2), ɵɵdirectiveInject(JsonLdScriptFactory), ɵɵdirectiveInject(ElementRef));
    };
  }
  static {
    this.ɵdir = ɵɵdefineDirective({
      type: _JsonLdDirective,
      selectors: [["", "cxJsonLd", ""]],
      inputs: {
        cxJsonLd: "cxJsonLd"
      },
      standalone: false
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(JsonLdDirective, [{
    type: Directive,
    args: [{
      selector: "[cxJsonLd]",
      standalone: false
    }]
  }], () => [{
    type: Renderer2
  }, {
    type: JsonLdScriptFactory
  }, {
    type: ElementRef
  }], {
    cxJsonLd: [{
      type: Input
    }]
  });
})();
var SCHEMA_BUILDER = new InjectionToken("SchemaBuilderToken");
var JSONLD_PRODUCT_BUILDER = new InjectionToken("JsonLdProductBuilderToken");
var StructuredDataFactory = class _StructuredDataFactory {
  constructor(scriptBuilder, builders) {
    this.scriptBuilder = scriptBuilder;
    this.builders = builders;
    this.subscription = new Subscription();
  }
  /**
   * Initiates the build of structured data by collecting all schema
   * builders.
   */
  build() {
    if (this.scriptBuilder.isJsonLdRequired() && this.builders) {
      this.subscription.add(this.collectSchemas().subscribe((schema) => {
        this.scriptBuilder.build(schema);
      }));
    }
  }
  /**
   * Collects all schema builders and observe their structured data.
   */
  collectSchemas() {
    return combineLatest(this.builders.map((builder) => builder.build()));
  }
  ngOnDestroy() {
    this.subscription.unsubscribe();
  }
  static {
    this.ɵfac = function StructuredDataFactory_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _StructuredDataFactory)(ɵɵinject(JsonLdScriptFactory), ɵɵinject(SCHEMA_BUILDER, 8));
    };
  }
  static {
    this.ɵprov = ɵɵdefineInjectable({
      token: _StructuredDataFactory,
      factory: _StructuredDataFactory.ɵfac,
      providedIn: "root"
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(StructuredDataFactory, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], () => [{
    type: JsonLdScriptFactory
  }, {
    type: void 0,
    decorators: [{
      type: Optional
    }, {
      type: Inject,
      args: [SCHEMA_BUILDER]
    }]
  }], null);
})();
function getStructuredDataFactory(injector) {
  const result = () => {
    const factory = injector.get(StructuredDataFactory);
    factory.build();
  };
  return result;
}
var StructuredDataModule = class _StructuredDataModule {
  static {
    this.ɵfac = function StructuredDataModule_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _StructuredDataModule)();
    };
  }
  static {
    this.ɵmod = ɵɵdefineNgModule({
      type: _StructuredDataModule,
      declarations: [JsonLdDirective],
      imports: [CommonModule],
      exports: [JsonLdDirective]
    });
  }
  static {
    this.ɵinj = ɵɵdefineInjector({
      providers: [{
        provide: APP_INITIALIZER,
        useFactory: getStructuredDataFactory,
        deps: [Injector],
        multi: true
      }],
      imports: [CommonModule]
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(StructuredDataModule, [{
    type: NgModule,
    args: [{
      imports: [CommonModule],
      declarations: [JsonLdDirective],
      exports: [JsonLdDirective],
      providers: [{
        provide: APP_INITIALIZER,
        useFactory: getStructuredDataFactory,
        deps: [Injector],
        multi: true
      }]
    }]
  }], null, null);
})();
function initSeoService(injector) {
  const result = () => {
    const service = injector.get(SeoMetaService);
    service.init();
  };
  return result;
}
var SeoModule = class _SeoModule {
  static {
    this.ɵfac = function SeoModule_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _SeoModule)();
    };
  }
  static {
    this.ɵmod = ɵɵdefineNgModule({
      type: _SeoModule,
      imports: [StructuredDataModule]
    });
  }
  static {
    this.ɵinj = ɵɵdefineInjector({
      providers: [provideDefaultConfig(defaultSeoConfig), {
        provide: APP_INITIALIZER,
        useFactory: initSeoService,
        deps: [Injector],
        multi: true
      }, htmlLangProvider],
      imports: [StructuredDataModule]
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(SeoModule, [{
    type: NgModule,
    args: [{
      imports: [StructuredDataModule],
      providers: [provideDefaultConfig(defaultSeoConfig), {
        provide: APP_INITIALIZER,
        useFactory: initSeoService,
        deps: [Injector],
        multi: true
      }, htmlLangProvider]
    }]
  }], null, null);
})();
var BreadcrumbSchemaBuilder = class _BreadcrumbSchemaBuilder {
  constructor(pageMetaService) {
    this.pageMetaService = pageMetaService;
  }
  build() {
    return this.pageMetaService.getMeta().pipe(map((pageMeta) => this.collect(pageMeta)));
  }
  collect(pageMeta) {
    if (!pageMeta?.breadcrumbs) {
      return;
    }
    const crumbs = pageMeta.breadcrumbs.map((crumb, index) => {
      return {
        "@type": "ListItem",
        position: index + 1,
        item: {
          "@id": crumb.link,
          name: crumb.label
        }
      };
    });
    if (pageMeta.title) {
      crumbs.push({
        "@type": "ListItem",
        position: crumbs.length + 1,
        item: {
          "@id": pageMeta.title,
          name: pageMeta.title
        }
      });
    }
    return {
      "@context": "http://schema.org",
      "@type": "BreadcrumbList",
      itemListElement: crumbs
    };
  }
  static {
    this.ɵfac = function BreadcrumbSchemaBuilder_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _BreadcrumbSchemaBuilder)(ɵɵinject(PageMetaService));
    };
  }
  static {
    this.ɵprov = ɵɵdefineInjectable({
      token: _BreadcrumbSchemaBuilder,
      factory: _BreadcrumbSchemaBuilder.ɵfac,
      providedIn: "root"
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(BreadcrumbSchemaBuilder, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], () => [{
    type: PageMetaService
  }], null);
})();
var JsonLdBaseProductBuilder = class _JsonLdBaseProductBuilder {
  build(product) {
    return of(__spreadValues(__spreadValues(__spreadValues({}, this.getProductBase(product)), this.getProductBrand(product)), this.getProductImage(product)));
  }
  /**
   * Returns the product sku, name and description.
   */
  getProductBase(product) {
    const result = {
      sku: product.code
    };
    if (product.name) {
      result.name = product.name;
    }
    if (product.summary) {
      result.description = product.summary;
    }
    return result;
  }
  /**
   * Returns the image object with the main product image url.
   *
   * If the image is not available, an empty object is returned.
   */
  getProductImage(product) {
    const image = product.images?.PRIMARY?.zoom?.url;
    return image ? {
      image
    } : {};
  }
  /**
   * Returns the brand object with the product manufacturer.
   *
   * If the brand is not available, an empty object is returned.
   */
  getProductBrand(product) {
    const brand = product.manufacturer;
    return brand ? {
      brand
    } : {};
  }
  static {
    this.ɵfac = function JsonLdBaseProductBuilder_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _JsonLdBaseProductBuilder)();
    };
  }
  static {
    this.ɵprov = ɵɵdefineInjectable({
      token: _JsonLdBaseProductBuilder,
      factory: _JsonLdBaseProductBuilder.ɵfac,
      providedIn: "root"
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(JsonLdBaseProductBuilder, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], null, null);
})();
var JsonLdProductOfferBuilder = class _JsonLdProductOfferBuilder {
  build(product) {
    const schema = {
      "@type": "Offer"
    };
    if (product.price?.value) {
      schema.price = product.price.value;
      if (product.price.currencyIso) {
        schema.priceCurrency = product.price.currencyIso;
      }
    }
    if (product.stock && product.stock.stockLevelStatus) {
      schema.availability = product.stock.stockLevelStatus === "inStock" ? "InStock" : "OutOfStock";
    }
    return of({
      offers: schema
    });
  }
  static {
    this.ɵfac = function JsonLdProductOfferBuilder_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _JsonLdProductOfferBuilder)();
    };
  }
  static {
    this.ɵprov = ɵɵdefineInjectable({
      token: _JsonLdProductOfferBuilder,
      factory: _JsonLdProductOfferBuilder.ɵfac,
      providedIn: "root"
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(JsonLdProductOfferBuilder, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], null, null);
})();
var JsonLdProductReviewBuilder = class _JsonLdProductReviewBuilder {
  constructor(reviewService, config) {
    this.reviewService = reviewService;
    this.config = config;
  }
  build(product) {
    return this.reviewService.getByProductCode(product.code ?? "").pipe(map((reviews) => reviews?.length > 0 ? {
      aggregateRating: this.buildAggregatedReviews(product, reviews),
      review: reviews.map((review) => this.buildReviews(review))
    } : {}));
  }
  buildAggregatedReviews(product, reviews) {
    const aggregated = {
      "@type": "AggregateRating"
    };
    if (product.averageRating) {
      aggregated.ratingValue = product.averageRating;
    }
    if (reviews) {
      aggregated.ratingCount = reviews.filter((rev) => !!rev.rating).length;
      aggregated.reviewCount = reviews.filter((rev) => !!rev.comment).length;
    }
    return aggregated;
  }
  buildReviews(review) {
    const reviewSchema = {
      "@type": "review"
    };
    if (review.principal && review.principal.name) {
      reviewSchema.author = review.principal.name;
    }
    if (review.date) {
      const date = new Date(review.date);
      reviewSchema.datePublished = `${date.getFullYear()}-${date.getMonth() + 1}-${date.getDate()}`;
    }
    if (review.headline) {
      reviewSchema.name = review.headline;
    }
    if (review.comment) {
      reviewSchema.description = review.comment;
    }
    if (review.rating) {
      reviewSchema.reviewRating = {
        "@type": "Rating",
        ratingValue: review.rating.toString()
      };
    }
    return reviewSchema;
  }
  static {
    this.ɵfac = function JsonLdProductReviewBuilder_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _JsonLdProductReviewBuilder)(ɵɵinject(ProductReviewService), ɵɵinject(SeoConfig));
    };
  }
  static {
    this.ɵprov = ɵɵdefineInjectable({
      token: _JsonLdProductReviewBuilder,
      factory: _JsonLdProductReviewBuilder.ɵfac,
      providedIn: "root"
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(JsonLdProductReviewBuilder, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], () => [{
    type: ProductReviewService
  }, {
    type: SeoConfig
  }], null);
})();
var CurrentProductService = class _CurrentProductService {
  constructor(routingService, productService) {
    this.routingService = routingService;
    this.productService = productService;
    this.DEFAULT_PRODUCT_SCOPE = ProductScope.DETAILS;
  }
  /**
   * Returns an observable for the current product
   * @returns Product
   * @returns null if product can't be found
   *
   * @param scopes
   */
  getProduct(scopes) {
    return this.getCode().pipe(distinctUntilChanged(), switchMap((productCode) => {
      return productCode ? this.productService.get(productCode, scopes || this.DEFAULT_PRODUCT_SCOPE) : of(null);
    }), filter(isNotUndefined));
  }
  getCode() {
    return this.routingService.getRouterState().pipe(map((state) => state.state.params["productCode"]));
  }
  static {
    this.ɵfac = function CurrentProductService_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _CurrentProductService)(ɵɵinject(RoutingService), ɵɵinject(ProductService));
    };
  }
  static {
    this.ɵprov = ɵɵdefineInjectable({
      token: _CurrentProductService,
      factory: _CurrentProductService.ɵfac,
      providedIn: "root"
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(CurrentProductService, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], () => [{
    type: RoutingService
  }, {
    type: ProductService
  }], null);
})();
var ProductSchemaBuilder = class _ProductSchemaBuilder {
  constructor(currentProduct, builders) {
    this.currentProduct = currentProduct;
    this.builders = builders;
  }
  build() {
    return this.currentProduct.getProduct().pipe(switchMap((product) => {
      if (product) {
        return combineLatest(this.collect(product)).pipe(map((res) => Object.assign({}, ...res)));
      }
      return of({});
    }));
  }
  collect(product) {
    if (!product || !product.code) {
      return [];
    }
    const builders = this.builders ? this.builders.map((builder) => builder.build(product)) : [];
    return [of({
      "@context": "http://schema.org",
      "@type": "Product"
    }), ...builders];
  }
  static {
    this.ɵfac = function ProductSchemaBuilder_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _ProductSchemaBuilder)(ɵɵinject(CurrentProductService), ɵɵinject(JSONLD_PRODUCT_BUILDER, 8));
    };
  }
  static {
    this.ɵprov = ɵɵdefineInjectable({
      token: _ProductSchemaBuilder,
      factory: _ProductSchemaBuilder.ɵfac,
      providedIn: "root"
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(ProductSchemaBuilder, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], () => [{
    type: CurrentProductService
  }, {
    type: void 0,
    decorators: [{
      type: Optional
    }, {
      type: Inject,
      args: [JSONLD_PRODUCT_BUILDER]
    }]
  }], null);
})();
var JsonLdBuilderModule = class _JsonLdBuilderModule {
  static {
    this.ɵfac = function JsonLdBuilderModule_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _JsonLdBuilderModule)();
    };
  }
  static {
    this.ɵmod = ɵɵdefineNgModule({
      type: _JsonLdBuilderModule
    });
  }
  static {
    this.ɵinj = ɵɵdefineInjector({
      providers: [
        {
          provide: SCHEMA_BUILDER,
          useExisting: ProductSchemaBuilder,
          multi: true
        },
        {
          provide: SCHEMA_BUILDER,
          useExisting: BreadcrumbSchemaBuilder,
          multi: true
        },
        // lower level json-ld builder classes offering fine-grained control
        // for product related schemas
        {
          provide: JSONLD_PRODUCT_BUILDER,
          useExisting: JsonLdBaseProductBuilder,
          multi: true
        },
        {
          provide: JSONLD_PRODUCT_BUILDER,
          useExisting: JsonLdProductOfferBuilder,
          multi: true
        },
        {
          provide: JSONLD_PRODUCT_BUILDER,
          useExisting: JsonLdProductReviewBuilder,
          multi: true
        }
      ]
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(JsonLdBuilderModule, [{
    type: NgModule,
    args: [{
      providers: [
        {
          provide: SCHEMA_BUILDER,
          useExisting: ProductSchemaBuilder,
          multi: true
        },
        {
          provide: SCHEMA_BUILDER,
          useExisting: BreadcrumbSchemaBuilder,
          multi: true
        },
        // lower level json-ld builder classes offering fine-grained control
        // for product related schemas
        {
          provide: JSONLD_PRODUCT_BUILDER,
          useExisting: JsonLdBaseProductBuilder,
          multi: true
        },
        {
          provide: JSONLD_PRODUCT_BUILDER,
          useExisting: JsonLdProductOfferBuilder,
          multi: true
        },
        {
          provide: JSONLD_PRODUCT_BUILDER,
          useExisting: JsonLdProductReviewBuilder,
          multi: true
        }
      ]
    }]
  }], null, null);
})();
var PageSection;
(function(PageSection2) {
  PageSection2["HEADER"] = "header";
  PageSection2["FOOTER"] = "footer";
  PageSection2["NAVIGATION"] = "navigation";
})(PageSection || (PageSection = {}));
function provideCmsStructure(options) {
  return provideConfig(__spreadValues(__spreadValues({}, buildCmsStructure(options)), buildLayoutConfig(options)));
}
function buildCmsStructure({
  componentId,
  pageSlotPosition
} = {}) {
  const config = {
    cmsStructure: {}
  };
  if (componentId) {
    config.cmsStructure = {
      components: {
        [componentId]: {
          typeCode: componentId,
          flexType: componentId
        }
      }
    };
  }
  if (componentId && pageSlotPosition && config.cmsStructure) {
    config.cmsStructure.slots = {
      [pageSlotPosition]: {
        componentIds: [componentId]
      }
    };
  }
  return config;
}
function buildLayoutConfig({
  pageTemplate,
  pageSlotPosition,
  breakpoint,
  section
} = {}) {
  const layoutConfig2 = {};
  if (pageTemplate && pageSlotPosition) {
    const pageTemplateSlots = {};
    if (breakpoint) {
      pageTemplateSlots[breakpoint] = {
        slots: [pageSlotPosition]
      };
    } else {
      pageTemplateSlots.slots = [pageSlotPosition];
    }
    layoutConfig2.layoutSlots = {
      [pageTemplate]: pageTemplateSlots
    };
  }
  if (section && pageSlotPosition) {
    const sectionSlots = {};
    if (breakpoint) {
      sectionSlots[breakpoint] = {
        slots: [pageSlotPosition]
      };
    } else {
      sectionSlots.slots = [pageSlotPosition];
    }
    if (layoutConfig2.layoutSlots) {
      layoutConfig2.layoutSlots[section] = sectionSlots;
    } else {
      layoutConfig2.layoutSlots = {
        [section]: sectionSlots
      };
    }
  }
  return layoutConfig2;
}
var SkipLinkConfig = class _SkipLinkConfig {
  static {
    this.ɵfac = function SkipLinkConfig_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _SkipLinkConfig)();
    };
  }
  static {
    this.ɵprov = ɵɵdefineInjectable({
      token: _SkipLinkConfig,
      factory: function SkipLinkConfig_Factory(__ngFactoryType__) {
        let __ngConditionalFactory__ = null;
        if (__ngFactoryType__) {
          __ngConditionalFactory__ = new (__ngFactoryType__ || _SkipLinkConfig)();
        } else {
          __ngConditionalFactory__ = ɵɵinject(Config);
        }
        return __ngConditionalFactory__;
      },
      providedIn: "root"
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(SkipLinkConfig, [{
    type: Injectable,
    args: [{
      providedIn: "root",
      useExisting: Config
    }]
  }], null, null);
})();
var SkipLink = class {
};
var SkipLinkScrollPosition;
(function(SkipLinkScrollPosition2) {
  SkipLinkScrollPosition2["BEFORE"] = "BEFORE";
  SkipLinkScrollPosition2["AFTER"] = "AFTER";
})(SkipLinkScrollPosition || (SkipLinkScrollPosition = {}));
var SkipLinkService = class _SkipLinkService {
  constructor(config, keyboardFocusService) {
    this.config = config;
    this.keyboardFocusService = keyboardFocusService;
    this.skipLinks$ = new BehaviorSubject([]);
  }
  getSkipLinks() {
    return this.skipLinks$;
  }
  add(key, target) {
    const found = this.config.skipLinks?.find((skipLink) => skipLink.key === key);
    if (found) {
      const existing = this.skipLinks$.value;
      existing.splice(this.getSkipLinkIndexInArray(key), 0, {
        target,
        i18nKey: found.i18nKey,
        position: found.position,
        key
      });
      this.skipLinks$.next(existing);
    }
  }
  remove(key) {
    const found = this.config.skipLinks?.find((skipLink) => skipLink.key === key);
    if (found) {
      let existing = this.skipLinks$.value;
      existing = existing.filter((skipLink) => skipLink.key !== key);
      this.skipLinks$.next(existing);
    }
  }
  scrollToTarget(scrollTo) {
    const skipLink = typeof scrollTo === "string" ? this.findSkipLinkByKey(scrollTo) : scrollTo;
    if (!skipLink) {
      return;
    }
    const target = skipLink.target instanceof HTMLElement ? skipLink.target : skipLink.target?.parentElement;
    const firstFocusable = this.keyboardFocusService.findFirstFocusable(target) || target;
    const hasTabindex = firstFocusable?.hasAttribute("tabindex");
    if (!hasTabindex) {
      firstFocusable?.setAttribute("tabindex", "-1");
    }
    firstFocusable?.focus();
    if (!hasTabindex) {
      firstFocusable?.removeAttribute("tabindex");
    }
  }
  findSkipLinkByKey(key) {
    return this.skipLinks$.value.find((skipLink) => skipLink.key === key);
  }
  getSkipLinkIndexInArray(key) {
    let index = this.config.skipLinks?.findIndex((skipLink) => skipLink.key === key) ?? 0;
    while (index > 0) {
      index--;
      const previous = this.config.skipLinks?.[index];
      if (previous) {
        const existing = this.skipLinks$.value;
        const found = existing.findIndex((skipLink) => skipLink.key === previous.key);
        if (found > -1) {
          return found + 1;
        }
      }
    }
    return 0;
  }
  static {
    this.ɵfac = function SkipLinkService_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _SkipLinkService)(ɵɵinject(SkipLinkConfig), ɵɵinject(KeyboardFocusService));
    };
  }
  static {
    this.ɵprov = ɵɵdefineInjectable({
      token: _SkipLinkService,
      factory: _SkipLinkService.ɵfac,
      providedIn: "root"
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(SkipLinkService, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], () => [{
    type: SkipLinkConfig
  }, {
    type: KeyboardFocusService
  }], null);
})();
var SkipLinkComponent = class _SkipLinkComponent {
  constructor(skipLinkService) {
    this.skipLinkService = skipLinkService;
    this.skipLinks$ = this.skipLinkService.getSkipLinks();
  }
  scrollToTarget(skipLink) {
    this.skipLinkService.scrollToTarget(skipLink);
  }
  static {
    this.ɵfac = function SkipLinkComponent_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _SkipLinkComponent)(ɵɵdirectiveInject(SkipLinkService));
    };
  }
  static {
    this.ɵcmp = ɵɵdefineComponent({
      type: _SkipLinkComponent,
      selectors: [["cx-skip-link"]],
      standalone: false,
      decls: 2,
      vars: 3,
      consts: [[3, "cxFocus", 4, "ngIf"], [3, "cxFocus"], [3, "click", 4, "ngFor", "ngForOf"], [3, "click"]],
      template: function SkipLinkComponent_Template(rf, ctx) {
        if (rf & 1) {
          ɵɵtemplate(0, SkipLinkComponent_div_0_Template, 2, 3, "div", 0);
          ɵɵpipe(1, "async");
        }
        if (rf & 2) {
          ɵɵproperty("ngIf", ɵɵpipeBind1(1, 1, ctx.skipLinks$));
        }
      },
      dependencies: [NgForOf, NgIf, FocusDirective, AsyncPipe, TranslatePipe],
      encapsulation: 2,
      changeDetection: 0
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(SkipLinkComponent, [{
    type: Component,
    args: [{
      selector: "cx-skip-link",
      changeDetection: ChangeDetectionStrategy.OnPush,
      standalone: false,
      template: `<div [cxFocus]="{ tab: true }" *ngIf="skipLinks$ | async as links">
  <button *ngFor="let link of links" (click)="scrollToTarget(link)">
    {{ 'skipLink.skipTo' | cxTranslate }}
    {{ link.i18nKey | cxTranslate }}
  </button>
</div>
`
    }]
  }], () => [{
    type: SkipLinkService
  }], null);
})();
var defaultSkipLinkConfig = {
  skipLinks: [{
    key: "cx-header",
    i18nKey: "skipLink.labels.header"
  }, {
    key: "cx-main",
    i18nKey: "skipLink.labels.main"
  }, {
    key: "cx-footer",
    i18nKey: "skipLink.labels.footer"
  }]
};
var SkipLinkDirective = class _SkipLinkDirective {
  constructor(elementRef, skipLinkService) {
    this.elementRef = elementRef;
    this.skipLinkService = skipLinkService;
  }
  ngOnInit() {
    this.skipLinkService.add(this.cxSkipLink, this.elementRef.nativeElement);
  }
  ngOnDestroy() {
    this.skipLinkService.remove(this.cxSkipLink);
  }
  static {
    this.ɵfac = function SkipLinkDirective_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _SkipLinkDirective)(ɵɵdirectiveInject(ElementRef), ɵɵdirectiveInject(SkipLinkService));
    };
  }
  static {
    this.ɵdir = ɵɵdefineDirective({
      type: _SkipLinkDirective,
      selectors: [["", "cxSkipLink", ""]],
      inputs: {
        cxSkipLink: "cxSkipLink"
      },
      standalone: false
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(SkipLinkDirective, [{
    type: Directive,
    args: [{
      selector: "[cxSkipLink]",
      standalone: false
    }]
  }], () => [{
    type: ElementRef
  }, {
    type: SkipLinkService
  }], {
    cxSkipLink: [{
      type: Input
    }]
  });
})();
var SkipLinkModule = class _SkipLinkModule {
  static {
    this.ɵfac = function SkipLinkModule_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _SkipLinkModule)();
    };
  }
  static {
    this.ɵmod = ɵɵdefineNgModule({
      type: _SkipLinkModule,
      declarations: [SkipLinkComponent, SkipLinkDirective],
      imports: [CommonModule, I18nModule, ConfigModule, KeyboardFocusModule],
      exports: [SkipLinkDirective]
    });
  }
  static {
    this.ɵinj = ɵɵdefineInjector({
      providers: [provideDefaultConfig(defaultSkipLinkConfig), {
        provide: APP_INITIALIZER,
        useFactory: skipLinkFactory,
        deps: [ComponentFactoryResolver$1, OutletService],
        multi: true
      }],
      imports: [CommonModule, I18nModule, ConfigModule.withConfig(defaultSkipLinkConfig), KeyboardFocusModule]
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(SkipLinkModule, [{
    type: NgModule,
    args: [{
      imports: [CommonModule, I18nModule, ConfigModule.withConfig(defaultSkipLinkConfig), KeyboardFocusModule],
      declarations: [SkipLinkComponent, SkipLinkDirective],
      exports: [SkipLinkDirective],
      providers: [provideDefaultConfig(defaultSkipLinkConfig), {
        provide: APP_INITIALIZER,
        useFactory: skipLinkFactory,
        deps: [ComponentFactoryResolver$1, OutletService],
        multi: true
      }]
    }]
  }], null, null);
})();
function skipLinkFactory(componentFactoryResolver, outletService) {
  const isReady = () => {
    const factory = componentFactoryResolver.resolveComponentFactory(SkipLinkComponent);
    outletService.add("cx-storefront", factory, OutletPosition.BEFORE);
  };
  return isReady;
}
var BtnLikeLinkDirective = class _BtnLikeLinkDirective {
  static {
    this.ɵfac = function BtnLikeLinkDirective_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _BtnLikeLinkDirective)();
    };
  }
  static {
    this.ɵdir = ɵɵdefineDirective({
      type: _BtnLikeLinkDirective,
      selectors: [["a", "cxBtnLikeLink", "", 8, "btn"]],
      hostAttrs: ["tabindex", "0", "role", "button"],
      hostBindings: function BtnLikeLinkDirective_HostBindings(rf, ctx) {
        if (rf & 1) {
          ɵɵlistener("keydown.enter", function BtnLikeLinkDirective_keydown_enter_HostBindingHandler($event) {
            $event.preventDefault();
            return $event.target.click();
          })("keydown.space", function BtnLikeLinkDirective_keydown_space_HostBindingHandler($event) {
            $event.preventDefault();
            return $event.target.click();
          });
        }
      },
      standalone: false
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(BtnLikeLinkDirective, [{
    type: Directive,
    args: [{
      selector: "a[cxBtnLikeLink].btn",
      // 'a.btn'
      host: {
        // Adding [tabindex] allows tab-based access to this element.
        tabindex: "0",
        // Adding [role] tells screen reatders that this "link" is really a "button"
        role: "button",
        // Add Enter keydown click mimic native Button's behaviour
        "(keydown.enter)": "$event.preventDefault() ; $event.target.click() ;",
        // Add Space keydown click mimic native Button's behaviour
        "(keydown.space)": "$event.preventDefault() ; $event.target.click() ;"
      },
      standalone: false
    }]
  }], null, null);
})();
var directives = [BtnLikeLinkDirective];
var BtnLikeLinkModule = class _BtnLikeLinkModule {
  static {
    this.ɵfac = function BtnLikeLinkModule_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _BtnLikeLinkModule)();
    };
  }
  static {
    this.ɵmod = ɵɵdefineNgModule({
      type: _BtnLikeLinkModule,
      declarations: [BtnLikeLinkDirective],
      imports: [CommonModule],
      exports: [BtnLikeLinkDirective]
    });
  }
  static {
    this.ɵinj = ɵɵdefineInjector({
      imports: [CommonModule]
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(BtnLikeLinkModule, [{
    type: NgModule,
    args: [{
      imports: [CommonModule],
      declarations: [...directives],
      exports: [...directives]
    }]
  }], null, null);
})();
var DomChangeDirective = class _DomChangeDirective {
  constructor() {
    this.cxDomChange = new EventEmitter();
    this.elementRef = inject(ElementRef);
    this.changes = new MutationObserver((mutations) => {
      const affectedMutations = this.cxDomChangeTargetSelector ? mutations.filter((mutation) => mutation.target === this.elementRef.nativeElement.querySelector(this.cxDomChangeTargetSelector)) : mutations;
      affectedMutations.forEach((mutation) => this.cxDomChange.emit(mutation));
    });
    this.changes.observe(this.elementRef.nativeElement, {
      subtree: true,
      childList: true
    });
  }
  ngOnDestroy() {
    this.changes.disconnect();
  }
  static {
    this.ɵfac = function DomChangeDirective_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _DomChangeDirective)();
    };
  }
  static {
    this.ɵdir = ɵɵdefineDirective({
      type: _DomChangeDirective,
      selectors: [["", "cxDomChange", ""]],
      inputs: {
        cxDomChangeTargetSelector: "cxDomChangeTargetSelector"
      },
      outputs: {
        cxDomChange: "cxDomChange"
      },
      standalone: false
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(DomChangeDirective, [{
    type: Directive,
    args: [{
      selector: "[cxDomChange]",
      standalone: false
    }]
  }], () => [], {
    cxDomChangeTargetSelector: [{
      type: Input
    }],
    cxDomChange: [{
      type: Output
    }]
  });
})();
var DomChangeModule = class _DomChangeModule {
  static {
    this.ɵfac = function DomChangeModule_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _DomChangeModule)();
    };
  }
  static {
    this.ɵmod = ɵɵdefineNgModule({
      type: _DomChangeModule,
      declarations: [DomChangeDirective],
      imports: [CommonModule],
      exports: [DomChangeDirective]
    });
  }
  static {
    this.ɵinj = ɵɵdefineInjector({
      imports: [CommonModule]
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(DomChangeModule, [{
    type: NgModule,
    args: [{
      imports: [CommonModule],
      declarations: [DomChangeDirective],
      exports: [DomChangeDirective]
    }]
  }], null, null);
})();
var CarouselService = class _CarouselService {
  constructor(winRef) {
    this.winRef = winRef;
  }
  /**
   * The number of items per slide is calculated by the help of
   * the item width and the available width of the host element.
   * This appoach makes it possible to place the carousel in different
   * layouts. Instead of using the page breakpoints, the host size is
   * taken into account.
   *
   * Since there's no element resize API available, we use the
   * window `resize` event, so that we can adjust the number of items
   * whenever the window got resized.
   */
  getItemsPerSlide(nativeElement, itemWidth) {
    return this.winRef.resize$.pipe(map(() => nativeElement.clientWidth), map((totalWidth) => this.calculateItems(totalWidth, itemWidth)));
  }
  /**
   * Calculates the number of items per given hostSize.  calculated based on the given
   * intended size in pixels or percentages. The
   *
   * @param availableWidth The available width in pixels for the carousel items.
   * @param itemWidth The width per carousel item, in px or percentage.
   */
  calculateItems(availableWidth, itemWidth) {
    let calculatedItems = 0;
    if (itemWidth.endsWith("px")) {
      const num = itemWidth.substring(0, itemWidth.length - 2);
      calculatedItems = availableWidth / num;
    }
    if (itemWidth.endsWith("%")) {
      const perc = itemWidth.substring(0, itemWidth.length - 1);
      calculatedItems = availableWidth / (availableWidth * (perc / 100));
    }
    return Math.floor(calculatedItems) || 1;
  }
  static {
    this.ɵfac = function CarouselService_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _CarouselService)(ɵɵinject(WindowRef));
    };
  }
  static {
    this.ɵprov = ɵɵdefineInjectable({
      token: _CarouselService,
      factory: _CarouselService.ɵfac,
      providedIn: "root"
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(CarouselService, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], () => [{
    type: WindowRef
  }], null);
})();
var CarouselComponent = class _CarouselComponent {
  set setItems(inputItems) {
    this.items = inputItems;
    this.activeSlide = 0;
  }
  constructor(el, service) {
    this.el = el;
    this.service = service;
    this.keybordEvent = new BehaviorSubject(null);
    this.itemWidth = "300px";
    this.hideIndicators = false;
    this.indicatorIcon = ICON_TYPE.CIRCLE;
    this.previousIcon = ICON_TYPE.CARET_LEFT;
    this.nextIcon = ICON_TYPE.CARET_RIGHT;
    this.trackByFn = (_index, item) => item;
    this.logger = inject(LoggerService);
    useFeatureStyles("a11yAddPaddingToCarouselPanel");
  }
  ngOnInit() {
    if (!this.template && isDevMode()) {
      this.logger.error("No template reference provided to render the carousel items for the `cx-carousel`");
    }
  }
  ngOnChanges() {
    this.size$ = this.service.getItemsPerSlide(this.el.nativeElement, this.itemWidth).pipe(tap(() => this.activeSlide = 0));
  }
  onItemKeydown(event, size) {
    switch (event.key) {
      case "ArrowRight":
      case "ArrowLeft":
        event.preventDefault();
        this.focusNextPrevItem(event.target, event.key === "ArrowRight" ? 1 : -1, size);
        break;
      case "Tab":
        this.skipTabForCarouselItems();
        break;
    }
  }
  /**
   * Handles Tab key on carousel items. If the carousel items have `ArrowRight`/`ArrowLeft`
   * navigation enabled, it temporarily disables tab navigation for these items.
   * The `cxFocusableCarouselItem` selector is used because it identifies carousel
   * items that have `ArrowRight`/`ArrowLeft` navigation enabled. These items should not
   * use tab navigation according to a11y requirements.
   */
  skipTabForCarouselItems() {
    const carouselElements = Array.from(this.el.nativeElement.querySelectorAll("[cxFocusableCarouselItem]"));
    if (!carouselElements.length) {
      return;
    }
    disableTabbingForTick(carouselElements);
  }
  /**
   * Focuses the next or previous item in the carousel based on keyboard navigation.
   *
   * This method determines the next focusable carousel item, identified by the
   * `cxFocusableCarouselItem` directive, based on the current focus and the direction
   * given. It adjusts the carousel's active slide if the next focusable item is
   * outside the currently visible items.
   *
   * @param currentItem - The currently focused carousel item.
   * @param direction - The navigation direction (1 for right, -1 for left).
   * @param size - The number of items per slide, used to determine slide change is needed
   */
  focusNextPrevItem(currentItem, direction, size) {
    const focusableElements = this.el.nativeElement.querySelectorAll("[cxFocusableCarouselItem]");
    const currentIndex = Array.from(focusableElements).indexOf(currentItem);
    const nextIndex = currentIndex + direction;
    if (nextIndex < 0 || nextIndex >= focusableElements.length) {
      return;
    }
    const targetElement = focusableElements[nextIndex];
    const shouldChangeSlide = nextIndex < this.activeSlide || nextIndex >= this.activeSlide + size;
    if (shouldChangeSlide) {
      this.activeSlide = nextIndex - nextIndex % size;
      targetElement.addEventListener("transitionend", () => {
        targetElement.focus();
      }, {
        once: true
      });
    } else {
      targetElement.focus();
    }
  }
  getSlideNumber(size, currentIndex) {
    const normalizedCurrentIndex = currentIndex + 1;
    return Math.ceil(normalizedCurrentIndex / size);
  }
  shareEvent(event) {
    if (!event) {
      throw new Error("Missing Event");
    }
    this.keybordEvent.next(event);
  }
  static {
    this.ɵfac = function CarouselComponent_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _CarouselComponent)(ɵɵdirectiveInject(ElementRef), ɵɵdirectiveInject(CarouselService));
    };
  }
  static {
    this.ɵcmp = ɵɵdefineComponent({
      type: _CarouselComponent,
      selectors: [["cx-carousel"]],
      hostAttrs: ["ngSkipHydration", "true"],
      inputs: {
        title: "title",
        setItems: [0, "items", "setItems"],
        template: "template",
        itemWidth: "itemWidth",
        hideIndicators: "hideIndicators",
        indicatorIcon: "indicatorIcon",
        previousIcon: "previousIcon",
        nextIcon: "nextIcon",
        trackByFn: "trackByFn"
      },
      outputs: {
        keybordEvent: "keybordEvent"
      },
      standalone: false,
      features: [ɵɵNgOnChangesFeature],
      decls: 2,
      vars: 3,
      consts: [[4, "ngIf"], [1, "carousel-panel", 3, "ngClass"], ["class", "previous", 3, "click", "keydown.arrowleft", "keydown.arrowright", "keydown.arrowup", "keydown.arrowdown", 4, "ngIf"], [1, "slides"], [4, "ngFor", "ngForOf", "ngForTrackBy"], ["class", "next", 3, "click", "keydown.arrowleft", "keydown.arrowright", "keydown.arrowup", "keydown.arrowdown", 4, "ngIf"], ["class", "indicators", 4, "ngIf"], [1, "previous", 3, "click", "keydown.arrowleft", "keydown.arrowright", "keydown.arrowup", "keydown.arrowdown"], [3, "type"], ["class", "slide", 3, "active", 4, "ngIf"], [1, "slide"], ["class", "item", 3, "active", "keydown", 4, "ngIf"], [1, "item", 3, "keydown"], [4, "ngTemplateOutlet", "ngTemplateOutletContext"], [1, "next", 3, "click", "keydown.arrowleft", "keydown.arrowright", "keydown.arrowup", "keydown.arrowdown"], [1, "indicators"], [4, "ngFor", "ngForOf"], [3, "click", 4, "ngIf"], [3, "click"], ["aria-hidden", "true", 3, "type"]],
      template: function CarouselComponent_Template(rf, ctx) {
        if (rf & 1) {
          ɵɵtemplate(0, CarouselComponent_ng_container_0_Template, 8, 7, "ng-container", 0);
          ɵɵpipe(1, "async");
        }
        if (rf & 2) {
          ɵɵproperty("ngIf", ctx.items && ctx.items.length > 0 && ɵɵpipeBind1(1, 1, ctx.size$));
        }
      },
      dependencies: [NgClass, NgForOf, NgIf, NgTemplateOutlet, IconComponent, AsyncPipe, SlicePipe, TranslatePipe],
      encapsulation: 2,
      changeDetection: 0
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(CarouselComponent, [{
    type: Component,
    args: [{
      selector: "cx-carousel",
      changeDetection: ChangeDetectionStrategy.OnPush,
      host: {
        ngSkipHydration: "true"
      },
      standalone: false,
      template: `<ng-container *ngIf="items && items.length > 0 && (size$ | async) as size">
  <h2 *ngIf="title">{{ title }}</h2>

  <div class="carousel-panel" [ngClass]="'size-' + size">
    <button
      *ngIf="size < items.length"
      class="previous"
      (click)="
        $event.stopPropagation();
        activeSlide === 0 ? null : (activeSlide = activeSlide - size)
      "
      [attr.aria-disabled]="activeSlide === 0"
      [attr.aria-label]="'carousel.previousSlide' | cxTranslate"
      [attr.title]="'carousel.previousSlide' | cxTranslate"
      (keydown.arrowleft)="shareEvent($any($event))"
      (keydown.arrowright)="shareEvent($any($event))"
      (keydown.arrowup)="shareEvent($any($event))"
      (keydown.arrowdown)="shareEvent($any($event))"
    >
      <cx-icon [type]="previousIcon"></cx-icon>
    </button>
    <div class="slides">
      <ng-container *ngFor="let _ of items; let i = index; trackBy: trackByFn">
        <div
          class="slide"
          *ngIf="i % size === 0"
          [class.active]="i === activeSlide"
        >
          <ng-container
            *ngFor="
              let item of items | slice: i : i + size;
              let j = index;
              trackBy: trackByFn
            "
          >
            <div
              *ngIf="item | async as data"
              class="item"
              [class.active]="i === activeSlide"
              (keydown)="onItemKeydown($event, size)"
            >
              <ng-container
                *ngTemplateOutlet="
                  template;
                  context: {
                    item: data,
                    itemIndex: i + j,
                  }
                "
              ></ng-container>
            </div>
          </ng-container>
        </div>
      </ng-container>
    </div>

    <button
      *ngIf="size < items.length"
      class="next"
      (click)="
        $event.stopPropagation();
        activeSlide > items.length - size - 1
          ? null
          : (activeSlide = activeSlide + size)
      "
      [attr.aria-disabled]="activeSlide > items.length - size - 1"
      [attr.aria-label]="'carousel.nextSlide' | cxTranslate"
      [attr.title]="'carousel.nextSlide' | cxTranslate"
      (keydown.arrowleft)="shareEvent($any($event))"
      (keydown.arrowright)="shareEvent($any($event))"
      (keydown.arrowup)="shareEvent($any($event))"
      (keydown.arrowdown)="shareEvent($any($event))"
    >
      <cx-icon [type]="nextIcon"></cx-icon>
    </button>
  </div>

  <div *ngIf="!hideIndicators && size < items.length" class="indicators">
    <ng-container *ngFor="let _ of items; let i = index">
      <button
        *ngIf="i % size === 0"
        (click)="$event.stopPropagation(); activeSlide = i"
        [attr.aria-current]="i === activeSlide"
        [attr.aria-disabled]="i === activeSlide"
        [attr.aria-label]="
          'carousel.slideNumber'
            | cxTranslate
              : {
                  currentSlideNumber: getSlideNumber(size, i),
                  numberOfSlides: size,
                }
        "
        [attr.title]="
          'carousel.slideNumber'
            | cxTranslate
              : {
                  currentSlideNumber: getSlideNumber(size, i),
                  numberOfSlides: size,
                }
        "
      >
        <cx-icon [type]="indicatorIcon" aria-hidden="true"></cx-icon>
      </button>
    </ng-container>
  </div>
</ng-container>
`
    }]
  }], () => [{
    type: ElementRef
  }, {
    type: CarouselService
  }], {
    keybordEvent: [{
      type: Output
    }],
    title: [{
      type: Input
    }],
    setItems: [{
      type: Input,
      args: ["items"]
    }],
    template: [{
      type: Input
    }],
    itemWidth: [{
      type: Input
    }],
    hideIndicators: [{
      type: Input
    }],
    indicatorIcon: [{
      type: Input
    }],
    previousIcon: [{
      type: Input
    }],
    nextIcon: [{
      type: Input
    }],
    trackByFn: [{
      type: Input
    }]
  });
})();
var BannerCarouselComponent = class _BannerCarouselComponent {
  constructor(componentData, cmsService) {
    this.componentData = componentData;
    this.cmsService = cmsService;
    this.componentData$ = this.componentData.data$.pipe(filter((data) => Boolean(data)), tap((d) => this.theme = `${d.effect}-theme`));
    this.items$ = this.componentData$.pipe(map((data) => data.banners?.trim().split(" ") ?? []), map((codes) => codes.map((code) => this.cmsService.getComponentData(code))));
    this.theme = "";
  }
  /**
   * Returns an Obervable with an Array of Observables. This is done, so that
   * the component UI could consider to lazy load the UI components when they're
   * in the viewpoint.
   */
  getItems() {
    return this.items$;
  }
  static {
    this.ɵfac = function BannerCarouselComponent_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _BannerCarouselComponent)(ɵɵdirectiveInject(CmsComponentData), ɵɵdirectiveInject(CmsService));
    };
  }
  static {
    this.ɵcmp = ɵɵdefineComponent({
      type: _BannerCarouselComponent,
      selectors: [["cx-banner-carousel"]],
      hostVars: 2,
      hostBindings: function BannerCarouselComponent_HostBindings(rf, ctx) {
        if (rf & 2) {
          ɵɵclassMap(ctx.theme);
        }
      },
      standalone: false,
      decls: 4,
      vars: 3,
      consts: [["template", ""], ["itemWidth", "100%", "class", "inline-navigation", 3, "items", "template", 4, "ngIf"], ["itemWidth", "100%", 1, "inline-navigation", 3, "items", "template"], [3, "cxComponentWrapper"]],
      template: function BannerCarouselComponent_Template(rf, ctx) {
        if (rf & 1) {
          ɵɵtemplate(0, BannerCarouselComponent_cx_carousel_0_Template, 1, 2, "cx-carousel", 1);
          ɵɵpipe(1, "async");
          ɵɵtemplate(2, BannerCarouselComponent_ng_template_2_Template, 1, 5, "ng-template", null, 0, ɵɵtemplateRefExtractor);
        }
        if (rf & 2) {
          ɵɵproperty("ngIf", ɵɵpipeBind1(1, 1, ctx.getItems()));
        }
      },
      dependencies: [NgIf, ComponentWrapperDirective, CarouselComponent, AsyncPipe],
      encapsulation: 2,
      changeDetection: 0
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(BannerCarouselComponent, [{
    type: Component,
    args: [{
      selector: "cx-banner-carousel",
      changeDetection: ChangeDetectionStrategy.OnPush,
      standalone: false,
      template: '<cx-carousel\n  *ngIf="getItems() | async as items"\n  [items]="items"\n  [template]="template"\n  itemWidth="100%"\n  class="inline-navigation"\n></cx-carousel>\n\n<ng-template #template let-item="item">\n  <ng-container\n    [cxComponentWrapper]="{\n      flexType: item.typeCode,\n      typeCode: item.typeCode,\n      uid: item?.uid,\n    }"\n  >\n  </ng-container>\n</ng-template>\n'
    }]
  }], () => [{
    type: CmsComponentData
  }, {
    type: CmsService
  }], {
    theme: [{
      type: HostBinding,
      args: ["class"]
    }]
  });
})();
var SpinnerModule = class _SpinnerModule {
  static {
    this.ɵfac = function SpinnerModule_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _SpinnerModule)();
    };
  }
  static {
    this.ɵmod = ɵɵdefineNgModule({
      type: _SpinnerModule,
      declarations: [SpinnerComponent],
      imports: [CommonModule, I18nModule],
      exports: [SpinnerComponent]
    });
  }
  static {
    this.ɵinj = ɵɵdefineInjector({
      imports: [CommonModule, I18nModule]
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(SpinnerModule, [{
    type: NgModule,
    args: [{
      imports: [CommonModule, I18nModule],
      declarations: [SpinnerComponent],
      exports: [SpinnerComponent]
    }]
  }], null, null);
})();
var ConsentManagementComponentService = class _ConsentManagementComponentService {
  constructor(anonymousConsentsConfig) {
    this.anonymousConsentsConfig = anonymousConsentsConfig;
  }
  /**
   * Returns the list of mandatory consents
   * @param _templateList - list of all active consents. This parameter is not needed in core
   * implementation. But is needed in CDC implementation to fetch only the required consents
   * from this list
   * @returns array of consent IDs
   */
  getRequiredConsents(_templateList) {
    return this.anonymousConsentsConfig?.anonymousConsents?.requiredConsents || [];
  }
  static {
    this.ɵfac = function ConsentManagementComponentService_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _ConsentManagementComponentService)(ɵɵinject(AnonymousConsentsConfig));
    };
  }
  static {
    this.ɵprov = ɵɵdefineInjectable({
      token: _ConsentManagementComponentService,
      factory: _ConsentManagementComponentService.ɵfac
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(ConsentManagementComponentService, [{
    type: Injectable
  }], () => [{
    type: AnonymousConsentsConfig
  }], null);
})();
var ConsentManagementComponent = class _ConsentManagementComponent {
  get allConsentsLoading$() {
    return this.allConsentsLoading.asObservable();
  }
  constructor(userConsentService, globalMessageService, anonymousConsentsConfig, anonymousConsentsService, authService, consentManagementComponentService) {
    this.userConsentService = userConsentService;
    this.globalMessageService = globalMessageService;
    this.anonymousConsentsConfig = anonymousConsentsConfig;
    this.anonymousConsentsService = anonymousConsentsService;
    this.authService = authService;
    this.consentManagementComponentService = consentManagementComponentService;
    this.subscriptions = new Subscription();
    this.allConsentsLoading = new BehaviorSubject(false);
    this.requiredConsents = [];
  }
  ngOnInit() {
    this.loading$ = combineLatest([this.userConsentService.getConsentsResultLoading(), this.userConsentService.getGiveConsentResultLoading(), this.userConsentService.getWithdrawConsentResultLoading(), this.authService.isUserLoggedIn(), this.allConsentsLoading]).pipe(map(([consentLoading, giveConsentLoading, withdrawConsentLoading, isUserLoggedIn, allConsentsLoading]) => consentLoading || giveConsentLoading || withdrawConsentLoading || !isUserLoggedIn || allConsentsLoading));
    this.consentListInit();
    this.giveConsentInit();
    this.withdrawConsentInit();
  }
  consentListInit() {
    this.templateList$ = this.userConsentService.getConsents().pipe(withLatestFrom(this.anonymousConsentsService.getTemplates(), this.authService.isUserLoggedIn()), filter(([_templateList, _anonymousTemplates, isUserLoggedIn]) => isUserLoggedIn), tap(([templateList, _anonymousTemplates]) => {
      if (!this.consentsExists(templateList)) {
        this.userConsentService.loadConsents();
      }
    }), map(([templateList, anonymousTemplates]) => {
      this.requiredConsents = this.consentManagementComponentService ? this.consentManagementComponentService.getRequiredConsents(templateList) : [];
      if (this.anonymousConsentsConfig.anonymousConsents) {
        if (this.anonymousConsentsConfig.anonymousConsents.consentManagementPage) {
          return this.hideAnonymousConsents(templateList, anonymousTemplates);
        }
      }
      return templateList;
    }));
  }
  hideAnonymousConsents(templateList, anonymousTemplates = []) {
    let hideTemplateIds = [];
    if (!this.anonymousConsentsConfig.anonymousConsents?.consentManagementPage?.showAnonymousConsents) {
      hideTemplateIds = anonymousTemplates.map((template) => template.id ?? "");
      return this.userConsentService.filterConsentTemplates(templateList, hideTemplateIds);
    }
    if (this.anonymousConsentsConfig.anonymousConsents.consentManagementPage.hideConsents && this.anonymousConsentsConfig.anonymousConsents.consentManagementPage.hideConsents.length > 0) {
      hideTemplateIds = this.anonymousConsentsConfig.anonymousConsents.consentManagementPage.hideConsents;
    }
    return this.userConsentService.filterConsentTemplates(templateList, hideTemplateIds);
  }
  giveConsentInit() {
    this.userConsentService.resetGiveConsentProcessState();
    this.subscriptions.add(this.userConsentService.getGiveConsentResultSuccess().subscribe((success) => this.onConsentGivenSuccess(success)));
  }
  withdrawConsentInit() {
    this.userConsentService.resetWithdrawConsentProcessState();
    this.subscriptions.add(this.userConsentService.getWithdrawConsentResultLoading().pipe(skipWhile(Boolean), withLatestFrom(this.userConsentService.getWithdrawConsentResultSuccess()), map(([, withdrawalSuccess]) => withdrawalSuccess), tap((withdrawalSuccess) => {
      if (withdrawalSuccess) {
        this.userConsentService.loadConsents();
      }
    })).subscribe((withdrawalSuccess) => this.onConsentWithdrawnSuccess(withdrawalSuccess)));
  }
  consentsExists(templateList) {
    return Boolean(templateList) && templateList.length > 0;
  }
  onConsentChange({
    given,
    template
  }) {
    if (given && template.id && template.version !== void 0) {
      this.userConsentService.giveConsent(template.id, template.version);
    } else if (template.currentConsent?.code) {
      this.userConsentService.withdrawConsent(template.currentConsent.code, template?.id);
    }
  }
  onConsentGivenSuccess(success) {
    if (success) {
      this.userConsentService.resetGiveConsentProcessState();
      this.globalMessageService.add({
        key: "consentManagementForm.message.success.given"
      }, GlobalMessageType.MSG_TYPE_CONFIRMATION);
    }
  }
  onConsentWithdrawnSuccess(success) {
    if (success) {
      this.userConsentService.resetWithdrawConsentProcessState();
      this.globalMessageService.add({
        key: "consentManagementForm.message.success.withdrawn"
      }, GlobalMessageType.MSG_TYPE_CONFIRMATION);
    }
  }
  rejectAll(templates = []) {
    const consentsToWithdraw = [];
    templates.forEach((template) => {
      if (template.currentConsent && this.userConsentService.isConsentGiven(template.currentConsent)) {
        if (this.isRequiredConsent(template)) {
          return;
        }
        consentsToWithdraw.push(template);
      }
    });
    this.allConsentsLoading.next(true);
    this.subscriptions.add(this.setupWithdrawalStream(consentsToWithdraw).pipe(tap((_timesLoaded) => this.allConsentsLoading.next(false))).subscribe());
  }
  setupWithdrawalStream(consentsToWithdraw = []) {
    const loading$ = concat(this.userConsentService.getWithdrawConsentResultLoading()).pipe(distinctUntilChanged(), filter((loading) => !loading));
    const count$ = loading$.pipe(scan((acc, _value) => acc + 1, -1));
    const withdraw$ = count$.pipe(tap((i) => {
      if (i < consentsToWithdraw.length) {
        const code = consentsToWithdraw[i].currentConsent?.code;
        const id = consentsToWithdraw[i]?.id;
        if (code) {
          this.userConsentService.withdrawConsent(code, id);
        }
      }
    }));
    const checkTimesLoaded$ = withdraw$.pipe(filter((timesLoaded) => timesLoaded === consentsToWithdraw.length));
    return checkTimesLoaded$;
  }
  allowAll(templates = []) {
    const consentsToGive = [];
    templates.forEach((template) => {
      const givenDate = template.currentConsent?.consentGivenDate;
      const withdrawnDate = template.currentConsent?.consentWithdrawnDate;
      const isConsentGiven = givenDate && !withdrawnDate || givenDate && withdrawnDate && givenDate > withdrawnDate;
      if (isConsentGiven) {
        return;
      }
      if (template.currentConsent && this.userConsentService.isConsentWithdrawn(template.currentConsent)) {
        if (this.isRequiredConsent(template)) {
          return;
        }
      }
      consentsToGive.push(template);
    });
    this.allConsentsLoading.next(true);
    this.subscriptions.add(this.setupGiveStream(consentsToGive).pipe(tap((_timesLoaded) => this.allConsentsLoading.next(false))).subscribe());
  }
  setupGiveStream(consentsToGive = []) {
    const loading$ = concat(this.userConsentService.getGiveConsentResultLoading()).pipe(distinctUntilChanged(), filter((loading) => !loading));
    const count$ = loading$.pipe(scan((acc, _value) => acc + 1, -1));
    const giveConsent$ = count$.pipe(tap((i) => {
      if (i < consentsToGive.length) {
        const consent = consentsToGive[i];
        if (consent.id && consent.version !== void 0) {
          this.userConsentService.giveConsent(consent.id, consent.version);
        }
      }
    }));
    const checkTimesLoaded$ = giveConsent$.pipe(filter((timesLoaded) => timesLoaded === consentsToGive.length));
    return checkTimesLoaded$;
  }
  isRequiredConsent(template) {
    return Boolean(template.id && this.anonymousConsentsConfig.anonymousConsents && this.anonymousConsentsConfig.anonymousConsents?.requiredConsents && this.anonymousConsentsConfig.anonymousConsents.requiredConsents.includes(template.id));
  }
  ngOnDestroy() {
    this.subscriptions.unsubscribe();
    this.allConsentsLoading.unsubscribe();
    this.userConsentService.resetGiveConsentProcessState();
    this.userConsentService.resetWithdrawConsentProcessState();
  }
  static {
    this.ɵfac = function ConsentManagementComponent_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _ConsentManagementComponent)(ɵɵdirectiveInject(UserConsentService), ɵɵdirectiveInject(GlobalMessageService), ɵɵdirectiveInject(AnonymousConsentsConfig), ɵɵdirectiveInject(AnonymousConsentsService), ɵɵdirectiveInject(AuthService), ɵɵdirectiveInject(ConsentManagementComponentService));
    };
  }
  static {
    this.ɵcmp = ɵɵdefineComponent({
      type: _ConsentManagementComponent,
      selectors: [["cx-consent-management"]],
      standalone: false,
      decls: 4,
      vars: 11,
      consts: [[4, "ngIf"], [1, "cx-consent-action-links"], [1, "col-sm-12", "col-md-8", "col-lg-6"], [1, "btn", "btn-link", "cx-action-link", 3, "click", "disabled"], ["class", "cx-spinner", 4, "ngIf"], [1, "cx-spinner"], [1, "cx-consent-toggles"], [3, "consentTemplate", "requiredConsents", "disabled", "consentChanged", 4, "ngFor", "ngForOf"], [3, "consentChanged", "consentTemplate", "requiredConsents", "disabled"]],
      template: function ConsentManagementComponent_Template(rf, ctx) {
        if (rf & 1) {
          ɵɵtemplate(0, ConsentManagementComponent_ng_container_0_Template, 2, 1, "ng-container", 0);
          ɵɵpipe(1, "async");
          ɵɵpipe(2, "async");
          ɵɵpipe(3, "async");
        }
        if (rf & 2) {
          ɵɵproperty("ngIf", ɵɵpureFunction3(7, _c122, ɵɵpipeBind1(1, 1, ctx.loading$), ɵɵpipeBind1(2, 3, ctx.templateList$), ɵɵpipeBind1(3, 5, ctx.allConsentsLoading$)));
        }
      },
      dependencies: [NgForOf, NgIf, SpinnerComponent, ConsentManagementFormComponent, AsyncPipe, TranslatePipe],
      encapsulation: 2
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(ConsentManagementComponent, [{
    type: Component,
    args: [{
      selector: "cx-consent-management",
      standalone: false,
      template: `<ng-container
  *ngIf="{
    loading: loading$ | async,
    templateList: templateList$ | async,
    allConsentsLoading: allConsentsLoading$ | async,
  } as data"
>
  <ng-container *ngIf="data.templateList as templateList">
    <div class="cx-consent-action-links">
      <div class="col-sm-12 col-md-8 col-lg-6">
        <button
          class="btn btn-link cx-action-link"
          [disabled]="data.loading"
          (click)="rejectAll(templateList)"
        >
          {{ 'consentManagementForm.clearAll' | cxTranslate }}
        </button>
        <button
          class="btn btn-link cx-action-link"
          [disabled]="data.loading"
          (click)="allowAll(templateList)"
        >
          {{ 'consentManagementForm.selectAll' | cxTranslate }}
        </button>
      </div>
    </div>
    <div
      class="cx-spinner"
      *ngIf="data.allConsentsLoading || (data.loading && !templateList?.length)"
    >
      <cx-spinner></cx-spinner>
    </div>

    <ng-container *ngIf="!data.allConsentsLoading">
      <div class="cx-consent-toggles">
        <div class="col-sm-12 col-md-8 col-lg-6">
          <cx-consent-management-form
            *ngFor="let consentTemplate of templateList"
            [consentTemplate]="consentTemplate"
            [requiredConsents]="requiredConsents"
            [disabled]="!!data.loading"
            (consentChanged)="onConsentChange($event)"
          ></cx-consent-management-form>
        </div>
      </div>
    </ng-container>
  </ng-container>
</ng-container>
`
    }]
  }], () => [{
    type: UserConsentService
  }, {
    type: GlobalMessageService
  }, {
    type: AnonymousConsentsConfig
  }, {
    type: AnonymousConsentsService
  }, {
    type: AuthService
  }, {
    type: ConsentManagementComponentService
  }], null);
})();
var USE_MY_ACCOUNT_V2_NOTIFICATION_PREFERENCE = new InjectionToken("feature flag to enable enhanced UI for notification preference pages under My-Account", {
  providedIn: "root",
  factory: () => false
});
var USE_MY_ACCOUNT_V2_CONSENT = new InjectionToken("feature flag to enable enhanced UI for Consent Management pages under My-Account", {
  providedIn: "root",
  factory: () => false
});
var MyAccountV2ConsentManagementFormComponent = class _MyAccountV2ConsentManagementFormComponent extends ConsentManagementFormComponent {
  ngOnInit() {
    if (this.consent) {
      this.consentGiven = Boolean(this.consent.consentState === ANONYMOUS_CONSENT_STATUS.GIVEN);
    } else {
      if (this.consentTemplate && this.consentTemplate.currentConsent) {
        if (this.consentTemplate.currentConsent.consentWithdrawnDate) {
          this.consentGiven = false;
        } else if (this.consentTemplate.currentConsent.consentGivenDate) {
          this.consentGiven = true;
          const date = new Date(this.consentTemplate.currentConsent.consentGivenDate);
          this.consentApprovedTime = `${date.getDate()}/${date.getMonth() + 1}/${date.getFullYear()}`;
        }
      }
    }
  }
  static {
    this.ɵfac = /* @__PURE__ */ (() => {
      let ɵMyAccountV2ConsentManagementFormComponent_BaseFactory;
      return function MyAccountV2ConsentManagementFormComponent_Factory(__ngFactoryType__) {
        return (ɵMyAccountV2ConsentManagementFormComponent_BaseFactory || (ɵMyAccountV2ConsentManagementFormComponent_BaseFactory = ɵɵgetInheritedFactory(_MyAccountV2ConsentManagementFormComponent)))(__ngFactoryType__ || _MyAccountV2ConsentManagementFormComponent);
      };
    })();
  }
  static {
    this.ɵcmp = ɵɵdefineComponent({
      type: _MyAccountV2ConsentManagementFormComponent,
      selectors: [["cx-my-account-v2-consent-management-form"]],
      standalone: false,
      features: [ɵɵInheritDefinitionFeature],
      decls: 10,
      vars: 8,
      consts: [[1, "form-check", "consent-container"], [1, ""], ["type", "checkbox", 1, "checkbox-input", 3, "change", "id", "name", "checked", "disabled"], [3, "for"], [1, "name"], [4, "ngIf"], [1, "description"], ["class", "description", 4, "ngIf"]],
      template: function MyAccountV2ConsentManagementFormComponent_Template(rf, ctx) {
        if (rf & 1) {
          ɵɵelementStart(0, "div", 0)(1, "div", 1)(2, "label")(3, "input", 2);
          ɵɵlistener("change", function MyAccountV2ConsentManagementFormComponent_Template_input_change_3_listener() {
            return ctx.onConsentChange();
          });
          ɵɵelementEnd();
          ɵɵelement(4, "label", 3);
          ɵɵelementStart(5, "span", 4);
          ɵɵtemplate(6, MyAccountV2ConsentManagementFormComponent_ng_container_6_Template, 2, 1, "ng-container", 5);
          ɵɵelementEnd();
          ɵɵelementStart(7, "span", 6);
          ɵɵtext(8);
          ɵɵelementEnd();
          ɵɵtemplate(9, MyAccountV2ConsentManagementFormComponent_span_9_Template, 3, 4, "span", 7);
          ɵɵelementEnd()()();
        }
        if (rf & 2) {
          ɵɵadvance(3);
          ɵɵproperty("id", ctx.consentTemplate.id)("name", ctx.consentTemplate.id)("checked", ctx.consentGiven)("disabled", ctx.isRequired(ctx.consentTemplate == null ? null : ctx.consentTemplate.id));
          ɵɵadvance();
          ɵɵproperty("for", ctx.consentTemplate.id);
          ɵɵadvance(2);
          ɵɵproperty("ngIf", ctx.consentTemplate == null ? null : ctx.consentTemplate.name);
          ɵɵadvance(2);
          ɵɵtextInterpolate1(" ", ctx.consentTemplate == null ? null : ctx.consentTemplate.description, " ");
          ɵɵadvance();
          ɵɵproperty("ngIf", ctx.consentGiven);
        }
      },
      dependencies: [NgIf, TranslatePipe],
      encapsulation: 2
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(MyAccountV2ConsentManagementFormComponent, [{
    type: Component,
    args: [{
      selector: "cx-my-account-v2-consent-management-form",
      standalone: false,
      template: `<div class="form-check consent-container">
  <div class="">
    <label>
      <input
        type="checkbox"
        class="checkbox-input"
        [id]="consentTemplate.id"
        [name]="consentTemplate.id"
        [checked]="consentGiven"
        (change)="onConsentChange()"
        [disabled]="isRequired(consentTemplate?.id)"
      />
      <label [for]="consentTemplate.id"></label>
      <span class="name">
        <ng-container *ngIf="consentTemplate?.name">
          {{ consentTemplate?.name }}
        </ng-container>
      </span>
      <span class="description">
        {{ consentTemplate?.description }}
      </span>
      <span class="description" *ngIf="consentGiven">
        {{ 'myAccountV2Consent.dateDescription' | cxTranslate
        }}{{ consentApprovedTime }}
      </span>
    </label>
  </div>
</div>
`
    }]
  }], null, null);
})();
var MyAccountV2ConsentManagementComponent = class _MyAccountV2ConsentManagementComponent extends ConsentManagementComponent {
  static {
    this.ɵfac = /* @__PURE__ */ (() => {
      let ɵMyAccountV2ConsentManagementComponent_BaseFactory;
      return function MyAccountV2ConsentManagementComponent_Factory(__ngFactoryType__) {
        return (ɵMyAccountV2ConsentManagementComponent_BaseFactory || (ɵMyAccountV2ConsentManagementComponent_BaseFactory = ɵɵgetInheritedFactory(_MyAccountV2ConsentManagementComponent)))(__ngFactoryType__ || _MyAccountV2ConsentManagementComponent);
      };
    })();
  }
  static {
    this.ɵcmp = ɵɵdefineComponent({
      type: _MyAccountV2ConsentManagementComponent,
      selectors: [["cx-my-account-v2-consent-management"]],
      standalone: false,
      features: [ɵɵInheritDefinitionFeature],
      decls: 4,
      vars: 4,
      consts: [["consentManagementForm", ""], [4, "ngIf", "ngIfElse"], [1, "cx-spinner"], [4, "ngIf"], [1, "cx-consent-toggles"], [1, "consent-form-container"], [1, "header"], [1, "cx-consent-action-links"], ["tabindex", "0", 1, "btn", "btn-link", "cx-action-link", 3, "click"], [3, "consentTemplate", "requiredConsents", "consentChanged", 4, "ngFor", "ngForOf"], [3, "consentChanged", "consentTemplate", "requiredConsents"]],
      template: function MyAccountV2ConsentManagementComponent_Template(rf, ctx) {
        if (rf & 1) {
          ɵɵtemplate(0, MyAccountV2ConsentManagementComponent_div_0_Template, 3, 0, "div", 1);
          ɵɵpipe(1, "async");
          ɵɵtemplate(2, MyAccountV2ConsentManagementComponent_ng_template_2_Template, 2, 3, "ng-template", null, 0, ɵɵtemplateRefExtractor);
        }
        if (rf & 2) {
          const consentManagementForm_r6 = ɵɵreference(3);
          ɵɵproperty("ngIf", ɵɵpipeBind1(1, 2, ctx.loading$))("ngIfElse", consentManagementForm_r6);
        }
      },
      dependencies: [NgForOf, NgIf, SpinnerComponent, MyAccountV2ConsentManagementFormComponent, AsyncPipe, TranslatePipe],
      encapsulation: 2
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(MyAccountV2ConsentManagementComponent, [{
    type: Component,
    args: [{
      selector: "cx-my-account-v2-consent-management",
      standalone: false,
      template: `<div *ngIf="loading$ | async; else consentManagementForm">
  <div class="cx-spinner">
    <cx-spinner></cx-spinner>
  </div>
</div>
<ng-template #consentManagementForm>
  <ng-container *ngIf="templateList$ | async as templateList">
    <div class="cx-consent-toggles">
      <div class="consent-form-container">
        <div class="header">
          {{ 'myAccountV2Consent.header' | cxTranslate }}
        </div>
        <div class="cx-consent-action-links">
          <button
            tabindex="0"
            class="btn btn-link cx-action-link"
            (click)="rejectAll(templateList)"
          >
            {{ 'myAccountV2Consent.clearAll' | cxTranslate }}
          </button>
          <button
            tabindex="0"
            class="btn btn-link cx-action-link"
            (click)="allowAll(templateList)"
          >
            {{ 'myAccountV2Consent.selectAll' | cxTranslate }}
          </button>
        </div>
        <cx-my-account-v2-consent-management-form
          *ngFor="let consentTemplate of templateList"
          [consentTemplate]="consentTemplate"
          [requiredConsents]="requiredConsents"
          (consentChanged)="onConsentChange($event)"
        ></cx-my-account-v2-consent-management-form>
      </div>
    </div>
  </ng-container>
</ng-template>
`
    }]
  }], null, null);
})();
var MyAccountV2ConsentManagementModule = class _MyAccountV2ConsentManagementModule {
  static {
    this.ɵfac = function MyAccountV2ConsentManagementModule_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _MyAccountV2ConsentManagementModule)();
    };
  }
  static {
    this.ɵmod = ɵɵdefineNgModule({
      type: _MyAccountV2ConsentManagementModule,
      declarations: [MyAccountV2ConsentManagementComponent, MyAccountV2ConsentManagementFormComponent],
      imports: [CommonModule, FormsModule, ReactiveFormsModule, SpinnerModule, I18nModule, IconModule, FeaturesConfigModule],
      exports: [MyAccountV2ConsentManagementComponent, MyAccountV2ConsentManagementFormComponent]
    });
  }
  static {
    this.ɵinj = ɵɵdefineInjector({
      providers: [ConsentManagementComponentService],
      imports: [CommonModule, FormsModule, ReactiveFormsModule, SpinnerModule, I18nModule, IconModule, FeaturesConfigModule]
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(MyAccountV2ConsentManagementModule, [{
    type: NgModule,
    args: [{
      imports: [CommonModule, FormsModule, ReactiveFormsModule, SpinnerModule, I18nModule, IconModule, FeaturesConfigModule],
      providers: [ConsentManagementComponentService],
      declarations: [MyAccountV2ConsentManagementComponent, MyAccountV2ConsentManagementFormComponent],
      exports: [MyAccountV2ConsentManagementComponent, MyAccountV2ConsentManagementFormComponent]
    }]
  }], null, null);
})();
var myAccountV2CmsMapping$1 = {
  cmsComponents: {
    ConsentManagementComponent: {
      component: MyAccountV2ConsentManagementComponent
      //guards: inherited from standard config,
    }
  }
};
var ConsentManagementModule = class _ConsentManagementModule {
  static {
    this.ɵfac = function ConsentManagementModule_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _ConsentManagementModule)();
    };
  }
  static {
    this.ɵmod = ɵɵdefineNgModule({
      type: _ConsentManagementModule,
      declarations: [ConsentManagementComponent, ConsentManagementFormComponent],
      imports: [CommonModule, FormsModule, ReactiveFormsModule, SpinnerModule, I18nModule, IconModule, FeaturesConfigModule],
      exports: [ConsentManagementComponent, ConsentManagementFormComponent]
    });
  }
  static {
    this.ɵinj = ɵɵdefineInjector({
      providers: [ConsentManagementComponentService, provideDefaultConfig({
        cmsComponents: {
          ConsentManagementComponent: {
            component: ConsentManagementComponent,
            guards: [AuthGuard]
          }
        }
      }), provideDefaultConfigFactory(() => inject(USE_MY_ACCOUNT_V2_CONSENT) ? myAccountV2CmsMapping$1 : {})],
      imports: [CommonModule, FormsModule, ReactiveFormsModule, SpinnerModule, I18nModule, IconModule, FeaturesConfigModule]
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(ConsentManagementModule, [{
    type: NgModule,
    args: [{
      imports: [CommonModule, FormsModule, ReactiveFormsModule, SpinnerModule, I18nModule, IconModule, FeaturesConfigModule],
      providers: [ConsentManagementComponentService, provideDefaultConfig({
        cmsComponents: {
          ConsentManagementComponent: {
            component: ConsentManagementComponent,
            guards: [AuthGuard]
          }
        }
      }), provideDefaultConfigFactory(() => inject(USE_MY_ACCOUNT_V2_CONSENT) ? myAccountV2CmsMapping$1 : {})],
      declarations: [ConsentManagementComponent, ConsentManagementFormComponent],
      exports: [ConsentManagementComponent, ConsentManagementFormComponent]
    }]
  }], null, null);
})();
var AtMessageModule = class _AtMessageModule {
  static {
    this.ɵfac = function AtMessageModule_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _AtMessageModule)();
    };
  }
  static {
    this.ɵmod = ɵɵdefineNgModule({
      type: _AtMessageModule,
      declarations: [AtMessageDirective],
      exports: [AtMessageDirective]
    });
  }
  static {
    this.ɵinj = ɵɵdefineInjector({});
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(AtMessageModule, [{
    type: NgModule,
    args: [{
      declarations: [AtMessageDirective],
      exports: [AtMessageDirective]
    }]
  }], null, null);
})();
var MessageComponentModule = class _MessageComponentModule {
  static {
    this.ɵfac = function MessageComponentModule_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _MessageComponentModule)();
    };
  }
  static {
    this.ɵmod = ɵɵdefineNgModule({
      type: _MessageComponentModule,
      declarations: [MessageComponent],
      imports: [AtMessageModule, CommonModule, I18nModule, IconModule, FeaturesConfigModule],
      exports: [MessageComponent]
    });
  }
  static {
    this.ɵinj = ɵɵdefineInjector({
      imports: [AtMessageModule, CommonModule, I18nModule, IconModule, FeaturesConfigModule]
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(MessageComponentModule, [{
    type: NgModule,
    args: [{
      imports: [AtMessageModule, CommonModule, I18nModule, IconModule, FeaturesConfigModule],
      declarations: [MessageComponent],
      exports: [MessageComponent]
    }]
  }], null, null);
})();
var AnonymousConsentsDialogModule = class _AnonymousConsentsDialogModule {
  static {
    this.ɵfac = function AnonymousConsentsDialogModule_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _AnonymousConsentsDialogModule)();
    };
  }
  static {
    this.ɵmod = ɵɵdefineNgModule({
      type: _AnonymousConsentsDialogModule,
      declarations: [AnonymousConsentDialogComponent],
      imports: [CommonModule, I18nModule, IconModule, SpinnerModule, ConsentManagementModule, KeyboardFocusModule, FeaturesConfigModule, MessageComponentModule],
      exports: [AnonymousConsentDialogComponent]
    });
  }
  static {
    this.ɵinj = ɵɵdefineInjector({
      imports: [CommonModule, I18nModule, IconModule, SpinnerModule, ConsentManagementModule, KeyboardFocusModule, FeaturesConfigModule, MessageComponentModule]
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(AnonymousConsentsDialogModule, [{
    type: NgModule,
    args: [{
      imports: [CommonModule, I18nModule, IconModule, SpinnerModule, ConsentManagementModule, KeyboardFocusModule, FeaturesConfigModule, MessageComponentModule],
      declarations: [AnonymousConsentDialogComponent],
      exports: [AnonymousConsentDialogComponent]
    }]
  }], null, null);
})();
var PopoverEvent;
(function(PopoverEvent2) {
  PopoverEvent2["INSIDE_CLICK"] = "insideClick";
  PopoverEvent2["OUTSIDE_CLICK"] = "outsideClick";
  PopoverEvent2["ESCAPE_KEYDOWN"] = "escapeKeydown";
  PopoverEvent2["CLOSE_BUTTON_CLICK"] = "closeButtonClick";
  PopoverEvent2["CLOSE_BUTTON_KEYDOWN"] = "closeButtonKeydown";
  PopoverEvent2["ROUTE_CHANGE"] = "routeChange";
  PopoverEvent2["OPEN"] = "open";
  PopoverEvent2["OPEN_BY_KEYBOARD"] = "openByKeyboard";
})(PopoverEvent || (PopoverEvent = {}));
var PositioningService = class _PositioningService {
  constructor(winRef) {
    this.winRef = winRef;
  }
  get allowedPlacements() {
    return ["top", "bottom", "left", "right", "top-left", "top-right", "bottom-left", "bottom-right", "left-top", "left-bottom", "right-top", "right-bottom"];
  }
  get placementSeparator() {
    return /\s+/;
  }
  get window() {
    return this.winRef.nativeWindow;
  }
  get document() {
    return this.winRef.document;
  }
  getAllStyles(element) {
    return this.window?.getComputedStyle(element);
  }
  getPositionStyleProperty(element) {
    const styles = this.getAllStyles(element);
    if (styles) {
      return styles["position"] || void 0;
    }
  }
  isStaticPositioned(element) {
    return (this.getPositionStyleProperty(element) || "static") === "static";
  }
  offsetParent(element) {
    let offsetParentEl = element.offsetParent || this.document.documentElement;
    while (offsetParentEl && offsetParentEl !== this.document.documentElement && this.isStaticPositioned(offsetParentEl)) {
      offsetParentEl = offsetParentEl.offsetParent;
    }
    return offsetParentEl || this.document.documentElement;
  }
  position(element, round = true) {
    let elPosition;
    let parentOffset = {
      width: 0,
      height: 0,
      top: 0,
      bottom: 0,
      left: 0,
      right: 0
    };
    if (this.getPositionStyleProperty(element) === "fixed") {
      elPosition = element.getBoundingClientRect();
      elPosition = {
        top: elPosition.top,
        bottom: elPosition.bottom,
        left: elPosition.left,
        right: elPosition.right,
        height: elPosition.height,
        width: elPosition.width
      };
    } else {
      const offsetParentEl = this.offsetParent(element);
      elPosition = this.offset(element, false);
      if (offsetParentEl !== this.document.documentElement) {
        parentOffset = this.offset(offsetParentEl, false);
      }
      parentOffset.top += offsetParentEl.clientTop;
      parentOffset.left += offsetParentEl.clientLeft;
    }
    elPosition.top -= parentOffset.top;
    elPosition.bottom -= parentOffset.top;
    elPosition.left -= parentOffset.left;
    elPosition.right -= parentOffset.left;
    if (round) {
      elPosition.top = Math.round(elPosition.top);
      elPosition.bottom = Math.round(elPosition.bottom);
      elPosition.left = Math.round(elPosition.left);
      elPosition.right = Math.round(elPosition.right);
    }
    return elPosition;
  }
  offset(element, round = true) {
    const elBcr = element.getBoundingClientRect();
    const viewportOffset = {
      top: this.window && this.window.pageYOffset - this.document.documentElement.clientTop || 0,
      left: this.window && this.window.pageXOffset - this.document.documentElement.clientLeft || 0
    };
    const elOffset = {
      height: elBcr.height || element.offsetHeight,
      width: elBcr.width || element.offsetWidth,
      top: elBcr.top + viewportOffset.top,
      bottom: elBcr.bottom + viewportOffset.top,
      left: elBcr.left + viewportOffset.left,
      right: elBcr.right + viewportOffset.left
    };
    if (round) {
      elOffset.height = Math.round(elOffset.height);
      elOffset.width = Math.round(elOffset.width);
      elOffset.top = Math.round(elOffset.top);
      elOffset.bottom = Math.round(elOffset.bottom);
      elOffset.left = Math.round(elOffset.left);
      elOffset.right = Math.round(elOffset.right);
    }
    return elOffset;
  }
  /*
    Return false if the element to position is outside the viewport.
  */
  _positionElements(hostElement, targetElement, placement, appendToBody) {
    const [placementPrimary = "top", placementSecondary = "center"] = placement.split("-");
    const hostElPosition = appendToBody ? this.offset(hostElement, false) : this.position(hostElement, false);
    const targetElStyles = this.getAllStyles(targetElement);
    if (targetElStyles) {
      const marginTop = parseFloat(targetElStyles.marginTop);
      const marginBottom = parseFloat(targetElStyles.marginBottom);
      const marginLeft = parseFloat(targetElStyles.marginLeft);
      const marginRight = parseFloat(targetElStyles.marginRight);
      let topPosition = 0;
      let leftPosition = 0;
      switch (placementPrimary) {
        case "top":
          topPosition = hostElPosition.top - (targetElement.offsetHeight + marginTop + marginBottom);
          break;
        case "bottom":
          topPosition = hostElPosition.top + hostElPosition.height;
          break;
        case "left":
          leftPosition = hostElPosition.left - (targetElement.offsetWidth + marginLeft + marginRight);
          break;
        case "right":
          leftPosition = hostElPosition.left + hostElPosition.width;
          break;
      }
      switch (placementSecondary) {
        case "top":
          topPosition = hostElPosition.top;
          break;
        case "bottom":
          topPosition = hostElPosition.top + hostElPosition.height - targetElement.offsetHeight;
          break;
        case "left":
          leftPosition = hostElPosition.left;
          break;
        case "right":
          leftPosition = hostElPosition.left + hostElPosition.width - targetElement.offsetWidth;
          break;
        case "center":
          if (placementPrimary === "top" || placementPrimary === "bottom") {
            leftPosition = hostElPosition.left + hostElPosition.width / 2 - targetElement.offsetWidth / 2;
          } else {
            topPosition = hostElPosition.top + hostElPosition.height / 2 - targetElement.offsetHeight / 2;
          }
          break;
      }
      targetElement.style.transform = `translate(${Math.round(leftPosition)}px, ${Math.round(topPosition)}px)`;
      const targetElBCR = targetElement.getBoundingClientRect();
      const html = this.document.documentElement;
      const windowHeight = this.window?.innerHeight || html.clientHeight;
      const windowWidth = this.window?.innerWidth || html.clientWidth;
      return targetElBCR.left >= 0 && targetElBCR.top >= 0 && targetElBCR.right <= windowWidth && targetElBCR.bottom <= windowHeight;
    }
    return false;
  }
  /*
   * Accept the placement array and applies the appropriate placement dependent on the viewport.
   * Returns the applied placement.
   * In case of auto placement, placements are selected in order
   *   'top', 'bottom', 'left', 'right',
   *   'top-left', 'top-right',
   *   'bottom-left', 'bottom-right',
   *   'left-top', 'left-bottom',
   *   'right-top', 'right-bottom'.
   * */
  positionElements(hostElement, targetElement, placement, appendToBody) {
    const placementVals = Array.isArray(placement) ? placement : placement.split(this.placementSeparator);
    let hasAuto = placementVals.findIndex((val) => val === "auto");
    if (hasAuto >= 0) {
      this.allowedPlacements.forEach((obj) => {
        if (placementVals.find((val) => val.search("^" + obj) !== -1) == null) {
          placementVals.splice(hasAuto++, 1, obj);
        }
      });
    }
    const style = targetElement.style;
    style.position = "absolute";
    style.top = "0";
    style.left = "0";
    let testPlacement = "auto";
    let isInViewport = false;
    for (testPlacement of placementVals) {
      if (this._positionElements(hostElement, targetElement, testPlacement, appendToBody)) {
        isInViewport = true;
        break;
      }
    }
    if (!isInViewport) {
      this._positionElements(hostElement, targetElement, testPlacement, appendToBody);
    }
    return testPlacement;
  }
  getPositioningClass(position, autoPositioning) {
    let positionClass = `${position || "top"}`;
    if (autoPositioning && positionClass !== "auto") {
      positionClass = `${positionClass} auto`;
    }
    return positionClass;
  }
  static {
    this.ɵfac = function PositioningService_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _PositioningService)(ɵɵinject(WindowRef));
    };
  }
  static {
    this.ɵprov = ɵɵdefineInjectable({
      token: _PositioningService,
      factory: _PositioningService.ɵfac,
      providedIn: "root"
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PositioningService, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], () => [{
    type: WindowRef
  }], null);
})();
var PopoverComponent = class _PopoverComponent {
  /**
   * Listens for click inside popover component wrapper.
   */
  insideClick() {
    this.eventSubject.next(PopoverEvent.INSIDE_CLICK);
  }
  /**
   * Listens for every document click and ignores clicks
   * inside component.
   */
  outsideClick(event) {
    if (!this.isClickedOnPopover(event) && !this.isClickedOnDirective(event)) {
      this.eventSubject.next(PopoverEvent.OUTSIDE_CLICK);
    }
  }
  /**
   * Listens for `escape` keydown event.
   */
  escapeKeydown() {
    this.eventSubject.next(PopoverEvent.ESCAPE_KEYDOWN);
  }
  isClickedOnPopover(event) {
    return this.popoverInstance.location.nativeElement.contains(event.target);
  }
  isClickedOnDirective(event) {
    return this.triggerElement.nativeElement.contains(event.target);
  }
  /**
   * Emits close event trigger.
   */
  close(event) {
    event.preventDefault();
    if (event instanceof MouseEvent) {
      this.eventSubject.next(PopoverEvent.CLOSE_BUTTON_CLICK);
    } else {
      this.eventSubject.next(PopoverEvent.CLOSE_BUTTON_KEYDOWN);
    }
  }
  /**
   * Method uses `Renderer2` service to listen window scroll event.
   *
   * Registered only if property `positionOnScroll` is set to `true`.
   */
  triggerScrollEvent() {
    this.scrollEventUnlistener = this.renderer.listen(this.winRef.nativeWindow, "scroll", () => this.positionPopover());
  }
  /**
   * Method uses positioning service calculation and based on that
   * updates class name for popover component instance.
   */
  positionPopover() {
    this.popoverClass = this.positioningService.positionElements(this.triggerElement.nativeElement, this.popoverInstance.location.nativeElement, this.positioningService.getPositioningClass(this.position, this.autoPositioning), this.appendToBody);
    this.changeDetectionRef.markForCheck();
    this.baseClass = `${this.customClass} ${this.popoverClass} opened`;
  }
  ngOnInit() {
    if (!this.customClass) {
      this.customClass = "cx-popover";
    }
    if (!this.position) {
      this.position = "top";
    }
    if (this.autoPositioning === void 0) {
      this.autoPositioning = true;
    }
    this.baseClass = `${this.customClass}`;
    this.resizeSub = this.winRef.resize$.subscribe(() => {
      this.positionPopover();
    });
    this.routeChangeSub = this.router.events.pipe(filter((event) => event instanceof NavigationStart)).subscribe(() => {
      this.eventSubject.next(PopoverEvent.ROUTE_CHANGE);
    });
    if (this.positionOnScroll) {
      this.triggerScrollEvent();
    }
  }
  /**
   * indicates if passed content is a TemplateRef or string.
   */
  isTemplate(content) {
    return content instanceof TemplateRef;
  }
  isString(content) {
    return !(content instanceof TemplateRef);
  }
  ngAfterViewChecked() {
    this.positionPopover();
  }
  ngOnDestroy() {
    if (this.resizeSub) {
      this.resizeSub.unsubscribe();
    }
    if (this.routeChangeSub) {
      this.routeChangeSub.unsubscribe();
    }
    if (this.scrollEventUnlistener) {
      this.scrollEventUnlistener();
    }
  }
  constructor(positioningService, winRef, changeDetectionRef, renderer, router) {
    this.positioningService = positioningService;
    this.winRef = winRef;
    this.changeDetectionRef = changeDetectionRef;
    this.renderer = renderer;
    this.router = router;
    this.iconTypes = ICON_TYPE;
  }
  static {
    this.ɵfac = function PopoverComponent_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _PopoverComponent)(ɵɵdirectiveInject(PositioningService), ɵɵdirectiveInject(WindowRef), ɵɵdirectiveInject(ChangeDetectorRef), ɵɵdirectiveInject(Renderer2), ɵɵdirectiveInject(Router));
    };
  }
  static {
    this.ɵcmp = ɵɵdefineComponent({
      type: _PopoverComponent,
      selectors: [["cx-popover"]],
      hostVars: 1,
      hostBindings: function PopoverComponent_HostBindings(rf, ctx) {
        if (rf & 1) {
          ɵɵlistener("click", function PopoverComponent_click_HostBindingHandler() {
            return ctx.insideClick();
          })("click", function PopoverComponent_click_HostBindingHandler($event) {
            return ctx.outsideClick($event);
          }, false, ɵɵresolveDocument)("keydown.escape", function PopoverComponent_keydown_escape_HostBindingHandler() {
            return ctx.escapeKeydown();
          });
        }
        if (rf & 2) {
          ɵɵhostProperty("className", ctx.baseClass);
        }
      },
      standalone: false,
      decls: 6,
      vars: 4,
      consts: [[1, "arrow"], [1, "popover-body", 3, "cxFocus"], [1, "cx-close-row"], ["type", "button", "class", "close", 3, "title", "keydown.enter", "keydown.space", "click", 4, "ngIf"], [4, "ngIf"], ["type", "button", 1, "close", 3, "keydown.enter", "keydown.space", "click", "title"], [3, "type"], [4, "ngTemplateOutlet"]],
      template: function PopoverComponent_Template(rf, ctx) {
        if (rf & 1) {
          ɵɵelement(0, "div", 0);
          ɵɵelementStart(1, "div", 1)(2, "div", 2);
          ɵɵtemplate(3, PopoverComponent_button_3_Template, 4, 7, "button", 3);
          ɵɵelementEnd();
          ɵɵtemplate(4, PopoverComponent_ng_container_4_Template, 2, 1, "ng-container", 4)(5, PopoverComponent_span_5_Template, 2, 1, "span", 4);
          ɵɵelementEnd();
        }
        if (rf & 2) {
          ɵɵadvance();
          ɵɵproperty("cxFocus", ctx.focusConfig);
          ɵɵadvance(2);
          ɵɵproperty("ngIf", ctx.displayCloseButton);
          ɵɵadvance();
          ɵɵproperty("ngIf", ctx.isTemplate(ctx.content));
          ɵɵadvance();
          ɵɵproperty("ngIf", ctx.isString(ctx.content));
        }
      },
      dependencies: [NgIf, NgTemplateOutlet, FocusDirective, IconComponent, TranslatePipe],
      encapsulation: 2,
      changeDetection: 0
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PopoverComponent, [{
    type: Component,
    args: [{
      selector: "cx-popover",
      changeDetection: ChangeDetectionStrategy.OnPush,
      standalone: false,
      template: `<div class="arrow"></div>
<div class="popover-body" [cxFocus]="focusConfig">
  <div class="cx-close-row">
    <button
      *ngIf="displayCloseButton"
      type="button"
      class="close"
      attr.aria-label="{{ 'common.close' | cxTranslate }}"
      title="{{ 'common.close' | cxTranslate }}"
      (keydown.enter)="close($event)"
      (keydown.space)="close($event)"
      (click)="close($event)"
    >
      <cx-icon [type]="iconTypes.CLOSE"></cx-icon>
    </button>
  </div>
  <ng-container *ngIf="isTemplate(content)">
    <ng-container *ngTemplateOutlet="content"></ng-container>
  </ng-container>
  <span *ngIf="isString(content)">{{ content }}</span>
</div>
`
    }]
  }], () => [{
    type: PositioningService
  }, {
    type: WindowRef
  }, {
    type: ChangeDetectorRef
  }, {
    type: Renderer2
  }, {
    type: Router
  }], {
    baseClass: [{
      type: HostBinding,
      args: ["className"]
    }],
    insideClick: [{
      type: HostListener,
      args: ["click"]
    }],
    outsideClick: [{
      type: HostListener,
      args: ["document:click", ["$event"]]
    }],
    escapeKeydown: [{
      type: HostListener,
      args: ["keydown.escape"]
    }]
  });
})();
var PopoverService = class _PopoverService {
  /**
   * For a11y improvements method returns different `FocusConfig`
   * based on which event popover was triggered.
   */
  getFocusConfig(event, appendToBody) {
    let config = {};
    if (event === PopoverEvent.OPEN_BY_KEYBOARD && appendToBody) {
      config = {
        trap: true,
        block: true,
        focusOnEscape: false,
        autofocus: true
      };
    }
    return config;
  }
  setFocusOnElement(element) {
    setTimeout(() => {
      element.nativeElement.focus();
    }, 0);
  }
  static {
    this.ɵfac = function PopoverService_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _PopoverService)();
    };
  }
  static {
    this.ɵprov = ɵɵdefineInjectable({
      token: _PopoverService,
      factory: _PopoverService.ɵfac,
      providedIn: "root"
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PopoverService, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], null, null);
})();
var PopoverDirective = class _PopoverDirective {
  /**
   * Listen events fired on element binded to popover directive.
   *
   * Based on event type some a11y improvements can be made.
   * For example if popover was opened by `space` or `enter` key
   * dedicated `FocusConfig` can be set to autofocus first
   * focusable element in popover container.
   */
  handlePress(event) {
    event?.preventDefault();
    if (event?.target === this.element.nativeElement && !this.isOpen) {
      this.eventSubject.next(PopoverEvent.OPEN_BY_KEYBOARD);
    } else if (this.isOpen) {
      this.eventSubject.next(PopoverEvent.CLOSE_BUTTON_KEYDOWN);
    }
  }
  handleTab() {
    if (!this.focusConfig?.trap && this.isOpen) {
      this.eventSubject.next(PopoverEvent.CLOSE_BUTTON_KEYDOWN);
    }
  }
  handleEscape() {
    this.eventSubject.next(PopoverEvent.ESCAPE_KEYDOWN);
  }
  handleClick(event) {
    event?.preventDefault();
    if (event?.target === this.element.nativeElement && !this.isOpen) {
      this.eventSubject.next(PopoverEvent.OPEN);
    } else if (this.isOpen) {
      this.eventSubject.next(PopoverEvent.CLOSE_BUTTON_CLICK);
    }
  }
  /**
   * Method performs open action for popover component.
   */
  open(event) {
    if (!this.cxPopoverOptions?.disable) {
      this.isOpen = true;
      this.focusConfig = this.popoverService.getFocusConfig(event, this.cxPopoverOptions?.appendToBody || false);
      this.renderPopover();
      this.openPopover.emit();
    }
  }
  /**
   * Method performs close action for popover component.
   */
  close() {
    this.isOpen = false;
    this.viewContainer.clear();
    this.closePopover.emit();
  }
  /**
   * Method subscribes for events emitted by popover component
   * and based on event performs specific action.
   */
  handlePopoverEvents() {
    this.eventSubject.subscribe((event) => {
      if (this.openTriggerEvents.includes(event)) {
        this.open(event);
      }
      if (this.focusPopoverTriggerEvents.includes(event)) {
        this.popoverContainer.location.nativeElement.focus();
      }
      if (this.closeTriggerEvents.includes(event)) {
        this.close();
      }
      if (this.focusDirectiveTriggerEvents.includes(event)) {
        this.popoverService.setFocusOnElement(this.element);
      }
    });
  }
  /**
   * Method creates instance and pass parameters to popover component.
   */
  renderPopover() {
    const containerFactory = this.componentFactoryResolver.resolveComponentFactory(PopoverComponent);
    this.popoverContainer = this.viewContainer.createComponent(containerFactory);
    const componentInstance = this.popoverContainer.instance;
    if (componentInstance) {
      componentInstance.content = this.cxPopover;
      componentInstance.triggerElement = this.element;
      componentInstance.popoverInstance = this.popoverContainer;
      componentInstance.focusConfig = this.focusConfig;
      componentInstance.eventSubject = this.eventSubject;
      componentInstance.position = this.cxPopoverOptions?.placement;
      componentInstance.customClass = this.cxPopoverOptions?.class;
      componentInstance.appendToBody = this.cxPopoverOptions?.appendToBody;
      componentInstance.positionOnScroll = this.cxPopoverOptions?.positionOnScroll;
      componentInstance.displayCloseButton = this.cxPopoverOptions?.displayCloseButton;
      componentInstance.autoPositioning = this.cxPopoverOptions?.autoPositioning;
      if (this.cxPopoverOptions?.appendToBody) {
        const body = this.winRef.document.body;
        const element = this.featureConfigService?.isEnabled("a11yPopoverHighContrast") ? (
          // we need to select first child element if exists,
          // otherwise HCT theming in popover will not be picked up.
          body.firstElementChild ?? body
        ) : body;
        this.renderer.appendChild(element, this.popoverContainer.location.nativeElement);
      }
      this.popoverContainer.changeDetectorRef.detectChanges();
    }
  }
  ngOnInit() {
    this.handlePopoverEvents();
  }
  constructor(element, viewContainer, componentFactoryResolver, renderer, changeDetectorRef, popoverService, winRef) {
    this.element = element;
    this.viewContainer = viewContainer;
    this.componentFactoryResolver = componentFactoryResolver;
    this.renderer = renderer;
    this.changeDetectorRef = changeDetectorRef;
    this.popoverService = popoverService;
    this.winRef = winRef;
    this.openPopover = new EventEmitter();
    this.closePopover = new EventEmitter();
    this.eventSubject = new Subject();
    this.featureConfigService = inject(FeatureConfigService, {
      optional: true
    });
    this.openTriggerEvents = [PopoverEvent.OPEN, PopoverEvent.OPEN_BY_KEYBOARD];
    this.focusPopoverTriggerEvents = [PopoverEvent.OPEN_BY_KEYBOARD];
    this.closeTriggerEvents = [PopoverEvent.ROUTE_CHANGE, PopoverEvent.ESCAPE_KEYDOWN, PopoverEvent.OUTSIDE_CLICK, PopoverEvent.CLOSE_BUTTON_KEYDOWN, PopoverEvent.CLOSE_BUTTON_CLICK];
    this.focusDirectiveTriggerEvents = [PopoverEvent.ESCAPE_KEYDOWN, PopoverEvent.CLOSE_BUTTON_KEYDOWN];
  }
  static {
    this.ɵfac = function PopoverDirective_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _PopoverDirective)(ɵɵdirectiveInject(ElementRef), ɵɵdirectiveInject(ViewContainerRef), ɵɵdirectiveInject(ComponentFactoryResolver$1), ɵɵdirectiveInject(Renderer2), ɵɵdirectiveInject(ChangeDetectorRef), ɵɵdirectiveInject(PopoverService), ɵɵdirectiveInject(WindowRef));
    };
  }
  static {
    this.ɵdir = ɵɵdefineDirective({
      type: _PopoverDirective,
      selectors: [["", "cxPopover", ""]],
      hostBindings: function PopoverDirective_HostBindings(rf, ctx) {
        if (rf & 1) {
          ɵɵlistener("keydown.enter", function PopoverDirective_keydown_enter_HostBindingHandler($event) {
            return ctx.handlePress($event);
          })("keydown.space", function PopoverDirective_keydown_space_HostBindingHandler($event) {
            return ctx.handlePress($event);
          })("keydown.tab", function PopoverDirective_keydown_tab_HostBindingHandler() {
            return ctx.handleTab();
          })("keydown.shift.tab", function PopoverDirective_keydown_shift_tab_HostBindingHandler() {
            return ctx.handleTab();
          })("keydown.escape", function PopoverDirective_keydown_escape_HostBindingHandler() {
            return ctx.handleEscape();
          })("click", function PopoverDirective_click_HostBindingHandler($event) {
            return ctx.handleClick($event);
          });
        }
      },
      inputs: {
        cxPopover: "cxPopover",
        cxPopoverOptions: "cxPopoverOptions"
      },
      outputs: {
        openPopover: "openPopover",
        closePopover: "closePopover"
      },
      standalone: false
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PopoverDirective, [{
    type: Directive,
    args: [{
      selector: "[cxPopover]",
      standalone: false
    }]
  }], () => [{
    type: ElementRef
  }, {
    type: ViewContainerRef
  }, {
    type: ComponentFactoryResolver$1
  }, {
    type: Renderer2
  }, {
    type: ChangeDetectorRef
  }, {
    type: PopoverService
  }, {
    type: WindowRef
  }], {
    cxPopover: [{
      type: Input
    }],
    cxPopoverOptions: [{
      type: Input
    }],
    openPopover: [{
      type: Output
    }],
    closePopover: [{
      type: Output
    }],
    handlePress: [{
      type: HostListener,
      args: ["keydown.enter", ["$event"]]
    }, {
      type: HostListener,
      args: ["keydown.space", ["$event"]]
    }],
    handleTab: [{
      type: HostListener,
      args: ["keydown.tab"]
    }, {
      type: HostListener,
      args: ["keydown.shift.tab"]
    }],
    handleEscape: [{
      type: HostListener,
      args: ["keydown.escape"]
    }],
    handleClick: [{
      type: HostListener,
      args: ["click", ["$event"]]
    }],
    featureConfigService: [{
      type: Optional
    }]
  });
})();
var defaultLimit = 20;
var TruncatePipe = class _TruncatePipe {
  /**
   * example usage {{ exampleString | cxTruncate: [1, ''] }}
   */
  transform(value, args) {
    if (!args) {
      return value;
    }
    let trail = "...";
    const limit = args.length > 0 && args[0] && Number.isInteger(+args[0]) ? args[0] : defaultLimit;
    if (args.length > 1 && args[1] !== void 0) {
      trail = args[1];
    }
    return value.length > limit ? value.substring(0, limit) + trail : value;
  }
  static {
    this.ɵfac = function TruncatePipe_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _TruncatePipe)();
    };
  }
  static {
    this.ɵpipe = ɵɵdefinePipe({
      name: "cxTruncate",
      type: _TruncatePipe,
      pure: true,
      standalone: false
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(TruncatePipe, [{
    type: Pipe,
    args: [{
      name: "cxTruncate",
      standalone: false
    }]
  }], null, null);
})();
var TruncateTextPopoverComponent = class _TruncateTextPopoverComponent {
  constructor() {
    this.charactersLimit = 100;
  }
  get isTruncated() {
    return this.content.length > +this.charactersLimit;
  }
  static {
    this.ɵfac = function TruncateTextPopoverComponent_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _TruncateTextPopoverComponent)();
    };
  }
  static {
    this.ɵcmp = ɵɵdefineComponent({
      type: _TruncateTextPopoverComponent,
      selectors: [["cx-truncate-text-popover"]],
      inputs: {
        content: "content",
        charactersLimit: "charactersLimit",
        customClass: "customClass"
      },
      standalone: false,
      decls: 8,
      vars: 8,
      consts: [["fullText", ""], [1, "truncated-text", 3, "ngClass"], ["class", "ml-1 link cx-action-link", 3, "cxPopover", "cxPopoverOptions", 4, "ngIf"], [1, "ml-1", "link", "cx-action-link", 3, "cxPopover", "cxPopoverOptions"]],
      template: function TruncateTextPopoverComponent_Template(rf, ctx) {
        if (rf & 1) {
          ɵɵelementContainerStart(0)(1);
          ɵɵelementStart(2, "span", 1);
          ɵɵtext(3);
          ɵɵpipe(4, "cxTruncate");
          ɵɵelementEnd();
          ɵɵelementContainerEnd();
          ɵɵtemplate(5, TruncateTextPopoverComponent_ng_template_5_Template, 1, 1, "ng-template", null, 0, ɵɵtemplateRefExtractor)(7, TruncateTextPopoverComponent_button_7_Template, 3, 6, "button", 2);
          ɵɵelementContainerEnd();
        }
        if (rf & 2) {
          ɵɵadvance(2);
          ɵɵproperty("ngClass", ctx.customClass);
          ɵɵadvance();
          ɵɵtextInterpolate1(" ", ɵɵpipeBind2(4, 3, ctx.content, ɵɵpureFunction1(6, _c13, ctx.charactersLimit)), " ");
          ɵɵadvance(4);
          ɵɵproperty("ngIf", ctx.isTruncated);
        }
      },
      dependencies: [NgClass, NgIf, PopoverDirective, TranslatePipe, TruncatePipe],
      encapsulation: 2,
      changeDetection: 0
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(TruncateTextPopoverComponent, [{
    type: Component,
    args: [{
      selector: "cx-truncate-text-popover",
      changeDetection: ChangeDetectionStrategy.OnPush,
      standalone: false,
      template: `<ng-container>
  <ng-container>
    <span class="truncated-text" [ngClass]="customClass">
      {{ content | cxTruncate: [charactersLimit] }}
    </span>
  </ng-container>

  <ng-template #fullText>
    {{ content }}
  </ng-template>

  <button
    *ngIf="isTruncated"
    [cxPopover]="fullText"
    [cxPopoverOptions]="{
      placement: 'auto',
      appendToBody: true,
      displayCloseButton: true,
    }"
    class="ml-1 link cx-action-link"
  >
    {{ 'common.more' | cxTranslate }}
  </button>
</ng-container>
`
    }]
  }], null, {
    content: [{
      type: Input
    }],
    charactersLimit: [{
      type: Input
    }],
    customClass: [{
      type: Input
    }]
  });
})();
var GenericLinkComponentService = class _GenericLinkComponentService {
  constructor() {
    this.HTTP_PROTOCOL_REGEX = /^https?:\/\//i;
    this.MAILTO_PROTOCOL_REGEX = /^mailto:/i;
    this.TEL_PROTOCOL_REGEX = /^tel:/i;
  }
  /**
   * Returns true when the @Input `url` is a string starting with `http://`, `https://`, `mailto:`, `tel:`.
   */
  isExternalUrl(url) {
    return typeof url === "string" && (this.HTTP_PROTOCOL_REGEX.test(url) || this.MAILTO_PROTOCOL_REGEX.test(url) || this.TEL_PROTOCOL_REGEX.test(url));
  }
  static {
    this.ɵfac = function GenericLinkComponentService_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _GenericLinkComponentService)();
    };
  }
  static {
    this.ɵprov = ɵɵdefineInjectable({
      token: _GenericLinkComponentService,
      factory: _GenericLinkComponentService.ɵfac,
      providedIn: "root"
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(GenericLinkComponentService, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], null, null);
})();
var GenericLinkComponent = class _GenericLinkComponent {
  constructor(router, service) {
    this.router = router;
    this.service = service;
    this.URL_SPLIT = /(^[^#?]*)(.*)/;
    this.routeParts = {};
    this.tabindex = 0;
  }
  isExternalUrl() {
    return this.service.isExternalUrl(this.url);
  }
  get rel() {
    return this.target === "_blank" ? "noopener" : null;
  }
  ngOnChanges(changes) {
    if (changes["url"]) {
      this.setUrlParts(changes["url"].currentValue);
    }
  }
  /**
   * The part with the path of the local url.
   */
  get routerUrl() {
    return this.routeParts.path;
  }
  /**
   * The part with the query params of the local url.
   */
  get queryParams() {
    return this.routeParts.queryParams;
  }
  /**
   * The part with the hash fragment of the local url.
   */
  get fragment() {
    return this.routeParts.fragment ?? void 0;
  }
  /**
   * Parses the given url and sets the property `urlParts` accordingly.
   */
  setUrlParts(url) {
    if (typeof url === "string") {
      url = this.getAbsoluteUrl(url);
      this.routeParts = this.splitUrl(url);
    } else {
      this.routeParts = {
        path: url
      };
    }
  }
  /**
   * Parses the given string into 3 parts:
   * - string path (wrapped in an array to be compatible with Angular syntax for the `routerLink`)
   * - query params (as an object)
   * - hash fragment (string)
   */
  splitUrl(url = "") {
    const {
      queryParams,
      fragment
    } = this.router.parseUrl(url);
    const [, path] = url.match(this.URL_SPLIT) ?? [, ""];
    return {
      path: [path ?? ""],
      queryParams,
      fragment
    };
  }
  /**
   * Prepends a leading slash to the given URL string, in case it doesn't have it.
   */
  getAbsoluteUrl(url) {
    return url.startsWith("/") ? url : "/" + url;
  }
  static {
    this.ɵfac = function GenericLinkComponent_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _GenericLinkComponent)(ɵɵdirectiveInject(Router), ɵɵdirectiveInject(GenericLinkComponentService));
    };
  }
  static {
    this.ɵcmp = ɵɵdefineComponent({
      type: _GenericLinkComponent,
      selectors: [["cx-generic-link"]],
      inputs: {
        url: "url",
        target: "target",
        id: "id",
        class: "class",
        style: "style",
        title: "title",
        ariaLabel: "ariaLabel",
        tabindex: "tabindex"
      },
      standalone: false,
      features: [ɵɵNgOnChangesFeature],
      ngContentSelectors: _c12,
      decls: 5,
      vars: 2,
      consts: [["isLocalUrl", ""], ["content", ""], [4, "ngIf", "ngIfElse"], [3, "href", "tabindex"], [4, "ngTemplateOutlet"], [3, "routerLink", "queryParams", "fragment", "target", "tabindex"]],
      template: function GenericLinkComponent_Template(rf, ctx) {
        if (rf & 1) {
          ɵɵprojectionDef();
          ɵɵtemplate(0, GenericLinkComponent_ng_container_0_Template, 3, 10, "ng-container", 2)(1, GenericLinkComponent_ng_template_1_Template, 2, 11, "ng-template", null, 0, ɵɵtemplateRefExtractor)(3, GenericLinkComponent_ng_template_3_Template, 1, 0, "ng-template", null, 1, ɵɵtemplateRefExtractor);
        }
        if (rf & 2) {
          const isLocalUrl_r3 = ɵɵreference(2);
          ɵɵproperty("ngIf", ctx.isExternalUrl())("ngIfElse", isLocalUrl_r3);
        }
      },
      dependencies: [NgIf, NgTemplateOutlet, RouterLink],
      encapsulation: 2
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(GenericLinkComponent, [{
    type: Component,
    args: [{
      selector: "cx-generic-link",
      standalone: false,
      template: '<!-- https://github.com/angular/angular/issues/24567 -->\n\n<ng-container *ngIf="isExternalUrl(); else isLocalUrl">\n  <a\n    [href]="url"\n    [attr.target]="target"\n    [attr.rel]="rel"\n    [attr.id]="id"\n    [attr.class]="class"\n    [attr.style]="style"\n    [attr.title]="title"\n    [attr.aria-label]="ariaLabel ? ariaLabel : null"\n    [tabindex]="tabindex"\n  >\n    <ng-container *ngTemplateOutlet="content"></ng-container>\n  </a>\n</ng-container>\n\n<ng-template #isLocalUrl>\n  <a\n    [routerLink]="routerUrl"\n    [queryParams]="queryParams"\n    [fragment]="fragment"\n    [target]="target || undefined"\n    [attr.id]="id"\n    [attr.class]="class"\n    [attr.style]="style"\n    [attr.title]="title"\n    [attr.aria-label]="ariaLabel ? ariaLabel : null"\n    [tabindex]="tabindex"\n  >\n    <ng-container *ngTemplateOutlet="content"></ng-container>\n  </a>\n</ng-template>\n\n<ng-template #content>\n  <ng-content></ng-content>\n</ng-template>\n'
    }]
  }], () => [{
    type: Router
  }, {
    type: GenericLinkComponentService
  }], {
    url: [{
      type: Input
    }],
    target: [{
      type: Input
    }],
    id: [{
      type: Input
    }],
    class: [{
      type: Input
    }],
    style: [{
      type: Input
    }],
    title: [{
      type: Input
    }],
    ariaLabel: [{
      type: Input
    }],
    tabindex: [{
      type: Input
    }]
  });
})();
var CardComponent = class _CardComponent {
  // ACTIONS
  setEditMode() {
    this.editMode = true;
  }
  cancelEdit() {
    this.editMode = false;
    this.cancelCard.emit(5);
  }
  delete() {
    this.deleteCard.emit(1);
  }
  setDefault() {
    this.isDefault = true;
    this.setDefaultCard.emit(2);
  }
  send() {
    this.sendCard.emit(3);
  }
  edit() {
    this.editCard.emit(4);
  }
  isCardAction(action) {
    return action.event !== void 0;
  }
  isCardLinkAction(action) {
    return action.link !== void 0;
  }
  trackByIndex(index) {
    return index;
  }
  constructor() {
    this.iconTypes = ICON_TYPE;
    this.deleteCard = new EventEmitter();
    this.setDefaultCard = new EventEmitter();
    this.sendCard = new EventEmitter();
    this.editCard = new EventEmitter();
    this.cancelCard = new EventEmitter();
    this.border = false;
    this.editMode = false;
    this.isDefault = false;
    this.fitToContainer = false;
    this.truncateText = false;
    this.truncateParagraphText = false;
    this.charactersLimit = 100;
  }
  /* eslint @angular-eslint/no-empty-lifecycle-method: 1 */
  ngOnInit() {
  }
  get isHeaderVisible() {
    return !!this.content?.header && !this.editMode;
  }
  /**
   * ariaDescribedBy: Computes the value for the 'aria-describedby' attribute.
   * If `content` has a `title`, it returns a string including the title and container IDs, with index if available.
   * If no title, it returns only the container ID with the index if available.
   *
   * @returns {string} The 'aria-describedby' value.
   */
  get ariaDescribedBy() {
    if (this.content && this.content.title) {
      return `${this.getLabel("content-title")} ${this.getLabel("cx-card-container")}`;
    }
    return `${this.getLabel("cx-card-container")}`;
  }
  /**
   * getLabel: Computes a label string by concatenating the provided prefix with the index.
   * If the index is greater than or equal to 0, it appends the index to the prefix; otherwise, it just returns the prefix.
   *
   * @param {string} prefix - The string to prefix to the label.
   * @returns {string} The computed label, including the index if it's available.
   */
  getLabel(prefix) {
    return prefix + (this.index >= 0 ? "-" + this.index : "");
  }
  static {
    this.ɵfac = function CardComponent_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _CardComponent)();
    };
  }
  static {
    this.ɵcmp = ɵɵdefineComponent({
      type: _CardComponent,
      selectors: [["cx-card"]],
      inputs: {
        border: "border",
        editMode: "editMode",
        isDefault: "isDefault",
        content: "content",
        fitToContainer: "fitToContainer",
        truncateText: "truncateText",
        truncateParagraphText: "truncateParagraphText",
        charactersLimit: "charactersLimit",
        index: "index",
        role: "role"
      },
      outputs: {
        deleteCard: "deleteCard",
        setDefaultCard: "setDefaultCard",
        sendCard: "sendCard",
        editCard: "editCard",
        cancelCard: "cancelCard"
      },
      standalone: false,
      ngContentSelectors: _c16,
      decls: 3,
      vars: 1,
      consts: [["showText", ""], ["class", "cx-card", 3, "tabindex", "cx-card-border", "cx-card-fit-to-container", 4, "ngIf"], [1, "cx-card", 3, "tabindex"], ["class", "card-header", 3, "id", 4, "ngIf"], [1, "card-body", "cx-card-body"], ["role", "alert", "class", "cx-card-delete-msg", 4, "ngIf"], ["class", "cx-card-title", 3, "id", 4, "ngIf"], [1, "cx-card-container", 3, "id"], [1, "cx-card-label-container"], ["class", "cx-card-label-bold", 4, "ngIf"], [4, "ngFor", "ngForOf"], ["class", "cx-card-paragraph", 4, "ngFor", "ngForOf", "ngForTrackBy"], ["class", "cx-card-img-container", "role", "img", 4, "ngIf"], ["class", "row cx-card-body-delete", 4, "ngIf"], ["class", "cx-card-actions", 4, "ngIf"], [1, "card-header", 3, "id"], ["role", "alert", 1, "cx-card-delete-msg"], [1, "cx-card-title", 3, "id"], [1, "cx-card-label-bold"], [4, "ngTemplateOutlet", "ngTemplateOutletContext"], [1, "cx-card-paragraph"], [1, "cx-card-paragraph-title"], ["role", "img", 1, "cx-card-img-container"], [3, "type"], [1, "row", "cx-card-body-delete"], ["class", "row col-md-12", 4, "cxFeature"], [1, "row", "col-md-12"], [1, "col-md-6", 3, "cxFocus"], [1, "btn", "btn-block", "btn-primary", 3, "click"], [1, "col-md-6"], [1, "btn", "btn-block", "btn-secondary", 3, "click", "cxAtMessage"], [1, "cx-card-actions"], [4, "cxFeature"], [3, "ngSwitch", 4, "ngIf"], ["class", "link cx-action-link", 3, "href", 4, "ngIf"], [3, "ngSwitch"], ["type", "button", "class", "link cx-action-link", 3, "click", 4, "ngSwitchCase"], ["type", "button", 1, "link", "cx-action-link", 3, "click"], [1, "link", "cx-action-link", 3, "href"], ["class", "cx-card-action-item", 4, "ngFor", "ngForOf"], [1, "cx-card-action-item"], [3, "url", "ariaLabel", "target", 4, "ngIf"], ["type", "button", "class", "btn btn-tertiary", 3, "click", 4, "ngSwitchCase"], ["type", "button", 1, "btn", "btn-tertiary", 3, "click"], [3, "url", "ariaLabel", "target"], [3, "class", "ngClass", 4, "ngIf"], [3, "charactersLimit", "content", "customClass", 4, "ngIf"], [3, "ngClass"], [3, "charactersLimit", "content", "customClass"]],
      template: function CardComponent_Template(rf, ctx) {
        if (rf & 1) {
          ɵɵprojectionDef(_c15);
          ɵɵtemplate(0, CardComponent_div_0_Template, 14, 21, "div", 1)(1, CardComponent_ng_template_1_Template, 2, 2, "ng-template", null, 0, ɵɵtemplateRefExtractor);
        }
        if (rf & 2) {
          ɵɵproperty("ngIf", ctx.content);
        }
      },
      dependencies: [NgClass, NgForOf, NgIf, NgTemplateOutlet, NgSwitch, NgSwitchCase, AtMessageDirective, IconComponent, TruncateTextPopoverComponent, FocusDirective, FeatureDirective, GenericLinkComponent, TranslatePipe],
      encapsulation: 2
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(CardComponent, [{
    type: Component,
    args: [{
      selector: "cx-card",
      standalone: false,
      template: `<div
  *ngIf="content"
  class="cx-card"
  [tabindex]="border ? 0 : -1"
  [attr.role]="role"
  [attr.aria-labelledby]="isHeaderVisible ? getLabel('content-header') : null"
  [attr.aria-describedby]="ariaDescribedBy"
  [class.cx-card-border]="border"
  [class.cx-card-fit-to-container]="fitToContainer"
>
  <!-- Card Header -->
  <div
    *ngIf="isHeaderVisible"
    class="card-header"
    [id]="getLabel('content-header')"
  >
    {{ content.header }}
  </div>
  <!-- Card Body -->
  <div class="card-body cx-card-body" [class.cx-card-delete]="editMode">
    <!-- Edit message -->
    <div *ngIf="editMode" role="alert" class="cx-card-delete-msg">
      {{ content.deleteMsg }}
    </div>
    <!-- Card title -->
    <span
      *ngIf="content.title"
      class="cx-card-title"
      [id]="getLabel('content-title')"
    >
      {{ content.title }}
    </span>
    <!-- Card Content -->
    <div class="cx-card-container" [id]="getLabel('cx-card-container')">
      <!-- Card Label -->
      <div class="cx-card-label-container">
        <div *ngIf="content.textBold" class="cx-card-label-bold">
          {{ content.textBold }}
        </div>
        <div *ngFor="let line of content.text">
          <ng-container
            *ngTemplateOutlet="
              showText;
              context: {
                content: content,
                truncateText: truncateText,
                charactersLimit: charactersLimit,
                class: 'cx-card-label',
                text: line,
              }
            "
          >
          </ng-container>
        </div>
        <div
          class="cx-card-paragraph"
          *ngFor="let item of content.paragraphs; trackBy: trackByIndex"
        >
          <div class="cx-card-paragraph-title">{{ item.title }}</div>
          <div *ngFor="let text of item.text">
            <ng-container
              *ngTemplateOutlet="
                showText;
                context: {
                  content: content,
                  truncateText: truncateParagraphText,
                  charactersLimit: charactersLimit,
                  class: 'cx-card-paragraph-text',
                  text: text,
                }
              "
            >
            </ng-container>
          </div>
        </div>
        <ng-content select="[label_container_bottom]"></ng-content>
      </div>
      <!-- Image -->
      <div
        *ngIf="content.img"
        class="cx-card-img-container"
        [attr.aria-label]="
          content.imgLabel ?? 'paymentCard.credit' | cxTranslate
        "
        role="img"
        [attr.title]="content.imgLabel ?? 'paymentCard.credit' | cxTranslate"
      >
        <cx-icon [type]="content.img"></cx-icon>
      </div>
    </div>
    <!-- Edit Mode Actions -->
    <div *ngIf="editMode" class="row cx-card-body-delete">
      <div class="row col-md-12" *cxFeature="'a11yDeleteButton2First'">
        <div class="col-md-6" [cxFocus]="{ autofocus: true }">
          <button class="btn btn-block btn-primary" (click)="delete()">
            {{ 'common.delete' | cxTranslate }}
          </button>
        </div>
        <div class="col-md-6">
          <button
            [cxAtMessage]="'assistiveMessage.actionCancelled' | cxTranslate"
            class="btn btn-block btn-secondary"
            (click)="cancelEdit()"
          >
            {{ 'common.cancel' | cxTranslate }}
          </button>
        </div>
      </div>
      <div class="row col-md-12" *cxFeature="'!a11yDeleteButton2First'">
        <div class="col-md-6">
          <button
            [cxAtMessage]="'assistiveMessage.actionCancelled' | cxTranslate"
            class="btn btn-block btn-secondary"
            (click)="cancelEdit()"
          >
            {{ 'common.cancel' | cxTranslate }}
          </button>
        </div>
        <div class="col-md-6" [cxFocus]="{ autofocus: true }">
          <button class="btn btn-block btn-primary" (click)="delete()">
            {{ 'common.delete' | cxTranslate }}
          </button>
        </div>
      </div>
    </div>
    <!-- Actions -->
    <div *ngIf="content.actions && !editMode" class="cx-card-actions">
      <ng-container *cxFeature="'!a11yImproveButtonsInCardComponent'">
        <div *ngFor="let action of content.actions">
          <div>
            <ng-container
              *ngIf="isCardAction(action)"
              [ngSwitch]="action.event"
            >
              <button
                type="button"
                *ngSwitchCase="'delete'"
                class="link cx-action-link"
                (click)="delete()"
              >
                {{ action.name }}
              </button>
              <button
                type="button"
                *ngSwitchCase="'default'"
                class="link cx-action-link"
                (click)="setDefault()"
              >
                {{ action.name }}
              </button>
              <button
                type="button"
                *ngSwitchCase="'send'"
                class="link cx-action-link"
                (click)="send()"
                [attr.aria-describedby]="
                  index >= 0 && content.header && !editMode
                    ? 'content-header-' + index
                    : null
                "
              >
                {{ action.name }}
              </button>
              <button
                type="button"
                *ngSwitchCase="'edit'"
                class="link cx-action-link"
                (click)="edit()"
              >
                {{ action.name }}
              </button>
            </ng-container>
            <a
              *ngIf="isCardLinkAction(action)"
              href="{{ action.link }}"
              class="link cx-action-link"
            >
              {{ action.name }}
            </a>
          </div>
        </div>
      </ng-container>
      <ng-container *cxFeature="'a11yImproveButtonsInCardComponent'">
        <div *ngFor="let action of content.actions" class="cx-card-action-item">
          <div>
            <ng-container
              *ngIf="isCardAction(action)"
              [ngSwitch]="action.event"
            >
              <button
                type="button"
                *ngSwitchCase="'delete'"
                class="btn btn-tertiary"
                (click)="delete()"
              >
                {{ action.name }}
              </button>
              <button
                type="button"
                *ngSwitchCase="'default'"
                class="btn btn-tertiary"
                (click)="setDefault()"
              >
                {{ action.name }}
              </button>
              <button
                type="button"
                *ngSwitchCase="'send'"
                class="btn btn-tertiary"
                (click)="send()"
                [attr.aria-describedby]="
                  index >= 0 && content.header && !editMode
                    ? 'content-header-' + index
                    : null
                "
              >
                {{ action.name }}
              </button>
              <button
                type="button"
                *ngSwitchCase="'edit'"
                class="btn btn-tertiary"
                (click)="edit()"
              >
                {{ action.name }}
              </button>
            </ng-container>
            <cx-generic-link
              *ngIf="isCardLinkAction(action)"
              [url]="action.link"
              [ariaLabel]="
                action.ariaLabel ? (action.ariaLabel | cxTranslate) : undefined
              "
              [target]="action.target ?? null"
              >{{ action.name }}</cx-generic-link
            >
          </div>
        </div>
      </ng-container>
    </div>
  </div>
</div>

<ng-template
  #showText
  let-content="content"
  let-truncateText="truncateText"
  let-charactersLimit="charactersLimit"
  let-class="class"
  let-text="text"
>
  <div *ngIf="!truncateText" [class]="class" [ngClass]="content.customClass">
    {{ text }}
  </div>

  <cx-truncate-text-popover
    *ngIf="truncateText"
    [charactersLimit]="charactersLimit"
    [content]="text"
    [customClass]="content.customClass || class"
  ></cx-truncate-text-popover>
</ng-template>
`
    }]
  }], () => [], {
    deleteCard: [{
      type: Output
    }],
    setDefaultCard: [{
      type: Output
    }],
    sendCard: [{
      type: Output
    }],
    editCard: [{
      type: Output
    }],
    cancelCard: [{
      type: Output
    }],
    border: [{
      type: Input
    }],
    editMode: [{
      type: Input
    }],
    isDefault: [{
      type: Input
    }],
    content: [{
      type: Input
    }],
    fitToContainer: [{
      type: Input
    }],
    truncateText: [{
      type: Input
    }],
    truncateParagraphText: [{
      type: Input
    }],
    charactersLimit: [{
      type: Input
    }],
    index: [{
      type: Input
    }],
    role: [{
      type: Input
    }]
  });
})();
var PopoverModule = class _PopoverModule {
  static {
    this.ɵfac = function PopoverModule_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _PopoverModule)();
    };
  }
  static {
    this.ɵmod = ɵɵdefineNgModule({
      type: _PopoverModule,
      declarations: [PopoverDirective, PopoverComponent],
      imports: [CommonModule, RouterModule, KeyboardFocusModule, IconModule, I18nModule],
      exports: [PopoverDirective, PopoverComponent]
    });
  }
  static {
    this.ɵinj = ɵɵdefineInjector({
      imports: [CommonModule, RouterModule, KeyboardFocusModule, IconModule, I18nModule]
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PopoverModule, [{
    type: NgModule,
    args: [{
      imports: [CommonModule, RouterModule, KeyboardFocusModule, IconModule, I18nModule],
      declarations: [PopoverDirective, PopoverComponent],
      exports: [PopoverDirective, PopoverComponent]
    }]
  }], null, null);
})();
var TruncateTextPopoverModule = class _TruncateTextPopoverModule {
  static {
    this.ɵfac = function TruncateTextPopoverModule_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _TruncateTextPopoverModule)();
    };
  }
  static {
    this.ɵmod = ɵɵdefineNgModule({
      type: _TruncateTextPopoverModule,
      declarations: [TruncateTextPopoverComponent, TruncatePipe],
      imports: [CommonModule, I18nModule, PopoverModule],
      exports: [TruncateTextPopoverComponent, TruncatePipe]
    });
  }
  static {
    this.ɵinj = ɵɵdefineInjector({
      imports: [CommonModule, I18nModule, PopoverModule]
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(TruncateTextPopoverModule, [{
    type: NgModule,
    args: [{
      imports: [CommonModule, I18nModule, PopoverModule],
      declarations: [TruncateTextPopoverComponent, TruncatePipe],
      exports: [TruncateTextPopoverComponent, TruncatePipe]
    }]
  }], null, null);
})();
var GenericLinkModule = class _GenericLinkModule {
  static {
    this.ɵfac = function GenericLinkModule_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _GenericLinkModule)();
    };
  }
  static {
    this.ɵmod = ɵɵdefineNgModule({
      type: _GenericLinkModule,
      declarations: [GenericLinkComponent],
      imports: [CommonModule, RouterModule],
      exports: [GenericLinkComponent]
    });
  }
  static {
    this.ɵinj = ɵɵdefineInjector({
      imports: [CommonModule, RouterModule]
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(GenericLinkModule, [{
    type: NgModule,
    args: [{
      imports: [CommonModule, RouterModule],
      declarations: [GenericLinkComponent],
      exports: [GenericLinkComponent]
    }]
  }], null, null);
})();
var CardModule = class _CardModule {
  static {
    this.ɵfac = function CardModule_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _CardModule)();
    };
  }
  static {
    this.ɵmod = ɵɵdefineNgModule({
      type: _CardModule,
      declarations: [CardComponent],
      imports: [CommonModule, AtMessageModule, I18nModule, IconModule, TruncateTextPopoverModule, KeyboardFocusModule, FeaturesConfigModule, GenericLinkModule],
      exports: [CardComponent]
    });
  }
  static {
    this.ɵinj = ɵɵdefineInjector({
      imports: [CommonModule, AtMessageModule, I18nModule, IconModule, TruncateTextPopoverModule, KeyboardFocusModule, FeaturesConfigModule, GenericLinkModule]
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(CardModule, [{
    type: NgModule,
    args: [{
      imports: [CommonModule, AtMessageModule, I18nModule, IconModule, TruncateTextPopoverModule, KeyboardFocusModule, FeaturesConfigModule, GenericLinkModule],
      declarations: [CardComponent],
      exports: [CardComponent]
    }]
  }], null, null);
})();
var MediaSourcesPipe = class _MediaSourcesPipe {
  transform(sizes) {
    const sources = [];
    return sizes.split(/,\s*/).reduceRight((acc, set) => {
      let [srcset, media] = set.split(" ");
      if (!srcset || !media) {
        return acc;
      }
      media = `(min-width: ${media.replace("w", "px")})`;
      acc.push({
        srcset,
        media
      });
      return acc;
    }, sources);
  }
  static {
    this.ɵfac = function MediaSourcesPipe_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _MediaSourcesPipe)();
    };
  }
  static {
    this.ɵpipe = ɵɵdefinePipe({
      name: "cxMediaSources",
      type: _MediaSourcesPipe,
      pure: true,
      standalone: false
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(MediaSourcesPipe, [{
    type: Pipe,
    args: [{
      name: "cxMediaSources",
      standalone: false
    }]
  }], null, null);
})();
var USE_LEGACY_MEDIA_COMPONENT = new InjectionToken("USE_LEGACY_MEDIA_COMPONENT");
var MediaComponent = class _MediaComponent {
  constructor(mediaService) {
    this.mediaService = mediaService;
    this.loading = this.loadingStrategy;
    this.elementType = "img";
    this.loaded = new EventEmitter();
    this.isInitialized = false;
    this.isLoading = true;
    this.isMissing = false;
    this.trackByMedia = (_, item) => item.media;
    this.isLegacy = inject(USE_LEGACY_MEDIA_COMPONENT, {
      optional: true
    }) || inject(Config)["useLegacyMediaComponent"] || false;
  }
  ngOnChanges() {
    this.create();
  }
  /**
   * Creates the `Media` object
   */
  create() {
    this.media = this.mediaService.getMediaBasedOnHTMLElementType(this.elementType, this.container instanceof Array ? this.container[0] : this.container, this.format, this.alt, this.role);
    if (!this.media?.src) {
      this.handleMissing();
    }
  }
  /**
   * This handler is called from the UI when the image is loaded.
   */
  loadHandler() {
    this.isLoading = false;
    this.isInitialized = true;
    this.isMissing = false;
    this.loaded.emit(true);
  }
  /**
   * Indicates whether the browser should lazy load the image.
   */
  get loadingStrategy() {
    return this.mediaService.loadingStrategy === ImageLoadingStrategy.LAZY ? ImageLoadingStrategy.LAZY : null;
  }
  /**
   * Whenever an error happens during load, we mark the component
   * with css classes to have a missing media.
   */
  errorHandler() {
    this.handleMissing();
  }
  handleMissing() {
    this.isLoading = false;
    this.isInitialized = true;
    this.isMissing = true;
    this.loaded.emit(false);
  }
  /**
   * If the media has a high fetch priority, we load the image eagerly,
   * no matter what the explicit loading strategy is.
   *
   * It's for convenience to not set explicitly `loading="eager"`,
   * when we already know that the image has a high fetch priority.
   */
  get effectiveLoadingStrategy() {
    if (this.fetchPriority === ImageFetchPriority.HIGH) {
      return ImageLoadingStrategy.EAGER;
    }
    return this.loading ?? this.loadingStrategy;
  }
  static {
    this.ɵfac = function MediaComponent_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _MediaComponent)(ɵɵdirectiveInject(MediaService));
    };
  }
  static {
    this.ɵcmp = ɵɵdefineComponent({
      type: _MediaComponent,
      selectors: [["cx-media"]],
      hostVars: 6,
      hostBindings: function MediaComponent_HostBindings(rf, ctx) {
        if (rf & 2) {
          ɵɵclassProp("is-initialized", ctx.isInitialized)("is-loading", ctx.isLoading)("is-missing", ctx.isMissing);
        }
      },
      inputs: {
        container: "container",
        format: "format",
        alt: "alt",
        role: "role",
        loading: "loading",
        fetchPriority: "fetchPriority",
        elementType: "elementType",
        sizesForImgElement: "sizesForImgElement"
      },
      outputs: {
        loaded: "loaded"
      },
      standalone: false,
      features: [ɵɵNgOnChangesFeature],
      decls: 1,
      vars: 1,
      consts: [["imageElementTmp", ""], ["legacyImgTmp", ""], [4, "ngIf"], [4, "cxFeature"], [4, "ngIf", "ngIfElse"], [3, "media", "srcset", 4, "ngFor", "ngForOf", "ngForTrackBy"], [3, "load", "error", "loading", "alt", "title", "src"], [3, "media", "srcset"], [3, "loading", "alt", "title", "src", "srcset", "load", "error", 4, "ngIf"], [3, "load", "error", "loading", "alt", "title", "src", "srcset"], [3, "srcset", "media", 4, "ngFor", "ngForOf", "ngForTrackBy"], [3, "srcset", "media"]],
      template: function MediaComponent_Template(rf, ctx) {
        if (rf & 1) {
          ɵɵtemplate(0, MediaComponent_ng_container_0_Template, 3, 2, "ng-container", 2);
        }
        if (rf & 2) {
          ɵɵproperty("ngIf", !!ctx.media);
        }
      },
      dependencies: [NgForOf, NgIf, FeatureDirective, MediaSourcesPipe],
      encapsulation: 2,
      changeDetection: 0
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(MediaComponent, [{
    type: Component,
    args: [{
      selector: "cx-media",
      changeDetection: ChangeDetectionStrategy.OnPush,
      standalone: false,
      template: `<ng-container *ngIf="!!media">
  <ng-container *cxFeature="'useExtendedMediaComponentConfiguration'">
    <picture
      *ngIf="
        !!media?.sources?.length && elementType !== 'img';
        else imageElementTmp
      "
    >
      <source
        *ngFor="let source of media.sources; trackBy: trackByMedia"
        [media]="source.media"
        [srcset]="source.srcset"
        [attr.width]="source.width"
        [attr.height]="source.height"
      />

      <img
        [loading]="effectiveLoadingStrategy"
        [attr.fetchpriority]="fetchPriority"
        [alt]="media.alt"
        [title]="media.alt"
        [src]="media.src"
        [attr.role]="media.role"
        [attr.width]="media.width"
        [attr.height]="media.height"
        (load)="loadHandler()"
        (error)="errorHandler()"
      />
    </picture>

    <ng-template #imageElementTmp>
      <img
        *ngIf="media.src"
        [loading]="effectiveLoadingStrategy"
        [attr.fetchpriority]="fetchPriority"
        [alt]="media.alt"
        [title]="media.alt"
        [src]="media.src"
        [srcset]="media.srcset || media.src"
        [attr.role]="media.role"
        [attr.width]="media.width"
        [attr.height]="media.height"
        [attr.sizes]="sizesForImgElement"
        (load)="loadHandler()"
        (error)="errorHandler()"
      />
    </ng-template>
  </ng-container>

  <ng-container *cxFeature="'!useExtendedMediaComponentConfiguration'">
    <picture *ngIf="media.srcset && !isLegacy; else legacyImgTmp">
      <source
        *ngFor="
          let source of media!.srcset! | cxMediaSources;
          trackBy: trackByMedia
        "
        [srcset]="source.srcset"
        [media]="source.media"
      />
      <img
        [loading]="effectiveLoadingStrategy"
        [attr.fetchpriority]="fetchPriority"
        [alt]="media.alt"
        [title]="media.alt"
        [src]="media.src"
        [attr.role]="media.role"
        (load)="loadHandler()"
        (error)="errorHandler()"
      />
    </picture>

    <ng-template #legacyImgTmp>
      <img
        *ngIf="media.src"
        [loading]="effectiveLoadingStrategy"
        [attr.fetchpriority]="fetchPriority"
        [alt]="media.alt"
        [title]="media.alt"
        [src]="media.src"
        [srcset]="media?.srcset || media.src"
        [attr.role]="media.role"
        (load)="loadHandler()"
        (error)="errorHandler()"
      />
    </ng-template>
  </ng-container>
</ng-container>
`
    }]
  }], () => [{
    type: MediaService
  }], {
    container: [{
      type: Input
    }],
    format: [{
      type: Input
    }],
    alt: [{
      type: Input
    }],
    role: [{
      type: Input
    }],
    loading: [{
      type: Input
    }],
    fetchPriority: [{
      type: Input
    }],
    elementType: [{
      type: Input
    }],
    sizesForImgElement: [{
      type: Input
    }],
    loaded: [{
      type: Output
    }],
    isInitialized: [{
      type: HostBinding,
      args: ["class.is-initialized"]
    }],
    isLoading: [{
      type: HostBinding,
      args: ["class.is-loading"]
    }],
    isMissing: [{
      type: HostBinding,
      args: ["class.is-missing"]
    }]
  });
})();
function mediaPreconnectInitializer(mediaPreconnectService) {
  return () => mediaPreconnectService.addPreconnectLink();
}
var MediaModule = class _MediaModule {
  static forRoot() {
    return {
      ngModule: _MediaModule,
      providers: [{
        provide: APP_INITIALIZER,
        useFactory: mediaPreconnectInitializer,
        deps: [MediaPreconnectService],
        multi: true
      }]
    };
  }
  static {
    this.ɵfac = function MediaModule_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _MediaModule)();
    };
  }
  static {
    this.ɵmod = ɵɵdefineNgModule({
      type: _MediaModule,
      declarations: [MediaComponent, MediaSourcesPipe],
      imports: [CommonModule, FeaturesConfigModule],
      exports: [MediaComponent]
    });
  }
  static {
    this.ɵinj = ɵɵdefineInjector({
      imports: [CommonModule, FeaturesConfigModule]
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(MediaModule, [{
    type: NgModule,
    args: [{
      imports: [CommonModule, FeaturesConfigModule],
      declarations: [MediaComponent, MediaSourcesPipe],
      exports: [MediaComponent]
    }]
  }], null, null);
})();
var FocusableCarouselItemDirective = class _FocusableCarouselItemDirective {
  constructor(logger, el) {
    this.logger = logger;
    this.el = el;
    if (isDevMode() && !this.canElementReceiveFocus()) {
      this.logger.warn(`cxFocusableCarouselItem: Element cannot receive focus: ${this.el.nativeElement.tagName}. Either use one of the focusable elements, or add a non-negative tabindex.`);
    }
  }
  canElementReceiveFocus() {
    const focusableTags = ["INPUT", "SELECT", "TEXTAREA", "BUTTON", "A"];
    const element = this.el.nativeElement;
    const hasTabIndex = element.hasAttribute("tabindex") && element.getAttribute("tabindex") !== "-1";
    const isFocusableTag = focusableTags.includes(element.tagName);
    const isDisabled = element.hasAttribute("disabled");
    return (hasTabIndex || isFocusableTag) && !isDisabled;
  }
  static {
    this.ɵfac = function FocusableCarouselItemDirective_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _FocusableCarouselItemDirective)(ɵɵdirectiveInject(LoggerService), ɵɵdirectiveInject(ElementRef));
    };
  }
  static {
    this.ɵdir = ɵɵdefineDirective({
      type: _FocusableCarouselItemDirective,
      selectors: [["", "cxFocusableCarouselItem", ""]],
      standalone: false
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(FocusableCarouselItemDirective, [{
    type: Directive,
    args: [{
      selector: "[cxFocusableCarouselItem]",
      standalone: false
    }]
  }], () => [{
    type: LoggerService
  }, {
    type: ElementRef
  }], null);
})();
var CarouselModule = class _CarouselModule {
  static {
    this.ɵfac = function CarouselModule_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _CarouselModule)();
    };
  }
  static {
    this.ɵmod = ɵɵdefineNgModule({
      type: _CarouselModule,
      declarations: [CarouselComponent, FocusableCarouselItemDirective],
      imports: [CommonModule, RouterModule, IconModule, MediaModule, UrlModule, I18nModule],
      exports: [CarouselComponent, FocusableCarouselItemDirective]
    });
  }
  static {
    this.ɵinj = ɵɵdefineInjector({
      imports: [CommonModule, RouterModule, IconModule, MediaModule, UrlModule, I18nModule]
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(CarouselModule, [{
    type: NgModule,
    args: [{
      imports: [CommonModule, RouterModule, IconModule, MediaModule, UrlModule, I18nModule],
      declarations: [CarouselComponent, FocusableCarouselItemDirective],
      exports: [CarouselComponent, FocusableCarouselItemDirective]
    }]
  }], null, null);
})();
var HorizontalScrollingPositionDirective = class _HorizontalScrollingPositionDirective {
  constructor() {
    this.logger = inject(LoggerService);
    this.windowRef = inject(WindowRef);
    this._scrollingAreaWidth$ = new BehaviorSubject(0);
    this.scrollingAreaWidth$ = this._scrollingAreaWidth$.pipe(debounceTime(300), distinctUntilChanged());
    this._isScrollStart$ = new BehaviorSubject(true);
    this.isScrollStart$ = this._isScrollStart$.asObservable();
    this._isScrollEnd$ = new BehaviorSubject(false);
    this.isScrollEnd$ = this._isScrollEnd$.asObservable();
    this.isScrollNeeded$ = combineLatest([this._isScrollStart$, this._isScrollEnd$]).pipe(map(([isScrollStart, isScrollEnd]) => !isScrollStart || !isScrollEnd), distinctUntilChanged());
  }
  /**
   * When the scrolling area is appearing (or disappearing),
   * it subscribes (or unsubscribes) to the scrolling position.
   */
  ngOnChanges() {
    if (!this.windowRef.isBrowser()) {
      return;
    }
    this.unsubscribeScrollingArea();
    if (this.scrollingArea && this.scrollingAreaStart && this.scrollingAreaEnd) {
      this.subscribeScrollingArea();
    }
  }
  ngOnDestroy() {
    this.unsubscribeScrollingArea();
    this._isScrollStart$.complete();
    this._isScrollEnd$.complete();
    this._scrollingAreaWidth$.complete();
  }
  /**
   * Scrolls the carousel forward by a width of the carousel's visible area.
   */
  scrollForward(options) {
    if (this._isScrollEnd$.value === true) {
      return;
    }
    const distance = options?.distance ?? this._scrollingAreaWidth$.value;
    const behavior = options?.behavior ?? "smooth";
    this.scrollingArea?.scrollBy({
      left: distance,
      behavior
    });
  }
  /**
   * Scrolls the carousel backward by a width of the carousel's visible area.
   */
  scrollBackward(options) {
    if (this._isScrollStart$.value === true) {
      return;
    }
    const distance = options?.distance ?? this._scrollingAreaWidth$.value;
    const behavior = options?.behavior ?? "smooth";
    this.scrollingArea?.scrollBy({
      left: -distance,
      behavior
    });
  }
  /**
   * Starts observing the width of the scrolling area and
   * whether the user has scrolled to the start or end of the carousel.
   */
  subscribeScrollingArea() {
    if (this.scrollingArea) {
      this.scrollingAreaResizeObserver = new ResizeObserver(() => {
        if (this.scrollingArea) {
          this._scrollingAreaWidth$.next(this.scrollingArea.clientWidth);
        }
      });
      this.scrollingAreaResizeObserver.observe(this.scrollingArea);
      this._scrollingAreaWidth$.next(this.scrollingArea.clientWidth);
    }
    this.scrollingAreaIntersectionObserver = new IntersectionObserver((entries) => {
      entries.forEach((entry) => {
        if (entry.target === this.scrollingAreaStart) {
          this._isScrollStart$.next(entry.isIntersecting);
        } else if (entry.target === this.scrollingAreaEnd) {
          this._isScrollEnd$.next(entry.isIntersecting);
        }
      });
    }, {
      root: this.scrollingArea,
      threshold: 0,
      // In highly zoomed-out viewports (e.g., 25% zoom), the `scrollingAreaStart`
      // and `scrollingAreaEnd` elements that have 0px width might not be detected
      // by the IntersectionObserver. To workaround this, we add an arbitrary 2px
      // margin to improve the detection in such cases.
      rootMargin: "2px"
    });
    if (!this.scrollingAreaStart || !this.scrollingAreaEnd) {
      this.logger.error("scrollingAreaStart/End element is not available in the <cx-scrolling-carousel>.");
      return;
    }
    this.scrollingAreaIntersectionObserver.observe(this.scrollingAreaStart);
    this.scrollingAreaIntersectionObserver.observe(this.scrollingAreaEnd);
  }
  /**
   * Stops observing the width of the scrolling area and
   * whether the user has scrolled to the start or end of the carousel.
   */
  unsubscribeScrollingArea() {
    this.windowResizeSubscription?.unsubscribe();
    this.windowResizeSubscription = void 0;
    this.scrollingAreaResizeObserver?.disconnect();
    this.scrollingAreaResizeObserver = void 0;
    this.scrollingAreaIntersectionObserver?.disconnect();
    this.scrollingAreaIntersectionObserver = void 0;
  }
  static {
    this.ɵfac = function HorizontalScrollingPositionDirective_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _HorizontalScrollingPositionDirective)();
    };
  }
  static {
    this.ɵdir = ɵɵdefineDirective({
      type: _HorizontalScrollingPositionDirective,
      selectors: [["", "cxHorizontalScrollingPosition", ""]],
      inputs: {
        scrollingArea: "scrollingArea",
        scrollingAreaStart: "scrollingAreaStart",
        scrollingAreaEnd: "scrollingAreaEnd"
      },
      exportAs: ["cxHorizontalScrollingPosition"],
      standalone: false,
      features: [ɵɵNgOnChangesFeature]
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(HorizontalScrollingPositionDirective, [{
    type: Directive,
    args: [{
      selector: "[cxHorizontalScrollingPosition]",
      exportAs: "cxHorizontalScrollingPosition",
      standalone: false
    }]
  }], null, {
    scrollingArea: [{
      type: Input
    }],
    scrollingAreaStart: [{
      type: Input
    }],
    scrollingAreaEnd: [{
      type: Input
    }]
  });
})();
var KeyboardEventKeys;
(function(KeyboardEventKeys2) {
  KeyboardEventKeys2["ARROW_LEFT"] = "ArrowLeft";
  KeyboardEventKeys2["ARROW_RIGHT"] = "ArrowRight";
  KeyboardEventKeys2["TAB"] = "Tab";
})(KeyboardEventKeys || (KeyboardEventKeys = {}));
var CarouselScrollingComponent = class _CarouselScrollingComponent {
  constructor() {
    this.logger = inject(LoggerService);
    this.el = inject(ElementRef);
    this.previousIcon = ICON_TYPE.CARET_LEFT;
    this.nextIcon = ICON_TYPE.CARET_RIGHT;
    this.trackByFn = (_index, item) => item;
  }
  ngOnInit() {
    if (!this.template && isDevMode()) {
      this.logger.error("No template reference provided to render the carousel items for the `cx-carousel-scrolling`");
    }
  }
  onItemKeydown(event) {
    switch (event.key) {
      case KeyboardEventKeys.ARROW_RIGHT:
      case KeyboardEventKeys.ARROW_LEFT:
        event.preventDefault();
        this.focusNextPrevItem(event.target, event.key === KeyboardEventKeys.ARROW_RIGHT ? 1 : -1);
        break;
      case KeyboardEventKeys.TAB:
        this.skipTabForCarouselItems();
        break;
    }
  }
  /**
   * Handles Tab key on carousel items. If the carousel items have `ArrowRight`/`ArrowLeft`
   * navigation enabled, it temporarily disables tab navigation for these items.
   * The `cxFocusableCarouselItem` selector is used because it identifies carousel
   * items that have `ArrowRight`/`ArrowLeft` navigation enabled. These items should not
   * use tab navigation according to a11y requirements.
   */
  skipTabForCarouselItems() {
    const carouselElements = Array.from(this.el.nativeElement.querySelectorAll("[cxFocusableCarouselItem]"));
    if (!carouselElements.length) {
      return;
    }
    disableTabbingForTick(carouselElements);
  }
  /**
   * Focuses the next or previous item in the carousel based on keyboard navigation.
   *
   * This method determines the next focusable carousel item, identified by the
   * `cxFocusableCarouselItem` directive, based on the current focus and the direction
   * given. It adjusts the carousel's active slide if the next focusable item is
   * outside the currently visible items.
   *
   * @param currentItem - The currently focused carousel item.
   * @param direction - The navigation direction (1 for right, -1 for left).
   * @param size - The number of items per slide, used to determine slide change is needed
   */
  focusNextPrevItem(currentItem, direction) {
    const focusableElements = this.el.nativeElement.querySelectorAll("[cxFocusableCarouselItem]");
    const currentIndex = Array.from(focusableElements).indexOf(currentItem);
    const nextIndex = currentIndex + direction;
    if (nextIndex < 0 || nextIndex >= focusableElements.length) {
      return;
    }
    const targetElement = focusableElements[nextIndex];
    targetElement.focus();
  }
  /**
   * When a carousel item receives focus (e.g. via keyboard navigation),
   * but it's only partially visible in the viewport (not fully scrolled into),
   * this method ensures the item is scrolled into view.
   */
  onItemFocusIn(event) {
    const target = event.target;
    try {
      if (target && typeof target.scrollIntoView === "function") {
        target.scrollIntoView({
          block: "nearest",
          inline: "nearest",
          behavior: "instant"
        });
      }
    } catch (error) {
      this.logger.error("Failed to scroll carousel item into view", error);
    }
  }
  static {
    this.ɵfac = function CarouselScrollingComponent_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _CarouselScrollingComponent)();
    };
  }
  static {
    this.ɵcmp = ɵɵdefineComponent({
      type: _CarouselScrollingComponent,
      selectors: [["cx-carousel-scrolling"]],
      inputs: {
        title: "title",
        items: "items",
        template: "template",
        previousIcon: "previousIcon",
        nextIcon: "nextIcon",
        trackByFn: "trackByFn"
      },
      standalone: false,
      decls: 1,
      vars: 1,
      consts: [["cxHorizontalScrollingPosition", "cxHorizontalScrollingPosition"], ["scrollingArea", ""], ["scrollingAreaStart", ""], ["scrollingAreaEnd", ""], ["cxHorizontalScrollingPosition", "", 3, "scrollingArea", "scrollingAreaStart", "scrollingAreaEnd", 4, "ngIf"], ["cxHorizontalScrollingPosition", "", 3, "scrollingArea", "scrollingAreaStart", "scrollingAreaEnd"], [4, "ngIf"], [1, "carousel-panel"], ["tabindex", "-1", 1, "previous", 3, "click"], [3, "type"], [1, "carousel-items"], [1, "carousel-items-start"], [4, "ngFor", "ngForOf", "ngForTrackBy"], [1, "carousel-items-end"], ["tabindex", "-1", 1, "next", 3, "click"], ["class", "item", 3, "keydown", "focusin", 4, "ngIf"], [1, "item", 3, "keydown", "focusin"], [4, "ngTemplateOutlet", "ngTemplateOutletContext"]],
      template: function CarouselScrollingComponent_Template(rf, ctx) {
        if (rf & 1) {
          ɵɵtemplate(0, CarouselScrollingComponent_ng_container_0_Template, 23, 34, "ng-container", 4);
        }
        if (rf & 2) {
          ɵɵproperty("ngIf", ctx.items && ctx.items.length > 0);
        }
      },
      dependencies: [NgForOf, NgIf, NgTemplateOutlet, IconComponent, HorizontalScrollingPositionDirective, AsyncPipe, TranslatePipe],
      encapsulation: 2,
      changeDetection: 0
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(CarouselScrollingComponent, [{
    type: Component,
    args: [{
      selector: "cx-carousel-scrolling",
      changeDetection: ChangeDetectionStrategy.OnPush,
      standalone: false,
      template: `<ng-container
  *ngIf="items && items.length > 0"
  cxHorizontalScrollingPosition
  #cxHorizontalScrollingPosition="cxHorizontalScrollingPosition"
  [scrollingArea]="scrollingArea"
  [scrollingAreaStart]="scrollingAreaStart"
  [scrollingAreaEnd]="scrollingAreaEnd"
>
  <h2 *ngIf="title">{{ title }}</h2>

  <div class="carousel-panel">
    <button
      tabindex="-1"
      class="previous"
      (click)="
        $event.stopPropagation(); cxHorizontalScrollingPosition.scrollBackward()
      "
      [attr.aria-disabled]="
        cxHorizontalScrollingPosition.isScrollStart$ | async
      "
      [attr.aria-label]="'carousel.scrollBackward' | cxTranslate"
      [attr.title]="'carousel.scrollBackward' | cxTranslate"
      [class.scroll-possible]="
        cxHorizontalScrollingPosition.isScrollNeeded$ | async
      "
    >
      <cx-icon [type]="previousIcon"></cx-icon>
    </button>
    <div class="carousel-items" #scrollingArea>
      <span #scrollingAreaStart class="carousel-items-start"></span>

      <ng-container
        *ngFor="let item of items; let i = index; trackBy: trackByFn"
      >
        <div
          *ngIf="item | async as data"
          class="item"
          (keydown)="onItemKeydown($event)"
          (focusin)="onItemFocusIn($event)"
        >
          <ng-container
            *ngTemplateOutlet="template; context: { item: data, itemIndex: i }"
          ></ng-container>
        </div>
      </ng-container>

      <span #scrollingAreaEnd class="carousel-items-end"></span>
    </div>
    <button
      tabindex="-1"
      class="next"
      (click)="
        $event.stopPropagation(); cxHorizontalScrollingPosition.scrollForward()
      "
      [attr.aria-disabled]="cxHorizontalScrollingPosition.isScrollEnd$ | async"
      [attr.aria-label]="'carousel.scrollForward' | cxTranslate"
      [attr.title]="'carousel.scrollForward' | cxTranslate"
      [class.scroll-possible]="
        cxHorizontalScrollingPosition.isScrollNeeded$ | async
      "
    >
      <cx-icon [type]="nextIcon"></cx-icon>
    </button>
  </div>
</ng-container>
`
    }]
  }], null, {
    title: [{
      type: Input
    }],
    items: [{
      type: Input
    }],
    template: [{
      type: Input
    }],
    previousIcon: [{
      type: Input
    }],
    nextIcon: [{
      type: Input
    }],
    trackByFn: [{
      type: Input
    }]
  });
})();
var HorizontalScrollingPositionDirectiveModule = class _HorizontalScrollingPositionDirectiveModule {
  static {
    this.ɵfac = function HorizontalScrollingPositionDirectiveModule_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _HorizontalScrollingPositionDirectiveModule)();
    };
  }
  static {
    this.ɵmod = ɵɵdefineNgModule({
      type: _HorizontalScrollingPositionDirectiveModule,
      declarations: [HorizontalScrollingPositionDirective],
      exports: [HorizontalScrollingPositionDirective]
    });
  }
  static {
    this.ɵinj = ɵɵdefineInjector({});
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(HorizontalScrollingPositionDirectiveModule, [{
    type: NgModule,
    args: [{
      declarations: [HorizontalScrollingPositionDirective],
      exports: [HorizontalScrollingPositionDirective]
    }]
  }], null, null);
})();
var CarouselScrollingModule = class _CarouselScrollingModule {
  static {
    this.ɵfac = function CarouselScrollingModule_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _CarouselScrollingModule)();
    };
  }
  static {
    this.ɵmod = ɵɵdefineNgModule({
      type: _CarouselScrollingModule,
      declarations: [CarouselScrollingComponent],
      imports: [CommonModule, RouterModule, IconModule, MediaModule, UrlModule, I18nModule, HorizontalScrollingPositionDirectiveModule],
      exports: [CarouselScrollingComponent]
    });
  }
  static {
    this.ɵinj = ɵɵdefineInjector({
      imports: [CommonModule, RouterModule, IconModule, MediaModule, UrlModule, I18nModule, HorizontalScrollingPositionDirectiveModule]
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(CarouselScrollingModule, [{
    type: NgModule,
    args: [{
      imports: [CommonModule, RouterModule, IconModule, MediaModule, UrlModule, I18nModule, HorizontalScrollingPositionDirectiveModule],
      declarations: [CarouselScrollingComponent],
      exports: [CarouselScrollingComponent]
    }]
  }], null, null);
})();
var DatePickerService = class _DatePickerService {
  get placeholder() {
    return "yyyy-mm-dd";
  }
  /**
   * The default date pattern is based on the placeholder string;
   */
  get pattern() {
    return this.placeholder.replace("yyyy", "\\d{4}").replace("mm", "\\d{1,2}").replace("dd", "\\d{1,2}");
  }
  /**
   * Validates if the string based date value is a valid date.
   */
  isValidFormat(date, pattern) {
    const patternRegex = new RegExp(`^${pattern ?? this.pattern}$`);
    return patternRegex.test(date);
  }
  /**
   * Since Safari doesn't support proper date formats (ISO 8601), we need to do this
   * ourselves. We cannot rely on `new Date('2020-1-1')`. This will fail, only
   * `new Date('2020-01-01')` works.
   */
  getDate(value) {
    if (!value) {
      return;
    }
    const delimiter = this.placeholder.replace("yyyy", "").replace("mm", "").replace("dd", "").substring(0, 1);
    const dateParts = value.split(delimiter);
    const placeholderParts = this.placeholder.split(delimiter);
    const y = placeholderParts.indexOf("yyyy");
    const m = placeholderParts.indexOf("mm");
    const d = placeholderParts.indexOf("dd");
    return new Date(Number(dateParts[y]), Number(dateParts[m]) - 1, Number(dateParts[d]));
  }
  static {
    this.ɵfac = function DatePickerService_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _DatePickerService)();
    };
  }
  static {
    this.ɵprov = ɵɵdefineInjectable({
      token: _DatePickerService,
      factory: _DatePickerService.ɵfac,
      providedIn: "root"
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(DatePickerService, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], null, null);
})();
var FormErrorsComponent = class _FormErrorsComponent {
  constructor(ChangeDetectionRef) {
    this.ChangeDetectionRef = ChangeDetectionRef;
    this.featureConfigService = inject(FeatureConfigService);
    this.elementRef = inject(ElementRef, {
      optional: true
    });
    this.ariaLiveToken = inject(new HostAttributeToken("aria-live"), {
      optional: true
    });
    this.prefix = "formErrors.labeled";
    this.fallbackPrefix = "formErrors";
    this.previousTouchedState = false;
    this.role = this.featureConfigService.isEnabled("a11yImprovedErrorMessage") ? null : "alert";
    this.ariaLive = !this.featureConfigService.isEnabled("a11yImprovedErrorMessage") ? this.ariaLiveToken : (
      // If no aria-live value is set add 'polite' as a default. This is preferred over setting
      // role='alert' so that screen readers do not interrupt the current task to read this aloud.
      this.ariaLiveToken ?? "polite"
    );
    this.atomic = true;
  }
  set control(control) {
    if (!control) {
      return;
    }
    this._control = control;
    this.errorsDetails$ = control?.statusChanges.pipe(startWith({}), map(() => control.errors || {}), map((errors) => Object.entries(errors).filter(([_key, details]) => details)));
  }
  get control() {
    return this._control;
  }
  ngDoCheck() {
    if (this.control.touched !== this.previousTouchedState) {
      if (this.featureConfigService.isEnabled("a11yImprovedErrorMessage") && this.elementRef?.nativeElement?.getAttribute("aria-live") === "polite") {
        setTimeout(() => {
          this.previousTouchedState = this.control.touched;
          this.ChangeDetectionRef.markForCheck();
        });
      } else {
        this.previousTouchedState = this.control.touched;
        this.ChangeDetectionRef.markForCheck();
      }
    }
  }
  /**
   * Returns translation params composed of
   * the argument `errorDetails` (if only is an object) merged with
   * the component input object `translationParams`.
   *
   * In case of a conflicting object key, the value from
   * `translationParams` takes precedence.
   */
  getTranslationParams(errorDetails) {
    errorDetails = isObject(errorDetails) ? errorDetails : {};
    return __spreadValues(__spreadValues({}, errorDetails), this.translationParams);
  }
  get invalid() {
    return this.control?.invalid;
  }
  get dirty() {
    return this.control?.dirty;
  }
  get touched() {
    return this.control?.touched;
  }
  get hidden() {
    return !(this.invalid && (this.touched || this.dirty));
  }
  static {
    this.ɵfac = function FormErrorsComponent_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _FormErrorsComponent)(ɵɵdirectiveInject(ChangeDetectorRef));
    };
  }
  static {
    this.ɵcmp = ɵɵdefineComponent({
      type: _FormErrorsComponent,
      selectors: [["cx-form-errors"]],
      hostVars: 11,
      hostBindings: function FormErrorsComponent_HostBindings(rf, ctx) {
        if (rf & 2) {
          ɵɵattribute("role", ctx.role)("aria-live", ctx.ariaLive)("aria-atomic", ctx.atomic);
          ɵɵclassProp("control-invalid", ctx.invalid)("control-dirty", ctx.dirty)("control-touched", ctx.touched)("cx-visually-hidden", ctx.hidden);
        }
      },
      inputs: {
        prefix: "prefix",
        fallbackPrefix: "fallbackPrefix",
        translationParams: "translationParams",
        control: "control"
      },
      standalone: false,
      decls: 2,
      vars: 3,
      consts: [[4, "ngIf"], [4, "ngFor", "ngForOf"]],
      template: function FormErrorsComponent_Template(rf, ctx) {
        if (rf & 1) {
          ɵɵtemplate(0, FormErrorsComponent_ng_container_0_Template, 2, 1, "ng-container", 0);
          ɵɵpipe(1, "async");
        }
        if (rf & 2) {
          ɵɵproperty("ngIf", ɵɵpipeBind1(1, 1, ctx.errorsDetails$));
        }
      },
      dependencies: [NgForOf, NgIf, AsyncPipe, TranslatePipe],
      encapsulation: 2,
      changeDetection: 0
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(FormErrorsComponent, [{
    type: Component,
    args: [{
      selector: "cx-form-errors",
      changeDetection: ChangeDetectionStrategy.OnPush,
      standalone: false,
      template: `<ng-container *ngIf="errorsDetails$ | async as errorDetails">
  <ng-container *ngIf="!hidden">
    <p *ngFor="let error of errorDetails">
      {{
        [prefix + '.' + error[0], fallbackPrefix + '.' + error[0]]
          | cxTranslate: getTranslationParams(error[1])
      }}
    </p>
  </ng-container>
</ng-container>
`
    }]
  }], () => [{
    type: ChangeDetectorRef
  }], {
    prefix: [{
      type: Input
    }],
    fallbackPrefix: [{
      type: Input
    }],
    translationParams: [{
      type: Input
    }],
    control: [{
      type: Input
    }],
    invalid: [{
      type: HostBinding,
      args: ["class.control-invalid"]
    }],
    dirty: [{
      type: HostBinding,
      args: ["class.control-dirty"]
    }],
    touched: [{
      type: HostBinding,
      args: ["class.control-touched"]
    }],
    hidden: [{
      type: HostBinding,
      args: ["class.cx-visually-hidden"]
    }],
    role: [{
      type: HostBinding,
      args: ["attr.role"]
    }],
    ariaLive: [{
      type: HostBinding,
      args: ["attr.aria-live"]
    }],
    atomic: [{
      type: HostBinding,
      args: ["attr.aria-atomic"]
    }]
  });
})();
var DatePickerComponent = class _DatePickerComponent {
  constructor(service) {
    this.service = service;
    this.label = "Date";
    this.update = new EventEmitter();
  }
  change() {
    this.update.emit();
  }
  get placeholder() {
    return this.service.placeholder;
  }
  get pattern() {
    return this.service.pattern;
  }
  /**
   * Only returns the date if we have a valid format. We do this to avoid
   * loads of console errors coming from the datePipe while the user is typing
   * (in those browsers where the date picker isn't supported).
   */
  getDate(date) {
    return date && this.service.isValidFormat(date) ? date : void 0;
  }
  static {
    this.ɵfac = function DatePickerComponent_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _DatePickerComponent)(ɵɵdirectiveInject(DatePickerService));
    };
  }
  static {
    this.ɵcmp = ɵɵdefineComponent({
      type: _DatePickerComponent,
      selectors: [["cx-date-picker"]],
      inputs: {
        control: "control",
        min: "min",
        max: "max",
        required: "required",
        label: "label"
      },
      outputs: {
        update: "update"
      },
      standalone: false,
      decls: 5,
      vars: 20,
      consts: [["type", "date", 1, "form-control", 3, "change", "formControl", "placeholder", "pattern"], ["id", "controlError", "prefix", "formErrors.labeled.date", "fallbackPrefix", "formErrors.date", 3, "control", "translationParams"]],
      template: function DatePickerComponent_Template(rf, ctx) {
        if (rf & 1) {
          ɵɵelementStart(0, "input", 0);
          ɵɵlistener("change", function DatePickerComponent_Template_input_change_0_listener() {
            return ctx.change();
          });
          ɵɵelementEnd();
          ɵɵelement(1, "cx-form-errors", 1);
          ɵɵpipe(2, "cxTranslate");
          ɵɵpipe(3, "cxDate");
          ɵɵpipe(4, "cxDate");
        }
        if (rf & 2) {
          let tmp_9_0;
          ɵɵproperty("formControl", ctx.control)("placeholder", ctx.placeholder)("pattern", ctx.pattern);
          ɵɵattribute("min", ctx.min)("max", ctx.max)("required", ctx.required === false ? void 0 : ctx.required)("aria-invalid", !ctx.control.untouched && ctx.control.invalid)("aria-errormessage", "controlError");
          ɵɵadvance();
          ɵɵproperty("control", ctx.control)("translationParams", ɵɵpureFunction3(16, _c21, ɵɵpipeBind1(2, 10, (tmp_9_0 = ctx.label) !== null && tmp_9_0 !== void 0 ? tmp_9_0 : ""), ɵɵpipeBind1(3, 12, ctx.getDate(ctx.max)), ɵɵpipeBind1(4, 14, ctx.getDate(ctx.min))));
        }
      },
      dependencies: [DefaultValueAccessor, NgControlStatus, PatternValidator, FormControlDirective, FormErrorsComponent, TranslatePipe, CxDatePipe],
      encapsulation: 2
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(DatePickerComponent, [{
    type: Component,
    args: [{
      selector: "cx-date-picker",
      standalone: false,
      template: `<input
  class="form-control"
  type="date"
  [formControl]="control"
  [attr.min]="min"
  [attr.max]="max"
  [attr.required]="required === false ? undefined : required"
  (change)="change()"
  [placeholder]="placeholder"
  [pattern]="pattern"
  [attr.aria-invalid]="!control.untouched && control.invalid"
  [attr.aria-errormessage]="'controlError'"
/>

<cx-form-errors
  id="controlError"
  [control]="control"
  prefix="formErrors.labeled.date"
  fallbackPrefix="formErrors.date"
  [translationParams]="{
    label: label ?? '' | cxTranslate,
    max: getDate(max) | cxDate,
    min: getDate(min) | cxDate,
  }"
></cx-form-errors>
`
    }]
  }], () => [{
    type: DatePickerService
  }], {
    control: [{
      type: Input
    }],
    min: [{
      type: Input
    }],
    max: [{
      type: Input
    }],
    required: [{
      type: Input
    }],
    label: [{
      type: Input
    }],
    update: [{
      type: Output
    }]
  });
})();
var FormErrorsModule = class _FormErrorsModule {
  static {
    this.ɵfac = function FormErrorsModule_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _FormErrorsModule)();
    };
  }
  static {
    this.ɵmod = ɵɵdefineNgModule({
      type: _FormErrorsModule,
      declarations: [FormErrorsComponent],
      imports: [CommonModule, I18nModule, FeaturesConfigModule],
      exports: [FormErrorsComponent]
    });
  }
  static {
    this.ɵinj = ɵɵdefineInjector({
      imports: [CommonModule, I18nModule, FeaturesConfigModule]
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(FormErrorsModule, [{
    type: NgModule,
    args: [{
      imports: [CommonModule, I18nModule, FeaturesConfigModule],
      declarations: [FormErrorsComponent],
      exports: [FormErrorsComponent]
    }]
  }], null, null);
})();
var DatePickerModule = class _DatePickerModule {
  static {
    this.ɵfac = function DatePickerModule_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _DatePickerModule)();
    };
  }
  static {
    this.ɵmod = ɵɵdefineNgModule({
      type: _DatePickerModule,
      declarations: [DatePickerComponent],
      imports: [CommonModule, ReactiveFormsModule, FormErrorsModule, I18nModule, FeaturesConfigModule],
      exports: [DatePickerComponent]
    });
  }
  static {
    this.ɵinj = ɵɵdefineInjector({
      imports: [CommonModule, ReactiveFormsModule, FormErrorsModule, I18nModule, FeaturesConfigModule]
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(DatePickerModule, [{
    type: NgModule,
    args: [{
      imports: [CommonModule, ReactiveFormsModule, FormErrorsModule, I18nModule, FeaturesConfigModule],
      declarations: [DatePickerComponent],
      exports: [DatePickerComponent]
    }]
  }], null, null);
})();
var FileUploadComponent = class _FileUploadComponent {
  constructor() {
    this.accept = "*";
    this.multiple = false;
    this.update = new EventEmitter();
    this.onChangeCallback = () => {
    };
    this.onTouchedCallback = () => {
    };
  }
  selectFile($event) {
    const files = $event.target?.files;
    this.onChangeCallback(files);
    this.update.emit(files);
  }
  removeFile() {
    this.fileInput.nativeElement.value = "";
  }
  get selectedFiles() {
    if (this.fileInput.nativeElement.files) {
      return Array.from(this.fileInput.nativeElement.files);
    }
    return void 0;
  }
  registerOnChange(callback) {
    this.onChangeCallback = callback;
  }
  registerOnTouched(callback) {
    this.onTouchedCallback = callback;
  }
  setDisabledState(disabled) {
    this.fileInput.nativeElement.disabled = disabled;
  }
  writeValue(value) {
    if (value instanceof FileList) {
      this.fileInput.nativeElement.files = value;
    }
  }
  static {
    this.ɵfac = function FileUploadComponent_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _FileUploadComponent)();
    };
  }
  static {
    this.ɵcmp = ɵɵdefineComponent({
      type: _FileUploadComponent,
      selectors: [["cx-file-upload"]],
      contentQueries: function FileUploadComponent_ContentQueries(rf, ctx, dirIndex) {
        if (rf & 1) {
          ɵɵcontentQuery(dirIndex, TemplateRef, 5);
        }
        if (rf & 2) {
          let _t;
          ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.customButton = _t.first);
        }
      },
      viewQuery: function FileUploadComponent_Query(rf, ctx) {
        if (rf & 1) {
          ɵɵviewQuery(_c222, 7);
        }
        if (rf & 2) {
          let _t;
          ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.fileInput = _t.first);
        }
      },
      inputs: {
        accept: "accept",
        multiple: "multiple"
      },
      outputs: {
        update: "update"
      },
      standalone: false,
      features: [ɵɵProvidersFeature([{
        provide: NG_VALUE_ACCESSOR,
        useExisting: forwardRef(() => _FileUploadComponent),
        multi: true
      }])],
      ngContentSelectors: _c12,
      decls: 5,
      vars: 5,
      consts: [["fileInput", ""], ["type", "file", "aria-hidden", "true", 3, "change", "accept", "multiple"], ["class", "btn btn-secondary", "type", "button", 3, "click", 4, "ngIf"], ["class", "link cx-action-link", 3, "click", 4, "ngIf"], [4, "ngFor", "ngForOf"], ["type", "button", 1, "btn", "btn-secondary", 3, "click"], [1, "link", "cx-action-link", 3, "click"], [4, "ngTemplateOutlet"]],
      template: function FileUploadComponent_Template(rf, ctx) {
        if (rf & 1) {
          const _r1 = ɵɵgetCurrentView();
          ɵɵprojectionDef();
          ɵɵelementStart(0, "input", 1, 0);
          ɵɵlistener("change", function FileUploadComponent_Template_input_change_0_listener($event) {
            ɵɵrestoreView(_r1);
            return ɵɵresetView(ctx.selectFile($event));
          });
          ɵɵelementEnd();
          ɵɵtemplate(2, FileUploadComponent_button_2_Template, 3, 3, "button", 2)(3, FileUploadComponent_button_3_Template, 2, 1, "button", 3)(4, FileUploadComponent_p_4_Template, 2, 1, "p", 4);
        }
        if (rf & 2) {
          ɵɵproperty("accept", ctx.accept)("multiple", ctx.multiple);
          ɵɵadvance(2);
          ɵɵproperty("ngIf", !ctx.customButton);
          ɵɵadvance();
          ɵɵproperty("ngIf", ctx.customButton);
          ɵɵadvance();
          ɵɵproperty("ngForOf", ctx.selectedFiles);
        }
      },
      dependencies: [NgForOf, NgIf, NgTemplateOutlet, TranslatePipe],
      encapsulation: 2
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(FileUploadComponent, [{
    type: Component,
    args: [{
      selector: "cx-file-upload",
      providers: [{
        provide: NG_VALUE_ACCESSOR,
        useExisting: forwardRef(() => FileUploadComponent),
        multi: true
      }],
      standalone: false,
      template: `<input
  type="file"
  aria-hidden="true"
  [accept]="accept"
  [multiple]="multiple"
  (change)="selectFile($event)"
  #fileInput
/>
<button
  *ngIf="!customButton"
  [attr.aria-label]="'common.selectFile' | cxTranslate"
  class="btn btn-secondary"
  type="button"
  (click)="fileInput.click()"
>
  <ng-content></ng-content>
</button>
<!-- TODO(Brian-Parth): CXEC-17196 -->
<button
  class="link cx-action-link"
  *ngIf="customButton"
  (click)="fileInput.click()"
>
  <ng-container *ngTemplateOutlet="customButton"> </ng-container>
</button>
<p *ngFor="let file of selectedFiles">
  {{ file?.name }}
</p>
`
    }]
  }], null, {
    accept: [{
      type: Input
    }],
    multiple: [{
      type: Input
    }],
    customButton: [{
      type: ContentChild,
      args: [TemplateRef]
    }],
    update: [{
      type: Output
    }],
    fileInput: [{
      type: ViewChild,
      args: ["fileInput", {
        static: true
      }]
    }]
  });
})();
var FileUploadModule = class _FileUploadModule {
  static {
    this.ɵfac = function FileUploadModule_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _FileUploadModule)();
    };
  }
  static {
    this.ɵmod = ɵɵdefineNgModule({
      type: _FileUploadModule,
      declarations: [FileUploadComponent],
      imports: [CommonModule, ReactiveFormsModule, FormErrorsModule, I18nModule, KeyboardFocusModule],
      exports: [FileUploadComponent]
    });
  }
  static {
    this.ɵinj = ɵɵdefineInjector({
      imports: [CommonModule, ReactiveFormsModule, FormErrorsModule, I18nModule, KeyboardFocusModule]
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(FileUploadModule, [{
    type: NgModule,
    args: [{
      imports: [CommonModule, ReactiveFormsModule, FormErrorsModule, I18nModule, KeyboardFocusModule],
      declarations: [FileUploadComponent],
      exports: [FileUploadComponent]
    }]
  }], null, null);
})();
var PasswordVisibilityToggleComponent = class _PasswordVisibilityToggleComponent {
  constructor() {
    this.showState = {
      icon: ICON_TYPE.EYE_SLASH,
      inputType: "text",
      ariaLabel: "passwordVisibility.hidePassword"
    };
    this.hideState = {
      icon: ICON_TYPE.EYE,
      inputType: "password",
      ariaLabel: "passwordVisibility.showPassword"
    };
    this.state = this.hideState;
  }
  /**
   * Toggle the visibility of the text of the input field.
   */
  toggle() {
    this.state = this.state === this.hideState ? this.showState : this.hideState;
    this.inputElement.setAttribute("type", this.state.inputType);
  }
  static {
    this.ɵfac = function PasswordVisibilityToggleComponent_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _PasswordVisibilityToggleComponent)();
    };
  }
  static {
    this.ɵcmp = ɵɵdefineComponent({
      type: _PasswordVisibilityToggleComponent,
      selectors: [["cx-password-visibility-toggle"]],
      inputs: {
        inputElement: "inputElement"
      },
      standalone: false,
      decls: 5,
      vars: 8,
      consts: [["type", "button", 3, "click"], ["aria-hidden", "true"], [3, "type"]],
      template: function PasswordVisibilityToggleComponent_Template(rf, ctx) {
        if (rf & 1) {
          ɵɵelementStart(0, "button", 0);
          ɵɵpipe(1, "cxTranslate");
          ɵɵpipe(2, "cxTranslate");
          ɵɵlistener("click", function PasswordVisibilityToggleComponent_Template_button_click_0_listener() {
            return ctx.toggle();
          });
          ɵɵelementStart(3, "span", 1);
          ɵɵelement(4, "cx-icon", 2);
          ɵɵelementEnd()();
        }
        if (rf & 2) {
          ɵɵattribute("aria-label", ɵɵpipeBind1(1, 4, ctx.state.ariaLabel))("title", ɵɵpipeBind1(2, 6, ctx.state.ariaLabel))("aria-pressed", ctx.state.inputType === ctx.showState.inputType);
          ɵɵadvance(4);
          ɵɵproperty("type", ctx.state.icon);
        }
      },
      dependencies: [IconComponent, TranslatePipe],
      encapsulation: 2,
      changeDetection: 0
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PasswordVisibilityToggleComponent, [{
    type: Component,
    args: [{
      selector: "cx-password-visibility-toggle",
      changeDetection: ChangeDetectionStrategy.OnPush,
      standalone: false,
      template: '<button\n  type="button"\n  (click)="toggle()"\n  [attr.aria-label]="state.ariaLabel | cxTranslate"\n  [attr.title]="state.ariaLabel | cxTranslate"\n  [attr.aria-pressed]="state.inputType === showState.inputType"\n>\n  <span aria-hidden="true">\n    <cx-icon [type]="state.icon"></cx-icon>\n  </span>\n</button>\n'
    }]
  }], null, {
    inputElement: [{
      type: Input
    }]
  });
})();
var FormConfig = class _FormConfig {
  static {
    this.ɵfac = function FormConfig_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _FormConfig)();
    };
  }
  static {
    this.ɵprov = ɵɵdefineInjectable({
      token: _FormConfig,
      factory: function FormConfig_Factory(__ngFactoryType__) {
        let __ngConditionalFactory__ = null;
        if (__ngFactoryType__) {
          __ngConditionalFactory__ = new (__ngFactoryType__ || _FormConfig)();
        } else {
          __ngConditionalFactory__ = ɵɵinject(Config);
        }
        return __ngConditionalFactory__;
      },
      providedIn: "root"
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(FormConfig, [{
    type: Injectable,
    args: [{
      providedIn: "root",
      useExisting: Config
    }]
  }], null, null);
})();
var PasswordVisibilityToggleDirective = class _PasswordVisibilityToggleDirective {
  constructor(winRef, config, elementRef, viewContainerRef, changeDetectorRef) {
    this.winRef = winRef;
    this.config = config;
    this.elementRef = elementRef;
    this.viewContainerRef = viewContainerRef;
    this.changeDetectorRef = changeDetectorRef;
  }
  ngAfterViewInit() {
    if (this.config.form?.passwordVisibilityToggle) {
      this.wrapInput();
      this.insertComponent();
      this.changeDetectorRef.detectChanges();
    }
  }
  insertComponent() {
    const component = this.viewContainerRef.createComponent(PasswordVisibilityToggleComponent);
    component.instance.inputElement = this.elementRef.nativeElement;
    this.inputWrapper?.appendChild(component.location.nativeElement);
  }
  /**
   * We need to wrap the input element in a div to be able to position the toggle button in the right place.
   */
  wrapInput() {
    const input2 = this.elementRef.nativeElement;
    const parent = input2.parentNode;
    this.inputWrapper = this.winRef.document.createElement("div");
    this.inputWrapper.setAttribute("class", "cx-password-input-wrapper");
    parent.replaceChild(this.inputWrapper, input2);
    this.inputWrapper.appendChild(input2);
  }
  static {
    this.ɵfac = function PasswordVisibilityToggleDirective_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _PasswordVisibilityToggleDirective)(ɵɵdirectiveInject(WindowRef), ɵɵdirectiveInject(FormConfig), ɵɵdirectiveInject(ElementRef), ɵɵdirectiveInject(ViewContainerRef), ɵɵdirectiveInject(ChangeDetectorRef));
    };
  }
  static {
    this.ɵdir = ɵɵdefineDirective({
      type: _PasswordVisibilityToggleDirective,
      selectors: [["", "cxPasswordVisibilitySwitch", "", "type", "password"]],
      standalone: false
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PasswordVisibilityToggleDirective, [{
    type: Directive,
    args: [{
      selector: '[cxPasswordVisibilitySwitch][type="password"]',
      standalone: false
    }]
  }], () => [{
    type: WindowRef
  }, {
    type: FormConfig
  }, {
    type: ElementRef
  }, {
    type: ViewContainerRef
  }, {
    type: ChangeDetectorRef
  }], null);
})();
var defaultFormConfig = {
  form: {
    passwordVisibilityToggle: true
  }
};
var PasswordVisibilityToggleModule = class _PasswordVisibilityToggleModule {
  static {
    this.ɵfac = function PasswordVisibilityToggleModule_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _PasswordVisibilityToggleModule)();
    };
  }
  static {
    this.ɵmod = ɵɵdefineNgModule({
      type: _PasswordVisibilityToggleModule,
      declarations: [PasswordVisibilityToggleDirective, PasswordVisibilityToggleComponent],
      imports: [CommonModule, IconModule, I18nModule],
      exports: [PasswordVisibilityToggleDirective, PasswordVisibilityToggleComponent]
    });
  }
  static {
    this.ɵinj = ɵɵdefineInjector({
      providers: [provideDefaultConfig(defaultFormConfig)],
      imports: [CommonModule, IconModule, I18nModule]
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PasswordVisibilityToggleModule, [{
    type: NgModule,
    args: [{
      imports: [CommonModule, IconModule, I18nModule],
      providers: [provideDefaultConfig(defaultFormConfig)],
      declarations: [PasswordVisibilityToggleDirective, PasswordVisibilityToggleComponent],
      exports: [PasswordVisibilityToggleDirective, PasswordVisibilityToggleComponent]
    }]
  }], null, null);
})();
var FormRequiredAsterisksComponent = class _FormRequiredAsterisksComponent {
  constructor() {
    this.titleTranslation = "common.required";
  }
  static {
    this.ɵfac = function FormRequiredAsterisksComponent_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _FormRequiredAsterisksComponent)();
    };
  }
  static {
    this.ɵcmp = ɵɵdefineComponent({
      type: _FormRequiredAsterisksComponent,
      selectors: [["cx-form-required-asterisks"]],
      inputs: {
        titleTranslation: "titleTranslation"
      },
      decls: 3,
      vars: 3,
      consts: [["aria-hidden", "true", 1, "text-decoration-none", "required-asterisk", 3, "title"]],
      template: function FormRequiredAsterisksComponent_Template(rf, ctx) {
        if (rf & 1) {
          ɵɵelementStart(0, "abbr", 0);
          ɵɵpipe(1, "cxTranslate");
          ɵɵtext(2, "*");
          ɵɵelementEnd();
        }
        if (rf & 2) {
          ɵɵpropertyInterpolate("title", ɵɵpipeBind1(1, 1, ctx.titleTranslation));
        }
      },
      dependencies: [CommonModule, I18nModule, TranslatePipe],
      encapsulation: 2,
      changeDetection: 0
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(FormRequiredAsterisksComponent, [{
    type: Component,
    args: [{
      selector: "cx-form-required-asterisks",
      changeDetection: ChangeDetectionStrategy.OnPush,
      standalone: true,
      imports: [CommonModule, I18nModule],
      template: '<abbr\n  class="text-decoration-none required-asterisk"\n  aria-hidden="true"\n  title="{{ titleTranslation! | cxTranslate }}"\n  >*</abbr\n>\n'
    }]
  }], null, {
    titleTranslation: [{
      type: Input
    }]
  });
})();
var FormRequiredLegendComponent = class _FormRequiredLegendComponent {
  constructor() {
    this.formLegendTranslation = "formLegend.required";
  }
  static {
    this.ɵfac = function FormRequiredLegendComponent_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _FormRequiredLegendComponent)();
    };
  }
  static {
    this.ɵcmp = ɵɵdefineComponent({
      type: _FormRequiredLegendComponent,
      selectors: [["cx-form-required-legend"]],
      inputs: {
        formLegendTranslation: "formLegendTranslation"
      },
      decls: 3,
      vars: 3,
      consts: [[1, "form-legend"]],
      template: function FormRequiredLegendComponent_Template(rf, ctx) {
        if (rf & 1) {
          ɵɵelementStart(0, "p", 0);
          ɵɵtext(1);
          ɵɵpipe(2, "cxTranslate");
          ɵɵelementEnd();
        }
        if (rf & 2) {
          ɵɵadvance();
          ɵɵtextInterpolate1(" ", ɵɵpipeBind1(2, 1, ctx.formLegendTranslation), "\n");
        }
      },
      dependencies: [CommonModule, I18nModule, TranslatePipe],
      encapsulation: 2,
      changeDetection: 0
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(FormRequiredLegendComponent, [{
    type: Component,
    args: [{
      selector: "cx-form-required-legend",
      changeDetection: ChangeDetectionStrategy.OnPush,
      standalone: true,
      imports: [CommonModule, I18nModule],
      template: '<p class="form-legend">\n  {{ formLegendTranslation! | cxTranslate }}\n</p>\n'
    }]
  }], null, {
    formLegendTranslation: [{
      type: Input
    }]
  });
})();
var ItemCounterComponent = class _ItemCounterComponent {
  constructor() {
    this.min = 1;
    this.step = 1;
    this.allowZero = false;
    this.ariaDescribedById = "";
    this.readonly = false;
    useFeatureStyles("a11yItemCounterFocus");
  }
  ngOnInit() {
    this.sub = this.control.valueChanges.pipe(startWith(this.control.value)).subscribe((value) => this.control.setValue(this.getValidCount(value), {
      emitEvent: false
    }));
  }
  ngOnDestroy() {
    if (this.sub) {
      this.sub.unsubscribe();
    }
  }
  /**
   * Updates the form control value with the input value.
   * It is used to improve keyboard controls of the component.
   */
  updateValue() {
    this.control.setValue(+this.input.nativeElement.value);
    this.control.markAsDirty();
  }
  increment() {
    this.control.setValue(this.control.value + this.step);
    this.control.markAsDirty();
  }
  decrement() {
    this.control.setValue(this.control.value - this.step);
    this.control.markAsDirty();
  }
  /**
   * Validate that the given value is in between
   * the `min` and `max` value. If the value is out
   * of  the min/max range, it will be altered.
   * If `allowZero` is set to true, the 0 value is ignored.
   *
   */
  getValidCount(value) {
    if (value < this.min && !(value === 0 && this.allowZero)) {
      value = this.min;
    }
    if (this.max && value > this.max) {
      value = this.max;
    }
    return value;
  }
  static {
    this.ɵfac = function ItemCounterComponent_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _ItemCounterComponent)();
    };
  }
  static {
    this.ɵcmp = ɵɵdefineComponent({
      type: _ItemCounterComponent,
      selectors: [["cx-item-counter"]],
      viewQuery: function ItemCounterComponent_Query(rf, ctx) {
        if (rf & 1) {
          ɵɵviewQuery(_c23, 5);
        }
        if (rf & 2) {
          let _t;
          ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.input = _t.first);
        }
      },
      hostVars: 2,
      hostBindings: function ItemCounterComponent_HostBindings(rf, ctx) {
        if (rf & 2) {
          ɵɵclassProp("readonly", ctx.readonly);
        }
      },
      inputs: {
        control: "control",
        min: "min",
        max: "max",
        step: "step",
        allowZero: "allowZero",
        ariaDescribedById: "ariaDescribedById",
        readonly: "readonly"
      },
      standalone: false,
      decls: 7,
      vars: 16,
      consts: [["qty", ""], ["type", "button", 3, "tabindex", "cxFocus", "title", "click", 4, "cxFeature"], ["type", "button", 3, "disabled", "tabindex", "cxFocus", "title", "click", 4, "cxFeature"], ["type", "number", 3, "keyup.enter", "min", "max", "step", "readonly", "tabindex", "formControl", "cxFocus"], ["type", "button", 3, "cxFocus", "title", "click", 4, "cxFeature"], ["type", "button", 3, "disabled", "cxFocus", "title", "click", 4, "cxFeature"], ["type", "button", 3, "click", "tabindex", "cxFocus", "title"], ["type", "button", 3, "click", "disabled", "tabindex", "cxFocus", "title"], ["type", "button", 3, "click", "cxFocus", "title"], ["type", "button", 3, "click", "disabled", "cxFocus", "title"]],
      template: function ItemCounterComponent_Template(rf, ctx) {
        if (rf & 1) {
          const _r1 = ɵɵgetCurrentView();
          ɵɵtemplate(0, ItemCounterComponent_button_0_Template, 4, 10, "button", 1)(1, ItemCounterComponent_button_1_Template, 4, 10, "button", 2);
          ɵɵelementStart(2, "input", 3, 0);
          ɵɵpipe(4, "cxTranslate");
          ɵɵlistener("keyup.enter", function ItemCounterComponent_Template_input_keyup_enter_2_listener() {
            ɵɵrestoreView(_r1);
            return ɵɵresetView(ctx.updateValue());
          });
          ɵɵelementEnd();
          ɵɵtemplate(5, ItemCounterComponent_button_5_Template, 4, 9, "button", 4)(6, ItemCounterComponent_button_6_Template, 4, 9, "button", 5);
        }
        if (rf & 2) {
          ɵɵproperty("cxFeature", "a11yItemCounterFocus");
          ɵɵadvance();
          ɵɵproperty("cxFeature", "!a11yItemCounterFocus");
          ɵɵadvance();
          ɵɵproperty("min", ctx.min)("max", ctx.max)("step", ctx.step)("readonly", ctx.readonly)("tabindex", ctx.readonly ? -1 : 0)("formControl", ctx.control)("cxFocus", ɵɵpureFunction0(15, _c24));
          ɵɵattribute("aria-label", ɵɵpipeBind1(4, 13, "itemCounter.quantity"))("aria-describedby", ctx.ariaDescribedById);
          ɵɵadvance(3);
          ɵɵproperty("cxFeature", "a11yItemCounterFocus");
          ɵɵadvance();
          ɵɵproperty("cxFeature", "!a11yItemCounterFocus");
        }
      },
      dependencies: [DefaultValueAccessor, NumberValueAccessor, NgControlStatus, MinValidator, MaxValidator, FormControlDirective, FocusDirective, FeatureDirective, TranslatePipe],
      encapsulation: 2
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(ItemCounterComponent, [{
    type: Component,
    args: [{
      selector: "cx-item-counter",
      standalone: false,
      template: `<button
  *cxFeature="'a11yItemCounterFocus'"
  type="button"
  (click)="decrement()"
  [attr.aria-disabled]="control.disabled || control.value <= min"
  [tabindex]="control.disabled || control.value <= min ? -1 : 0"
  [cxFocus]="{ key: 'decrement' }"
  attr.aria-label="{{ 'itemCounter.removeOne' | cxTranslate }}"
  title="{{ 'itemCounter.removeOne' | cxTranslate }}"
>
  -
</button>
<button
  *cxFeature="'!a11yItemCounterFocus'"
  type="button"
  (click)="decrement()"
  [disabled]="control.disabled || control.value <= min"
  [tabindex]="control.disabled || control.value <= min ? -1 : 0"
  [cxFocus]="{ key: 'decrement' }"
  attr.aria-label="{{ 'itemCounter.removeOne' | cxTranslate }}"
  title="{{ 'itemCounter.removeOne' | cxTranslate }}"
>
  -
</button>
<input
  #qty
  type="number"
  [min]="min"
  [max]="max"
  [step]="step"
  [readonly]="readonly"
  [tabindex]="readonly ? -1 : 0"
  [formControl]="control"
  (keyup.enter)="updateValue()"
  [cxFocus]="{ key: 'qty' }"
  attr.aria-label="{{ 'itemCounter.quantity' | cxTranslate }}"
  [attr.aria-describedby]="ariaDescribedById"
/>
<button
  *cxFeature="'a11yItemCounterFocus'"
  type="button"
  (click)="increment()"
  [attr.aria-disabled]="control.disabled || control.value >= max"
  [cxFocus]="{ key: 'increment' }"
  attr.aria-label="{{ 'itemCounter.addOneMore' | cxTranslate }}"
  title="{{ 'itemCounter.addOneMore' | cxTranslate }}"
>
  +
</button>
<button
  *cxFeature="'!a11yItemCounterFocus'"
  type="button"
  (click)="increment()"
  [disabled]="control.disabled || control.value >= max"
  [cxFocus]="{ key: 'increment' }"
  attr.aria-label="{{ 'itemCounter.addOneMore' | cxTranslate }}"
  title="{{ 'itemCounter.addOneMore' | cxTranslate }}"
>
  +
</button>
`
    }]
  }], () => [], {
    control: [{
      type: Input
    }],
    min: [{
      type: Input
    }],
    max: [{
      type: Input
    }],
    step: [{
      type: Input
    }],
    allowZero: [{
      type: Input
    }],
    ariaDescribedById: [{
      type: Input
    }],
    readonly: [{
      type: HostBinding,
      args: ["class.readonly"]
    }, {
      type: Input
    }],
    input: [{
      type: ViewChild,
      args: ["qty"]
    }]
  });
})();
var ItemCounterModule = class _ItemCounterModule {
  static {
    this.ɵfac = function ItemCounterModule_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _ItemCounterModule)();
    };
  }
  static {
    this.ɵmod = ɵɵdefineNgModule({
      type: _ItemCounterModule,
      declarations: [ItemCounterComponent],
      imports: [CommonModule, ReactiveFormsModule, I18nModule, KeyboardFocusModule, FeaturesConfigModule],
      exports: [ItemCounterComponent]
    });
  }
  static {
    this.ɵinj = ɵɵdefineInjector({
      imports: [CommonModule, ReactiveFormsModule, I18nModule, KeyboardFocusModule, FeaturesConfigModule]
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(ItemCounterModule, [{
    type: NgModule,
    args: [{
      imports: [CommonModule, ReactiveFormsModule, I18nModule, KeyboardFocusModule, FeaturesConfigModule],
      declarations: [ItemCounterComponent],
      exports: [ItemCounterComponent]
    }]
  }], null, null);
})();
var defaultLayoutConfig = {
  breakpoints: {
    xs: 576,
    sm: 768,
    md: 992,
    lg: 1200,
    xl: {
      min: 1200
    }
  }
};
var DirectionConfig = class _DirectionConfig {
  static {
    this.ɵfac = function DirectionConfig_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _DirectionConfig)();
    };
  }
  static {
    this.ɵprov = ɵɵdefineInjectable({
      token: _DirectionConfig,
      factory: function DirectionConfig_Factory(__ngFactoryType__) {
        let __ngConditionalFactory__ = null;
        if (__ngFactoryType__) {
          __ngConditionalFactory__ = new (__ngFactoryType__ || _DirectionConfig)();
        } else {
          __ngConditionalFactory__ = ɵɵinject(Config);
        }
        return __ngConditionalFactory__;
      },
      providedIn: "root"
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(DirectionConfig, [{
    type: Injectable,
    args: [{
      providedIn: "root",
      useExisting: Config
    }]
  }], null, null);
})();
var defaultDirectionConfig = {
  direction: {
    detect: true,
    default: DirectionMode.LTR,
    // we're not polluting the system with all defaults for ltr, but add 2 common used
    // languages (hebrew and arabic) to easily demo directionality
    // see https://meta.wikimedia.org/wiki/Template:List_of_language_names_ordered_by_code
    rtlLanguages: ["he", "ar"]
  }
};
var DirectionService = class _DirectionService {
  constructor(configInit, languageService, winRef) {
    this.configInit = configInit;
    this.languageService = languageService;
    this.winRef = winRef;
    this.startsDetecting = false;
    this.subscription = new Subscription();
  }
  /**
   * Initializes the layout direction for the storefront.
   */
  initialize() {
    return lastValueFrom(this.configInit.getStable("direction").pipe(tap((config) => {
      this.config = config?.direction;
      if (this.config?.detect) {
        this.detect();
      } else {
        this.setDirection(this.winRef.document.documentElement, this.config?.default);
      }
    })));
  }
  /**
   * Observes the _active_ language and set the required direction for the given language.
   * The method is guarded to ensure that the active language is observed only once.
   */
  detect() {
    if (this.startsDetecting) {
      return;
    }
    this.subscription.add(this.languageService.getActive().subscribe((isoCode) => this.setDirection(this.winRef.document.documentElement, this.getDirection(isoCode))));
    this.startsDetecting = true;
  }
  /**
   * Sets the direction attribute for the given element. If the direction is undefined, the `dir`
   * attribute is removed.
   */
  setDirection(el, direction) {
    if (direction) {
      el.setAttribute("dir", direction);
    } else {
      el.removeAttribute("dir");
    }
  }
  /**
   * Gets the `DirectionMode` for the given language isoCode. The language isoCode is compared
   * to the configured list of languages(`direction.rtlLanguages` vs `direction.ltrLanguages`).
   *
   * If no language is given, or no language mapping could be found, we fallback to the default
   * `direction.mode`.
   */
  getDirection(language) {
    if (language && this.config?.rtlLanguages?.includes(language)) {
      return DirectionMode.RTL;
    }
    if (language && this.config?.ltrLanguages?.includes(language)) {
      return DirectionMode.LTR;
    }
    return this.config?.default;
  }
  ngOnDestroy() {
    this.subscription.unsubscribe();
  }
  static {
    this.ɵfac = function DirectionService_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _DirectionService)(ɵɵinject(ConfigInitializerService), ɵɵinject(LanguageService), ɵɵinject(WindowRef));
    };
  }
  static {
    this.ɵprov = ɵɵdefineInjectable({
      token: _DirectionService,
      factory: _DirectionService.ɵfac,
      providedIn: "root"
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(DirectionService, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], () => [{
    type: ConfigInitializerService
  }, {
    type: LanguageService
  }, {
    type: WindowRef
  }], null);
})();
function initHtmlDirAttribute(directionService) {
  const result = () => {
    return directionService.initialize();
  };
  return result;
}
var DirectionModule = class _DirectionModule {
  static {
    this.ɵfac = function DirectionModule_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _DirectionModule)();
    };
  }
  static {
    this.ɵmod = ɵɵdefineNgModule({
      type: _DirectionModule
    });
  }
  static {
    this.ɵinj = ɵɵdefineInjector({
      providers: [{
        provide: APP_INITIALIZER,
        multi: true,
        useFactory: initHtmlDirAttribute,
        deps: [DirectionService]
      }, provideDefaultConfig(defaultDirectionConfig)]
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(DirectionModule, [{
    type: NgModule,
    args: [{
      providers: [{
        provide: APP_INITIALIZER,
        multi: true,
        useFactory: initHtmlDirAttribute,
        deps: [DirectionService]
      }, provideDefaultConfig(defaultDirectionConfig)]
    }]
  }], null, null);
})();
var HamburgerMenuService = class _HamburgerMenuService {
  constructor(router) {
    this.isExpanded = new BehaviorSubject(false);
    router.events.pipe(filter((event) => event instanceof NavigationStart)).subscribe(() => {
      this.toggle(true);
    });
  }
  /**
   * toggles the expand state of the hamburger menu
   */
  toggle(forceCollapse) {
    if (forceCollapse) {
      this.isExpanded.next(false);
    } else {
      this.isExpanded.next(!this.isExpanded.value);
    }
  }
  static {
    this.ɵfac = function HamburgerMenuService_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _HamburgerMenuService)(ɵɵinject(Router));
    };
  }
  static {
    this.ɵprov = ɵɵdefineInjectable({
      token: _HamburgerMenuService,
      factory: _HamburgerMenuService.ɵfac,
      providedIn: "root"
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(HamburgerMenuService, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], () => [{
    type: Router
  }], null);
})();
var HamburgerMenuComponent = class _HamburgerMenuComponent {
  constructor(hamburgerMenuService) {
    this.hamburgerMenuService = hamburgerMenuService;
  }
  toggle() {
    this.hamburgerMenuService.toggle();
  }
  get isExpanded() {
    return this.hamburgerMenuService.isExpanded;
  }
  static {
    this.ɵfac = function HamburgerMenuComponent_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _HamburgerMenuComponent)(ɵɵdirectiveInject(HamburgerMenuService));
    };
  }
  static {
    this.ɵcmp = ɵɵdefineComponent({
      type: _HamburgerMenuComponent,
      selectors: [["cx-hamburger-menu"]],
      standalone: false,
      decls: 11,
      vars: 21,
      consts: [["type", "button", "aria-controls", "cx-header", 1, "cx-hamburger", 3, "click"], [1, "hamburger-box"], [1, "hamburger-inner"]],
      template: function HamburgerMenuComponent_Template(rf, ctx) {
        if (rf & 1) {
          ɵɵelementStart(0, "button", 0);
          ɵɵpipe(1, "async");
          ɵɵpipe(2, "async");
          ɵɵpipe(3, "async");
          ɵɵpipe(4, "cxTranslate");
          ɵɵpipe(5, "cxTranslate");
          ɵɵpipe(6, "async");
          ɵɵpipe(7, "cxTranslate");
          ɵɵpipe(8, "cxTranslate");
          ɵɵlistener("click", function HamburgerMenuComponent_Template_button_click_0_listener() {
            return ctx.toggle();
          });
          ɵɵelementStart(9, "span", 1);
          ɵɵelement(10, "span", 2);
          ɵɵelementEnd()();
        }
        if (rf & 2) {
          ɵɵclassProp("is-active", ɵɵpipeBind1(1, 5, ctx.isExpanded));
          ɵɵattribute("aria-expanded", ɵɵpipeBind1(2, 7, ctx.isExpanded))("aria-label", ɵɵpipeBind1(3, 9, ctx.isExpanded) ? ɵɵpipeBind1(4, 11, "common.close") : ɵɵpipeBind1(5, 13, "common.menu"))("title", ɵɵpipeBind1(6, 15, ctx.isExpanded) ? ɵɵpipeBind1(7, 17, "common.close") : ɵɵpipeBind1(8, 19, "common.menu"));
        }
      },
      dependencies: [AsyncPipe, TranslatePipe],
      encapsulation: 2,
      changeDetection: 0
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(HamburgerMenuComponent, [{
    type: Component,
    args: [{
      selector: "cx-hamburger-menu",
      changeDetection: ChangeDetectionStrategy.OnPush,
      standalone: false,
      template: `<button
  class="cx-hamburger"
  type="button"
  (click)="toggle()"
  [class.is-active]="isExpanded | async"
  [attr.aria-expanded]="isExpanded | async"
  [attr.aria-label]="
    (isExpanded | async)
      ? ('common.close' | cxTranslate)
      : ('common.menu' | cxTranslate)
  "
  [attr.title]="
    (isExpanded | async)
      ? ('common.close' | cxTranslate)
      : ('common.menu' | cxTranslate)
  "
  aria-controls="cx-header"
>
  <span class="hamburger-box">
    <span class="hamburger-inner"></span>
  </span>
</button>
`
    }]
  }], () => [{
    type: HamburgerMenuService
  }], null);
})();
var HamburgerMenuModule = class _HamburgerMenuModule {
  static {
    this.ɵfac = function HamburgerMenuModule_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _HamburgerMenuModule)();
    };
  }
  static {
    this.ɵmod = ɵɵdefineNgModule({
      type: _HamburgerMenuModule,
      declarations: [HamburgerMenuComponent],
      imports: [CommonModule, I18nModule],
      exports: [HamburgerMenuComponent]
    });
  }
  static {
    this.ɵinj = ɵɵdefineInjector({
      providers: [provideDefaultConfig({
        cmsComponents: {
          HamburgerMenuComponent: {
            component: HamburgerMenuComponent
          }
        }
      })],
      imports: [CommonModule, I18nModule]
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(HamburgerMenuModule, [{
    type: NgModule,
    args: [{
      imports: [CommonModule, I18nModule],
      providers: [provideDefaultConfig({
        cmsComponents: {
          HamburgerMenuComponent: {
            component: HamburgerMenuComponent
          }
        }
      })],
      declarations: [HamburgerMenuComponent],
      exports: [HamburgerMenuComponent]
    }]
  }], null, null);
})();
var ThemeService = class _ThemeService {
  constructor(config, rendererFactory) {
    this.config = config;
    this.rendererFactory = rendererFactory;
    this.siteThemeService = inject(SiteThemeService);
    this.featureConfigService = inject(FeatureConfigService);
    this.subscription = new Subscription();
  }
  /**
   * This function is to be called for the root component that is
   * bootstrapped.
   */
  init(rootComponent) {
    this.renderer = this.rendererFactory.createRenderer(null, null);
    this.rootComponent = rootComponent;
    this.subscription.add(this.siteThemeService.getActive().subscribe((activeTheme) => this.setTheme(activeTheme)));
  }
  ngOnDestroy() {
    this.subscription.unsubscribe();
  }
  setTheme(theme) {
    const element = this.rootComponent.location.nativeElement;
    if (this.existingTheme) {
      this.renderer.removeClass(element, this.existingTheme);
    }
    if (theme) {
      this.renderer.addClass(element, theme);
      this.existingTheme = theme;
    }
  }
  static {
    this.ɵfac = function ThemeService_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _ThemeService)(ɵɵinject(SiteContextConfig), ɵɵinject(RendererFactory2));
    };
  }
  static {
    this.ɵprov = ɵɵdefineInjectable({
      token: _ThemeService,
      factory: _ThemeService.ɵfac,
      providedIn: "root"
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(ThemeService, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], () => [{
    type: SiteContextConfig
  }, {
    type: RendererFactory2
  }], null);
})();
function initTheme(themeService) {
  const result = (component) => themeService.init(component);
  return result;
}
var ThemeModule = class _ThemeModule {
  static {
    this.ɵfac = function ThemeModule_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _ThemeModule)();
    };
  }
  static {
    this.ɵmod = ɵɵdefineNgModule({
      type: _ThemeModule
    });
  }
  static {
    this.ɵinj = ɵɵdefineInjector({
      providers: [{
        provide: APP_BOOTSTRAP_LISTENER,
        multi: true,
        useFactory: initTheme,
        deps: [ThemeService]
      }]
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(ThemeModule, [{
    type: NgModule,
    args: [{
      providers: [{
        provide: APP_BOOTSTRAP_LISTENER,
        multi: true,
        useFactory: initTheme,
        deps: [ThemeService]
      }]
    }]
  }], null, null);
})();
var LayoutModule = class _LayoutModule {
  static {
    this.ɵfac = function LayoutModule_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _LayoutModule)();
    };
  }
  static {
    this.ɵmod = ɵɵdefineNgModule({
      type: _LayoutModule,
      imports: [OutletRefModule, LaunchDialogModule, DirectionModule, ThemeModule],
      exports: [OutletRefModule]
    });
  }
  static {
    this.ɵinj = ɵɵdefineInjector({
      providers: [provideDefaultConfig(defaultLayoutConfig)],
      imports: [OutletRefModule, LaunchDialogModule.forRoot(), DirectionModule, ThemeModule, OutletRefModule]
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(LayoutModule, [{
    type: NgModule,
    args: [{
      imports: [OutletRefModule, LaunchDialogModule.forRoot(), DirectionModule, ThemeModule],
      providers: [provideDefaultConfig(defaultLayoutConfig)],
      exports: [OutletRefModule]
    }]
  }], null, null);
})();
var StorefrontOutlets;
(function(StorefrontOutlets2) {
  StorefrontOutlets2["STOREFRONT"] = "cx-storefront";
})(StorefrontOutlets || (StorefrontOutlets = {}));
var GlobalMessageComponent = class _GlobalMessageComponent {
  constructor(globalMessageService) {
    this.globalMessageService = globalMessageService;
    this.iconTypes = ICON_TYPE;
    this.messageType = GlobalMessageType;
  }
  ngOnInit() {
    this.messages$ = this.globalMessageService.get();
  }
  clear(type, index) {
    this.globalMessageService.remove(type, index);
  }
  static {
    this.ɵfac = function GlobalMessageComponent_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _GlobalMessageComponent)(ɵɵdirectiveInject(GlobalMessageService));
    };
  }
  static {
    this.ɵcmp = ɵɵdefineComponent({
      type: _GlobalMessageComponent,
      selectors: [["cx-global-message"]],
      standalone: false,
      decls: 2,
      vars: 3,
      consts: [[4, "ngIf"], ["class", "cx-visually-hidden", 4, "ngFor", "ngForOf"], ["class", "alert alert-success", 4, "ngFor", "ngForOf"], ["class", "alert alert-info", 4, "ngFor", "ngForOf"], ["class", "alert alert-warning", 4, "ngFor", "ngForOf"], ["class", "alert alert-danger", 4, "ngFor", "ngForOf"], [1, "cx-visually-hidden"], [1, "alert", "alert-success"], [1, "alert-icon"], [3, "type"], [1, "close", 3, "click", "title"], [1, "alert", "alert-info"], [1, "alert", "alert-warning"], [1, "alert", "alert-danger"]],
      template: function GlobalMessageComponent_Template(rf, ctx) {
        if (rf & 1) {
          ɵɵtemplate(0, GlobalMessageComponent_div_0_Template, 6, 5, "div", 0);
          ɵɵpipe(1, "async");
        }
        if (rf & 2) {
          ɵɵproperty("ngIf", ɵɵpipeBind1(1, 1, ctx.messages$));
        }
      },
      dependencies: [NgForOf, NgIf, IconComponent, AsyncPipe, TranslatePipe],
      encapsulation: 2
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(GlobalMessageComponent, [{
    type: Component,
    args: [{
      selector: "cx-global-message",
      standalone: false,
      template: `<div *ngIf="messages$ | async as messages">
  <div
    class="cx-visually-hidden"
    *ngFor="let assistiveMsg of messages[messageType.MSG_TYPE_ASSISTIVE]"
  >
    <span>{{ assistiveMsg | cxTranslate }}</span>
  </div>
  <div
    class="alert alert-success"
    *ngFor="
      let confMsg of messages[messageType.MSG_TYPE_CONFIRMATION];
      let i = index
    "
  >
    <span class="alert-icon">
      <cx-icon [type]="iconTypes.SUCCESS"></cx-icon>
    </span>
    <span>{{ confMsg | cxTranslate }}</span>
    <button
      class="close"
      title="{{ 'common.close' | cxTranslate }}"
      (click)="clear(messageType.MSG_TYPE_CONFIRMATION, i)"
    >
      <cx-icon [type]="iconTypes.CLOSE"></cx-icon>
    </button>
  </div>
  <div
    class="alert alert-info"
    *ngFor="let infoMsg of messages[messageType.MSG_TYPE_INFO]; let i = index"
  >
    <span class="alert-icon">
      <cx-icon [type]="iconTypes.INFO"></cx-icon>
    </span>
    <span>{{ infoMsg | cxTranslate }}</span>
    <button
      class="close"
      title="{{ 'common.close' | cxTranslate }}"
      (click)="clear(messageType.MSG_TYPE_INFO, i)"
    >
      <cx-icon [type]="iconTypes.CLOSE"></cx-icon>
    </button>
  </div>
  <div
    class="alert alert-warning"
    *ngFor="
      let infoMsg of messages[messageType.MSG_TYPE_WARNING];
      let i = index
    "
  >
    <span class="alert-icon">
      <cx-icon [type]="iconTypes.WARNING"></cx-icon>
    </span>
    <span>{{ infoMsg | cxTranslate }}</span>
    <button
      class="close"
      title="{{ 'common.close' | cxTranslate }}"
      (click)="clear(messageType.MSG_TYPE_WARNING, i)"
    >
      <cx-icon [type]="iconTypes.CLOSE"></cx-icon>
    </button>
  </div>
  <div
    class="alert alert-danger"
    *ngFor="let errorMsg of messages[messageType.MSG_TYPE_ERROR]; let i = index"
  >
    <span class="alert-icon">
      <cx-icon [type]="iconTypes.ERROR"></cx-icon>
    </span>
    <span>{{ errorMsg | cxTranslate }}</span>
    <button
      class="close"
      title="{{ 'common.close' | cxTranslate }}"
      (click)="clear(messageType.MSG_TYPE_ERROR, i)"
    >
      <cx-icon [type]="iconTypes.CLOSE"></cx-icon>
    </button>
  </div>
</div>
`
    }]
  }], () => [{
    type: GlobalMessageService
  }], null);
})();
var StorefrontComponent = class _StorefrontComponent {
  handleEscape(event) {
    this.keyboardFocusService.handleEscape(this.elementRef.nativeElement, this.keyboardFocusConfig, event);
  }
  constructor(hamburgerMenuService, routingService, elementRef, keyboardFocusService) {
    this.hamburgerMenuService = hamburgerMenuService;
    this.routingService = routingService;
    this.elementRef = elementRef;
    this.keyboardFocusService = keyboardFocusService;
    this.focusConfig = {
      disableMouseFocus: true,
      trap: false
    };
    this.skipFocusConfig = {
      isEnabled: false,
      activeElementSelectors: ["button.cx-hamburger"]
    };
    this.isExpanded$ = this.hamburgerMenuService.isExpanded;
    this.StorefrontOutlets = StorefrontOutlets;
    this.featureConfigService = inject(FeatureConfigService);
    this.destroyRef = inject(DestroyRef);
    this.document = inject(DOCUMENT, {
      optional: true
    });
    this.skipLinkService = inject(SkipLinkService, {
      optional: true
    });
    this.tabindex = "-1";
    this.keyboardFocusConfig = {
      focusOnEscape: true,
      focusOnDoubleEscape: true
    };
    useFeatureStyles("headerLayoutForSmallerViewports");
    useFeatureStyles("a11yPdpGridArrangement");
    useFeatureStyles("a11yKeyboardFocusInSearchBox");
    useFeatureStyles("a11yNgSelectLayering");
    useFeatureStyles("topProgressBarUseTransformAnimation");
    useFeatureStyles("unifiedDefaultHeaderSlotsAcrossBreakpoints");
  }
  ngOnInit() {
    this.navigateSubscription = this.routingService.isNavigating().subscribe((val) => this.onNavigation(val));
    this.isExpanded$ = this.hamburgerMenuService.isExpanded.pipe(tap((isExpanded) => {
      if (isExpanded) {
        this.focusOnFirstNavigationItem();
      }
    }));
    if (this.featureConfigService.isEnabled("a11yHamburgerMenuTrapFocus")) {
      this.trapFocusOnMenuIfExpanded();
    }
  }
  collapseMenuIfClickOutside(event) {
    const element = event.target;
    if (element.nodeName.toLowerCase() === "header" && element.className.includes("is-expanded")) {
      this.collapseMenu();
    }
  }
  collapseMenu() {
    this.hamburgerMenuService.toggle(true);
  }
  trapFocusOnMenuIfExpanded() {
    this.isExpanded$.pipe(distinctUntilChanged(), takeUntilDestroyed(this.destroyRef)).subscribe((isExpanded) => {
      this.focusConfig = __spreadProps(__spreadValues({}, this.focusConfig), {
        trap: isExpanded
      });
      this.skipFocusConfig = __spreadProps(__spreadValues({}, this.skipFocusConfig), {
        isEnabled: isExpanded
      });
    });
  }
  focusOnFirstNavigationItem() {
    const closestNavigationUi = this.elementRef.nativeElement.querySelector("header cx-navigation-ui");
    const focusable = closestNavigationUi?.querySelector('li:not(.back) button, [tabindex="0"]');
    if (focusable) {
      setTimeout(() => focusable.focus());
    }
  }
  ngOnDestroy() {
    if (this.navigateSubscription) {
      this.navigateSubscription.unsubscribe();
    }
  }
  onNavigation(isNavigating) {
    this.startNavigating = isNavigating === true;
    this.stopNavigating = isNavigating === false;
    if (this.featureConfigService.isEnabled("a11yResetFocusAfterNavigating") && this.stopNavigating && this.document?.activeElement !== this.document?.body) {
      this.skipLinkService?.scrollToTarget("cx-main");
    }
  }
  static {
    this.ɵfac = function StorefrontComponent_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _StorefrontComponent)(ɵɵdirectiveInject(HamburgerMenuService), ɵɵdirectiveInject(RoutingService), ɵɵdirectiveInject(ElementRef), ɵɵdirectiveInject(KeyboardFocusService));
    };
  }
  static {
    this.ɵcmp = ɵɵdefineComponent({
      type: _StorefrontComponent,
      selectors: [["cx-storefront"]],
      viewQuery: function StorefrontComponent_Query(rf, ctx) {
        if (rf & 1) {
          ɵɵviewQuery(SkipLinkComponent, 5);
        }
        if (rf & 2) {
          let _t;
          ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.child = _t.first);
        }
      },
      hostVars: 5,
      hostBindings: function StorefrontComponent_HostBindings(rf, ctx) {
        if (rf & 1) {
          ɵɵlistener("keydown.escape", function StorefrontComponent_keydown_escape_HostBindingHandler($event) {
            return ctx.handleEscape($event);
          });
        }
        if (rf & 2) {
          ɵɵhostProperty("tabindex", ctx.tabindex);
          ɵɵclassProp("start-navigating", ctx.startNavigating)("stop-navigating", ctx.stopNavigating);
        }
      },
      standalone: false,
      decls: 1,
      vars: 1,
      consts: [["cxPageTemplateStyle", "", 3, "cxOutlet"], ["cxOutlet", "cx-header"], ["cxSkipLink", "cx-main", 3, "cxFocus"], ["cxOutlet", "cx-footer"], ["id", "cx-header", "cxSkipLink", "cx-header", 3, "keydown.escape", "click", "cxFocus"], ["section", "header", 3, "cxSkipFocus"], ["section", "navigation"], ["position", "BottomHeaderSlot", 1, "cx-bottom-header-slot"], ["aria-atomic", "true", "aria-live", "assertive"], ["cxSkipLink", "cx-footer", 3, "cxFocus"], ["section", "footer"]],
      template: function StorefrontComponent_Template(rf, ctx) {
        if (rf & 1) {
          ɵɵtemplate(0, StorefrontComponent_ng_template_0_Template, 4, 2, "ng-template", 0);
        }
        if (rf & 2) {
          ɵɵproperty("cxOutlet", ctx.StorefrontOutlets.STOREFRONT);
        }
      },
      dependencies: [RouterOutlet, GlobalMessageComponent, OutletDirective, PageLayoutComponent, PageTemplateDirective, PageSlotComponent, FocusDirective, SkipFocusDirective, SkipLinkDirective, AsyncPipe],
      encapsulation: 2
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(StorefrontComponent, [{
    type: Component,
    args: [{
      selector: "cx-storefront",
      standalone: false,
      template: '<ng-template [cxOutlet]="StorefrontOutlets.STOREFRONT" cxPageTemplateStyle>\n  <ng-template cxOutlet="cx-header">\n    <header\n      id="cx-header"\n      cxSkipLink="cx-header"\n      [cxFocus]="focusConfig"\n      [class.is-expanded]="isExpanded$ | async"\n      (keydown.escape)="collapseMenu()"\n      (click)="collapseMenuIfClickOutside($event)"\n    >\n      <cx-page-layout\n        section="header"\n        [cxSkipFocus]="skipFocusConfig"\n      ></cx-page-layout>\n\n      <cx-page-layout section="navigation"></cx-page-layout>\n    </header>\n    <cx-page-slot\n      position="BottomHeaderSlot"\n      class="cx-bottom-header-slot"\n    ></cx-page-slot>\n    <cx-global-message\n      aria-atomic="true"\n      aria-live="assertive"\n    ></cx-global-message>\n  </ng-template>\n\n  <main cxSkipLink="cx-main" [cxFocus]="{ disableMouseFocus: true }">\n    <router-outlet></router-outlet>\n  </main>\n\n  <ng-template cxOutlet="cx-footer">\n    <footer cxSkipLink="cx-footer" [cxFocus]="{ disableMouseFocus: true }">\n      <cx-page-layout section="footer"></cx-page-layout>\n    </footer>\n  </ng-template>\n</ng-template>\n'
    }]
  }], () => [{
    type: HamburgerMenuService
  }, {
    type: RoutingService
  }, {
    type: ElementRef
  }, {
    type: KeyboardFocusService
  }], {
    document: [{
      type: Optional
    }],
    skipLinkService: [{
      type: Optional
    }],
    startNavigating: [{
      type: HostBinding,
      args: ["class.start-navigating"]
    }],
    stopNavigating: [{
      type: HostBinding,
      args: ["class.stop-navigating"]
    }],
    tabindex: [{
      type: HostBinding,
      args: ["tabindex"]
    }],
    child: [{
      type: ViewChild,
      args: [SkipLinkComponent]
    }],
    handleEscape: [{
      type: HostListener,
      args: ["keydown.escape", ["$event"]]
    }]
  });
})();
var GlobalMessageComponentModule = class _GlobalMessageComponentModule {
  static {
    this.ɵfac = function GlobalMessageComponentModule_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _GlobalMessageComponentModule)();
    };
  }
  static {
    this.ɵmod = ɵɵdefineNgModule({
      type: _GlobalMessageComponentModule,
      declarations: [GlobalMessageComponent],
      imports: [CommonModule, IconModule, I18nModule],
      exports: [GlobalMessageComponent]
    });
  }
  static {
    this.ɵinj = ɵɵdefineInjector({
      imports: [CommonModule, IconModule, I18nModule]
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(GlobalMessageComponentModule, [{
    type: NgModule,
    args: [{
      imports: [CommonModule, IconModule, I18nModule],
      declarations: [GlobalMessageComponent],
      exports: [GlobalMessageComponent]
    }]
  }], null, null);
})();
var StorefrontComponentModule = class _StorefrontComponentModule {
  static {
    this.ɵfac = function StorefrontComponentModule_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _StorefrontComponentModule)();
    };
  }
  static {
    this.ɵmod = ɵɵdefineNgModule({
      type: _StorefrontComponentModule,
      declarations: [StorefrontComponent],
      imports: [CommonModule, RouterModule, GlobalMessageComponentModule, OutletModule, OutletRefModule, PageLayoutModule, PageSlotModule, KeyboardFocusModule, SkipLinkModule],
      exports: [StorefrontComponent]
    });
  }
  static {
    this.ɵinj = ɵɵdefineInjector({
      imports: [CommonModule, RouterModule, GlobalMessageComponentModule, OutletModule, OutletRefModule, PageLayoutModule, PageSlotModule, KeyboardFocusModule, SkipLinkModule]
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(StorefrontComponentModule, [{
    type: NgModule,
    args: [{
      imports: [CommonModule, RouterModule, GlobalMessageComponentModule, OutletModule, OutletRefModule, PageLayoutModule, PageSlotModule, KeyboardFocusModule, SkipLinkModule],
      declarations: [StorefrontComponent],
      exports: [StorefrontComponent]
    }]
  }], null, null);
})();
var ARIA_LABEL = "aria-label";
var NgSelectA11yDirective = class _NgSelectA11yDirective {
  //TODO: CXSPA-9005: Remove this method in next major release
  /**
   * @deprecated since 2211.33
   */
  onOpen() {
    const observer = new MutationObserver((changes, observerInstance) => this.appendAriaLabelToOptions(changes, observerInstance));
    observer.observe(this.elementRef.nativeElement, {
      childList: true
    });
  }
  /**
   * When we inside a combo box using JAWS screen reader and press escape key
   * an escape keyboard event doesn't get fired, instead an AltLeft is fired.
   */
  onKeyUp(event) {
    if (!this.featureConfigService?.isEnabled("a11yNgSelectCloseDropdownOnEscape")) {
      return;
    }
    const jawsEscapeCode = "AltLeft";
    if (event.code === jawsEscapeCode) {
      this.selectComponent.close();
    }
  }
  onEscape() {
    setTimeout(() => {
      this.elementRef.nativeElement.querySelector("input").focus();
    });
  }
  constructor(renderer, elementRef) {
    this.renderer = renderer;
    this.elementRef = elementRef;
    this.translationService = inject(TranslationService);
    this.domSanitizer = inject(DomSanitizer);
    this.selectComponent = inject(NgSelectComponent);
    this.destroyRef = inject(DestroyRef);
    this.featureConfigService = inject(FeatureConfigService);
    this.breakpointService = inject(BreakpointService, {
      optional: true
    });
  }
  ngAfterViewInit() {
    const inputCombobox = this.elementRef.nativeElement.querySelector('[role="combobox"]');
    this.renderer.setAttribute(inputCombobox, "role", "combobox");
    this.renderer.setAttribute(inputCombobox, "aria-expanded", "false");
    if (this.featureConfigService?.isEnabled("a11yNgSelectAriaLabelDropdownCustomized")) {
      this.customizeNgSelectAriaLabelDropdown();
    }
    const isOpened$ = this.selectComponent.openEvent.pipe(map(() => "true"));
    const isClosed$ = this.selectComponent.closeEvent.pipe(map(() => "false"));
    merge(isOpened$, isClosed$).pipe(takeUntilDestroyed(this.destroyRef)).subscribe((state) => {
      this.renderer.setAttribute(inputCombobox, "aria-expanded", state);
      if (ariaControls && this.featureConfigService.isEnabled("a11yNgSelectAriaControls")) {
        setTimeout(() => {
          this.renderer.setAttribute(inputCombobox, "aria-controls", ariaControls);
        });
      }
    });
    const ariaLabel = this.cxNgSelectA11y.ariaLabel;
    const elementId = this.elementRef.nativeElement.id;
    const ariaControls = this.cxNgSelectA11y.ariaControls ?? elementId;
    if (ariaLabel) {
      this.renderer.setAttribute(inputCombobox, ARIA_LABEL, ariaLabel);
    }
    if (ariaControls) {
      this.renderer.setAttribute(inputCombobox, "aria-controls", ariaControls);
    }
    if (inputCombobox.readOnly && isPlatformBrowser(this.platformId)) {
      this.breakpointService?.isDown(BREAKPOINT.md).pipe(filter(Boolean), take(1)).subscribe(() => {
        const selectObserver = new MutationObserver((changes, observer) => {
          this.appendValueToAriaLabel(changes, observer, inputCombobox);
        });
        selectObserver.observe(this.elementRef.nativeElement, {
          subtree: true,
          characterData: true
        });
      });
    }
  }
  //TODO: CXSPA-9005: Remove this method in next major release
  /**
   * @deprecated since 2211.33
   */
  appendAriaLabelToOptions(_changes, observerInstance) {
    const options = this.elementRef?.nativeElement.querySelectorAll(".ng-option");
    if (options?.length) {
      this.translationService.translate("common.of").pipe(take(1)).subscribe((translation) => {
        options.forEach((option, index) => {
          const sanitizedOptionText = this.domSanitizer.sanitize(SecurityContext.HTML, option.innerText);
          const ariaLabel = `${sanitizedOptionText}, ${+index + 1} ${translation} ${options.length}`;
          this.renderer.setAttribute(option, ARIA_LABEL, ariaLabel);
        });
      });
    }
    observerInstance.disconnect();
  }
  /**
   * Hides the input value from the screen reader and provides it as part of the aria-label instead.
   * This improves the screen reader output on mobile devices.
   */
  appendValueToAriaLabel(_changes, observer, divCombobox) {
    const sanitizedValueLabel = this.domSanitizer.sanitize(SecurityContext.HTML, this.elementRef.nativeElement.querySelector(".ng-value-label")?.innerText);
    if (sanitizedValueLabel) {
      const comboboxAriaLabel = divCombobox?.getAttribute(ARIA_LABEL) || "";
      const valueElement = this.elementRef.nativeElement.querySelector(".ng-value");
      this.renderer.setAttribute(valueElement, "aria-hidden", "true");
      this.renderer.setAttribute(divCombobox, ARIA_LABEL, comboboxAriaLabel + ", " + sanitizedValueLabel);
    }
    observer.disconnect();
  }
  customizeNgSelectAriaLabelDropdown() {
    this.translationService.translate("common.ngSelectDropdownOptionsList").pipe(take(1)).subscribe((translation) => {
      this.selectComponent.ariaLabelDropdown = translation;
    });
  }
  static {
    this.ɵfac = function NgSelectA11yDirective_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _NgSelectA11yDirective)(ɵɵdirectiveInject(Renderer2), ɵɵdirectiveInject(ElementRef));
    };
  }
  static {
    this.ɵdir = ɵɵdefineDirective({
      type: _NgSelectA11yDirective,
      selectors: [["", "cxNgSelectA11y", ""]],
      hostBindings: function NgSelectA11yDirective_HostBindings(rf, ctx) {
        if (rf & 1) {
          ɵɵlistener("open", function NgSelectA11yDirective_open_HostBindingHandler() {
            return ctx.onOpen();
          })("keyup", function NgSelectA11yDirective_keyup_HostBindingHandler($event) {
            return ctx.onKeyUp($event);
          })("keydown.escape", function NgSelectA11yDirective_keydown_escape_HostBindingHandler() {
            return ctx.onEscape();
          });
        }
      },
      inputs: {
        cxNgSelectA11y: "cxNgSelectA11y"
      },
      standalone: false
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(NgSelectA11yDirective, [{
    type: Directive,
    args: [{
      selector: "[cxNgSelectA11y]",
      standalone: false
    }]
  }], () => [{
    type: Renderer2
  }, {
    type: ElementRef
  }], {
    cxNgSelectA11y: [{
      type: Input
    }],
    //TODO: CXSPA-9005: Remove this method in next major release
    /**
     * @deprecated since 2211.33
     */
    onOpen: [{
      type: HostListener,
      args: ["open"]
    }],
    onKeyUp: [{
      type: HostListener,
      args: ["keyup", ["$event"]]
    }],
    onEscape: [{
      type: HostListener,
      args: ["keydown.escape"]
    }],
    breakpointService: [{
      type: Optional
    }],
    platformId: [{
      type: Inject,
      args: [PLATFORM_ID]
    }]
  });
})();
var SortingComponent = class _SortingComponent {
  constructor() {
    this.sortListEvent = new EventEmitter();
  }
  sortList(sortCode) {
    this.sortListEvent.emit(sortCode);
  }
  get selectedLabel() {
    if (this.selectedOption) {
      return this.sortOptions?.find((sort) => sort.code === this.selectedOption)?.name ?? this.sortLabels?.[this.selectedOption];
    }
  }
  static {
    this.ɵfac = function SortingComponent_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _SortingComponent)();
    };
  }
  static {
    this.ɵcmp = ɵɵdefineComponent({
      type: _SortingComponent,
      selectors: [["cx-sorting"]],
      inputs: {
        sortOptions: "sortOptions",
        ariaControls: "ariaControls",
        ariaLabel: "ariaLabel",
        selectedOption: "selectedOption",
        placeholder: "placeholder",
        sortLabels: "sortLabels"
      },
      outputs: {
        sortListEvent: "sortListEvent"
      },
      standalone: false,
      decls: 3,
      vars: 11,
      consts: [[3, "change", "searchable", "clearable", "placeholder", "ngModel", "cxNgSelectA11y"], [3, "value", 4, "ngFor", "ngForOf"], [3, "value"]],
      template: function SortingComponent_Template(rf, ctx) {
        if (rf & 1) {
          ɵɵelementStart(0, "ng-select", 0);
          ɵɵpipe(1, "cxTranslate");
          ɵɵlistener("change", function SortingComponent_Template_ng_select_change_0_listener($event) {
            return ctx.sortList($event);
          });
          ɵɵtemplate(2, SortingComponent_ng_option_2_Template, 2, 2, "ng-option", 1);
          ɵɵelementEnd();
        }
        if (rf & 2) {
          ɵɵpropertyInterpolate("placeholder", ctx.placeholder);
          ɵɵproperty("searchable", false)("clearable", false)("ngModel", ctx.selectedOption)("cxNgSelectA11y", ɵɵpureFunction2(8, _c28, ctx.ariaLabel || ɵɵpipeBind1(1, 6, "productList.sortResults"), ctx.ariaControls));
          ɵɵadvance(2);
          ɵɵproperty("ngForOf", ctx.sortOptions);
        }
      },
      dependencies: [NgForOf, NgSelectComponent, NgOptionComponent, NgControlStatus, NgModel, NgSelectA11yDirective, TranslatePipe],
      encapsulation: 2,
      changeDetection: 0
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(SortingComponent, [{
    type: Component,
    args: [{
      selector: "cx-sorting",
      changeDetection: ChangeDetectionStrategy.OnPush,
      standalone: false,
      template: `<ng-select
  [searchable]="false"
  [clearable]="false"
  placeholder="{{ placeholder }}"
  (change)="sortList($event)"
  [ngModel]="selectedOption"
  [cxNgSelectA11y]="{
    ariaLabel: ariaLabel || ('productList.sortResults' | cxTranslate),
    ariaControls: ariaControls,
  }"
>
  <ng-option *ngFor="let sort of sortOptions" [value]="sort.code">{{
    sort.name ? sort.name : sortLabels && sort.code ? sortLabels[sort.code] : ''
  }}</ng-option>
</ng-select>
`
    }]
  }], () => [], {
    sortOptions: [{
      type: Input
    }],
    ariaControls: [{
      type: Input
    }],
    ariaLabel: [{
      type: Input
    }],
    selectedOption: [{
      type: Input
    }],
    placeholder: [{
      type: Input
    }],
    sortLabels: [{
      type: Input
    }],
    sortListEvent: [{
      type: Output
    }]
  });
})();
var NgSelectA11yModule = class _NgSelectA11yModule {
  static {
    this.ɵfac = function NgSelectA11yModule_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _NgSelectA11yModule)();
    };
  }
  static {
    this.ɵmod = ɵɵdefineNgModule({
      type: _NgSelectA11yModule,
      declarations: [NgSelectA11yDirective],
      exports: [NgSelectA11yDirective]
    });
  }
  static {
    this.ɵinj = ɵɵdefineInjector({});
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(NgSelectA11yModule, [{
    type: NgModule,
    args: [{
      declarations: [NgSelectA11yDirective],
      exports: [NgSelectA11yDirective]
    }]
  }], null, null);
})();
var SortingModule = class _SortingModule {
  static {
    this.ɵfac = function SortingModule_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _SortingModule)();
    };
  }
  static {
    this.ɵmod = ɵɵdefineNgModule({
      type: _SortingModule,
      declarations: [SortingComponent],
      imports: [CommonModule, NgSelectModule, FormsModule, NgSelectA11yModule, I18nModule],
      exports: [SortingComponent]
    });
  }
  static {
    this.ɵinj = ɵɵdefineInjector({
      imports: [CommonModule, NgSelectModule, FormsModule, NgSelectA11yModule, I18nModule]
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(SortingModule, [{
    type: NgModule,
    args: [{
      imports: [CommonModule, NgSelectModule, FormsModule, NgSelectA11yModule, I18nModule],
      declarations: [SortingComponent],
      exports: [SortingComponent]
    }]
  }], null, null);
})();
var defaultPaginationConfig = {
  pagination: {
    addStart: true,
    addEnd: true
  }
};
var PaginationConfig = class _PaginationConfig {
  static {
    this.ɵfac = function PaginationConfig_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _PaginationConfig)();
    };
  }
  static {
    this.ɵprov = ɵɵdefineInjectable({
      token: _PaginationConfig,
      factory: function PaginationConfig_Factory(__ngFactoryType__) {
        let __ngConditionalFactory__ = null;
        if (__ngFactoryType__) {
          __ngConditionalFactory__ = new (__ngFactoryType__ || _PaginationConfig)();
        } else {
          __ngConditionalFactory__ = ɵɵinject(Config);
        }
        return __ngConditionalFactory__;
      },
      providedIn: "root"
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PaginationConfig, [{
    type: Injectable,
    args: [{
      providedIn: "root",
      useExisting: Config
    }]
  }], null, null);
})();
var PaginationItemType;
(function(PaginationItemType2) {
  PaginationItemType2["GAP"] = "gap";
  PaginationItemType2["FIRST"] = "first";
  PaginationItemType2["LAST"] = "last";
  PaginationItemType2["PREVIOUS"] = "previous";
  PaginationItemType2["NEXT"] = "next";
  PaginationItemType2["START"] = "start";
  PaginationItemType2["END"] = "end";
  PaginationItemType2["PAGE"] = "page";
})(PaginationItemType || (PaginationItemType = {}));
var PaginationNavigationPosition;
(function(PaginationNavigationPosition2) {
  PaginationNavigationPosition2["ASIDE"] = "aside";
  PaginationNavigationPosition2["BEFORE"] = "before";
  PaginationNavigationPosition2["AFTER"] = "after";
})(PaginationNavigationPosition || (PaginationNavigationPosition = {}));
var FALLBACK_PAGINATION_OPTIONS = {
  rangeCount: 3,
  dotsLabel: "...",
  startLabel: "«",
  previousLabel: "‹",
  nextLabel: "›",
  endLabel: "»"
};
var PaginationBuilder = class _PaginationBuilder {
  constructor(paginationConfig) {
    this.paginationConfig = paginationConfig;
  }
  /**
   * Builds a list of `PaginationItem`. The give pageCount and current are used
   * to build out the full pagination. There are various {@link PaginationConfig} options
   * which can be used to configure the behavior of the build. Alternatively, CSS
   * can be used to further specialize visibility of the pagination.
   *
   * @param pageCount The total number of pages
   * @param current The current page number, 0-index based
   * @returns An array of `PaginationItem`
   */
  paginate(pageCount, current) {
    const pages = [];
    if (!pageCount || pageCount < 2) {
      return pages;
    }
    this.addPages(pages, pageCount, current);
    this.addDots(pages, pageCount);
    this.addFirstLast(pages, pageCount);
    this.addNavigation(pages, pageCount, current);
    return pages;
  }
  /**
   * Returns the current page with surrounding pages (based on the `config.rangeCount`).
   * The current page is always centered to provide direct access to the previous and next page.
   *
   * @param pages The list of page items that is used to amend
   * @param pageCount The total number of pages
   * @param current The current page number, 0-index based
   */
  addPages(pages, pageCount, current) {
    const start = this.getStartOfRange(pageCount, current);
    if (this.config.rangeCount !== void 0 && start !== null) {
      const max = Math.min(this.config.rangeCount, pageCount);
      Array.from(Array(max)).forEach((_, i) => {
        pages.push({
          number: i + start,
          label: String(i + start + 1),
          type: PaginationItemType.PAGE
        });
      });
    }
  }
  /**
   * Adds dots before and after the given pages, if configured (defaults to true).
   * If the dots only represent a single page, the page number is added instead of
   * the dots, unless the configuration requires dots always.
   *
   * @param pages The list of page items that is used to amend
   * @param pageCount The total number of pages
   */
  addDots(pages, pageCount) {
    if (!this.config.addDots) {
      return;
    }
    pages.unshift(...this.addFirstGapToDots(pages));
    pages.push(...this.addLastGapToDots(pages, pageCount));
  }
  /**
   * Helper method for addDots() whether to add the first gap.
   */
  addFirstGapToDots(pages) {
    const firstItemNumber = pages[0].number;
    const gapNumber = Number(this.config.addFirst);
    if (firstItemNumber !== void 0 && firstItemNumber > gapNumber) {
      const isGap = !this.config.substituteDotsForSingularPage || firstItemNumber !== gapNumber + 1;
      const isSubstituted = this.config.addFirst && this.config.substituteDotsForSingularPage && gapNumber === 0;
      const type = isGap ? PaginationItemType.GAP : isSubstituted ? PaginationItemType.FIRST : PaginationItemType.PAGE;
      return [Object.assign({
        label: isGap ? this.config.dotsLabel : String(gapNumber + 1),
        type
      }, isGap ? null : {
        number: gapNumber
      })];
    } else {
      return [];
    }
  }
  /**
   * Helper method for addDots() whether to add the last gap.
   */
  addLastGapToDots(pages, pageCount) {
    const pageNumber = pages[pages.length - 1].number;
    const nextPageNumber = pageNumber ? pageNumber + 1 : void 0;
    const last = pageCount - (Number(this.config.addLast) + 1);
    if (nextPageNumber && nextPageNumber <= last) {
      const isSubstituted = this.config.addLast && this.config.substituteDotsForSingularPage && nextPageNumber === last;
      const isGap = nextPageNumber < pageCount - Number(this.config.substituteDotsForSingularPage) - Number(this.config.addLast);
      const type = isGap ? PaginationItemType.GAP : isSubstituted ? PaginationItemType.LAST : PaginationItemType.PAGE;
      return [Object.assign({
        label: isGap ? this.config.dotsLabel : String(nextPageNumber + 1),
        type
      }, isGap ? null : {
        number: nextPageNumber
      })];
    } else {
      return [];
    }
  }
  /**
   * Add links to the first and last page, if configured to do so.
   *
   * @param pages The list of page items that is used to amend
   * @param pageCount The total number of pages
   *
   */
  addFirstLast(pages, pageCount) {
    if (this.config.addFirst && pages[0].number !== 0) {
      pages.unshift({
        number: 0,
        label: "1",
        type: PaginationItemType.FIRST
      });
    }
    if (this.config.addLast && pages[pages.length - 1].number !== pageCount - 1) {
      pages.push({
        number: pageCount - 1,
        label: String(pageCount),
        type: PaginationItemType.LAST
      });
    }
  }
  /**
   * Add links to the start, previous, next and last page, if configured to do so.
   * The order of the links can be configured by using the {@link PaginationConfig},
   * using the `PaginationNavigationPosition` (`BEFORE` or `AFTER`).
   * The `PaginationNavigationPosition` allows for 3 flavours:
   *
   * - by default the pagination starts with start and previous and ends with the next and end links
   * - BEFORE – all navigation links are added in the front of the pagination list
   * - AFTER – all navigation links are pushed to the end of the pagination list
   *
   * @param pages The list of page items that is used to amend
   * @param pageCount The total number of pages
   * @param current The current page number, 0-index based
   *
   */
  addNavigation(pages, pageCount, current) {
    const before = this.getBeforeLinks(current);
    const after = this.getAfterLinks(pageCount, current);
    const pos = this.config.navigationPosition;
    if (!pos || pos === PaginationNavigationPosition.ASIDE) {
      pages.unshift(...before);
      pages.push(...after);
    } else {
      if (pos === PaginationNavigationPosition.BEFORE) {
        pages.unshift(...before, ...after);
      }
      if (pos === PaginationNavigationPosition.AFTER) {
        pages.push(...before, ...after);
      }
    }
  }
  /**
   * Returns the start and previous links, if applicable.
   */
  getBeforeLinks(current) {
    const list = [];
    if (this.config.addStart) {
      const start = () => {
        return Object.assign({
          label: this.config.startLabel,
          type: PaginationItemType.START
        }, current > 0 ? {
          number: 0
        } : null);
      };
      list.push(start());
    }
    if (this.config.addPrevious) {
      const previous = () => {
        return Object.assign({
          label: this.config.previousLabel,
          type: PaginationItemType.PREVIOUS
        }, current > 0 ? {
          number: current - 1
        } : null);
      };
      list.push(previous());
    }
    return list;
  }
  /**
   * Returns the next and end links, if applicable.
   */
  getAfterLinks(pageCount, current) {
    const list = [];
    if (this.config.addNext) {
      const next = () => {
        return Object.assign({
          label: this.config.nextLabel,
          type: PaginationItemType.NEXT
        }, current < pageCount - 1 ? {
          number: current + 1
        } : null);
      };
      list.push(next());
    }
    if (this.config.addEnd) {
      const end = () => {
        return Object.assign({
          label: this.config.endLabel,
          type: PaginationItemType.END
        }, current < pageCount - 1 ? {
          number: pageCount - 1
        } : null);
      };
      list.push(end());
    }
    return list;
  }
  /**
   * Resolves the first page of the range we need to build.
   * This is the page that is leading up to the range of the
   * current page.
   *
   * @param pageCount The total number of pages.
   * @param current The current page number, 0-index based.
   */
  getStartOfRange(pageCount, current) {
    if (this.config.rangeCount !== void 0) {
      const count = this.config.rangeCount - 1;
      const delta = Math.round(count / 2);
      const minStart = Math.max(0, current - delta);
      const maxStart = Math.max(0, pageCount - count - 1);
      return Math.min(maxStart, minStart);
    }
    return null;
  }
  /**
   * Returns the pagination configuration. The configuration is driven by the
   * (default) application configuration.
   *
   * The default application is limited to adding the start and end link:
   * ```ts
   *   addStart: true,
   *   addEnd: true
   * ```
   *
   * The application configuration is however merged into the following static configuration:
   * ```ts
   * {
   *   rangeCount: 3,
   *   dotsLabel: '...',
   *   startLabel: '«',
   *   previousLabel: '‹',
   *   nextLabel: '›',
   *   endLabel: '»'
   * }
   * ```
   */
  get config() {
    return Object.assign(FALLBACK_PAGINATION_OPTIONS, this.paginationConfig.pagination);
  }
  static {
    this.ɵfac = function PaginationBuilder_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _PaginationBuilder)(ɵɵinject(PaginationConfig));
    };
  }
  static {
    this.ɵprov = ɵɵdefineInjectable({
      token: _PaginationBuilder,
      factory: _PaginationBuilder.ɵfac,
      providedIn: "root"
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PaginationBuilder, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], () => [{
    type: PaginationConfig
  }], null);
})();
var PaginationComponent = class _PaginationComponent {
  get pagination() {
    return this._pagination;
  }
  set pagination(value) {
    if (value) {
      this._pagination = value;
      this.render(value);
    }
  }
  constructor(paginationBuilder, activatedRoute) {
    this.paginationBuilder = paginationBuilder;
    this.activatedRoute = activatedRoute;
    this.pageRoute = ".";
    this.paginationID = "pagination";
    this.viewPageEvent = new EventEmitter();
    this.pages = [];
    this.translationService = inject(TranslationService);
  }
  render(pagination) {
    if (!pagination) {
      return;
    }
    this.pages = this.paginationBuilder.paginate(pagination.totalPages ?? 0, pagination.currentPage ?? 0);
  }
  /**
   * Format aria-label based on pagination item type.
   *
   * @param item PaginationItem
   * @returns Observable
   */
  getAriaLabel$(item) {
    let type = item.type;
    type = type === PaginationItemType.START ? PaginationItemType.FIRST : type;
    type = type === PaginationItemType.END ? PaginationItemType.LAST : type;
    const initialLabel = type === PaginationItemType.PAGE ? `${type} ${item.label}` : `${type} ${PaginationItemType.PAGE}`;
    if (!this.translationService) {
      return of(initialLabel);
    }
    return combineLatest([this.translationService?.translate("navigation.goTo", {
      location: initialLabel
    }), this.translationService?.translate("common.selected")]).pipe(map(([goToTranslation, selectedTranslation]) => {
      return this.isCurrent(item) ? `${goToTranslation}, ${selectedTranslation}` : goToTranslation;
    }));
  }
  /**
   * Indicates whether the given item is the current item.
   *
   * @param item PaginationItem
   * @returns boolean
   */
  isCurrent(item) {
    return item.type === PaginationItemType.PAGE && item.number === this.pagination.currentPage;
  }
  /**
   * Indicates whether the pagination item is inactive. This is used
   * to disabled a link or set the tabindex to `-1`.
   *
   * Defaults to true
   *
   * @param item PaginationItem
   * @returns returns -1 in case of a disabled
   */
  isInactive(item) {
    return !item.hasOwnProperty("number") || item.number === this.pagination.currentPage;
  }
  getQueryParams(item) {
    const queryParams = Object.assign({}, this.activatedRoute.snapshot.queryParams);
    if (this.queryParam && item.number !== void 0 && this.pagination.totalPages !== void 0 && item.number < this.pagination.totalPages && !this.isCurrent(item)) {
      queryParams[this.queryParam] = item.number;
    }
    if (queryParams[this.queryParam] === this.defaultPage) {
      delete queryParams[this.queryParam];
    }
    return queryParams;
  }
  pageChange(page) {
    this.viewPageEvent.emit(page.number);
  }
  static {
    this.ɵfac = function PaginationComponent_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _PaginationComponent)(ɵɵdirectiveInject(PaginationBuilder), ɵɵdirectiveInject(ActivatedRoute));
    };
  }
  static {
    this.ɵcmp = ɵɵdefineComponent({
      type: _PaginationComponent,
      selectors: [["cx-pagination"]],
      inputs: {
        pageRoute: "pageRoute",
        queryParam: "queryParam",
        defaultPage: "defaultPage",
        pagination: "pagination",
        paginationID: "paginationID"
      },
      outputs: {
        viewPageEvent: "viewPageEvent"
      },
      standalone: false,
      decls: 1,
      vars: 1,
      consts: [[3, "class", "disabled", "current", "routerLink", "queryParams", "tabIndex", "cxFocus", "click", 4, "ngFor", "ngForOf"], [3, "click", "routerLink", "queryParams", "tabIndex", "cxFocus"]],
      template: function PaginationComponent_Template(rf, ctx) {
        if (rf & 1) {
          ɵɵtemplate(0, PaginationComponent_a_0_Template, 4, 21, "a", 0);
        }
        if (rf & 2) {
          ɵɵproperty("ngForOf", ctx.pages);
        }
      },
      dependencies: [NgForOf, RouterLink, FocusDirective, AsyncPipe],
      encapsulation: 2,
      changeDetection: 0
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PaginationComponent, [{
    type: Component,
    args: [{
      selector: "cx-pagination",
      changeDetection: ChangeDetectionStrategy.OnPush,
      standalone: false,
      template: '<a\n  *ngFor="let item of pages; let i = index"\n  [class]="item.type"\n  [class.disabled]="isInactive(item)"\n  [class.current]="isCurrent(item)"\n  [routerLink]="pageRoute"\n  [queryParams]="getQueryParams(item)"\n  [tabIndex]="isInactive(item) ? -1 : 0"\n  [attr.aria-current]="isCurrent(item)"\n  [attr.aria-disabled]="isInactive(item)"\n  (click)="pageChange(item)"\n  [attr.aria-label]="getAriaLabel$(item) | async"\n  [attr.title]="getAriaLabel$(item) | async"\n  [cxFocus]="{ key: paginationID + i.toString() }"\n>\n  {{ item.label }}\n</a>\n'
    }]
  }], () => [{
    type: PaginationBuilder
  }, {
    type: ActivatedRoute
  }], {
    pageRoute: [{
      type: Input
    }],
    queryParam: [{
      type: Input
    }],
    defaultPage: [{
      type: Input
    }],
    pagination: [{
      type: Input
    }],
    paginationID: [{
      type: Input
    }],
    viewPageEvent: [{
      type: Output
    }]
  });
})();
var PaginationModule = class _PaginationModule {
  static {
    this.ɵfac = function PaginationModule_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _PaginationModule)();
    };
  }
  static {
    this.ɵmod = ɵɵdefineNgModule({
      type: _PaginationModule,
      declarations: [PaginationComponent],
      imports: [CommonModule, RouterModule, I18nModule, FeaturesConfigModule, KeyboardFocusModule],
      exports: [PaginationComponent]
    });
  }
  static {
    this.ɵinj = ɵɵdefineInjector({
      providers: [provideDefaultConfig(defaultPaginationConfig)],
      imports: [CommonModule, RouterModule, I18nModule, FeaturesConfigModule, KeyboardFocusModule]
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PaginationModule, [{
    type: NgModule,
    args: [{
      imports: [CommonModule, RouterModule, I18nModule, FeaturesConfigModule, KeyboardFocusModule],
      providers: [provideDefaultConfig(defaultPaginationConfig)],
      declarations: [PaginationComponent],
      exports: [PaginationComponent]
    }]
  }], null, null);
})();
var TotalComponent = class _TotalComponent {
  get pagination() {
    return this._pagination;
  }
  set pagination(paginationModel) {
    this._pagination = paginationModel ?? {
      totalResults: 0
    };
  }
  /**
   * Current page, starting form page 0
   * */
  get currentPage() {
    return this.pagination?.currentPage ?? 0;
  }
  get pageSize() {
    return this.pagination?.pageSize ?? 0;
  }
  get totalResults() {
    return this.pagination?.totalResults ?? 0;
  }
  get fromItem() {
    return this.currentPage * this.pageSize + 1;
  }
  get toItem() {
    const lastItem = (this.currentPage + 1) * this.pageSize;
    return lastItem > this.totalResults ? this.totalResults : lastItem;
  }
  static {
    this.ɵfac = function TotalComponent_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _TotalComponent)();
    };
  }
  static {
    this.ɵcmp = ɵɵdefineComponent({
      type: _TotalComponent,
      selectors: [["cx-total"]],
      inputs: {
        pagination: "pagination"
      },
      standalone: false,
      decls: 1,
      vars: 1,
      consts: [[4, "ngIf"], [1, "cx-vertical-line-separator"]],
      template: function TotalComponent_Template(rf, ctx) {
        if (rf & 1) {
          ɵɵtemplate(0, TotalComponent_ng_container_0_Template, 8, 10, "ng-container", 0);
        }
        if (rf & 2) {
          ɵɵproperty("ngIf", ctx.totalResults > 0);
        }
      },
      dependencies: [NgIf, TranslatePipe],
      encapsulation: 2,
      changeDetection: 0
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(TotalComponent, [{
    type: Component,
    args: [{
      selector: "cx-total",
      changeDetection: ChangeDetectionStrategy.OnPush,
      standalone: false,
      template: `<ng-container *ngIf="totalResults > 0">
  <div>{{ totalResults }} {{ 'common.results' | cxTranslate }}</div>
  <div class="cx-vertical-line-separator"></div>
  <div>
    {{ fromItem }}-{{ toItem }} {{ 'common.of' | cxTranslate }}
    {{ totalResults }}
  </div>
</ng-container>
`
    }]
  }], null, {
    pagination: [{
      type: Input
    }]
  });
})();
var TotalModule = class _TotalModule {
  static {
    this.ɵfac = function TotalModule_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _TotalModule)();
    };
  }
  static {
    this.ɵmod = ɵɵdefineNgModule({
      type: _TotalModule,
      declarations: [TotalComponent],
      imports: [CommonModule, FormsModule, I18nModule],
      exports: [TotalComponent]
    });
  }
  static {
    this.ɵinj = ɵɵdefineInjector({
      imports: [CommonModule, FormsModule, I18nModule]
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(TotalModule, [{
    type: NgModule,
    args: [{
      imports: [CommonModule, FormsModule, I18nModule],
      declarations: [TotalComponent],
      exports: [TotalComponent]
    }]
  }], null, null);
})();
var ListNavigationModule = class _ListNavigationModule {
  static {
    this.ɵfac = function ListNavigationModule_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _ListNavigationModule)();
    };
  }
  static {
    this.ɵmod = ɵɵdefineNgModule({
      type: _ListNavigationModule,
      imports: [CommonModule, NgSelectModule, FormsModule, ReactiveFormsModule, IconModule, PaginationModule, SortingModule, TotalModule],
      exports: [SortingComponent, PaginationComponent, TotalComponent]
    });
  }
  static {
    this.ɵinj = ɵɵdefineInjector({
      imports: [CommonModule, NgSelectModule, FormsModule, ReactiveFormsModule, IconModule, PaginationModule, SortingModule, TotalModule]
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(ListNavigationModule, [{
    type: NgModule,
    args: [{
      imports: [CommonModule, NgSelectModule, FormsModule, ReactiveFormsModule, IconModule, PaginationModule, SortingModule, TotalModule],
      exports: [SortingComponent, PaginationComponent, TotalComponent]
    }]
  }], null, null);
})();
var MediaConfig = class _MediaConfig {
  static {
    this.ɵfac = function MediaConfig_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _MediaConfig)();
    };
  }
  static {
    this.ɵprov = ɵɵdefineInjectable({
      token: _MediaConfig,
      factory: function MediaConfig_Factory(__ngFactoryType__) {
        let __ngConditionalFactory__ = null;
        if (__ngFactoryType__) {
          __ngConditionalFactory__ = new (__ngFactoryType__ || _MediaConfig)();
        } else {
          __ngConditionalFactory__ = ɵɵinject(Config);
        }
        return __ngConditionalFactory__;
      },
      providedIn: "root"
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(MediaConfig, [{
    type: Injectable,
    args: [{
      providedIn: "root",
      useExisting: Config
    }]
  }], null, null);
})();
var ProgressButtonComponent = class _ProgressButtonComponent {
  constructor() {
    this.ariaLabel = "";
    this.class = "";
    this.disabled = false;
    this.loading = false;
    this.clickEvent = new EventEmitter();
  }
  static {
    this.ɵfac = function ProgressButtonComponent_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _ProgressButtonComponent)();
    };
  }
  static {
    this.ɵcmp = ɵɵdefineComponent({
      type: _ProgressButtonComponent,
      selectors: [["cx-progress-button"]],
      inputs: {
        ariaLabel: "ariaLabel",
        class: "class",
        disabled: "disabled",
        loading: "loading"
      },
      outputs: {
        clickEvent: "clickEvent"
      },
      standalone: false,
      ngContentSelectors: _c12,
      decls: 4,
      vars: 4,
      consts: [[1, "btn", "btn-primary", 3, "click", "disabled", "ngClass"], [1, "cx-progress-button-container"], ["class", "loader-container", 4, "ngIf"], [1, "loader-container"], [1, "loader"]],
      template: function ProgressButtonComponent_Template(rf, ctx) {
        if (rf & 1) {
          ɵɵprojectionDef();
          ɵɵelementStart(0, "button", 0);
          ɵɵlistener("click", function ProgressButtonComponent_Template_button_click_0_listener() {
            return ctx.clickEvent.emit();
          });
          ɵɵelementStart(1, "div", 1);
          ɵɵtemplate(2, ProgressButtonComponent_div_2_Template, 4, 3, "div", 2);
          ɵɵprojection(3);
          ɵɵelementEnd()();
        }
        if (rf & 2) {
          ɵɵproperty("disabled", ctx.disabled || ctx.loading)("ngClass", ctx.class);
          ɵɵattribute("aria-label", ctx.ariaLabel);
          ɵɵadvance(2);
          ɵɵproperty("ngIf", ctx.loading);
        }
      },
      dependencies: [NgClass, NgIf, TranslatePipe],
      encapsulation: 2
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(ProgressButtonComponent, [{
    type: Component,
    args: [{
      selector: "cx-progress-button",
      standalone: false,
      template: `<button
  (click)="clickEvent.emit()"
  [attr.aria-label]="ariaLabel"
  [disabled]="disabled || loading"
  [ngClass]="class"
  class="btn btn-primary"
>
  <div class="cx-progress-button-container">
    <div *ngIf="loading" class="loader-container">
      <div class="loader">{{ 'spinner.loading' | cxTranslate }}</div>
    </div>
    <ng-content></ng-content>
  </div>
</button>
`
    }]
  }], () => [], {
    ariaLabel: [{
      type: Input
    }],
    class: [{
      type: Input
    }],
    disabled: [{
      type: Input
    }],
    loading: [{
      type: Input
    }],
    clickEvent: [{
      type: Output
    }]
  });
})();
var ProgressButtonModule = class _ProgressButtonModule {
  static {
    this.ɵfac = function ProgressButtonModule_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _ProgressButtonModule)();
    };
  }
  static {
    this.ɵmod = ɵɵdefineNgModule({
      type: _ProgressButtonModule,
      declarations: [ProgressButtonComponent],
      imports: [CommonModule, I18nModule],
      exports: [ProgressButtonComponent]
    });
  }
  static {
    this.ɵinj = ɵɵdefineInjector({
      imports: [CommonModule, I18nModule]
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(ProgressButtonModule, [{
    type: NgModule,
    args: [{
      imports: [CommonModule, I18nModule],
      declarations: [ProgressButtonComponent],
      exports: [ProgressButtonComponent]
    }]
  }], null, null);
})();
var SplitViewService = class _SplitViewService {
  constructor() {
    this.defaultHideMode = true;
    this.splitViewCount = 1;
    this._views$ = new BehaviorSubject([]);
  }
  /**
   * Adds a view to the list of views. The view is initialized with the `SplitViewState`
   * state. If no state is provided, the state is created with the hidden property. The hidden
   * property is provided by the `defaultHideMode`, unless it's the first view (position: 0).
   */
  add(position, initialState) {
    const state = __spreadValues(__spreadValues({}, {
      hidden: position === 0 ? false : this.defaultHideMode
    }), initialState);
    if (!this.views[position]) {
      this.views[position] = state;
      this.updateState(position, state.hidden);
      this._views$.next(this.views);
    }
  }
  /**
   * The split view is based on a number of views that can be used next to each other.
   * When the number changes (i.e. if the screen goes from wide to small), the visibility state
   * of the views should be updated.
   */
  updateSplitView(splitViewCount) {
    if (splitViewCount !== this.splitViewCount) {
      this.splitViewCount = splitViewCount;
      this.updateState();
    }
  }
  /**
   * Returns an observable with the active view number. The active view number
   * represents the last visible view.
   */
  getActiveView() {
    return this._views$.pipe(map((views) => this.getActive(views)), distinctUntilChanged());
  }
  /**
   * Returns an observable with the SplitViewState for the given view position.
   */
  getViewState(position) {
    return this._views$.pipe(
      map((views) => views[position]),
      // we must filter here, since outlet driven views will destroyed the view
      filter((view) => Boolean(view))
    );
  }
  /**
   * Removes a view from the list of views.
   *
   * Removing a view is different from hiding a view. Removing a view is typically done
   * when a component is destroyed.
   *
   * When the view is removed, the SplitViewState is updated to reflect that new organization
   * of views.
   */
  remove(position) {
    const activePosition = this.getActive(this.views);
    this._views$.next(this.views.splice(0, position));
    if (activePosition >= position) {
      this.updateState(position);
    }
  }
  /**
   * Returns the next view position. This is useful for views that do not want to be bothered
   * with controlling view numbers.
   */
  get nextPosition() {
    return this.views.length || 0;
  }
  /**
   * Toggles the visibility of the views based on the given view position. If the view
   * is already visible, we close the view and active the former view. Unless the hide flag
   * is used, to force the view.
   *
   * The view state of other views in the split view are updated as well.
   *
   * @param position The zero-based position number of the view.
   * @param forceHide The (optional) hide state for the view position.
   */
  toggle(position, forceHide) {
    if (!this.views[position]) {
      this.add(position, {
        hidden: forceHide ?? false
      });
    }
    if (this.views[position] && forceHide === void 0 && !this.views[position].hidden) {
      position--;
    }
    this.updateState(position, forceHide === true);
  }
  /**
   * Updates the hidden state of all the views.
   */
  updateState(position, hide) {
    const views = [...this.views];
    if (hide !== void 0 && position && views[position]) {
      views[position].hidden = hide;
    }
    let lastVisible = views.length - [...views].reverse().findIndex((view) => !view.hidden) - 1;
    if (lastVisible === views.length) {
      if (position) {
        lastVisible = position - 1;
      } else {
        lastVisible = views.length - 1;
      }
    }
    views.forEach((view, pos) => {
      if (view && pos !== position) {
        view.hidden = pos > lastVisible || pos < lastVisible - (this.splitViewCount - 1);
      }
    });
    this._views$.next(views);
  }
  /**
   * Returns the active view count for the list of views.
   */
  getActive(views) {
    const l = [...views].reverse().findIndex((view) => !view.hidden);
    const last = l === -1 ? 0 : views.length - l - 1;
    return last;
  }
  /**
   * Utility method that resolves all views from the subject.
   */
  get views() {
    return this._views$.value;
  }
  static {
    this.ɵfac = function SplitViewService_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _SplitViewService)();
    };
  }
  static {
    this.ɵprov = ɵɵdefineInjectable({
      token: _SplitViewService,
      factory: _SplitViewService.ɵfac
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(SplitViewService, [{
    type: Injectable
  }], null, null);
})();
var SplitViewComponent = class _SplitViewComponent {
  /**
   * Sets the default hide mode for views. This mode is useful in case views are dynamically being created,
   * for example when they are created by router components.
   *
   * The mode defaults to true, unless this is the first view; the first view is never hidden.
   */
  set hideMode(mode) {
    this.splitService.defaultHideMode = mode;
  }
  constructor(splitService, breakpointService, elementRef) {
    this.splitService = splitService;
    this.breakpointService = breakpointService;
    this.elementRef = elementRef;
    this.subscription = new Subscription();
    this.lastVisibleView = 1;
  }
  ngOnInit() {
    this.subscription.add(this.splitService.getActiveView().subscribe((lastVisible) => this.lastVisibleView = lastVisible + 1));
    this.subscription.add(this.breakpointService.breakpoint$.subscribe(() => {
      this.splitService.updateSplitView(this.splitViewCount);
    }));
  }
  /**
   * Returns the maximum number of views per split-view. The number is based on the
   * CSS custom property `--cx-max-views`.
   */
  get splitViewCount() {
    return Number(getComputedStyle(this.elementRef.nativeElement).getPropertyValue("--cx-max-views"));
  }
  ngOnDestroy() {
    this.subscription.unsubscribe();
  }
  static {
    this.ɵfac = function SplitViewComponent_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _SplitViewComponent)(ɵɵdirectiveInject(SplitViewService), ɵɵdirectiveInject(BreakpointService), ɵɵdirectiveInject(ElementRef));
    };
  }
  static {
    this.ɵcmp = ɵɵdefineComponent({
      type: _SplitViewComponent,
      selectors: [["cx-split-view"]],
      hostVars: 3,
      hostBindings: function SplitViewComponent_HostBindings(rf, ctx) {
        if (rf & 2) {
          ɵɵattribute("active-view", ctx.lastVisibleView);
          ɵɵstyleProp("--cx-active-view", ctx.lastVisibleView);
        }
      },
      inputs: {
        hideMode: "hideMode"
      },
      standalone: false,
      features: [ɵɵProvidersFeature([SplitViewService])],
      ngContentSelectors: _c12,
      decls: 1,
      vars: 0,
      template: function SplitViewComponent_Template(rf, ctx) {
        if (rf & 1) {
          ɵɵprojectionDef();
          ɵɵprojection(0);
        }
      },
      encapsulation: 2,
      changeDetection: 0
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(SplitViewComponent, [{
    type: Component,
    args: [{
      selector: "cx-split-view",
      changeDetection: ChangeDetectionStrategy.OnPush,
      providers: [SplitViewService],
      standalone: false,
      template: "<ng-content></ng-content>\n"
    }]
  }], () => [{
    type: SplitViewService
  }, {
    type: BreakpointService
  }, {
    type: ElementRef
  }], {
    hideMode: [{
      type: Input
    }],
    lastVisibleView: [{
      type: HostBinding,
      args: ["style.--cx-active-view"]
    }, {
      type: HostBinding,
      args: ["attr.active-view"]
    }]
  });
})();
var ViewComponent = class _ViewComponent {
  /**
   * The hidden input is used to set the initial visible state of the view.
   * The hidden state defaults to false.
   *
   * The hidden input supports 2-way binding, see `hiddenChange` property.
   */
  set hidden(hidden) {
    this._hidden = hidden;
    this.splitService.toggle(this.viewPosition, hidden);
  }
  constructor(splitService, elementRef, cd) {
    this.splitService = splitService;
    this.elementRef = elementRef;
    this.cd = cd;
    this.disappeared = true;
    this.hiddenChange = new EventEmitter();
    this.globalMessageService = inject(GlobalMessageService);
  }
  ngOnInit() {
    const hidden = this._hidden ? {
      hidden: this._hidden
    } : {};
    this.splitService.add(this.viewPosition, hidden);
    this.subscription = this.splitService.getViewState(this.viewPosition).pipe(delayWhen((view) => timer(view.hidden ? this.duration * 1.25 : 0))).subscribe((view) => {
      this.hiddenChange.emit(view.hidden);
      this._hidden = view.hidden;
      this.disappeared = view.hidden;
      this.cd.markForCheck();
    });
    this.onViewChangeShowAssistiveMessage();
  }
  /**
   * Toggles the visibility of the view.
   *
   * An optional force flag can be used to explicitly show or hide view component.
   */
  toggle(force) {
    this.splitService.toggle(this.viewPosition, force);
  }
  /**
   * Returns the position for the view.
   *
   * The position is either taken from the input `position` or generated by the `SplitService`.
   */
  get viewPosition() {
    if (!(Number(this.position) >= 0)) {
      this.position = this.splitService.nextPosition.toString();
    }
    return Number(this.position);
  }
  /**
   * Returns the duration in milliseconds. The duration is based on the CSS custom property
   * `--cx-transition-duration`. Defaults to 300 milliseconds.
   */
  get duration() {
    const duration = getComputedStyle(this.elementRef.nativeElement).getPropertyValue("--cx-transition-duration").trim();
    if (duration.indexOf("ms") > -1) {
      return Number(duration.split("ms")[0]);
    } else if (duration.indexOf("s") > -1) {
      return Number(duration.split("s")[0]) * 1e3;
    } else {
      return 300;
    }
  }
  onViewChangeShowAssistiveMessage() {
    this.subscription.add(this.splitService.getActiveView().pipe(distinctUntilChanged()).subscribe((activeView) => {
      if (this.viewPosition !== activeView || !this.viewTitle) {
        return;
      }
      this.globalMessageService?.add(this.viewTitle, GlobalMessageType.MSG_TYPE_ASSISTIVE, 500);
    }));
  }
  /**
   * The view is removed from the `SplitService` so that the view no longer
   * plays a role in the overall split view.
   */
  ngOnDestroy() {
    this.splitService.remove(this.viewPosition);
    this.subscription?.unsubscribe();
  }
  static {
    this.ɵfac = function ViewComponent_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _ViewComponent)(ɵɵdirectiveInject(SplitViewService), ɵɵdirectiveInject(ElementRef), ɵɵdirectiveInject(ChangeDetectorRef));
    };
  }
  static {
    this.ɵcmp = ɵɵdefineComponent({
      type: _ViewComponent,
      selectors: [["cx-view"]],
      hostVars: 4,
      hostBindings: function ViewComponent_HostBindings(rf, ctx) {
        if (rf & 2) {
          ɵɵattribute("position", ctx.position)("disappeared", ctx.disappeared);
          ɵɵstyleProp("--cx-view-position", ctx.position);
        }
      },
      inputs: {
        position: "position",
        hidden: "hidden",
        viewTitle: "viewTitle"
      },
      outputs: {
        hiddenChange: "hiddenChange"
      },
      standalone: false,
      ngContentSelectors: _c12,
      decls: 1,
      vars: 0,
      template: function ViewComponent_Template(rf, ctx) {
        if (rf & 1) {
          ɵɵprojectionDef();
          ɵɵprojection(0);
        }
      },
      encapsulation: 2,
      changeDetection: 0
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(ViewComponent, [{
    type: Component,
    args: [{
      selector: "cx-view",
      changeDetection: ChangeDetectionStrategy.OnPush,
      standalone: false,
      template: "<ng-content></ng-content>\n"
    }]
  }], () => [{
    type: SplitViewService
  }, {
    type: ElementRef
  }, {
    type: ChangeDetectorRef
  }], {
    position: [{
      type: Input
    }, {
      type: HostBinding,
      args: ["attr.position"]
    }, {
      type: HostBinding,
      args: ["style.--cx-view-position"]
    }],
    disappeared: [{
      type: HostBinding,
      args: ["attr.disappeared"]
    }],
    hidden: [{
      type: Input
    }],
    hiddenChange: [{
      type: Output
    }],
    viewTitle: [{
      type: Input
    }]
  });
})();
var SplitViewModule = class _SplitViewModule {
  static {
    this.ɵfac = function SplitViewModule_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _SplitViewModule)();
    };
  }
  static {
    this.ɵmod = ɵɵdefineNgModule({
      type: _SplitViewModule,
      declarations: [SplitViewComponent, ViewComponent],
      imports: [CommonModule, RouterModule],
      exports: [SplitViewComponent, ViewComponent]
    });
  }
  static {
    this.ɵinj = ɵɵdefineInjector({
      imports: [CommonModule, RouterModule]
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(SplitViewModule, [{
    type: NgModule,
    args: [{
      declarations: [SplitViewComponent, ViewComponent],
      imports: [CommonModule, RouterModule],
      exports: [SplitViewComponent, ViewComponent]
    }]
  }], null, null);
})();
var PromotionsComponent = class _PromotionsComponent {
  constructor() {
  }
  static {
    this.ɵfac = function PromotionsComponent_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _PromotionsComponent)();
    };
  }
  static {
    this.ɵcmp = ɵɵdefineComponent({
      type: _PromotionsComponent,
      selectors: [["cx-promotions"]],
      inputs: {
        promotions: "promotions"
      },
      standalone: false,
      decls: 1,
      vars: 1,
      consts: [["class", "cx-promotions", 4, "ngIf"], [1, "cx-promotions"], [4, "ngIf"], [4, "ngFor", "ngForOf"]],
      template: function PromotionsComponent_Template(rf, ctx) {
        if (rf & 1) {
          ɵɵtemplate(0, PromotionsComponent_div_0_Template, 3, 2, "div", 0);
        }
        if (rf & 2) {
          ɵɵproperty("ngIf", ctx.promotions);
        }
      },
      dependencies: [NgForOf, NgIf],
      encapsulation: 2,
      changeDetection: 0
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PromotionsComponent, [{
    type: Component,
    args: [{
      selector: "cx-promotions",
      changeDetection: ChangeDetectionStrategy.OnPush,
      standalone: false,
      template: '<div class="cx-promotions" *ngIf="promotions">\n  <ng-container *ngIf="promotions.length === 1">\n    <p>{{ promotions[0].description }}</p>\n  </ng-container>\n  <ng-container *ngIf="promotions.length > 1">\n    <ul>\n      <li *ngFor="let promotion of promotions">\n        {{ promotion.description }}\n      </li>\n    </ul>\n  </ng-container>\n</div>\n'
    }]
  }], () => [], {
    promotions: [{
      type: Input
    }]
  });
})();
var PromotionsModule = class _PromotionsModule {
  static {
    this.ɵfac = function PromotionsModule_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _PromotionsModule)();
    };
  }
  static {
    this.ɵmod = ɵɵdefineNgModule({
      type: _PromotionsModule,
      declarations: [PromotionsComponent],
      imports: [CommonModule],
      exports: [PromotionsComponent]
    });
  }
  static {
    this.ɵinj = ɵɵdefineInjector({
      imports: [CommonModule]
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PromotionsModule, [{
    type: NgModule,
    args: [{
      imports: [CommonModule],
      declarations: [PromotionsComponent],
      exports: [PromotionsComponent]
    }]
  }], null, null);
})();
var SiteContextType;
(function(SiteContextType2) {
  SiteContextType2["LANGUAGE"] = "LANGUAGE";
  SiteContextType2["CURRENCY"] = "CURRENCY";
})(SiteContextType || (SiteContextType = {}));
var LABELS = {
  [LANGUAGE_CONTEXT_ID]: "Language",
  [CURRENCY_CONTEXT_ID]: "Currency"
};
var SiteContextComponentService = class _SiteContextComponentService {
  constructor(componentData, contextServiceMap, injector) {
    this.componentData = componentData;
    this.contextServiceMap = contextServiceMap;
    this.injector = injector;
  }
  getItems(context) {
    return this.getService(context).pipe(switchMap((service) => service.getAll()), switchMap((items) => this.getContext(context).pipe(switchMap((ctx) => {
      const itemsCopy = [];
      for (const item of items) {
        itemsCopy.push(__spreadProps(__spreadValues({}, item), {
          label: this.getOptionLabel(item, ctx)
        }));
      }
      return of(itemsCopy);
    }))));
  }
  getActiveItem(context) {
    return this.getService(context).pipe(switchMap((service) => service.getActive()));
  }
  getLabel(context) {
    return this.getContext(context).pipe(map((ctx) => {
      if (ctx) {
        return LABELS[ctx];
      }
    }));
  }
  setActive(value, context) {
    this.getService(context).pipe(take(1)).subscribe((service) => {
      service.setActive(value);
    });
  }
  getService(context) {
    return this.getContext(context).pipe(map((ctx) => ctx ? this.getInjectedService(ctx) : void 0), filter(isNotUndefined));
  }
  getContext(context) {
    if (context) {
      if (context === SiteContextType.CURRENCY) {
        return of(CURRENCY_CONTEXT_ID);
      } else if (context === SiteContextType.LANGUAGE) {
        return of(LANGUAGE_CONTEXT_ID);
      } else {
        return of(context);
      }
    } else if (this.componentData) {
      return this.componentData.data$.pipe(map((data) => data.context), map((ctx) => {
        switch (ctx) {
          case "LANGUAGE":
            return LANGUAGE_CONTEXT_ID;
          case "CURRENCY":
            return CURRENCY_CONTEXT_ID;
          default:
            return ctx;
        }
      }));
    }
    return of(void 0);
  }
  getInjectedService(context) {
    return this.injector.get(this.contextServiceMap[context], void 0);
  }
  getOptionLabel(item, context) {
    switch (context) {
      case LANGUAGE_CONTEXT_ID:
        return item.nativeName;
      case CURRENCY_CONTEXT_ID:
        return item.symbol + " " + item.isocode;
      default:
        return item.isocode;
    }
  }
  static {
    this.ɵfac = function SiteContextComponentService_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _SiteContextComponentService)(ɵɵinject(CmsComponentData, 8), ɵɵinject(ContextServiceMap), ɵɵinject(Injector));
    };
  }
  static {
    this.ɵprov = ɵɵdefineInjectable({
      token: _SiteContextComponentService,
      factory: _SiteContextComponentService.ɵfac
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(SiteContextComponentService, [{
    type: Injectable
  }], () => [{
    type: CmsComponentData,
    decorators: [{
      type: Optional
    }]
  }, {
    type: ContextServiceMap
  }, {
    type: Injector
  }], null);
})();
var SiteContextSelectorComponent = class _SiteContextSelectorComponent {
  constructor(componentService) {
    this.componentService = componentService;
    this.iconTypes = ICON_TYPE;
    this.translationService = inject(TranslationService);
    useFeatureStyles("a11yShowDownArrowOnFocusedSelectMenu");
  }
  get items$() {
    return this.componentService.getItems(this.context);
  }
  get activeItem$() {
    return this.componentService.getActiveItem(this.context);
  }
  set active(value) {
    this.componentService.setActive(value, this.context);
  }
  get label$() {
    return this.componentService.getLabel(this.context);
  }
  ariaLabel$(label, index, length) {
    return this.translationService.translate("common.of").pipe(map((translation) => {
      return `${label}, ${index + 1} ${translation} ${length}`;
    }));
  }
  static {
    this.ɵfac = function SiteContextSelectorComponent_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _SiteContextSelectorComponent)(ɵɵdirectiveInject(SiteContextComponentService));
    };
  }
  static {
    this.ɵcmp = ɵɵdefineComponent({
      type: _SiteContextSelectorComponent,
      selectors: [["cx-site-context-selector"]],
      inputs: {
        context: "context"
      },
      standalone: false,
      decls: 3,
      vars: 5,
      consts: [[4, "ngIf"], [3, "change"], [3, "value", "selected", 4, "ngFor", "ngForOf"], ["aria-hidden", "true", 1, "small", 3, "type"], [3, "value", "selected"]],
      template: function SiteContextSelectorComponent_Template(rf, ctx) {
        if (rf & 1) {
          ɵɵtemplate(0, SiteContextSelectorComponent_label_0_Template, 7, 5, "label", 0);
          ɵɵpipe(1, "async");
          ɵɵpipe(2, "async");
        }
        if (rf & 2) {
          let tmp_0_0;
          ɵɵproperty("ngIf", ((tmp_0_0 = ɵɵpipeBind1(1, 1, ctx.items$)) == null ? null : tmp_0_0.length) > 1 && ɵɵpipeBind1(2, 3, ctx.items$));
        }
      },
      dependencies: [NgForOf, NgIf, IconComponent, AsyncPipe],
      encapsulation: 2,
      changeDetection: 0
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(SiteContextSelectorComponent, [{
    type: Component,
    args: [{
      selector: "cx-site-context-selector",
      changeDetection: ChangeDetectionStrategy.OnPush,
      standalone: false,
      template: '<label *ngIf="(items$ | async)?.length > 1 && (items$ | async) as items">\n  <span>{{ label$ | async }}:</span>\n  <select (change)="active = $any($event).target.value">\n    <option\n      [attr.aria-label]="ariaLabel$(item.label, i, items.length) | async"\n      *ngFor="let item of items; index as i"\n      value="{{ item.isocode }}"\n      [selected]="(activeItem$ | async) === item.isocode"\n    >\n      {{ item.label }}\n    </option>\n  </select>\n  <cx-icon\n    aria-hidden="true"\n    [type]="iconTypes.CARET_DOWN"\n    class="small"\n  ></cx-icon>\n</label>\n'
    }]
  }], () => [{
    type: SiteContextComponentService
  }], {
    context: [{
      type: Input
    }]
  });
})();
var LanguageCurrencyComponent = class _LanguageCurrencyComponent {
  constructor() {
    this.siteContextType = SiteContextType;
  }
  static {
    this.ɵfac = function LanguageCurrencyComponent_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _LanguageCurrencyComponent)();
    };
  }
  static {
    this.ɵcmp = ɵɵdefineComponent({
      type: _LanguageCurrencyComponent,
      selectors: [["cx-language-currency-selector"]],
      standalone: false,
      decls: 2,
      vars: 2,
      consts: [[3, "context"]],
      template: function LanguageCurrencyComponent_Template(rf, ctx) {
        if (rf & 1) {
          ɵɵelement(0, "cx-site-context-selector", 0)(1, "cx-site-context-selector", 0);
        }
        if (rf & 2) {
          ɵɵproperty("context", ctx.siteContextType.LANGUAGE);
          ɵɵadvance();
          ɵɵproperty("context", ctx.siteContextType.CURRENCY);
        }
      },
      dependencies: [SiteContextSelectorComponent],
      encapsulation: 2,
      changeDetection: 0
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(LanguageCurrencyComponent, [{
    type: Component,
    args: [{
      selector: "cx-language-currency-selector",
      template: `
    <cx-site-context-selector
      [context]="siteContextType.LANGUAGE"
    ></cx-site-context-selector>
    <cx-site-context-selector
      [context]="siteContextType.CURRENCY"
    ></cx-site-context-selector>
  `,
      changeDetection: ChangeDetectionStrategy.OnPush,
      standalone: false
    }]
  }], null, null);
})();
var SiteContextSelectorModule = class _SiteContextSelectorModule {
  static {
    this.ɵfac = function SiteContextSelectorModule_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _SiteContextSelectorModule)();
    };
  }
  static {
    this.ɵmod = ɵɵdefineNgModule({
      type: _SiteContextSelectorModule,
      declarations: [SiteContextSelectorComponent, LanguageCurrencyComponent],
      imports: [CommonModule, RouterModule, SiteContextModule, IconModule],
      exports: [SiteContextSelectorComponent, LanguageCurrencyComponent]
    });
  }
  static {
    this.ɵinj = ɵɵdefineInjector({
      providers: [provideDefaultConfig({
        cmsComponents: {
          CMSSiteContextComponent: {
            component: SiteContextSelectorComponent,
            providers: [{
              provide: SiteContextComponentService,
              useClass: SiteContextComponentService,
              deps: [CmsComponentData, ContextServiceMap, Injector]
            }]
          },
          LanguageCurrencyComponent: {
            component: LanguageCurrencyComponent
          }
        }
      }), SiteContextComponentService],
      imports: [CommonModule, RouterModule, SiteContextModule, IconModule]
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(SiteContextSelectorModule, [{
    type: NgModule,
    args: [{
      imports: [CommonModule, RouterModule, SiteContextModule, IconModule],
      providers: [provideDefaultConfig({
        cmsComponents: {
          CMSSiteContextComponent: {
            component: SiteContextSelectorComponent,
            providers: [{
              provide: SiteContextComponentService,
              useClass: SiteContextComponentService,
              deps: [CmsComponentData, ContextServiceMap, Injector]
            }]
          },
          LanguageCurrencyComponent: {
            component: LanguageCurrencyComponent
          }
        }
      }), SiteContextComponentService],
      declarations: [SiteContextSelectorComponent, LanguageCurrencyComponent],
      exports: [SiteContextSelectorComponent, LanguageCurrencyComponent]
    }]
  }], null, null);
})();
var SiteThemeSwitcherComponentService = class _SiteThemeSwitcherComponentService {
  constructor() {
    this.siteThemeService = inject(SiteThemeService);
  }
  getItems() {
    return this.siteThemeService.getAll();
  }
  getActiveItem() {
    return this.siteThemeService.getActive();
  }
  setActive(value) {
    this.siteThemeService.setActive(value);
  }
  static {
    this.ɵfac = function SiteThemeSwitcherComponentService_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _SiteThemeSwitcherComponentService)();
    };
  }
  static {
    this.ɵprov = ɵɵdefineInjectable({
      token: _SiteThemeSwitcherComponentService,
      factory: _SiteThemeSwitcherComponentService.ɵfac
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(SiteThemeSwitcherComponentService, [{
    type: Injectable
  }], null, null);
})();
var SiteThemeSwitcherComponent = class _SiteThemeSwitcherComponent {
  constructor() {
    this.translationService = inject(TranslationService);
    this.iconTypes = ICON_TYPE;
    this.themeSwitcherComponentService = inject(SiteThemeSwitcherComponentService);
    useFeatureStyles("a11yShowDownArrowOnFocusedSelectMenu");
  }
  get items$() {
    return this.themeSwitcherComponentService.getItems();
  }
  get activeItem$() {
    return this.themeSwitcherComponentService.getActiveItem();
  }
  set activeItem(value) {
    this.themeSwitcherComponentService.setActive(value);
  }
  ariaLabel$(theme, index, length) {
    const themeTranslation$ = theme.i18nNameKey ? this.translationService.translate(theme.i18nNameKey) : of(theme.className);
    const ofTranslation$ = this.translationService.translate("common.of");
    return combineLatest([themeTranslation$, ofTranslation$]).pipe(take(1), map(([themeTranslation, ofTranslation]) => {
      return `${themeTranslation}, ${index + 1} ${ofTranslation} ${length}`;
    }));
  }
  static {
    this.ɵfac = function SiteThemeSwitcherComponent_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _SiteThemeSwitcherComponent)();
    };
  }
  static {
    this.ɵcmp = ɵɵdefineComponent({
      type: _SiteThemeSwitcherComponent,
      selectors: [["cx-site-theme-switcher"]],
      standalone: false,
      decls: 2,
      vars: 3,
      consts: [[4, "ngIf"], [3, "change"], [3, "value", "selected", 4, "ngFor", "ngForOf"], ["aria-hidden", "true", 1, "small", 3, "type"], [3, "value", "selected"]],
      template: function SiteThemeSwitcherComponent_Template(rf, ctx) {
        if (rf & 1) {
          ɵɵtemplate(0, SiteThemeSwitcherComponent_label_0_Template, 7, 5, "label", 0);
          ɵɵpipe(1, "async");
        }
        if (rf & 2) {
          ɵɵproperty("ngIf", ɵɵpipeBind1(1, 1, ctx.items$));
        }
      },
      dependencies: [NgForOf, NgIf, IconComponent, AsyncPipe, TranslatePipe],
      encapsulation: 2,
      changeDetection: 0
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(SiteThemeSwitcherComponent, [{
    type: Component,
    args: [{
      selector: "cx-site-theme-switcher",
      changeDetection: ChangeDetectionStrategy.OnPush,
      standalone: false,
      template: `<label *ngIf="items$ | async as themes">
  <span> {{ 'siteThemeSwitcher.theme' | cxTranslate }}:</span>
  <select (change)="activeItem = $any($event).target.value">
    <option
      [attr.aria-label]="ariaLabel$(theme, i, themes.length) | async"
      *ngFor="let theme of themes; index as i"
      value="{{ theme.className }}"
      [selected]="(activeItem$ | async) === theme.className"
    >
      {{
        theme.i18nNameKey ? (theme.i18nNameKey | cxTranslate) : theme.className
      }}
    </option>
  </select>
  <cx-icon aria-hidden="true" [type]="iconTypes.CARET_DOWN" class="small" />
</label>
`
    }]
  }], () => [], null);
})();
var SiteThemeSwitcherModule = class _SiteThemeSwitcherModule {
  static {
    this.ɵfac = function SiteThemeSwitcherModule_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _SiteThemeSwitcherModule)();
    };
  }
  static {
    this.ɵmod = ɵɵdefineNgModule({
      type: _SiteThemeSwitcherModule,
      declarations: [SiteThemeSwitcherComponent],
      imports: [CommonModule, I18nModule, IconModule, SiteThemeModule],
      exports: [SiteThemeSwitcherComponent]
    });
  }
  static {
    this.ɵinj = ɵɵdefineInjector({
      providers: [provideDefaultConfig({
        cmsComponents: {
          SiteThemeSwitcherComponent: {
            component: SiteThemeSwitcherComponent
          }
        }
      }), SiteThemeSwitcherComponentService],
      imports: [CommonModule, I18nModule, IconModule, SiteThemeModule]
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(SiteThemeSwitcherModule, [{
    type: NgModule,
    args: [{
      imports: [CommonModule, I18nModule, IconModule, SiteThemeModule],
      providers: [provideDefaultConfig({
        cmsComponents: {
          SiteThemeSwitcherComponent: {
            component: SiteThemeSwitcherComponent
          }
        }
      }), SiteThemeSwitcherComponentService],
      declarations: [SiteThemeSwitcherComponent],
      exports: [SiteThemeSwitcherComponent]
    }]
  }], null, null);
})();
var StarRatingComponent = class _StarRatingComponent {
  constructor() {
    this.initialRate = 0;
    this.icon = ICON_TYPE.STAR;
    this.disabled = true;
    this.rating = this.initialRate;
    this.change = new EventEmitter();
    useFeatureStyles("reserveHorizontalSpaceStarRating");
  }
  setRate(value) {
    if (this.disabled) {
      return;
    }
    this.rating = value;
  }
  reset() {
    if (this.disabled) {
      return;
    }
    this.rating = this.initialRate ?? 0;
  }
  saveRate(rating) {
    if (this.disabled) {
      return;
    }
    this.initialRate = rating;
    this.setRate(rating);
    this.change.emit(rating);
  }
  static {
    this.ɵfac = function StarRatingComponent_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _StarRatingComponent)();
    };
  }
  static {
    this.ɵcmp = ɵɵdefineComponent({
      type: _StarRatingComponent,
      selectors: [["cx-star-rating"]],
      hostVars: 3,
      hostBindings: function StarRatingComponent_HostBindings(rf, ctx) {
        if (rf & 1) {
          ɵɵlistener("mouseout", function StarRatingComponent_mouseout_HostBindingHandler() {
            return ctx.reset();
          });
        }
        if (rf & 2) {
          ɵɵattribute("disabled", ctx.disabled);
          ɵɵstyleProp("--star-fill", ctx.rating);
        }
      },
      inputs: {
        disabled: "disabled",
        rating: "rating"
      },
      outputs: {
        change: "change"
      },
      standalone: false,
      decls: 3,
      vars: 2,
      consts: [["starRating", ""], ["role", "img", 3, "title", 4, "ngIf", "ngIfElse"], ["role", "img", 3, "title"], [4, "ngTemplateOutlet"], ["class", "star", 3, "type", "mouseover", "click", "keydown.space", 4, "ngFor", "ngForOf"], [1, "star", 3, "mouseover", "click", "keydown.space", "type"]],
      template: function StarRatingComponent_Template(rf, ctx) {
        if (rf & 1) {
          ɵɵtemplate(0, StarRatingComponent_div_0_Template, 4, 13, "div", 1)(1, StarRatingComponent_ng_template_1_Template, 1, 2, "ng-template", null, 0, ɵɵtemplateRefExtractor);
        }
        if (rf & 2) {
          const starRating_r2 = ɵɵreference(2);
          ɵɵproperty("ngIf", ctx.disabled)("ngIfElse", starRating_r2);
        }
      },
      dependencies: [NgForOf, NgIf, NgTemplateOutlet, IconComponent, TranslatePipe],
      encapsulation: 2,
      changeDetection: 0
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(StarRatingComponent, [{
    type: Component,
    args: [{
      selector: "cx-star-rating",
      changeDetection: ChangeDetectionStrategy.OnPush,
      standalone: false,
      template: `<div
  *ngIf="disabled; else starRating"
  role="img"
  [title]="
    'productReview.ratedOutOf' | cxTranslate: { rating: rating.toFixed(1) }
  "
  [attr.aria-label]="
    'productReview.ratedOutOf' | cxTranslate: { rating: rating.toFixed(1) }
  "
>
  <ng-container *ngTemplateOutlet="starRating"></ng-container>
</div>

<ng-template #starRating>
  <cx-icon
    *ngFor="let i of [1, 2, 3, 4, 5]"
    [type]="icon"
    class="star"
    (mouseover)="setRate(i)"
    (click)="saveRate(i)"
    (keydown.space)="saveRate(i)"
    [attr.aria-current]="!disabled && i === rating"
    [attr.tabindex]="disabled ? null : 0"
    [attr.role]="disabled ? null : 'button'"
    [attr.aria-label]="
      !disabled ? ('productReview.addRate' | cxTranslate: { count: i }) : null
    "
    [attr.aria-hidden]="disabled"
  ></cx-icon>
</ng-template>
`
    }]
  }], () => [], {
    disabled: [{
      type: Input
    }, {
      type: HostBinding,
      args: ["attr.disabled"]
    }],
    rating: [{
      type: Input
    }, {
      type: HostBinding,
      args: ["style.--star-fill"]
    }],
    change: [{
      type: Output
    }],
    reset: [{
      type: HostListener,
      args: ["mouseout"]
    }]
  });
})();
var StarRatingModule = class _StarRatingModule {
  static {
    this.ɵfac = function StarRatingModule_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _StarRatingModule)();
    };
  }
  static {
    this.ɵmod = ɵɵdefineNgModule({
      type: _StarRatingModule,
      declarations: [StarRatingComponent],
      imports: [CommonModule, IconModule, I18nModule],
      exports: [StarRatingComponent]
    });
  }
  static {
    this.ɵinj = ɵɵdefineInjector({
      imports: [CommonModule, IconModule, I18nModule]
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(StarRatingModule, [{
    type: NgModule,
    args: [{
      imports: [CommonModule, IconModule, I18nModule],
      declarations: [StarRatingComponent],
      exports: [StarRatingComponent]
    }]
  }], null, null);
})();
var TableDataCellComponent = class _TableDataCellComponent {
  constructor(outlet) {
    this.outlet = outlet;
  }
  get value() {
    return this.model[this.field];
  }
  get model() {
    return this.outlet?.context;
  }
  get field() {
    return this.outlet?.context?._field;
  }
  static {
    this.ɵfac = function TableDataCellComponent_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _TableDataCellComponent)(ɵɵdirectiveInject(OutletContextData));
    };
  }
  static {
    this.ɵcmp = ɵɵdefineComponent({
      type: _TableDataCellComponent,
      selectors: [["cx-table-data-cell"]],
      hostVars: 1,
      hostBindings: function TableDataCellComponent_HostBindings(rf, ctx) {
        if (rf & 2) {
          ɵɵattribute("title", ctx.value);
        }
      },
      standalone: false,
      decls: 1,
      vars: 1,
      template: function TableDataCellComponent_Template(rf, ctx) {
        if (rf & 1) {
          ɵɵtext(0);
        }
        if (rf & 2) {
          ɵɵtextInterpolate(ctx.value);
        }
      },
      encapsulation: 2,
      changeDetection: 0
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(TableDataCellComponent, [{
    type: Component,
    args: [{
      selector: "cx-table-data-cell",
      template: `{{ value }}`,
      changeDetection: ChangeDetectionStrategy.OnPush,
      standalone: false
    }]
  }], () => [{
    type: OutletContextData
  }], {
    value: [{
      type: HostBinding,
      args: ["attr.title"]
    }]
  });
})();
var TableHeaderCellComponent = class _TableHeaderCellComponent {
  constructor(outlet) {
    this.outlet = outlet;
  }
  /**
   * Returns the static label for the given field, if available.
   */
  get header() {
    if (typeof this.fieldOptions?.label === "string") {
      return this.fieldOptions.label;
    }
  }
  /**
   * Returns the localized label for the given field.
   *
   * The localized label is either driven by the configured `label.i18nKey`
   * or concatenated by the table `type` and field `key`:
   *
   * `[tableType].[fieldKey]`
   *
   * The localized header can be translated with the `cxTranslate` pipe or `TranslationService`.
   */
  get localizedHeader() {
    return this.fieldOptions?.label?.i18nKey || `${this.i18nRoot}.${this.field}`;
  }
  get fieldOptions() {
    return this.outlet?.context._options?.cells?.[this.field];
  }
  get field() {
    return this.outlet?.context?._field;
  }
  get type() {
    return this.outlet?.context?._type;
  }
  get i18nRoot() {
    return this.outlet?.context?._i18nRoot;
  }
  static {
    this.ɵfac = function TableHeaderCellComponent_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _TableHeaderCellComponent)(ɵɵdirectiveInject(OutletContextData));
    };
  }
  static {
    this.ɵcmp = ɵɵdefineComponent({
      type: _TableHeaderCellComponent,
      selectors: [["cx-table-header-cell"]],
      standalone: false,
      decls: 2,
      vars: 3,
      template: function TableHeaderCellComponent_Template(rf, ctx) {
        if (rf & 1) {
          ɵɵtext(0);
          ɵɵpipe(1, "cxTranslate");
        }
        if (rf & 2) {
          ɵɵtextInterpolate(ctx.header || ɵɵpipeBind1(1, 1, ctx.localizedHeader));
        }
      },
      dependencies: [TranslatePipe],
      encapsulation: 2,
      changeDetection: 0
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(TableHeaderCellComponent, [{
    type: Component,
    args: [{
      selector: "cx-table-header-cell",
      template: `{{ header || (localizedHeader | cxTranslate) }}`,
      changeDetection: ChangeDetectionStrategy.OnPush,
      standalone: false
    }]
  }], () => [{
    type: OutletContextData
  }], null);
})();
var defaultTableConfig = {
  tableOptions: {
    headerComponent: TableHeaderCellComponent,
    dataComponent: TableDataCellComponent
  }
};
var TableConfig = class _TableConfig {
  static {
    this.ɵfac = function TableConfig_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _TableConfig)();
    };
  }
  static {
    this.ɵprov = ɵɵdefineInjectable({
      token: _TableConfig,
      factory: function TableConfig_Factory(__ngFactoryType__) {
        let __ngConditionalFactory__ = null;
        if (__ngFactoryType__) {
          __ngConditionalFactory__ = new (__ngFactoryType__ || _TableConfig)();
        } else {
          __ngConditionalFactory__ = ɵɵinject(Config);
        }
        return __ngConditionalFactory__;
      },
      providedIn: "root"
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(TableConfig, [{
    type: Injectable,
    args: [{
      providedIn: "root",
      useExisting: Config
    }]
  }], null, null);
})();
var TableDataCellModule = class _TableDataCellModule {
  static {
    this.ɵfac = function TableDataCellModule_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _TableDataCellModule)();
    };
  }
  static {
    this.ɵmod = ɵɵdefineNgModule({
      type: _TableDataCellModule,
      declarations: [TableDataCellComponent],
      imports: [CommonModule]
    });
  }
  static {
    this.ɵinj = ɵɵdefineInjector({
      imports: [CommonModule]
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(TableDataCellModule, [{
    type: NgModule,
    args: [{
      imports: [CommonModule],
      declarations: [TableDataCellComponent]
    }]
  }], null, null);
})();
var TableHeaderCellModule = class _TableHeaderCellModule {
  static {
    this.ɵfac = function TableHeaderCellModule_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _TableHeaderCellModule)();
    };
  }
  static {
    this.ɵmod = ɵɵdefineNgModule({
      type: _TableHeaderCellModule,
      declarations: [TableHeaderCellComponent],
      imports: [CommonModule, I18nModule]
    });
  }
  static {
    this.ɵinj = ɵɵdefineInjector({
      imports: [CommonModule, I18nModule]
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(TableHeaderCellModule, [{
    type: NgModule,
    args: [{
      imports: [CommonModule, I18nModule],
      declarations: [TableHeaderCellComponent]
    }]
  }], null, null);
})();
var TableRendererService = class _TableRendererService {
  constructor(outletService, componentFactoryResolver, config) {
    this.outletService = outletService;
    this.componentFactoryResolver = componentFactoryResolver;
    this.config = config;
    this.outletRefs = /* @__PURE__ */ new Map();
  }
  /**
   * Adds the configured table component for the header and data.
   */
  add(structure) {
    structure?.cells?.forEach((field) => {
      const thRenderer = this.getHeaderRenderer(structure, field);
      if (thRenderer) {
        const ref = this.getHeaderOutletRef(structure.type, field);
        this.render(ref, thRenderer);
      }
      const tdRenderer = this.getDataRenderer(structure, field);
      if (tdRenderer) {
        const ref = this.getDataOutletRef(structure.type, field);
        this.render(ref, tdRenderer);
      }
    });
  }
  render(outletRef, renderer) {
    if (this.outletRefs.has(outletRef)) {
      return;
    }
    this.outletRefs.set(outletRef, true);
    const template = this.componentFactoryResolver.resolveComponentFactory(renderer);
    this.outletService.add(outletRef, template);
  }
  /**
   * Returns the header render component for the given field.
   */
  getHeaderRenderer(structure, field) {
    return structure.options?.cells?.[field]?.headerComponent || structure.options?.headerComponent || this.config.tableOptions?.headerComponent;
  }
  /**
   * Returns the data render component for the given field.
   */
  getDataRenderer(structure, field) {
    return structure.options?.cells?.[field]?.dataComponent || structure.options?.dataComponent || this.config.tableOptions?.dataComponent;
  }
  /**
   * Returns the header (th) outlet reference for the given field.
   *
   * The outlet reference is generated as:
   * `table.[tableType].header.[field]`
   */
  getHeaderOutletRef(type, field) {
    return `table.${type}.header.${field}`;
  }
  /**
   * Returns the header (th) outlet context for the given field.
   */
  getHeaderOutletContext(type, options, i18nRoot, field) {
    return {
      _type: type,
      _options: options,
      _field: field,
      _i18nRoot: i18nRoot
    };
  }
  /**
   * Returns the data (td) outlet reference for the given field.
   *
   * The field is generated as:
   * `table.[tableType].data.[tableField]`
   */
  getDataOutletRef(type, field) {
    return `table.${type}.data.${field}`;
  }
  /**
   * Returns the data (td) outlet context for the given field.
   */
  getDataOutletContext(type, options, i18nRoot, field, data) {
    return __spreadProps(__spreadValues({}, data), {
      _type: type,
      _options: options,
      _field: field,
      _i18nRoot: i18nRoot
    });
  }
  static {
    this.ɵfac = function TableRendererService_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _TableRendererService)(ɵɵinject(OutletService), ɵɵinject(ComponentFactoryResolver$1), ɵɵinject(TableConfig));
    };
  }
  static {
    this.ɵprov = ɵɵdefineInjectable({
      token: _TableRendererService,
      factory: _TableRendererService.ɵfac,
      providedIn: "root"
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(TableRendererService, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], () => [{
    type: OutletService
  }, {
    type: ComponentFactoryResolver$1
  }, {
    type: TableConfig
  }], null);
})();
var TableLayout;
(function(TableLayout2) {
  TableLayout2[TableLayout2["VERTICAL"] = 1] = "VERTICAL";
  TableLayout2[TableLayout2["VERTICAL_STACKED"] = 2] = "VERTICAL_STACKED";
  TableLayout2[TableLayout2["HORIZONTAL"] = 3] = "HORIZONTAL";
})(TableLayout || (TableLayout = {}));
var TableComponent = class _TableComponent {
  set structure(structure) {
    this._structure = structure;
    this.init();
  }
  get structure() {
    return this._structure;
  }
  constructor(rendererService) {
    this.rendererService = rendererService;
    this.launch = new EventEmitter();
  }
  init() {
    this.verticalLayout = !this.layout || this.layout === TableLayout.VERTICAL;
    this.verticalStackedLayout = this.layout === TableLayout.VERTICAL_STACKED;
    this.horizontalLayout = this.layout === TableLayout.HORIZONTAL;
    this.rendererService.add(this.structure);
    this.addTableDebugInfo();
  }
  launchItem(item) {
    this.launch.emit(item);
  }
  /**
   * Indicates whether the given item is the current item.
   *
   * The current item is driven by the `currentItem`, that holds a
   * property and value to compare.
   */
  isCurrentItem(item) {
    if (!this.currentItem || !this.currentItem.value) {
      return false;
    }
    return this.currentItem?.value === item?.[this.currentItem?.property];
  }
  /**
   * Returns the header (th) outlet reference for the given field.
   */
  getHeaderOutletRef(field) {
    return this.rendererService.getHeaderOutletRef(this.type, field);
  }
  /**
   * Returns the header (th) outlet context for the given field.
   */
  getHeaderOutletContext(field) {
    return this.rendererService.getHeaderOutletContext(this.type, this.options, this.i18nRoot, field);
  }
  /**
   * Returns the data (td) outlet reference for the given field.
   */
  getDataOutletRef(field) {
    return this.rendererService.getDataOutletRef(this.type, field);
  }
  /**
   * Returns the data (td) outlet context for the given field.
   */
  getDataOutletContext(field, data) {
    return this.rendererService.getDataOutletContext(this.type, this.options, this.i18nRoot, field, data);
  }
  trackData(_i, item) {
    return JSON.stringify(item);
  }
  /**
   * Generates the table type into the UI in devMode, so that developers
   * can easily get the notion of the table type.
   */
  addTableDebugInfo() {
    if (isDevMode() && this.type) {
      this.tableType = this.type;
    }
  }
  /**
   * Helper method to return the deeply nested orientation configuration.
   */
  get layout() {
    return this.structure?.options?.layout;
  }
  /**
   * Helper method to return the deeply nested type.
   */
  get type() {
    return this.structure?.type;
  }
  get options() {
    return this.structure?.options;
  }
  static {
    this.ɵfac = function TableComponent_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _TableComponent)(ɵɵdirectiveInject(TableRendererService));
    };
  }
  static {
    this.ɵcmp = ɵɵdefineComponent({
      type: _TableComponent,
      selectors: [["cx-table"]],
      hostVars: 7,
      hostBindings: function TableComponent_HostBindings(rf, ctx) {
        if (rf & 2) {
          ɵɵattribute("__cx-table-type", ctx.tableType);
          ɵɵclassProp("horizontal", ctx.horizontalLayout)("vertical", ctx.verticalLayout)("vertical-stacked", ctx.verticalStackedLayout);
        }
      },
      inputs: {
        structure: "structure",
        data: "data",
        i18nRoot: "i18nRoot",
        currentItem: "currentItem"
      },
      outputs: {
        launch: "launch"
      },
      standalone: false,
      decls: 1,
      vars: 1,
      consts: [[4, "ngIf"], [3, "is-current", "click", 4, "ngFor", "ngForOf", "ngForTrackBy"], [3, "click"], [3, "class", 4, "ngFor", "ngForOf"], [3, "cxOutlet", "cxOutletContext"], ["scope", "col", 3, "class", 4, "ngFor", "ngForOf"], ["scope", "col"]],
      template: function TableComponent_Template(rf, ctx) {
        if (rf & 1) {
          ɵɵtemplate(0, TableComponent_table_0_Template, 4, 3, "table", 0);
        }
        if (rf & 2) {
          ɵɵproperty("ngIf", ctx.structure);
        }
      },
      dependencies: [NgForOf, NgIf, OutletDirective],
      encapsulation: 2,
      changeDetection: 0
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(TableComponent, [{
    type: Component,
    args: [{
      selector: "cx-table",
      changeDetection: ChangeDetectionStrategy.OnPush,
      standalone: false,
      template: '<table *ngIf="structure">\n  <ng-container *ngIf="verticalStackedLayout">\n    <tbody\n      *ngFor="let item of data; trackBy: trackData"\n      (click)="launchItem(item)"\n      [class.is-current]="isCurrentItem(item)"\n    >\n      <tr *ngFor="let cell of structure.cells" [class]="cell">\n        <th>\n          <ng-template\n            [cxOutlet]="getHeaderOutletRef(cell)"\n            [cxOutletContext]="getHeaderOutletContext(cell)"\n          >\n            {{ cell }}\n          </ng-template>\n        </th>\n        <td>\n          <ng-template\n            [cxOutlet]="getDataOutletRef(cell)"\n            [cxOutletContext]="getDataOutletContext(cell, item)"\n          >\n            {{ $any(item)[cell] }}\n          </ng-template>\n        </td>\n      </tr>\n    </tbody>\n  </ng-container>\n\n  <!-- vertical tables render the item  -->\n  <ng-container *ngIf="verticalLayout">\n    <thead>\n      <tr>\n        <th scope="col" *ngFor="let cell of structure.cells" [class]="cell">\n          <ng-template\n            [cxOutlet]="getHeaderOutletRef(cell)"\n            [cxOutletContext]="getHeaderOutletContext(cell)"\n          >\n            {{ cell }}\n          </ng-template>\n        </th>\n      </tr>\n    </thead>\n\n    <tr\n      *ngFor="let item of data; trackBy: trackData"\n      [class.is-current]="isCurrentItem(item)"\n      (click)="launchItem(item)"\n    >\n      <td *ngFor="let cell of structure.cells; let i = index" [class]="cell">\n        <ng-template\n          [cxOutlet]="getDataOutletRef(cell)"\n          [cxOutletContext]="getDataOutletContext(cell, item)"\n        >\n          {{ $any(item)[cell] }}\n        </ng-template>\n      </td>\n    </tr>\n  </ng-container>\n\n  <ng-container *ngIf="horizontalLayout">\n    <tr *ngFor="let cell of structure.cells" [class]="cell">\n      <th scope="col">\n        <ng-template\n          [cxOutlet]="getHeaderOutletRef(cell)"\n          [cxOutletContext]="getHeaderOutletContext(cell)"\n        >\n          {{ cell }}\n        </ng-template>\n      </th>\n      <td\n        *ngFor="let item of data; trackBy: trackData"\n        [class.is-current]="isCurrentItem(item)"\n        (click)="launchItem(item)"\n      >\n        <ng-template\n          [cxOutlet]="getDataOutletRef(cell)"\n          [cxOutletContext]="getDataOutletContext(cell, item)"\n        >\n          {{ $any(item)[cell] }}\n        </ng-template>\n      </td>\n    </tr>\n  </ng-container>\n</table>\n'
    }]
  }], () => [{
    type: TableRendererService
  }], {
    tableType: [{
      type: HostBinding,
      args: ["attr.__cx-table-type"]
    }],
    horizontalLayout: [{
      type: HostBinding,
      args: ["class.horizontal"]
    }],
    verticalLayout: [{
      type: HostBinding,
      args: ["class.vertical"]
    }],
    verticalStackedLayout: [{
      type: HostBinding,
      args: ["class.vertical-stacked"]
    }],
    structure: [{
      type: Input
    }],
    data: [{
      type: Input
    }],
    i18nRoot: [{
      type: Input
    }],
    currentItem: [{
      type: Input
    }],
    launch: [{
      type: Output
    }]
  });
})();
var TableModule = class _TableModule {
  static {
    this.ɵfac = function TableModule_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _TableModule)();
    };
  }
  static {
    this.ɵmod = ɵɵdefineNgModule({
      type: _TableModule,
      declarations: [TableComponent],
      imports: [CommonModule, OutletModule, TableHeaderCellModule, TableDataCellModule],
      exports: [TableComponent]
    });
  }
  static {
    this.ɵinj = ɵɵdefineInjector({
      providers: [provideDefaultConfig(defaultTableConfig)],
      imports: [CommonModule, OutletModule, TableHeaderCellModule, TableDataCellModule]
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(TableModule, [{
    type: NgModule,
    args: [{
      imports: [CommonModule, OutletModule, TableHeaderCellModule, TableDataCellModule],
      declarations: [TableComponent],
      exports: [TableComponent],
      providers: [provideDefaultConfig(defaultTableConfig)]
    }]
  }], null, null);
})();
var TableService = class _TableService {
  constructor(breakpointService, config) {
    this.breakpointService = breakpointService;
    this.config = config;
    this.logger = inject(LoggerService);
  }
  /**
   * Builds the table structure.
   *
   * @param tableType The table type is used  to find the specific table configuration.
   * @param defaultStructure (optional) Default table structure that contains fallback options. More specific options are merged with the default structure.
   * @param data$ (optional) The actual data can be passed in to generate the table structure based on actual data.
   */
  buildStructure(tableType, defaultStructure) {
    if (this.hasTableConfig(tableType)) {
      return this.buildStructureFromConfig(tableType, defaultStructure);
    } else {
      return this.buildRandomStructure(tableType);
    }
  }
  /**
   * Returns the table structure by configuration. The configuration can be
   * breakpoint-driven, which means that an alternative header structure can
   * be created per screen size.
   *
   * The breakpoint is resolved by teh `BreakpointService`.
   */
  buildStructureFromConfig(type, defaultStructure) {
    return this.breakpointService.breakpoint$.pipe(map((breakpoint) => __spreadProps(__spreadValues({}, this.getTableConfig(type, breakpoint, defaultStructure)), {
      type
    })));
  }
  /**
   * Finds all applicable table configuration for the given type and breakpoint.
   * The default table configuration is merged with all relevant breakpoint
   * configurations.
   *
   * This allows to have some default configurations that apply to all screens, and
   * add configuration options for some screens.
   */
  getTableConfig(type, breakpoint, defaultStructure) {
    if (!this.config.table?.[type]) {
      return null;
    }
    const relevant = this.findRelevantBreakpoints(breakpoint);
    const closestBreakpoint = [...relevant].reverse().find((br) => !!this.config.table?.[type][br]?.cells);
    const cells = closestBreakpoint && this.config.table[type][closestBreakpoint]?.cells || this.config.table[type].cells || defaultStructure?.cells;
    let options = __spreadValues(__spreadValues({}, defaultStructure?.options), this.config.table[type].options);
    relevant.forEach((br) => {
      options = __spreadValues(__spreadValues(__spreadValues({}, options), defaultStructure?.[br]?.options), this.config.table?.[type]?.[br]?.options);
    });
    return {
      cells,
      options
    };
  }
  /**
   * Generates the table structure by the help of the first data row.
   */
  buildStructureFromData(type, data$) {
    this.logWarning(`No table configuration found to render table with type "${type}". The table header for "${type}" is generated by the help of the first data item`);
    return data$.pipe(map((data) => {
      const cells = Object.keys(data?.[0]).map((key) => key);
      return {
        type,
        cells
      };
    }));
  }
  /**
   * As a last resort, the table structure is randomly created. The random structure
   * contains 5 headers, so that some of the unknown data is visualized.
   */
  buildRandomStructure(type) {
    this.logWarning(`No data available for "${type}", a random structure is generated (with hidden table headers).`);
    return of({
      type,
      cells: ["unknown", "unknown", "unknown", "unknown", "unknown"]
    });
  }
  /**
   * Finds all the breakpoints can contribute to the table configuration, from small
   * to current.
   *
   * For example, if the current breakpoint is `MD`, this returns `[XS, SM, MD]`.
   */
  findRelevantBreakpoints(breakpoint) {
    const current = this.breakpointService.breakpoints.indexOf(breakpoint);
    return this.breakpointService.breakpoints.slice(0, current + 1);
  }
  /**
   * Indicates if the there is a configuration for the table available.
   */
  hasTableConfig(tableType) {
    return !!this.config.table?.[tableType];
  }
  /**
   * Logs a message in the console to increase developer experience.
   *
   * The message is only logged in dev mode.
   */
  logWarning(message) {
    if (isDevMode()) {
      this.logger.warn(message);
    }
  }
  static {
    this.ɵfac = function TableService_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _TableService)(ɵɵinject(BreakpointService), ɵɵinject(TableConfig));
    };
  }
  static {
    this.ɵprov = ɵɵdefineInjectable({
      token: _TableService,
      factory: _TableService.ɵfac,
      providedIn: "root"
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(TableService, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], () => [{
    type: BreakpointService
  }, {
    type: TableConfig
  }], null);
})();
var CaptchaApiConfig = class _CaptchaApiConfig {
  static {
    this.ɵfac = function CaptchaApiConfig_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _CaptchaApiConfig)();
    };
  }
  static {
    this.ɵprov = ɵɵdefineInjectable({
      token: _CaptchaApiConfig,
      factory: function CaptchaApiConfig_Factory(__ngFactoryType__) {
        let __ngConditionalFactory__ = null;
        if (__ngFactoryType__) {
          __ngConditionalFactory__ = new (__ngFactoryType__ || _CaptchaApiConfig)();
        } else {
          __ngConditionalFactory__ = ɵɵinject(Config);
        }
        return __ngConditionalFactory__;
      },
      providedIn: "root"
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(CaptchaApiConfig, [{
    type: Injectable,
    args: [{
      providedIn: "root",
      useExisting: Config
    }]
  }], null, null);
})();
var CaptchaComponent = class _CaptchaComponent {
  constructor(config, injector) {
    this.config = config;
    this.injector = injector;
    this.confirmed = new EventEmitter();
    this.subscription = new Subscription();
  }
  /**
   * Add fields from CaptchaApiConfig. Call backend to get captcha
   * config.
   */
  ngAfterViewInit() {
    if (this.config?.captchaRenderer) {
      const captchaRenderer = this.injector.get(this.config.captchaRenderer);
      this.subscription.add(captchaRenderer.getCaptchaConfig().pipe(concatMap((captchaConfig) => {
        if (captchaConfig?.enabled) {
          return captchaRenderer.renderCaptcha({
            element: this.captchaRef.nativeElement
          });
        } else {
          return of(null);
        }
      })).subscribe(() => {
        this.confirmed.emit(true);
      }));
    }
  }
  ngOnDestroy() {
    this.subscription.unsubscribe();
  }
  static {
    this.ɵfac = function CaptchaComponent_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _CaptchaComponent)(ɵɵdirectiveInject(CaptchaApiConfig), ɵɵdirectiveInject(Injector));
    };
  }
  static {
    this.ɵcmp = ɵɵdefineComponent({
      type: _CaptchaComponent,
      selectors: [["cx-captcha"]],
      viewQuery: function CaptchaComponent_Query(rf, ctx) {
        if (rf & 1) {
          ɵɵviewQuery(_c33, 5);
        }
        if (rf & 2) {
          let _t;
          ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.captchaRef = _t.first);
        }
      },
      outputs: {
        confirmed: "confirmed"
      },
      standalone: false,
      decls: 2,
      vars: 0,
      consts: [["captcha", ""]],
      template: function CaptchaComponent_Template(rf, ctx) {
        if (rf & 1) {
          ɵɵelement(0, "div", null, 0);
        }
      },
      encapsulation: 2,
      changeDetection: 0
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(CaptchaComponent, [{
    type: Component,
    args: [{
      selector: "cx-captcha",
      changeDetection: ChangeDetectionStrategy.OnPush,
      standalone: false,
      template: "<div #captcha></div>\n"
    }]
  }], () => [{
    type: CaptchaApiConfig
  }, {
    type: Injector
  }], {
    confirmed: [{
      type: Output
    }],
    captchaRef: [{
      type: ViewChild,
      args: ["captcha", {
        static: false
      }]
    }]
  });
})();
var CaptchaService = class _CaptchaService {
  constructor(adapter, apiConfig, languageService, scriptLoader, baseSiteService) {
    this.adapter = adapter;
    this.apiConfig = apiConfig;
    this.languageService = languageService;
    this.scriptLoader = scriptLoader;
    this.baseSiteService = baseSiteService;
    this.subscription = new Subscription();
    this.captchaConfigSubject$ = new ReplaySubject(1);
    this.initialize();
  }
  /**
   * Retrieve captcha configuration from the backend, and if enabled
   * call @function loadScript with active language
   */
  initialize() {
    this.subscription.add(this.fetchCaptchaConfigFromServer().subscribe((config) => {
      if (config?.enabled) {
        this.captchaConfig = config;
        this.loadResource();
      } else {
        this.captchaConfigSubject$.next({
          enabled: false
        });
      }
    }));
  }
  fetchCaptchaConfigFromServer() {
    return forkJoin([this.languageService.getActive().pipe(take(1)), this.baseSiteService.getActive().pipe(concatMap((value) => this.adapter.loadBaseSite(value)), take(1))]).pipe(map((result) => result[1]?.captchaConfig));
  }
  getCaptchaConfig() {
    return this.captchaConfigSubject$.asObservable();
  }
  ngOnDestroy() {
    this.subscription.unsubscribe();
  }
  getToken() {
    return this.token;
  }
  /**
   * Load external resource if needed with dependencies to be added to <head>.
   * @param - Language and configuration read from server
   */
  loadResource() {
    this.captchaConfigSubject$.next(this.captchaConfig);
  }
  static {
    this.ɵfac = function CaptchaService_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _CaptchaService)(ɵɵinject(SiteAdapter), ɵɵinject(CaptchaApiConfig), ɵɵinject(LanguageService), ɵɵinject(ScriptLoader), ɵɵinject(BaseSiteService));
    };
  }
  static {
    this.ɵprov = ɵɵdefineInjectable({
      token: _CaptchaService,
      factory: _CaptchaService.ɵfac,
      providedIn: "root"
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(CaptchaService, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], () => [{
    type: SiteAdapter
  }, {
    type: CaptchaApiConfig
  }, {
    type: LanguageService
  }, {
    type: ScriptLoader
  }, {
    type: BaseSiteService
  }], null);
})();
var MockCaptchaService = class _MockCaptchaService extends CaptchaService {
  initialize() {
    super.initialize();
    this.container = document.createElement("div");
    this.container.className = "form-check";
    this.checkbox = document.createElement("input");
    this.checkbox.type = "checkbox";
    this.checkbox.className = "mock-captcha";
    this.label = document.createElement("label");
    this.label.textContent = "I'm not a robot";
    this.container.appendChild(this.checkbox);
    this.container.appendChild(this.label);
    this.spinner = document.createElement("icon");
    this.spinner.className = "fa-solid fa-spinner";
    this.checkbox.addEventListener("change", this.onCheckBoxClicked.bind(this));
  }
  onCheckBoxClicked() {
    this.label.textContent = "";
    this.container.appendChild(this.spinner);
    this.checkbox.disabled = true;
    this.checkbox.checked = true;
    setTimeout(() => {
      this.container.removeChild(this.spinner);
      this.retVal.next("succeed");
      this.retVal.complete();
      this.token = "myToken";
      this.label.textContent = "Verified";
    }, 500);
  }
  /**
   * Add element to page.
   * @param {HTMLElement} element - HTML element to render captcha widget within.
   */
  renderCaptcha(renderParams) {
    if (renderParams.element instanceof HTMLElement) {
      this.checkbox.disabled = false;
      this.checkbox.checked = false;
      this.label.textContent = "I'm not a robot";
      this.token = "";
      this.retVal = new Subject();
      renderParams.element.appendChild(this.container);
    }
    return this.retVal.asObservable();
  }
  static {
    this.ɵfac = /* @__PURE__ */ (() => {
      let ɵMockCaptchaService_BaseFactory;
      return function MockCaptchaService_Factory(__ngFactoryType__) {
        return (ɵMockCaptchaService_BaseFactory || (ɵMockCaptchaService_BaseFactory = ɵɵgetInheritedFactory(_MockCaptchaService)))(__ngFactoryType__ || _MockCaptchaService);
      };
    })();
  }
  static {
    this.ɵprov = ɵɵdefineInjectable({
      token: _MockCaptchaService,
      factory: _MockCaptchaService.ɵfac,
      providedIn: "root"
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(MockCaptchaService, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], null, null);
})();
var MockCaptchaApiConfig = {
  captchaRenderer: MockCaptchaService
};
var CaptchaModule = class _CaptchaModule {
  static {
    this.ɵfac = function CaptchaModule_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _CaptchaModule)();
    };
  }
  static {
    this.ɵmod = ɵɵdefineNgModule({
      type: _CaptchaModule,
      declarations: [CaptchaComponent],
      imports: [CommonModule, ReactiveFormsModule, FormErrorsModule],
      exports: [CaptchaComponent]
    });
  }
  static {
    this.ɵinj = ɵɵdefineInjector({
      providers: [provideDefaultConfig(MockCaptchaApiConfig)],
      imports: [CommonModule, ReactiveFormsModule, FormErrorsModule]
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(CaptchaModule, [{
    type: NgModule,
    args: [{
      imports: [CommonModule, ReactiveFormsModule, FormErrorsModule],
      declarations: [CaptchaComponent],
      providers: [provideDefaultConfig(MockCaptchaApiConfig)],
      exports: [CaptchaComponent]
    }]
  }], null, null);
})();
var FilesFormValidators = class _FilesFormValidators {
  constructor() {
    this.CONVERT_TO_MB = 1e6;
    this.extenstionRegEx = /\.([0-9a-z]+)(?:[\?#]|$)/i;
  }
  /**
   * Checks max size of file
   *
   * @param {number} maxSize Max size [MB]
   * @returns Uses 'tooLarge' validator error with maxSize property
   * @memberOf FilesFormValidators
   */
  maxSize(maxSize) {
    return (control) => {
      const errors = {};
      if (maxSize && control.value) {
        const files = Array.from(control.value);
        files.forEach(({
          size,
          name
        }) => {
          if (size > maxSize * this.CONVERT_TO_MB) {
            const invalidFiles = errors.tooLarge?.invalidFiles ?? [];
            errors.tooLarge = {
              maxSize,
              invalidFiles: [...invalidFiles, name]
            };
          }
        });
      }
      return Object.keys(errors).length === 0 ? null : errors;
    };
  }
  /**
   * Checks maximum entries
   *
   * @param {number} maxEntries Max number of entries
   * @returns Uses 'tooManyEntries' validator error with maxEntries property
   * @memberOf FilesFormValidators
   */
  maxEntries(maxEntries) {
    return (control) => {
      const errors = {};
      if (maxEntries && control.value) {
        const files = Array.from(control.value);
        if (files.length > maxEntries) {
          errors.tooManyEntries = {
            maxEntries
          };
        }
      }
      return Object.keys(errors).length === 0 ? null : errors;
    };
  }
  /**
   * Checks allowed types
   *
   * @param {Array<string>} allowedTypes Allowed types of files
   * @returns Uses 'notParsable' validator error with allowedTypes property
   * @memberOf FilesFormValidators
   */
  allowedTypes(allowedTypes) {
    return (control) => {
      const errors = {};
      if (allowedTypes && control.value) {
        const files = Array.from(control.value);
        errors.fileNotAllowed = files.some(({
          name
        }) => !allowedTypes.includes(this.getExtension(name)));
      }
      return errors[Object.keys(errors)?.[0]] ? errors : null;
    };
  }
  getExtension(filename) {
    return (filename?.match(this.extenstionRegEx) || [])[0] || "";
  }
  static {
    this.ɵfac = function FilesFormValidators_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _FilesFormValidators)();
    };
  }
  static {
    this.ɵprov = ɵɵdefineInjectable({
      token: _FilesFormValidators,
      factory: _FilesFormValidators.ɵfac,
      providedIn: "root"
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(FilesFormValidators, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], null, null);
})();
var AvatarComponent = class _AvatarComponent {
  constructor() {
    this.iconTypes = ICON_TYPE;
  }
  getInitials(author) {
    return author.split(" ").map((string) => string[0]).join("");
  }
  static {
    this.ɵfac = function AvatarComponent_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _AvatarComponent)();
    };
  }
  static {
    this.ɵcmp = ɵɵdefineComponent({
      type: _AvatarComponent,
      selectors: [["cx-avatar"]],
      inputs: {
        message: "message"
      },
      standalone: false,
      decls: 1,
      vars: 1,
      consts: [["class", "cx-avatar", 3, "ngClass", 4, "ngIf"], [1, "cx-avatar", 3, "ngClass"], [4, "ngIf"], [3, "type", 4, "ngIf"], [3, "type"]],
      template: function AvatarComponent_Template(rf, ctx) {
        if (rf & 1) {
          ɵɵtemplate(0, AvatarComponent_div_0_Template, 4, 6, "div", 0);
        }
        if (rf & 2) {
          ɵɵproperty("ngIf", ctx.message);
        }
      },
      dependencies: [NgClass, NgIf, IconComponent],
      encapsulation: 2
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(AvatarComponent, [{
    type: Component,
    args: [{
      selector: "cx-avatar",
      standalone: false,
      template: `<div
  *ngIf="message"
  class="cx-avatar"
  [ngClass]="{ 'right-align': message.rightAlign }"
>
  <span *ngIf="!message.rightAlign && message?.author">
    {{ getInitials(message?.author || '') }}
  </span>

  <cx-icon
    *ngIf="!message.rightAlign && !message?.author"
    [type]="iconTypes.USER"
  ></cx-icon>

  <cx-icon *ngIf="message.rightAlign" [type]="iconTypes.HEADSET"></cx-icon>
</div>
`
    }]
  }], null, {
    message: [{
      type: Input
    }]
  });
})();
var MessagingComponent = class _MessagingComponent {
  get inputCharacterLeft() {
    return (this.messagingConfigs?.charactersLimit || this.MAX_INPUT_CHARACTERS) - (this.form.get("message")?.value?.length || 0);
  }
  get maxSize() {
    return this.messagingConfigs?.attachmentRestrictions?.maxSize || this.MAX_SIZE;
  }
  get maxEntries() {
    return this.messagingConfigs?.attachmentRestrictions?.maxEntries || this.MAX_ENTRIES;
  }
  get allowedTypes() {
    return this.messagingConfigs?.attachmentRestrictions?.allowedTypes || [];
  }
  constructor(windowRef, filesFormValidators) {
    this.windowRef = windowRef;
    this.filesFormValidators = filesFormValidators;
    this.scrollToInput = true;
    this.send = new EventEmitter();
    this.itemClicked = new EventEmitter();
    this.downloadAttachment = new EventEmitter();
    this.iconTypes = ICON_TYPE;
    this.MAX_INPUT_CHARACTERS = 2e3;
    this.MAX_SIZE = 10;
    this.MAX_ENTRIES = 1;
    this.dateFormat = "MMMM d, YYYY h:mm aa";
    this.scrollOnceOnLoad = true;
    useFeatureStyles("a11yCroppedFocusRing");
  }
  ngOnInit() {
    this.buildForm();
  }
  ngAfterViewChecked() {
    if (this.scrollToInput) {
      this.observeScroll();
      if (this.scrollOnceOnLoad) {
        this.scrollOnLoad();
      }
    }
  }
  onSend() {
    if (this.form.valid) {
      const event = {
        files: this.form.get("file")?.value,
        message: this.form.get("message")?.value
      };
      const itemId = this.form.get("item")?.value;
      if (itemId) {
        event.itemId = itemId;
      }
      this.send.emit(event);
    }
  }
  resetForm() {
    this.form.reset({
      item: this.messagingConfigs?.defaultItemId
    });
    this.fileUploadComponent?.removeFile();
  }
  triggerDownload(messageCode, attachmentId, fileName) {
    this.downloadAttachment.emit({
      messageCode,
      attachmentId,
      fileName
    });
  }
  buildForm() {
    const form = new UntypedFormGroup({});
    form.setControl("message", new UntypedFormControl("", [Validators.required, Validators.maxLength(this.messagingConfigs?.charactersLimit || this.MAX_INPUT_CHARACTERS)]));
    form.setControl("file", new UntypedFormControl("", [this.filesFormValidators.maxSize(this.maxSize), this.filesFormValidators.maxEntries(this.maxEntries), this.filesFormValidators.allowedTypes(this.allowedTypes)]));
    form.setControl("item", new UntypedFormControl(this.messagingConfigs?.defaultItemId));
    this.form = form;
  }
  focusNextChild(event) {
    event.preventDefault();
    const [results, focusedIndex] = [this.getResultElements(), this.getFocusedIndex()];
    if (results.length) {
      if (focusedIndex >= results.length - 1) {
        results[0].focus();
      } else {
        results[focusedIndex + 1].focus();
      }
    }
  }
  focusPreviousChild(event) {
    if (!this.windowRef.isBrowser()) {
      return;
    }
    event.preventDefault();
    const [results, focusedIndex] = [this.getResultElements(), this.getFocusedIndex()];
    if (results.length) {
      if (focusedIndex < 1) {
        results[results.length - 1].focus();
      } else {
        results[focusedIndex - 1].focus();
      }
    }
  }
  /**
   * A message can be linked to an item. In this case the item name will be prefixed to the message text.
   *
   * @param {MessageEvent} message message
   * @returns {string | undefined} message text prefixed with item name, in case its given.
   */
  getMessageText(message) {
    return message.item ? message.item.name + ": " + message.text : message.text;
  }
  onItemClicked(item) {
    this.itemClicked.emit({
      item
    });
  }
  observeScroll() {
    const element = this.windowRef.document.querySelector(".cx-messages");
    if (element) {
      const resizeObserver = new ResizeObserver((entries) => {
        this.scrollToBottom(element, entries[0].target.scrollHeight);
        this.updatedScrollHeight = entries[0].target.scrollHeight;
      });
      resizeObserver.observe(element);
    }
  }
  scrollToBottom(element, previousScrollHeight) {
    if (this.heightChanged(previousScrollHeight)) {
      element?.scroll({
        top: element?.scrollHeight,
        behavior: "auto"
      });
    }
  }
  heightChanged(previousScrollHeight) {
    return this.updatedScrollHeight !== previousScrollHeight;
  }
  scrollOnLoad() {
    const element = this.windowRef.document.getElementById("cx-message-footer");
    const resizeObserver = new ResizeObserver(() => {
      element?.scrollIntoView({
        behavior: "auto",
        block: "end"
      });
      this.scrollOnceOnLoad = false;
    });
    if (element) {
      resizeObserver.observe(element);
    }
  }
  getResultElements() {
    return Array.from(this.windowRef.document.querySelectorAll('[role="listitem"]'));
  }
  getFocusedIndex() {
    return this.getResultElements().indexOf(this.getFocusedElement());
  }
  getFocusedElement() {
    return this.windowRef.document.activeElement;
  }
  static {
    this.ɵfac = function MessagingComponent_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _MessagingComponent)(ɵɵdirectiveInject(WindowRef), ɵɵdirectiveInject(FilesFormValidators));
    };
  }
  static {
    this.ɵcmp = ɵɵdefineComponent({
      type: _MessagingComponent,
      selectors: [["cx-messaging"]],
      viewQuery: function MessagingComponent_Query(rf, ctx) {
        if (rf & 1) {
          ɵɵviewQuery(FileUploadComponent, 5);
        }
        if (rf & 2) {
          let _t;
          ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.fileUploadComponent = _t.first);
        }
      },
      inputs: {
        messageEvents$: "messageEvents$",
        scrollToInput: "scrollToInput",
        messagingConfigs: "messagingConfigs"
      },
      outputs: {
        send: "send",
        itemClicked: "itemClicked",
        downloadAttachment: "downloadAttachment"
      },
      standalone: false,
      decls: 2,
      vars: 3,
      consts: [[4, "ngIf"], [1, "container"], [1, "cx-avatar-line"], ["role", "list", "id", "cx-messages", 1, "cx-messages", 3, "keydown.arrowdown", "keydown.arrowup"], [4, "ngFor", "ngForOf"], ["class", "cx-message-footer", 3, "formGroup", 4, "ngIf"], ["role", "listitem", 1, "cx-message-card", 3, "ngClass"], [3, "message"], [3, "ngClass", "tabindex"], ["class", "cx-message-item-link", 3, "click", 4, "ngIf"], ["class", "cx-attachment", "target", "_blank", "rel", "noopener noreferrer", 3, "click", 4, "ngFor", "ngForOf"], [1, "cx-message-item-link", 3, "click"], ["target", "_blank", "rel", "noopener noreferrer", 1, "cx-attachment", 3, "click"], [3, "type"], [1, "cx-message-footer", 3, "formGroup"], [1, "cx-message-box"], ["id", "cx-messaging-footer-label", 1, "cx-footer-label"], ["class", "cx-message-item-selection", "formControlName", "item", 4, "ngIf"], [1, "cx-message-input"], ["aria-labelledby", "cx-messaging-footer-label", "formControlName", "message", "type", "text", 1, "form-control", 3, "keydown.enter", "maxLength", "placeholder"], ["class", "cx-visually-hidden", "role", "alert", 4, "ngIf"], ["aria-labelledby", "cx-messaging-footer-label", 1, "btn", "btn-block", "cx-send", 3, "click", "ngClass"], ["id", "cx-message-footer", 1, "cx-message-footer-info"], [3, "formControl", "accept", 4, "ngIf"], [1, "cx-message-footer-info-text"], ["prefix", "formErrors.file", 3, "control"], ["formControlName", "item", 1, "cx-message-item-selection"], [3, "value", 4, "ngFor", "ngForOf"], [3, "value"], ["role", "alert", 1, "cx-visually-hidden"], [3, "formControl", "accept"], [1, "cx-message-footer-text"]],
      template: function MessagingComponent_Template(rf, ctx) {
        if (rf & 1) {
          ɵɵtemplate(0, MessagingComponent_ng_container_0_Template, 8, 7, "ng-container", 0);
          ɵɵpipe(1, "async");
        }
        if (rf & 2) {
          ɵɵproperty("ngIf", ɵɵpipeBind1(1, 1, ctx.messageEvents$));
        }
      },
      dependencies: [NgClass, NgForOf, NgIf, IconComponent, NgSelectOption, ɵNgSelectMultipleOption, DefaultValueAccessor, SelectControlValueAccessor, NgControlStatus, NgControlStatusGroup, FormControlDirective, FormGroupDirective, FormControlName, FileUploadComponent, FormErrorsComponent, AvatarComponent, AsyncPipe, TranslatePipe, CxDatePipe],
      encapsulation: 2
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(MessagingComponent, [{
    type: Component,
    args: [{
      selector: "cx-messaging",
      standalone: false,
      template: `<ng-container *ngIf="messageEvents$ | async as messageEvents">
  <div class="container">
    <div class="cx-avatar-line"></div>
    <div
      role="list"
      class="cx-messages"
      id="cx-messages"
      (keydown.arrowdown)="focusNextChild($any($event))"
      (keydown.arrowup)="focusPreviousChild($any($event))"
      [attr.aria-label]="'chatMessaging.messages' | cxTranslate"
    >
      <ng-container *ngFor="let message of messageEvents; let i = index">
        <div
          class="cx-message-card"
          role="listitem"
          [ngClass]="{ 'cx-message-display': message.rightAlign ?? false }"
        >
          <cx-avatar [message]="message"> </cx-avatar>

          <div>
            <label>{{
              (message.createdAt | cxDate: messagingConfigs?.dateFormat) ??
                dateFormat
            }}</label>
            <div
              [ngClass]="
                (message.rightAlign ?? false)
                  ? 'cx-message-right-align-text'
                  : 'cx-message-left-align-text'
              "
              [tabindex]="i ? -1 : 0"
              [attr.aria-label]="
                'chatMessaging.informationLabel'
                  | cxTranslate
                    : {
                        author: message.author,
                        text: getMessageText(message),
                        date:
                          (message.createdAt
                            | cxDate: messagingConfigs?.dateFormat) ??
                          dateFormat,
                      }
              "
            >
              <a
                *ngIf="message.item"
                class="cx-message-item-link"
                (click)="onItemClicked(message.item)"
                >{{ message.item.name }}:</a
              >
              {{ message.text }}
            </div>
            <a
              *ngFor="let attachment of message.attachments ?? []"
              class="cx-attachment"
              (click)="
                triggerDownload(
                  message.code,
                  attachment.id,
                  attachment.filename
                )
              "
              target="_blank"
              rel="noopener noreferrer"
            >
              <cx-icon [type]="iconTypes.ATTACHMENT"></cx-icon>
              {{ attachment.filename }}
            </a>
          </div>
        </div>
      </ng-container>
    </div>
  </div>

  <div
    [formGroup]="form"
    class="cx-message-footer"
    *ngIf="messagingConfigs?.displayAddMessageSection | async"
  >
    <div class="cx-message-box">
      <label class="cx-footer-label" id="cx-messaging-footer-label">
        <span>
          {{ 'chatMessaging.addNewMessage' | cxTranslate }}
          <select
            [attr.aria-label]="
              'chatMessaging.a11y.itemListBoxLabel' | cxTranslate
            "
            *ngIf="messagingConfigs?.itemList$ | async as itemList"
            class="cx-message-item-selection"
            formControlName="item"
          >
            <option *ngFor="let item of itemList" value="{{ item.id }}">
              {{ item.name }}
            </option>
          </select>
        </span>
        <div class="cx-message-input">
          <input
            aria-labelledby="cx-messaging-footer-label"
            formControlName="message"
            type="text"
            class="form-control"
            (keydown.enter)="onSend()"
            [maxLength]="
              messagingConfigs?.charactersLimit || MAX_INPUT_CHARACTERS
            "
            placeholder="{{
              messagingConfigs?.newMessagePlaceHolder ||
                ('chatMessaging.addMessagePlaceHolder' | cxTranslate)
            }}"
          />
          <span
            class="cx-visually-hidden"
            *ngIf="inputCharacterLeft === 0"
            role="alert"
          >
            {{ 'chatMessaging.characterLimitAlert' | cxTranslate }}
          </span>
          <button
            aria-labelledby="cx-messaging-footer-label"
            class="btn btn-block cx-send"
            [ngClass]="
              !!messagingConfigs?.sendBtnIsNotPrimary
                ? 'btn-secondary'
                : 'btn-primary'
            "
            (click)="onSend()"
          >
            {{ 'chatMessaging.send' | cxTranslate }}
          </button>
        </div>
      </label>
      <div class="cx-message-footer-info" id="cx-message-footer">
        <cx-file-upload
          [formControl]="$any(form.get('file'))"
          [accept]="allowedTypes"
          *ngIf="messagingConfigs?.enableFileUploadOption"
        >
          <ng-template>
            <cx-icon [type]="iconTypes.UPLOAD"></cx-icon>
            <span class="cx-message-footer-text"
              >{{ 'chatMessaging.uploadFile' | cxTranslate }}
            </span>
          </ng-template>
        </cx-file-upload>

        <p class="cx-message-footer-info-text">
          {{
            'chatMessaging.charactersLeft'
              | cxTranslate: { count: inputCharacterLeft }
          }}
        </p>
      </div>
      <cx-form-errors
        [control]="form.get('file')"
        prefix="formErrors.file"
      ></cx-form-errors>
    </div>
  </div>
</ng-container>
`
    }]
  }], () => [{
    type: WindowRef
  }, {
    type: FilesFormValidators
  }], {
    fileUploadComponent: [{
      type: ViewChild,
      args: [FileUploadComponent]
    }],
    messageEvents$: [{
      type: Input
    }],
    scrollToInput: [{
      type: Input
    }],
    messagingConfigs: [{
      type: Input
    }],
    send: [{
      type: Output
    }],
    itemClicked: [{
      type: Output
    }],
    downloadAttachment: [{
      type: Output
    }]
  });
})();
var ChatMessagingModule = class _ChatMessagingModule {
  static {
    this.ɵfac = function ChatMessagingModule_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _ChatMessagingModule)();
    };
  }
  static {
    this.ɵmod = ɵɵdefineNgModule({
      type: _ChatMessagingModule,
      declarations: [AvatarComponent, MessagingComponent],
      imports: [CommonModule, I18nModule, IconModule, FormsModule, ReactiveFormsModule, FileUploadModule, FormErrorsModule],
      exports: [AvatarComponent, MessagingComponent]
    });
  }
  static {
    this.ɵinj = ɵɵdefineInjector({
      imports: [CommonModule, I18nModule, IconModule, FormsModule, ReactiveFormsModule, FileUploadModule, FormErrorsModule]
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(ChatMessagingModule, [{
    type: NgModule,
    args: [{
      imports: [CommonModule, I18nModule, IconModule, FormsModule, ReactiveFormsModule, FileUploadModule, FormErrorsModule],
      declarations: [AvatarComponent, MessagingComponent],
      exports: [AvatarComponent, MessagingComponent]
    }]
  }], null, null);
})();
var ReadMoreComponent = class _ReadMoreComponent {
  constructor() {
    this.readMoreI18nKey = "common.readMore";
    this.readLessI18nKey = "common.readLess";
    this.maxLength = 300;
    this.text = "";
    this.showReadMore = false;
    this.isCollapsed = true;
  }
  ngOnChanges(changes) {
    if (changes["text"].currentValue.length > this.maxLength) {
      this.showReadMore = true;
    }
  }
  static {
    this.ɵfac = function ReadMoreComponent_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _ReadMoreComponent)();
    };
  }
  static {
    this.ɵcmp = ɵɵdefineComponent({
      type: _ReadMoreComponent,
      selectors: [["cx-read-more"]],
      inputs: {
        readMoreI18nKey: "readMoreI18nKey",
        readLessI18nKey: "readLessI18nKey",
        maxLength: "maxLength",
        text: "text"
      },
      features: [ɵɵNgOnChangesFeature],
      decls: 3,
      vars: 3,
      consts: [[4, "ngIf"], ["class", "btn-link", 3, "click", 4, "ngIf"], [1, "btn-link", 3, "click"]],
      template: function ReadMoreComponent_Template(rf, ctx) {
        if (rf & 1) {
          ɵɵtemplate(0, ReadMoreComponent_ng_container_0_Template, 2, 1, "ng-container", 0)(1, ReadMoreComponent_ng_container_1_Template, 2, 1, "ng-container", 0)(2, ReadMoreComponent_button_2_Template, 3, 3, "button", 1);
        }
        if (rf & 2) {
          ɵɵproperty("ngIf", ctx.isCollapsed);
          ɵɵadvance();
          ɵɵproperty("ngIf", !ctx.isCollapsed);
          ɵɵadvance();
          ɵɵproperty("ngIf", ctx.showReadMore);
        }
      },
      dependencies: [CommonModule, NgIf, I18nModule, TranslatePipe],
      encapsulation: 2,
      changeDetection: 0
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(ReadMoreComponent, [{
    type: Component,
    args: [{
      selector: "cx-read-more",
      changeDetection: ChangeDetectionStrategy.OnPush,
      standalone: true,
      imports: [CommonModule, I18nModule],
      template: '<ng-container *ngIf="isCollapsed">\n  {{ text.slice(0, maxLength) }}\n</ng-container>\n<ng-container *ngIf="!isCollapsed">\n  {{ text }}\n</ng-container>\n\n<button\n  class="btn-link"\n  *ngIf="showReadMore"\n  (click)="isCollapsed = !isCollapsed"\n>\n  {{ (isCollapsed ? readMoreI18nKey! : readLessI18nKey!) | cxTranslate }}\n</button>\n'
    }]
  }], null, {
    readMoreI18nKey: [{
      type: Input
    }],
    readLessI18nKey: [{
      type: Input
    }],
    maxLength: [{
      type: Input
    }],
    text: [{
      type: Input
    }]
  });
})();
var BannerCarouselModule = class _BannerCarouselModule {
  static {
    this.ɵfac = function BannerCarouselModule_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _BannerCarouselModule)();
    };
  }
  static {
    this.ɵmod = ɵɵdefineNgModule({
      type: _BannerCarouselModule,
      declarations: [BannerCarouselComponent],
      imports: [CommonModule, PageComponentModule, CarouselModule, MediaModule],
      exports: [BannerCarouselComponent]
    });
  }
  static {
    this.ɵinj = ɵɵdefineInjector({
      providers: [provideDefaultConfig({
        cmsComponents: {
          RotatingImagesComponent: {
            component: BannerCarouselComponent
          }
        }
      })],
      imports: [CommonModule, PageComponentModule, CarouselModule, MediaModule]
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(BannerCarouselModule, [{
    type: NgModule,
    args: [{
      imports: [CommonModule, PageComponentModule, CarouselModule, MediaModule],
      providers: [provideDefaultConfig({
        cmsComponents: {
          RotatingImagesComponent: {
            component: BannerCarouselComponent
          }
        }
      })],
      declarations: [BannerCarouselComponent],
      exports: [BannerCarouselComponent]
    }]
  }], null, null);
})();
var LcpPresenceMappingService = class _LcpPresenceMappingService {
  getFetchPriority(lcpContext) {
    if (lcpContext === LcpPresence.HAS_LCP) {
      return ImageFetchPriority.HIGH;
    }
    return void 0;
  }
  static {
    this.ɵfac = function LcpPresenceMappingService_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _LcpPresenceMappingService)();
    };
  }
  static {
    this.ɵprov = ɵɵdefineInjectable({
      token: _LcpPresenceMappingService,
      factory: _LcpPresenceMappingService.ɵfac,
      providedIn: "root"
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(LcpPresenceMappingService, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], null, null);
})();
var LcpContextDirective = class _LcpContextDirective {
  constructor() {
    this.lcpPresence$ = inject(LCP_PRESENCE);
    this.lcpPresenceMappingService = inject(LcpPresenceMappingService);
    this.fetchPriority$ = this.lcpPresence$.pipe(map((lcpElementInfo) => this.lcpPresenceMappingService.getFetchPriority(lcpElementInfo)), distinctUntilChanged(), shareReplay({
      bufferSize: 1,
      refCount: true
    }));
    this.templateRef = inject(TemplateRef);
    this.viewContainer = inject(ViewContainerRef);
  }
  ngOnInit() {
    this.viewContainer.createEmbeddedView(this.templateRef, {
      $implicit: {
        lcpPresence$: this.lcpPresence$,
        fetchPriority$: this.fetchPriority$
      }
    });
  }
  static ngTemplateContextGuard(_dir, _ctx) {
    return true;
  }
  static {
    this.ɵfac = function LcpContextDirective_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _LcpContextDirective)();
    };
  }
  static {
    this.ɵdir = ɵɵdefineDirective({
      type: _LcpContextDirective,
      selectors: [["", "cxLcpContext", ""]],
      standalone: false
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(LcpContextDirective, [{
    type: Directive,
    args: [{
      selector: "[cxLcpContext]",
      standalone: false
    }]
  }], null, null);
})();
var BannerComponent = class _BannerComponent {
  constructor(component, urlService, cmsService) {
    this.component = component;
    this.urlService = urlService;
    this.cmsService = cmsService;
    this.data$ = this.component.data$.pipe(tap((data) => {
      this.setRouterLink(data);
      this.styleClasses = data.styleClasses;
    }));
  }
  /**
   * Returns `_blank` to force opening the link in a new window whenever the
   * `data.external` flag is set to true.
   */
  getTarget(data) {
    return data.external === "true" || data.external === true ? "_blank" : null;
  }
  setRouterLink(data) {
    if (data.urlLink) {
      this.routerLink = data.urlLink;
    } else if (data.contentPage) {
      this.cmsService.getPage({
        id: data.contentPage,
        type: PageType.CONTENT_PAGE
      }).pipe(take(1)).subscribe((page) => {
        this.routerLink = page?.label;
      });
    } else if (data.product) {
      this.routerLink = this.urlService.transform({
        cxRoute: "product",
        params: {
          code: data.product
        }
      });
    } else if (data.category) {
      this.routerLink = this.urlService.transform({
        cxRoute: "category",
        params: {
          code: data.category
        }
      });
    }
  }
  getImage(data) {
    if (data.media) {
      if ("url" in data.media) {
        return data.media;
      } else {
        return data.media;
      }
    }
  }
  getImageAltText(data) {
    const img = this.getImage(data);
    if (!img) {
      return;
    }
    return "url" in img ? img.altText : Object.values(img)[0]?.altText;
  }
  getLinkAriaLabel(data) {
    const imgAltText = this.getImageAltText(data);
    return data.headline ?? imgAltText;
  }
  static {
    this.ɵfac = function BannerComponent_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _BannerComponent)(ɵɵdirectiveInject(CmsComponentData), ɵɵdirectiveInject(SemanticPathService), ɵɵdirectiveInject(CmsService));
    };
  }
  static {
    this.ɵcmp = ɵɵdefineComponent({
      type: _BannerComponent,
      selectors: [["cx-banner"]],
      hostVars: 2,
      hostBindings: function BannerComponent_HostBindings(rf, ctx) {
        if (rf & 2) {
          ɵɵclassMap(ctx.styleClasses);
        }
      },
      standalone: false,
      decls: 1,
      vars: 0,
      consts: [[4, "cxLcpContext"], [4, "ngIf"], [3, "url", "target", "ariaLabel"], ["class", "headline", 3, "innerHTML", 4, "ngIf"], [3, "container", "elementType", "fetchPriority", 4, "cxFeature"], [3, "container", "fetchPriority", 4, "cxFeature"], ["class", "content", 3, "innerHTML", 4, "ngIf"], [1, "headline", 3, "innerHTML"], [3, "container", "elementType", "fetchPriority"], [3, "container", "fetchPriority"], [1, "content", 3, "innerHTML"], [1, "no-link"]],
      template: function BannerComponent_Template(rf, ctx) {
        if (rf & 1) {
          ɵɵtemplate(0, BannerComponent_ng_container_0_Template, 3, 3, "ng-container", 0);
        }
      },
      dependencies: [NgIf, GenericLinkComponent, MediaComponent, FeatureDirective, LcpContextDirective, AsyncPipe],
      encapsulation: 2,
      changeDetection: 0
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(BannerComponent, [{
    type: Component,
    args: [{
      selector: "cx-banner",
      changeDetection: ChangeDetectionStrategy.OnPush,
      standalone: false,
      template: `<ng-container *cxLcpContext="let lcpContext">
  <ng-container *ngIf="data$ | async as data">
    <ng-container *ngIf="routerLink">
      <cx-generic-link
        [url]="routerLink"
        [target]="getTarget(data)"
        [ariaLabel]="getLinkAriaLabel(data)"
        [attr.title]="!data.headline ? getImageAltText(data) : null"
      >
      </cx-generic-link>
      <p class="headline" *ngIf="data.headline" [innerHTML]="data.headline"></p>
      <cx-media
        *cxFeature="'useExtendedMediaComponentConfiguration'"
        [container]="getImage(data)"
        [elementType]="'picture'"
        [fetchPriority]="lcpContext.fetchPriority$ | async"
      ></cx-media>
      <cx-media
        *cxFeature="'!useExtendedMediaComponentConfiguration'"
        [container]="getImage(data)"
        [fetchPriority]="lcpContext.fetchPriority$ | async"
      ></cx-media>
      <p class="content" *ngIf="data.content" [innerHTML]="data.content"></p>
    </ng-container>

    <ng-container *ngIf="!routerLink">
      <div class="no-link">
        <p
          class="headline"
          *ngIf="data.headline"
          [innerHTML]="data.headline"
        ></p>
        <cx-media
          *cxFeature="'useExtendedMediaComponentConfiguration'"
          [container]="getImage(data)"
          [elementType]="'picture'"
          [fetchPriority]="lcpContext.fetchPriority$ | async"
        ></cx-media>
        <cx-media
          *cxFeature="'!useExtendedMediaComponentConfiguration'"
          [container]="getImage(data)"
          [fetchPriority]="lcpContext.fetchPriority$ | async"
        ></cx-media>
        <p class="content" *ngIf="data.content" [innerHTML]="data.content"></p>
      </div>
    </ng-container>
  </ng-container>
</ng-container>
`
    }]
  }], () => [{
    type: CmsComponentData
  }, {
    type: SemanticPathService
  }, {
    type: CmsService
  }], {
    styleClasses: [{
      type: HostBinding,
      args: ["class"]
    }]
  });
})();
var LcpContextDirectiveModule = class _LcpContextDirectiveModule {
  static {
    this.ɵfac = function LcpContextDirectiveModule_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _LcpContextDirectiveModule)();
    };
  }
  static {
    this.ɵmod = ɵɵdefineNgModule({
      type: _LcpContextDirectiveModule,
      declarations: [LcpContextDirective],
      exports: [LcpContextDirective]
    });
  }
  static {
    this.ɵinj = ɵɵdefineInjector({});
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(LcpContextDirectiveModule, [{
    type: NgModule,
    args: [{
      declarations: [LcpContextDirective],
      exports: [LcpContextDirective]
    }]
  }], null, null);
})();
var BannerModule = class _BannerModule {
  static {
    this.ɵfac = function BannerModule_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _BannerModule)();
    };
  }
  static {
    this.ɵmod = ɵɵdefineNgModule({
      type: _BannerModule,
      declarations: [BannerComponent],
      imports: [CommonModule, RouterModule, GenericLinkModule, MediaModule, FeaturesConfigModule, LcpContextDirectiveModule],
      exports: [BannerComponent]
    });
  }
  static {
    this.ɵinj = ɵɵdefineInjector({
      providers: [provideDefaultConfig({
        cmsComponents: {
          SimpleResponsiveBannerComponent: {
            component: BannerComponent
          },
          BannerComponent: {
            component: BannerComponent
          },
          SimpleBannerComponent: {
            component: BannerComponent
          }
        }
      })],
      imports: [CommonModule, RouterModule, GenericLinkModule, MediaModule, FeaturesConfigModule, LcpContextDirectiveModule]
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(BannerModule, [{
    type: NgModule,
    args: [{
      imports: [CommonModule, RouterModule, GenericLinkModule, MediaModule, FeaturesConfigModule, LcpContextDirectiveModule],
      providers: [provideDefaultConfig({
        cmsComponents: {
          SimpleResponsiveBannerComponent: {
            component: BannerComponent
          },
          BannerComponent: {
            component: BannerComponent
          },
          SimpleBannerComponent: {
            component: BannerComponent
          }
        }
      })],
      declarations: [BannerComponent],
      exports: [BannerComponent]
    }]
  }], null, null);
})();
var LinkComponent = class _LinkComponent {
  constructor(component) {
    this.component = component;
    this.data$ = this.component.data$;
    this.subscriptions = new Subscription();
  }
  ngOnInit() {
    this.subscriptions.add(this.data$.subscribe((data) => {
      this.styleClasses = data?.styleClasses;
    }));
  }
  /**
   * Returns `_blank` to force opening the link in a new window whenever the
   * `data.target` flag is set to `true`.
   */
  getTarget(data) {
    return data.target === "true" || data.target === true ? "_blank" : null;
  }
  ngOnDestroy() {
    this.subscriptions?.unsubscribe();
  }
  static {
    this.ɵfac = function LinkComponent_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _LinkComponent)(ɵɵdirectiveInject(CmsComponentData));
    };
  }
  static {
    this.ɵcmp = ɵɵdefineComponent({
      type: _LinkComponent,
      selectors: [["cx-link"]],
      hostVars: 2,
      hostBindings: function LinkComponent_HostBindings(rf, ctx) {
        if (rf & 2) {
          ɵɵclassMap(ctx.styleClasses);
        }
      },
      standalone: false,
      decls: 2,
      vars: 3,
      consts: [[3, "url", "style", "target", 4, "ngIf"], [3, "url", "target"]],
      template: function LinkComponent_Template(rf, ctx) {
        if (rf & 1) {
          ɵɵtemplate(0, LinkComponent_cx_generic_link_0_Template, 2, 5, "cx-generic-link", 0);
          ɵɵpipe(1, "async");
        }
        if (rf & 2) {
          ɵɵproperty("ngIf", ɵɵpipeBind1(1, 1, ctx.data$));
        }
      },
      dependencies: [NgIf, GenericLinkComponent, AsyncPipe],
      encapsulation: 2,
      changeDetection: 0
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(LinkComponent, [{
    type: Component,
    args: [{
      selector: "cx-link",
      changeDetection: ChangeDetectionStrategy.OnPush,
      standalone: false,
      template: `<cx-generic-link
  *ngIf="data$ | async as data"
  [url]="data.url ?? ''"
  [style]="data.styleAttributes"
  [target]="getTarget(data)"
  >{{ data.linkName }}</cx-generic-link
>
`
    }]
  }], () => [{
    type: CmsComponentData
  }], {
    styleClasses: [{
      type: HostBinding,
      args: ["class"]
    }]
  });
})();
var LinkModule = class _LinkModule {
  static {
    this.ɵfac = function LinkModule_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _LinkModule)();
    };
  }
  static {
    this.ɵmod = ɵɵdefineNgModule({
      type: _LinkModule,
      declarations: [LinkComponent],
      imports: [CommonModule, RouterModule, GenericLinkModule],
      exports: [LinkComponent]
    });
  }
  static {
    this.ɵinj = ɵɵdefineInjector({
      providers: [provideDefaultConfig({
        cmsComponents: {
          CMSLinkComponent: {
            component: LinkComponent
          }
        }
      })],
      imports: [CommonModule, RouterModule, GenericLinkModule]
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(LinkModule, [{
    type: NgModule,
    args: [{
      imports: [CommonModule, RouterModule, GenericLinkModule],
      providers: [provideDefaultConfig({
        cmsComponents: {
          CMSLinkComponent: {
            component: LinkComponent
          }
        }
      })],
      declarations: [LinkComponent],
      exports: [LinkComponent]
    }]
  }], null, null);
})();
var SupplementHashAnchorsPipe = class _SupplementHashAnchorsPipe {
  constructor(renderer, winRef) {
    this.renderer = renderer;
    this.winRef = winRef;
  }
  getPath(anchorId) {
    const currentUrlWithoutFragment = this.winRef.location.href?.replace(/#.*$/, "");
    return `${currentUrlWithoutFragment}${anchorId}`;
  }
  transform(html = "") {
    const template = this.renderer.createElement("template");
    template.innerHTML = html.trim();
    const linkNodes = template.content.querySelectorAll("a");
    Array.from(linkNodes).forEach((link) => {
      const href = link.getAttribute("href");
      if (href?.indexOf("#") === 0) {
        this.renderer.setProperty(link, "href", this.getPath(href));
      }
    });
    return template.innerHTML;
  }
  static {
    this.ɵfac = function SupplementHashAnchorsPipe_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _SupplementHashAnchorsPipe)(ɵɵdirectiveInject(Renderer2, 16), ɵɵdirectiveInject(WindowRef, 16));
    };
  }
  static {
    this.ɵpipe = ɵɵdefinePipe({
      name: "cxSupplementHashAnchors",
      type: _SupplementHashAnchorsPipe,
      pure: true,
      standalone: false
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(SupplementHashAnchorsPipe, [{
    type: Pipe,
    args: [{
      name: "cxSupplementHashAnchors",
      standalone: false
    }]
  }], () => [{
    type: Renderer2
  }, {
    type: WindowRef
  }], null);
})();
var ParagraphComponent = class _ParagraphComponent {
  handleClick(event) {
    if (event.target instanceof HTMLAnchorElement) {
      const element = event.target;
      const href = element?.getAttribute("href");
      const documentUrlObject = new URL(element.ownerDocument.URL);
      if (href && documentUrlObject.host === element.host) {
        event.preventDefault();
        this.router.navigateByUrl(href.replace(documentUrlObject.origin, ""));
      }
    }
  }
  constructor(component, router) {
    this.component = component;
    this.router = router;
    this.sanitizer = inject(DomSanitizer);
  }
  bypassSecurityTrustHtml(html = "") {
    return this.sanitizer.bypassSecurityTrustHtml(html);
  }
  static {
    this.ɵfac = function ParagraphComponent_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _ParagraphComponent)(ɵɵdirectiveInject(CmsComponentData), ɵɵdirectiveInject(Router));
    };
  }
  static {
    this.ɵcmp = ɵɵdefineComponent({
      type: _ParagraphComponent,
      selectors: [["cx-paragraph"]],
      hostBindings: function ParagraphComponent_HostBindings(rf, ctx) {
        if (rf & 1) {
          ɵɵlistener("click", function ParagraphComponent_click_HostBindingHandler($event) {
            return ctx.handleClick($event);
          });
        }
      },
      standalone: false,
      decls: 2,
      vars: 3,
      consts: [[3, "innerHTML", 4, "ngIf"], [3, "innerHTML"]],
      template: function ParagraphComponent_Template(rf, ctx) {
        if (rf & 1) {
          ɵɵtemplate(0, ParagraphComponent_div_0_Template, 2, 3, "div", 0);
          ɵɵpipe(1, "async");
        }
        if (rf & 2) {
          ɵɵproperty("ngIf", ɵɵpipeBind1(1, 1, ctx.component.data$));
        }
      },
      dependencies: [NgIf, AsyncPipe, SupplementHashAnchorsPipe],
      encapsulation: 2,
      changeDetection: 0
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(ParagraphComponent, [{
    type: Component,
    args: [{
      selector: "cx-paragraph",
      changeDetection: ChangeDetectionStrategy.OnPush,
      standalone: false,
      template: '<div\n  *ngIf="component.data$ | async as data"\n  [innerHTML]="bypassSecurityTrustHtml(data.content | cxSupplementHashAnchors)"\n></div>\n'
    }]
  }], () => [{
    type: CmsComponentData
  }, {
    type: Router
  }], {
    handleClick: [{
      type: HostListener,
      args: ["click", ["$event"]]
    }]
  });
})();
var SupplementHashAnchorsModule = class _SupplementHashAnchorsModule {
  static {
    this.ɵfac = function SupplementHashAnchorsModule_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _SupplementHashAnchorsModule)();
    };
  }
  static {
    this.ɵmod = ɵɵdefineNgModule({
      type: _SupplementHashAnchorsModule,
      declarations: [SupplementHashAnchorsPipe],
      exports: [SupplementHashAnchorsPipe]
    });
  }
  static {
    this.ɵinj = ɵɵdefineInjector({});
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(SupplementHashAnchorsModule, [{
    type: NgModule,
    args: [{
      declarations: [SupplementHashAnchorsPipe],
      exports: [SupplementHashAnchorsPipe]
    }]
  }], null, null);
})();
var CmsParagraphModule = class _CmsParagraphModule {
  static {
    this.ɵfac = function CmsParagraphModule_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _CmsParagraphModule)();
    };
  }
  static {
    this.ɵmod = ɵɵdefineNgModule({
      type: _CmsParagraphModule,
      declarations: [ParagraphComponent],
      imports: [CommonModule, RouterModule, SupplementHashAnchorsModule],
      exports: [ParagraphComponent]
    });
  }
  static {
    this.ɵinj = ɵɵdefineInjector({
      providers: [provideDefaultConfig({
        cmsComponents: {
          CMSParagraphComponent: {
            component: ParagraphComponent
          },
          CMSTabParagraphComponent: {
            component: ParagraphComponent
          }
        }
      })],
      imports: [CommonModule, RouterModule, SupplementHashAnchorsModule]
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(CmsParagraphModule, [{
    type: NgModule,
    args: [{
      imports: [CommonModule, RouterModule, SupplementHashAnchorsModule],
      providers: [provideDefaultConfig({
        cmsComponents: {
          CMSParagraphComponent: {
            component: ParagraphComponent
          },
          CMSTabParagraphComponent: {
            component: ParagraphComponent
          }
        }
      })],
      declarations: [ParagraphComponent],
      exports: [ParagraphComponent]
    }]
  }], null, null);
})();
var PDFComponent = class _PDFComponent {
  constructor(component, mediaService) {
    this.component = component;
    this.mediaService = mediaService;
    this.data$ = this.component.data$.pipe(tap((data) => {
      if (data?.pdfFile?.url) {
        this.url = this.mediaService.getMedia(data.pdfFile)?.src;
      }
    }));
  }
  addPdfExtension(title) {
    if (!title) {
      return "";
    }
    const trimTitle = title.trim();
    return trimTitle.endsWith(".pdf") ? trimTitle : `${trimTitle}.pdf`;
  }
  static {
    this.ɵfac = function PDFComponent_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _PDFComponent)(ɵɵdirectiveInject(CmsComponentData), ɵɵdirectiveInject(MediaService));
    };
  }
  static {
    this.ɵcmp = ɵɵdefineComponent({
      type: _PDFComponent,
      selectors: [["cx-pdf"]],
      standalone: false,
      decls: 2,
      vars: 3,
      consts: [[4, "ngIf"], [1, "pdf-container"], ["target", "_blank", "rel", "noopener noreferrer", 3, "href"], ["aria-hidden", "true"], [3, "type"]],
      template: function PDFComponent_Template(rf, ctx) {
        if (rf & 1) {
          ɵɵtemplate(0, PDFComponent_ng_container_0_Template, 8, 5, "ng-container", 0);
          ɵɵpipe(1, "async");
        }
        if (rf & 2) {
          ɵɵproperty("ngIf", ɵɵpipeBind1(1, 1, ctx.data$));
        }
      },
      dependencies: [NgIf, IconComponent, AsyncPipe, TranslatePipe],
      encapsulation: 2,
      changeDetection: 0
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PDFComponent, [{
    type: Component,
    args: [{
      selector: "cx-pdf",
      changeDetection: ChangeDetectionStrategy.OnPush,
      standalone: false,
      template: `<ng-container *ngIf="data$ | async as data">
  <div class="pdf-container">
    <a [href]="url" target="_blank" rel="noopener noreferrer">
      <span>{{
        addPdfExtension(
          data?.title ||
            data?.pdfFile?.altText ||
            ('pdf.defaultTitle' | cxTranslate)
        )
      }}</span>
      <span aria-hidden="true">
        <cx-icon [type]="'PDF_FILE'"></cx-icon>
      </span>
    </a>
  </div>
</ng-container>
`
    }]
  }], () => [{
    type: CmsComponentData
  }, {
    type: MediaService
  }], null);
})();
var PDFModule = class _PDFModule {
  static {
    this.ɵfac = function PDFModule_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _PDFModule)();
    };
  }
  static {
    this.ɵmod = ɵɵdefineNgModule({
      type: _PDFModule,
      declarations: [PDFComponent],
      imports: [CommonModule, IconModule, I18nModule],
      exports: [PDFComponent]
    });
  }
  static {
    this.ɵinj = ɵɵdefineInjector({
      providers: [provideDefaultConfig({
        cmsComponents: {
          PDFDocumentComponent: {
            component: PDFComponent
          }
        }
      })],
      imports: [CommonModule, IconModule, I18nModule]
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PDFModule, [{
    type: NgModule,
    args: [{
      imports: [CommonModule, IconModule, I18nModule],
      providers: [provideDefaultConfig({
        cmsComponents: {
          PDFDocumentComponent: {
            component: PDFComponent
          }
        }
      })],
      declarations: [PDFComponent],
      exports: [PDFComponent]
    }]
  }], null, null);
})();
var TAB_MODE;
(function(TAB_MODE2) {
  TAB_MODE2["TAB"] = "TAB";
  TAB_MODE2["ACCORDIAN"] = "ACCORDIAN";
})(TAB_MODE || (TAB_MODE = {}));
function wrapIntoBounds(index, max, min = 0) {
  if (index < min) {
    return max;
  } else if (index > max) {
    return min;
  }
  return index;
}
var TabPanelComponent = class _TabPanelComponent {
  constructor() {
    this.TAB_MODE = TAB_MODE;
    this.mode = TAB_MODE.TAB;
  }
  static {
    this.ɵfac = function TabPanelComponent_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _TabPanelComponent)();
    };
  }
  static {
    this.ɵcmp = ɵɵdefineComponent({
      type: _TabPanelComponent,
      selectors: [["cx-tab-panel"]],
      inputs: {
        tab: "tab",
        mode: "mode"
      },
      standalone: false,
      decls: 2,
      vars: 8,
      consts: [[1, "active", 3, "tabindex", "cxFocus"], [3, "ngTemplateOutlet", 4, "ngIf"], [3, "ngTemplateOutlet"]],
      template: function TabPanelComponent_Template(rf, ctx) {
        if (rf & 1) {
          ɵɵelementStart(0, "div", 0);
          ɵɵtemplate(1, TabPanelComponent_ng_container_1_Template, 1, 1, "ng-container", 1);
          ɵɵelementEnd();
        }
        if (rf & 2) {
          let tmp_3_0;
          ɵɵproperty("tabindex", ctx.tab.disableBorderFocus ? -1 : 0)("cxFocus", ɵɵpureFunction1(6, _c38, !ctx.tab.disableBorderFocus));
          ɵɵattribute("role", ctx.mode === ctx.TAB_MODE.ACCORDIAN ? "region" : "tabpanel")("aria-labelledby", (tmp_3_0 = ctx.tab.id) !== null && tmp_3_0 !== void 0 ? tmp_3_0 : null)("id", ctx.tab.id !== null && ctx.tab.id !== void 0 ? "section-" + ctx.tab.id : null);
          ɵɵadvance();
          ɵɵproperty("ngIf", ctx.tab.content);
        }
      },
      dependencies: [NgIf, NgTemplateOutlet, FocusDirective],
      encapsulation: 2
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(TabPanelComponent, [{
    type: Component,
    args: [{
      selector: "cx-tab-panel",
      standalone: false,
      template: `<div
  [attr.role]="mode === TAB_MODE.ACCORDIAN ? 'region' : 'tabpanel'"
  [tabindex]="tab.disableBorderFocus ? -1 : 0"
  [attr.aria-labelledby]="tab.id ?? null"
  [attr.id]="
    tab.id !== null && tab.id !== undefined ? 'section-' + tab.id : null
  "
  class="active"
  [cxFocus]="{ focusOnEscape: !tab.disableBorderFocus }"
>
  <ng-container
    *ngIf="tab.content"
    [ngTemplateOutlet]="tab.content"
  ></ng-container>
</div>
`
    }]
  }], null, {
    tab: [{
      type: Input
    }],
    mode: [{
      type: Input
    }]
  });
})();
var TabComponent = class _TabComponent {
  constructor() {
    this.disabled = false;
    this.TAB_MODE = TAB_MODE;
    this.breakpointService = inject(BreakpointService);
    this.translationService = inject(TranslationService);
    this.cd = inject(ChangeDetectorRef);
    this.featureConfigService = inject(FeatureConfigService, {
      optional: true
    });
    this.subscriptions = new Subscription();
    useFeatureStyles("a11yCroppedFocusRing");
  }
  ngOnInit() {
    this.openTabs$ = new BehaviorSubject(this.config?.openTabs ?? []);
    this.mode$ = this.getMode();
  }
  ngAfterViewInit() {
    this.subscriptions.add(this.tabs$?.subscribe((tabs) => {
      this.tabs = tabs;
      this.cd.detectChanges();
    }));
  }
  ngOnDestroy() {
    this.subscriptions.unsubscribe();
  }
  /**
   * Tab selection works differently depending on the given mode.
   *
   * Modes:
   * - Tab: Closes all other tabs and opens the given tab.
   * - Accordian: Toggles the given tab open or closed.
   */
  select(tabNum, mode) {
    this.focus(tabNum);
    switch (mode) {
      case TAB_MODE.TAB:
        return this.openTabs$.next([tabNum]);
      case TAB_MODE.ACCORDIAN:
        return this.toggleTab(tabNum);
    }
  }
  /**
   * Focuses the given tab according to the number.
   */
  focus(tabNum) {
    this.tabHeaders.toArray()[tabNum].nativeElement.focus();
  }
  /**
   * Calls select or focus methods depending on the tab mode.
   */
  selectOrFocus(tabNum, mode, event) {
    event.preventDefault();
    if (this.featureConfigService?.isEnabled("a11yTabsManualActivation")) {
      switch (mode) {
        case TAB_MODE.TAB:
        case TAB_MODE.ACCORDIAN:
          return this.focus(tabNum);
      }
    } else {
      switch (mode) {
        case TAB_MODE.TAB:
          return this.select(tabNum, mode);
        case TAB_MODE.ACCORDIAN:
          return this.focus(tabNum);
      }
    }
  }
  /**
   * Handles keydown events made on tabs.
   *
   * Keys:
   * - ArrowUp / ArrowLeft: Select or focus the previous tab.
   * - ArrowRight / ArrowDown: Select or focus the next tab.
   * - Home: Select or focus the first tab.
   * - End: Select or focus the last tab.
   */
  handleKeydownEvent(tabNum, tabs, mode, event) {
    const FIRST_TAB = 0;
    const LAST_TAB = tabs.length - 1;
    const PREVIOUS_TAB = wrapIntoBounds(tabNum - 1, LAST_TAB);
    const NEXT_TAB = wrapIntoBounds(tabNum + 1, LAST_TAB);
    if (this.config.restrictDirectionKeys && this.shouldRestrictKeys(mode, event)) {
      return;
    }
    switch (event.key) {
      case "ArrowLeft":
      case "ArrowUp":
        return this.selectOrFocus(PREVIOUS_TAB, mode, event);
      case "ArrowRight":
      case "ArrowDown":
        return this.selectOrFocus(NEXT_TAB, mode, event);
      case "Home":
        return this.selectOrFocus(FIRST_TAB, mode, event);
      case "End":
        return this.selectOrFocus(LAST_TAB, mode, event);
    }
  }
  shouldRestrictKeys(mode, event) {
    if (mode === TAB_MODE.TAB && ["ArrowUp", "ArrowDown"].includes(event.key)) {
      return true;
    }
    if (mode === TAB_MODE.ACCORDIAN && ["ArrowLeft", "ArrowRight"].includes(event.key)) {
      return true;
    }
    return false;
  }
  /**
   * Indicates whether a tab is open (in the open tabs array).
   */
  isOpen(tabNum) {
    return this.getOpenTabs().find((open) => open === tabNum) !== void 0;
  }
  /**
   * Inverts the state of the given tab between open and closed.
   */
  toggleTab(tabNum) {
    const openTabs = this.getOpenTabs();
    const openTabIndex = openTabs.indexOf(tabNum);
    openTabIndex > -1 ? openTabs.splice(openTabIndex, 1) : openTabs.push(tabNum);
    this.openTabs$.next(openTabs);
  }
  /**
   * Returns index 0 if the tab is already open,
   * no tabs are open and its the first tab (ie. 0),
   * or in 'ACCORDIAN' mode.
   * Otherwise returns -1.
   */
  getTabIndex(tabNum, mode) {
    return this.isOpen(tabNum) || tabNum === 0 && this.getOpenTabs().length === 0 || mode === "ACCORDIAN" ? 0 : -1;
  }
  getTitle(mode, index) {
    const tab = this.tabs[index];
    if (mode === TAB_MODE.TAB) {
      return null;
    }
    let header = tab.header;
    if (tab.headerKey) {
      this.translationService.translate(tab.headerKey).pipe(take(1)).subscribe((val) => {
        header = val;
      });
    }
    return (
      // Show expanded or collapsed.
      (this.isOpen(index) ? "Collapse" : "Expand") + " " + header
    );
  }
  getOpenTabs() {
    return this.openTabs$.value;
  }
  /**
   * Returns the mode specified by the config.
   * If unspecified mode, return 'TAB' or 'ACCORDIAN' modes responsively using the specified breakpoint in the config.
   * If unspecified breakpoint, return 'TAB' mode by default.
   */
  getMode() {
    if (this.config.mode) {
      return of(this.config.mode);
    }
    if (this.config.breakpoint) {
      return this.breakpointService.isUp(this.config.breakpoint).pipe(map((isUp) => isUp ? TAB_MODE.TAB : TAB_MODE.ACCORDIAN));
    }
    return of(TAB_MODE.TAB);
  }
  static {
    this.ɵfac = function TabComponent_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _TabComponent)();
    };
  }
  static {
    this.ɵcmp = ɵɵdefineComponent({
      type: _TabComponent,
      selectors: [["cx-tab"]],
      viewQuery: function TabComponent_Query(rf, ctx) {
        if (rf & 1) {
          ɵɵviewQuery(_c39, 5);
        }
        if (rf & 2) {
          let _t;
          ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.tabHeaders = _t);
        }
      },
      inputs: {
        tabs$: "tabs$",
        tabs: "tabs",
        config: "config",
        disabled: "disabled"
      },
      standalone: false,
      decls: 2,
      vars: 3,
      consts: [["tabPanel", ""], ["tabHeader", ""], [4, "ngIf"], [4, "ngFor", "ngForOf"], ["type", "button", 1, "tab-btn", 3, "click", "keydown", "disabled"], ["class", "tab-icon", "aria-hidden", "true", 4, "ngIf"], [3, "ngTemplateOutlet", "ngTemplateOutletContext", 4, "ngIf"], ["aria-hidden", "true", 1, "tab-icon"], [3, "ngTemplateOutlet", "ngTemplateOutletContext"], [3, "ngTemplateOutlet", "ngTemplateOutletContext", 4, "ngFor", "ngForOf"], [3, "tab", "mode", 4, "ngIf"], [3, "tab", "mode"]],
      template: function TabComponent_Template(rf, ctx) {
        if (rf & 1) {
          ɵɵtemplate(0, TabComponent_ng_container_0_Template, 7, 8, "ng-container", 2);
          ɵɵpipe(1, "async");
        }
        if (rf & 2) {
          ɵɵproperty("ngIf", ɵɵpipeBind1(1, 1, ctx.mode$));
        }
      },
      dependencies: [NgForOf, NgIf, NgTemplateOutlet, TabPanelComponent, AsyncPipe, TranslatePipe],
      encapsulation: 2,
      changeDetection: 0
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(TabComponent, [{
    type: Component,
    args: [{
      selector: "cx-tab",
      changeDetection: ChangeDetectionStrategy.OnPush,
      standalone: false,
      template: `<ng-container *ngIf="mode$ | async as mode">
  <!-- Tab List is the list of buttons to open tabs -->
  <div
    [attr.role]="mode === TAB_MODE.ACCORDIAN ? null : 'tablist'"
    [attr.aria-label]="config.label | cxTranslate"
    [class]="mode.toLowerCase()"
  >
    <ng-container *ngFor="let tab of tabs; let i = index">
      <button
        type="button"
        class="tab-btn"
        [attr.role]="mode === TAB_MODE.ACCORDIAN ? 'button' : 'tab'"
        (click)="select(i, mode)"
        (keydown)="handleKeydownEvent(i, tabs, mode, $event)"
        [attr.aria-selected]="mode === TAB_MODE.ACCORDIAN ? null : isOpen(i)"
        [attr.aria-expanded]="mode === TAB_MODE.TAB ? null : isOpen(i)"
        [attr.aria-controls]="isOpen(i) ? 'section-' + i : null"
        [attr.tabindex]="getTabIndex(i, mode)"
        [attr.id]="tab.id"
        [attr.title]="getTitle(mode, i)"
        [class.active]="isOpen(i)"
        [disabled]="disabled"
        #tabHeader
      >
        {{
          tab.headerKey
            ? (tab.headerKey | cxTranslate: tab.headerParams || {})
            : tab.header
        }}
        <span
          *ngIf="mode === TAB_MODE.ACCORDIAN"
          class="tab-icon"
          aria-hidden="true"
        >
          {{ isOpen(i) ? '–' : '+' }}
        </span>
      </button>

      <!-- Show tab panels under selected button in Accordian mode -->
      <ng-container
        *ngIf="mode === TAB_MODE.ACCORDIAN"
        [ngTemplateOutlet]="tabPanel"
        [ngTemplateOutletContext]="{ tabNum: i }"
      ></ng-container>
    </ng-container>
  </div>

  <!-- Show selected tab panel under all buttons in Tab mode -->
  <ng-container *ngIf="mode === TAB_MODE.TAB">
    <ng-container
      *ngFor="let tab of tabs; let i = index"
      [ngTemplateOutlet]="tabPanel"
      [ngTemplateOutletContext]="{ tabNum: i }"
    >
    </ng-container>
  </ng-container>

  <!-- Tab Panel shows the content of the given tab -->
  <ng-template #tabPanel let-tabNum="tabNum">
    <cx-tab-panel
      [tab]="tabs[tabNum]"
      [mode]="mode"
      *ngIf="isOpen(tabNum)"
    ></cx-tab-panel>
  </ng-template>
</ng-container>
`
    }]
  }], () => [], {
    tabs$: [{
      type: Input
    }],
    tabs: [{
      type: Input
    }],
    config: [{
      type: Input
    }],
    disabled: [{
      type: Input
    }],
    tabHeaders: [{
      type: ViewChildren,
      args: ["tabHeader"]
    }]
  });
})();
var defaultTabConfig = {
  openTabs: [0],
  breakpoint: BREAKPOINT.md
};
var TabParagraphContainerComponent = class _TabParagraphContainerComponent {
  constructor(componentData, cmsService, winRef) {
    this.componentData = componentData;
    this.cmsService = cmsService;
    this.winRef = winRef;
    this.cdr = inject(ChangeDetectorRef);
    this.activeTabNum = 0;
    this.tabTitleParams = [];
    this.tabConfig$ = new BehaviorSubject(defaultTabConfig);
    this.tabTitleParams$ = new BehaviorSubject(/* @__PURE__ */ new Map());
    this.components$ = this.componentData.data$.pipe(distinctUntilChanged((x, y) => x?.components === y?.components), tap((data) => {
      this.ariaLabel = `${data?.uid}.tabPanelContainerRegion`;
    }), switchMap((data) => combineLatest((data?.components ?? "").split(" ").map((component) => this.cmsService.getComponentData(component).pipe(distinctUntilChanged(), map((tab) => {
      if (!tab) {
        return void 0;
      }
      if (!tab.flexType) {
        tab = __spreadProps(__spreadValues({}, tab), {
          flexType: tab.typeCode
        });
      }
      return __spreadProps(__spreadValues({}, tab), {
        title: `${data.uid}.tabs.${tab.uid}`
      });
    })))).pipe(
      // Update tablist label with name from CMS
      tap(() => {
        this.tabConfig$.next(__spreadValues({
          label: `${data?.uid}.tabPanelContainerRegionGroup`
        }, defaultTabConfig));
      })
    )));
  }
  /**
   * @deprecated This method will be removed.
   */
  select(tabNum, event) {
    this.activeTabNum = this.activeTabNum === tabNum ? -1 : tabNum;
    if (event && event?.target) {
      const target = event.target;
      const parentNode = target.parentNode;
      this.winRef?.nativeWindow?.scrollTo({
        left: 0,
        top: parentNode.offsetTop,
        behavior: "smooth"
      });
    }
  }
  /**
   * @deprecated This method will be removed.
   */
  ngOnInit() {
    this.cdr.markForCheck();
    this.activeTabNum = this.winRef?.nativeWindow?.history?.state?.activeTab ?? this.activeTabNum;
  }
  ngAfterViewInit() {
    if (this.children.length > 0) {
      this.getTitleParams(this.children);
    }
    this.tabs$ = combineLatest([this.components$, this.tabRefs.changes.pipe(startWith(this.tabRefs)), this.tabTitleParams$]).pipe(switchMap(([components, refs, params]) => {
      const paramObservables = components.map((component) => params.get(component.uid) || of(null));
      return combineLatest(paramObservables).pipe(map((resolvedParams) => components.map((component, index) => ({
        headerKey: component.title,
        content: refs.get(index),
        id: index,
        headerParams: {
          param: resolvedParams[index]
        }
      }))));
    }));
    this.cdr.detectChanges();
  }
  tabCompLoaded(componentRef, componentId) {
    this.tabTitleParams.push(componentRef.instance.tabTitleParam$);
    if (componentId) {
      const currentParams = this.tabTitleParams$.getValue();
      if (!currentParams.has(componentId)) {
        currentParams.set(componentId, componentRef.instance.tabTitleParam$);
        this.tabTitleParams$.next(currentParams);
      }
    }
  }
  getTitleParams(children) {
    children.forEach((comp) => {
      this.tabTitleParams.push(comp["cmpRef"]?.instance.tabTitleParam$ ?? null);
    });
  }
  static {
    this.ɵfac = function TabParagraphContainerComponent_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _TabParagraphContainerComponent)(ɵɵdirectiveInject(CmsComponentData), ɵɵdirectiveInject(CmsService), ɵɵdirectiveInject(WindowRef));
    };
  }
  static {
    this.ɵcmp = ɵɵdefineComponent({
      type: _TabParagraphContainerComponent,
      selectors: [["cx-tab-paragraph-container"]],
      viewQuery: function TabParagraphContainerComponent_Query(rf, ctx) {
        if (rf & 1) {
          ɵɵviewQuery(ComponentWrapperDirective, 5);
          ɵɵviewQuery(_c422, 5);
        }
        if (rf & 2) {
          let _t;
          ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.children = _t);
          ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.tabRefs = _t);
        }
      },
      standalone: false,
      decls: 2,
      vars: 3,
      consts: [["tabRef", ""], [4, "ngIf"], [3, "tabs", "config"], [4, "ngFor", "ngForOf"], [3, "cxOutlet"], [3, "cxComponentRef", "cxComponentWrapper"]],
      template: function TabParagraphContainerComponent_Template(rf, ctx) {
        if (rf & 1) {
          ɵɵtemplate(0, TabParagraphContainerComponent_ng_container_0_Template, 5, 7, "ng-container", 1);
          ɵɵpipe(1, "async");
        }
        if (rf & 2) {
          ɵɵproperty("ngIf", ɵɵpipeBind1(1, 1, ctx.components$));
        }
      },
      dependencies: [NgForOf, NgIf, ComponentWrapperDirective, OutletDirective, TabComponent, AsyncPipe],
      encapsulation: 2,
      changeDetection: 0
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(TabParagraphContainerComponent, [{
    type: Component,
    args: [{
      selector: "cx-tab-paragraph-container",
      changeDetection: ChangeDetectionStrategy.OnPush,
      standalone: false,
      template: '<ng-container *ngIf="components$ | async as components">\n  <cx-tab [tabs]="tabs$ | async" [config]="tabConfig$ | async">\n    <ng-template\n      *ngFor="let component of components; let i = index"\n      [cxOutlet]="component.flexType"\n      #tabRef\n    >\n      <ng-container\n        [cxComponentWrapper]="component"\n        (cxComponentRef)="tabCompLoaded($event, component.uid)"\n      ></ng-container>\n    </ng-template>\n  </cx-tab>\n</ng-container>\n'
    }]
  }], () => [{
    type: CmsComponentData
  }, {
    type: CmsService
  }, {
    type: WindowRef
  }], {
    children: [{
      type: ViewChildren,
      args: [ComponentWrapperDirective]
    }],
    tabRefs: [{
      type: ViewChildren,
      args: ["tabRef"]
    }]
  });
})();
var TabPanelModule = class _TabPanelModule {
  static {
    this.ɵfac = function TabPanelModule_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _TabPanelModule)();
    };
  }
  static {
    this.ɵmod = ɵɵdefineNgModule({
      type: _TabPanelModule,
      declarations: [TabPanelComponent],
      imports: [CommonModule, PageComponentModule, OutletModule, I18nModule, KeyboardFocusModule],
      exports: [TabPanelComponent]
    });
  }
  static {
    this.ɵinj = ɵɵdefineInjector({
      imports: [CommonModule, PageComponentModule, OutletModule, I18nModule, KeyboardFocusModule]
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(TabPanelModule, [{
    type: NgModule,
    args: [{
      imports: [CommonModule, PageComponentModule, OutletModule, I18nModule, KeyboardFocusModule],
      declarations: [TabPanelComponent],
      exports: [TabPanelComponent]
    }]
  }], null, null);
})();
var TabModule = class _TabModule {
  static {
    this.ɵfac = function TabModule_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _TabModule)();
    };
  }
  static {
    this.ɵmod = ɵɵdefineNgModule({
      type: _TabModule,
      declarations: [TabComponent],
      imports: [CommonModule, I18nModule, TabPanelModule],
      exports: [TabComponent]
    });
  }
  static {
    this.ɵinj = ɵɵdefineInjector({
      imports: [CommonModule, I18nModule, TabPanelModule]
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(TabModule, [{
    type: NgModule,
    args: [{
      imports: [CommonModule, I18nModule, TabPanelModule],
      declarations: [TabComponent],
      exports: [TabComponent]
    }]
  }], null, null);
})();
var TabParagraphContainerModule = class _TabParagraphContainerModule {
  static {
    this.ɵfac = function TabParagraphContainerModule_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _TabParagraphContainerModule)();
    };
  }
  static {
    this.ɵmod = ɵɵdefineNgModule({
      type: _TabParagraphContainerModule,
      declarations: [TabParagraphContainerComponent],
      imports: [CommonModule, PageComponentModule, OutletModule, I18nModule, TabModule, FeaturesConfigModule],
      exports: [TabParagraphContainerComponent]
    });
  }
  static {
    this.ɵinj = ɵɵdefineInjector({
      providers: [provideDefaultConfig({
        cmsComponents: {
          CMSTabParagraphContainer: {
            component: TabParagraphContainerComponent
          }
        }
      })],
      imports: [CommonModule, PageComponentModule, OutletModule, I18nModule, TabModule, FeaturesConfigModule]
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(TabParagraphContainerModule, [{
    type: NgModule,
    args: [{
      imports: [CommonModule, PageComponentModule, OutletModule, I18nModule, TabModule, FeaturesConfigModule],
      providers: [provideDefaultConfig({
        cmsComponents: {
          CMSTabParagraphContainer: {
            component: TabParagraphContainerComponent
          }
        }
      })],
      declarations: [TabParagraphContainerComponent],
      exports: [TabParagraphContainerComponent]
    }]
  }], null, null);
})();
var VideoComponent = class _VideoComponent {
  constructor(component, mediaService, urlService, cmsService, cd) {
    this.component = component;
    this.mediaService = mediaService;
    this.urlService = urlService;
    this.cmsService = cmsService;
    this.cd = cd;
    this.data$ = this.component.data$.pipe(distinctUntilChanged(), tap((data) => {
      this.styleClasses = data.styleClasses;
      this.setMedia(data);
      this.setControls(data);
      this.setRouting(data);
    }));
  }
  setMedia(data) {
    if (data.video) {
      this.source = this.mediaService.getMedia(data.video)?.src;
    }
    if (data?.containerBackground === ContainerBackgroundOptions.UPLOAD_RESPONSIVE_IMAGE && data?.videoMedia) {
      this.thumbnail = this.mediaService.getMedia(data.videoMedia);
    } else if (data?.thumbnailSelector === ContainerBackgroundOptions.UPLOAD_THUMBNAIL && data?.thumbnail) {
      this.thumbnail = this.mediaService.getMedia(data.thumbnail);
    } else {
      this.thumbnail = void 0;
    }
  }
  setControls(data) {
    this.autoPlay = data.autoPlay === "true";
    this.loop = data.loop === "true";
    this.mute = data.mute === "true" ? "muted" : void 0;
  }
  setRouting(data) {
    if (data.url) {
      this.routerLink = data.url;
    } else if (data.contentPage) {
      this.cmsService.getPage({
        id: data.contentPage,
        type: PageType.CONTENT_PAGE
      }).pipe(take(1)).subscribe((page) => {
        this.routerLink = page?.label;
        this.cd.markForCheck();
      });
    } else if (data.product) {
      this.routerLink = this.urlService.transform({
        cxRoute: "product",
        params: {
          code: data.product
        }
      });
    } else if (data.category) {
      this.routerLink = this.urlService.transform({
        cxRoute: "category",
        params: {
          code: data.category
        }
      });
    }
  }
  static {
    this.ɵfac = function VideoComponent_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _VideoComponent)(ɵɵdirectiveInject(CmsComponentData), ɵɵdirectiveInject(MediaService), ɵɵdirectiveInject(SemanticPathService), ɵɵdirectiveInject(CmsService), ɵɵdirectiveInject(ChangeDetectorRef));
    };
  }
  static {
    this.ɵcmp = ɵɵdefineComponent({
      type: _VideoComponent,
      selectors: [["cx-video"]],
      hostVars: 2,
      hostBindings: function VideoComponent_HostBindings(rf, ctx) {
        if (rf & 2) {
          ɵɵclassMap(ctx.styleClasses);
        }
      },
      standalone: false,
      decls: 4,
      vars: 4,
      consts: [["loading", ""], ["class", "video-container", "tabindex", "-1", 4, "ngIf", "ngIfElse"], ["tabindex", "-1", 1, "video-container"], [3, "routerLink", 4, "ngIf"], ["controls", "", 3, "height", "poster", "loop", "autoplay", "muted", "src", 4, "ngIf"], [3, "routerLink"], ["controls", "", 3, "poster", "loop", "autoplay", "muted", "src"], [1, "cx-spinner"]],
      template: function VideoComponent_Template(rf, ctx) {
        if (rf & 1) {
          ɵɵtemplate(0, VideoComponent_div_0_Template, 3, 2, "div", 1);
          ɵɵpipe(1, "async");
          ɵɵtemplate(2, VideoComponent_ng_template_2_Template, 2, 0, "ng-template", null, 0, ɵɵtemplateRefExtractor);
        }
        if (rf & 2) {
          const loading_r3 = ɵɵreference(3);
          ɵɵproperty("ngIf", ɵɵpipeBind1(1, 2, ctx.data$))("ngIfElse", loading_r3);
        }
      },
      dependencies: [NgIf, RouterLink, SpinnerComponent, AsyncPipe, TranslatePipe],
      encapsulation: 2,
      changeDetection: 0
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(VideoComponent, [{
    type: Component,
    args: [{
      selector: "cx-video",
      changeDetection: ChangeDetectionStrategy.OnPush,
      standalone: false,
      template: `<div
  *ngIf="data$ | async as data; else loading"
  class="video-container"
  tabindex="-1"
>
  <a *ngIf="data.overlayTitle" [routerLink]="routerLink">{{
    data.overlayTitle
  }}</a>
  <video
    *ngIf="source"
    [style.height.px]="data.videoContainerHeight"
    controls
    [poster]="thumbnail?.src"
    [loop]="loop"
    [autoplay]="autoPlay"
    [muted]="mute"
    [attr.aria-label]="'player.label' | cxTranslate"
    [src]="source"
  ></video>
</div>

<ng-template #loading>
  <div class="cx-spinner"><cx-spinner></cx-spinner></div>
</ng-template>
`
    }]
  }], () => [{
    type: CmsComponentData
  }, {
    type: MediaService
  }, {
    type: SemanticPathService
  }, {
    type: CmsService
  }, {
    type: ChangeDetectorRef
  }], {
    styleClasses: [{
      type: HostBinding,
      args: ["class"]
    }]
  });
})();
var VideoModule = class _VideoModule {
  static {
    this.ɵfac = function VideoModule_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _VideoModule)();
    };
  }
  static {
    this.ɵmod = ɵɵdefineNgModule({
      type: _VideoModule,
      declarations: [VideoComponent],
      imports: [CommonModule, RouterModule, GenericLinkModule, MediaModule, I18nModule, SpinnerModule],
      exports: [VideoComponent]
    });
  }
  static {
    this.ɵinj = ɵɵdefineInjector({
      providers: [provideDefaultConfig({
        cmsComponents: {
          VideoComponent: {
            component: VideoComponent
          }
        }
      })],
      imports: [CommonModule, RouterModule, GenericLinkModule, MediaModule, I18nModule, SpinnerModule]
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(VideoModule, [{
    type: NgModule,
    args: [{
      imports: [CommonModule, RouterModule, GenericLinkModule, MediaModule, I18nModule, SpinnerModule],
      providers: [provideDefaultConfig({
        cmsComponents: {
          VideoComponent: {
            component: VideoComponent
          }
        }
      })],
      declarations: [VideoComponent],
      exports: [VideoComponent]
    }]
  }], null, null);
})();
var MyCouponsComponentService = class _MyCouponsComponentService {
  constructor(routingService, translation) {
    this.routingService = routingService;
    this.translation = translation;
    this.RELEVANCE = ":relevance";
    this.CUSTOMER_COUPON_CODE = ":customerCouponCode:";
  }
  launchSearchPage(coupon) {
    this.routingService.go({
      cxRoute: "search",
      params: {
        query: this.buildSearchParam(coupon)
      }
    }, {
      queryParams: {
        couponcode: coupon.couponId
      }
    });
  }
  buildSearchParam(coupon) {
    return coupon.allProductsApplicable ? this.RELEVANCE : this.RELEVANCE + this.CUSTOMER_COUPON_CODE + coupon.couponId;
  }
  getSortLabels() {
    return combineLatest([this.translation.translate("myCoupons.startDateAsc"), this.translation.translate("myCoupons.startDateDesc"), this.translation.translate("myCoupons.endDateAsc"), this.translation.translate("myCoupons.endDateDesc")]).pipe(map(([textByStartDateAsc, textByStartDateDesc, textByEndDateAsc, textByEndDateDesc]) => {
      return {
        byStartDateAsc: textByStartDateAsc,
        byStartDateDesc: textByStartDateDesc,
        byEndDateAsc: textByEndDateAsc,
        byEndDateDesc: textByEndDateDesc
      };
    }));
  }
  static {
    this.ɵfac = function MyCouponsComponentService_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _MyCouponsComponentService)(ɵɵinject(RoutingService), ɵɵinject(TranslationService));
    };
  }
  static {
    this.ɵprov = ɵɵdefineInjectable({
      token: _MyCouponsComponentService,
      factory: _MyCouponsComponentService.ɵfac,
      providedIn: "root"
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(MyCouponsComponentService, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], () => [{
    type: RoutingService
  }, {
    type: TranslationService
  }], null);
})();
var CouponCardComponent = class _CouponCardComponent {
  constructor(myCouponsComponentService, launchDialogService, vcr) {
    this.myCouponsComponentService = myCouponsComponentService;
    this.launchDialogService = launchDialogService;
    this.vcr = vcr;
    this.notificationChanged = new EventEmitter();
  }
  onSubscriptionChange() {
    this.notificationChanged.emit({
      couponId: this.coupon.couponId ?? "",
      notification: !this.coupon.notificationOn
    });
  }
  readMore() {
    const dialog = this.launchDialogService.openDialog(LAUNCH_CALLER.COUPON, this.element, this.vcr, {
      coupon: this.coupon
    });
    if (dialog) {
      dialog.pipe(take(1)).subscribe();
    }
  }
  findProducts() {
    this.myCouponsComponentService.launchSearchPage(this.coupon);
  }
  static {
    this.ɵfac = function CouponCardComponent_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _CouponCardComponent)(ɵɵdirectiveInject(MyCouponsComponentService), ɵɵdirectiveInject(LaunchDialogService), ɵɵdirectiveInject(ViewContainerRef));
    };
  }
  static {
    this.ɵcmp = ɵɵdefineComponent({
      type: _CouponCardComponent,
      selectors: [["cx-coupon-card"]],
      viewQuery: function CouponCardComponent_Query(rf, ctx) {
        if (rf & 1) {
          ɵɵviewQuery(_c43, 5);
        }
        if (rf & 2) {
          let _t;
          ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.element = _t.first);
        }
      },
      inputs: {
        coupon: "coupon",
        couponSubscriptionLoading$: "couponSubscriptionLoading$"
      },
      outputs: {
        notificationChanged: "notificationChanged"
      },
      standalone: false,
      decls: 40,
      vars: 38,
      consts: [[1, "card"], [1, "card-body", "cx-card-body"], [1, "cx-coupon-data"], [1, "cx-coupon-card-row", "top"], [1, "cx-coupon-card-head"], [1, "card-label-bold", "cx-coupon-card-id"], [1, "cx-coupon-card-date"], [1, "cx-coupon-date"], [1, "cx-coupon-date-start"], [1, "cx-coupon-date-end"], [1, "cx-card-read-more", "link", "cx-action-link", 3, "click"], [1, "cx-coupon-card-row", "bottom"], [1, "cx-coupon-notification", "form-check"], ["type", "checkbox", 1, "form-check-input", 3, "change", "checked", "disabled"], [1, "form-check-label"], [1, "cx-coupon-find-product", "col-lg-6", "col-md-12", "col-sm-6"], [1, "btn", "btn-block", "btn-secondary", 3, "click"]],
      template: function CouponCardComponent_Template(rf, ctx) {
        if (rf & 1) {
          ɵɵelementStart(0, "div", 0)(1, "div", 1)(2, "div", 2)(3, "div", 3)(4, "div", 4)(5, "span", 5);
          ɵɵtext(6);
          ɵɵelementEnd();
          ɵɵelementStart(7, "span");
          ɵɵtext(8);
          ɵɵelementEnd()();
          ɵɵelementStart(9, "div");
          ɵɵpipe(10, "lowercase");
          ɵɵtext(11);
          ɵɵpipe(12, "cxTranslate");
          ɵɵelementEnd()();
          ɵɵelementStart(13, "div", 6)(14, "p");
          ɵɵtext(15);
          ɵɵpipe(16, "cxTranslate");
          ɵɵelementEnd();
          ɵɵelementStart(17, "div", 7)(18, "div", 8);
          ɵɵtext(19);
          ɵɵpipe(20, "cxDate");
          ɵɵelementEnd();
          ɵɵelementStart(21, "div", 9);
          ɵɵtext(22);
          ɵɵpipe(23, "cxDate");
          ɵɵelementEnd()()();
          ɵɵelementStart(24, "button", 10);
          ɵɵlistener("click", function CouponCardComponent_Template_button_click_24_listener() {
            return ctx.readMore();
          });
          ɵɵtext(25);
          ɵɵpipe(26, "cxTranslate");
          ɵɵelementEnd();
          ɵɵelementStart(27, "div", 11)(28, "div", 12)(29, "label")(30, "input", 13);
          ɵɵpipe(31, "async");
          ɵɵpipe(32, "async");
          ɵɵlistener("change", function CouponCardComponent_Template_input_change_30_listener() {
            return ctx.onSubscriptionChange();
          });
          ɵɵelementEnd();
          ɵɵelementStart(33, "span", 14);
          ɵɵtext(34);
          ɵɵpipe(35, "cxTranslate");
          ɵɵelementEnd()()();
          ɵɵelementStart(36, "div", 15)(37, "button", 16);
          ɵɵlistener("click", function CouponCardComponent_Template_button_click_37_listener() {
            return ctx.findProducts();
          });
          ɵɵtext(38);
          ɵɵpipe(39, "cxTranslate");
          ɵɵelementEnd()()()()()();
        }
        if (rf & 2) {
          ɵɵadvance(6);
          ɵɵtextInterpolate(ctx.coupon == null ? null : ctx.coupon.couponId);
          ɵɵadvance(2);
          ɵɵtextInterpolate1(": ", ctx.coupon == null ? null : ctx.coupon.name, "");
          ɵɵadvance();
          ɵɵclassMapInterpolate1("cx-coupon-status ", ɵɵpipeBind1(10, 16, ctx.coupon == null ? null : ctx.coupon.status), "");
          ɵɵadvance(2);
          ɵɵtextInterpolate1(" ", ɵɵpipeBind1(12, 18, "myCoupons." + (ctx.coupon == null ? null : ctx.coupon.status)), " ");
          ɵɵadvance(4);
          ɵɵtextInterpolate(ɵɵpipeBind1(16, 20, "myCoupons.effectiveTitle"));
          ɵɵadvance(4);
          ɵɵtextInterpolate1(" ", ɵɵpipeBind2(20, 22, ctx.coupon == null ? null : ctx.coupon.startDate, "medium"), " -  ");
          ɵɵadvance(3);
          ɵɵtextInterpolate1(" ", ɵɵpipeBind2(23, 25, ctx.coupon == null ? null : ctx.coupon.endDate, "medium"), " ");
          ɵɵadvance(3);
          ɵɵtextInterpolate1(" ", ɵɵpipeBind1(26, 28, "myCoupons.readMore"), " ");
          ɵɵadvance(5);
          ɵɵclassProp("disabled", ɵɵpipeBind1(31, 30, ctx.couponSubscriptionLoading$));
          ɵɵproperty("checked", ctx.coupon == null ? null : ctx.coupon.notificationOn)("disabled", ɵɵpipeBind1(32, 32, ctx.couponSubscriptionLoading$));
          ɵɵadvance(4);
          ɵɵtextInterpolate1(" ", ɵɵpipeBind1(35, 34, "myCoupons.notification"), " ");
          ɵɵadvance(4);
          ɵɵtextInterpolate1(" ", ɵɵpipeBind1(39, 36, "myCoupons.findProducts"), " ");
        }
      },
      dependencies: [AsyncPipe, LowerCasePipe, TranslatePipe, CxDatePipe],
      encapsulation: 2,
      changeDetection: 0
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(CouponCardComponent, [{
    type: Component,
    args: [{
      selector: "cx-coupon-card",
      changeDetection: ChangeDetectionStrategy.OnPush,
      standalone: false,
      template: `<div class="card">
  <div class="card-body cx-card-body">
    <div class="cx-coupon-data">
      <div class="cx-coupon-card-row top">
        <div class="cx-coupon-card-head">
          <span class="card-label-bold cx-coupon-card-id">{{
            coupon?.couponId
          }}</span>
          <span>: {{ coupon?.name }}</span>
        </div>

        <div class="cx-coupon-status {{ coupon?.status | lowercase }}">
          {{ 'myCoupons.' + coupon?.status | cxTranslate }}
        </div>
      </div>

      <div class="cx-coupon-card-date">
        <p>{{ 'myCoupons.effectiveTitle' | cxTranslate }}</p>
        <div class="cx-coupon-date">
          <div class="cx-coupon-date-start">
            {{ coupon?.startDate | cxDate: 'medium' }} -&nbsp;
          </div>
          <div class="cx-coupon-date-end">
            {{ coupon?.endDate | cxDate: 'medium' }}
          </div>
        </div>
      </div>
      <button
        class="cx-card-read-more link cx-action-link"
        (click)="readMore()"
      >
        {{ 'myCoupons.readMore' | cxTranslate }}
      </button>
      <div class="cx-coupon-card-row bottom">
        <div class="cx-coupon-notification form-check">
          <label>
            <input
              type="checkbox"
              class="form-check-input"
              [checked]="coupon?.notificationOn"
              [class.disabled]="couponSubscriptionLoading$ | async"
              [disabled]="couponSubscriptionLoading$ | async"
              (change)="onSubscriptionChange()"
            />
            <span class="form-check-label">
              {{ 'myCoupons.notification' | cxTranslate }}
            </span>
          </label>
        </div>

        <div class="cx-coupon-find-product col-lg-6 col-md-12 col-sm-6">
          <button class="btn btn-block btn-secondary" (click)="findProducts()">
            {{ 'myCoupons.findProducts' | cxTranslate }}
          </button>
        </div>
      </div>
    </div>
  </div>
</div>
`
    }]
  }], () => [{
    type: MyCouponsComponentService
  }, {
    type: LaunchDialogService
  }, {
    type: ViewContainerRef
  }], {
    coupon: [{
      type: Input
    }],
    couponSubscriptionLoading$: [{
      type: Input
    }],
    notificationChanged: [{
      type: Output
    }],
    element: [{
      type: ViewChild,
      args: ["element"]
    }]
  });
})();
var MyCouponsComponent = class _MyCouponsComponent {
  constructor(couponService, myCouponsComponentService) {
    this.couponService = couponService;
    this.myCouponsComponentService = myCouponsComponentService;
    this.iconTypes = ICON_TYPE;
    this.subscriptions = new Subscription();
    this.PAGE_SIZE = 10;
    this.sortMapping = {
      byStartDateAsc: "startDate:asc",
      byStartDateDesc: "startDate:desc",
      byEndDateAsc: "endDate:asc",
      byEndDateDesc: "endDate:desc"
    };
    this.sort = "byStartDateAsc";
    this.sortOptions = [{
      code: "byStartDateAsc",
      selected: false
    }, {
      code: "byStartDateDesc",
      selected: false
    }, {
      code: "byEndDateAsc",
      selected: false
    }, {
      code: "byEndDateDesc",
      selected: false
    }];
    this.launchDialogService = inject(LaunchDialogService);
  }
  ngOnInit() {
    this.couponResult$ = this.couponService.getCustomerCoupons(this.PAGE_SIZE).pipe(tap((coupons) => this.pagination = {
      currentPage: coupons.pagination?.page,
      pageSize: coupons.pagination?.count,
      totalPages: coupons.pagination?.totalPages,
      totalResults: coupons.pagination?.totalCount,
      sort: this.sort
    }));
    this.couponsLoading$ = this.couponService.getCustomerCouponsLoading();
    this.couponSubscriptionLoading$ = combineLatest([this.couponService.getSubscribeCustomerCouponResultLoading(), this.couponService.getUnsubscribeCustomerCouponResultLoading()]).pipe(map(([subscribing, unsubscribing]) => subscribing || unsubscribing));
    this.sortLabels = this.myCouponsComponentService.getSortLabels();
    this.subscriptions.add(this.couponService.getSubscribeCustomerCouponResultError().subscribe((error) => {
      this.subscriptionFail(error);
    }));
    this.subscriptions.add(this.couponService.getUnsubscribeCustomerCouponResultError().subscribe((error) => {
      this.subscriptionFail(error);
    }));
    const resultStr = decodeURIComponent(this.getHashStr());
    const index = resultStr.indexOf("#");
    if (index !== -1) {
      const couponCode = resultStr.substring(index + 1);
      if (couponCode !== void 0 && couponCode.length > 0) {
        this.launchDialogService.openDialogAndSubscribe(LAUNCH_CALLER.CLAIM_DIALOG, void 0, {
          coupon: couponCode,
          pageSize: this.PAGE_SIZE
        });
      }
    }
  }
  getHashStr() {
    return location.hash;
  }
  subscriptionFail(error) {
    if (error) {
      this.couponService.loadCustomerCoupons(this.PAGE_SIZE);
    }
  }
  sortChange(sort) {
    this.sort = sort;
    this.couponService.loadCustomerCoupons(this.PAGE_SIZE, this.pagination.currentPage, this.sortMapping[sort]);
  }
  pageChange(page) {
    this.couponService.loadCustomerCoupons(this.PAGE_SIZE, page, this.sortMapping[this.sort]);
  }
  notificationChange({
    couponId,
    notification
  }) {
    if (notification) {
      this.couponService.subscribeCustomerCoupon(couponId);
    } else {
      this.couponService.unsubscribeCustomerCoupon(couponId);
    }
  }
  ngOnDestroy() {
    this.subscriptions.unsubscribe();
  }
  static {
    this.ɵfac = function MyCouponsComponent_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _MyCouponsComponent)(ɵɵdirectiveInject(CustomerCouponService), ɵɵdirectiveInject(MyCouponsComponentService));
    };
  }
  static {
    this.ɵcmp = ɵɵdefineComponent({
      type: _MyCouponsComponent,
      selectors: [["cx-my-coupons"]],
      standalone: false,
      decls: 5,
      vars: 4,
      consts: [["loading", ""], ["noCoupons", ""], [1, "cx-section"], [4, "ngIf", "ngIfElse"], [4, "ngIf"], [1, "cx-my-coupons-header"], [1, "cx-my-coupons-sort", "top", "row"], [1, "cx-my-coupons-form-group", "form-group", "col-sm-12", "col-md-4", "col-lg-4"], ["ariaControls", "coupon-deck", 3, "sortListEvent", "sortOptions", "sortLabels", "selectedOption", "ariaLabel"], [1, "cx-my-coupons-pagination", "cx-mycoupon-thead-mobile"], ["paginationID", "pagination-top", 3, "viewPageEvent", "pagination"], ["id", "coupon-deck", 1, "row", "cx-coupon-deck"], ["class", "col-md-6 cx-coupon-card", 4, "ngFor", "ngForOf"], [1, "cx-my-coupons-sort", "bottom", "row"], [1, "cx-my-coupons-form-group", "form-group", "cx-mycoupon-thead-mobile", "col-sm-12", "col-md-4", "col-lg-4"], ["ariaControls", "coupon-deck", 3, "sortListEvent", "sortOptions", "sortLabels", "selectedOption", "placeholder", "ariaLabel"], [1, "cx-my-coupons-pagination"], ["paginationID", "pagination-bottom", 3, "viewPageEvent", "pagination"], [1, "cx-my-coupons-notes"], [3, "type"], [3, "routerLink"], [1, "col-md-6", "cx-coupon-card"], [3, "notificationChanged", "coupon", "couponSubscriptionLoading$"], [1, "cx-section-msg"], [1, "col-md-12", "cx-coupon-spinner"]],
      template: function MyCouponsComponent_Template(rf, ctx) {
        if (rf & 1) {
          ɵɵelementStart(0, "div", 2);
          ɵɵtemplate(1, MyCouponsComponent_ng_container_1_Template, 5, 3, "ng-container", 3);
          ɵɵpipe(2, "async");
          ɵɵtemplate(3, MyCouponsComponent_ng_template_3_Template, 2, 0, "ng-template", null, 0, ɵɵtemplateRefExtractor);
          ɵɵelementEnd();
        }
        if (rf & 2) {
          const loading_r7 = ɵɵreference(4);
          ɵɵadvance();
          ɵɵproperty("ngIf", !ɵɵpipeBind1(2, 2, ctx.couponsLoading$))("ngIfElse", loading_r7);
        }
      },
      dependencies: [NgForOf, NgIf, SpinnerComponent, RouterLink, IconComponent, SortingComponent, PaginationComponent, CouponCardComponent, AsyncPipe, TranslatePipe],
      encapsulation: 2
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(MyCouponsComponent, [{
    type: Component,
    args: [{
      selector: "cx-my-coupons",
      standalone: false,
      template: `<div class="cx-section">
  <ng-container *ngIf="!(couponsLoading$ | async); else loading">
    <ng-container *ngIf="couponResult$ | async as couponResult">
      <div class="cx-my-coupons-header">
        <h2>
          {{ 'myCoupons.myCoupons' | cxTranslate }}
        </h2>
      </div>
      <ng-container
        *ngIf="
          couponResult.pagination &&
            couponResult.pagination.totalCount &&
            couponResult.pagination.totalCount > 0;
          else noCoupons
        "
      >
        <div class="cx-my-coupons-sort top row">
          <label
            class="cx-my-coupons-form-group form-group col-sm-12 col-md-4 col-lg-4"
          >
            <span>{{ 'myCoupons.sortBy' | cxTranslate }}</span>
            <cx-sorting
              [sortOptions]="sortOptions"
              [sortLabels]="sortLabels | async"
              (sortListEvent)="sortChange($event)"
              [selectedOption]="sort"
              [ariaLabel]="'myCoupons.sortCoupons' | cxTranslate"
              ariaControls="coupon-deck"
            >
            </cx-sorting>
          </label>
          <div class="cx-my-coupons-pagination cx-mycoupon-thead-mobile">
            <cx-pagination
              [pagination]="pagination"
              (viewPageEvent)="pageChange($event)"
              paginationID="pagination-top"
            ></cx-pagination>
          </div>
        </div>

        <div id="coupon-deck" class="row cx-coupon-deck">
          <div
            *ngFor="let coupon of couponResult.coupons"
            class="col-md-6 cx-coupon-card"
          >
            <cx-coupon-card
              [coupon]="coupon"
              [couponSubscriptionLoading$]="couponSubscriptionLoading$"
              (notificationChanged)="notificationChange($event)"
            ></cx-coupon-card>
          </div>
        </div>

        <div class="cx-my-coupons-sort bottom row">
          <label
            class="cx-my-coupons-form-group form-group cx-mycoupon-thead-mobile col-sm-12 col-md-4 col-lg-4"
          >
            <span>{{ 'myCoupons.sortBy' | cxTranslate }}</span>
            <cx-sorting
              [sortOptions]="sortOptions"
              [sortLabels]="sortLabels | async"
              (sortListEvent)="sortChange($event)"
              [selectedOption]="sort"
              placeholder="{{ 'myCoupons.sortBy' | cxTranslate }}"
              [ariaLabel]="'myCoupons.sortCoupons' | cxTranslate"
              ariaControls="coupon-deck"
            >
            </cx-sorting>
          </label>
          <div class="cx-my-coupons-pagination">
            <cx-pagination
              [pagination]="pagination"
              (viewPageEvent)="pageChange($event)"
              paginationID="pagination-bottom"
            ></cx-pagination>
          </div>
        </div>
        <div class="cx-my-coupons-notes">
          <span>
            <cx-icon [type]="iconTypes.INFO"></cx-icon>
            {{ 'myCoupons.notesPreffix' | cxTranslate
            }}<a [routerLink]="['/my-account/notification-preference']">{{
              'myCoupons.notesLink' | cxTranslate
            }}</a
            >{{ 'myCoupons.notesSuffix' | cxTranslate }}</span
          >
        </div>
      </ng-container>
    </ng-container>

    <ng-template #noCoupons>
      <section>
        <p class="cx-section-msg">
          {{ 'myCoupons.noCouponsMessage' | cxTranslate }}
        </p>
      </section>
    </ng-template>
  </ng-container>

  <ng-template #loading>
    <div class="col-md-12 cx-coupon-spinner">
      <cx-spinner></cx-spinner>
    </div>
  </ng-template>
</div>
`
    }]
  }], () => [{
    type: CustomerCouponService
  }, {
    type: MyCouponsComponentService
  }], null);
})();
var CouponDialogComponent = class _CouponDialogComponent {
  handleClick(event) {
    if (event.target.tagName === this.el.nativeElement.tagName) {
      this.close("Cross click");
    }
  }
  constructor(launchDialogService, el) {
    this.launchDialogService = launchDialogService;
    this.el = el;
    this.subscription = new Subscription();
    this.iconTypes = ICON_TYPE;
    this.focusConfig = {
      trap: true,
      block: true,
      autofocus: "button",
      focusOnEscape: true
    };
  }
  ngOnInit() {
    this.subscription.add(this.launchDialogService.data$.subscribe((data) => {
      if (data) {
        this.coupon = data.coupon;
      }
    }));
  }
  ngOnDestroy() {
    this.subscription?.unsubscribe();
  }
  close(reason) {
    this.launchDialogService.closeDialog(reason);
  }
  static {
    this.ɵfac = function CouponDialogComponent_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _CouponDialogComponent)(ɵɵdirectiveInject(LaunchDialogService), ɵɵdirectiveInject(ElementRef));
    };
  }
  static {
    this.ɵcmp = ɵɵdefineComponent({
      type: _CouponDialogComponent,
      selectors: [["cx-coupon-dialog"]],
      hostBindings: function CouponDialogComponent_HostBindings(rf, ctx) {
        if (rf & 1) {
          ɵɵlistener("click", function CouponDialogComponent_click_HostBindingHandler($event) {
            return ctx.handleClick($event);
          });
        }
      },
      standalone: false,
      decls: 37,
      vars: 36,
      consts: [["role", "dialog", "aria-labelledby", "dialogTitle", 1, "modal-dialog", "modal-dialog-centered", "cx-coupon-dialog", 3, "esc", "cxFocus"], [1, "modal-content", "cx-coupon-container"], [1, "cx-dialog-header", "modal-header"], ["id", "dialogTitle", 1, "cx-dialog-title", "modal-title"], ["type", "button", 1, "close", 3, "click", "title"], ["aria-hidden", "true"], [3, "type"], [1, "cx-dialog-body", "modal-body"], [1, "cx-dialog-row"], [1, "cx-dialog-item", "col-sm-12", "col-md-12"], [1, "cx-coupon-card-head"], [1, "card-label-bold", "cx-coupon-card-id"], [1, "cx-coupon-description"], [1, "cx-coupon-dialog-date"], [1, "cx-coupon-date"], [1, "cx-coupon-dialog-status"]],
      template: function CouponDialogComponent_Template(rf, ctx) {
        if (rf & 1) {
          ɵɵelementStart(0, "div", 0);
          ɵɵlistener("esc", function CouponDialogComponent_Template_div_esc_0_listener() {
            return ctx.close("Escape clicked");
          });
          ɵɵelementStart(1, "div", 1)(2, "div", 2)(3, "h3", 3);
          ɵɵtext(4);
          ɵɵpipe(5, "cxTranslate");
          ɵɵelementEnd();
          ɵɵelementStart(6, "button", 4);
          ɵɵpipe(7, "cxTranslate");
          ɵɵpipe(8, "cxTranslate");
          ɵɵlistener("click", function CouponDialogComponent_Template_button_click_6_listener() {
            return ctx.close("Cross click");
          });
          ɵɵelementStart(9, "span", 5);
          ɵɵelement(10, "cx-icon", 6);
          ɵɵelementEnd()()();
          ɵɵelementStart(11, "div", 7)(12, "div", 8)(13, "div", 9)(14, "div", 10)(15, "span", 11);
          ɵɵtext(16);
          ɵɵelementEnd();
          ɵɵelementStart(17, "span");
          ɵɵtext(18);
          ɵɵelementEnd()();
          ɵɵelementStart(19, "div", 12);
          ɵɵtext(20);
          ɵɵelementEnd();
          ɵɵelementStart(21, "div", 13)(22, "p");
          ɵɵtext(23);
          ɵɵpipe(24, "cxTranslate");
          ɵɵelementEnd();
          ɵɵelementStart(25, "div", 14);
          ɵɵtext(26);
          ɵɵpipe(27, "cxDate");
          ɵɵpipe(28, "cxDate");
          ɵɵelementEnd()();
          ɵɵelementStart(29, "div", 15)(30, "p");
          ɵɵtext(31);
          ɵɵpipe(32, "cxTranslate");
          ɵɵelementEnd();
          ɵɵelementStart(33, "div");
          ɵɵpipe(34, "lowercase");
          ɵɵtext(35);
          ɵɵpipe(36, "cxTranslate");
          ɵɵelementEnd()()()()()()();
        }
        if (rf & 2) {
          ɵɵproperty("cxFocus", ctx.focusConfig);
          ɵɵadvance(4);
          ɵɵtextInterpolate1(" ", ɵɵpipeBind1(5, 16, "myCoupons.dialogTitle"), " ");
          ɵɵadvance(2);
          ɵɵpropertyInterpolate("title", ɵɵpipeBind1(7, 18, "common.close"));
          ɵɵattribute("aria-label", ɵɵpipeBind1(8, 20, "common.close"));
          ɵɵadvance(4);
          ɵɵproperty("type", ctx.iconTypes.CLOSE);
          ɵɵadvance(6);
          ɵɵtextInterpolate(ctx.coupon == null ? null : ctx.coupon.couponId);
          ɵɵadvance(2);
          ɵɵtextInterpolate1(": ", ctx.coupon == null ? null : ctx.coupon.name, "");
          ɵɵadvance(2);
          ɵɵtextInterpolate(ctx.coupon == null ? null : ctx.coupon.description);
          ɵɵadvance(3);
          ɵɵtextInterpolate(ɵɵpipeBind1(24, 22, "myCoupons.effectiveTitle"));
          ɵɵadvance(3);
          ɵɵtextInterpolate2(" ", ɵɵpipeBind2(27, 24, ctx.coupon == null ? null : ctx.coupon.startDate, "medium"), " - ", ɵɵpipeBind2(28, 27, ctx.coupon == null ? null : ctx.coupon.endDate, "medium"), " ");
          ɵɵadvance(5);
          ɵɵtextInterpolate(ɵɵpipeBind1(32, 30, "myCoupons.status"));
          ɵɵadvance(2);
          ɵɵclassMapInterpolate1("cx-coupon-status ", ɵɵpipeBind1(34, 32, ctx.coupon == null ? null : ctx.coupon.status), "");
          ɵɵadvance(2);
          ɵɵtextInterpolate1(" ", ɵɵpipeBind1(36, 34, "myCoupons." + (ctx.coupon == null ? null : ctx.coupon.status)), " ");
        }
      },
      dependencies: [IconComponent, FocusDirective, LowerCasePipe, TranslatePipe, CxDatePipe],
      encapsulation: 2,
      changeDetection: 0
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(CouponDialogComponent, [{
    type: Component,
    args: [{
      selector: "cx-coupon-dialog",
      changeDetection: ChangeDetectionStrategy.OnPush,
      standalone: false,
      template: `<div
  class="modal-dialog modal-dialog-centered cx-coupon-dialog"
  role="dialog"
  [cxFocus]="focusConfig"
  (esc)="close('Escape clicked')"
  aria-labelledby="dialogTitle"
>
  <div class="modal-content cx-coupon-container">
    <!-- Modal Header -->

    <div class="cx-dialog-header modal-header">
      <h3 id="dialogTitle" class="cx-dialog-title modal-title">
        {{ 'myCoupons.dialogTitle' | cxTranslate }}
      </h3>
      <button
        type="button"
        class="close"
        title="{{ 'common.close' | cxTranslate }}"
        [attr.aria-label]="'common.close' | cxTranslate"
        (click)="close('Cross click')"
      >
        <span aria-hidden="true">
          <cx-icon [type]="iconTypes.CLOSE"></cx-icon>
        </span>
      </button>
    </div>
    <!-- Modal Body -->
    <div class="cx-dialog-body modal-body">
      <div class="cx-dialog-row">
        <div class="cx-dialog-item col-sm-12 col-md-12">
          <div class="cx-coupon-card-head">
            <span class="card-label-bold cx-coupon-card-id">{{
              coupon?.couponId
            }}</span>
            <span>: {{ coupon?.name }}</span>
          </div>
          <div class="cx-coupon-description">{{ coupon?.description }}</div>

          <div class="cx-coupon-dialog-date">
            <p>{{ 'myCoupons.effectiveTitle' | cxTranslate }}</p>
            <div class="cx-coupon-date">
              {{ coupon?.startDate | cxDate: 'medium' }} -
              {{ coupon?.endDate | cxDate: 'medium' }}
            </div>
          </div>

          <div class="cx-coupon-dialog-status">
            <p>{{ 'myCoupons.status' | cxTranslate }}</p>
            <div class="cx-coupon-status {{ coupon?.status | lowercase }}">
              {{ 'myCoupons.' + coupon?.status | cxTranslate }}
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
`
    }]
  }], () => [{
    type: LaunchDialogService
  }, {
    type: ElementRef
  }], {
    handleClick: [{
      type: HostListener,
      args: ["click", ["$event"]]
    }]
  });
})();
var CouponClaimComponent = class _CouponClaimComponent {
  constructor(couponService, routingService, messageService) {
    this.couponService = couponService;
    this.routingService = routingService;
    this.messageService = messageService;
  }
  ngOnInit() {
    this.routingService.getRouterState().subscribe((k) => {
      const couponCode = k.state.params.couponCode;
      if (couponCode) {
        this.couponService.claimCustomerCoupon(couponCode);
        this.subscription = this.couponService.getClaimCustomerCouponResultSuccess().subscribe((success) => {
          if (success) {
            this.messageService.add({
              key: "myCoupons.claimCustomerCoupon"
            }, GlobalMessageType.MSG_TYPE_CONFIRMATION);
          }
          this.routingService.go({
            cxRoute: "coupons"
          });
        });
      } else {
        this.routingService.go({
          cxRoute: "notFound"
        });
      }
    }).unsubscribe();
  }
  ngOnDestroy() {
    if (this.subscription) {
      this.subscription.unsubscribe();
    }
  }
  static {
    this.ɵfac = function CouponClaimComponent_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _CouponClaimComponent)(ɵɵdirectiveInject(CustomerCouponService), ɵɵdirectiveInject(RoutingService), ɵɵdirectiveInject(GlobalMessageService));
    };
  }
  static {
    this.ɵcmp = ɵɵdefineComponent({
      type: _CouponClaimComponent,
      selectors: [["cx-coupon-claim"]],
      standalone: false,
      decls: 0,
      vars: 0,
      template: function CouponClaimComponent_Template(rf, ctx) {
      },
      encapsulation: 2
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(CouponClaimComponent, [{
    type: Component,
    args: [{
      template: "",
      selector: "cx-coupon-claim",
      standalone: false
    }]
  }], () => [{
    type: CustomerCouponService
  }, {
    type: RoutingService
  }, {
    type: GlobalMessageService
  }], null);
})();
var ClaimDialogComponent = class _ClaimDialogComponent {
  constructor() {
    this.subscription = new Subscription();
    this.iconTypes = ICON_TYPE;
    this.pageSize = 10;
    this.focusConfig = {
      trap: true,
      block: true,
      autofocus: "button",
      focusOnEscape: true
    };
    this.form = new FormGroup({
      couponCode: new FormControl("", [Validators.required])
    });
    this.couponService = inject(CustomerCouponService);
    this.routingService = inject(RoutingService);
    this.messageService = inject(GlobalMessageService);
    this.launchDialogService = inject(LaunchDialogService);
  }
  ngOnInit() {
    this.subscription.add(this.launchDialogService.data$.subscribe((data) => {
      if (data) {
        this.couponCode = data.coupon;
        this.pageSize = data.pageSize;
        this.form.get("couponCode").setValue(this.couponCode);
      }
    }));
  }
  onSubmit() {
    if (!this.form.valid) {
      this.form.markAllAsTouched();
      return;
    }
    const couponVal = this.form.get("couponCode").value;
    if (couponVal) {
      this.couponService.claimCustomerCoupon(couponVal);
      this.subscription = this.couponService.getClaimCustomerCouponResultSuccess().subscribe((success) => {
        if (success) {
          this.messageService.add({
            key: "myCoupons.claimCustomerCoupon"
          }, GlobalMessageType.MSG_TYPE_CONFIRMATION);
        }
        this.routingService.go({
          cxRoute: "coupons"
        });
        this.couponService.loadCustomerCoupons(this.pageSize);
        this.close("Cross click");
      });
    } else {
      this.routingService.go({
        cxRoute: "notFound"
      });
    }
  }
  ngOnDestroy() {
    this.subscription?.unsubscribe();
  }
  close(reason) {
    this.launchDialogService.closeDialog(reason);
  }
  cancelEdit() {
    this.form.get("couponCode").setValue(this.couponCode);
  }
  static {
    this.ɵfac = function ClaimDialogComponent_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _ClaimDialogComponent)();
    };
  }
  static {
    this.ɵcmp = ɵɵdefineComponent({
      type: _ClaimDialogComponent,
      selectors: [["cx-claim-dialog"]],
      standalone: false,
      decls: 31,
      vars: 33,
      consts: [["role", "dialog", "aria-labelledby", "dialogTitle", 1, "cx-coupon-dialog", 3, "esc", "cxFocus"], [1, "cx-coupon-container"], [1, "cx-dialog-header", "modal-header"], ["id", "dialogTitle", 1, "cx-dialog-title", "modal-title"], ["type", "button", 1, "close", 3, "click", "title"], ["aria-hidden", "true"], [3, "type"], [1, "cx-dialog-body", "modal-body"], [1, "cx-dialog-item", 3, "ngSubmit", "formGroup"], [1, "label-content"], ["required", "true", "type", "text", "name", "couponCode", "formControlName", "couponCode", 1, "form-control", 3, "placeholder"], ["id", "couponCodeError", 3, "translationParams", "control"], [1, "cx-dialog-row"], [1, "col-md-6", "cx-dialog-row--reset-button"], ["role", "button", 1, "btn", "btn-block", "btn-secondary", 3, "click"], [1, "col-md-6", "cx-dialog-row-submit-button"], [1, "btn", "btn-block", "btn-primary", 3, "disabled"]],
      template: function ClaimDialogComponent_Template(rf, ctx) {
        if (rf & 1) {
          ɵɵelementStart(0, "div", 0);
          ɵɵlistener("esc", function ClaimDialogComponent_Template_div_esc_0_listener() {
            return ctx.close("Escape clicked");
          });
          ɵɵelementStart(1, "div", 1)(2, "div", 2)(3, "h3", 3);
          ɵɵtext(4);
          ɵɵpipe(5, "cxTranslate");
          ɵɵelementEnd();
          ɵɵelementStart(6, "button", 4);
          ɵɵpipe(7, "cxTranslate");
          ɵɵpipe(8, "cxTranslate");
          ɵɵlistener("click", function ClaimDialogComponent_Template_button_click_6_listener() {
            return ctx.close("Cross click");
          });
          ɵɵelementStart(9, "span", 5);
          ɵɵelement(10, "cx-icon", 6);
          ɵɵelementEnd()()();
          ɵɵelementStart(11, "div", 7)(12, "form", 8);
          ɵɵlistener("ngSubmit", function ClaimDialogComponent_Template_form_ngSubmit_12_listener() {
            return ctx.onSubmit();
          });
          ɵɵelementStart(13, "label")(14, "span", 9);
          ɵɵtext(15);
          ɵɵpipe(16, "cxTranslate");
          ɵɵelement(17, "cx-form-required-asterisks");
          ɵɵelementEnd();
          ɵɵelement(18, "input", 10);
          ɵɵpipe(19, "cxTranslate");
          ɵɵelement(20, "cx-form-errors", 11);
          ɵɵpipe(21, "cxTranslate");
          ɵɵelementEnd();
          ɵɵelementStart(22, "div", 12)(23, "div", 13)(24, "a", 14);
          ɵɵlistener("click", function ClaimDialogComponent_Template_a_click_24_listener() {
            return ctx.cancelEdit();
          });
          ɵɵtext(25);
          ɵɵpipe(26, "cxTranslate");
          ɵɵelementEnd()();
          ɵɵelementStart(27, "div", 15)(28, "button", 16);
          ɵɵtext(29);
          ɵɵpipe(30, "cxTranslate");
          ɵɵelementEnd()()()()()()();
        }
        if (rf & 2) {
          let tmp_8_0;
          ɵɵproperty("cxFocus", ctx.focusConfig);
          ɵɵadvance(4);
          ɵɵtextInterpolate1(" ", ɵɵpipeBind1(5, 15, "myCoupons.claimCoupondialogTitle"), " ");
          ɵɵadvance(2);
          ɵɵpropertyInterpolate("title", ɵɵpipeBind1(7, 17, "common.close"));
          ɵɵattribute("aria-label", ɵɵpipeBind1(8, 19, "common.close"));
          ɵɵadvance(4);
          ɵɵproperty("type", ctx.iconTypes.CLOSE);
          ɵɵadvance(2);
          ɵɵproperty("formGroup", ctx.form);
          ɵɵadvance(3);
          ɵɵtextInterpolate1("", ɵɵpipeBind1(16, 21, "myCoupons.claimCouponCode.label"), " ");
          ɵɵadvance(3);
          ɵɵpropertyInterpolate("placeholder", ɵɵpipeBind1(19, 23, "myCoupons.claimCouponCode.placeholder"));
          ɵɵattribute("aria-invalid", ((tmp_8_0 = ctx.form.get("couponCode")) == null ? null : tmp_8_0.touched) && ((tmp_8_0 = ctx.form.get("couponCode")) == null ? null : tmp_8_0.invalid))("aria-errormessage", "couponCodeError");
          ɵɵadvance(2);
          ɵɵproperty("translationParams", ɵɵpureFunction1(31, _c45, ɵɵpipeBind1(21, 25, "myCoupons.claimCouponCode.label")))("control", ctx.form.get("couponCode"));
          ɵɵadvance(5);
          ɵɵtextInterpolate1(" ", ɵɵpipeBind1(26, 27, "myCoupons.reset"), " ");
          ɵɵadvance(3);
          ɵɵproperty("disabled", ctx.form.disabled);
          ɵɵadvance();
          ɵɵtextInterpolate1(" ", ɵɵpipeBind1(30, 29, "myCoupons.claim"), " ");
        }
      },
      dependencies: [ɵNgNoValidate, DefaultValueAccessor, NgControlStatus, NgControlStatusGroup, RequiredValidator, FormGroupDirective, FormControlName, FormErrorsComponent, FormRequiredAsterisksComponent, IconComponent, FocusDirective, TranslatePipe],
      encapsulation: 2,
      changeDetection: 0
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(ClaimDialogComponent, [{
    type: Component,
    args: [{
      selector: "cx-claim-dialog",
      changeDetection: ChangeDetectionStrategy.OnPush,
      standalone: false,
      template: `<div
  class="cx-coupon-dialog"
  role="dialog"
  [cxFocus]="focusConfig"
  (esc)="close('Escape clicked')"
  aria-labelledby="dialogTitle"
>
  <div class="cx-coupon-container">
    <!-- Modal Header -->

    <div class="cx-dialog-header modal-header">
      <h3 id="dialogTitle" class="cx-dialog-title modal-title">
        {{ 'myCoupons.claimCoupondialogTitle' | cxTranslate }}
      </h3>
      <button
        type="button"
        class="close"
        title="{{ 'common.close' | cxTranslate }}"
        [attr.aria-label]="'common.close' | cxTranslate"
        (click)="close('Cross click')"
      >
        <span aria-hidden="true">
          <cx-icon [type]="iconTypes.CLOSE"></cx-icon>
        </span>
      </button>
    </div>
    <!-- Modal Body -->
    <div class="cx-dialog-body modal-body">
      <form (ngSubmit)="onSubmit()" [formGroup]="form" class="cx-dialog-item">
        <label>
          <span class="label-content"
            >{{ 'myCoupons.claimCouponCode.label' | cxTranslate }}
            <cx-form-required-asterisks />
          </span>
          <input
            required="true"
            type="text"
            name="couponCode"
            formControlName="couponCode"
            placeholder="{{
              'myCoupons.claimCouponCode.placeholder' | cxTranslate
            }}"
            class="form-control"
            [attr.aria-invalid]="
              form.get('couponCode')?.touched && form.get('couponCode')?.invalid
            "
            [attr.aria-errormessage]="'couponCodeError'"
          />
          <cx-form-errors
            id="couponCodeError"
            [translationParams]="{
              label: 'myCoupons.claimCouponCode.label' | cxTranslate,
            }"
            [control]="form.get('couponCode')"
          ></cx-form-errors>
        </label>
        <div class="cx-dialog-row">
          <div class="col-md-6 cx-dialog-row--reset-button">
            <a
              role="button"
              class="btn btn-block btn-secondary"
              (click)="cancelEdit()"
            >
              {{ 'myCoupons.reset' | cxTranslate }}
            </a>
          </div>
          <div class="col-md-6 cx-dialog-row-submit-button">
            <button
              class="btn btn-block btn-primary"
              [disabled]="form.disabled"
            >
              {{ 'myCoupons.claim' | cxTranslate }}
            </button>
          </div>
        </div>
      </form>
    </div>
  </div>
</div>
`
    }]
  }], null, null);
})();
var defaultCouponLayoutConfig = {
  launch: {
    COUPON: {
      inlineRoot: true,
      component: CouponDialogComponent,
      dialogType: DIALOG_TYPE.DIALOG
    },
    CLAIM_DIALOG: {
      inlineRoot: true,
      component: ClaimDialogComponent,
      dialogType: DIALOG_TYPE.DIALOG
    }
  }
};
var MyCouponsModule = class _MyCouponsModule {
  static {
    this.ɵfac = function MyCouponsModule_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _MyCouponsModule)();
    };
  }
  static {
    this.ɵmod = ɵɵdefineNgModule({
      type: _MyCouponsModule,
      declarations: [MyCouponsComponent, CouponCardComponent, CouponDialogComponent, CouponClaimComponent, ClaimDialogComponent],
      imports: [CommonModule, ReactiveFormsModule, FormErrorsModule, FormRequiredAsterisksComponent, CardModule, SpinnerModule, I18nModule, RouterModule, UrlModule, IconModule, ListNavigationModule, RouterModule, KeyboardFocusModule, FeaturesConfigModule],
      exports: [MyCouponsComponent, CouponClaimComponent, ClaimDialogComponent]
    });
  }
  static {
    this.ɵinj = ɵɵdefineInjector({
      providers: [provideDefaultConfig({
        cmsComponents: {
          MyCouponsComponent: {
            component: MyCouponsComponent,
            guards: [AuthGuard]
          },
          CouponClaimComponent: {
            component: CouponClaimComponent,
            guards: [AuthGuard]
          },
          ClaimDialogComponent: {
            component: ClaimDialogComponent,
            guards: [AuthGuard]
          }
        }
      }), provideDefaultConfig(defaultCouponLayoutConfig)],
      imports: [CommonModule, ReactiveFormsModule, FormErrorsModule, FormRequiredAsterisksComponent, CardModule, SpinnerModule, I18nModule, RouterModule, UrlModule, IconModule, ListNavigationModule, RouterModule.forChild([{
        // @ts-ignore
        path: null,
        canActivate: [AuthGuard, CmsPageGuard],
        component: PageLayoutComponent,
        data: {
          cxRoute: "couponClaim"
        }
      }]), KeyboardFocusModule, FeaturesConfigModule]
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(MyCouponsModule, [{
    type: NgModule,
    args: [{
      imports: [CommonModule, ReactiveFormsModule, FormErrorsModule, FormRequiredAsterisksComponent, CardModule, SpinnerModule, I18nModule, RouterModule, UrlModule, IconModule, ListNavigationModule, RouterModule.forChild([{
        // @ts-ignore
        path: null,
        canActivate: [AuthGuard, CmsPageGuard],
        component: PageLayoutComponent,
        data: {
          cxRoute: "couponClaim"
        }
      }]), KeyboardFocusModule, FeaturesConfigModule],
      declarations: [MyCouponsComponent, CouponCardComponent, CouponDialogComponent, CouponClaimComponent, ClaimDialogComponent],
      providers: [provideDefaultConfig({
        cmsComponents: {
          MyCouponsComponent: {
            component: MyCouponsComponent,
            guards: [AuthGuard]
          },
          CouponClaimComponent: {
            component: CouponClaimComponent,
            guards: [AuthGuard]
          },
          ClaimDialogComponent: {
            component: ClaimDialogComponent,
            guards: [AuthGuard]
          }
        }
      }), provideDefaultConfig(defaultCouponLayoutConfig)],
      exports: [MyCouponsComponent, CouponClaimComponent, ClaimDialogComponent]
    }]
  }], null, null);
})();
var MyInterestsComponent = class _MyInterestsComponent {
  constructor(productInterestService, translationService, productService, globalMessageService) {
    this.productInterestService = productInterestService;
    this.translationService = translationService;
    this.productService = productService;
    this.globalMessageService = globalMessageService;
    this.DEFAULT_PAGE_SIZE = 10;
    this.sortMapping = {
      byNameAsc: "name:asc",
      byNameDesc: "name:desc"
    };
    this.sortChanged = false;
    this.sort = "byNameAsc";
    this.sortOptions = [{
      code: "byNameAsc",
      selected: false
    }, {
      code: "byNameDesc",
      selected: false
    }];
  }
  ngOnInit() {
    this.interests$ = this.productInterestService.getAndLoadProductInterests(this.DEFAULT_PAGE_SIZE).pipe(tap((interests) => this.pagination = {
      currentPage: interests.pagination?.page,
      pageSize: interests.pagination?.count,
      totalPages: interests.pagination?.totalPages,
      totalResults: interests.pagination?.totalCount,
      sort: "byNameAsc"
    }), tap(() => {
      if (this.sortChanged) {
        this.sortChanged = false;
        this.globalMessageService?.add({
          key: "sorting.pageViewUpdated"
        }, GlobalMessageType.MSG_TYPE_ASSISTIVE, 500);
      }
    }), map((interest) => __spreadProps(__spreadValues({}, interest), {
      results: interest.results ? interest.results.map((result) => __spreadProps(__spreadValues({}, result), {
        product$: this.getProduct(result)
      })) : interest.results
    })));
    this.getInterestsloading$ = this.productInterestService.getProdutInterestsLoading();
    this.isRemoveDisabled$ = combineLatest([this.getInterestsloading$, this.productInterestService.getRemoveProdutInterestLoading()]).pipe(map(([getLoading, removeLoading]) => getLoading || removeLoading));
    this.sortLabels = this.getSortLabels();
  }
  getSortLabels() {
    return combineLatest([this.translationService.translate("myInterests.sorting.byNameAsc"), this.translationService.translate("myInterests.sorting.byNameDesc")]).pipe(map(([asc, desc]) => {
      return {
        byNameAsc: asc,
        byNameDesc: desc
      };
    }));
  }
  getProduct(interest) {
    return this.productService.get(interest.product?.code ?? "", ProductScope.DETAILS);
  }
  removeInterest(relation) {
    this.productInterestService.removeProdutInterest({
      product: relation.product,
      productInterestEntry: relation.productInterestEntry
    });
  }
  sortChange(sort) {
    this.sort = sort;
    this.sortChanged = true;
    this.productInterestService.loadProductInterests(this.DEFAULT_PAGE_SIZE, 0, this.sortMapping[sort]);
  }
  pageChange(page) {
    this.productInterestService.loadProductInterests(this.DEFAULT_PAGE_SIZE, page, this.sortMapping[this.sort]);
  }
  ngOnDestroy() {
    this.productInterestService.clearProductInterests();
    this.productInterestService.resetRemoveInterestState();
  }
  static {
    this.ɵfac = function MyInterestsComponent_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _MyInterestsComponent)(ɵɵdirectiveInject(UserInterestsService), ɵɵdirectiveInject(TranslationService), ɵɵdirectiveInject(ProductService), ɵɵdirectiveInject(GlobalMessageService));
    };
  }
  static {
    this.ɵcmp = ɵɵdefineComponent({
      type: _MyInterestsComponent,
      selectors: [["cx-my-interests"]],
      standalone: false,
      decls: 6,
      vars: 3,
      consts: [["noInterest", ""], ["loading", ""], ["class", "container", 4, "ngIf"], [1, "container"], [1, "cx-product-interests-title"], ["class", "cx-product-interests-body", 4, "ngIf", "ngIfElse"], [1, "cx-product-interests-body"], [4, "ngIf", "ngIfElse"], [1, "cx-product-interests-sort", "top", "row"], [1, "cx-product-interests-form-group", "form-group", "col-sm-12", "col-md-4", "col-lg-4"], ["ariaControls", "product-interests-table", 3, "sortListEvent", "sortOptions", "sortLabels", "selectedOption", "placeholder", "ariaLabel"], [1, "cx-product-interests-pagination", "cx-product-interests-thead-mobile"], ["paginationID", "pagination-top", 3, "viewPageEvent", "pagination"], ["id", "product-interests-table", 1, "table", "cx-product-interests-table", "cx-product-interests-table-list"], [1, "cx-visually-hidden"], ["class", "cx-product-interests-thead-mobile cx-product-interests-header", 4, "cxFeature"], ["class", "cx-product-interests-product-item", "role", "row", 4, "ngFor", "ngForOf"], [1, "cx-product-interests-sort", "bottom", "row"], [1, "cx-product-interests-form-group", "cx-product-interests-thead-mobile", "form-group", "col-sm-12", "col-md-4", "col-lg-4"], [1, "cx-product-interests-pagination"], ["paginationID", "pagination-bottom", 3, "viewPageEvent", "pagination"], [1, "cx-product-interests-thead-mobile", "cx-product-interests-header"], ["role", "columnheader"], ["srole", "columnheader"], ["role", "row", 1, "cx-product-interests-product-item"], [4, "ngIf"], ["role", "cell"], [1, "cx-product-interests-cell-wrapper"], [1, "cx-product-interests-label"], ["tabindex", "-1", 1, "cx-product-interests-product-image-link", 3, "routerLink"], ["format", "thumbnail", 3, "container"], [1, "cx-info", "col-10"], [1, "cx-info-container", "row"], ["class", "cx-name", 4, "ngIf"], ["class", "cx-code", 4, "ngIf"], [4, "ngFor", "ngForOf"], ["class", "cx-property", 4, "ngIf"], [1, "cx-product-interests-product-price"], [1, "d-md-none", "cx-product-interests-label"], [1, "cx-product-interests-subscriptions"], ["class", "cx-product-interests-notification", 4, "ngFor", "ngForOf"], [1, "cx-actions", "cx-product-interests-remove-button"], ["type", "button", 1, "cx-product-interests-remove-btn", "link", "cx-action-link", "cx-remove-btn", 3, "click", "cxAtMessage", "disabled"], [1, "cx-name"], [1, "cx-link", "cx-product-interests-product-code-link", 3, "routerLink"], [1, "cx-code"], ["class", "cx-property", 4, "ngFor", "ngForOf"], [1, "cx-property"], [1, "cx-label", "cx-product-interests-variant-name"], [1, "cx-value", "cx-product-interests-variant-value"], [1, "cx-label", "cx-product-interests-product-stock"], [1, "cx-product-interests-notification"], [1, "cx-product-interests-type"], [1, "cx-product-interests-expiration-date"], [1, "cx-product-interests-message"], [1, "cx-spinner"]],
      template: function MyInterestsComponent_Template(rf, ctx) {
        if (rf & 1) {
          ɵɵtemplate(0, MyInterestsComponent_div_0_Template, 7, 7, "div", 2);
          ɵɵpipe(1, "async");
          ɵɵtemplate(2, MyInterestsComponent_ng_template_2_Template, 3, 3, "ng-template", null, 0, ɵɵtemplateRefExtractor)(4, MyInterestsComponent_ng_template_4_Template, 2, 0, "ng-template", null, 1, ɵɵtemplateRefExtractor);
        }
        if (rf & 2) {
          ɵɵproperty("ngIf", ɵɵpipeBind1(1, 1, ctx.interests$));
        }
      },
      dependencies: [AtMessageDirective, NgForOf, NgIf, SortingComponent, PaginationComponent, MediaComponent, SpinnerComponent, RouterLink, FeatureDirective, AsyncPipe, TranslatePipe, CxDatePipe, UrlPipe],
      encapsulation: 2,
      changeDetection: 0
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(MyInterestsComponent, [{
    type: Component,
    args: [{
      selector: "cx-my-interests",
      changeDetection: ChangeDetectionStrategy.OnPush,
      standalone: false,
      template: `<div *ngIf="interests$ | async as interests" class="container">
  <div class="cx-product-interests-title">
    <h2>{{ 'myInterests.header' | cxTranslate }}</h2>
  </div>
  <div
    class="cx-product-interests-body"
    *ngIf="!(getInterestsloading$ | async); else loading"
  >
    <ng-container
      *ngIf="
        interests.pagination &&
          interests.pagination.totalCount &&
          interests.pagination.totalCount > 0;
        else noInterest
      "
    >
      <div class="cx-product-interests-sort top row">
        <label
          class="cx-product-interests-form-group form-group col-sm-12 col-md-4 col-lg-4"
          ><span>{{ 'myInterests.sortBy' | cxTranslate }}</span>
          <cx-sorting
            [sortOptions]="sortOptions"
            [sortLabels]="sortLabels | async"
            (sortListEvent)="sortChange($event)"
            [selectedOption]="sort"
            placeholder="{{ 'myInterests.sortBy' | cxTranslate }}"
            [ariaLabel]="'myInterests.sortInterests' | cxTranslate"
            ariaControls="product-interests-table"
          >
          </cx-sorting>
        </label>
        <div
          class="cx-product-interests-pagination cx-product-interests-thead-mobile"
        >
          <cx-pagination
            [pagination]="pagination"
            (viewPageEvent)="pageChange($event)"
            paginationID="pagination-top"
          ></cx-pagination>
        </div>
      </div>

      <table
        id="product-interests-table"
        class="table cx-product-interests-table cx-product-interests-table-list"
      >
        <caption class="cx-visually-hidden">
          {{
            'myInterests.caption' | cxTranslate
          }}
        </caption>
        <thead
          *cxFeature="'!a11yTableHeaderReadout'"
          class="cx-product-interests-thead-mobile cx-product-interests-header"
        >
          <th role="columnheader">
            {{ 'myInterests.item' | cxTranslate }}
          </th>
          <th role="columnheader">
            {{ 'myInterests.price' | cxTranslate }}
          </th>
          <th role="columnheader">
            {{ 'myInterests.notifications' | cxTranslate }}
          </th>
          <th srole="columnheader">
            {{ 'myInterests.actions' | cxTranslate }}
          </th>
        </thead>
        <thead
          *cxFeature="'a11yTableHeaderReadout'"
          class="cx-product-interests-thead-mobile cx-product-interests-header"
        >
          <tr>
            <th>
              {{ 'myInterests.item' | cxTranslate }}
            </th>
            <th>
              {{ 'myInterests.price' | cxTranslate }}
            </th>
            <th>
              {{ 'myInterests.notifications' | cxTranslate }}
            </th>
            <th>
              {{ 'myInterests.actions' | cxTranslate }}
            </th>
          </tr>
        </thead>
        <tbody>
          <tr
            *ngFor="let interest of interests.results"
            class="cx-product-interests-product-item"
            role="row"
          >
            <ng-container *ngIf="interest.product$ | async as product">
              <td role="cell">
                <div class="cx-product-interests-cell-wrapper">
                  <div class="cx-product-interests-label">
                    <a
                      class="cx-product-interests-product-image-link"
                      tabindex="-1"
                      [routerLink]="
                        { cxRoute: 'product', params: product } | cxUrl
                      "
                    >
                      <cx-media
                        [container]="product.images?.PRIMARY"
                        format="thumbnail"
                      ></cx-media>
                    </a>
                  </div>

                  <div class="cx-info col-10">
                    <div class="cx-info-container row">
                      <div>
                        <div *ngIf="product.name" class="cx-name">
                          <a
                            class="cx-link cx-product-interests-product-code-link"
                            [routerLink]="
                              { cxRoute: 'product', params: product } | cxUrl
                            "
                          >
                            {{ product.name }}
                          </a>
                        </div>
                        <div *ngIf="product.code" class="cx-code">
                          <span>{{
                            'myInterests.productId'
                              | cxTranslate: { code: product.code }
                          }}</span>
                        </div>

                        <ng-container
                          *ngFor="let baseOptions of product.baseOptions"
                        >
                          <div
                            *ngFor="
                              let variant of baseOptions.selected
                                ?.variantOptionQualifiers
                            "
                            class="cx-property"
                          >
                            <div
                              class="cx-label cx-product-interests-variant-name"
                            >
                              {{ variant.name }}
                            </div>
                            <div
                              class="cx-value cx-product-interests-variant-value"
                            >
                              {{ variant.value }}
                            </div>
                          </div>
                        </ng-container>
                        <div
                          class="cx-property"
                          *ngIf="
                            product.stock?.stockLevelStatus === 'outOfStock'
                          "
                        >
                          <div
                            class="cx-label cx-product-interests-product-stock"
                          >
                            {{ 'myInterests.outOfStock' | cxTranslate }}
                          </div>
                        </div>
                      </div>
                    </div>
                  </div>
                </div>
              </td>
              <td role="cell">
                <div class="cx-product-interests-product-price">
                  <div class="d-md-none cx-product-interests-label">
                    {{ 'myInterests.price' | cxTranslate }}
                  </div>
                  <span>{{ product.price?.formattedValue }}</span>
                </div>
              </td>
              <td role="cell">
                <div class="cx-product-interests-subscriptions">
                  <div class="d-md-none cx-product-interests-label">
                    {{ 'myInterests.notifications' | cxTranslate }}
                  </div>
                  <div
                    class="cx-product-interests-notification"
                    *ngFor="let interestEntry of interest.productInterestEntry"
                  >
                    <span class="cx-product-interests-type">
                      {{
                        'myInterests.' + interestEntry.interestType
                          | cxTranslate
                      }}
                    </span>
                    <span class="cx-product-interests-expiration-date">
                      {{
                        'myInterests.expirationDate'
                          | cxTranslate
                            : {
                                expirationDate:
                                  interestEntry.expirationDate | cxDate,
                              }
                      }}
                    </span>
                  </div>
                </div>
              </td>
              <td role="cell">
                <div class="cx-actions cx-product-interests-remove-button">
                  <button
                    type="button"
                    class="cx-product-interests-remove-btn link cx-action-link cx-remove-btn"
                    [cxAtMessage]="'myInterests.itemRemoved' | cxTranslate"
                    [disabled]="isRemoveDisabled$ | async"
                    (click)="removeInterest(interest)"
                  >
                    {{ 'myInterests.remove' | cxTranslate }}
                  </button>
                </div>
              </td>
            </ng-container>
          </tr>
        </tbody>
      </table>

      <div class="cx-product-interests-sort bottom row">
        <label
          class="cx-product-interests-form-group cx-product-interests-thead-mobile form-group col-sm-12 col-md-4 col-lg-4"
          ><span>{{ 'myInterests.sortBy' | cxTranslate }}</span>
          <cx-sorting
            [sortOptions]="sortOptions"
            [sortLabels]="sortLabels | async"
            (sortListEvent)="sortChange($event)"
            [selectedOption]="sort"
            placeholder="{{ 'myInterests.sortBy' | cxTranslate }}"
            [ariaLabel]="'myInterests.sortInterests' | cxTranslate"
            ariaControls="product-interests-table"
          >
          </cx-sorting>
        </label>
        <div class="cx-product-interests-pagination">
          <cx-pagination
            [pagination]="pagination"
            (viewPageEvent)="pageChange($event)"
            paginationID="pagination-bottom"
          ></cx-pagination>
        </div>
      </div>
    </ng-container>
  </div>
</div>
<ng-template #noInterest>
  <div class="cx-product-interests-message">
    {{ 'myInterests.noInterests' | cxTranslate }}
  </div>
</ng-template>
<ng-template #loading>
  <div class="cx-spinner">
    <cx-spinner></cx-spinner>
  </div>
</ng-template>
`
    }]
  }], () => [{
    type: UserInterestsService
  }, {
    type: TranslationService
  }, {
    type: ProductService
  }, {
    type: GlobalMessageService
  }], null);
})();
var MyInterestsModule = class _MyInterestsModule {
  static {
    this.ɵfac = function MyInterestsModule_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _MyInterestsModule)();
    };
  }
  static {
    this.ɵmod = ɵɵdefineNgModule({
      type: _MyInterestsModule,
      declarations: [MyInterestsComponent],
      imports: [AtMessageModule, CommonModule, I18nModule, ListNavigationModule, I18nModule, UrlModule, MediaModule, SpinnerModule, RouterModule, FeaturesConfigModule],
      exports: [MyInterestsComponent]
    });
  }
  static {
    this.ɵinj = ɵɵdefineInjector({
      providers: [provideDefaultConfig({
        cmsComponents: {
          MyInterestsComponent: {
            component: MyInterestsComponent,
            guards: [AuthGuard]
          }
        }
      })],
      imports: [AtMessageModule, CommonModule, I18nModule, ListNavigationModule, I18nModule, UrlModule, MediaModule, SpinnerModule, RouterModule.forChild([{
        // @ts-ignore
        path: null,
        canActivate: [AuthGuard, CmsPageGuard],
        component: PageLayoutComponent,
        data: {
          cxRoute: "myInterests"
        }
      }]), FeaturesConfigModule]
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(MyInterestsModule, [{
    type: NgModule,
    args: [{
      declarations: [MyInterestsComponent],
      imports: [AtMessageModule, CommonModule, I18nModule, ListNavigationModule, I18nModule, UrlModule, MediaModule, SpinnerModule, RouterModule.forChild([{
        // @ts-ignore
        path: null,
        canActivate: [AuthGuard, CmsPageGuard],
        component: PageLayoutComponent,
        data: {
          cxRoute: "myInterests"
        }
      }]), FeaturesConfigModule],
      providers: [provideDefaultConfig({
        cmsComponents: {
          MyInterestsComponent: {
            component: MyInterestsComponent,
            guards: [AuthGuard]
          }
        }
      })],
      exports: [MyInterestsComponent]
    }]
  }], null, null);
})();
var NotificationPreferenceComponent = class _NotificationPreferenceComponent {
  constructor(notificationPreferenceService) {
    this.notificationPreferenceService = notificationPreferenceService;
    this.preferences = [];
  }
  ngOnInit() {
    this.notificationPreferenceService.resetNotificationPreferences();
    this.preferences$ = this.notificationPreferenceService.getPreferences().pipe(tap((preferences) => this.preferences = preferences));
    this.notificationPreferenceService.loadPreferences();
    this.isLoading$ = combineLatest([this.notificationPreferenceService.getPreferencesLoading(), this.notificationPreferenceService.getUpdatePreferencesResultLoading()]).pipe(map(([prefsLoading, updateLoading]) => prefsLoading || updateLoading));
  }
  updatePreference(preference) {
    const updatedPreferences = [];
    this.preferences.forEach((p) => {
      if (p.channel === preference.channel) {
        updatedPreferences.push(__spreadProps(__spreadValues({}, p), {
          enabled: !p.enabled
        }));
      } else {
        updatedPreferences.push(p);
      }
    });
    this.notificationPreferenceService.updatePreferences(updatedPreferences);
  }
  static {
    this.ɵfac = function NotificationPreferenceComponent_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _NotificationPreferenceComponent)(ɵɵdirectiveInject(UserNotificationPreferenceService));
    };
  }
  static {
    this.ɵcmp = ɵɵdefineComponent({
      type: _NotificationPreferenceComponent,
      selectors: [["cx-notification-preference"]],
      standalone: false,
      decls: 2,
      vars: 3,
      consts: [["loading", ""], [4, "ngIf"], [4, "ngIf", "ngIfElse"], [1, "row", "d-flex", "justify-content-center"], [1, "col-md-8"], [1, "pref-header"], [4, "ngFor", "ngForOf"], [1, "pref-note"], ["class", "form-check pref-channel", 4, "ngIf"], [1, "form-check", "pref-channel"], ["role", "checkbox", "type", "checkbox", 1, "form-check-input", "cx-np-checkbox", 3, "change", "id", "checked", "disabled"], [1, "form-check-label", 3, "for"], [1, "cx-spinner"]],
      template: function NotificationPreferenceComponent_Template(rf, ctx) {
        if (rf & 1) {
          ɵɵtemplate(0, NotificationPreferenceComponent_ng_container_0_Template, 4, 2, "ng-container", 1);
          ɵɵpipe(1, "async");
        }
        if (rf & 2) {
          ɵɵproperty("ngIf", ɵɵpipeBind1(1, 1, ctx.preferences$));
        }
      },
      dependencies: [NgForOf, NgIf, SpinnerComponent, AsyncPipe, TranslatePipe],
      encapsulation: 2,
      changeDetection: 0
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(NotificationPreferenceComponent, [{
    type: Component,
    args: [{
      selector: "cx-notification-preference",
      changeDetection: ChangeDetectionStrategy.OnPush,
      standalone: false,
      template: `<ng-container *ngIf="preferences$ | async as preferences">
  <div *ngIf="preferences.length > 0; else loading">
    <div class="row d-flex justify-content-center">
      <div class="col-md-8">
        <fieldset>
          <legend class="pref-header">
            {{ 'notificationPreference.message' | cxTranslate }}
          </legend>
          <ng-container *ngFor="let preference of preferences">
            <div *ngIf="preference.visible" class="form-check pref-channel">
              <input
                class="form-check-input cx-np-checkbox"
                role="checkbox"
                type="checkbox"
                id="{{ preference.channel }}"
                [checked]="preference.enabled"
                (change)="updatePreference(preference)"
                [disabled]="isLoading$ | async"
              />
              <label for="{{ preference.channel }}" class="form-check-label">
                {{
                  'notificationPreference.' + preference.channel | cxTranslate
                }}
                {{ preference.value }}
              </label>
            </div>
          </ng-container>
        </fieldset>
        <p class="pref-note">
          <strong>{{ 'notificationPreference.note' | cxTranslate }}</strong
          >{{ 'notificationPreference.noteMessage' | cxTranslate }}
        </p>
      </div>
    </div>
  </div>

  <ng-template #loading>
    <div class="cx-spinner">
      <cx-spinner></cx-spinner>
    </div>
  </ng-template>
</ng-container>
`
    }]
  }], () => [{
    type: UserNotificationPreferenceService
  }], null);
})();
var PageTitleComponent = class _PageTitleComponent {
  constructor(component, pageMetaService) {
    this.component = component;
    this.pageMetaService = pageMetaService;
  }
  ngOnInit() {
    this.setTitle();
  }
  ngAfterViewInit() {
    this.lastestTitle$ = this.title$;
  }
  setTitle() {
    this.title$ = this.pageMetaService.getMeta().pipe(filter(isNotNullable), map((meta) => (meta.heading || meta.title) ?? ""));
  }
  static {
    this.ɵfac = function PageTitleComponent_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _PageTitleComponent)(ɵɵdirectiveInject(CmsComponentData), ɵɵdirectiveInject(PageMetaService));
    };
  }
  static {
    this.ɵcmp = ɵɵdefineComponent({
      type: _PageTitleComponent,
      selectors: [["cx-page-title"]],
      standalone: false,
      decls: 3,
      vars: 3,
      consts: [[1, "cx-visually-hidden"]],
      template: function PageTitleComponent_Template(rf, ctx) {
        if (rf & 1) {
          ɵɵelementStart(0, "h1", 0);
          ɵɵtext(1);
          ɵɵpipe(2, "async");
          ɵɵelementEnd();
        }
        if (rf & 2) {
          ɵɵadvance();
          ɵɵtextInterpolate(ɵɵpipeBind1(2, 1, ctx.title$));
        }
      },
      dependencies: [AsyncPipe],
      encapsulation: 2,
      changeDetection: 0
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PageTitleComponent, [{
    type: Component,
    args: [{
      selector: "cx-page-title",
      changeDetection: ChangeDetectionStrategy.OnPush,
      standalone: false,
      template: '<h1 class="cx-visually-hidden">{{ title$ | async }}</h1>\n'
    }]
  }], () => [{
    type: CmsComponentData
  }, {
    type: PageMetaService
  }], null);
})();
var BreadcrumbComponent = class _BreadcrumbComponent extends PageTitleComponent {
  constructor(component, pageMetaService, translation) {
    super(component, pageMetaService);
    this.component = component;
    this.pageMetaService = pageMetaService;
    this.translation = translation;
    this.router = inject(Router);
    this.ariaLive$ = this.router.events.pipe(filter((e) => e instanceof NavigationEnd), map(() => {
      return document.activeElement !== document.body;
    }));
  }
  ngOnInit() {
    super.ngOnInit();
    this.setCrumbs();
  }
  setCrumbs() {
    this.crumbs$ = combineLatest([this.pageMetaService.getMeta(), this.translation.translate("common.home")]).pipe(map(([meta, textHome]) => meta?.breadcrumbs ? meta.breadcrumbs : [{
      label: textHome,
      link: "/"
    }]));
  }
  static {
    this.ɵfac = function BreadcrumbComponent_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _BreadcrumbComponent)(ɵɵdirectiveInject(CmsComponentData), ɵɵdirectiveInject(PageMetaService), ɵɵdirectiveInject(TranslationService));
    };
  }
  static {
    this.ɵcmp = ɵɵdefineComponent({
      type: _BreadcrumbComponent,
      selectors: [["cx-breadcrumb"]],
      standalone: false,
      features: [ɵɵInheritDefinitionFeature],
      decls: 10,
      vars: 15,
      consts: [["tabindex", "-1"], [4, "ngFor", "ngForOf"], [3, "routerLink", "innerHTML"]],
      template: function BreadcrumbComponent_Template(rf, ctx) {
        if (rf & 1) {
          ɵɵelementStart(0, "nav", 0);
          ɵɵpipe(1, "cxTranslate");
          ɵɵelementStart(2, "ol");
          ɵɵpipe(3, "cxTranslate");
          ɵɵtemplate(4, BreadcrumbComponent_li_4_Template, 5, 9, "li", 1);
          ɵɵpipe(5, "async");
          ɵɵelementEnd()();
          ɵɵelementStart(6, "h1");
          ɵɵpipe(7, "async");
          ɵɵtext(8);
          ɵɵpipe(9, "async");
          ɵɵelementEnd();
        }
        if (rf & 2) {
          ɵɵattribute("aria-label", ɵɵpipeBind1(1, 5, "common.breadcrumbs"));
          ɵɵadvance(2);
          ɵɵattribute("aria-label", ɵɵpipeBind1(3, 7, "common.breadcrumbs"));
          ɵɵadvance(2);
          ɵɵproperty("ngForOf", ɵɵpipeBind1(5, 9, ctx.crumbs$));
          ɵɵadvance(2);
          ɵɵattribute("aria-live", ɵɵpipeBind1(7, 11, ctx.ariaLive$) ? "polite" : null);
          ɵɵadvance(2);
          ɵɵtextInterpolate1(" ", ɵɵpipeBind1(9, 13, ctx.title$), "\n");
        }
      },
      dependencies: [NgForOf, RouterLink, AsyncPipe, TranslatePipe],
      encapsulation: 2,
      changeDetection: 0
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(BreadcrumbComponent, [{
    type: Component,
    args: [{
      selector: "cx-breadcrumb",
      changeDetection: ChangeDetectionStrategy.OnPush,
      standalone: false,
      template: `<nav [attr.aria-label]="'common.breadcrumbs' | cxTranslate" tabindex="-1">
  <ol [attr.aria-label]="'common.breadcrumbs' | cxTranslate">
    <li
      *ngFor="
        let crumb of crumbs$ | async as crumbList;
        let i = index;
        let last = last
      "
    >
      <a
        [routerLink]="crumb.link"
        [innerHTML]="crumb.label"
        [attr.aria-label]="
          last
            ? ('common.breadcrumbsCurrentPage' | cxTranslate) +
              ' ' +
              crumb.label +
              (i + 1) +
              ('common.of' | cxTranslate) +
              crumbList.length
            : crumb.label +
              (i + 1) +
              ('common.of' | cxTranslate) +
              crumbList.length
        "
      ></a>
    </li>
  </ol>
</nav>

<h1 [attr.aria-live]="(ariaLive$ | async) ? 'polite' : null">
  {{ title$ | async }}
</h1>
`
    }]
  }], () => [{
    type: CmsComponentData
  }, {
    type: PageMetaService
  }, {
    type: TranslationService
  }], null);
})();
var BreadcrumbModule = class _BreadcrumbModule {
  static {
    this.ɵfac = function BreadcrumbModule_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _BreadcrumbModule)();
    };
  }
  static {
    this.ɵmod = ɵɵdefineNgModule({
      type: _BreadcrumbModule,
      declarations: [BreadcrumbComponent],
      imports: [CommonModule, RouterModule, PageMetaModule, I18nModule, FeaturesConfigModule],
      exports: [BreadcrumbComponent]
    });
  }
  static {
    this.ɵinj = ɵɵdefineInjector({
      providers: [provideDefaultConfig({
        cmsComponents: {
          BreadcrumbComponent: {
            component: BreadcrumbComponent
          }
        }
      })],
      imports: [CommonModule, RouterModule, PageMetaModule, I18nModule, FeaturesConfigModule]
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(BreadcrumbModule, [{
    type: NgModule,
    args: [{
      imports: [CommonModule, RouterModule, PageMetaModule, I18nModule, FeaturesConfigModule],
      providers: [provideDefaultConfig({
        cmsComponents: {
          BreadcrumbComponent: {
            component: BreadcrumbComponent
          }
        }
      })],
      declarations: [BreadcrumbComponent],
      exports: [BreadcrumbComponent]
    }]
  }], null, null);
})();
var NavigationService = class _NavigationService {
  constructor(cmsService, semanticPathService) {
    this.cmsService = cmsService;
    this.semanticPathService = semanticPathService;
  }
  createNavigation(data$) {
    return combineLatest([data$, this.getNavigationNode(data$)]).pipe(map(([data, nav]) => {
      return {
        title: data.name,
        children: [nav]
      };
    }));
  }
  /**
   * returns an observable with the `NavigationNode` for the given `CmsNavigationComponent`.
   * This function will load the navigation underlying entries and children if they haven't been
   * loaded so far.
   */
  getNavigationNode(data$) {
    if (!data$) {
      return EMPTY;
    }
    return data$.pipe(filter((data) => !!data), switchMap((data) => {
      const navigation = data.navigationNode ? data.navigationNode : data;
      return this.cmsService.getNavigationEntryItems(navigation.uid ?? "").pipe(tap((items) => {
        if (items === void 0) {
          this.loadNavigationEntryItems(navigation, true);
          return;
        }
        const expectedItems = [];
        this.loadNavigationEntryItems(navigation, false, expectedItems);
        const existingItems = Object.keys(items).map((key) => items[key].uid ?? "");
        const missingItems = expectedItems.filter((it) => it.id && !existingItems.includes(it.id));
        if (missingItems.length > 0) {
          this.cmsService.loadNavigationItems(navigation.uid ?? "", missingItems);
        }
      }), filter(Boolean), map((items) => this.populateNavigationNode(navigation, items) ?? {}));
    }));
  }
  /**
   * Loads all navigation entry items' type and id. Dispatch action to load all these items
   * @param nodeData
   * @param root
   * @param itemsList
   */
  loadNavigationEntryItems(nodeData, root, itemsList = []) {
    if (nodeData.entries && nodeData.entries.length > 0) {
      nodeData.entries.forEach((entry) => {
        itemsList.push({
          superType: entry.itemSuperType,
          id: entry.itemId
        });
      });
    }
    if (nodeData.children && nodeData.children.length > 0) {
      nodeData.children.forEach((child) => this.loadNavigationEntryItems(child, false, itemsList));
    }
    if (root && nodeData.uid) {
      this.cmsService.loadNavigationItems(nodeData.uid, itemsList);
    }
  }
  /**
   * Create a new node tree for the view
   * @param nodeData
   * @param items
   */
  populateNavigationNode(nodeData, items) {
    const node = {};
    if (nodeData.title) {
      node.title = nodeData.title;
    }
    if (nodeData.styleClasses) {
      node.styleClasses = nodeData.styleClasses;
    }
    if (nodeData.styleAttributes) {
      node.styleAttributes = nodeData.styleAttributes;
    }
    if (nodeData.entries && nodeData.entries.length > 0) {
      this.populateLink(node, nodeData.entries[0], items);
    }
    if (nodeData.children?.length > 0) {
      const children = nodeData.children.map((child) => this.populateNavigationNode(child, items)).filter(Boolean);
      if (children.length > 0) {
        node.children = children;
      }
    }
    return Object.keys(node).length === 0 ? null : node;
  }
  /**
   * The node link is driven by the first entry.
   */
  populateLink(node, entry, items) {
    const item = items[`${entry.itemId}_${entry.itemSuperType}`];
    if (item && entry.itemType === "CMSLinkComponent") {
      if (!node.title) {
        node.title = item.linkName;
      }
      const url = this.getLink(item);
      if (node.title && url) {
        node.url = url;
        if (item.target === "true" || item.target === true) {
          node.target = "_blank";
        }
      }
      if (item.styleClasses) {
        node.styleClasses = item.styleClasses;
      }
      if (item.styleAttributes) {
        node.styleAttributes = item.styleAttributes;
      }
    }
  }
  /**
   *
   * Gets the URL or link to a related item (category),
   * also taking into account content pages (contentPageLabelOrId)
   * and product pages (productCode)
   */
  getLink(item) {
    if (item.url) {
      return item.url;
    } else if (item.contentPageLabelOrId) {
      return item.contentPageLabelOrId;
    } else if (item.categoryCode) {
      return this.semanticPathService.transform({
        cxRoute: "category",
        params: {
          code: item.categoryCode,
          name: item.name
        }
      });
    } else if (item.productCode) {
      return this.semanticPathService.transform({
        cxRoute: "product",
        params: {
          code: item.productCode,
          name: item.name
        }
      });
    }
  }
  static {
    this.ɵfac = function NavigationService_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _NavigationService)(ɵɵinject(CmsService), ɵɵinject(SemanticPathService));
    };
  }
  static {
    this.ɵprov = ɵɵdefineInjectable({
      token: _NavigationService,
      factory: _NavigationService.ɵfac,
      providedIn: "root"
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(NavigationService, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], () => [{
    type: CmsService
  }, {
    type: SemanticPathService
  }], null);
})();
var ARIA_EXPANDED_ATTR = "aria-expanded";
var NavigationUIComponent = class _NavigationUIComponent {
  onResize() {
    this.resize.next(void 0);
  }
  onArrow(e) {
    this.arrowControls.next(e);
  }
  constructor(router, renderer, elemRef, hamburgerMenuService, winRef, featureConfigService) {
    this.router = router;
    this.renderer = renderer;
    this.elemRef = elemRef;
    this.hamburgerMenuService = hamburgerMenuService;
    this.winRef = winRef;
    this.featureConfigService = featureConfigService;
    this.focusableNodeTitles = false;
    this.iconType = ICON_TYPE;
    this.flyout = true;
    this.isOpen = false;
    this.openNodes = [];
    this.subscriptions = new Subscription();
    this.resize = new EventEmitter();
    this.arrowControls = new Subject();
    this.breakpointService = inject(BreakpointService);
    this.isDesktop$ = this.breakpointService.isUp(BREAKPOINT.lg);
    this.subscriptions.add(this.router.events.pipe(filter((event) => event instanceof NavigationEnd)).subscribe(() => this.clear()));
    this.subscriptions.add(this.resize.pipe(debounceTime(50)).subscribe(() => {
      this.alignWrappersToRightIfStickOut();
    }));
    useFeatureStyles("a11yOptimizedMenuSpacing");
    useFeatureStyles("a11yNavigationButtonsAriaFixes");
  }
  /**
   * During initialization of this component, we will check the resetMenuOnClose flag and attach a menu reset listener if needed.
   */
  ngOnInit() {
    if (this.resetMenuOnClose) {
      this.resetOnMenuCollapse();
    }
  }
  /**
   * This method performs the action of resetting the menu (close all sub menus and return to main options)
   * when the menu is closed.
   */
  resetOnMenuCollapse() {
    this.subscriptions.add(this.hamburgerMenuService?.isExpanded.pipe(distinctUntilChanged(), filter(Boolean)).subscribe(() => {
      this.reinitializeMenu();
    }));
  }
  closeIfClickedTheSameLink(navNode) {
    if (typeof navNode.url === "string" && this.winRef.nativeWindow?.location.href.includes(navNode.url)) {
      this.reinitializeMenu();
      this.hamburgerMenuService.toggle();
    }
  }
  /**
   * This method performs the actions required to reset the state of the menu and reset any visual components.
   */
  reinitializeMenu() {
    const listItems = this.elemRef.nativeElement.querySelectorAll("li.is-open:not(.back), li.is-opened");
    listItems.forEach((el) => {
      Array.from(el.children).filter((childNode) => childNode?.tagName === "BUTTON").forEach((childNode) => {
        this.renderer.setAttribute(childNode, ARIA_EXPANDED_ATTR, "false");
      });
    });
    listItems.forEach((el) => {
      this.renderer.removeClass(el, "is-open");
      this.renderer.removeClass(el, "is-opened");
    });
    this.clear();
    this.renderer.removeClass(this.elemRef.nativeElement, "is-open");
    this.updateClasses();
  }
  ariaCollapseNodes() {
    this.openNodes.forEach((parentNode) => {
      Array.from(parentNode.children).filter((childNode) => childNode?.tagName === "BUTTON").forEach((childNode) => {
        this.renderer.setAttribute(childNode, ARIA_EXPANDED_ATTR, "false");
      });
    });
  }
  toggleOpen(event) {
    if (event.type === "keydown") {
      event.preventDefault();
    }
    this.ariaCollapseNodes();
    const node = event.currentTarget;
    const parentNode = node.parentNode;
    if (this.openNodes.includes(parentNode)) {
      if (event.type === "keydown") {
        this.back();
      } else {
        this.openNodes = this.openNodes.filter((n) => n !== parentNode);
        this.renderer.removeClass(parentNode, "is-open");
      }
    } else {
      this.openNodes.push(parentNode);
      this.renderer.setAttribute(node, ARIA_EXPANDED_ATTR, "true");
    }
    this.updateClasses();
    event.stopImmediatePropagation();
    event.stopPropagation();
  }
  /**
   * Opens dropdown and starts keyboard navigation
   */
  onSpace(event) {
    this.hamburgerMenuService.isExpanded.pipe(take(1)).subscribe((isExpanded) => {
      if (isExpanded) {
        this.toggleOpen(event);
        return;
      }
      if (!this.openNodes.length) {
        this.toggleOpen(event);
        return;
      }
      event.preventDefault();
    });
    this.focusOnNode(event);
    this.setupArrowControls();
  }
  /**
   * Subscribes to arrow keys and enables navigation between dropdown items
   */
  setupArrowControls() {
    this.subscriptions.add(this.arrowControls.subscribe((e) => {
      e.preventDefault();
      const parentElement = e.target.parentElement?.parentElement;
      const nextLink = parentElement?.nextElementSibling?.querySelector("a");
      const previousLink = parentElement?.previousElementSibling?.querySelector("a");
      e.code === "ArrowDown" ? nextLink?.focus() : previousLink?.focus();
    }));
  }
  /**
   * Focuses on the first focusable element in the dropdown
   */
  focusOnNode(event) {
    if (this.featureConfigService?.isEnabled("a11yNavigationButtonsAriaFixes")) {
      const firstFocusableNode = event.target?.nextElementSibling?.querySelector("button, h4, a");
      firstFocusableNode?.focus();
    } else {
      const firstFocusableElement = event.target.nextElementSibling?.querySelector("button") || event.target.nextElementSibling?.querySelector("a");
      firstFocusableElement?.focus();
    }
  }
  back() {
    if (this.openNodes[this.openNodes.length - 1]) {
      this.renderer.removeClass(this.openNodes[this.openNodes.length - 1], "is-open");
      const removedNode = this.openNodes.pop();
      setTimeout(() => {
        (removedNode?.querySelector('[aria-haspopup="true"]')).focus();
      }, 0);
      this.updateClasses();
    }
  }
  clear() {
    this.openNodes = [];
    this.updateClasses();
  }
  onMouseEnter(event) {
    this.alignWrapperToRightIfStickOut(event.currentTarget);
    this.focusAfterPreviousClicked(event);
  }
  getTotalDepth(node, depth = 0) {
    if (node.children && node.children.length > 0) {
      return Math.max(...node.children.map((n) => this.getTotalDepth(n, depth + 1)));
    } else {
      return depth;
    }
  }
  getColumnCount(length) {
    if (!this.wrapAfter || length <= 0) {
      return 1;
    }
    let subSectionColumns = Math.floor(length / this.wrapAfter);
    if (subSectionColumns >= 1 && length % this.wrapAfter > 0) {
      subSectionColumns += 1;
    }
    return subSectionColumns;
  }
  focusAfterPreviousClicked(event) {
    const target = event.target || event.relatedTarget;
    if (target.ownerDocument.activeElement?.matches("nav[tabindex]") && target.parentElement?.matches(".flyout")) {
      target.focus();
    }
    return target.ownerDocument;
  }
  ngOnDestroy() {
    if (this.subscriptions) {
      this.subscriptions.unsubscribe();
    }
  }
  alignWrapperToRightIfStickOut(node) {
    const wrapper = node.querySelector(".wrapper");
    const body = node.closest("body");
    if (wrapper) {
      this.renderer.removeStyle(wrapper, "margin-left");
      if (wrapper.offsetLeft + wrapper.offsetWidth > body.offsetLeft + body.offsetWidth) {
        this.renderer.setStyle(wrapper, "margin-left", `${node.offsetWidth - wrapper.offsetWidth}px`);
      }
    }
  }
  alignWrappersToRightIfStickOut() {
    const navs = this.elemRef.nativeElement.childNodes;
    Array.from(navs).filter((node) => node.tagName === "LI").forEach((nav) => this.alignWrapperToRightIfStickOut(nav));
  }
  updateClasses() {
    this.openNodes.forEach((node, i) => {
      if (i + 1 < this.openNodes.length) {
        this.renderer.addClass(node, "is-opened");
        this.renderer.removeClass(node, "is-open");
      } else {
        this.renderer.removeClass(node, "is-opened");
        this.renderer.addClass(node, "is-open");
      }
    });
    this.isOpen = this.openNodes.length > 0;
  }
  /**
   * Restores default tabbing order for non flyout navigation.
   */
  getTabIndex(node, depth) {
    if (!this.flyout) {
      return 0;
    }
    return depth > 0 && !node?.children ? -1 : 0;
  }
  // TODO: Delete deprecated methods once `a11yNavigationButtonsAriaFixes` feature flag is removed.
  /**
   * Replace spaces with hyphens and convert to lowercase
   * @deprecated
   */
  getSanitizedTitle(title) {
    return title ? title.replace(/\s+/g, "-").toLowerCase() : null;
  }
  /**
   * Returns the value for the `aria-control` and the `aria-label` attribute of a button.
   * @deprecated
   */
  getAriaLabelAndControl(node) {
    return this.getSanitizedTitle(node.title) || null;
  }
  transformIntoValidID(string) {
    return string?.replace(/[^a-zA-Z0-9-_]/g, "-") || null;
  }
  static {
    this.ɵfac = function NavigationUIComponent_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _NavigationUIComponent)(ɵɵdirectiveInject(Router), ɵɵdirectiveInject(Renderer2), ɵɵdirectiveInject(ElementRef), ɵɵdirectiveInject(HamburgerMenuService), ɵɵdirectiveInject(WindowRef), ɵɵdirectiveInject(FeatureConfigService, 8));
    };
  }
  static {
    this.ɵcmp = ɵɵdefineComponent({
      type: _NavigationUIComponent,
      selectors: [["cx-navigation-ui"]],
      hostVars: 4,
      hostBindings: function NavigationUIComponent_HostBindings(rf, ctx) {
        if (rf & 1) {
          ɵɵlistener("resize", function NavigationUIComponent_resize_HostBindingHandler() {
            return ctx.onResize();
          }, false, ɵɵresolveWindow)("keyDown.arrowUp", function NavigationUIComponent_keyDown_arrowUp_HostBindingHandler($event) {
            return ctx.onArrow($event);
          }, false, ɵɵresolveDocument)("keyDown.arrowDown", function NavigationUIComponent_keyDown_arrowDown_HostBindingHandler($event) {
            return ctx.onArrow($event);
          }, false, ɵɵresolveDocument);
        }
        if (rf & 2) {
          ɵɵclassProp("flyout", ctx.flyout)("is-open", ctx.isOpen);
        }
      },
      inputs: {
        node: "node",
        wrapAfter: "wrapAfter",
        resetMenuOnClose: "resetMenuOnClose",
        focusableNodeTitles: "focusableNodeTitles",
        navAriaLabel: "navAriaLabel",
        flyout: "flyout",
        isOpen: "isOpen"
      },
      standalone: false,
      decls: 6,
      vars: 4,
      consts: [["nav", ""], ["heading", ""], ["title", ""], ["dropdownHeader", ""], ["class", "back is-open", 4, "ngIf"], [4, "ngFor", "ngForOf"], [1, "back", "is-open"], [3, "click"], [3, "type"], [4, "ngTemplateOutlet", "ngTemplateOutletContext"], [3, "ariaLabel", "url", "target", "style", "class", "tabindex", "click", "focus", "keydown.space", "keydown.esc", 4, "ngIf", "ngIfElse"], ["class", "wrapper", 3, "id", 4, "ngIf"], [3, "click", "focus", "keydown.space", "keydown.esc", "ariaLabel", "url", "target", "tabindex"], [4, "ngIf", "ngIfElse"], [3, "url", "target", "click", 4, "ngIf"], [4, "cxFeature"], [3, "click", "url", "target"], ["aria-level", "4", 3, "click", "mouseenter", "keydown.space", "keydown.enter", "keydown.esc", "keydown.arrowDown", "focus"], [4, "ngIf"], ["tabindex", "0", 3, "keydown.arrowDown", "keydown.space", 4, "ngIf", "ngIfElse"], ["tabindex", "0", 3, "keydown.arrowDown", "keydown.space"], [3, "click", "mouseenter", "keydown.space", "keydown.enter", "keydown.esc", "keydown.arrowDown", "focus"], ["role", "heading", "aria-level", "4", 4, "ngIf"], ["role", "heading", "aria-level", "4"], [1, "wrapper", 3, "id"], [1, "childs"]],
      template: function NavigationUIComponent_Template(rf, ctx) {
        if (rf & 1) {
          ɵɵelementStart(0, "nav")(1, "ul");
          ɵɵtemplate(2, NavigationUIComponent_li_2_Template, 5, 4, "li", 4)(3, NavigationUIComponent_ng_container_3_Template, 2, 4, "ng-container", 5);
          ɵɵelementEnd()();
          ɵɵtemplate(4, NavigationUIComponent_ng_template_4_Template, 5, 4, "ng-template", null, 0, ɵɵtemplateRefExtractor);
        }
        if (rf & 2) {
          let tmp_2_0;
          let tmp_3_0;
          ɵɵattribute("aria-label", ctx.navAriaLabel);
          ɵɵadvance();
          ɵɵattribute("role", ctx.flyout && ((tmp_2_0 = ctx.node == null ? null : ctx.node.children == null ? null : ctx.node.children.length) !== null && tmp_2_0 !== void 0 ? tmp_2_0 : 0) > 1 ? "list" : "presentation");
          ɵɵadvance();
          ɵɵproperty("ngIf", ctx.flyout && ((tmp_3_0 = ctx.node == null ? null : ctx.node.children == null ? null : ctx.node.children.length) !== null && tmp_3_0 !== void 0 ? tmp_3_0 : 0) > 1);
          ɵɵadvance();
          ɵɵproperty("ngForOf", ctx.node == null ? null : ctx.node.children);
        }
      },
      dependencies: [NgForOf, NgIf, NgTemplateOutlet, IconComponent, GenericLinkComponent, FeatureDirective, AsyncPipe, TranslatePipe],
      encapsulation: 2,
      changeDetection: 0
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(NavigationUIComponent, [{
    type: Component,
    args: [{
      selector: "cx-navigation-ui",
      changeDetection: ChangeDetectionStrategy.OnPush,
      standalone: false,
      template: `<nav [attr.aria-label]="navAriaLabel">
  <ul
    [attr.role]="
      flyout && (node?.children?.length ?? 0) > 1 ? 'list' : 'presentation'
    "
  >
    <li
      *ngIf="flyout && (node?.children?.length ?? 0) > 1"
      class="back is-open"
    >
      <button (click)="back()">
        <cx-icon [type]="iconType.CARET_LEFT"></cx-icon>
        {{ 'common.back' | cxTranslate }}
      </button>
    </li>

    <ng-container *ngFor="let child of node?.children">
      <ng-container *ngTemplateOutlet="nav; context: { node: child, depth: 0 }">
      </ng-container>
    </ng-container>
  </ul>
</nav>
<!-- we generate links in a recursive manner -->

<ng-template
  #nav
  let-node="node"
  let-depth="depth"
  let-listLength="listLength"
  let-position="position"
>
  <li [attr.role]="!flyout && !depth ? 'presentation' : 'listitem'">
    <cx-generic-link
      *ngIf="
        node.url && (!node.children || node.children?.length === 0);
        else heading
      "
      [ariaLabel]="
        'navigation.linkItemInList'
          | cxTranslate
            : {
                title: node.title,
                position,
                listLength,
              }
      "
      [url]="node.url"
      [target]="node.target"
      [style]="node.styleAttributes"
      [class]="node.styleClasses"
      (click)="closeIfClickedTheSameLink(node)"
      [tabindex]="getTabIndex(node, depth)"
      (focus)="depth || reinitializeMenu()"
      (keydown.space)="toggleOpen($any($event))"
      (keydown.esc)="back()"
    >
      {{ node.title }}
    </cx-generic-link>

    <ng-template #heading>
      <ng-container *ngIf="flyout && node.children?.length > 0; else title">
        <cx-generic-link
          *ngIf="node.url"
          [url]="node.url"
          [target]="node.target"
          (click)="closeIfClickedTheSameLink(node)"
        >
          {{ node.title }}
        </cx-generic-link>
        <ng-container *cxFeature="'!a11yNavigationButtonsAriaFixes'">
          <button
            aria-level="4"
            [attr.role]="(isDesktop$ | async) && depth ? 'heading' : 'button'"
            [attr.aria-haspopup]="true"
            [attr.aria-expanded]="false"
            [attr.aria-controls]="node.title"
            [attr.aria-describedby]="'greeting'"
            (click)="toggleOpen($any($event))"
            (mouseenter)="onMouseEnter($event)"
            (keydown.space)="onSpace($any($event))"
            (keydown.enter)="onSpace($any($event))"
            (keydown.esc)="back()"
            (keydown.arrowDown)="focusOnNode($any($event))"
            (focus)="depth || reinitializeMenu()"
          >
            <ng-container *ngIf="!node.url">
              {{ node.title }}
            </ng-container>
            <cx-icon [type]="iconType.CARET_DOWN"></cx-icon>
          </button>
        </ng-container>
        <ng-container *cxFeature="'a11yNavigationButtonsAriaFixes'">
          <h4
            *ngIf="(isDesktop$ | async) && depth; else dropdownHeader"
            (keydown.arrowDown)="focusOnNode($any($event))"
            (keydown.space)="onSpace($any($event))"
            tabindex="0"
          >
            {{ node.title }}
          </h4>
          <ng-template #dropdownHeader>
            <button
              [attr.aria-haspopup]="true"
              [attr.aria-expanded]="false"
              [attr.aria-label]="node.url ? node.title : null"
              [attr.aria-controls]="transformIntoValidID(node.title)"
              [attr.title]="
                'navigation.menuButonTitle' | cxTranslate: { title: node.title }
              "
              (click)="toggleOpen($any($event))"
              (mouseenter)="onMouseEnter($event)"
              (keydown.space)="onSpace($any($event))"
              (keydown.enter)="onSpace($any($event))"
              (keydown.esc)="back()"
              (keydown.arrowDown)="focusOnNode($any($event))"
              (focus)="depth || reinitializeMenu()"
            >
              <ng-container *ngIf="!node.url">
                {{ node.title }}
              </ng-container>
              <cx-icon [type]="iconType.CARET_DOWN"></cx-icon>
            </button>
          </ng-template>
        </ng-container>
      </ng-container>
      <ng-template #title>
        <span
          role="heading"
          aria-level="4"
          *ngIf="node.title"
          [attr.tabindex]="focusableNodeTitles ? 0 : -1"
        >
          {{ node.title }}
        </span>
      </ng-template>
    </ng-template>

    <!-- we add a wrapper to allow for better layout handling in CSS -->
    <div
      [id]="transformIntoValidID(node.title)"
      class="wrapper"
      *ngIf="node.children && node.children.length > 0"
    >
      <ul
        class="childs"
        [attr.depth]="getTotalDepth(node)"
        [attr.wrap-after]="node.children.length > wrapAfter ? wrapAfter : null"
        [attr.columns]="getColumnCount(node.children.length)"
        [attr.aria-label]="node.title"
      >
        <ng-container *ngFor="let child of node.children; index as i">
          <ng-container
            *ngTemplateOutlet="
              nav;
              context: {
                node: child,
                depth: depth + 1,
                listLength: node.children.length,
                position: i + 1,
              }
            "
          >
          </ng-container>
        </ng-container>
      </ul>
    </div>
  </li>
</ng-template>
`
    }]
  }], () => [{
    type: Router
  }, {
    type: Renderer2
  }, {
    type: ElementRef
  }, {
    type: HamburgerMenuService
  }, {
    type: WindowRef
  }, {
    type: FeatureConfigService,
    decorators: [{
      type: Optional
    }]
  }], {
    node: [{
      type: Input
    }],
    wrapAfter: [{
      type: Input
    }],
    resetMenuOnClose: [{
      type: Input
    }],
    focusableNodeTitles: [{
      type: Input
    }],
    navAriaLabel: [{
      type: Input
    }],
    flyout: [{
      type: Input
    }, {
      type: HostBinding,
      args: ["class.flyout"]
    }],
    isOpen: [{
      type: Input
    }, {
      type: HostBinding,
      args: ["class.is-open"]
    }],
    onResize: [{
      type: HostListener,
      args: ["window:resize"]
    }],
    onArrow: [{
      type: HostListener,
      args: ["document:keyDown.arrowUp", ["$event"]]
    }, {
      type: HostListener,
      args: ["document:keyDown.arrowDown", ["$event"]]
    }]
  });
})();
var CategoryNavigationComponent = class _CategoryNavigationComponent {
  constructor(componentData, service) {
    this.componentData = componentData;
    this.service = service;
    this.node$ = this.service.getNavigationNode(this.componentData.data$);
    this.data$ = this.componentData.data$;
  }
  static {
    this.ɵfac = function CategoryNavigationComponent_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _CategoryNavigationComponent)(ɵɵdirectiveInject(CmsComponentData), ɵɵdirectiveInject(NavigationService));
    };
  }
  static {
    this.ɵcmp = ɵɵdefineComponent({
      type: _CategoryNavigationComponent,
      selectors: [["cx-category-navigation"]],
      standalone: false,
      decls: 2,
      vars: 3,
      consts: [[3, "node", "ngClass", "wrapAfter", "resetMenuOnClose", "navAriaLabel", 4, "ngIf"], [3, "node", "ngClass", "wrapAfter", "resetMenuOnClose", "navAriaLabel"]],
      template: function CategoryNavigationComponent_Template(rf, ctx) {
        if (rf & 1) {
          ɵɵtemplate(0, CategoryNavigationComponent_cx_navigation_ui_0_Template, 3, 9, "cx-navigation-ui", 0);
          ɵɵpipe(1, "async");
        }
        if (rf & 2) {
          ɵɵproperty("ngIf", ɵɵpipeBind1(1, 1, ctx.data$));
        }
      },
      dependencies: [NgClass, NgIf, NavigationUIComponent, AsyncPipe, TranslatePipe],
      encapsulation: 2,
      changeDetection: 0
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(CategoryNavigationComponent, [{
    type: Component,
    args: [{
      selector: "cx-category-navigation",
      changeDetection: ChangeDetectionStrategy.OnPush,
      standalone: false,
      template: `<cx-navigation-ui
  *ngIf="data$ | async as data"
  [node]="node$ | async"
  [ngClass]="data.styleClass ?? ''"
  [wrapAfter]="+(data.wrapAfter ?? '')"
  [resetMenuOnClose]="data.resetMenuOnClose"
  [navAriaLabel]="'navigation.categoryNavLabel' | cxTranslate"
></cx-navigation-ui>
`
    }]
  }], () => [{
    type: CmsComponentData
  }, {
    type: NavigationService
  }], null);
})();
var NavigationComponent = class _NavigationComponent {
  constructor(componentData, service) {
    this.componentData = componentData;
    this.service = service;
    this.node$ = this.service.createNavigation(this.componentData.data$);
    this.name$ = this.componentData.data$.pipe(map((d) => d?.navigationNode?.title));
    this.styleClass$ = this.componentData.data$.pipe(map((d) => d?.styleClass));
  }
  static {
    this.ɵfac = function NavigationComponent_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _NavigationComponent)(ɵɵdirectiveInject(CmsComponentData), ɵɵdirectiveInject(NavigationService));
    };
  }
  static {
    this.ɵcmp = ɵɵdefineComponent({
      type: _NavigationComponent,
      selectors: [["cx-navigation"]],
      standalone: false,
      decls: 4,
      vars: 9,
      consts: [[3, "node", "ngClass", "navAriaLabel"]],
      template: function NavigationComponent_Template(rf, ctx) {
        if (rf & 1) {
          ɵɵelement(0, "cx-navigation-ui", 0);
          ɵɵpipe(1, "async");
          ɵɵpipe(2, "async");
          ɵɵpipe(3, "async");
        }
        if (rf & 2) {
          let tmp_1_0;
          ɵɵproperty("node", ɵɵpipeBind1(1, 3, ctx.node$))("ngClass", (tmp_1_0 = ɵɵpipeBind1(2, 5, ctx.styleClass$)) !== null && tmp_1_0 !== void 0 ? tmp_1_0 : "")("navAriaLabel", ɵɵpipeBind1(3, 7, ctx.name$));
        }
      },
      dependencies: [NgClass, NavigationUIComponent, AsyncPipe],
      encapsulation: 2,
      changeDetection: 0
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(NavigationComponent, [{
    type: Component,
    args: [{
      selector: "cx-navigation",
      changeDetection: ChangeDetectionStrategy.OnPush,
      standalone: false,
      template: `<cx-navigation-ui
  [node]="node$ | async"
  [ngClass]="(styleClass$ | async) ?? ''"
  [navAriaLabel]="name$ | async"
>
</cx-navigation-ui>
`
    }]
  }], () => [{
    type: CmsComponentData
  }, {
    type: NavigationService
  }], null);
})();
var NavigationModule = class _NavigationModule {
  static {
    this.ɵfac = function NavigationModule_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _NavigationModule)();
    };
  }
  static {
    this.ɵmod = ɵɵdefineNgModule({
      type: _NavigationModule,
      declarations: [NavigationComponent, NavigationUIComponent],
      imports: [CommonModule, RouterModule, IconModule, GenericLinkModule, I18nModule, FeaturesConfigModule],
      exports: [NavigationComponent, NavigationUIComponent]
    });
  }
  static {
    this.ɵinj = ɵɵdefineInjector({
      providers: [provideDefaultConfig({
        cmsComponents: {
          NavigationComponent: {
            component: NavigationComponent
          }
        }
      })],
      imports: [CommonModule, RouterModule, IconModule, GenericLinkModule, I18nModule, FeaturesConfigModule]
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(NavigationModule, [{
    type: NgModule,
    args: [{
      imports: [CommonModule, RouterModule, IconModule, GenericLinkModule, I18nModule, FeaturesConfigModule],
      providers: [provideDefaultConfig({
        cmsComponents: {
          NavigationComponent: {
            component: NavigationComponent
          }
        }
      })],
      declarations: [NavigationComponent, NavigationUIComponent],
      exports: [NavigationComponent, NavigationUIComponent]
    }]
  }], null, null);
})();
var CategoryNavigationModule = class _CategoryNavigationModule {
  static {
    this.ɵfac = function CategoryNavigationModule_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _CategoryNavigationModule)();
    };
  }
  static {
    this.ɵmod = ɵɵdefineNgModule({
      type: _CategoryNavigationModule,
      declarations: [CategoryNavigationComponent],
      imports: [CommonModule, NavigationModule, I18nModule],
      exports: [CategoryNavigationComponent]
    });
  }
  static {
    this.ɵinj = ɵɵdefineInjector({
      providers: [provideDefaultConfig({
        cmsComponents: {
          CategoryNavigationComponent: {
            component: CategoryNavigationComponent,
            data: {
              resetMenuOnClose: true
            }
          }
        }
      })],
      imports: [CommonModule, NavigationModule, I18nModule]
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(CategoryNavigationModule, [{
    type: NgModule,
    args: [{
      imports: [CommonModule, NavigationModule, I18nModule],
      providers: [provideDefaultConfig({
        cmsComponents: {
          CategoryNavigationComponent: {
            component: CategoryNavigationComponent,
            data: {
              resetMenuOnClose: true
            }
          }
        }
      })],
      declarations: [CategoryNavigationComponent],
      exports: [CategoryNavigationComponent]
    }]
  }], null, null);
})();
var FooterNavigationComponent = class _FooterNavigationComponent {
  constructor(componentData, service) {
    this.componentData = componentData;
    this.service = service;
    this.node$ = this.service.getNavigationNode(this.componentData.data$);
    this.styleClass$ = this.componentData.data$.pipe(map((d) => d?.styleClass));
  }
  static {
    this.ɵfac = function FooterNavigationComponent_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _FooterNavigationComponent)(ɵɵdirectiveInject(CmsComponentData), ɵɵdirectiveInject(NavigationService));
    };
  }
  static {
    this.ɵcmp = ɵɵdefineComponent({
      type: _FooterNavigationComponent,
      selectors: [["cx-footer-navigation"]],
      standalone: false,
      decls: 2,
      vars: 3,
      consts: [[3, "node", "flyout", "ngClass", "navAriaLabel", 4, "ngIf"], [3, "node", "flyout", "ngClass", "navAriaLabel"]],
      template: function FooterNavigationComponent_Template(rf, ctx) {
        if (rf & 1) {
          ɵɵtemplate(0, FooterNavigationComponent_cx_navigation_ui_0_Template, 3, 8, "cx-navigation-ui", 0);
          ɵɵpipe(1, "async");
        }
        if (rf & 2) {
          ɵɵproperty("ngIf", ɵɵpipeBind1(1, 1, ctx.node$));
        }
      },
      dependencies: [NgClass, NgIf, NavigationUIComponent, AsyncPipe, TranslatePipe],
      encapsulation: 2,
      changeDetection: 0
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(FooterNavigationComponent, [{
    type: Component,
    args: [{
      selector: "cx-footer-navigation",
      changeDetection: ChangeDetectionStrategy.OnPush,
      standalone: false,
      template: `<cx-navigation-ui
  *ngIf="node$ | async as node"
  [node]="node"
  [flyout]="false"
  [ngClass]="(styleClass$ | async) ?? ''"
  [navAriaLabel]="'navigation.footerNavLabel' | cxTranslate"
></cx-navigation-ui>
`
    }]
  }], () => [{
    type: CmsComponentData
  }, {
    type: NavigationService
  }], null);
})();
var FooterNavigationModule = class _FooterNavigationModule {
  static {
    this.ɵfac = function FooterNavigationModule_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _FooterNavigationModule)();
    };
  }
  static {
    this.ɵmod = ɵɵdefineNgModule({
      type: _FooterNavigationModule,
      declarations: [FooterNavigationComponent],
      imports: [CommonModule, RouterModule, NavigationModule, GenericLinkModule, I18nModule],
      exports: [FooterNavigationComponent]
    });
  }
  static {
    this.ɵinj = ɵɵdefineInjector({
      providers: [provideDefaultConfig({
        cmsComponents: {
          FooterNavigationComponent: {
            component: FooterNavigationComponent
          }
        }
      })],
      imports: [CommonModule, RouterModule, NavigationModule, GenericLinkModule, I18nModule]
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(FooterNavigationModule, [{
    type: NgModule,
    args: [{
      imports: [CommonModule, RouterModule, NavigationModule, GenericLinkModule, I18nModule],
      providers: [provideDefaultConfig({
        cmsComponents: {
          FooterNavigationComponent: {
            component: FooterNavigationComponent
          }
        }
      })],
      declarations: [FooterNavigationComponent],
      exports: [FooterNavigationComponent]
    }]
  }], null, null);
})();
var PageTitleModule = class _PageTitleModule {
  static {
    this.ɵfac = function PageTitleModule_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _PageTitleModule)();
    };
  }
  static {
    this.ɵmod = ɵɵdefineNgModule({
      type: _PageTitleModule,
      declarations: [PageTitleComponent],
      imports: [CommonModule, RouterModule, PageMetaModule],
      exports: [PageTitleComponent]
    });
  }
  static {
    this.ɵinj = ɵɵdefineInjector({
      providers: [provideDefaultConfig({
        cmsComponents: {
          PageTitleComponent: {
            component: PageTitleComponent
          }
        }
      })],
      imports: [CommonModule, RouterModule, PageMetaModule]
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PageTitleModule, [{
    type: NgModule,
    args: [{
      imports: [CommonModule, RouterModule, PageMetaModule],
      providers: [provideDefaultConfig({
        cmsComponents: {
          PageTitleComponent: {
            component: PageTitleComponent
          }
        }
      })],
      declarations: [PageTitleComponent],
      exports: [PageTitleComponent]
    }]
  }], null, null);
})();
var HighlightPipe = class _HighlightPipe {
  transform(text, match, caseSensitive = true) {
    if (!match) {
      return text;
    }
    if (!caseSensitive) {
      const textStartIndex = text.toLowerCase().indexOf(match.toLowerCase());
      const matchLength = match.length;
      return text.replaceAll(text.substring(textStartIndex, textStartIndex + matchLength), `<span class="highlight">${text.substring(textStartIndex, textStartIndex + matchLength)}</span>`);
    }
    return text.replace(match.trim(), `<span class="highlight">${match.trim()}</span>`);
  }
  static {
    this.ɵfac = function HighlightPipe_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _HighlightPipe)();
    };
  }
  static {
    this.ɵpipe = ɵɵdefinePipe({
      name: "cxHighlight",
      type: _HighlightPipe,
      pure: true,
      standalone: false
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(HighlightPipe, [{
    type: Pipe,
    args: [{
      name: "cxHighlight",
      standalone: false
    }]
  }], null, null);
})();
var SearchBoxSuggestionSelectedEvent = class extends CxEvent {
  static {
    this.type = "SearchBoxSuggestionSelectedEvent";
  }
};
var SearchBoxProductSelectedEvent = class extends CxEvent {
  static {
    this.type = "SearchBoxProductSelectedEvent";
  }
};
var HAS_SEARCH_RESULT_CLASS = "has-searchbox-results";
var SearchBoxComponentService = class _SearchBoxComponentService {
  constructor(searchService, routingService, translationService, winRef, eventService) {
    this.searchService = searchService;
    this.routingService = routingService;
    this.translationService = translationService;
    this.winRef = winRef;
    this.eventService = eventService;
    this.chosenWord = new ReplaySubject();
    this.sharedEvent = new ReplaySubject();
    this.searchCompleted = new BehaviorSubject(false);
    this.enableRecentSearches = false;
    this.enableTrendingSearches = false;
    this.currentQueryLength = 0;
    this.hasKeywordRedirect = false;
  }
  /**
   * Executes the search for products and suggestions,
   * unless the configuration is setup to not search for
   * products or suggestions.
   */
  search(query, config) {
    this.hasKeywordRedirect = false;
    this.currentQueryLength = query ? query.length : 0;
    this.searchCompleted.next(false);
    if (!this.enableRecentSearches && !this.enableTrendingSearches && (!query || query === "")) {
      this.clearResults();
      return;
    }
    if (config.minCharactersBeforeRequest && query.length < config.minCharactersBeforeRequest) {
      return;
    }
    let productsComplete = !config.displayProducts;
    let suggestionsComplete = !config.displaySuggestions;
    if (config.displayProducts) {
      this.searchService.searchWithCompletion(query, {
        pageSize: config.maxProducts
      }).subscribe((result) => {
        if (result?.type === productGroup_actions.SEARCH_PRODUCTS_SUCCESS && result?.payload?.keywordRedirectUrl) {
          this.hasKeywordRedirect = true;
        }
        productsComplete = true;
        this.checkSearchCompletion(productsComplete, suggestionsComplete);
      });
    }
    if (config.displaySuggestions) {
      this.searchService.searchSuggestionsWithCompletion(query, {
        pageSize: config.maxSuggestions
      }).subscribe((result) => {
        if (result?.type === productGroup_actions.GET_PRODUCT_SUGGESTIONS_SUCCESS && result?.payload?.keywordRedirectUrl) {
          this.hasKeywordRedirect = true;
        }
        suggestionsComplete = true;
        this.checkSearchCompletion(productsComplete, suggestionsComplete);
      });
    }
  }
  /**
   * Check if search operations are complete based on actual completion flags
   */
  checkSearchCompletion(productsComplete, suggestionsComplete) {
    if (productsComplete && suggestionsComplete && this.currentQueryLength > 0) {
      this.searchCompleted.next(true);
    }
  }
  /**
   * Returns an observable with the SearchResults. When there's any
   * result, the body tag will get a classname, so that specific style
   * rules can be applied.
   */
  getResults(config) {
    return combineLatest([this.getProductResults(config), this.getProductSuggestions(config), this.getSearchMessage(config)]).pipe(map(([productResults, suggestions, message]) => {
      return {
        products: productResults ? productResults.products : void 0,
        suggestions,
        message
      };
    }), tap((results) => this.toggleBodyClass(HAS_SEARCH_RESULT_CLASS, this.hasResults(results))));
  }
  /**
   * Clears the searchbox results, so that old values are
   * no longer emited upon next search.
   */
  clearResults() {
    this.searchService.clearResults();
    this.toggleBodyClass(HAS_SEARCH_RESULT_CLASS, false);
    this.hasKeywordRedirect = false;
    this.currentQueryLength = 0;
  }
  hasBodyClass(className) {
    return this.winRef.document.body.classList.contains(className);
  }
  toggleBodyClass(className, add) {
    if (add === void 0) {
      this.winRef.document.body.classList.toggle(className);
    } else {
      add ? this.winRef.document.body.classList.add(className) : this.winRef.document.body.classList.remove(className);
    }
  }
  /**
   * Dispatches a searchbox event for product selected
   *
   * @param eventData data for the "SearchBoxProductSelectedEvent"
   */
  dispatchProductSelectedEvent(eventData) {
    this.eventService.dispatch({
      freeText: eventData.freeText,
      productCode: eventData.productCode
    }, SearchBoxProductSelectedEvent);
  }
  /**
   * Dispatches a searchbox event for suggestion selected
   *
   * @param eventData data for the "SearchBoxSuggestionSelectedEvent"
   */
  dispatchSuggestionSelectedEvent(eventData) {
    this.eventService.dispatch({
      freeText: eventData.freeText,
      selectedSuggestion: eventData.selectedSuggestion,
      searchSuggestions: eventData.searchSuggestions
    }, SearchBoxSuggestionSelectedEvent);
  }
  /**
   * For search results model, it returns true when:
   * * there is any product OR
   * * the is any search suggestion OR
   * * there is a message.
   *
   * Otherwise it returns false.
   */
  hasResults(results) {
    return !!results.products && results.products.length > 0 || !!results.suggestions && results.suggestions.length > 0 || !!results.message || !!results.recentSearches;
  }
  /**
   * Emits product search results in case when the config property `displayProducts` is true.
   * Otherwise it emits an empty object.
   */
  getProductResults(config) {
    if (config.displayProducts) {
      return this.searchService.getResults();
    } else {
      return of({});
    }
  }
  /**
   * Loads suggestions from the backend. In case there's no suggestion
   * available, we try to get an exact match suggestion.
   */
  getProductSuggestions(config) {
    if (!config.displaySuggestions) {
      return of([]);
    } else {
      return this.searchService.getSuggestionResults().pipe(map((res) => res.map((suggestion) => suggestion.value).filter(isNotUndefined)), switchMap((suggestions) => {
        if (suggestions.length === 0) {
          return this.getExactSuggestion(config).pipe(map((match) => match ? [match] : []));
        } else {
          return of(suggestions);
        }
      }));
    }
  }
  /**
   * Whenever there is at least 1 product, we simulate
   * a suggestion to provide easy access to the search result page
   */
  getExactSuggestion(config) {
    return this.getProductResults(config).pipe(switchMap((productResult) => {
      return productResult.products && productResult.products.length > 0 ? this.fetchTranslation("searchBox.help.exactMatch", {
        term: productResult.freeTextSearch
      }) : of(void 0);
    }));
  }
  /**
   * Emits a 'no match' message, in case the product search results and search suggestions are empty.
   * Otherwise it emits null.
   */
  getSearchMessage(config) {
    return combineLatest([this.getProductResults(config), this.getProductSuggestions(config)]).pipe(switchMap(([productResult, suggestions]) => {
      if (productResult && productResult.products && productResult.products.length === 0 && suggestions && suggestions.length === 0) {
        return this.fetchTranslation("searchBox.help.noMatch");
      } else {
        return of(void 0);
      }
    }));
  }
  /**
   * Navigates to the search result page with a given query
   * after waiting for all search operations to complete
   */
  launchSearchPage(query) {
    this.searchCompleted.pipe(
      filter((complete) => complete),
      // Only proceed when true
      take(1)
      // Take only the first completion
    ).subscribe(() => {
      if (!this.hasKeywordRedirect) {
        this.routingService.go({
          cxRoute: "search",
          params: {
            query
          }
        });
      }
    });
  }
  fetchTranslation(translationKey, options) {
    return this.translationService.translate(translationKey, options);
  }
  changeSelectedWord(selectedWord) {
    this.chosenWord.next(selectedWord);
  }
  shareEvent($event) {
    this.sharedEvent.next($event);
  }
  setTrendingSearches(enabled = false) {
    this.enableTrendingSearches = enabled;
  }
  setRecentSearches(enabled = false) {
    this.enableRecentSearches = enabled;
  }
  static {
    this.ɵfac = function SearchBoxComponentService_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _SearchBoxComponentService)(ɵɵinject(SearchboxService), ɵɵinject(RoutingService), ɵɵinject(TranslationService), ɵɵinject(WindowRef), ɵɵinject(EventService));
    };
  }
  static {
    this.ɵprov = ɵɵdefineInjectable({
      token: _SearchBoxComponentService,
      factory: _SearchBoxComponentService.ɵfac,
      providedIn: "root"
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(SearchBoxComponentService, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], () => [{
    type: SearchboxService
  }, {
    type: RoutingService
  }, {
    type: TranslationService
  }, {
    type: WindowRef
  }, {
    type: EventService
  }], null);
})();
var SearchBoxOutlets;
(function(SearchBoxOutlets2) {
  SearchBoxOutlets2["RECENT_SEARCHES"] = "SearchBoxOutlets.RECENT_SEARCHES";
  SearchBoxOutlets2["TRENDING_SEARCHES"] = "SearchBoxOutlets.TRENDING_SEARCHES";
})(SearchBoxOutlets || (SearchBoxOutlets = {}));
var SearchBoxFeatures;
(function(SearchBoxFeatures2) {
  SearchBoxFeatures2["SEARCH_BOX_V2"] = "searchBoxV2";
  SearchBoxFeatures2["TRENDING_SEARCHES_FEATURE"] = "trendingSearches";
})(SearchBoxFeatures || (SearchBoxFeatures = {}));
var DEFAULT_SEARCH_BOX_CONFIG = {
  minCharactersBeforeRequest: 1,
  displayProducts: true,
  displaySuggestions: true,
  maxProducts: 5,
  maxSuggestions: 5,
  displayProductImages: true,
  recentSearches: true,
  maxRecentSearches: 5,
  trendingSearches: true,
  maxTrendingSearches: 5
};
var SEARCHBOX_IS_ACTIVE = "searchbox-is-active";
var SearchBoxComponent = class _SearchBoxComponent {
  /**
   * Sets the search box input field
   */
  set queryText(value) {
    if (value) {
      this.updateChosenWord(value);
      this.search(value);
    }
  }
  get searchBoxV2() {
    return this.isEnabledFeature(SearchBoxFeatures.SEARCH_BOX_V2);
  }
  get hasSearchBoxV2() {
    const hostElement = this.elementRef.nativeElement;
    return hostElement.classList.contains("search-box-v2");
  }
  /**
   * Listener for clickout out of searchInput and searchPanel
   * */
  clickout(event) {
    if (!this.elementRef.nativeElement.contains(event.target)) {
      this.softClose();
    }
  }
  onEscape() {
    if (this.winRef.document.activeElement !== this.searchInputEl?.nativeElement || this.searchBoxActive) {
      setTimeout(() => {
        this.searchInputEl.nativeElement.focus();
      });
    }
  }
  get isMobile() {
    return this.breakpointService?.isDown(BREAKPOINT.sm);
  }
  constructor(searchBoxComponentService, componentData, winRef, routingService) {
    this.searchBoxComponentService = searchBoxComponentService;
    this.componentData = componentData;
    this.winRef = winRef;
    this.routingService = routingService;
    this.elementRef = inject(ElementRef);
    this.renderer = inject(Renderer2);
    this.searchBoxOutlets = SearchBoxOutlets;
    this.searchBoxFeatures = SearchBoxFeatures;
    this.iconTypes = ICON_TYPE;
    this.searchBoxActive = false;
    this.ignoreCloseEvent = false;
    this.chosenWord = "";
    this.subscriptions = new Subscription();
    this.changeDetecorRef = inject(ChangeDetectorRef, {
      optional: true
    });
    this.breakpointService = inject(BreakpointService, {
      optional: true
    });
    this.featureConfigService = inject(FeatureConfigService, {
      optional: true
    });
    this.config$ = (this.componentData?.data$ || of({})).pipe(map((config) => {
      const isBool = (obj, prop) => obj[prop] !== "false" && obj[prop] !== false;
      return __spreadValues(__spreadProps(__spreadValues(__spreadValues({}, DEFAULT_SEARCH_BOX_CONFIG), config), {
        displayProducts: isBool(config, "displayProducts"),
        displayProductImages: isBool(config, "displayProductImages"),
        displaySuggestions: isBool(config, "displaySuggestions")
      }), this.config);
    }), tap((config) => this.config = config));
    this.results$ = this.config$.pipe(switchMap((config) => this.searchBoxComponentService.getResults(config)));
    this.items$ = this.results$.pipe(map((result) => result.products?.map((prod) => of(prod))));
    useFeatureStyles("a11ySearchboxLabel");
    useFeatureStyles("a11yKeyboardFocusInSearchBox");
  }
  ngOnInit() {
    const routeStateSubscription = this.routingService.getRouterState().pipe(filter((data) => !data.nextState)).subscribe((data) => {
      if (!(data.state.context?.id === "search" && data.state.context?.type === PageType.CONTENT_PAGE)) {
        this.updateChosenWord("");
      }
    });
    this.subscriptions.add(routeStateSubscription);
    const chosenWordSubscription = this.searchBoxComponentService.chosenWord.subscribe((chosenWord) => {
      this.updateChosenWord(chosenWord);
    });
    this.subscriptions.add(chosenWordSubscription);
    const UIEventSubscription = this.searchBoxComponentService.sharedEvent.subscribe((event) => {
      this.propagateEvent(event);
    });
    this.subscriptions.add(UIEventSubscription);
  }
  /**
   * The Searchbox should not be focusable while not visible.
   */
  getTabIndex(isMobile) {
    if (isMobile) {
      return this.searchBoxActive ? 0 : -1;
    }
    return 0;
  }
  /**
   * Closes the searchBox and opens the search result page.
   */
  search(query) {
    this.searchBoxComponentService.search(query, this.config);
    this.checkOuterResults();
    this.open();
  }
  /**
   * Opens the type-ahead searchBox
   */
  open() {
    if (!this.searchBoxActive) {
      this.searchBoxComponentService.toggleBodyClass(SEARCHBOX_IS_ACTIVE, true);
      this.searchBoxActive = true;
      this.searchInputEl?.nativeElement.focus();
    }
  }
  /**
   * Dispatch UI events for Suggestion selected
   *
   * @param eventData the data for the event
   */
  dispatchSuggestionEvent(eventData) {
    this.searchBoxComponentService.dispatchSuggestionSelectedEvent(eventData);
  }
  /**
   * Dispatch UI events for Product selected
   *
   * @param eventData the data for the event
   */
  dispatchProductEvent(eventData) {
    this.searchBoxComponentService.dispatchProductSelectedEvent(eventData);
  }
  /**
   * Closes the type-ahead searchBox.
   */
  close(force = false) {
    setTimeout(() => {
      if (!this.ignoreCloseEvent && !this.isSearchBoxFocused() || force) {
        this.blurSearchBox();
      }
    });
  }
  softClose() {
    this.searchBoxComponentService.toggleBodyClass(SEARCHBOX_IS_ACTIVE, false);
    this.searchBoxActive = false;
  }
  blurSearchBox() {
    this.softClose();
    this.searchBoxComponentService.toggleBodyClass(SEARCHBOX_IS_ACTIVE, false);
    this.searchBoxActive = false;
    this.changeDetecorRef?.detectChanges();
    this.searchButton?.nativeElement.focus();
  }
  // Check if focus is on searchbox or result list elements
  isSearchBoxFocused() {
    return this.getResultElements().includes(this.getFocusedElement()) || this.winRef.document.querySelector('input[aria-label="Search"]') === this.getFocusedElement();
  }
  checkOuterResults() {
    const recentSearches = this.elementRef.nativeElement.querySelector("cx-recent-searches .recent-searches");
    const trendingSearches = this.elementRef.nativeElement.querySelector("cx-trending-searches .trending-searches");
    const results = this.elementRef.nativeElement.querySelector(".results");
    if (recentSearches || trendingSearches) {
      this.renderer.addClass(results, "has-outer-results");
    }
  }
  /**
   * Especially in mobile we do not want the search icon
   * to focus the input again when it's already open.
   * */
  avoidReopen(event) {
    if (this.searchBoxComponentService.hasBodyClass(SEARCHBOX_IS_ACTIVE)) {
      this.close();
      event.preventDefault();
    }
  }
  // Return result list as HTMLElement array
  getResultElements() {
    return Array.from(this.winRef.document.querySelectorAll(".products ul:not(.hidden) > li a, .suggestions ul  > li a, .recent-searches ul > li a,.trending-searches ul > li a, .carousel-panel .item.active > a, .products .carousel-panel > button:not([disabled])"));
  }
  // Return group list as HTMLElement array
  getGroupElements() {
    const groups = [];
    groups.push(Array.from(this.winRef.document.querySelectorAll(".products ul:not(.hidden) > li a")));
    groups.push(Array.from(this.winRef.document.querySelectorAll(".suggestions ul  > li a")));
    groups.push(Array.from(this.winRef.document.querySelectorAll(".trending-searches-container.d-block .trending-searches ul > li a")));
    groups.push(Array.from(this.winRef.document.querySelectorAll(".recent-searches ul > li a")));
    groups.push(Array.from(this.winRef.document.querySelectorAll(".carousel-panel .item.active > a, .carousel-panel > button:not([disabled])")));
    groups.push(Array.from(this.winRef.document.querySelectorAll(".search-panel-close-btn")));
    return groups.filter((group) => group.length);
  }
  // Return focused element as HTMLElement
  getFocusedElement() {
    return this.winRef.document.activeElement;
  }
  updateChosenWord(chosenWord) {
    this.chosenWord = chosenWord;
    if (this.searchInputEl) {
      this.searchInputEl.nativeElement.value = this.chosenWord;
    }
  }
  getFocusedIndex() {
    return this.getResultElements().indexOf(this.getFocusedElement());
  }
  getFocusedGroupIndex() {
    return this.getGroupElements().findIndex((group) => group.indexOf(this.getFocusedElement()) !== -1) ?? 0;
  }
  propagateEvent(event) {
    if (event.code) {
      switch (event.code) {
        case "Escape":
        case "Enter":
          this.close(true);
          return;
        case "ArrowUp":
          this.focusPreviousChild(event);
          return;
        case "ArrowDown":
          this.focusNextChild(event);
          return;
        case "ArrowLeft":
          this.focusPreviousGroup(event);
          return;
        case "ArrowRight":
          this.focusNextGroup(event);
          return;
        default:
          return;
      }
    } else if (event.type === "blur") {
      this.close();
    }
  }
  // Focus on previous item in results list
  focusPreviousChild(event) {
    event.preventDefault();
    const [results, focusedIndex] = [this.getResultElements(), this.getFocusedIndex()];
    if (results.length) {
      if (focusedIndex < 1) {
        results[results.length - 1].focus();
      } else {
        results[focusedIndex - 1].focus();
      }
    }
  }
  // Focus on next item in results list
  focusNextChild(event) {
    this.open();
    event.preventDefault();
    const [results, focusedIndex] = [this.getResultElements(), this.getFocusedIndex()];
    if (results.length) {
      if (this.featureConfigService?.isEnabled("a11ySearchableDropdownFirstElementFocus")) {
        this.winRef.document.querySelector("header")?.classList.remove("mouse-focus");
      }
      if (focusedIndex >= results.length - 1) {
        results[0].focus();
      } else {
        results[focusedIndex + 1].focus();
      }
    }
  }
  // Focus on previous item in results list
  focusPreviousGroup(event) {
    event.preventDefault();
    const results = this.getGroupElements();
    const focusedGroupIndex = this.getFocusedGroupIndex();
    if (!results.length || focusedGroupIndex < 0 || focusedGroupIndex >= results.length) {
      return;
    }
    const currentGroup = results[focusedGroupIndex];
    if (currentGroup.length === 0) {
      return;
    }
    const previousGroupIndex = focusedGroupIndex > 0 ? focusedGroupIndex - 1 : 0;
    const previousGroup = results[previousGroupIndex];
    if (previousGroup.length > 0) {
      previousGroup[0].focus();
    }
  }
  // Focus on next item in results list
  focusNextGroup(event) {
    this.open();
    event.preventDefault();
    const results = this.getGroupElements();
    const focusedGroupIndex = this.getFocusedGroupIndex();
    if (!results.length || focusedGroupIndex < 0 || focusedGroupIndex >= results.length) {
      return;
    }
    let nextGroupIndex = focusedGroupIndex + 1;
    while (nextGroupIndex < results.length && results[nextGroupIndex].length === 0) {
      nextGroupIndex++;
    }
    if (nextGroupIndex >= results.length) {
      nextGroupIndex = 0;
      if (results[nextGroupIndex].length === 0) {
        return;
      }
    }
    results[nextGroupIndex][0].focus();
  }
  carouselEventPropagator(event) {
    if (!event || !event?.code) {
      return;
    }
    switch (event.code) {
      case "ArrowRight":
        this.focusNextChild(event);
        return;
      case "ArrowLeft": {
        this.getGroupElements().forEach((group) => {
          if (group.indexOf(this.getFocusedElement()) !== -1) {
            if (group.indexOf(this.getFocusedElement()) === 0) {
              this.focusPreviousGroup(event);
            } else {
              this.focusPreviousChild(event);
            }
          }
        });
        return;
      }
      case "ArrowUp":
        this.focusNextGroup(event);
        return;
      default:
        return;
    }
  }
  /**
   * Opens the PLP with the given query.
   *
   * TODO: if there's a single product match, we could open the PDP.
   */
  launchSearchResult(query) {
    if (!query || query.trim().length === 0) {
      return;
    }
    this.close();
    this.searchBoxComponentService.launchSearchPage(query);
  }
  /**
   * Disables closing the search result list.
   */
  disableClose() {
    this.ignoreCloseEvent = true;
  }
  preventDefault(ev) {
    ev.preventDefault();
  }
  /**
   * Clears the search box input field
   */
  clear(el) {
    this.disableClose();
    el.value = "";
    this.searchBoxComponentService.clearResults();
    setTimeout(() => {
      el.focus();
      this.ignoreCloseEvent = false;
    });
  }
  isEnabledFeature(feature) {
    return this.featureConfigService?.isEnabled(feature);
  }
  ngOnDestroy() {
    this.subscriptions?.unsubscribe();
  }
  static {
    this.ɵfac = function SearchBoxComponent_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _SearchBoxComponent)(ɵɵdirectiveInject(SearchBoxComponentService), ɵɵdirectiveInject(CmsComponentData, 8), ɵɵdirectiveInject(WindowRef), ɵɵdirectiveInject(RoutingService));
    };
  }
  static {
    this.ɵcmp = ɵɵdefineComponent({
      type: _SearchBoxComponent,
      selectors: [["cx-searchbox"]],
      viewQuery: function SearchBoxComponent_Query(rf, ctx) {
        if (rf & 1) {
          ɵɵviewQuery(_c53, 5);
          ɵɵviewQuery(_c54, 5);
        }
        if (rf & 2) {
          let _t;
          ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.searchInputEl = _t.first);
          ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.searchButton = _t.first);
        }
      },
      hostVars: 2,
      hostBindings: function SearchBoxComponent_HostBindings(rf, ctx) {
        if (rf & 1) {
          ɵɵlistener("click", function SearchBoxComponent_click_HostBindingHandler($event) {
            return ctx.clickout($event);
          }, false, ɵɵresolveDocument)("keydown.escape", function SearchBoxComponent_keydown_escape_HostBindingHandler() {
            return ctx.onEscape();
          });
        }
        if (rf & 2) {
          ɵɵclassProp("search-box-v2", ctx.searchBoxV2);
        }
      },
      inputs: {
        config: "config",
        queryText: "queryText"
      },
      standalone: false,
      decls: 4,
      vars: 5,
      consts: [["searchInput", ""], ["searchButton", ""], ["carouselItem", ""], ["role", "search", 4, "cxFeature"], ["role", "search", "class", "cx-searchbox-container", 4, "cxFeature"], ["class", "results", "id", "results", 3, "no-headers", "click", 4, "ngIf"], ["role", "search"], [1, "searchbox"], ["autocomplete", "off", "aria-describedby", "initialDescription", "aria-controls", "results", 3, "click", "input", "blur", "keydown.tab", "keydown.escape", "keydown.enter", "keydown.arrowup", "keydown.arrowdown", "placeholder", "value"], [1, "reset", 3, "click", "title"], [3, "type"], ["role", "presentation", 1, "search-icon", 3, "title"], [1, "search", 3, "click", "title"], ["role", "search", 1, "cx-searchbox-container"], [1, "searchbox", "cxFeat_a11ySearchboxLabel"], [1, "cx-input-label"], [1, "cx-label-inner-container"], ["id", "results", 1, "results", 3, "click"], ["class", "btn btn-tertiary search-panel-close-btn", 3, "keydown.arrowdown", "click", 4, "ngIf"], [3, "innerHTML", 4, "ngIf"], ["class", "suggestions", 4, "ngIf"], [1, "trending-searches-container"], [4, "ngIf"], [1, "products"], ["role", "listbox", 3, "hidden", 4, "ngIf"], ["class", "search-panel-header", 3, "d-none", "header-panel-divider", 4, "ngIf"], ["id", "initialDescription", 1, "cx-visually-hidden"], ["aria-live", "assertive", "class", "cx-visually-hidden", 4, "ngIf"], [1, "btn", "btn-tertiary", "search-panel-close-btn", 3, "keydown.arrowdown", "click"], [3, "innerHTML"], [1, "suggestions"], ["tabindex", "0", "role", "listbox"], ["role", "option", 4, "ngFor", "ngForOf"], ["role", "option"], ["role", "option", 3, "keydown.arrowup", "keydown.arrowdown", "keydown.arrowleft", "keydown.arrowright", "keydown.enter", "keydown.escape", "blur", "mousedown", "click", "innerHTML", "routerLink"], [4, "cxFeature"], [3, "cxOutlet", "cxOutletContext"], ["role", "listbox"], ["role", "option", 3, "keydown.arrowup", "keydown.arrowdown", "keydown.arrowleft", "keydown.arrowright", "keydown.enter", "keydown.escape", "blur", "mousedown", "click", "routerLink"], ["format", "thumbnail", "role", "presentation", 3, "container", 4, "ngIf"], [1, "name", 3, "innerHTML"], [1, "price"], ["format", "thumbnail", "role", "presentation", 3, "container"], ["role", "region", "itemWidth", "160px", 3, "keybordEvent", "items", "template"], ["tabindex", "0", 3, "keydown.enter", "keydown.escape", "keydown.arrowleft", "keydown.arrowright", "keydown.arrowup", "mousedown", "click", "routerLink"], ["tabindex", "0", "format", "product", 3, "container", "alt"], [1, "cx-product-name", 3, "innerHtml"], [1, "actions"], ["cxInnerComponentsHost", ""], [1, "search-panel-header"], ["class", "search-panel-message", 3, "innerHTML", 4, "ngIf"], [1, "search-panel-message", 3, "innerHTML"], [1, "suggestions-header"], ["class", "trendingSearches-header", 4, "ngIf"], [1, "recentSearches-header"], ["class", "products-header", 4, "ngIf"], [1, "trendingSearches-header"], [1, "products-header"], ["aria-live", "assertive", 1, "cx-visually-hidden"]],
      template: function SearchBoxComponent_Template(rf, ctx) {
        if (rf & 1) {
          ɵɵtemplate(0, SearchBoxComponent_div_0_Template, 20, 33, "div", 3)(1, SearchBoxComponent_div_1_Template, 23, 33, "div", 4)(2, SearchBoxComponent_div_2_Template, 17, 19, "div", 5);
          ɵɵpipe(3, "async");
        }
        if (rf & 2) {
          ɵɵproperty("cxFeature", "!a11ySearchboxLabel");
          ɵɵadvance();
          ɵɵproperty("cxFeature", "a11ySearchboxLabel");
          ɵɵadvance();
          ɵɵproperty("ngIf", ɵɵpipeBind1(3, 3, ctx.results$));
        }
      },
      dependencies: [NgForOf, NgIf, RouterLink, MediaComponent, IconComponent, OutletDirective, FeatureDirective, CarouselComponent, AsyncPipe, UrlPipe, TranslatePipe, HighlightPipe],
      encapsulation: 2,
      changeDetection: 0
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(SearchBoxComponent, [{
    type: Component,
    args: [{
      selector: "cx-searchbox",
      changeDetection: ChangeDetectionStrategy.OnPush,
      standalone: false,
      template: `<div
  *cxFeature="'!a11ySearchboxLabel'"
  [attr.aria-label]="'searchBox.productSearch' | cxTranslate"
  role="search"
>
  <label class="searchbox" [class.dirty]="!!searchInput.value">
    <input
      #searchInput
      [placeholder]="'searchBox.placeholder' | cxTranslate"
      autocomplete="off"
      aria-describedby="initialDescription"
      aria-controls="results"
      [attr.tabindex]="getTabIndex(isMobile | async)"
      [attr.aria-label]="'searchBox.placeholder' | cxTranslate"
      (click)="search(searchInput.value)"
      (input)="search(searchInput.value)"
      (blur)="close()"
      (keydown.tab)="close()"
      (keydown.escape)="close()"
      (keydown.enter)="
        close(true);
        launchSearchResult(searchInput.value);
        updateChosenWord(searchInput.value)
      "
      (keydown.arrowup)="focusPreviousChild($any($event))"
      (keydown.arrowdown)="focusNextChild($any($event))"
      value="{{ chosenWord }}"
    />

    <button
      [attr.aria-label]="'common.reset' | cxTranslate"
      [title]="'common.reset' | cxTranslate"
      (click)="clear(searchInput)"
      class="reset"
    >
      <cx-icon [type]="iconTypes.RESET"></cx-icon>
    </button>

    <div
      role="presentation"
      class="search-icon"
      [title]="'common.search' | cxTranslate"
    >
      <cx-icon [type]="iconTypes.SEARCH"></cx-icon>
    </div>

    <button
      #searchButton
      [attr.aria-label]="'common.search' | cxTranslate"
      [title]="'common.search' | cxTranslate"
      class="search"
      (click)="open()"
    >
      <cx-icon [type]="iconTypes.SEARCH"></cx-icon>
    </button>
  </label>
</div>
<div
  *cxFeature="'a11ySearchboxLabel'"
  [attr.aria-label]="'searchBox.productSearch' | cxTranslate"
  role="search"
  class="cx-searchbox-container"
>
  <label
    class="searchbox cxFeat_a11ySearchboxLabel"
    [class.dirty]="!!searchInput?.value"
  >
    <span class="cx-input-label">{{ 'common.search' | cxTranslate }}</span>
    <div class="cx-label-inner-container">
      <input
        #searchInput
        [placeholder]="'searchBox.placeholder' | cxTranslate"
        autocomplete="off"
        aria-describedby="initialDescription"
        aria-controls="results"
        [attr.tabindex]="getTabIndex(isMobile | async)"
        (click)="open()"
        (input)="search(searchInput.value)"
        (blur)="close()"
        (keydown.tab)="close()"
        (keydown.escape)="close()"
        (keydown.enter)="
          close(true);
          launchSearchResult(searchInput.value);
          updateChosenWord(searchInput.value)
        "
        (keydown.arrowup)="focusPreviousChild($any($event))"
        (keydown.arrowdown)="focusNextChild($any($event))"
        value="{{ chosenWord }}"
      />

      <button
        [attr.aria-label]="'common.reset' | cxTranslate"
        [title]="'common.reset' | cxTranslate"
        (click)="clear(searchInput)"
        class="reset"
      >
        <cx-icon [type]="iconTypes.RESET"></cx-icon>
      </button>

      <div
        role="presentation"
        class="search-icon"
        [title]="'common.search' | cxTranslate"
      >
        <cx-icon [type]="iconTypes.SEARCH"></cx-icon>
      </div>
    </div>
    <button
      #searchButton
      [attr.aria-label]="'common.search' | cxTranslate"
      [title]="'common.search' | cxTranslate"
      class="search"
      (click)="open()"
    >
      <cx-icon [type]="iconTypes.SEARCH"></cx-icon>
    </button>
  </label>
</div>

<div
  *ngIf="results$ | async as result"
  class="results"
  id="results"
  (click)="close(true)"
  [class.no-headers]="
    !isEnabledFeature(searchBoxFeatures.TRENDING_SEARCHES_FEATURE) &&
    !isEnabledFeature(searchBoxFeatures.SEARCH_BOX_V2)
  "
>
  <button
    *ngIf="isEnabledFeature(searchBoxFeatures.SEARCH_BOX_V2)"
    class="btn btn-tertiary search-panel-close-btn"
    (keydown.arrowdown)="focusPreviousGroup($any($event))"
    (click)="close()"
  >
    {{ 'searchBox.closeSearchPanel' | cxTranslate }}
  </button>
  <h3 *ngIf="result.message" [innerHTML]="result.message"></h3>
  <!--RESULT SUGGESTIONS-->
  <div
    class="suggestions"
    *ngIf="(searchInputEl?.nativeElement?.value ?? '').length > 0"
  >
    <ng-container
      *ngIf="
        isEnabledFeature(searchBoxFeatures.TRENDING_SEARCHES_FEATURE) ||
        isEnabledFeature(searchBoxFeatures.SEARCH_BOX_V2)
      "
    >
      <h3>
        {{ 'searchBox.suggestions' | cxTranslate }}
      </h3>
    </ng-container>
    <ul
      tabindex="0"
      attr.aria-label="{{ 'searchBox.ariaLabelSuggestions' | cxTranslate }}"
      role="listbox"
      [class.no-headers]="
        !isEnabledFeature(searchBoxFeatures.TRENDING_SEARCHES_FEATURE) &&
        !isEnabledFeature(searchBoxFeatures.SEARCH_BOX_V2)
      "
    >
      <li *ngFor="let suggestion of result.suggestions" role="option">
        <a
          role="option"
          [innerHTML]="
            suggestion | cxHighlight: searchInputEl?.nativeElement?.value
          "
          [routerLink]="
            {
              cxRoute: 'search',
              params: { query: suggestion },
            } | cxUrl
          "
          (keydown.arrowup)="focusPreviousChild($any($event))"
          (keydown.arrowdown)="focusNextChild($any($event))"
          (keydown.arrowleft)="focusPreviousGroup($any($event))"
          (keydown.arrowright)="focusNextGroup($any($event))"
          (keydown.enter)="close(true)"
          (keydown.escape)="close(true)"
          (blur)="close()"
          (mousedown)="preventDefault($event)"
          (click)="
            dispatchSuggestionEvent({
              freeText: searchInputEl?.nativeElement?.value,
              selectedSuggestion: suggestion,
              searchSuggestions: result.suggestions ?? [],
            });
            updateChosenWord(suggestion)
          "
        >
        </a>
      </li>
    </ul>
  </div>

  <!--  TRENDING SEARCHES-->
  <div
    class="trending-searches-container"
    [class.d-block]="searchInputEl?.nativeElement?.value?.length === 0"
  >
    <ng-container *ngIf="searchBoxFeatures.TRENDING_SEARCHES_FEATURE">
      <ng-container *cxFeature="searchBoxFeatures.TRENDING_SEARCHES_FEATURE">
        <ng-template
          [cxOutlet]="searchBoxOutlets.TRENDING_SEARCHES"
          [cxOutletContext]="{
            maxTrendingSearches: config.maxTrendingSearches,
          }"
        ></ng-template>
      </ng-container>
    </ng-container>
  </div>

  <!--  RECENT SEARCHES-->
  <ng-template
    *ngIf="config.recentSearches"
    [cxOutlet]="searchBoxOutlets.RECENT_SEARCHES"
    [cxOutletContext]="{
      search: searchInputEl?.nativeElement?.value ?? '',
      searchBoxActive: searchBoxActive,
      maxRecentSearches: config.maxRecentSearches,
    }"
  ></ng-template>
  <!--RESULT  PRODUCTS-->
  <div class="products">
    <ng-container
      *ngIf="
        isEnabledFeature(searchBoxFeatures.TRENDING_SEARCHES_FEATURE) ||
        isEnabledFeature(searchBoxFeatures.SEARCH_BOX_V2)
      "
    >
      <h3 *ngIf="result.products?.length">
        {{ 'searchBox.products' | cxTranslate }}
      </h3>
    </ng-container>
    <ul
      *ngIf="result.products"
      attr.aria-label="{{ 'searchBox.ariaLabelProducts' | cxTranslate }}"
      role="listbox"
      [class.hidden]="hasSearchBoxV2"
    >
      <li *ngFor="let product of result.products" role="option">
        <a
          role="option"
          [routerLink]="
            {
              cxRoute: 'product',
              params: product,
            } | cxUrl
          "
          [class.has-media]="config.displayProductImages"
          (keydown.arrowup)="focusPreviousChild($any($event))"
          (keydown.arrowdown)="focusNextChild($any($event))"
          (keydown.arrowleft)="focusPreviousGroup($any($event))"
          (keydown.arrowright)="focusNextGroup($any($event))"
          (keydown.enter)="close(true)"
          (keydown.escape)="close(true)"
          (blur)="close()"
          (mousedown)="preventDefault($event)"
          (click)="
            dispatchProductEvent({
              freeText: searchInputEl?.nativeElement?.value,
              productCode: product.code,
            })
          "
        >
          <cx-media
            *ngIf="config.displayProductImages"
            [container]="product.images?.PRIMARY"
            format="thumbnail"
            role="presentation"
          ></cx-media>
          <div class="name" [innerHTML]="product.nameHtml"></div>
          <span class="price">{{ product.price?.formattedValue }}</span>
        </a>
      </li>
    </ul>
    <ng-container *ngIf="items$ | async as items">
      <cx-carousel
        role="region"
        [attr.aria-label]="'searchBox.ariaLabelProducts' | cxTranslate"
        [items]="items"
        [template]="carouselItem"
        itemWidth="160px"
        (keybordEvent)="carouselEventPropagator($event)"
      >
      </cx-carousel>

      <ng-template #carouselItem let-product="item">
        <a
          tabindex="0"
          [routerLink]="
            {
              cxRoute: 'product',
              params: product,
            } | cxUrl
          "
          [class.has-media]="config.displayProductImages"
          (keydown.enter)="close(true)"
          (keydown.escape)="close(true)"
          (keydown.arrowleft)="carouselEventPropagator($any($event))"
          (keydown.arrowright)="carouselEventPropagator($any($event))"
          (keydown.arrowup)="carouselEventPropagator($any($event))"
          (mousedown)="preventDefault($event)"
          (click)="
            dispatchProductEvent({
              freeText: searchInputEl?.nativeElement?.value,
              productCode: product.code,
            })
          "
        >
          <cx-media
            tabindex="0"
            [container]="product.images?.PRIMARY"
            format="product"
            [alt]="product.name ?? ''"
          ></cx-media>
          <h3 class="cx-product-name" [innerHtml]="product.nameHtml"></h3>
          <div class="price">
            {{ product.price?.formattedValue }}
          </div>
        </a>
        <div class="actions">
          <ng-container cxInnerComponentsHost></ng-container>
        </div>
      </ng-template>
    </ng-container>
  </div>

  <div
    [class.d-none]="!hasSearchBoxV2"
    class="search-panel-header"
    [class.header-panel-divider]="!result.message"
    *ngIf="isEnabledFeature(searchBoxFeatures.SEARCH_BOX_V2)"
  >
    <h3
      class="search-panel-message"
      *ngIf="result.message"
      [innerHTML]="result.message"
    ></h3>
    <ng-container *ngIf="!result.message">
      <h3 class="suggestions-header">
        {{ 'searchBox.suggestions' | cxTranslate }}
      </h3>
      <h3
        class="trendingSearches-header"
        *ngIf="searchInputEl?.nativeElement?.value?.length === 0"
      >
        {{ 'searchBox.trendingSearches' | cxTranslate }}
      </h3>
      <h3 class="recentSearches-header">
        {{ 'searchBox.recentSearches' | cxTranslate }}
      </h3>
      <h3 class="products-header" *ngIf="result.products?.length">
        {{ 'searchBox.products' | cxTranslate }}
      </h3>
    </ng-container>
    <button
      class="btn btn-tertiary search-panel-close-btn"
      (keydown.arrowdown)="focusPreviousGroup($any($event))"
      (click)="close()"
    >
      {{ 'searchBox.closeSearchPanel' | cxTranslate }}
    </button>
  </div>

  <span id="initialDescription" class="cx-visually-hidden">
    {{ 'searchBox.initialDescription' | cxTranslate }}
  </span>
  <div
    *ngIf="result.suggestions?.length || result.products?.length"
    aria-live="assertive"
    class="cx-visually-hidden"
  >
    {{
      'searchBox.suggestionsResult'
        | cxTranslate: { count: result.suggestions?.length }
    }}
    {{
      'searchBox.productsResult'
        | cxTranslate: { count: result.products?.length }
    }}
    <ng-container *cxFeature="'!a11ySearchboxAssistiveMessage'">
      {{ 'searchBox.initialDescription' | cxTranslate }}
    </ng-container>
  </div>
</div>
`
    }]
  }], () => [{
    type: SearchBoxComponentService
  }, {
    type: CmsComponentData,
    decorators: [{
      type: Optional
    }]
  }, {
    type: WindowRef
  }, {
    type: RoutingService
  }], {
    config: [{
      type: Input
    }],
    queryText: [{
      type: Input,
      args: ["queryText"]
    }],
    searchBoxV2: [{
      type: HostBinding,
      args: ["class.search-box-v2"]
    }],
    clickout: [{
      type: HostListener,
      args: ["document:click", ["$event"]]
    }],
    searchInputEl: [{
      type: ViewChild,
      args: ["searchInput"]
    }],
    searchButton: [{
      type: ViewChild,
      args: ["searchButton"]
    }],
    onEscape: [{
      type: HostListener,
      args: ["keydown.escape"]
    }],
    changeDetecorRef: [{
      type: Optional
    }],
    breakpointService: [{
      type: Optional
    }],
    featureConfigService: [{
      type: Optional
    }]
  });
})();
var ViewConfig = class _ViewConfig {
  static {
    this.ɵfac = function ViewConfig_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _ViewConfig)();
    };
  }
  static {
    this.ɵprov = ɵɵdefineInjectable({
      token: _ViewConfig,
      factory: function ViewConfig_Factory(__ngFactoryType__) {
        let __ngConditionalFactory__ = null;
        if (__ngFactoryType__) {
          __ngConditionalFactory__ = new (__ngFactoryType__ || _ViewConfig)();
        } else {
          __ngConditionalFactory__ = ɵɵinject(Config);
        }
        return __ngConditionalFactory__;
      },
      providedIn: "root"
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(ViewConfig, [{
    type: Injectable,
    args: [{
      providedIn: "root",
      useExisting: Config
    }]
  }], null, null);
})();
var ProvideLcpPresenceDirective = class _ProvideLcpPresenceDirective {
  constructor() {
    this._lcpPresence$ = new ReplaySubject(1);
    this.lcpPresence$ = this._lcpPresence$.pipe(distinctUntilChanged(), shareReplay({
      bufferSize: 1,
      refCount: true
    }));
  }
  ngOnChanges() {
    const value = this.cxProvideLcpPresence ?? LcpPresence.NO_LCP;
    this._lcpPresence$.next(value);
  }
  static {
    this.ɵfac = function ProvideLcpPresenceDirective_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _ProvideLcpPresenceDirective)();
    };
  }
  static {
    this.ɵdir = ɵɵdefineDirective({
      type: _ProvideLcpPresenceDirective,
      selectors: [["", "cxProvideLcpPresence", ""]],
      inputs: {
        cxProvideLcpPresence: "cxProvideLcpPresence"
      },
      standalone: false,
      features: [ɵɵProvidersFeature([{
        provide: LCP_PRESENCE,
        useFactory: () => inject(_ProvideLcpPresenceDirective).lcpPresence$
      }]), ɵɵNgOnChangesFeature]
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(ProvideLcpPresenceDirective, [{
    type: Directive,
    args: [{
      selector: "[cxProvideLcpPresence]",
      providers: [{
        provide: LCP_PRESENCE,
        useFactory: () => inject(ProvideLcpPresenceDirective).lcpPresence$
      }],
      standalone: false
    }]
  }], null, {
    cxProvideLcpPresence: [{
      type: Input
    }]
  });
})();
var ProvideLcpPresenceDirectiveModule = class _ProvideLcpPresenceDirectiveModule {
  static {
    this.ɵfac = function ProvideLcpPresenceDirectiveModule_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _ProvideLcpPresenceDirectiveModule)();
    };
  }
  static {
    this.ɵmod = ɵɵdefineNgModule({
      type: _ProvideLcpPresenceDirectiveModule,
      declarations: [ProvideLcpPresenceDirective],
      exports: [ProvideLcpPresenceDirective]
    });
  }
  static {
    this.ɵinj = ɵɵdefineInjector({});
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(ProvideLcpPresenceDirectiveModule, [{
    type: NgModule,
    args: [{
      declarations: [ProvideLcpPresenceDirective],
      exports: [ProvideLcpPresenceDirective]
    }]
  }], null, null);
})();
var FileDownloadService = class _FileDownloadService {
  /**
   * Triggers the browser downloading of the file from the given URL.
   *
   * @param {string} url URL to file.
   * @param {string} [fileName] The optional parameter for defining filename
   * (including the extension) when saving the file.
   */
  download(url, fileName) {
    const link = document.createElement("a");
    link.setAttribute("href", url);
    link.setAttribute("download", `${fileName}`);
    link.style.visibility = "hidden";
    document.body.appendChild(link);
    link.click();
    document.body.removeChild(link);
  }
  static {
    this.ɵfac = function FileDownloadService_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _FileDownloadService)();
    };
  }
  static {
    this.ɵprov = ɵɵdefineInjectable({
      token: _FileDownloadService,
      factory: _FileDownloadService.ɵfac,
      providedIn: "root"
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(FileDownloadService, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], null, null);
})();
var ExportCsvFileService = class _ExportCsvFileService {
  constructor(fileDownloadService) {
    this.fileDownloadService = fileDownloadService;
  }
  /**
   * Converts array of objects into CSV data structure.
   *
   * @param objectsArray Array of objects which should be converted to CSV.
   * @param separator Separator for CSV data.
   * @returns Processed string ready to be saved into file.
   */
  convert(objectsArray, separator) {
    return objectsArray.reduce((csvString, row) => {
      const line = row.reduce((currentLine, column) => {
        currentLine += currentLine !== "" ? separator : "";
        const cell = column.includes(separator) ? `"${column}"` : column;
        return `${currentLine}${cell}`;
      }, "");
      return `${csvString}${line}\r
`;
    }, "");
  }
  /**
   * Creates and download CSV file.
   *
   * @param objectsArray Array of objects which should be converted to CSV.
   * @param separator Separator for CSV data.
   * @param fileOptions Exported file options.
   */
  download(objectsArray, separator, fileOptions) {
    const {
      fileName,
      type,
      extension
    } = fileOptions;
    const fileContent = this.convert(objectsArray, separator);
    const blob = new Blob([fileContent], {
      type
    });
    const url = URL.createObjectURL(blob);
    this.fileDownloadService.download(url, `${fileName}.${extension}`);
  }
  static {
    this.ɵfac = function ExportCsvFileService_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _ExportCsvFileService)(ɵɵinject(FileDownloadService));
    };
  }
  static {
    this.ɵprov = ɵɵdefineInjectable({
      token: _ExportCsvFileService,
      factory: _ExportCsvFileService.ɵfac,
      providedIn: "root"
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(ExportCsvFileService, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], () => [{
    type: FileDownloadService
  }], null);
})();
var FileReaderService = class _FileReaderService {
  /**
   * Load text file
   *
   * @param file text file to extract the data
   * @returns Observable from file reader
   */
  loadTextFile(file) {
    return new Observable((observer) => {
      const fileReader = new FileReader();
      fileReader.readAsText(file);
      fileReader.onload = () => {
        observer.next(fileReader.result);
        observer.complete();
      };
      fileReader.onerror = (error) => {
        fileReader.abort();
        observer.error(error);
      };
    });
  }
  static {
    this.ɵfac = function FileReaderService_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _FileReaderService)();
    };
  }
  static {
    this.ɵprov = ɵɵdefineInjectable({
      token: _FileReaderService,
      factory: _FileReaderService.ɵfac,
      providedIn: "root"
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(FileReaderService, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], null, null);
})();
var ImportCsvFileService = class _ImportCsvFileService {
  constructor(fileReaderService) {
    this.fileReaderService = fileReaderService;
  }
  /**
   * Load CSV file.
   *
   * @param file File we want to load as CSV.
   * @param separator Separator for CSV data.
   * @return {Observable<string[][]>} Imported file
   */
  loadFile(file, separator) {
    return this.fileReaderService.loadTextFile(file).pipe(map((res) => this.parse(res, separator)));
  }
  /**
   * Combined csv validation
   *
   * @param file File we want to load as CSV.
   * @param separator Separator for CSV data.
   * @param isDataParsable (optional) Callback for verify that structure type is proper.
   * @param maxEntries (optional) Limitation for maximum entries count.
   * @return {Observable<CsvFileValidationErrors | null>} Result of validation
   */
  validateFile(file, {
    separator,
    isDataParsable,
    maxEntries
  }) {
    const validationErrors = {};
    return this.fileReaderService.loadTextFile(file).pipe(tap((data) => {
      this.validateEmpty(data, validationErrors);
    }), map((res) => this.parse(res, separator)), tap((data) => {
      this.validateNotParsable(data, validationErrors, isDataParsable);
      this.validateTooManyEntries(data, validationErrors, maxEntries);
    }), catchError((errors) => of(errors)), map(() => Object.keys(validationErrors).length === 0 ? null : validationErrors));
  }
  /**
   * Processes the CSV data
   *
   * @param csvString raw extracted data from CSV
   * @param separator for csv data
   * @param ignoreHeader (optional) flag allows for ignore headers row while reading
   * @returns {string[][]} Parsed file
   */
  parse(csvString, separator, ignoreHeader = true) {
    return csvString.split("\n").map((row) => row.split(separator).map((cell) => cell.replace(/"/g, ""))).filter((value, index) => !(ignoreHeader && index === 0) && value[0] !== "");
  }
  validateEmpty(data, errors) {
    if (data.toString().length === 0) {
      errors.empty = true;
      throw errors;
    }
  }
  validateTooManyEntries(data, errors, maxEntries) {
    if (maxEntries && data.length > maxEntries) {
      errors.tooManyEntries = {
        maxEntries
      };
      throw errors;
    }
  }
  validateNotParsable(data, errors, isDataParsable) {
    if (isDataParsable && !isDataParsable(data)) {
      errors.notParsable = true;
      throw errors;
    }
  }
  static {
    this.ɵfac = function ImportCsvFileService_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _ImportCsvFileService)(ɵɵinject(FileReaderService));
    };
  }
  static {
    this.ɵprov = ɵɵdefineInjectable({
      token: _ImportCsvFileService,
      factory: _ImportCsvFileService.ɵfac,
      providedIn: "root"
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(ImportCsvFileService, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], () => [{
    type: FileReaderService
  }], null);
})();
var CustomFormValidators = class {
  /**
   * Checks control's value with predefined email regexp
   *
   * NOTE: Use it as a control validator
   *
   * @static
   * @param {AbstractControl} control
   * @returns {(ValidationErrors | null)} Uses 'cxInvalidEmail' validator error
   * @memberof CustomFormValidators
   */
  static emailValidator(control) {
    const email = control.value;
    return email && (!email.length || email.match(EMAIL_PATTERN)) ? null : {
      cxInvalidEmail: true
    };
  }
  static securePasswordValidator(control) {
    const password = control.value;
    return password && (!password.length || password.match(SECURE_PASSWORD_PATTERN)) ? null : {
      cxInvalidPassword: true
    };
  }
  // TODO: (CXSPA-7567) Remove after removing formErrorsDescriptiveMessages feature toggle
  /**
   * Checks control's value with predefined password regexp
   *
   * NOTE: Use it as a control validator
   *
   * @deprecated Use passwordValidators instead
   * @static
   * @param {AbstractControl} control
   * @returns {(ValidationErrors | null)} Uses 'cxInvalidPassword' validator error
   * @memberof CustomFormValidators
   */
  static strongPasswordValidator(control) {
    const password = control.value;
    return password && (!password.length || password.match(STRONG_PASSWORD_PATTERN)) ? null : {
      cxInvalidPassword: true
    };
  }
  /**
   * Checks control's value with predefined  at least one upper case character regexp
   *
   * NOTE: Use it as a control validator
   *
   * @static
   * @param {AbstractControl} control
   * @returns {(ValidationErrors | null)} Uses 'cxMinOneUpperCaseCharacter' validator error
   * @memberof CustomFormValidators
   */
  static minOneUpperCaseCharacterValidator(control) {
    const password = control.value;
    return password && (!password.length || password.match(MIN_ONE_UPPER_CASE_CHARACTER_PATTERN)) ? null : {
      cxMinOneUpperCaseCharacter: true
    };
  }
  /**
   * Checks control's value with predefined  at least one upper case character regexp
   *
   * NOTE: Use it as a control validator
   *
   * @static
   * @param {AbstractControl} control
   * @returns {(ValidationErrors | null)} Uses 'cxMinOneDigit' validator error
   * @memberof CustomFormValidators
   */
  static minOneDigitValidator(control) {
    const password = control.value;
    return password && (!password.length || password.match(MIN_ONE_DIGIT_PATTERN)) ? null : {
      cxMinOneDigit: true
    };
  }
  /**
   * Checks control's value with predefined  at least one upper case character regexp
   *
   * NOTE: Use it as a control validator
   *
   * @static
   * @param {AbstractControl} control
   * @returns {(ValidationErrors | null)} Uses 'cxMinOneSpecialCharacter' validator error
   * @memberof CustomFormValidators
   */
  static minOneSpecialCharacterValidator(control) {
    const password = control.value;
    return password && (!password.length || password.match(MIN_ONE_SPECIAL_CHARACTER_PATTERN)) ? null : {
      cxMinOneSpecialCharacter: true
    };
  }
  /**
   * Checks control's value with predefined  at least one upper case character regexp
   *
   * NOTE: Use it as a control validator
   *
   * @static
   * @param {AbstractControl} control
   * @returns {(ValidationErrors | null)} Uses 'cxMinSixCharactersLength' validator error
   * @memberof CustomFormValidators
   */
  static minSixCharactersLengthValidator(control) {
    const password = control.value;
    return password && (!password.length || password.match(MIN_SIX_CHARACTERS_PATTERN)) ? null : {
      cxMinSixCharactersLength: true
    };
  }
  /**
   * Checks control's value with predefined  at least one upper case character regexp
   *
   * NOTE: Use it as a control validator
   *
   * @static
   * @param {AbstractControl} control
   * @returns {(ValidationErrors | null)} Uses 'cxMinEightCharactersLength' validator error
   * @memberof CustomFormValidators
   */
  static minEightCharactersLengthValidator(control) {
    const password = control.value;
    return password && (!password.length || password.match(MIN_EIGHT_CHARACTERS_PATTERN)) ? null : {
      cxMinEightCharactersLength: true
    };
  }
  static maxCharactersLengthValidator(control) {
    const password = control.value;
    return password && (!password.length || password.match(MAX_CHARACTERS_PATTERN)) ? null : {
      cxMaxCharactersLength: true
    };
  }
  /**
   * Validates that the control's value does not contain consecutive identical characters.
   *
   * NOTE: Use this as a control validator.
   *
   * @static
   * @param {AbstractControl} control The form control to validate.
   * @returns {(ValidationErrors | null)} Returns an error object with the key 'cxNoConsecutiveCharacters'
   * if the value contains consecutive characters, or null if the validation passes.
   * @memberof CustomFormValidators
   */
  static noConsecutiveCharacters(control) {
    const password = control.value;
    return password && (!password.length || password.match(CONSECUTIVE_CHARACTERS)) ? {
      cxNoConsecutiveCharacters: true
    } : null;
  }
  static {
    this.passwordValidators = [this.minOneDigitValidator, this.minOneUpperCaseCharacterValidator, this.minOneSpecialCharacterValidator, this.minSixCharactersLengthValidator];
  }
  static {
    this.securePasswordValidators = [this.minOneDigitValidator, this.minOneUpperCaseCharacterValidator, this.minOneSpecialCharacterValidator, this.minEightCharactersLengthValidator, this.maxCharactersLengthValidator, this.noConsecutiveCharacters];
  }
  /**
   * Checks if control's value is between 1 and 5
   *
   * NOTE: Use it as a control validator
   *
   * @static
   * @param {AbstractControl} control
   * @returns {(ValidationErrors | null)} Uses 'cxStarRatingEmpty' validator error
   * @memberof CustomFormValidators
   */
  static starRatingEmpty(control) {
    const rating = control.value;
    return rating >= 1 && rating <= 5 ? null : {
      cxStarRatingEmpty: true
    };
  }
  /**
   * Checks if two password controls match
   *
   * NOTE: Use it as a form validator and pass password control names as parameters
   *
   * @static
   * @param {string} password First password control name
   * @param {string} passwordConfirmation Second password control name
   * @returns Uses 'cxPasswordsMustMatch' validator error
   * @memberof CustomFormValidators
   */
  static passwordsMustMatch(password, passwordConfirmation) {
    const validator = (formGroup) => controlsMustMatch(formGroup, password, passwordConfirmation, "cxPasswordsMustMatch");
    return validator;
  }
  /**
   * Checks if two password controls don't match
   *
   * NOTE: Use it as a form validator and pass password control names as parameters
   *
   * @static
   * @param {string} password First password control name
   * @param {string} passwordConfirmation Second password control name
   * @returns Uses 'cxPasswordsCannotMatch' validator error
   * @memberof CustomFormValidators
   */
  static passwordsCannotMatch(password, passwordConfirmation) {
    const validator = (formGroup) => controlsMustMatch(formGroup, password, passwordConfirmation, "cxPasswordsCannotMatch", true);
    return validator;
  }
  /**
   * Checks if two email controls match
   *
   * NOTE: Use it as a form validator and pass email control names as parameters
   *
   * @static
   * @param {string} email First email control name
   * @param {string} emailConfirmation Second email control name
   * @returns Uses 'cxEmailsMustMatch' validator error
   * @memberof CustomFormValidators
   */
  static emailsMustMatch(email, emailConfirmation) {
    const validator = (formGroup) => controlsMustMatch(formGroup, email, emailConfirmation, "cxEmailsMustMatch");
    return validator;
  }
  /**
   * Checks if control's value is euqal or greater than 0
   *
   * NOTE: Use it as a control validator
   *
   * @static
   * @param {AbstractControl} control
   * @returns {(ValidationErrors | null)} Uses 'cxNegativeAmount' validator error
   * @memberof CustomFormValidators
   */
  static mustBePositive(control) {
    const amount = control.value;
    return amount >= 0 ? null : {
      cxNegativeAmount: true
    };
  }
  /**
   * Checks if control's value does not contain any special characters
   *
   * NOTE: Use it as a control validator
   *
   * @static
   * @param {AbstractControl} control
   * @returns {(ValidationErrors | null)} Uses 'cxContainsSpecialCharacters' validator error
   * @memberof CustomFormValidators
   */
  static noSpecialCharacters(control) {
    const forbiddenChars = ["/"];
    const str = String(control.value);
    const containsSpecialChars = forbiddenChars.some((char) => str.includes(char));
    return !containsSpecialChars ? null : {
      cxContainsSpecialCharacters: true
    };
  }
  /**
   * Checks if control's value passes pattern
   *
   * NOTE: Use it as a control validator
   *
   * @static
   * @param {(date: string) => boolean} isValidFormat Pattern verification function
   * @returns {(control: AbstractControl): ValidationErrors | null} Uses 'pattern' validator error
   * @memberof CustomFormValidators
   */
  static patternValidation(isValidFormat) {
    const validator = (control) => {
      const errors = {};
      if (control.value && control.value !== "" && !isValidFormat(control.value)) {
        errors.pattern = true;
      }
      return Object.keys(errors).length === 0 ? null : errors;
    };
    return validator;
  }
  /**
   * Checks if two email controls match
   *
   * NOTE: Use it as a form validator and pass dates for range
   *
   * @static
   * @param {string} startDateKey First date control name
   * @param {string} endDateKey Second date control name
   * @param {(value: string) => Date} getDate Converting function
   * @returns Uses 'min' and 'max validator error
   * @memberof CustomFormValidators
   */
  static dateRange(startDateKey, endDateKey, getDate) {
    return (formGroup) => {
      const startDateControl = formGroup.controls[startDateKey];
      const endDateControl = formGroup.controls[endDateKey];
      const startDate = getDate(startDateControl.value);
      const endDate = getDate(endDateControl.value);
      if (startDate && endDate) {
        if (!startDateControl.errors?.pattern) {
          if (startDate > endDate) {
            startDateControl.setErrors({
              max: true
            });
          }
        }
        if (!endDateControl.errors?.pattern) {
          if (endDate < startDate) {
            endDateControl.setErrors({
              min: true
            });
          }
        }
      }
      return null;
    };
  }
};
function controlsMustMatch(formGroup, firstControlName, secondControlName, errorName, cannotMatch = false) {
  const firstControl = formGroup.controls[firstControlName];
  const secondControl = formGroup.controls[secondControlName];
  if (secondControl.errors && !secondControl.errors[errorName]) {
    return;
  }
  const shouldSetError = cannotMatch ? firstControl.value === secondControl.value : firstControl.value !== secondControl.value;
  secondControl.setErrors(shouldSetError ? {
    [errorName]: true
  } : null);
}
var titleScores = {
  mr: 1,
  mrs: 2,
  miss: 3,
  ms: 4,
  dr: 5,
  rev: 6
};
function sortTitles(title1, title2) {
  if (title1.code && title2.code) {
    if (!titleScores[title1.code] || !titleScores[title2.code]) {
      return 1;
    } else {
      return titleScores[title1.code] - titleScores[title2.code];
    }
  }
  return 1;
}
var SearchBoxModule = class _SearchBoxModule {
  static {
    this.ɵfac = function SearchBoxModule_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _SearchBoxModule)();
    };
  }
  static {
    this.ɵmod = ɵɵdefineNgModule({
      type: _SearchBoxModule,
      declarations: [SearchBoxComponent, HighlightPipe],
      imports: [CommonModule, RouterModule, MediaModule, IconModule, UrlModule, I18nModule, OutletModule, FeaturesConfigModule, CarouselModule, KeyboardFocusModule],
      exports: [SearchBoxComponent, HighlightPipe]
    });
  }
  static {
    this.ɵinj = ɵɵdefineInjector({
      providers: [provideDefaultConfig({
        cmsComponents: {
          SearchBoxComponent: {
            component: SearchBoxComponent
          }
        }
      })],
      imports: [CommonModule, RouterModule, MediaModule, IconModule, UrlModule, I18nModule, OutletModule, FeaturesConfigModule, CarouselModule, KeyboardFocusModule]
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(SearchBoxModule, [{
    type: NgModule,
    args: [{
      imports: [CommonModule, RouterModule, MediaModule, IconModule, UrlModule, I18nModule, OutletModule, FeaturesConfigModule, CarouselModule, KeyboardFocusModule],
      providers: [provideDefaultConfig({
        cmsComponents: {
          SearchBoxComponent: {
            component: SearchBoxComponent
          }
        }
      })],
      declarations: [SearchBoxComponent, HighlightPipe],
      exports: [SearchBoxComponent, HighlightPipe]
    }]
  }], null, null);
})();
var ScrollToTopComponent = class _ScrollToTopComponent {
  constructor(winRef, componentData, selectFocusUtility) {
    this.winRef = winRef;
    this.componentData = componentData;
    this.selectFocusUtility = selectFocusUtility;
    this.iconTypes = ICON_TYPE;
    this.window = this.winRef.nativeWindow;
    this.scrollBehavior = ScrollBehavior.SMOOTH;
    this.displayThreshold = (this.window?.innerHeight ?? 400) / 2;
    this.triggedByKeypress = false;
  }
  ngOnInit() {
    this.setConfig();
  }
  onScroll() {
    this.switchDisplay();
  }
  /**
   * Scroll back to the top of the page, and recognize if triggerd by keyboard.
   */
  scrollToTop(event) {
    this.window?.scrollTo({
      top: 0,
      behavior: this.scrollBehavior
    });
    this.triggedByKeypress = event?.detail === 0;
  }
  onFocusOut() {
    if (this.display) {
      this.switchDisplay();
    }
  }
  /**
   * After scrolling to top, pressing Tab should focus first focusable element within body.
   */
  onTab(event) {
    const tabEvent = event;
    const scrollToTopHasFocus = document.activeElement === this.button.nativeElement;
    const isAtTopOfPage = this.window?.scrollY === 0;
    if (scrollToTopHasFocus && isAtTopOfPage && tabEvent.key === "Tab" && !tabEvent.shiftKey) {
      event.preventDefault();
      this.selectFocusUtility.findFirstFocusable(this.winRef.document.body, {
        autofocus: ""
      })?.focus();
    }
  }
  setConfig() {
    this.componentData.data$.pipe(take(1)).subscribe((data) => {
      this.scrollBehavior = data.scrollBehavior ?? this.scrollBehavior;
      this.displayThreshold = data.displayThreshold ?? this.displayThreshold;
    });
  }
  /**
   * This methods decides when the ScrollToTop button should be displayed.
   */
  switchDisplay() {
    const isPastThreshold = this.window && this.window.scrollY > this.displayThreshold;
    const buttonHasFocus = this.button.nativeElement === document.activeElement;
    this.display = isPastThreshold || this.triggedByKeypress && buttonHasFocus;
    if (!this.display) {
      this.triggedByKeypress = false;
    }
  }
  static {
    this.ɵfac = function ScrollToTopComponent_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _ScrollToTopComponent)(ɵɵdirectiveInject(WindowRef), ɵɵdirectiveInject(CmsComponentData), ɵɵdirectiveInject(SelectFocusUtility));
    };
  }
  static {
    this.ɵcmp = ɵɵdefineComponent({
      type: _ScrollToTopComponent,
      selectors: [["cx-scroll-to-top"]],
      viewQuery: function ScrollToTopComponent_Query(rf, ctx) {
        if (rf & 1) {
          ɵɵviewQuery(_c59, 5);
        }
        if (rf & 2) {
          let _t;
          ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.button = _t.first);
        }
      },
      hostVars: 2,
      hostBindings: function ScrollToTopComponent_HostBindings(rf, ctx) {
        if (rf & 1) {
          ɵɵlistener("scroll", function ScrollToTopComponent_scroll_HostBindingHandler($event) {
            return ctx.onScroll($event);
          }, false, ɵɵresolveWindow);
        }
        if (rf & 2) {
          ɵɵclassProp("display", ctx.display);
        }
      },
      standalone: false,
      decls: 6,
      vars: 7,
      consts: [["button", ""], [1, "cx-scroll-to-top-btn", 3, "click", "focusout", "keydown.Tab", "title"], ["aria-hidden", "true"], [1, "caret-up-icon", 3, "type"]],
      template: function ScrollToTopComponent_Template(rf, ctx) {
        if (rf & 1) {
          const _r1 = ɵɵgetCurrentView();
          ɵɵelementStart(0, "button", 1, 0);
          ɵɵpipe(2, "cxTranslate");
          ɵɵpipe(3, "cxTranslate");
          ɵɵlistener("click", function ScrollToTopComponent_Template_button_click_0_listener($event) {
            ɵɵrestoreView(_r1);
            return ɵɵresetView(ctx.scrollToTop($event));
          })("focusout", function ScrollToTopComponent_Template_button_focusout_0_listener() {
            ɵɵrestoreView(_r1);
            return ɵɵresetView(ctx.onFocusOut());
          })("keydown.Tab", function ScrollToTopComponent_Template_button_keydown_Tab_0_listener($event) {
            ɵɵrestoreView(_r1);
            return ɵɵresetView(ctx.onTab($event));
          });
          ɵɵelementStart(4, "span", 2);
          ɵɵelement(5, "cx-icon", 3);
          ɵɵelementEnd()();
        }
        if (rf & 2) {
          ɵɵproperty("title", ɵɵpipeBind1(2, 3, "navigation.scrollToTop"));
          ɵɵattribute("aria-label", ɵɵpipeBind1(3, 5, "navigation.scrollToTop"));
          ɵɵadvance(5);
          ɵɵproperty("type", ctx.iconTypes.CARET_UP);
        }
      },
      dependencies: [IconComponent, TranslatePipe],
      encapsulation: 2,
      changeDetection: 0
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(ScrollToTopComponent, [{
    type: Component,
    args: [{
      selector: "cx-scroll-to-top",
      changeDetection: ChangeDetectionStrategy.OnPush,
      standalone: false,
      template: `<button
  #button
  [attr.aria-label]="'navigation.scrollToTop' | cxTranslate"
  [title]="'navigation.scrollToTop' | cxTranslate"
  class="cx-scroll-to-top-btn"
  (click)="scrollToTop($event)"
  (focusout)="onFocusOut()"
  (keydown.Tab)="onTab($event)"
>
  <span aria-hidden="true">
    <cx-icon class="caret-up-icon" [type]="iconTypes.CARET_UP"></cx-icon>
  </span>
</button>
`
    }]
  }], () => [{
    type: WindowRef
  }, {
    type: CmsComponentData
  }, {
    type: SelectFocusUtility
  }], {
    display: [{
      type: HostBinding,
      args: ["class.display"]
    }],
    button: [{
      type: ViewChild,
      args: ["button"]
    }],
    onScroll: [{
      type: HostListener,
      args: ["window:scroll", ["$event"]]
    }]
  });
})();
var ScrollToTopModule = class _ScrollToTopModule {
  static {
    this.ɵfac = function ScrollToTopModule_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _ScrollToTopModule)();
    };
  }
  static {
    this.ɵmod = ɵɵdefineNgModule({
      type: _ScrollToTopModule,
      declarations: [ScrollToTopComponent],
      imports: [CommonModule, IconModule, I18nModule],
      exports: [ScrollToTopComponent]
    });
  }
  static {
    this.ɵinj = ɵɵdefineInjector({
      providers: [provideDefaultConfig({
        cmsComponents: {
          ScrollToTopComponent: {
            component: ScrollToTopComponent
          }
        }
      })],
      imports: [CommonModule, IconModule, I18nModule]
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(ScrollToTopModule, [{
    type: NgModule,
    args: [{
      imports: [CommonModule, IconModule, I18nModule],
      providers: [provideDefaultConfig({
        cmsComponents: {
          ScrollToTopComponent: {
            component: ScrollToTopComponent
          }
        }
      })],
      declarations: [ScrollToTopComponent],
      exports: [ScrollToTopComponent]
    }]
  }], null, null);
})();
var MyAccountV2NavigationComponent = class _MyAccountV2NavigationComponent extends NavigationComponent {
  static {
    this.ɵfac = /* @__PURE__ */ (() => {
      let ɵMyAccountV2NavigationComponent_BaseFactory;
      return function MyAccountV2NavigationComponent_Factory(__ngFactoryType__) {
        return (ɵMyAccountV2NavigationComponent_BaseFactory || (ɵMyAccountV2NavigationComponent_BaseFactory = ɵɵgetInheritedFactory(_MyAccountV2NavigationComponent)))(__ngFactoryType__ || _MyAccountV2NavigationComponent);
      };
    })();
  }
  static {
    this.ɵcmp = ɵɵdefineComponent({
      type: _MyAccountV2NavigationComponent,
      selectors: [["cx-my-account-v2-navigation"]],
      standalone: false,
      features: [ɵɵInheritDefinitionFeature],
      decls: 2,
      vars: 3,
      consts: [[3, "node", "flyout", "ngClass", "navAriaLabel", "focusableNodeTitles", 4, "ngIf"], [3, "node", "flyout", "ngClass", "navAriaLabel", "focusableNodeTitles"]],
      template: function MyAccountV2NavigationComponent_Template(rf, ctx) {
        if (rf & 1) {
          ɵɵtemplate(0, MyAccountV2NavigationComponent_cx_navigation_ui_0_Template, 3, 9, "cx-navigation-ui", 0);
          ɵɵpipe(1, "async");
        }
        if (rf & 2) {
          ɵɵproperty("ngIf", ɵɵpipeBind1(1, 1, ctx.node$));
        }
      },
      dependencies: [NgClass, NgIf, NavigationUIComponent, AsyncPipe],
      encapsulation: 2
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(MyAccountV2NavigationComponent, [{
    type: Component,
    args: [{
      selector: "cx-my-account-v2-navigation",
      standalone: false,
      template: `<cx-navigation-ui
  *ngIf="node$ | async as node"
  [node]="node"
  [flyout]="false"
  [ngClass]="(styleClass$ | async) ?? ''"
  [navAriaLabel]="name$ | async"
  [focusableNodeTitles]="true"
></cx-navigation-ui>
`
    }]
  }], null, null);
})();
var MyAccountV2NavigationModule = class _MyAccountV2NavigationModule {
  static {
    this.ɵfac = function MyAccountV2NavigationModule_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _MyAccountV2NavigationModule)();
    };
  }
  static {
    this.ɵmod = ɵɵdefineNgModule({
      type: _MyAccountV2NavigationModule,
      declarations: [MyAccountV2NavigationComponent],
      imports: [CommonModule, NavigationModule, I18nModule],
      exports: [MyAccountV2NavigationComponent]
    });
  }
  static {
    this.ɵinj = ɵɵdefineInjector({
      providers: [provideDefaultConfig({
        cmsComponents: {
          MyAccountSideNavigationComponent: {
            component: MyAccountV2NavigationComponent,
            guards: [AuthGuard]
          }
        }
      })],
      imports: [CommonModule, NavigationModule, I18nModule]
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(MyAccountV2NavigationModule, [{
    type: NgModule,
    args: [{
      providers: [provideDefaultConfig({
        cmsComponents: {
          MyAccountSideNavigationComponent: {
            component: MyAccountV2NavigationComponent,
            guards: [AuthGuard]
          }
        }
      })],
      declarations: [MyAccountV2NavigationComponent],
      exports: [MyAccountV2NavigationComponent],
      imports: [CommonModule, NavigationModule, I18nModule]
    }]
  }], null, null);
})();
var MyAccountV2Module = class _MyAccountV2Module {
  static {
    this.ɵfac = function MyAccountV2Module_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _MyAccountV2Module)();
    };
  }
  static {
    this.ɵmod = ɵɵdefineNgModule({
      type: _MyAccountV2Module,
      imports: [MyAccountV2NavigationModule]
    });
  }
  static {
    this.ɵinj = ɵɵdefineInjector({
      imports: [MyAccountV2NavigationModule]
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(MyAccountV2Module, [{
    type: NgModule,
    args: [{
      imports: [MyAccountV2NavigationModule]
    }]
  }], null, null);
})();
var MyAccountV2NotificationPreferenceComponent = class _MyAccountV2NotificationPreferenceComponent extends NotificationPreferenceComponent {
  static {
    this.ɵfac = /* @__PURE__ */ (() => {
      let ɵMyAccountV2NotificationPreferenceComponent_BaseFactory;
      return function MyAccountV2NotificationPreferenceComponent_Factory(__ngFactoryType__) {
        return (ɵMyAccountV2NotificationPreferenceComponent_BaseFactory || (ɵMyAccountV2NotificationPreferenceComponent_BaseFactory = ɵɵgetInheritedFactory(_MyAccountV2NotificationPreferenceComponent)))(__ngFactoryType__ || _MyAccountV2NotificationPreferenceComponent);
      };
    })();
  }
  static {
    this.ɵcmp = ɵɵdefineComponent({
      type: _MyAccountV2NotificationPreferenceComponent,
      selectors: [["cx-my-account-v2-notification-preference"]],
      standalone: false,
      features: [ɵɵInheritDefinitionFeature],
      decls: 2,
      vars: 3,
      consts: [["loading", ""], [4, "ngIf"], [4, "ngIf", "ngIfElse"], [1, "np-row", "np-flex", "np-content-center"], [1, "col-md-8"], [1, "header"], [1, "pref-info"], [1, "form-check", "cx-notification-channels"], [4, "ngFor", "ngForOf"], ["class", "pref-channel", 4, "ngIf"], [1, "pref-channel"], ["role", "checkbox", "type", "checkbox", 1, "form-check-input", "cx-np-checkbox", 3, "change", "checked", "disabled"], [1, "form-check-label"], [1, "cx-spinner"]],
      template: function MyAccountV2NotificationPreferenceComponent_Template(rf, ctx) {
        if (rf & 1) {
          ɵɵtemplate(0, MyAccountV2NotificationPreferenceComponent_ng_container_0_Template, 4, 2, "ng-container", 1);
          ɵɵpipe(1, "async");
        }
        if (rf & 2) {
          ɵɵproperty("ngIf", ɵɵpipeBind1(1, 1, ctx.preferences$));
        }
      },
      dependencies: [NgForOf, NgIf, SpinnerComponent, AsyncPipe, TranslatePipe],
      encapsulation: 2,
      changeDetection: 0
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(MyAccountV2NotificationPreferenceComponent, [{
    type: Component,
    args: [{
      selector: "cx-my-account-v2-notification-preference",
      changeDetection: ChangeDetectionStrategy.OnPush,
      standalone: false,
      template: `<ng-container *ngIf="preferences$ | async as preferences">
  <div *ngIf="preferences.length > 0; else loading">
    <div class="np-row np-flex np-content-center">
      <div class="col-md-8">
        <div class="header">
          {{ 'myAccountV2NotificationPreference.header' | cxTranslate }}
        </div>
        <div class="pref-info">
          {{ 'myAccountV2NotificationPreference.message' | cxTranslate }}
        </div>
        <div class="form-check cx-notification-channels">
          <ng-container *ngFor="let preference of preferences">
            <label *ngIf="preference.visible" class="pref-channel">
              <input
                class="form-check-input cx-np-checkbox"
                role="checkbox"
                type="checkbox"
                [checked]="preference.enabled"
                (change)="updatePreference(preference)"
                [disabled]="isLoading$ | async"
              />
              <span class="form-check-label">
                {{
                  'myAccountV2NotificationPreference.' + preference.channel
                    | cxTranslate
                }}
                {{ preference.value }}
              </span>
            </label>
          </ng-container>
        </div>
        <label
          ><strong>{{
            'myAccountV2NotificationPreference.note' | cxTranslate
          }}</strong
          >{{ 'myAccountV2NotificationPreference.noteMessage' | cxTranslate }}
        </label>
      </div>
    </div>
  </div>

  <ng-template #loading>
    <div class="cx-spinner">
      <cx-spinner></cx-spinner>
    </div>
  </ng-template>
</ng-container>
`
    }]
  }], null, null);
})();
var MyAccountV2NotificationPreferenceModule = class _MyAccountV2NotificationPreferenceModule {
  static {
    this.ɵfac = function MyAccountV2NotificationPreferenceModule_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _MyAccountV2NotificationPreferenceModule)();
    };
  }
  static {
    this.ɵmod = ɵɵdefineNgModule({
      type: _MyAccountV2NotificationPreferenceModule,
      declarations: [MyAccountV2NotificationPreferenceComponent],
      imports: [CommonModule, SpinnerModule, I18nModule, RouterModule, FeaturesConfigModule],
      exports: [MyAccountV2NotificationPreferenceComponent]
    });
  }
  static {
    this.ɵinj = ɵɵdefineInjector({
      imports: [CommonModule, SpinnerModule, I18nModule, RouterModule.forChild([{
        // @ts-ignore
        path: null,
        canActivate: [AuthGuard, CmsPageGuard],
        component: PageLayoutComponent,
        data: {
          cxRoute: "notificationPreference"
        }
      }]), FeaturesConfigModule]
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(MyAccountV2NotificationPreferenceModule, [{
    type: NgModule,
    args: [{
      declarations: [MyAccountV2NotificationPreferenceComponent],
      imports: [CommonModule, SpinnerModule, I18nModule, RouterModule.forChild([{
        // @ts-ignore
        path: null,
        canActivate: [AuthGuard, CmsPageGuard],
        component: PageLayoutComponent,
        data: {
          cxRoute: "notificationPreference"
        }
      }]), FeaturesConfigModule],
      exports: [MyAccountV2NotificationPreferenceComponent]
    }]
  }], null, null);
})();
var myAccountV2CmsMapping = {
  cmsComponents: {
    NotificationPreferenceComponent: {
      component: MyAccountV2NotificationPreferenceComponent
      //guards: inherited from standard config,
    }
  }
};
var NotificationPreferenceModule = class _NotificationPreferenceModule {
  static {
    this.ɵfac = function NotificationPreferenceModule_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _NotificationPreferenceModule)();
    };
  }
  static {
    this.ɵmod = ɵɵdefineNgModule({
      type: _NotificationPreferenceModule,
      declarations: [NotificationPreferenceComponent],
      imports: [CommonModule, SpinnerModule, I18nModule, FeaturesConfigModule, RouterModule],
      exports: [NotificationPreferenceComponent]
    });
  }
  static {
    this.ɵinj = ɵɵdefineInjector({
      providers: [provideDefaultConfig({
        cmsComponents: {
          NotificationPreferenceComponent: {
            component: NotificationPreferenceComponent,
            guards: [AuthGuard]
          }
        }
      }), provideDefaultConfigFactory(() => inject(USE_MY_ACCOUNT_V2_NOTIFICATION_PREFERENCE) ? myAccountV2CmsMapping : {})],
      imports: [CommonModule, SpinnerModule, I18nModule, FeaturesConfigModule, RouterModule.forChild([{
        // @ts-ignore
        path: null,
        canActivate: [AuthGuard, CmsPageGuard],
        component: PageLayoutComponent,
        data: {
          cxRoute: "notificationPreference"
        }
      }])]
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(NotificationPreferenceModule, [{
    type: NgModule,
    args: [{
      declarations: [NotificationPreferenceComponent],
      imports: [CommonModule, SpinnerModule, I18nModule, FeaturesConfigModule, RouterModule.forChild([{
        // @ts-ignore
        path: null,
        canActivate: [AuthGuard, CmsPageGuard],
        component: PageLayoutComponent,
        data: {
          cxRoute: "notificationPreference"
        }
      }])],
      providers: [provideDefaultConfig({
        cmsComponents: {
          NotificationPreferenceComponent: {
            component: NotificationPreferenceComponent,
            guards: [AuthGuard]
          }
        }
      }), provideDefaultConfigFactory(() => inject(USE_MY_ACCOUNT_V2_NOTIFICATION_PREFERENCE) ? myAccountV2CmsMapping : {})],
      exports: [NotificationPreferenceComponent]
    }]
  }], null, null);
})();
var PaymentMethodsComponent = class _PaymentMethodsComponent {
  constructor(userPaymentService, translation, globalMessageService) {
    this.userPaymentService = userPaymentService;
    this.translation = translation;
    this.globalMessageService = globalMessageService;
    this.iconTypes = ICON_TYPE;
  }
  ngOnInit() {
    this.paymentMethods$ = this.userPaymentService.getPaymentMethods().pipe(tap((paymentDetails) => {
      if (paymentDetails.length > 0 && !paymentDetails.find((paymentDetail) => paymentDetail.defaultPayment)) {
        this.setDefaultPaymentMethod(paymentDetails[0]);
      }
    }));
    this.editCard = void 0;
    this.loading$ = this.userPaymentService.getPaymentMethodsLoading();
    this.userPaymentService.loadPaymentMethods();
  }
  getCardContent({
    defaultPayment,
    accountHolderName,
    expiryMonth,
    expiryYear,
    cardNumber,
    cardType
  }) {
    return combineLatest([this.translation.translate("paymentCard.setAsDefault"), this.translation.translate("common.delete"), this.translation.translate("paymentCard.deleteConfirmation"), this.translation.translate("paymentCard.expires", {
      month: expiryMonth,
      year: expiryYear
    }), this.translation.translate("paymentCard.defaultPaymentMethod")]).pipe(map(([textSetAsDefault, textDelete, textDeleteConfirmation, textExpires, textDefaultPaymentMethod]) => {
      const actions = [];
      if (!defaultPayment) {
        actions.push({
          name: textSetAsDefault,
          event: "default"
        });
      }
      actions.push({
        name: textDelete,
        event: "edit"
      });
      const card = {
        role: "application",
        header: defaultPayment ? textDefaultPaymentMethod : void 0,
        textBold: accountHolderName,
        text: [cardNumber ?? "", textExpires],
        actions,
        deleteMsg: textDeleteConfirmation,
        img: this.getCardIcon(cardType?.code ?? ""),
        imgLabel: this.getCardIconLabel(cardType?.code),
        label: defaultPayment ? "paymentCard.defaultPaymentLabel" : "paymentCard.additionalPaymentLabel"
      };
      return card;
    }));
  }
  deletePaymentMethod(paymentMethod) {
    if (paymentMethod.id) {
      this.userPaymentService.deletePaymentMethod(paymentMethod.id);
      this.editCard = void 0;
    }
  }
  setEdit(paymentMethod) {
    this.editCard = paymentMethod.id;
  }
  cancelCard() {
    this.editCard = void 0;
  }
  setDefaultPaymentMethod(paymentMethod) {
    this.userPaymentService.setPaymentMethodAsDefault(paymentMethod.id ?? "");
    this.globalMessageService?.add({
      key: "paymentMessages.setAsDefaultSuccessfully"
    }, GlobalMessageType.MSG_TYPE_CONFIRMATION);
  }
  getCardIcon(code) {
    let ccIcon;
    if (code === "visa") {
      ccIcon = this.iconTypes.VISA;
    } else if (code === "master" || code === "mastercard_eurocard") {
      ccIcon = this.iconTypes.MASTER_CARD;
    } else if (code === "diners") {
      ccIcon = this.iconTypes.DINERS_CLUB;
    } else if (code === "amex") {
      ccIcon = this.iconTypes.AMEX;
    } else {
      ccIcon = this.iconTypes.CREDIT_CARD;
    }
    return ccIcon;
  }
  getCardIconLabel(code) {
    let ccIconLabel;
    if (code === "visa") {
      ccIconLabel = "paymentCard.visa";
    } else if (code === "master") {
      ccIconLabel = "paymentCard.master";
    } else if (code === "mastercard_eurocard") {
      ccIconLabel = "paymentCard.masterEuro";
    } else if (code === "diners") {
      ccIconLabel = "paymentCard.dinersClub";
    } else if (code === "amex") {
      ccIconLabel = "paymentCard.amex";
    } else if (code === "switch") {
      ccIconLabel = "paymentCard.switch";
    } else if (code === "maestro") {
      ccIconLabel = "paymentCard.maestro";
    } else {
      ccIconLabel = "paymentCard.credit";
    }
    return ccIconLabel;
  }
  static {
    this.ɵfac = function PaymentMethodsComponent_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _PaymentMethodsComponent)(ɵɵdirectiveInject(UserPaymentService), ɵɵdirectiveInject(TranslationService), ɵɵdirectiveInject(GlobalMessageService));
    };
  }
  static {
    this.ɵcmp = ɵɵdefineComponent({
      type: _PaymentMethodsComponent,
      selectors: [["cx-payment-methods"]],
      standalone: false,
      decls: 2,
      vars: 3,
      consts: [["cards", ""], [4, "ngIf"], [1, "cx-payment", "container"], [1, "cx-header"], [1, "cx-body"], [1, "cx-msg"], [4, "ngIf", "ngIfElse"], [1, "cx-existing", "row"], ["class", "cx-payment-card col-sm-12 col-md-12 col-lg-6", 4, "ngFor", "ngForOf"], [1, "cx-payment-card", "col-sm-12", "col-md-12", "col-lg-6"], [1, "cx-payment-inner"], [3, "deleteCard", "setDefaultCard", "editCard", "cancelCard", "index", "border", "fitToContainer", "content", "editMode"]],
      template: function PaymentMethodsComponent_Template(rf, ctx) {
        if (rf & 1) {
          ɵɵtemplate(0, PaymentMethodsComponent_ng_container_0_Template, 15, 10, "ng-container", 1);
          ɵɵpipe(1, "async");
        }
        if (rf & 2) {
          ɵɵproperty("ngIf", ɵɵpipeBind1(1, 1, ctx.paymentMethods$));
        }
      },
      dependencies: [NgForOf, NgIf, CardComponent, SpinnerComponent, FormRequiredLegendComponent, AsyncPipe, TranslatePipe],
      encapsulation: 2
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PaymentMethodsComponent, [{
    type: Component,
    args: [{
      selector: "cx-payment-methods",
      standalone: false,
      template: `<ng-container *ngIf="paymentMethods$ | async as paymentMethods">
  <!-- TODO: (CXSPA-5953) Remove feature flags next major -->
  <cx-form-required-legend />
  <div class="cx-payment container">
    <div class="cx-header">
      <h2>
        {{ 'paymentMethods.paymentMethods' | cxTranslate }}
      </h2>
    </div>

    <div class="cx-body">
      <div class="cx-msg">
        {{
          'paymentMethods.newPaymentMethodsAreAddedDuringCheckout' | cxTranslate
        }}
      </div>
      <div *ngIf="loading$ | async; else cards"><cx-spinner></cx-spinner></div>
      <ng-template #cards>
        <div class="cx-existing row">
          <div
            class="cx-payment-card col-sm-12 col-md-12 col-lg-6"
            *ngFor="let paymentMethod of paymentMethods; let i = index"
          >
            <div class="cx-payment-inner">
              <cx-card
                [index]="i"
                [border]="true"
                [fitToContainer]="true"
                [content]="getCardContent(paymentMethod) | async"
                (deleteCard)="deletePaymentMethod(paymentMethod)"
                (setDefaultCard)="setDefaultPaymentMethod(paymentMethod)"
                (editCard)="setEdit(paymentMethod)"
                [editMode]="editCard === paymentMethod.id"
                (cancelCard)="cancelCard()"
              ></cx-card>
            </div>
          </div>
        </div>
      </ng-template>
    </div>
  </div>
</ng-container>
`
    }]
  }], () => [{
    type: UserPaymentService
  }, {
    type: TranslationService
  }, {
    type: GlobalMessageService
  }], null);
})();
var PaymentMethodsModule = class _PaymentMethodsModule {
  static {
    this.ɵfac = function PaymentMethodsModule_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _PaymentMethodsModule)();
    };
  }
  static {
    this.ɵmod = ɵɵdefineNgModule({
      type: _PaymentMethodsModule,
      declarations: [PaymentMethodsComponent],
      imports: [CommonModule, CardModule, SpinnerModule, I18nModule, FeaturesConfigModule, FormRequiredLegendComponent],
      exports: [PaymentMethodsComponent]
    });
  }
  static {
    this.ɵinj = ɵɵdefineInjector({
      providers: [provideDefaultConfig({
        cmsComponents: {
          AccountPaymentDetailsComponent: {
            component: PaymentMethodsComponent,
            guards: [AuthGuard]
          }
        }
      })],
      imports: [CommonModule, CardModule, SpinnerModule, I18nModule, FeaturesConfigModule, FormRequiredLegendComponent]
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PaymentMethodsModule, [{
    type: NgModule,
    args: [{
      imports: [CommonModule, CardModule, SpinnerModule, I18nModule, FeaturesConfigModule, FormRequiredLegendComponent],
      providers: [provideDefaultConfig({
        cmsComponents: {
          AccountPaymentDetailsComponent: {
            component: PaymentMethodsComponent,
            guards: [AuthGuard]
          }
        }
      })],
      declarations: [PaymentMethodsComponent],
      exports: [PaymentMethodsComponent]
    }]
  }], null, null);
})();
var ProductCarouselService = class _ProductCarouselService {
  constructor(productService, semanticPathService) {
    this.productService = productService;
    this.semanticPathService = semanticPathService;
  }
  /**
   * Loads the product data and converts it `CarouselItem`.
   */
  loadProduct(code) {
    return this.productService.get(code).pipe(filter(isNotUndefined), map((product) => this.convertProduct(product)));
  }
  /**
   * Converts the product to a generic CarouselItem
   */
  convertProduct(source, displayTitle = true, displayProductPrices = true) {
    const item = {};
    if (displayTitle) {
      item.title = source.name;
    }
    if (displayProductPrices && source.price && source.price.formattedValue) {
      item.price = source.price.formattedValue;
    }
    if (source.images && source.images.PRIMARY) {
      item.media = {
        container: source.images.PRIMARY,
        format: "product"
      };
    }
    item.route = this.semanticPathService.transform({
      cxRoute: "product",
      params: source
    });
    return item;
  }
  static {
    this.ɵfac = function ProductCarouselService_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _ProductCarouselService)(ɵɵinject(ProductService), ɵɵinject(SemanticPathService));
    };
  }
  static {
    this.ɵprov = ɵɵdefineInjectable({
      token: _ProductCarouselService,
      factory: _ProductCarouselService.ɵfac,
      providedIn: "root"
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(ProductCarouselService, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], () => [{
    type: ProductService
  }, {
    type: SemanticPathService
  }], null);
})();
var ProductListComponentService = class _ProductListComponentService {
  constructor(productSearchService, routing, activatedRoute, currencyService, languageService, router, config) {
    this.productSearchService = productSearchService;
    this.routing = routing;
    this.activatedRoute = activatedRoute;
    this.currencyService = currencyService;
    this.languageService = languageService;
    this.router = router;
    this.config = config;
    this.RELEVANCE_ALLCATEGORIES = ":relevance:allCategories:";
    this.searchResults$ = this.productSearchService.getResults().pipe(filter((searchResult) => Object.keys(searchResult).length > 0));
    this.searchByRouting$ = combineLatest([this.routing.getRouterState().pipe(distinctUntilChanged((x, y) => {
      return x.state.url === y.state.url;
    })), ...this.siteContext]).pipe(debounceTime(0), map(([routerState, ..._context]) => routerState.state), tap((state) => {
      const criteria = this.getCriteriaFromRoute(state.params, state.queryParams);
      this.searchIfCriteriaHasChanged(criteria);
    }));
    this.model$ = using(() => this.searchByRouting$.subscribe(), () => this.searchResults$).pipe(shareReplay({
      bufferSize: 1,
      refCount: true
    }));
  }
  /**
   * Search only if the previous search criteria does NOT match the new one.
   * This prevents repeating product search calls for queries that already have loaded data.
   */
  searchIfCriteriaHasChanged(criteria) {
    this.productSearchService.getResults().pipe(take(1)).subscribe((results) => {
      const previous = {
        query: results?.currentQuery?.query?.value,
        currentPage: results?.pagination?.currentPage,
        pageSize: results?.pagination?.pageSize,
        sortCode: results?.pagination?.sort
      };
      if (checkQueriesDiffer() || checkCurrentPagesDiffer() || checkPageSizesDiffer() || checkSortCodesDiffer()) {
        this.search(criteria);
      }
      function checkQueriesDiffer() {
        const previousQuery = sanitizeQuery(previous.query, previous.sortCode);
        const currentQuery = sanitizeQuery(criteria.query, criteria.sortCode);
        return previousQuery !== currentQuery;
        function sanitizeQuery(query, sortCode) {
          const DEFAULT_SORT_CODE = "relevance";
          query = query?.replace(":" + DEFAULT_SORT_CODE, "").replace(DEFAULT_SORT_CODE, "");
          if (sortCode) {
            query = query?.replace(":" + sortCode, "").replace(sortCode, "");
          }
          return query;
        }
      }
      function checkCurrentPagesDiffer() {
        const previousPage = previous.currentPage && previous.currentPage > 0 ? previous.currentPage : void 0;
        return previousPage?.toString() !== criteria.currentPage?.toString();
      }
      function checkPageSizesDiffer() {
        return previous.pageSize?.toString() !== criteria.pageSize?.toString();
      }
      function checkSortCodesDiffer() {
        const previousCode = criteria.sortCode ? previous?.sortCode : void 0;
        return previousCode?.toString() !== criteria.sortCode?.toString();
      }
    });
  }
  /**
   * Expose the `SearchCriteria`. The search criteria are driven by the route parameters.
   *
   * This search route configuration is not yet configurable
   * (see https://github.com/SAP/spartacus/issues/7191).
   */
  getCriteriaFromRoute(routeParams, queryParams) {
    return {
      query: queryParams.query || this.getQueryFromRouteParams(routeParams),
      pageSize: queryParams.pageSize || this.config.view?.defaultPageSize,
      currentPage: queryParams.currentPage,
      sortCode: queryParams.sortCode
    };
  }
  /**
   * Resolves the search query from the given `ProductListRouteParams`.
   */
  getQueryFromRouteParams({
    query,
    categoryCode,
    brandCode
  }) {
    if (query) {
      return query;
    }
    if (categoryCode) {
      return this.RELEVANCE_ALLCATEGORIES + categoryCode;
    }
    if (brandCode) {
      return this.RELEVANCE_ALLCATEGORIES + brandCode;
    }
  }
  /**
   * Performs a search based on the given search criteria.
   *
   * The search is delegated to the `ProductSearchService`.
   */
  search(criteria) {
    const currentPage = criteria.currentPage;
    const pageSize = criteria.pageSize;
    const sort = criteria.sortCode;
    this.productSearchService.search(
      criteria.query,
      // TODO: consider dropping this complex passing of cleaned object
      Object.assign({}, currentPage && {
        currentPage
      }, pageSize && {
        pageSize
      }, sort && {
        sort
      })
    );
  }
  /**
   * Get items from a given page without using navigation
   */
  getPageItems(pageNumber) {
    this.routing.getRouterState().subscribe((route) => {
      const routeCriteria = this.getCriteriaFromRoute(route.state.params, route.state.queryParams);
      const criteria = __spreadProps(__spreadValues({}, routeCriteria), {
        currentPage: pageNumber
      });
      this.search(criteria);
    }).unsubscribe();
  }
  /**
   * Sort the search results by the given sort code.
   */
  sort(sortCode) {
    this.route({
      sortCode
    });
  }
  /**
   * Routes to the next product listing page, using the given `queryParams`. The
   * `queryParams` support sorting, pagination and querying.
   *
   * The `queryParams` are delegated to the Angular router `NavigationExtras`.
   */
  route(queryParams) {
    this.router.navigate([], {
      queryParams,
      queryParamsHandling: "merge",
      relativeTo: this.activatedRoute
    });
  }
  /**
   * The site context is used to update the search query in case of a
   * changing context. The context will typically influence the search data.
   *
   * We keep this private for now, as we're likely refactoring this in the next
   * major version.
   */
  get siteContext() {
    return [this.languageService.getActive(), this.currencyService.getActive()];
  }
  static {
    this.ɵfac = function ProductListComponentService_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _ProductListComponentService)(ɵɵinject(ProductSearchService), ɵɵinject(RoutingService), ɵɵinject(ActivatedRoute), ɵɵinject(CurrencyService), ɵɵinject(LanguageService), ɵɵinject(Router), ɵɵinject(ViewConfig));
    };
  }
  static {
    this.ɵprov = ɵɵdefineInjectable({
      token: _ProductListComponentService,
      factory: _ProductListComponentService.ɵfac,
      providedIn: "root"
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(ProductListComponentService, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], () => [{
    type: ProductSearchService
  }, {
    type: RoutingService
  }, {
    type: ActivatedRoute
  }, {
    type: CurrencyService
  }, {
    type: LanguageService
  }, {
    type: Router
  }, {
    type: ViewConfig
  }], null);
})();
var ViewModes;
(function(ViewModes2) {
  ViewModes2["Grid"] = "grid";
  ViewModes2["List"] = "list";
})(ViewModes || (ViewModes = {}));
var ProductViewComponent = class _ProductViewComponent {
  constructor() {
    this.iconTypes = ICON_TYPE;
    this.modeChange = new EventEmitter();
  }
  get buttonClass() {
    const viewName = this.viewMode?.toLowerCase();
    return `cx-product-${viewName}`;
  }
  /**
   *   Display icons inversely to allow users
   *   to see the view they will navigate to
   */
  get viewMode() {
    if (this.mode === "list") {
      return this.iconTypes.GRID;
    }
    return this.iconTypes.LIST;
  }
  changeMode() {
    const newMode = this.mode === ViewModes.Grid ? ViewModes.List : ViewModes.Grid;
    this.modeChange.emit(newMode);
  }
  static {
    this.ɵfac = function ProductViewComponent_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _ProductViewComponent)();
    };
  }
  static {
    this.ɵcmp = ɵɵdefineComponent({
      type: _ProductViewComponent,
      selectors: [["cx-product-view"]],
      inputs: {
        mode: "mode"
      },
      outputs: {
        modeChange: "modeChange"
      },
      standalone: false,
      decls: 7,
      vars: 13,
      consts: [["tabindex", "0", 1, "btn", "cx-product-layout", 3, "click", "ngClass"], [3, "type", 4, "ngIf"], [3, "type"]],
      template: function ProductViewComponent_Template(rf, ctx) {
        if (rf & 1) {
          ɵɵelementStart(0, "button", 0);
          ɵɵpipe(1, "cxTranslate");
          ɵɵpipe(2, "cxTranslate");
          ɵɵpipe(3, "cxTranslate");
          ɵɵpipe(4, "cxTranslate");
          ɵɵlistener("click", function ProductViewComponent_Template_button_click_0_listener() {
            return ctx.changeMode();
          });
          ɵɵtemplate(5, ProductViewComponent_cx_icon_5_Template, 1, 1, "cx-icon", 1)(6, ProductViewComponent_cx_icon_6_Template, 1, 1, "cx-icon", 1);
          ɵɵelementEnd();
        }
        if (rf & 2) {
          ɵɵproperty("ngClass", ctx.buttonClass);
          ɵɵattribute("aria-label", ctx.viewMode === ctx.iconTypes.GRID ? ɵɵpipeBind1(1, 5, "productView.gridView") : ctx.viewMode === ctx.iconTypes.LIST ? ɵɵpipeBind1(2, 7, "productView.listView") : null)("title", ctx.viewMode === ctx.iconTypes.GRID ? ɵɵpipeBind1(3, 9, "productView.gridView") : ctx.viewMode === ctx.iconTypes.LIST ? ɵɵpipeBind1(4, 11, "productView.listView") : null);
          ɵɵadvance(5);
          ɵɵproperty("ngIf", ctx.viewMode === ctx.iconTypes.GRID);
          ɵɵadvance();
          ɵɵproperty("ngIf", ctx.viewMode === ctx.iconTypes.LIST);
        }
      },
      dependencies: [NgClass, NgIf, IconComponent, TranslatePipe],
      encapsulation: 2,
      changeDetection: 0
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(ProductViewComponent, [{
    type: Component,
    args: [{
      selector: "cx-product-view",
      changeDetection: ChangeDetectionStrategy.OnPush,
      standalone: false,
      template: `<button
  class="btn cx-product-layout"
  [ngClass]="buttonClass"
  (click)="changeMode()"
  tabindex="0"
  attr.aria-label="{{
    viewMode === iconTypes.GRID
      ? ('productView.gridView' | cxTranslate)
      : viewMode === iconTypes.LIST
        ? ('productView.listView' | cxTranslate)
        : null
  }}"
  attr.title="{{
    viewMode === iconTypes.GRID
      ? ('productView.gridView' | cxTranslate)
      : viewMode === iconTypes.LIST
        ? ('productView.listView' | cxTranslate)
        : null
  }}"
>
  <cx-icon
    *ngIf="viewMode === iconTypes.GRID"
    [type]="iconTypes.GRID"
  ></cx-icon>
  <cx-icon
    *ngIf="viewMode === iconTypes.LIST"
    [type]="iconTypes.LIST"
  ></cx-icon>
</button>
`
    }]
  }], null, {
    mode: [{
      type: Input
    }],
    modeChange: [{
      type: Output
    }]
  });
})();
var ProductDetailOutlets;
(function(ProductDetailOutlets2) {
  ProductDetailOutlets2["INTRO"] = "PDP.INTRO";
  ProductDetailOutlets2["PRICE"] = "PDP.PRICE";
  ProductDetailOutlets2["SHARE"] = "PDP.SHARE";
  ProductDetailOutlets2["SUMMARY"] = "PDP.SUMMARY";
})(ProductDetailOutlets || (ProductDetailOutlets = {}));
var ProductListOutlets;
(function(ProductListOutlets2) {
  ProductListOutlets2["ITEM_ACTIONS"] = "cx-product-list-item.actions";
  ProductListOutlets2["ITEM_DETAILS"] = "cx-product-list-item.details";
})(ProductListOutlets || (ProductListOutlets = {}));
var ProductListItemContext = class _ProductListItemContext {
  static {
    this.ɵfac = function ProductListItemContext_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _ProductListItemContext)();
    };
  }
  static {
    this.ɵprov = ɵɵdefineInjectable({
      token: _ProductListItemContext,
      factory: _ProductListItemContext.ɵfac
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(ProductListItemContext, [{
    type: Injectable
  }], null, null);
})();
var ProductListItemContextSource = class _ProductListItemContextSource extends ProductListItemContext {
  constructor() {
    super(...arguments);
    this.product$ = new ReplaySubject(1);
  }
  static {
    this.ɵfac = /* @__PURE__ */ (() => {
      let ɵProductListItemContextSource_BaseFactory;
      return function ProductListItemContextSource_Factory(__ngFactoryType__) {
        return (ɵProductListItemContextSource_BaseFactory || (ɵProductListItemContextSource_BaseFactory = ɵɵgetInheritedFactory(_ProductListItemContextSource)))(__ngFactoryType__ || _ProductListItemContextSource);
      };
    })();
  }
  static {
    this.ɵprov = ɵɵdefineInjectable({
      token: _ProductListItemContextSource,
      factory: _ProductListItemContextSource.ɵfac
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(ProductListItemContextSource, [{
    type: Injectable
  }], null, null);
})();
var ProductListService = class _ProductListService {
  shouldHideAddToCartButton(product) {
    return !product.price || product.purchasable === false;
  }
  static {
    this.ɵfac = function ProductListService_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _ProductListService)();
    };
  }
  static {
    this.ɵprov = ɵɵdefineInjectable({
      token: _ProductListService,
      factory: _ProductListService.ɵfac,
      providedIn: "root"
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(ProductListService, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], null, null);
})();
var ProductListItemComponent = class _ProductListItemComponent {
  constructor(productListItemContextSource) {
    this.productListItemContextSource = productListItemContextSource;
    this.productListService = inject(ProductListService);
    this.hideAddToCartButton = false;
    this.ProductListOutlets = ProductListOutlets;
    useFeatureStyles("consistentSizeProductCards");
  }
  ngOnChanges(changes) {
    if (changes?.product) {
      this.hideAddToCartButton = this.hideAddToCartButton = this.productListService.shouldHideAddToCartButton(this.product);
      this.productListItemContextSource.product$.next(this.product);
    }
  }
  static {
    this.ɵfac = function ProductListItemComponent_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _ProductListItemComponent)(ɵɵdirectiveInject(ProductListItemContextSource));
    };
  }
  static {
    this.ɵcmp = ɵɵdefineComponent({
      type: _ProductListItemComponent,
      selectors: [["cx-product-list-item"]],
      inputs: {
        product: "product",
        itemIndex: "itemIndex"
      },
      standalone: false,
      features: [ɵɵProvidersFeature([ProductListItemContextSource, {
        provide: ProductListItemContext,
        useExisting: ProductListItemContextSource
      }]), ɵɵNgOnChangesFeature],
      decls: 16,
      vars: 20,
      consts: [[1, "row"], [1, "col-12", "col-md-4"], [1, "cx-product-image-container", 3, "routerLink"], ["class", "cx-product-image", "format", "product", 3, "container", "alt", "fetchPriority", 4, "cxLcpContext"], [1, "col-12", "col-md-8"], [1, "cx-product-name"], [3, "routerLink", "innerHTML"], [3, "cxOutlet"], [1, "col-12", "col-md-7", "col-xl-7"], [1, "cx-product-summary", 3, "innerHTML"], [1, "col-12", "col-md-5", "col-xl-5"], ["format", "product", 1, "cx-product-image", 3, "container", "alt", "fetchPriority"], [3, "rating", 4, "ngIf"], ["class", "cx-product-no-review", 4, "ngIf"], [1, "cx-product-price"], [3, "rating"], [1, "cx-product-no-review"], [4, "ngIf"], ["cxInnerComponentsHost", ""]],
      template: function ProductListItemComponent_Template(rf, ctx) {
        if (rf & 1) {
          ɵɵelementStart(0, "div", 0)(1, "div", 1)(2, "a", 2);
          ɵɵpipe(3, "cxUrl");
          ɵɵpipe(4, "cxTranslate");
          ɵɵtemplate(5, ProductListItemComponent_cx_media_5_Template, 2, 5, "cx-media", 3);
          ɵɵelementEnd()();
          ɵɵelementStart(6, "div", 4)(7, "h2", 5);
          ɵɵelement(8, "a", 6);
          ɵɵpipe(9, "cxUrl");
          ɵɵelementEnd();
          ɵɵtemplate(10, ProductListItemComponent_ng_template_10_Template, 5, 6, "ng-template", 7);
          ɵɵelementStart(11, "div", 0)(12, "div", 8);
          ɵɵelement(13, "p", 9);
          ɵɵelementEnd();
          ɵɵelementStart(14, "div", 10);
          ɵɵtemplate(15, ProductListItemComponent_ng_template_15_Template, 1, 1, "ng-template", 7);
          ɵɵelementEnd()()()();
        }
        if (rf & 2) {
          ɵɵadvance(2);
          ɵɵproperty("routerLink", ɵɵpipeBind1(3, 7, ɵɵpureFunction1(14, _c46, ctx.product)));
          ɵɵattribute("aria-label", !(ctx.product.images == null ? null : ctx.product.images.hasOwnProperty("PRIMARY")) ? ɵɵpipeBind2(4, 9, "productDetails.noProductImage", ɵɵpureFunction1(16, _c60, ctx.product.name)) : void 0);
          ɵɵadvance(6);
          ɵɵproperty("routerLink", ɵɵpipeBind1(9, 12, ɵɵpureFunction1(18, _c46, ctx.product)))("innerHTML", ctx.product.nameHtml, ɵɵsanitizeHtml);
          ɵɵadvance(2);
          ɵɵproperty("cxOutlet", ctx.ProductListOutlets.ITEM_DETAILS);
          ɵɵadvance(3);
          ɵɵproperty("innerHTML", ctx.product.summary, ɵɵsanitizeHtml);
          ɵɵadvance(2);
          ɵɵproperty("cxOutlet", ctx.ProductListOutlets.ITEM_ACTIONS);
        }
      },
      dependencies: [NgIf, MediaComponent, OutletDirective, InnerComponentsHostDirective, RouterLink, StarRatingComponent, LcpContextDirective, AsyncPipe, TranslatePipe, UrlPipe],
      encapsulation: 2,
      changeDetection: 0
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(ProductListItemComponent, [{
    type: Component,
    args: [{
      selector: "cx-product-list-item",
      changeDetection: ChangeDetectionStrategy.OnPush,
      providers: [ProductListItemContextSource, {
        provide: ProductListItemContext,
        useExisting: ProductListItemContextSource
      }],
      standalone: false,
      template: `<div class="row">
  <div class="col-12 col-md-4">
    <a
      [attr.aria-label]="
        !product.images?.hasOwnProperty('PRIMARY')
          ? ('productDetails.noProductImage'
            | cxTranslate: { product: product.name })
          : undefined
      "
      [routerLink]="{ cxRoute: 'product', params: product } | cxUrl"
      class="cx-product-image-container"
    >
      <cx-media
        class="cx-product-image"
        [container]="product.images?.PRIMARY"
        format="product"
        [alt]="product.name || product.summary"
        *cxLcpContext="let lcpContext"
        [fetchPriority]="
          itemIndex === 0 ? (lcpContext.fetchPriority$ | async) : undefined
        "
      ></cx-media>
    </a>
  </div>
  <div class="col-12 col-md-8">
    <h2 class="cx-product-name">
      <a
        [routerLink]="{ cxRoute: 'product', params: product } | cxUrl"
        [innerHTML]="product.nameHtml"
      ></a>
    </h2>

    <ng-template [cxOutlet]="ProductListOutlets.ITEM_DETAILS">
      <cx-star-rating
        *ngIf="product.averageRating"
        [rating]="product?.averageRating"
      ></cx-star-rating>
      <div *ngIf="!product.averageRating" class="cx-product-no-review">
        {{ 'productDetails.noReviews' | cxTranslate }}
      </div>
      <div
        class="cx-product-price"
        [attr.aria-label]="'productDetails.productPrice' | cxTranslate"
      >
        {{ product.price?.formattedValue }}
      </div>
    </ng-template>

    <div class="row">
      <div class="col-12 col-md-7 col-xl-7">
        <p class="cx-product-summary" [innerHTML]="product.summary"></p>
      </div>
      <div class="col-12 col-md-5 col-xl-5">
        <ng-template [cxOutlet]="ProductListOutlets.ITEM_ACTIONS">
          <ng-container *ngIf="!hideAddToCartButton">
            <ng-container cxInnerComponentsHost></ng-container>
          </ng-container>
        </ng-template>
      </div>
    </div>
  </div>
</div>
`
    }]
  }], () => [{
    type: ProductListItemContextSource
  }], {
    product: [{
      type: Input
    }],
    itemIndex: [{
      type: Input
    }]
  });
})();
var ProductGridItemComponent = class _ProductGridItemComponent {
  constructor(productListItemContextSource) {
    this.productListItemContextSource = productListItemContextSource;
    this.productListService = inject(ProductListService);
    this.hideAddToCartButton = false;
    this.ProductListOutlets = ProductListOutlets;
    useFeatureStyles("consistentSizeProductCards");
  }
  ngOnChanges(changes) {
    if (changes?.product) {
      this.hideAddToCartButton = this.productListService.shouldHideAddToCartButton(this.product);
      this.productListItemContextSource.product$.next(this.product);
    }
  }
  static {
    this.ɵfac = function ProductGridItemComponent_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _ProductGridItemComponent)(ɵɵdirectiveInject(ProductListItemContextSource));
    };
  }
  static {
    this.ɵcmp = ɵɵdefineComponent({
      type: _ProductGridItemComponent,
      selectors: [["cx-product-grid-item"]],
      inputs: {
        product: "product",
        itemIndex: "itemIndex"
      },
      standalone: false,
      features: [ɵɵProvidersFeature([ProductListItemContextSource, {
        provide: ProductListItemContext,
        useExisting: ProductListItemContextSource
      }]), ɵɵNgOnChangesFeature],
      decls: 7,
      vars: 13,
      consts: [["tabindex", "-1", 1, "cx-product-image-container", 3, "routerLink"], ["class", "cx-product-image", "format", "product", 3, "container", "alt", "fetchPriority", 4, "cxLcpContext"], [1, "cx-product-name", 3, "routerLink", "innerHTML"], [3, "cxOutlet"], ["format", "product", 1, "cx-product-image", 3, "container", "alt", "fetchPriority"], [1, "cx-product-rating"], [3, "rating", 4, "ngIf"], [4, "ngIf"], [1, "cx-product-price-container"], [1, "cx-product-price"], [3, "rating"], ["cxInnerComponentsHost", ""]],
      template: function ProductGridItemComponent_Template(rf, ctx) {
        if (rf & 1) {
          ɵɵelementStart(0, "a", 0);
          ɵɵpipe(1, "cxUrl");
          ɵɵtemplate(2, ProductGridItemComponent_cx_media_2_Template, 2, 5, "cx-media", 1);
          ɵɵelementEnd();
          ɵɵelement(3, "a", 2);
          ɵɵpipe(4, "cxUrl");
          ɵɵtemplate(5, ProductGridItemComponent_ng_template_5_Template, 7, 6, "ng-template", 3)(6, ProductGridItemComponent_ng_template_6_Template, 1, 1, "ng-template", 3);
        }
        if (rf & 2) {
          ɵɵproperty("routerLink", ɵɵpipeBind1(1, 5, ɵɵpureFunction1(9, _c46, ctx.product)));
          ɵɵadvance(3);
          ɵɵproperty("routerLink", ɵɵpipeBind1(4, 7, ɵɵpureFunction1(11, _c46, ctx.product)))("innerHTML", ctx.product.nameHtml, ɵɵsanitizeHtml);
          ɵɵadvance(2);
          ɵɵproperty("cxOutlet", ctx.ProductListOutlets.ITEM_DETAILS);
          ɵɵadvance();
          ɵɵproperty("cxOutlet", ctx.ProductListOutlets.ITEM_ACTIONS);
        }
      },
      dependencies: [NgIf, MediaComponent, OutletDirective, InnerComponentsHostDirective, RouterLink, StarRatingComponent, LcpContextDirective, AsyncPipe, TranslatePipe, UrlPipe],
      encapsulation: 2,
      changeDetection: 0
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(ProductGridItemComponent, [{
    type: Component,
    args: [{
      selector: "cx-product-grid-item",
      changeDetection: ChangeDetectionStrategy.OnPush,
      providers: [ProductListItemContextSource, {
        provide: ProductListItemContext,
        useExisting: ProductListItemContextSource
      }],
      standalone: false,
      template: `<a
  [routerLink]="{ cxRoute: 'product', params: product } | cxUrl"
  class="cx-product-image-container"
  tabindex="-1"
>
  <cx-media
    class="cx-product-image"
    [container]="product.images?.PRIMARY"
    format="product"
    [alt]="product.name || product.summary"
    *cxLcpContext="let lcpContext"
    [fetchPriority]="
      itemIndex === 0 ? (lcpContext.fetchPriority$ | async) : undefined
    "
  ></cx-media>
</a>
<a
  [routerLink]="{ cxRoute: 'product', params: product } | cxUrl"
  class="cx-product-name"
  [innerHTML]="product.nameHtml"
></a>

<ng-template [cxOutlet]="ProductListOutlets.ITEM_DETAILS">
  <div class="cx-product-rating">
    <cx-star-rating
      *ngIf="product.averageRating"
      [rating]="product?.averageRating"
    ></cx-star-rating>
    <div *ngIf="!product.averageRating">
      {{ 'productDetails.noReviews' | cxTranslate }}
    </div>
  </div>
  <div class="cx-product-price-container">
    <div
      class="cx-product-price"
      [attr.aria-label]="'productDetails.productPrice' | cxTranslate"
    >
      {{ product.price?.formattedValue }}
    </div>
  </div>
</ng-template>

<ng-template [cxOutlet]="ProductListOutlets.ITEM_ACTIONS">
  <ng-container *ngIf="!hideAddToCartButton">
    <ng-container cxInnerComponentsHost></ng-container>
  </ng-container>
</ng-template>
`
    }]
  }], () => [{
    type: ProductListItemContextSource
  }], {
    product: [{
      type: Input
    }],
    itemIndex: [{
      type: Input
    }]
  });
})();
var ProductScrollComponent = class _ProductScrollComponent {
  set setConfig(inputConfig) {
    this.setComponentConfigurations(inputConfig);
  }
  set setModel(inputModel) {
    this.infiniteScrollOperations(inputModel);
  }
  set setViewMode(inputViewMode) {
    this.inputViewMode = inputViewMode;
    if (this.viewMode) {
      this.resetListOnViewModeChange();
    } else {
      this.viewMode = inputViewMode;
    }
  }
  constructor(productListComponentService, ref) {
    this.productListComponentService = productListComponentService;
    this.ref = ref;
    this.subscription = new Subscription();
    this.ViewModes = ViewModes;
    this.appendProducts = false;
    this.resetList = false;
    this.isMaxProducts = false;
    this.isLastPage = false;
    this.isEmpty = false;
  }
  scrollPage(pageNumber) {
    this.appendProducts = true;
    this.ref.markForCheck();
    this.productListComponentService.getPageItems(pageNumber);
  }
  loadNextPage(pageNumber) {
    this.isMaxProducts = false;
    this.scrollPage(pageNumber);
  }
  scrollToTop() {
    window.scroll(0, 0);
  }
  setComponentConfigurations(scrollConfig) {
    const isButton = scrollConfig.view?.infiniteScroll?.showMoreButton;
    const configProductLimit = scrollConfig.view?.infiniteScroll?.productLimit;
    this.productLimit = isButton ? 1 : configProductLimit;
  }
  infiniteScrollOperations(inputModel) {
    if (this.appendProducts) {
      this.model = __spreadProps(__spreadValues({}, inputModel), {
        products: this.model.products?.concat(inputModel.products ?? [])
      });
    } else {
      this.model = inputModel;
      this.maxProducts = this.productLimit;
    }
    this.setConditions();
    this.ref.markForCheck();
  }
  resetListOnViewModeChange() {
    this.scrollToTop();
    this.resetList = true;
    this.productListComponentService.getPageItems(0);
  }
  //Set booleans after model has been retrieved
  setConditions() {
    this.isEmpty = !this.model.products || this.model.products.length === 0;
    this.isLastPage = this.model.pagination?.currentPage === (this.model.pagination?.totalPages ?? 0) - 1;
    if (this.model.products) {
      this.isMaxProducts = this.productLimit !== void 0 && this.productLimit !== 0 && this.model.products.length >= (this.maxProducts ?? 0);
      if (this.isMaxProducts) {
        this.maxProducts = this.model.products.length + (this.productLimit ?? 0);
      }
    }
    if (this.viewMode !== this.inputViewMode) {
      this.viewMode = this.inputViewMode;
    }
    this.resetList = false;
    this.appendProducts = false;
  }
  ngOnDestroy() {
    this.subscription.unsubscribe();
  }
  static {
    this.ɵfac = function ProductScrollComponent_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _ProductScrollComponent)(ɵɵdirectiveInject(ProductListComponentService), ɵɵdirectiveInject(ChangeDetectorRef));
    };
  }
  static {
    this.ɵcmp = ɵɵdefineComponent({
      type: _ProductScrollComponent,
      selectors: [["cx-product-scroll"]],
      inputs: {
        setConfig: [0, "scrollConfig", "setConfig"],
        setModel: [0, "model", "setModel"],
        setViewMode: [0, "inputViewMode", "setViewMode"]
      },
      standalone: false,
      decls: 2,
      vars: 2,
      consts: [[4, "ngIf"], ["infiniteScroll", "", 3, "scrolled", "infiniteScrollDistance", "infiniteScrollThrottle", "infiniteScrollDisabled"], [1, "row"], ["class", "col-12 col-sm-6 col-md-4", 3, "product", "itemIndex", 4, "ngFor", "ngForOf"], [3, "className"], ["class", "btn btn-block btn-secondary", 3, "click", 4, "ngIf"], ["class", "btn btn-block btn-secondary align-btn", 3, "click", 4, "ngIf"], ["class", "cx-spinner", 4, "ngIf"], [1, "col-12", "col-sm-6", "col-md-4", 3, "product", "itemIndex"], [1, "btn", "btn-block", "btn-secondary", 3, "click"], [1, "btn", "btn-block", "btn-secondary", "align-btn", 3, "click"], [1, "cx-spinner"], ["class", "cx-product-search-list", 3, "product", "itemIndex", 4, "ngFor", "ngForOf"], [1, "cx-product-search-list", 3, "product", "itemIndex"]],
      template: function ProductScrollComponent_Template(rf, ctx) {
        if (rf & 1) {
          ɵɵtemplate(0, ProductScrollComponent_ng_container_0_Template, 8, 8, "ng-container", 0)(1, ProductScrollComponent_ng_container_1_Template, 7, 8, "ng-container", 0);
        }
        if (rf & 2) {
          ɵɵproperty("ngIf", ctx.viewMode === ctx.ViewModes.Grid);
          ɵɵadvance();
          ɵɵproperty("ngIf", ctx.viewMode === ctx.ViewModes.List);
        }
      },
      dependencies: [NgForOf, NgIf, InfiniteScrollDirective, SpinnerComponent, ProductListItemComponent, ProductGridItemComponent, TranslatePipe],
      encapsulation: 2
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(ProductScrollComponent, [{
    type: Component,
    args: [{
      selector: "cx-product-scroll",
      standalone: false,
      template: `<ng-container *ngIf="viewMode === ViewModes.Grid">
  <div
    infiniteScroll
    [infiniteScrollDistance]="5"
    [infiniteScrollThrottle]="50"
    [infiniteScrollDisabled]="isMaxProducts || isLastPage || isEmpty"
    (scrolled)="scrollPage((model?.pagination?.currentPage ?? 0) + 1)"
  >
    <div class="row">
      <cx-product-grid-item
        *ngFor="let product of model?.products; let i = index"
        [product]="product"
        [itemIndex]="i"
        class="col-12 col-sm-6 col-md-4"
      ></cx-product-grid-item>
    </div>
    <div
      [className]="
        !isLastPage && (model?.pagination?.currentPage ?? 0) > 0
          ? 'cx-double-btn-container grid-btn-padding'
          : 'cx-single-btn-container grid-btn-padding'
      "
    >
      <div
        *ngIf="
          (isMaxProducts || isLastPage) &&
          (model?.pagination?.currentPage ?? 0) > 0
        "
        (click)="scrollToTop()"
        class="btn btn-block btn-secondary"
      >
        {{ 'productList.backToTopBtn' | cxTranslate }}
      </div>
      <div
        *ngIf="isMaxProducts && !isLastPage"
        (click)="loadNextPage((model?.pagination?.currentPage ?? 0) + 1)"
        class="btn btn-block btn-secondary align-btn"
      >
        {{ 'productList.showMoreBtn' | cxTranslate }}
      </div>
    </div>
    <div *ngIf="appendProducts" class="cx-spinner">
      <cx-spinner></cx-spinner>
    </div>
  </div>
</ng-container>

<ng-container *ngIf="viewMode === ViewModes.List">
  <div
    infiniteScroll
    [infiniteScrollDistance]="3"
    [infiniteScrollThrottle]="50"
    [infiniteScrollDisabled]="isMaxProducts || isLastPage || isEmpty"
    (scrolled)="scrollPage((model?.pagination?.currentPage ?? 0) + 1)"
  >
    <cx-product-list-item
      *ngFor="let product of model?.products; let i = index"
      [product]="product"
      [itemIndex]="i"
      class="cx-product-search-list"
    ></cx-product-list-item>
    <div
      [className]="
        !isLastPage && (model?.pagination?.currentPage ?? 0) > 0
          ? 'cx-double-btn-container'
          : 'cx-single-btn-container'
      "
    >
      <div
        *ngIf="
          (isMaxProducts || isLastPage) &&
          (model?.pagination?.currentPage ?? 0) > 0
        "
        (click)="scrollToTop()"
        class="btn btn-block btn-secondary"
      >
        {{ 'productList.backToTopBtn' | cxTranslate }}
      </div>
      <div
        *ngIf="isMaxProducts && !isLastPage"
        (click)="loadNextPage((model?.pagination?.currentPage ?? 0) + 1)"
        class="btn btn-block btn-secondary align-btn"
      >
        {{ 'productList.showMoreBtn' | cxTranslate }}
      </div>
    </div>
    <div *ngIf="appendProducts" class="cx-spinner">
      <cx-spinner></cx-spinner>
    </div>
  </div>
</ng-container>
`
    }]
  }], () => [{
    type: ProductListComponentService
  }, {
    type: ChangeDetectorRef
  }], {
    setConfig: [{
      type: Input,
      args: ["scrollConfig"]
    }],
    setModel: [{
      type: Input,
      args: ["model"]
    }],
    setViewMode: [{
      type: Input,
      args: ["inputViewMode"]
    }]
  });
})();
var ProductListComponent = class _ProductListComponent {
  constructor(pageLayoutService, productListComponentService, globalMessageService, scrollConfig) {
    this.pageLayoutService = pageLayoutService;
    this.productListComponentService = productListComponentService;
    this.globalMessageService = globalMessageService;
    this.scrollConfig = scrollConfig;
    this.subscription = new Subscription();
    this.model$ = this.productListComponentService.model$;
    this.viewMode$ = new BehaviorSubject(ViewModes.Grid);
    this.ViewModes = ViewModes;
    useFeatureStyles("reserveSpaceForImagesOnPdpAndPlp");
  }
  ngOnInit() {
    this.isInfiniteScroll = this.scrollConfig.view?.infiniteScroll?.active;
    this.subscription.add(this.pageLayoutService.templateName$.pipe(take(1)).subscribe((template) => {
      this.viewMode$.next(template === "ProductGridPageTemplate" ? ViewModes.Grid : ViewModes.List);
    }));
    this.subscription.add(combineLatest([this.model$, this.viewMode$]).pipe(skip(1), filter(([model, mode]) => !!model && !!mode)).subscribe(() => this.globalMessageService.add({
      key: "sorting.pageViewUpdated"
    }, GlobalMessageType.MSG_TYPE_ASSISTIVE, 500)));
  }
  sortList(sortCode) {
    this.productListComponentService.sort(sortCode);
  }
  setViewMode(mode) {
    this.viewMode$.next(mode);
  }
  ngOnDestroy() {
    this.subscription.unsubscribe();
  }
  static {
    this.ɵfac = function ProductListComponent_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _ProductListComponent)(ɵɵdirectiveInject(PageLayoutService), ɵɵdirectiveInject(ProductListComponentService), ɵɵdirectiveInject(GlobalMessageService), ɵɵdirectiveInject(ViewConfig));
    };
  }
  static {
    this.ɵcmp = ɵɵdefineComponent({
      type: _ProductListComponent,
      selectors: [["cx-product-list"]],
      standalone: false,
      decls: 2,
      vars: 3,
      consts: [["infiniteScroll", ""], ["class", "cx-page", 4, "ngIf"], [1, "cx-page"], ["tabindex", "-1", 1, "cx-page-section"], [1, "container"], [1, "row"], ["class", "col-12 col-lg-12", 4, "ngIf"], [1, "col-12", "col-lg-12"], [1, "cx-sorting", "top"], [1, "form-group", "cx-sort-dropdown", "col-12", "col-lg-4", "mr-auto"], ["ariaControls", "product-results-list", 3, "sortListEvent", "sortOptions", "selectedOption", "ariaLabel", "placeholder"], ["class", "col-auto", 4, "ngIf"], [1, "col-auto", "ml-auto", "ml-lg-0"], [3, "modeChange", "mode"], ["id", "product-results-list", 1, "cx-product-container"], [4, "ngIf", "ngIfElse"], [1, "cx-sorting", "bottom"], [1, "col-auto"], [1, "cx-pagination"], ["queryParam", "currentPage", "paginationID", "pagination-top", 3, "pagination", "defaultPage"], [4, "ngIf"], ["class", "col-12 col-sm-6 col-md-4", 3, "product", "itemIndex", 4, "ngFor", "ngForOf"], [1, "col-12", "col-sm-6", "col-md-4", 3, "product", "itemIndex"], ["class", "cx-product-search-list", 3, "product", "itemIndex", 4, "ngFor", "ngForOf"], [1, "cx-product-search-list", 3, "product", "itemIndex"], [3, "scrollConfig", "model", "inputViewMode"], ["queryParam", "currentPage", "paginationID", "pagination-bottom", 3, "pagination", "defaultPage"]],
      template: function ProductListComponent_Template(rf, ctx) {
        if (rf & 1) {
          ɵɵtemplate(0, ProductListComponent_div_0_Template, 7, 6, "div", 1);
          ɵɵpipe(1, "async");
        }
        if (rf & 2) {
          ɵɵproperty("ngIf", ɵɵpipeBind1(1, 1, ctx.model$));
        }
      },
      dependencies: [NgForOf, NgIf, SortingComponent, PaginationComponent, ProductListItemComponent, ProductGridItemComponent, ProductViewComponent, ProductScrollComponent, AsyncPipe, TranslatePipe],
      encapsulation: 2
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(ProductListComponent, [{
    type: Component,
    args: [{
      selector: "cx-product-list",
      standalone: false,
      template: `<div class="cx-page" *ngIf="model$ | async as model">
  <section
    tabindex="-1"
    class="cx-page-section"
    [attr.aria-label]="'productList.productListResults' | cxTranslate"
  >
    <div class="container">
      <div class="row">
        <div class="col-12 col-lg-12" *ngIf="viewMode$ | async as viewMode">
          <div class="cx-sorting top">
            <div class="row">
              <label
                class="form-group cx-sort-dropdown col-12 col-lg-4 mr-auto"
              >
                <span>{{ 'productList.sortBy' | cxTranslate }}</span>
                <cx-sorting
                  [sortOptions]="model.sorts"
                  (sortListEvent)="sortList($event)"
                  [selectedOption]="model.pagination?.sort"
                  [ariaLabel]="'productList.sortResults' | cxTranslate"
                  ariaControls="product-results-list"
                  placeholder="{{ 'productList.sortBy' | cxTranslate }}"
                ></cx-sorting>
              </label>
              <div *ngIf="!isInfiniteScroll" class="col-auto">
                <div
                  class="cx-pagination"
                  [attr.aria-label]="
                    'productList.productSearchPagination' | cxTranslate
                  "
                >
                  <cx-pagination
                    [pagination]="model.pagination"
                    queryParam="currentPage"
                    [defaultPage]="0"
                    paginationID="pagination-top"
                  ></cx-pagination>
                </div>
              </div>
              <div class="col-auto ml-auto ml-lg-0">
                <cx-product-view
                  (modeChange)="setViewMode($event)"
                  [mode]="viewMode"
                ></cx-product-view>
              </div>
            </div>
          </div>
          <div id="product-results-list" class="cx-product-container">
            <!-- Product list when using pagination -->
            <ng-container *ngIf="!isInfiniteScroll; else infiniteScroll">
              <ng-container *ngIf="viewMode === ViewModes.Grid">
                <div class="row">
                  <cx-product-grid-item
                    *ngFor="let product of model?.products; let i = index"
                    [product]="product"
                    [itemIndex]="i"
                    class="col-12 col-sm-6 col-md-4"
                  ></cx-product-grid-item>
                </div>
              </ng-container>

              <ng-container *ngIf="viewMode === ViewModes.List">
                <cx-product-list-item
                  *ngFor="let product of model?.products; let i = index"
                  [product]="product"
                  [itemIndex]="i"
                  class="cx-product-search-list"
                ></cx-product-list-item>
              </ng-container>
            </ng-container>

            <!-- Product list when using infinite scroll -->
            <ng-template #infiniteScroll>
              <cx-product-scroll
                [scrollConfig]="scrollConfig"
                [model]="model"
                [inputViewMode]="viewMode"
              ></cx-product-scroll>
            </ng-template>
          </div>
          <div class="cx-sorting bottom">
            <div class="row">
              <label
                class="form-group cx-sort-dropdown col-12 col-lg-4 mr-auto"
              >
                <span>{{ 'productList.sortBy' | cxTranslate }}</span>
                <cx-sorting
                  [sortOptions]="model.sorts"
                  (sortListEvent)="sortList($event)"
                  [selectedOption]="model.pagination?.sort"
                  [ariaLabel]="'productList.sortResults' | cxTranslate"
                  ariaControls="product-results-list"
                  placeholder="{{ 'productList.sortBy' | cxTranslate }}"
                ></cx-sorting>
              </label>
              <div
                *ngIf="!isInfiniteScroll"
                class="col-auto"
                [attr.aria-label]="
                  'productList.productSearchPagination' | cxTranslate
                "
              >
                <div class="cx-pagination">
                  <cx-pagination
                    [pagination]="model.pagination"
                    queryParam="currentPage"
                    [defaultPage]="0"
                    paginationID="pagination-bottom"
                  ></cx-pagination>
                </div>
              </div>
              <div class="col-auto ml-auto ml-lg-0">
                <cx-product-view
                  (modeChange)="setViewMode($event)"
                  [mode]="viewMode"
                ></cx-product-view>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>
</div>
`
    }]
  }], () => [{
    type: PageLayoutService
  }, {
    type: ProductListComponentService
  }, {
    type: GlobalMessageService
  }, {
    type: ViewConfig
  }], null);
})();
var FacetGroupCollapsedState;
(function(FacetGroupCollapsedState2) {
  FacetGroupCollapsedState2["EXPANDED"] = "EXPANDED";
  FacetGroupCollapsedState2["COLLAPSED"] = "COLLAPSED";
})(FacetGroupCollapsedState || (FacetGroupCollapsedState = {}));
var ProductFacetService = class _ProductFacetService {
  constructor(routing, productListComponentService) {
    this.routing = routing;
    this.productListComponentService = productListComponentService;
    this.routeState$ = this.routing.getRouterState().pipe(map((routerState) => routerState.state));
    this.searchResult$ = this.routeState$.pipe(switchMap((state) => this.productListComponentService.model$.pipe(filter((page) => this.filterForPage(state, page)), map((page) => __spreadProps(__spreadValues({}, page), {
      breadcrumbs: this.filterBreadcrumbs(page?.breadcrumbs ?? [], state.params)
    })))));
    this.facetList$ = this.searchResult$.pipe(map((result) => ({
      facets: result.facets,
      activeFacets: result.breadcrumbs
    })));
  }
  /**
   * Filters the current result by verifying if the result is related to the page.
   * This is done to avoid a combination of the next page and the current search results.
   */
  filterForPage(state, page) {
    if (!page.currentQuery?.query?.value) {
      return false;
    }
    if (state.context.type === PageType.CATEGORY_PAGE) {
      return page.currentQuery.query.value.indexOf(`allCategories:${state.context.id}`) > -1;
    }
    if (state.context.type === PageType.CONTENT_PAGE && state.context.id === "search") {
      return page.freeTextSearch === state.params.query.split(":")[0];
    }
    return false;
  }
  /**
   * Filter breadcrumbs which are not actively selected but coming from
   * the route navigation.
   *
   * The breadcrumbs might include the active category page code, which is not actively
   * selected by the user.
   */
  filterBreadcrumbs(breadcrumbs, params) {
    return breadcrumbs ? breadcrumbs.filter((breadcrumb) => !(breadcrumb.facetCode === "allCategories" && (breadcrumb.facetValueCode === params.categoryCode || breadcrumb.facetValueCode === params.brandCode))) : [];
  }
  static {
    this.ɵfac = function ProductFacetService_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _ProductFacetService)(ɵɵinject(RoutingService), ɵɵinject(ProductListComponentService));
    };
  }
  static {
    this.ɵprov = ɵɵdefineInjectable({
      token: _ProductFacetService,
      factory: _ProductFacetService.ɵfac,
      providedIn: "root"
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(ProductFacetService, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], () => [{
    type: RoutingService
  }, {
    type: ProductListComponentService
  }], null);
})();
var FacetService = class _FacetService {
  constructor(productFacetService) {
    this.productFacetService = productFacetService;
    this.facetState = /* @__PURE__ */ new Map();
    this.codec = new HttpUrlEncodingCodec();
    this.facetList$ = this.productFacetService.facetList$.pipe(tap((facetList) => {
      facetList.facets?.forEach((facet) => this.initialize(facet));
    }));
  }
  /**
   * Returns the observed UI state for the facet.
   *
   * The state is initialized using the `initialize` method.
   */
  getState(facet) {
    this.initialize(facet);
    return facet.name ? this.facetState.get(facet.name) ?? of({}) : of({});
  }
  /**
   * Returns the UI state for the facet.
   *
   * The state is initialized using the `initialize` method.
   */
  getStateSnapshot(facet) {
    return this.getState(facet).value;
  }
  /**
   * Toggles the facet expanded state. If the expanded state becomes false,
   * the visible values will decrease to the top values only.
   *
   * If the optional value argument is provided the expanded state will be set
   * to this value, regardless of the current `expanded` state.
   */
  toggle(facet, isExpanded) {
    const state = this.getStateSnapshot(facet);
    const toggledState = {
      toggled: isExpanded ? FacetGroupCollapsedState.COLLAPSED : FacetGroupCollapsedState.EXPANDED
    };
    if (toggledState.toggled === FacetGroupCollapsedState.COLLAPSED) {
      toggledState.maxVisible = state.topVisible;
    }
    this.updateState(facet, toggledState);
  }
  /**
   * Increases the visible values to the maximum values of the facet.
   */
  increaseVisibleValues(facet) {
    this.updateState(facet, {
      maxVisible: facet.values?.length
    });
  }
  /**
   * Decreases the visible values to the topValueCount.
   *
   * The topValueCount defaults to 6, but can be controlled in
   * the backend as well.
   */
  decreaseVisibleValues(facet) {
    this.updateState(facet, {
      maxVisible: facet.topValueCount
    });
  }
  /**
   * Persists the facet state and initializes the default values for the top
   * and max visible values.
   */
  initialize(facet) {
    const topFacets = facet.topValueCount && facet.topValueCount > 0 ? facet.topValueCount : facet.values?.length || 0;
    if (facet.name && !this.hasState(facet)) {
      this.facetState.set(facet.name, new BehaviorSubject({
        topVisible: topFacets,
        maxVisible: topFacets
      }));
    }
  }
  /**
   * Updates the state of the facet in the local facet map.
   */
  updateState(facet, property) {
    const state = __spreadValues(__spreadValues({}, this.getStateSnapshot(facet)), property);
    if (facet.name) {
      this.facetState.get(facet.name)?.next(state);
    }
  }
  hasState(facet) {
    if (facet.name) {
      return this.facetState.has(facet.name);
    }
    return false;
  }
  getLinkParams(query) {
    return {
      // to avoid encoding issues with facets that have space (' ') in their name,
      // we replace the decoded '+' back to empty space ' '.
      // For more, see https://github.com/SAP/spartacus/issues/7348
      query: this.codec.decodeValue(this.decodeUriComponentSafe(query)).replace(/\+/g, " ")
    };
  }
  decodeUriComponentSafe(query) {
    return query.replace(/%(?![0-9a-fA-F]{2})/g, "%25");
  }
  static {
    this.ɵfac = function FacetService_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _FacetService)(ɵɵinject(ProductFacetService));
    };
  }
  static {
    this.ɵprov = ɵɵdefineInjectable({
      token: _FacetService,
      factory: _FacetService.ɵfac,
      providedIn: "root"
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(FacetService, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], () => [{
    type: ProductFacetService
  }], null);
})();
var ActiveFacetsComponent = class _ActiveFacetsComponent {
  constructor(facetService) {
    this.facetService = facetService;
    this.role = "group";
    this.labelledby = "cx-active-facets-groupName";
    this.facetList$ = this.facetService.facetList$;
    this.closeIcon = ICON_TYPE.CLOSE;
  }
  getLinkParams(facet) {
    return this.facetService.getLinkParams(facet.removeQuery?.query?.value ?? "");
  }
  /**
   * The focus key is used to persist the focus on the facet when the DOM is being
   * recreated. We only apply the focus key for the given _active_ facet when there
   * the original facets is not available. This happens for non multi-valued facets.
   *
   * With this approach, the we keep the focus, either at the facet list or on the
   * active facets.
   */
  getFocusKey(facetList, facet) {
    return facetList.facets?.find((f) => f.values?.find((val) => val.name === facet.facetValueName)) ? "" : facet.facetValueName;
  }
  /**
   * Purpose of this function is to allow keyboard users to click on a filter they
   * wish to remove by pressing spacebar. Event not handled natively by <a> elements.
   *
   * @param event spacebar keydown
   */
  removeFilterWithSpacebar(event) {
    event?.preventDefault();
    event?.target?.dispatchEvent(new MouseEvent("click", {
      cancelable: true
    }));
  }
  static {
    this.ɵfac = function ActiveFacetsComponent_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _ActiveFacetsComponent)(ɵɵdirectiveInject(FacetService));
    };
  }
  static {
    this.ɵcmp = ɵɵdefineComponent({
      type: _ActiveFacetsComponent,
      selectors: [["cx-active-facets"]],
      hostVars: 2,
      hostBindings: function ActiveFacetsComponent_HostBindings(rf, ctx) {
        if (rf & 2) {
          ɵɵattribute("role", ctx.role)("aria-labelledby", ctx.labelledby);
        }
      },
      inputs: {
        closeIcon: "closeIcon"
      },
      standalone: false,
      decls: 2,
      vars: 3,
      consts: [[4, "ngIf"], ["id", "cx-active-facets-groupName", 4, "ngIf"], ["routerLink", "./", "role", "button", 3, "queryParams", "cxFocus", "keydown.space", 4, "ngFor", "ngForOf"], ["id", "cx-active-facets-groupName"], ["routerLink", "./", "role", "button", 3, "keydown.space", "queryParams", "cxFocus"], ["aria-hidden", "true", 3, "type"]],
      template: function ActiveFacetsComponent_Template(rf, ctx) {
        if (rf & 1) {
          ɵɵtemplate(0, ActiveFacetsComponent_ng_container_0_Template, 3, 2, "ng-container", 0);
          ɵɵpipe(1, "async");
        }
        if (rf & 2) {
          ɵɵproperty("ngIf", ɵɵpipeBind1(1, 1, ctx.facetList$));
        }
      },
      dependencies: [NgForOf, NgIf, RouterLink, IconComponent, FocusDirective, AsyncPipe, TranslatePipe],
      encapsulation: 2
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(ActiveFacetsComponent, [{
    type: Component,
    args: [{
      selector: "cx-active-facets",
      changeDetection: ChangeDetectionStrategy.Default,
      standalone: false,
      template: `<ng-container *ngIf="facetList$ | async as facetList">
  <div
    id="cx-active-facets-groupName"
    *ngIf="facetList.activeFacets && facetList.activeFacets.length > 0"
  >
    {{ 'productList.appliedFilter' | cxTranslate }}
  </div>

  <a
    *ngFor="let facet of facetList?.activeFacets"
    routerLink="./"
    [queryParams]="getLinkParams(facet)"
    [cxFocus]="{ key: getFocusKey(facetList, facet) }"
    role="button"
    (keydown.space)="removeFilterWithSpacebar($event)"
    [attr.aria-label]="
      'productList.activeFilter' | cxTranslate: { filter: facet.facetValueName }
    "
    [attr.title]="
      'productList.activeFilter' | cxTranslate: { filter: facet.facetValueName }
    "
  >
    <span>{{ facet.facetValueName }}</span>
    <cx-icon aria-hidden="true" [type]="closeIcon"></cx-icon>
  </a>
</ng-container>
`
    }]
  }], () => [{
    type: FacetService
  }], {
    role: [{
      type: HostBinding,
      args: ["attr.role"]
    }],
    labelledby: [{
      type: HostBinding,
      args: ["attr.aria-labelledby"]
    }],
    closeIcon: [{
      type: Input
    }]
  });
})();
var ActiveFacetsModule = class _ActiveFacetsModule {
  static {
    this.ɵfac = function ActiveFacetsModule_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _ActiveFacetsModule)();
    };
  }
  static {
    this.ɵmod = ɵɵdefineNgModule({
      type: _ActiveFacetsModule,
      declarations: [ActiveFacetsComponent],
      imports: [CommonModule, RouterModule, I18nModule, IconModule, KeyboardFocusModule],
      exports: [ActiveFacetsComponent]
    });
  }
  static {
    this.ɵinj = ɵɵdefineInjector({
      imports: [CommonModule, RouterModule, I18nModule, IconModule, KeyboardFocusModule]
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(ActiveFacetsModule, [{
    type: NgModule,
    args: [{
      imports: [CommonModule, RouterModule, I18nModule, IconModule, KeyboardFocusModule],
      declarations: [ActiveFacetsComponent],
      exports: [ActiveFacetsComponent]
    }]
  }], null, null);
})();
var FacetComponent = class _FacetComponent {
  set facet(value) {
    this._facet = value;
    this.isMultiSelect = !!value.multiSelect;
    this.state$ = this.facetService.getState(value);
  }
  get facet() {
    return this._facet;
  }
  constructor(facetService, elementRef, cd) {
    this.facetService = facetService;
    this.elementRef = elementRef;
    this.cd = cd;
    this.expandIcon = ICON_TYPE.EXPAND;
    this.collapseIcon = ICON_TYPE.COLLAPSE;
    this.featureConfigService = inject(FeatureConfigService, {
      optional: true
    });
  }
  ngAfterViewInit() {
    this.cd.detectChanges();
  }
  /**
   * @deprecated: Header will no longer be used in favour of TabComponent headers.
   *
   * Handles clicking the heading of the facet group, which means toggling
   * the visibility of the group (collapse / expand) and optionally focusing
   * the group.
   */
  toggleGroup(event) {
    const host = this.elementRef.nativeElement;
    const isLocked = this.keyboardFocus?.isLocked;
    this.facetService.toggle(this.facet, this.isExpanded);
    if (!isLocked || this.isExpanded) {
      host.focus();
      event.stopPropagation();
    }
  }
  get isExpanded() {
    return this.values?.first?.nativeElement.offsetParent !== null;
  }
  openLink(event) {
    event.target.click();
    event.preventDefault();
  }
  /**
   * Increases the number of visible values for the facet. This is delegated
   * to `facetService.increaseVisibleValues`.
   */
  increaseVisibleValues() {
    this.facetService.increaseVisibleValues(this.facet);
  }
  /**
   * Decreases the number of visible values for the facet. This is delegated
   * to `facetService.decreaseVisibleValues`.
   */
  decreaseVisibleValues() {
    this.facetService.decreaseVisibleValues(this.facet);
  }
  getLinkParams(value) {
    return this.facetService.getLinkParams(value.query?.query?.value ?? "");
  }
  onKeydown(event) {
    const targetIndex = this.values.toArray().findIndex((el) => {
      return el.nativeElement === event.target;
    });
    switch (event.key) {
      case "ArrowDown":
        this.onArrowDown(event, targetIndex);
        break;
      case "ArrowUp":
        this.onArrowUp(event, targetIndex);
        break;
      case "Tab":
        this.onTabNavigation();
        break;
    }
  }
  /**
   * We temporarily disable tabbing for the facet values.
   * This is to use proper keyboard navigation keys(ArrowUp/ArrowDown) for navigating through the facet values.
   */
  onTabNavigation() {
    disableTabbingForTick(this.values.map((el) => el.nativeElement));
  }
  onArrowDown(event, targetIndex) {
    event.preventDefault();
    this.values.get(targetIndex + 1)?.nativeElement.focus();
  }
  onArrowUp(event, targetIndex) {
    event.preventDefault();
    this.values.get(targetIndex - 1)?.nativeElement.focus();
  }
  static {
    this.ɵfac = function FacetComponent_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _FacetComponent)(ɵɵdirectiveInject(FacetService), ɵɵdirectiveInject(ElementRef), ɵɵdirectiveInject(ChangeDetectorRef));
    };
  }
  static {
    this.ɵcmp = ɵɵdefineComponent({
      type: _FacetComponent,
      selectors: [["cx-facet"]],
      viewQuery: function FacetComponent_Query(rf, ctx) {
        if (rf & 1) {
          ɵɵviewQuery(FocusDirective, 5);
          ɵɵviewQuery(_c622, 5);
          ɵɵviewQuery(_c63, 5);
        }
        if (rf & 2) {
          let _t;
          ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.keyboardFocus = _t.first);
          ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.facetHeader = _t.first);
          ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.values = _t);
        }
      },
      hostVars: 2,
      hostBindings: function FacetComponent_HostBindings(rf, ctx) {
        if (rf & 2) {
          ɵɵclassProp("multi-select", ctx.isMultiSelect);
        }
      },
      inputs: {
        expandIcon: "expandIcon",
        collapseIcon: "collapseIcon",
        facet: "facet"
      },
      standalone: false,
      decls: 2,
      vars: 3,
      consts: [["facetValue", ""], [4, "ngIf"], ["routerLink", "./", "class", "value", 3, "queryParams", "selected", "cxFocus", "keydown", "keydown.space", 4, "ngFor", "ngForOf"], [1, "more"], ["aria-expanded", "true", "class", "cx-action-link", 3, "cxAtMessage", "cxFocus", "click", 4, "ngIf"], ["aria-expanded", "false", "class", "cx-action-link", 3, "cxAtMessage", "cxFocus", "click", 4, "ngIf"], ["routerLink", "./", 1, "value", 3, "keydown", "keydown.space", "queryParams", "cxFocus"], ["aria-hidden", "true"], ["aria-hidden", "true", 1, "count"], [1, "count"], ["aria-expanded", "true", 1, "cx-action-link", 3, "click", "cxAtMessage", "cxFocus"], ["aria-expanded", "false", 1, "cx-action-link", 3, "click", "cxAtMessage", "cxFocus"]],
      template: function FacetComponent_Template(rf, ctx) {
        if (rf & 1) {
          ɵɵtemplate(0, FacetComponent_ng_container_0_Template, 9, 12, "ng-container", 1);
          ɵɵpipe(1, "async");
        }
        if (rf & 2) {
          ɵɵproperty("ngIf", ɵɵpipeBind1(1, 1, ctx.state$));
        }
      },
      dependencies: [AtMessageDirective, NgForOf, NgIf, FocusDirective, RouterLink, AsyncPipe, SlicePipe, TranslatePipe],
      encapsulation: 2,
      changeDetection: 0
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(FacetComponent, [{
    type: Component,
    args: [{
      selector: "cx-facet",
      changeDetection: ChangeDetectionStrategy.OnPush,
      standalone: false,
      template: `<ng-container *ngIf="state$ | async as state">
  <div>
    <!-- TODO: (CXSPA-6892) - Remove feature flag next major release. -->
    <a
      *ngFor="let value of facet.values | slice: 0 : state.topVisible"
      #facetValue
      routerLink="./"
      [queryParams]="getLinkParams(value)"
      class="value"
      [class.selected]="value.selected"
      [cxFocus]="{ key: value.name }"
      (keydown)="onKeydown($event)"
      (keydown.space)="openLink($any($event))"
      [attr.role]="facet.multiSelect ? 'checkbox' : null"
      [attr.aria-checked]="facet.multiSelect ? value.selected : null"
      attr.aria-label="{{
        'productFacetNavigation.ariaLabelItemsAvailable'
          | cxTranslate: { name: value.name, count: value.count }
      }}"
    >
      <span aria-hidden="true"
        >{{ value.name
        }}<span aria-hidden="true" class="count">{{ value.count }}</span></span
      >
    </a>

    <div class="more">
      <a
        *ngFor="
          let value of facet.values
            | slice: state.topVisible ?? 0 : state.maxVisible
        "
        #facetValue
        routerLink="./"
        [queryParams]="getLinkParams(value)"
        class="value"
        [class.selected]="value.selected"
        [cxFocus]="{ key: value.name }"
        (keydown)="onKeydown($event)"
        (keydown.space)="openLink($any($event))"
      >
        <span
          >{{ value.name }}<span class="count">{{ value.count }}</span></span
        >
      </a>

      <button
        *ngIf="
          state.maxVisible &&
          state.topVisible &&
          state.maxVisible > state.topVisible
        "
        (click)="decreaseVisibleValues()"
        [cxAtMessage]="
          'productFacetNavigation.decreaseOptionsVisibility' | cxTranslate
        "
        aria-expanded="true"
        attr.aria-label="{{
          'productFacetNavigation.ariaLabelShowLess' | cxTranslate
        }}"
        class="cx-action-link"
        [cxFocus]="{ key: 'moreorless' }"
      >
        {{ 'productList.showLess' | cxTranslate }}
      </button>

      <button
        *ngIf="
          state.maxVisible &&
          facet.values &&
          state.maxVisible > 0 &&
          state.maxVisible < facet.values.length
        "
        (click)="increaseVisibleValues()"
        [cxAtMessage]="
          'productFacetNavigation.increaseOptionsVisibility' | cxTranslate
        "
        aria-expanded="false"
        attr.aria-label="{{
          'productFacetNavigation.ariaLabelShowMore' | cxTranslate
        }}"
        class="cx-action-link"
        [cxFocus]="{ key: 'moreorless' }"
      >
        {{ 'productList.showMore' | cxTranslate }}
      </button>
    </div>
  </div>
</ng-container>
`
    }]
  }], () => [{
    type: FacetService
  }, {
    type: ElementRef
  }, {
    type: ChangeDetectorRef
  }], {
    expandIcon: [{
      type: Input
    }],
    collapseIcon: [{
      type: Input
    }],
    isMultiSelect: [{
      type: HostBinding,
      args: ["class.multi-select"]
    }],
    values: [{
      type: ViewChildren,
      args: ["facetValue"]
    }],
    keyboardFocus: [{
      type: ViewChild,
      args: [FocusDirective]
    }],
    facetHeader: [{
      type: ViewChild,
      args: ["facetHeader"]
    }],
    facet: [{
      type: Input
    }],
    featureConfigService: [{
      type: Optional
    }]
  });
})();
var FacetListComponent = class _FacetListComponent {
  /**
   * Indicates that the facet navigation is rendered in dialog.
   */
  set isDialog(value) {
    this._isDialog = value;
    if (value) {
      this.renderer.addClass(document.body, "modal-open");
    }
  }
  get isDialog() {
    return this._isDialog;
  }
  handleClick() {
    this.close();
  }
  constructor(facetService, elementRef, renderer) {
    this.facetService = facetService;
    this.elementRef = elementRef;
    this.renderer = renderer;
    this.subscriptions = new Subscription();
    this.closeList = new EventEmitter();
    this.facetList$ = this.facetService.facetList$;
    this.iconTypes = ICON_TYPE;
    this.dialogFocusConfig = {
      trap: true,
      block: true,
      focusOnEscape: true,
      autofocus: "cx-facet > button"
    };
    this.tabConfig = {
      label: "productFacetNavigation.productFacets",
      mode: TAB_MODE.ACCORDIAN,
      openTabs: [0],
      restrictDirectionKeys: true
    };
    this.tabs$ = new BehaviorSubject([]);
    this.focusService = inject(KeyboardFocusService, {
      optional: true
    });
    this.featureConfigService = inject(FeatureConfigService, {
      optional: true
    });
  }
  ngOnInit() {
    this.enableFocusHandlingOnFacetListChanges();
    this.updateTabs();
  }
  ngAfterViewInit() {
    this.updateTabs();
    this.subscriptions.add(this.facetsRef.changes.pipe(filter((changes) => !!changes)).subscribe(() => {
      this.updateTabs();
    }));
  }
  updateTabs() {
    this.facetList$.pipe(take(1)).subscribe((list) => {
      const facets = list.facets;
      const tabs = facets.map((facet, i) => ({
        header: facet.name,
        content: this.facetsRef?.get(i),
        disableBorderFocus: true,
        id: i
      }));
      this.tabs$.next(tabs);
    });
  }
  /**
   * Toggles the facet group in case it is not expanded.
   */
  expandFacetGroup(facet, ref) {
    if (!ref.isExpanded) {
      this.facetService.toggle(facet, ref.isExpanded);
    }
  }
  /**
   * Indicates that the facet group has been expanded.
   */
  isExpanded(facet) {
    return this.facetService.getState(facet).pipe(map((value) => value.toggled === FacetGroupCollapsedState.EXPANDED));
  }
  /**
   * Indicates that the facet group has been collapsed.
   */
  isCollapsed(facet) {
    return this.facetService.getState(facet).pipe(map((value) => value.toggled === FacetGroupCollapsedState.COLLAPSED));
  }
  close(event) {
    this.renderer.removeClass(document.body, "modal-open");
    this.closeList.emit(event);
  }
  block(event) {
    event?.stopPropagation();
  }
  enableFocusHandlingOnFacetListChanges() {
    this.subscriptions.add(this.facetService.facetList$.subscribe((facetList) => this.handleDialogFocus(facetList.facets)));
  }
  handleDialogFocus(facets) {
    if (!this.isDialog) {
      return;
    }
    const focusKey = this.focusService?.get();
    if (!focusKey) {
      return;
    }
    const focusedFacet = facets.find((facet) => facet.values?.some((value) => {
      return value.name === focusKey;
    }));
    if (focusedFacet) {
      return;
    }
    if (!facets?.length) {
      this.backToResultsBtn?.nativeElement.focus();
      this.focusService?.clear();
      return;
    }
    const firstAvailableFacet = facets[0];
    this.focusService?.set(firstAvailableFacet.name);
  }
  ngOnDestroy() {
    this.subscriptions.unsubscribe();
  }
  static {
    this.ɵfac = function FacetListComponent_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _FacetListComponent)(ɵɵdirectiveInject(FacetService), ɵɵdirectiveInject(ElementRef), ɵɵdirectiveInject(Renderer2));
    };
  }
  static {
    this.ɵcmp = ɵɵdefineComponent({
      type: _FacetListComponent,
      selectors: [["cx-facet-list"]],
      viewQuery: function FacetListComponent_Query(rf, ctx) {
        if (rf & 1) {
          ɵɵviewQuery(_c66, 5);
          ɵɵviewQuery(_c67, 5);
        }
        if (rf & 2) {
          let _t;
          ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.backToResultsBtn = _t.first);
          ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.facetsRef = _t);
        }
      },
      hostBindings: function FacetListComponent_HostBindings(rf, ctx) {
        if (rf & 1) {
          ɵɵlistener("click", function FacetListComponent_click_HostBindingHandler() {
            return ctx.handleClick();
          });
        }
      },
      inputs: {
        isDialog: "isDialog"
      },
      outputs: {
        closeList: "closeList"
      },
      standalone: false,
      decls: 2,
      vars: 3,
      consts: [["facetsRef", ""], ["backToResultsBtn", ""], ["class", "inner", 3, "cxFocus", "tabindex", "esc", "click", 4, "ngIf"], [1, "inner", 3, "esc", "click", "cxFocus", "tabindex"], [1, "list-header"], ["type", "button", 1, "close", 3, "click", "title"], ["aria-hidden", "true", 3, "type"], [3, "tabs$", "config"], [4, "ngFor", "ngForOf"], ["class", "cx-facet-list-footer", 4, "ngIf"], ["role", "group", 3, "facet"], [1, "cx-facet-list-footer"], ["cxAutoFocus", "", 1, "btn", "btn-primary", 3, "click"]],
      template: function FacetListComponent_Template(rf, ctx) {
        if (rf & 1) {
          ɵɵtemplate(0, FacetListComponent_section_0_Template, 13, 20, "section", 2);
          ɵɵpipe(1, "async");
        }
        if (rf & 2) {
          let tmp_0_0;
          ɵɵproperty("ngIf", (tmp_0_0 = ɵɵpipeBind1(1, 1, ctx.facetList$)) == null ? null : tmp_0_0.facets);
        }
      },
      dependencies: [NgForOf, NgIf, IconComponent, FacetComponent, FocusDirective, TabComponent, AsyncPipe, TranslatePipe],
      encapsulation: 2,
      changeDetection: 0
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(FacetListComponent, [{
    type: Component,
    args: [{
      selector: "cx-facet-list",
      changeDetection: ChangeDetectionStrategy.OnPush,
      standalone: false,
      template: `<section
  class="inner"
  *ngIf="(facetList$ | async)?.facets as facets"
  [attr.aria-label]="'productFacetNavigation.filterBy.facet' | cxTranslate"
  [cxFocus]="isDialog ? dialogFocusConfig : {}"
  [tabindex]="-1"
  (esc)="close($event)"
  (click)="block($event)"
>
  <div class="list-header">
    <h4>{{ 'productList.filterBy.label' | cxTranslate }}</h4>
    <button
      type="button"
      class="close"
      title="{{ 'common.close' | cxTranslate }}"
      [attr.aria-label]="'common.close' | cxTranslate"
      (click)="close()"
    >
      <cx-icon aria-hidden="true" [type]="iconTypes.CLOSE"></cx-icon>
    </button>
  </div>

  <!--
      Here we'd like to introduce configurable facet components,
      either by using specific configuration or generic sproutlets
  -->
  <cx-tab [tabs$]="tabs$" [config]="tabConfig"></cx-tab>

  <ng-template #facetsRef *ngFor="let facet of facets">
    <cx-facet
      [facet]="facet"
      role="group"
      attr.aria-label="{{
        'productFacetNavigation.ariaLabelItemsAvailable'
          | cxTranslate
            : {
                name: facet.name,
                count: facet?.values?.length,
              }
      }}"
    ></cx-facet>
  </ng-template>

  <div *ngIf="isDialog" class="cx-facet-list-footer">
    <button
      #backToResultsBtn
      class="btn btn-primary"
      (click)="close()"
      cxAutoFocus
    >
      {{ 'productFacetNavigation.backToResults' | cxTranslate }}
    </button>
  </div>
</section>
`
    }]
  }], () => [{
    type: FacetService
  }, {
    type: ElementRef
  }, {
    type: Renderer2
  }], {
    backToResultsBtn: [{
      type: ViewChild,
      args: ["backToResultsBtn"]
    }],
    isDialog: [{
      type: Input
    }],
    closeList: [{
      type: Output
    }],
    facetsRef: [{
      type: ViewChildren,
      args: ["facetsRef"]
    }],
    handleClick: [{
      type: HostListener,
      args: ["click"]
    }],
    focusService: [{
      type: Optional
    }],
    featureConfigService: [{
      type: Optional
    }]
  });
})();
var FacetModule = class _FacetModule {
  static {
    this.ɵfac = function FacetModule_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _FacetModule)();
    };
  }
  static {
    this.ɵmod = ɵɵdefineNgModule({
      type: _FacetModule,
      declarations: [FacetComponent],
      imports: [AtMessageModule, CommonModule, I18nModule, IconModule, KeyboardFocusModule, RouterModule, UrlModule, FeaturesConfigModule],
      exports: [FacetComponent]
    });
  }
  static {
    this.ɵinj = ɵɵdefineInjector({
      imports: [AtMessageModule, CommonModule, I18nModule, IconModule, KeyboardFocusModule, RouterModule, UrlModule, FeaturesConfigModule]
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(FacetModule, [{
    type: NgModule,
    args: [{
      imports: [AtMessageModule, CommonModule, I18nModule, IconModule, KeyboardFocusModule, RouterModule, UrlModule, FeaturesConfigModule],
      declarations: [FacetComponent],
      exports: [FacetComponent]
    }]
  }], null, null);
})();
var FacetListModule = class _FacetListModule {
  static {
    this.ɵfac = function FacetListModule_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _FacetListModule)();
    };
  }
  static {
    this.ɵmod = ɵɵdefineNgModule({
      type: _FacetListModule,
      declarations: [FacetListComponent],
      imports: [CommonModule, I18nModule, IconModule, FacetModule, KeyboardFocusModule, FeaturesConfigModule, TabModule],
      exports: [FacetListComponent]
    });
  }
  static {
    this.ɵinj = ɵɵdefineInjector({
      imports: [CommonModule, I18nModule, IconModule, FacetModule, KeyboardFocusModule, FeaturesConfigModule, TabModule]
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(FacetListModule, [{
    type: NgModule,
    args: [{
      imports: [CommonModule, I18nModule, IconModule, FacetModule, KeyboardFocusModule, FeaturesConfigModule, TabModule],
      declarations: [FacetListComponent],
      exports: [FacetListComponent]
    }]
  }], null, null);
})();
var ProductFacetNavigationComponent = class _ProductFacetNavigationComponent {
  constructor(breakpointService) {
    this.breakpointService = breakpointService;
    this.iconTypes = ICON_TYPE;
    this.CLOSE_DELAY = 300;
    this.open$ = new BehaviorSubject(false);
    this.isOpen$ = this.breakpointService.breakpoint$.pipe(
      // deffer emitting a new value to the next micro-task to ensure that the `hasTrigger`
      // method represents the actual UI state.
      observeOn(asapScheduler),
      switchMap(() => this.hasTrigger ? this.open$ : of(true)),
      delayWhen((launched) => interval(launched ? 0 : this.CLOSE_DELAY))
    );
    this.isActive$ = this.open$.pipe(
      // deffer emitting a new value to the next micro-task to ensure the active class is
      //  applied after the DOM is created
      observeOn(asapScheduler)
    );
  }
  launch() {
    this.open$.next(true);
  }
  close() {
    this.open$.next(false);
    this.trigger.nativeElement.focus();
  }
  /**
   * Indicates that the facet navigation should be open explicitely by a trigger.
   * This is fully controlled by CSS, where the trigger button can be hidden
   * (display:none) for certain screen sizes.
   */
  get hasTrigger() {
    return this.trigger.nativeElement.offsetParent !== null;
  }
  static {
    this.ɵfac = function ProductFacetNavigationComponent_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _ProductFacetNavigationComponent)(ɵɵdirectiveInject(BreakpointService));
    };
  }
  static {
    this.ɵcmp = ɵɵdefineComponent({
      type: _ProductFacetNavigationComponent,
      selectors: [["cx-product-facet-navigation"]],
      viewQuery: function ProductFacetNavigationComponent_Query(rf, ctx) {
        if (rf & 1) {
          ɵɵviewQuery(_c68, 5);
        }
        if (rf & 2) {
          let _t;
          ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.trigger = _t.first);
        }
      },
      standalone: false,
      decls: 8,
      vars: 7,
      consts: [["trigger", ""], [1, "btn", "btn-secondary", "btn-block", "dialog-trigger", 3, "click"], [3, "type"], [3, "isDialog", "active", "dialog", "closeList", 4, "ngIf"], [3, "closeList", "isDialog"]],
      template: function ProductFacetNavigationComponent_Template(rf, ctx) {
        if (rf & 1) {
          const _r1 = ɵɵgetCurrentView();
          ɵɵelementStart(0, "button", 1, 0);
          ɵɵlistener("click", function ProductFacetNavigationComponent_Template_button_click_0_listener() {
            ɵɵrestoreView(_r1);
            return ɵɵresetView(ctx.launch());
          });
          ɵɵelement(2, "cx-icon", 2);
          ɵɵtext(3);
          ɵɵpipe(4, "cxTranslate");
          ɵɵelementEnd();
          ɵɵelement(5, "cx-active-facets");
          ɵɵtemplate(6, ProductFacetNavigationComponent_cx_facet_list_6_Template, 2, 7, "cx-facet-list", 3);
          ɵɵpipe(7, "async");
        }
        if (rf & 2) {
          ɵɵadvance(2);
          ɵɵproperty("type", ctx.iconTypes.FILTER);
          ɵɵadvance();
          ɵɵtextInterpolate1(" ", ɵɵpipeBind1(4, 3, "productList.filterBy.label"), "\n");
          ɵɵadvance(3);
          ɵɵproperty("ngIf", ɵɵpipeBind1(7, 5, ctx.isOpen$));
        }
      },
      dependencies: [NgIf, FacetListComponent, ActiveFacetsComponent, IconComponent, AsyncPipe, TranslatePipe],
      encapsulation: 2,
      changeDetection: 0
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(ProductFacetNavigationComponent, [{
    type: Component,
    args: [{
      selector: "cx-product-facet-navigation",
      changeDetection: ChangeDetectionStrategy.OnPush,
      standalone: false,
      template: `<button
  #trigger
  class="btn btn-secondary btn-block dialog-trigger"
  (click)="launch()"
>
  <cx-icon [type]="iconTypes.FILTER"></cx-icon>
  {{ 'productList.filterBy.label' | cxTranslate }}
</button>

<cx-active-facets></cx-active-facets>

<cx-facet-list
  *ngIf="isOpen$ | async"
  [isDialog]="hasTrigger"
  (closeList)="close()"
  [class.active]="isActive$ | async"
  [class.dialog]="hasTrigger"
></cx-facet-list>
`
    }]
  }], () => [{
    type: BreakpointService
  }], {
    trigger: [{
      type: ViewChild,
      args: ["trigger"]
    }]
  });
})();
var ProductFacetNavigationModule = class _ProductFacetNavigationModule {
  static {
    this.ɵfac = function ProductFacetNavigationModule_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _ProductFacetNavigationModule)();
    };
  }
  static {
    this.ɵmod = ɵɵdefineNgModule({
      type: _ProductFacetNavigationModule,
      declarations: [ProductFacetNavigationComponent],
      imports: [CommonModule, FacetListModule, ActiveFacetsModule, IconModule, I18nModule, ConfigModule],
      exports: [ProductFacetNavigationComponent]
    });
  }
  static {
    this.ɵinj = ɵɵdefineInjector({
      imports: [CommonModule, FacetListModule, ActiveFacetsModule, IconModule, I18nModule, ConfigModule.withConfig({
        cmsComponents: {
          ProductRefinementComponent: {
            component: ProductFacetNavigationComponent
          }
        }
      })]
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(ProductFacetNavigationModule, [{
    type: NgModule,
    args: [{
      imports: [CommonModule, FacetListModule, ActiveFacetsModule, IconModule, I18nModule, ConfigModule.withConfig({
        cmsComponents: {
          ProductRefinementComponent: {
            component: ProductFacetNavigationComponent
          }
        }
      })],
      declarations: [ProductFacetNavigationComponent],
      exports: [ProductFacetNavigationComponent]
    }]
  }], null, null);
})();
var defaultViewConfig = {
  view: {
    defaultPageSize: 12,
    infiniteScroll: {
      active: false,
      productLimit: 0,
      showMoreButton: false
    }
  }
};
var ProductListModule = class _ProductListModule {
  static {
    this.ɵfac = function ProductListModule_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _ProductListModule)();
    };
  }
  static {
    this.ɵmod = ɵɵdefineNgModule({
      type: _ProductListModule,
      declarations: [ProductListComponent, ProductListItemComponent, ProductGridItemComponent, ProductViewComponent, ProductScrollComponent],
      imports: [AtMessageModule, CommonModule, I18nModule, IconModule, InfiniteScrollModule, ItemCounterModule, ListNavigationModule, MediaModule, OutletModule, PageComponentModule, RouterModule, SpinnerModule, StarRatingModule, UrlModule, FeaturesConfigModule, LcpContextDirectiveModule],
      exports: [ProductListComponent, ProductListItemComponent, ProductGridItemComponent, ProductViewComponent, ProductScrollComponent]
    });
  }
  static {
    this.ɵinj = ɵɵdefineInjector({
      providers: [ProductListService, provideDefaultConfig(defaultViewConfig), provideDefaultConfig({
        cmsComponents: {
          CMSProductListComponent: {
            component: ProductListComponent,
            data: {
              composition: {
                inner: ["ProductAddToCartComponent"]
              }
            }
          },
          ProductGridComponent: {
            component: ProductListComponent,
            data: {
              composition: {
                inner: ["ProductAddToCartComponent"]
              }
            }
          },
          SearchResultsListComponent: {
            component: ProductListComponent,
            data: {
              composition: {
                inner: ["ProductAddToCartComponent"]
              }
            }
          }
        }
      })],
      imports: [AtMessageModule, CommonModule, I18nModule, IconModule, InfiniteScrollModule, ItemCounterModule, ListNavigationModule, MediaModule, OutletModule, PageComponentModule, RouterModule, SpinnerModule, StarRatingModule, UrlModule, FeaturesConfigModule, LcpContextDirectiveModule]
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(ProductListModule, [{
    type: NgModule,
    args: [{
      imports: [AtMessageModule, CommonModule, I18nModule, IconModule, InfiniteScrollModule, ItemCounterModule, ListNavigationModule, MediaModule, OutletModule, PageComponentModule, RouterModule, SpinnerModule, StarRatingModule, UrlModule, FeaturesConfigModule, LcpContextDirectiveModule],
      providers: [ProductListService, provideDefaultConfig(defaultViewConfig), provideDefaultConfig({
        cmsComponents: {
          CMSProductListComponent: {
            component: ProductListComponent,
            data: {
              composition: {
                inner: ["ProductAddToCartComponent"]
              }
            }
          },
          ProductGridComponent: {
            component: ProductListComponent,
            data: {
              composition: {
                inner: ["ProductAddToCartComponent"]
              }
            }
          },
          SearchResultsListComponent: {
            component: ProductListComponent,
            data: {
              composition: {
                inner: ["ProductAddToCartComponent"]
              }
            }
          }
        }
      })],
      declarations: [ProductListComponent, ProductListItemComponent, ProductGridItemComponent, ProductViewComponent, ProductScrollComponent],
      exports: [ProductListComponent, ProductListItemComponent, ProductGridItemComponent, ProductViewComponent, ProductScrollComponent]
    }]
  }], null, null);
})();
var ProductCarouselItemComponent = class _ProductCarouselItemComponent {
  constructor(productListItemContextSource) {
    this.productListItemContextSource = productListItemContextSource;
  }
  ngOnChanges(changes) {
    if (changes?.item) {
      this.productListItemContextSource.product$.next(this.item);
    }
  }
  static {
    this.ɵfac = function ProductCarouselItemComponent_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _ProductCarouselItemComponent)(ɵɵdirectiveInject(ProductListItemContextSource));
    };
  }
  static {
    this.ɵcmp = ɵɵdefineComponent({
      type: _ProductCarouselItemComponent,
      selectors: [["cx-product-carousel-item"]],
      inputs: {
        item: "item",
        itemIndex: "itemIndex"
      },
      standalone: false,
      features: [ɵɵProvidersFeature([ProductListItemContextSource, {
        provide: ProductListItemContext,
        useExisting: ProductListItemContextSource
      }]), ɵɵNgOnChangesFeature],
      decls: 9,
      vars: 7,
      consts: [["tabindex", "0", 3, "routerLink"], [4, "cxLcpContext"], [1, "cx-product-name"], [1, "price"], [1, "actions"], ["cxInnerComponentsHost", ""], ["format", "product", 3, "container", "alt", "fetchPriority"]],
      template: function ProductCarouselItemComponent_Template(rf, ctx) {
        if (rf & 1) {
          ɵɵelementStart(0, "a", 0);
          ɵɵpipe(1, "cxUrl");
          ɵɵtemplate(2, ProductCarouselItemComponent_ng_container_2_Template, 3, 5, "ng-container", 1);
          ɵɵelementStart(3, "h3", 2);
          ɵɵtext(4);
          ɵɵelementEnd();
          ɵɵelementStart(5, "div", 3);
          ɵɵtext(6);
          ɵɵelementEnd()();
          ɵɵelementStart(7, "div", 4);
          ɵɵelementContainer(8, 5);
          ɵɵelementEnd();
        }
        if (rf & 2) {
          ɵɵproperty("routerLink", ɵɵpipeBind1(1, 3, ɵɵpureFunction1(5, _c46, ctx.item)));
          ɵɵadvance(4);
          ɵɵtextInterpolate1(" ", ctx.item.name, " ");
          ɵɵadvance(2);
          ɵɵtextInterpolate1(" ", ctx.item.price == null ? null : ctx.item.price.formattedValue, " ");
        }
      },
      dependencies: [MediaComponent, RouterLink, InnerComponentsHostDirective, LcpContextDirective, AsyncPipe, UrlPipe],
      encapsulation: 2,
      changeDetection: 0
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(ProductCarouselItemComponent, [{
    type: Component,
    args: [{
      selector: "cx-product-carousel-item",
      providers: [ProductListItemContextSource, {
        provide: ProductListItemContext,
        useExisting: ProductListItemContextSource
      }],
      changeDetection: ChangeDetectionStrategy.OnPush,
      standalone: false,
      template: `<a tabindex="0" [routerLink]="{ cxRoute: 'product', params: item } | cxUrl">
  <ng-container *cxLcpContext="let lcpContext">
    <cx-media
      [container]="item.images?.PRIMARY"
      format="product"
      [alt]="item.name ?? ''"
      [fetchPriority]="
        itemIndex === 0 ? (lcpContext.fetchPriority$ | async) : undefined
      "
    ></cx-media>
  </ng-container>
  <h3 class="cx-product-name">
    {{ item.name }}
  </h3>
  <div class="price">
    {{ item.price?.formattedValue }}
  </div>
</a>
<div class="actions">
  <ng-container cxInnerComponentsHost></ng-container>
</div>
`
    }]
  }], () => [{
    type: ProductListItemContextSource
  }], {
    item: [{
      type: Input
    }],
    itemIndex: [{
      type: Input
    }]
  });
})();
var ProductCarouselComponent = class _ProductCarouselComponent {
  constructor(componentData, productService) {
    this.componentData = componentData;
    this.productService = productService;
    this.featureConfigService = inject(FeatureConfigService);
    this.productSearchByCodeService = inject(ProductSearchByCodeService);
    this.productSearchByCategoryService = inject(ProductSearchByCategoryService);
    this.PRODUCT_SCOPE = [ProductScope.LIST, ProductScope.STOCK];
    this.PRODUCT_SCOPE_ITEM = [ProductScope.LIST_ITEM];
    this.componentData$ = this.componentData.data$.pipe(filter((data) => Boolean(data)));
    this.title$ = this.componentData$.pipe(map((data) => data.title));
    this.items$ = this.componentData$.pipe(switchMap((data) => this.handleCategoryCodes(data)), map((data) => {
      const componentMappingExist = !!data.composition?.inner?.length;
      const codes = data.productCodes?.trim().split(" ") ?? [];
      return {
        componentMappingExist,
        codes
      };
    }), map(({
      componentMappingExist,
      codes
    }) => {
      const scope = componentMappingExist ? "carousel" : "carouselMinimal";
      return codes.map((code) => this.productSearchByCodeService.get({
        code,
        scope
      }));
    }));
    this.trackByFn = (_index, item) => item?.code;
    useFeatureStyles("productCarouselScrolling");
  }
  handleCategoryCodes(data) {
    const categoryCodes = data?.categoryCodes?.split(" ");
    if (categoryCodes && this.featureConfigService.isEnabled("enableCarouselCategoryProducts")) {
      return zip(categoryCodes.map((categoryCode) => this.productSearchByCategoryService.get({
        categoryCode,
        scope: ProductScope.CODE
      }))).pipe(map((results) => {
        const codes = results.flat().map((product) => product?.code).join(" ");
        return __spreadProps(__spreadValues({}, data), {
          productCodes: data.productCodes + " " + codes
        });
      }));
    }
    return of(data);
  }
  static {
    this.ɵfac = function ProductCarouselComponent_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _ProductCarouselComponent)(ɵɵdirectiveInject(CmsComponentData), ɵɵdirectiveInject(ProductService));
    };
  }
  static {
    this.ɵcmp = ɵɵdefineComponent({
      type: _ProductCarouselComponent,
      selectors: [["cx-product-carousel"]],
      standalone: false,
      decls: 4,
      vars: 2,
      consts: [["carouselItem", ""], [4, "cxFeature"], ["role", "region", 3, "items", "title", "template", "trackByFn", 4, "ngIf"], ["role", "region", 3, "items", "title", "template", "trackByFn"], ["role", "region", "itemWidth", "285px", 3, "items", "title", "template", "trackByFn", 4, "ngIf"], ["role", "region", "itemWidth", "285px", 3, "items", "title", "template", "trackByFn"], [3, "item", "itemIndex"]],
      template: function ProductCarouselComponent_Template(rf, ctx) {
        if (rf & 1) {
          ɵɵtemplate(0, ProductCarouselComponent_ng_container_0_Template, 3, 3, "ng-container", 1)(1, ProductCarouselComponent_ng_container_1_Template, 3, 3, "ng-container", 1)(2, ProductCarouselComponent_ng_template_2_Template, 1, 2, "ng-template", null, 0, ɵɵtemplateRefExtractor);
        }
        if (rf & 2) {
          ɵɵproperty("cxFeature", "productCarouselScrolling");
          ɵɵadvance();
          ɵɵproperty("cxFeature", "!productCarouselScrolling");
        }
      },
      dependencies: [NgIf, CarouselComponent, CarouselScrollingComponent, FeatureDirective, ProductCarouselItemComponent, AsyncPipe, TranslatePipe],
      encapsulation: 2,
      changeDetection: 0
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(ProductCarouselComponent, [{
    type: Component,
    args: [{
      selector: "cx-product-carousel",
      changeDetection: ChangeDetectionStrategy.OnPush,
      standalone: false,
      template: `<ng-container *cxFeature="'productCarouselScrolling'">
  <cx-carousel-scrolling
    *ngIf="items$ | async as items"
    role="region"
    [attr.aria-label]="
      'productCarousel.carouselLabel' | cxTranslate: { title: title$ | async }
    "
    [items]="items"
    [title]="title$ | async"
    [template]="carouselItem"
    [trackByFn]="trackByFn"
  >
  </cx-carousel-scrolling>
</ng-container>
<ng-container *cxFeature="'!productCarouselScrolling'">
  <cx-carousel
    *ngIf="items$ | async as items"
    role="region"
    [attr.aria-label]="
      'productCarousel.carouselLabel' | cxTranslate: { title: title$ | async }
    "
    [items]="items"
    [title]="title$ | async"
    [template]="carouselItem"
    itemWidth="285px"
    [trackByFn]="trackByFn"
  >
  </cx-carousel>
</ng-container>

<ng-template #carouselItem let-item="item" let-itemIndex="itemIndex">
  <cx-product-carousel-item
    [item]="item"
    [itemIndex]="itemIndex"
  ></cx-product-carousel-item>
</ng-template>
`
    }]
  }], () => [{
    type: CmsComponentData
  }, {
    type: ProductService
  }], null);
})();
var ProductCarouselModule = class _ProductCarouselModule {
  static {
    this.ɵfac = function ProductCarouselModule_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _ProductCarouselModule)();
    };
  }
  static {
    this.ɵmod = ɵɵdefineNgModule({
      type: _ProductCarouselModule,
      declarations: [ProductCarouselComponent, ProductCarouselItemComponent],
      imports: [CommonModule, CarouselModule, CarouselScrollingModule, MediaModule, RouterModule, UrlModule, I18nModule, PageComponentModule, FeaturesConfigModule, LcpContextDirectiveModule],
      exports: [ProductCarouselComponent, ProductCarouselItemComponent]
    });
  }
  static {
    this.ɵinj = ɵɵdefineInjector({
      providers: [provideDefaultConfig({
        cmsComponents: {
          ProductCarouselComponent: {
            component: ProductCarouselComponent
          }
        }
      })],
      imports: [CommonModule, CarouselModule, CarouselScrollingModule, MediaModule, RouterModule, UrlModule, I18nModule, PageComponentModule, FeaturesConfigModule, LcpContextDirectiveModule]
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(ProductCarouselModule, [{
    type: NgModule,
    args: [{
      imports: [CommonModule, CarouselModule, CarouselScrollingModule, MediaModule, RouterModule, UrlModule, I18nModule, PageComponentModule, FeaturesConfigModule, LcpContextDirectiveModule],
      providers: [provideDefaultConfig({
        cmsComponents: {
          ProductCarouselComponent: {
            component: ProductCarouselComponent
          }
        }
      })],
      declarations: [ProductCarouselComponent, ProductCarouselItemComponent],
      exports: [ProductCarouselComponent, ProductCarouselItemComponent]
    }]
  }], null, null);
})();
var ProductReferencesComponent = class _ProductReferencesComponent {
  constructor(cmsComponentData, currentProductService, productReferenceService) {
    this.cmsComponentData = cmsComponentData;
    this.currentProductService = currentProductService;
    this.productReferenceService = productReferenceService;
    this.items$ = this.productCode$.pipe(withLatestFrom(this.componentData$), tap(([productCode, data]) => this.productReferenceService.loadProductReferences(productCode, data.productReferenceTypes)), switchMap(([productCode, data]) => this.getProductReferences(productCode, data.productReferenceTypes ?? "")));
    this.trackByFn = (_index, item) => item?.code;
    useFeatureStyles("productCarouselScrolling");
  }
  get componentData$() {
    return this.cmsComponentData.data$.pipe(filter((data) => Boolean(data)));
  }
  /**
   * Returns an Observable String for the product code
   */
  get productCode$() {
    return this.currentProductService.getProduct().pipe(filter(isNotNullable), map((product) => product.code ?? ""), tap((_) => this.productReferenceService.cleanReferences()));
  }
  /**
   * Returns an Observable String for the title
   */
  get title$() {
    return this.componentData$.pipe(map((data) => data?.title));
  }
  /**
   * Returns an observable for product references
   */
  getProductReferences(code, referenceType) {
    return this.productReferenceService.getProductReferences(code, referenceType).pipe(filter((references) => Boolean(references)), map((references) => references.map((reference) => of(reference.target))));
  }
  static {
    this.ɵfac = function ProductReferencesComponent_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _ProductReferencesComponent)(ɵɵdirectiveInject(CmsComponentData), ɵɵdirectiveInject(CurrentProductService), ɵɵdirectiveInject(ProductReferenceService));
    };
  }
  static {
    this.ɵcmp = ɵɵdefineComponent({
      type: _ProductReferencesComponent,
      selectors: [["cx-product-references"]],
      standalone: false,
      decls: 4,
      vars: 2,
      consts: [["carouselItem", ""], [4, "cxFeature"], [4, "ngIf"], [3, "title", "items", "template", "trackByFn", 4, "ngIf"], [3, "title", "items", "template", "trackByFn"], ["tabindex", "0", 3, "routerLink"], ["format", "product", 3, "container"], [1, "price"]],
      template: function ProductReferencesComponent_Template(rf, ctx) {
        if (rf & 1) {
          ɵɵtemplate(0, ProductReferencesComponent_ng_container_0_Template, 3, 3, "ng-container", 1)(1, ProductReferencesComponent_ng_container_1_Template, 3, 3, "ng-container", 1)(2, ProductReferencesComponent_ng_template_2_Template, 7, 8, "ng-template", null, 0, ɵɵtemplateRefExtractor);
        }
        if (rf & 2) {
          ɵɵproperty("cxFeature", "productCarouselScrolling");
          ɵɵadvance();
          ɵɵproperty("cxFeature", "!productCarouselScrolling");
        }
      },
      dependencies: [NgIf, CarouselComponent, CarouselScrollingComponent, MediaComponent, RouterLink, FeatureDirective, AsyncPipe, UrlPipe],
      encapsulation: 2,
      changeDetection: 0
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(ProductReferencesComponent, [{
    type: Component,
    args: [{
      selector: "cx-product-references",
      changeDetection: ChangeDetectionStrategy.OnPush,
      standalone: false,
      template: `<ng-container *cxFeature="'productCarouselScrolling'">
  <ng-container *ngIf="items$ | async as items">
    <cx-carousel-scrolling
      *ngIf="items.length"
      [title]="title$ | async"
      [items]="items"
      [template]="carouselItem"
      [trackByFn]="trackByFn"
    >
    </cx-carousel-scrolling>
  </ng-container>
</ng-container>
<ng-container *cxFeature="'!productCarouselScrolling'">
  <ng-container *ngIf="items$ | async as items">
    <cx-carousel
      *ngIf="items$ | async as items"
      [title]="title$ | async"
      [items]="items"
      [template]="carouselItem"
      [trackByFn]="trackByFn"
    >
    </cx-carousel>
  </ng-container>
</ng-container>

<ng-template #carouselItem let-item="item">
  <a tabindex="0" [routerLink]="{ cxRoute: 'product', params: item } | cxUrl">
    <cx-media [container]="item.images?.PRIMARY" format="product"></cx-media>
    <h4>{{ item.name }}</h4>
    <div class="price">{{ item.price?.formattedValue }}</div>
  </a>
</ng-template>
`
    }]
  }], () => [{
    type: CmsComponentData
  }, {
    type: CurrentProductService
  }, {
    type: ProductReferenceService
  }], null);
})();
var ProductReferencesModule = class _ProductReferencesModule {
  static {
    this.ɵfac = function ProductReferencesModule_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _ProductReferencesModule)();
    };
  }
  static {
    this.ɵmod = ɵɵdefineNgModule({
      type: _ProductReferencesModule,
      declarations: [ProductReferencesComponent],
      imports: [CommonModule, CarouselModule, CarouselScrollingModule, MediaModule, RouterModule, UrlModule, FeaturesConfigModule],
      exports: [ProductReferencesComponent]
    });
  }
  static {
    this.ɵinj = ɵɵdefineInjector({
      providers: [provideDefaultConfig({
        cmsComponents: {
          ProductReferencesComponent: {
            component: ProductReferencesComponent
          }
        }
      })],
      imports: [CommonModule, CarouselModule, CarouselScrollingModule, MediaModule, RouterModule, UrlModule, FeaturesConfigModule]
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(ProductReferencesModule, [{
    type: NgModule,
    args: [{
      imports: [CommonModule, CarouselModule, CarouselScrollingModule, MediaModule, RouterModule, UrlModule, FeaturesConfigModule],
      providers: [provideDefaultConfig({
        cmsComponents: {
          ProductReferencesComponent: {
            component: ProductReferencesComponent
          }
        }
      })],
      declarations: [ProductReferencesComponent],
      exports: [ProductReferencesComponent]
    }]
  }], null, null);
})();
var ProductImagesComponent = class _ProductImagesComponent {
  constructor(currentProductService) {
    this.currentProductService = currentProductService;
    this.mainMediaContainer = new BehaviorSubject(null);
    this.product$ = this.currentProductService.getProduct().pipe(filter(isNotNullable), distinctUntilChanged(), tap((p) => {
      this.mainMediaContainer.next(p.images?.PRIMARY ? p.images.PRIMARY : {});
    }));
    this.thumbs$ = this.product$.pipe(map((p) => this.createThumbs(p)));
    this.mainImage$ = combineLatest([this.product$, this.mainMediaContainer]).pipe(map(([, container]) => container));
    this.trackByFn = (_index, item) => item?.code;
    useFeatureStyles("a11yDifferentiateFocusedAndSelected");
    useFeatureStyles("reserveSpaceForImagesOnPdpAndPlp");
    useFeatureStyles("productCarouselScrolling");
  }
  openImage(item) {
    this.mainMediaContainer.next(item);
  }
  isActive(thumbnail) {
    return this.mainMediaContainer.pipe(filter(Boolean), map((container) => {
      return container.zoom && container.zoom.url && thumbnail.zoom && thumbnail.zoom.url && container.zoom.url === thumbnail.zoom.url;
    }));
  }
  /** find the index of the main media in the list of media */
  getActive(thumbs) {
    return this.mainMediaContainer.pipe(filter(Boolean), map((container) => {
      const current = thumbs.find((t) => t.media && container.zoom && t.media.container && t.media.container.zoom && t.media.container.zoom.url === container.zoom.url);
      return thumbs.indexOf(current);
    }));
  }
  /**
   * Return an array of CarouselItems for the product thumbnails.
   * In case there are less then 2 thumbs, we return null.
   */
  createThumbs(product) {
    if (!product.images || !product.images.GALLERY || Array.isArray(product.images.GALLERY) && product.images.GALLERY.length < 2) {
      return [];
    }
    return product.images.GALLERY.map((c) => of({
      container: c
    }));
  }
  static {
    this.ɵfac = function ProductImagesComponent_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _ProductImagesComponent)(ɵɵdirectiveInject(CurrentProductService));
    };
  }
  static {
    this.ɵcmp = ɵɵdefineComponent({
      type: _ProductImagesComponent,
      selectors: [["cx-product-images"]],
      standalone: false,
      decls: 6,
      vars: 6,
      consts: [["thumb", ""], [4, "ngIf"], [3, "container", "fetchPriority", 4, "cxLcpContext"], [3, "container", "fetchPriority"], [4, "cxFeature"], ["class", "thumbs", 3, "items", "template", "trackByFn", 4, "ngIf"], [1, "thumbs", 3, "items", "template", "trackByFn"], ["class", "thumbs", "itemWidth", "120px", 3, "items", "hideIndicators", "template", 4, "ngIf"], ["itemWidth", "120px", 1, "thumbs", 3, "items", "hideIndicators", "template"], ["tabindex", "0", "format", "product", 3, "focus", "container"]],
      template: function ProductImagesComponent_Template(rf, ctx) {
        if (rf & 1) {
          ɵɵtemplate(0, ProductImagesComponent_ng_container_0_Template, 2, 0, "ng-container", 1);
          ɵɵpipe(1, "async");
          ɵɵtemplate(2, ProductImagesComponent_ng_container_2_Template, 3, 2, "ng-container", 1);
          ɵɵpipe(3, "async");
          ɵɵtemplate(4, ProductImagesComponent_ng_template_4_Template, 2, 5, "ng-template", null, 0, ɵɵtemplateRefExtractor);
        }
        if (rf & 2) {
          ɵɵproperty("ngIf", ɵɵpipeBind1(1, 2, ctx.mainImage$));
          ɵɵadvance(2);
          ɵɵproperty("ngIf", ɵɵpipeBind1(3, 4, ctx.thumbs$));
        }
      },
      dependencies: [NgIf, MediaComponent, CarouselComponent, CarouselScrollingComponent, LcpContextDirective, AsyncPipe],
      encapsulation: 2,
      changeDetection: 0
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(ProductImagesComponent, [{
    type: Component,
    args: [{
      selector: "cx-product-images",
      changeDetection: ChangeDetectionStrategy.OnPush,
      standalone: false,
      template: `<ng-container *ngIf="mainImage$ | async as main">
  <cx-media
    [container]="main"
    *cxLcpContext="let lcpContext"
    [fetchPriority]="lcpContext.fetchPriority$ | async"
  ></cx-media>
</ng-container>

<ng-container *ngIf="thumbs$ | async as thumbs">
  <ng-container *cxFeature="'productCarouselScrolling'">
    <cx-carousel-scrolling
      *ngIf="thumbs.length"
      class="thumbs"
      [items]="thumbs"
      [template]="thumb"
      [trackByFn]="trackByFn"
    ></cx-carousel-scrolling>
  </ng-container>

  <ng-container *cxFeature="'!productCarouselScrolling'">
    <cx-carousel
      *ngIf="thumbs.length"
      class="thumbs"
      [items]="thumbs"
      itemWidth="120px"
      [hideIndicators]="false"
      [template]="thumb"
    ></cx-carousel>
  </ng-container>
</ng-container>

<ng-template #thumb let-item="item">
  <cx-media
    [container]="item.container"
    tabindex="0"
    (focus)="openImage(item.container)"
    [class.is-active]="isActive(item.container) | async"
    format="product"
  >
  </cx-media>
</ng-template>
`
    }]
  }], () => [{
    type: CurrentProductService
  }], null);
})();
var ProductImagesModule = class _ProductImagesModule {
  static {
    this.ɵfac = function ProductImagesModule_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _ProductImagesModule)();
    };
  }
  static {
    this.ɵmod = ɵɵdefineNgModule({
      type: _ProductImagesModule,
      declarations: [ProductImagesComponent],
      imports: [CommonModule, RouterModule, MediaModule, OutletModule, CarouselModule, CarouselScrollingModule, LcpContextDirectiveModule],
      exports: [ProductImagesComponent]
    });
  }
  static {
    this.ɵinj = ɵɵdefineInjector({
      providers: [provideDefaultConfig({
        cmsComponents: {
          ProductImagesComponent: {
            component: ProductImagesComponent
          }
        }
      })],
      imports: [CommonModule, RouterModule, MediaModule, OutletModule, CarouselModule, CarouselScrollingModule, LcpContextDirectiveModule]
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(ProductImagesModule, [{
    type: NgModule,
    args: [{
      imports: [CommonModule, RouterModule, MediaModule, OutletModule, CarouselModule, CarouselScrollingModule, LcpContextDirectiveModule],
      providers: [provideDefaultConfig({
        cmsComponents: {
          ProductImagesComponent: {
            component: ProductImagesComponent
          }
        }
      })],
      declarations: [ProductImagesComponent],
      exports: [ProductImagesComponent]
    }]
  }], null, null);
})();
var ProductIntroComponent = class _ProductIntroComponent {
  constructor(currentProductService, translationService, winRef, eventService) {
    this.currentProductService = currentProductService;
    this.translationService = translationService;
    this.winRef = winRef;
    this.eventService = eventService;
    this.product$ = this.currentProductService.getProduct();
    this.featureConfigService = inject(FeatureConfigService);
    this.areReviewsAvailable$ = merge(
      // Check if reviews component is already defined:
      defer(() => of(!!this.getReviewsComponent())),
      // Observe EventService for reviews availability:
      this.eventService.get(ComponentCreateEvent).pipe(filter((event) => event.id === this.reviewsComponentId), map(() => true)),
      this.eventService.get(ComponentDestroyEvent).pipe(filter((event) => event.id === this.reviewsComponentId), map(() => false))
    );
    this.reviewsComponentId = "ProductReviewsTabComponent";
    this.reviewsTranslationKey = `TabPanelContainer.tabs.${this.reviewsComponentId}`;
  }
  /**
   * Scroll to views component on page and click "Reviews" tab
   */
  showReviews() {
    this.translationService.translate(this.reviewsTranslationKey).subscribe((reviewsTabLabel) => {
      const tabsComponent = this.getTabsComponent();
      const reviewsTab = tabsComponent && this.getTabByLabel(reviewsTabLabel, tabsComponent);
      if (reviewsTab) {
        this.clickTabIfInactive(reviewsTab);
        setTimeout(() => {
          if (this.featureConfigService?.isEnabled("a11yScrollToReviewByShowReview")) {
            requestAnimationFrame(() => {
              reviewsTab.scrollIntoView({
                behavior: "smooth",
                block: "start"
              });
              reviewsTab.focus({
                preventScroll: true
              });
            });
          } else {
            reviewsTab.scrollIntoView({
              behavior: "smooth",
              block: "start"
            });
            reviewsTab.focus({
              preventScroll: true
            });
          }
        });
      }
    }).unsubscribe();
  }
  // NOTE: Does not currently exists as its own component
  // but part of tabs component. This is likely to change in refactor.
  /**
   * Get Reviews Component if exists on page
   */
  getReviewsComponent() {
    return this.winRef.document.querySelector("cx-product-reviews");
  }
  /**
   * Get Tabs Component if exists on page
   */
  getTabsComponent() {
    return this.winRef.document.querySelector("cx-tab-paragraph-container");
  }
  /**
   * Click to activate tab if not already active
   *
   * @param tab tab to click if needed
   */
  clickTabIfInactive(tab) {
    if (!tab.classList.contains("active") || tab.classList.contains("toggled")) {
      tab.click();
    }
  }
  /**
   * Get Tab by label if exists on page
   *
   * @param label label of searched tab
   * @param tabsComponent component containing tabs
   */
  getTabByLabel(label, tabsComponent) {
    const tabElements = tabsComponent.getElementsByTagName("button");
    return Array.from(tabElements).find((buttonElement) => buttonElement.innerHTML.includes(label));
  }
  static {
    this.ɵfac = function ProductIntroComponent_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _ProductIntroComponent)(ɵɵdirectiveInject(CurrentProductService), ɵɵdirectiveInject(TranslationService), ɵɵdirectiveInject(WindowRef), ɵɵdirectiveInject(EventService));
    };
  }
  static {
    this.ɵcmp = ɵɵdefineComponent({
      type: _ProductIntroComponent,
      selectors: [["cx-product-intro"]],
      standalone: false,
      decls: 2,
      vars: 3,
      consts: [[4, "ngIf"], ["class", "rating", 4, "ngIf"], [1, "code"], [1, "rating"], [3, "rating"], [1, "count"], [4, "cxFeature"], ["class", "btn btn-tertiary", 3, "click", 4, "ngIf"], [1, "btn", "btn-tertiary", 3, "click"], ["class", "btn btn-link cx-action-link", 3, "click", 4, "ngIf"], [1, "btn", "btn-link", "cx-action-link", 3, "click"]],
      template: function ProductIntroComponent_Template(rf, ctx) {
        if (rf & 1) {
          ɵɵtemplate(0, ProductIntroComponent_ng_container_0_Template, 6, 6, "ng-container", 0);
          ɵɵpipe(1, "async");
        }
        if (rf & 2) {
          ɵɵproperty("ngIf", ɵɵpipeBind1(1, 1, ctx.product$));
        }
      },
      dependencies: [NgIf, StarRatingComponent, FeatureDirective, AsyncPipe, DecimalPipe, TranslatePipe],
      encapsulation: 2,
      changeDetection: 0
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(ProductIntroComponent, [{
    type: Component,
    args: [{
      selector: "cx-product-intro",
      changeDetection: ChangeDetectionStrategy.OnPush,
      standalone: false,
      template: `<ng-container *ngIf="product$ | async as product">
  <div class="rating" *ngIf="product?.averageRating">
    <cx-star-rating [rating]="product?.averageRating ?? 0"></cx-star-rating>

    <div class="count">({{ product?.numberOfReviews }})</div>
    <!-- TODO: (CXSPA-7603) - Remove feature flag next major release. -->
    <ng-container *cxFeature="'a11yLinkBtnsToTertiaryBtns'">
      <button
        *ngIf="product?.averageRating"
        class="btn btn-tertiary"
        (click)="showReviews()"
        [attr.aria-label]="
          'productSummary.showReviewsDetailed'
            | cxTranslate
              : {
                  rating: product?.averageRating | number: '1.0-1',
                  count: product?.numberOfReviews,
                }
        "
      >
        {{ 'productSummary.showReviews' | cxTranslate }}
      </button>
    </ng-container>
    <ng-container *cxFeature="'!a11yLinkBtnsToTertiaryBtns'">
      <button
        *ngIf="areReviewsAvailable$ | async"
        class="btn btn-link cx-action-link"
        (click)="showReviews()"
        [attr.aria-label]="
          'productSummary.showReviewsDetailed'
            | cxTranslate
              : {
                  rating: product?.averageRating | number: '1.0-1',
                  count: product?.numberOfReviews,
                }
        "
      >
        {{ 'productSummary.showReviews' | cxTranslate }}
      </button>
    </ng-container>
  </div>
  <div class="rating" *ngIf="!product?.averageRating">
    {{ 'productDetails.noReviews' | cxTranslate }}
  </div>
  <div class="code">
    {{ 'productSummary.id' | cxTranslate }} {{ product?.code }}
  </div>
</ng-container>
`
    }]
  }], () => [{
    type: CurrentProductService
  }, {
    type: TranslationService
  }, {
    type: WindowRef
  }, {
    type: EventService
  }], null);
})();
var ProductIntroModule = class _ProductIntroModule {
  static {
    this.ɵfac = function ProductIntroModule_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _ProductIntroModule)();
    };
  }
  static {
    this.ɵmod = ɵɵdefineNgModule({
      type: _ProductIntroModule,
      declarations: [ProductIntroComponent],
      imports: [CommonModule, I18nModule, StarRatingModule, FeaturesConfigModule],
      exports: [ProductIntroComponent]
    });
  }
  static {
    this.ɵinj = ɵɵdefineInjector({
      providers: [provideDefaultConfig({
        cmsComponents: {
          ProductIntroComponent: {
            component: ProductIntroComponent
          }
        }
      })],
      imports: [CommonModule, I18nModule, StarRatingModule, FeaturesConfigModule]
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(ProductIntroModule, [{
    type: NgModule,
    args: [{
      imports: [CommonModule, I18nModule, StarRatingModule, FeaturesConfigModule],
      providers: [provideDefaultConfig({
        cmsComponents: {
          ProductIntroComponent: {
            component: ProductIntroComponent
          }
        }
      })],
      declarations: [ProductIntroComponent],
      exports: [ProductIntroComponent]
    }]
  }], null, null);
})();
var ProductSummaryComponent = class _ProductSummaryComponent {
  getProduct() {
    const productScopes = [ProductScope.DETAILS, ProductScope.PRICE, ProductScope.PROMOTIONS];
    return this.currentProductService.getProduct(productScopes);
  }
  constructor(currentProductService) {
    this.currentProductService = currentProductService;
    this.outlets = ProductDetailOutlets;
    this.product$ = this.getProduct();
  }
  static {
    this.ɵfac = function ProductSummaryComponent_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _ProductSummaryComponent)(ɵɵdirectiveInject(CurrentProductService));
    };
  }
  static {
    this.ɵcmp = ɵɵdefineComponent({
      type: _ProductSummaryComponent,
      selectors: [["cx-product-summary"]],
      standalone: false,
      decls: 2,
      vars: 3,
      consts: [[4, "ngIf"], [3, "cxOutlet", "cxOutletContext"], [3, "promotions"], [1, "price"], [1, "summary", 3, "innerHTML"]],
      template: function ProductSummaryComponent_Template(rf, ctx) {
        if (rf & 1) {
          ɵɵtemplate(0, ProductSummaryComponent_ng_container_0_Template, 4, 9, "ng-container", 0);
          ɵɵpipe(1, "async");
        }
        if (rf & 2) {
          ɵɵproperty("ngIf", ɵɵpipeBind1(1, 1, ctx.product$));
        }
      },
      dependencies: [NgIf, OutletDirective, PromotionsComponent, AsyncPipe, TranslatePipe],
      encapsulation: 2,
      changeDetection: 0
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(ProductSummaryComponent, [{
    type: Component,
    args: [{
      selector: "cx-product-summary",
      changeDetection: ChangeDetectionStrategy.OnPush,
      standalone: false,
      template: `<ng-container *ngIf="product$ | async as product">
  <ng-container *ngIf="product.potentialPromotions">
    <cx-promotions [promotions]="product.potentialPromotions"></cx-promotions>
  </ng-container>

  <ng-template
    [cxOutlet]="outlets.PRICE"
    [cxOutletContext]="{ product: product }"
  >
    <div
      [attr.aria-label]="'productSummary.newItemPrice' | cxTranslate"
      class="price"
    >
      {{ product?.price?.formattedValue }}
    </div>
  </ng-template>

  <ng-template
    [cxOutlet]="outlets.SUMMARY"
    [cxOutletContext]="{ product: product }"
  >
    <p [innerHTML]="product?.summary" class="summary"></p>
  </ng-template>

  <!-- @TODO: Temp. Comment out share link while not in use by CMS -->
  <!-- <ng-container *cxOutlet="outlets.SHARE">
        <div>
          <a href="#" class="share btn-link">
            {{ 'productSummary.share' | cxTranslate }}
          </a>
        </div>
      </ng-container> -->
</ng-container>
`
    }]
  }], () => [{
    type: CurrentProductService
  }], null);
})();
var ProductSummaryModule = class _ProductSummaryModule {
  static {
    this.ɵfac = function ProductSummaryModule_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _ProductSummaryModule)();
    };
  }
  static {
    this.ɵmod = ɵɵdefineNgModule({
      type: _ProductSummaryModule,
      declarations: [ProductSummaryComponent],
      imports: [CommonModule, OutletModule, I18nModule, PromotionsModule, FeaturesConfigModule],
      exports: [ProductSummaryComponent]
    });
  }
  static {
    this.ɵinj = ɵɵdefineInjector({
      providers: [provideDefaultConfig({
        cmsComponents: {
          ProductSummaryComponent: {
            component: ProductSummaryComponent
          }
        }
      })],
      imports: [CommonModule, OutletModule, I18nModule, PromotionsModule, FeaturesConfigModule]
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(ProductSummaryModule, [{
    type: NgModule,
    args: [{
      providers: [provideDefaultConfig({
        cmsComponents: {
          ProductSummaryComponent: {
            component: ProductSummaryComponent
          }
        }
      })],
      declarations: [ProductSummaryComponent],
      exports: [ProductSummaryComponent],
      imports: [CommonModule, OutletModule, I18nModule, PromotionsModule, FeaturesConfigModule]
    }]
  }], null, null);
})();
var ProductAttributesComponent = class _ProductAttributesComponent {
  constructor(currentProductService) {
    this.currentProductService = currentProductService;
    this.product$ = this.currentProductService.getProduct(ProductScope.ATTRIBUTES);
  }
  static {
    this.ɵfac = function ProductAttributesComponent_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _ProductAttributesComponent)(ɵɵdirectiveInject(CurrentProductService));
    };
  }
  static {
    this.ɵcmp = ɵɵdefineComponent({
      type: _ProductAttributesComponent,
      selectors: [["cx-product-attributes"]],
      standalone: false,
      decls: 2,
      vars: 3,
      consts: [[4, "ngIf"], [1, "container"], [4, "ngFor", "ngForOf"]],
      template: function ProductAttributesComponent_Template(rf, ctx) {
        if (rf & 1) {
          ɵɵtemplate(0, ProductAttributesComponent_ng_container_0_Template, 6, 4, "ng-container", 0);
          ɵɵpipe(1, "async");
        }
        if (rf & 2) {
          ɵɵproperty("ngIf", ɵɵpipeBind1(1, 1, ctx.product$));
        }
      },
      dependencies: [NgForOf, NgIf, AsyncPipe, TranslatePipe],
      encapsulation: 2,
      changeDetection: 0
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(ProductAttributesComponent, [{
    type: Component,
    args: [{
      selector: "cx-product-attributes",
      changeDetection: ChangeDetectionStrategy.OnPush,
      standalone: false,
      template: `<ng-container *ngIf="product$ | async as product">
  <div class="container">
    <h2>{{ 'productDetails.specification' | cxTranslate }}</h2>
    <table *ngFor="let class of product?.classifications">
      <th>
        <h2>{{ class.name }}</h2>
      </th>
      <tr *ngFor="let feature of class.features">
        <td>{{ feature.name }}</td>
        <td>
          <ul>
            <li *ngFor="let featureValue of feature?.featureValues">
              {{ featureValue?.value }}
              <span
                *ngIf="
                  feature.featureUnit &&
                  feature.featureUnit.symbol &&
                  feature.featureUnit.symbol.length > 0 &&
                  feature.featureUnit?.unitType !== '300'
                "
              >
                {{ feature.featureUnit?.symbol }}
              </span>
            </li>
          </ul>
        </td>
      </tr>
    </table>
  </div>
</ng-container>
`
    }]
  }], () => [{
    type: CurrentProductService
  }], null);
})();
var ProductAttributesModule = class _ProductAttributesModule {
  static {
    this.ɵfac = function ProductAttributesModule_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _ProductAttributesModule)();
    };
  }
  static {
    this.ɵmod = ɵɵdefineNgModule({
      type: _ProductAttributesModule,
      declarations: [ProductAttributesComponent],
      imports: [CommonModule, I18nModule],
      exports: [ProductAttributesComponent]
    });
  }
  static {
    this.ɵinj = ɵɵdefineInjector({
      providers: [provideDefaultConfig({
        cmsComponents: {
          ProductSpecsTabComponent: {
            component: ProductAttributesComponent
          }
        }
      })],
      imports: [CommonModule, I18nModule]
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(ProductAttributesModule, [{
    type: NgModule,
    args: [{
      imports: [CommonModule, I18nModule],
      providers: [provideDefaultConfig({
        cmsComponents: {
          ProductSpecsTabComponent: {
            component: ProductAttributesComponent
          }
        }
      })],
      declarations: [ProductAttributesComponent],
      exports: [ProductAttributesComponent]
    }]
  }], null, null);
})();
var ProductDetailsTabComponent = class _ProductDetailsTabComponent {
  constructor(currentProductService, componentData, cmsService) {
    this.currentProductService = currentProductService;
    this.componentData = componentData;
    this.cmsService = cmsService;
    this.children$ = this.componentData.data$.pipe(switchMap((data) => combineLatest((data?.children ?? "").split(" ").map((component) => this.cmsService.getComponentData(component).pipe(distinctUntilChanged(), map((child) => {
      if (!child) {
        return void 0;
      }
      if (!child.flexType) {
        child = __spreadProps(__spreadValues({}, child), {
          flexType: child.typeCode
        });
      }
      return child;
    }))))));
  }
  ngOnInit() {
    this.product$ = this.currentProductService.getProduct();
  }
  static {
    this.ɵfac = function ProductDetailsTabComponent_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _ProductDetailsTabComponent)(ɵɵdirectiveInject(CurrentProductService), ɵɵdirectiveInject(CmsComponentData), ɵɵdirectiveInject(CmsService));
    };
  }
  static {
    this.ɵcmp = ɵɵdefineComponent({
      type: _ProductDetailsTabComponent,
      selectors: [["cx-product-details-tab"]],
      standalone: false,
      decls: 4,
      vars: 6,
      consts: [[4, "ngIf"], [1, "container", 3, "innerHTML"], [4, "ngFor", "ngForOf"], [3, "cxOutlet", "cxOutletContext"], [1, "container"], [3, "cxComponentWrapper"]],
      template: function ProductDetailsTabComponent_Template(rf, ctx) {
        if (rf & 1) {
          ɵɵtemplate(0, ProductDetailsTabComponent_ng_container_0_Template, 2, 1, "ng-container", 0);
          ɵɵpipe(1, "async");
          ɵɵtemplate(2, ProductDetailsTabComponent_ng_container_2_Template, 2, 1, "ng-container", 0);
          ɵɵpipe(3, "async");
        }
        if (rf & 2) {
          ɵɵproperty("ngIf", ɵɵpipeBind1(1, 2, ctx.product$));
          ɵɵadvance(2);
          ɵɵproperty("ngIf", ɵɵpipeBind1(3, 4, ctx.children$));
        }
      },
      dependencies: [NgForOf, NgIf, ComponentWrapperDirective, OutletDirective, AsyncPipe],
      encapsulation: 2,
      changeDetection: 0
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(ProductDetailsTabComponent, [{
    type: Component,
    args: [{
      selector: "cx-product-details-tab",
      changeDetection: ChangeDetectionStrategy.OnPush,
      standalone: false,
      template: '<ng-container *ngIf="product$ | async as product">\n  <div class="container" [innerHTML]="product?.description"></div>\n</ng-container>\n<ng-container *ngIf="children$ | async as children">\n  <ng-container *ngFor="let child of children">\n    <ng-container *ngIf="child">\n      <ng-template [cxOutlet]="child.flexType" [cxOutletContext]="{}">\n        <div class="container">\n          <ng-container [cxComponentWrapper]="child"></ng-container>\n        </div>\n      </ng-template>\n    </ng-container>\n  </ng-container>\n</ng-container>\n'
    }]
  }], () => [{
    type: CurrentProductService
  }, {
    type: CmsComponentData
  }, {
    type: CmsService
  }], null);
})();
var ProductDetailsTabModule = class _ProductDetailsTabModule {
  static {
    this.ɵfac = function ProductDetailsTabModule_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _ProductDetailsTabModule)();
    };
  }
  static {
    this.ɵmod = ɵɵdefineNgModule({
      type: _ProductDetailsTabModule,
      declarations: [ProductDetailsTabComponent],
      imports: [CommonModule, PageComponentModule, OutletModule],
      exports: [ProductDetailsTabComponent]
    });
  }
  static {
    this.ɵinj = ɵɵdefineInjector({
      providers: [provideDefaultConfig({
        cmsComponents: {
          ProductDetailsTabComponent: {
            component: ProductDetailsTabComponent
          }
        }
      })],
      imports: [CommonModule, PageComponentModule, OutletModule]
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(ProductDetailsTabModule, [{
    type: NgModule,
    args: [{
      imports: [CommonModule, PageComponentModule, OutletModule],
      providers: [provideDefaultConfig({
        cmsComponents: {
          ProductDetailsTabComponent: {
            component: ProductDetailsTabComponent
          }
        }
      })],
      declarations: [ProductDetailsTabComponent],
      exports: [ProductDetailsTabComponent]
    }]
  }], null, null);
})();
var ProductReviewsComponent = class _ProductReviewsComponent {
  constructor(reviewService, currentProductService, fb, cd) {
    this.reviewService = reviewService;
    this.currentProductService = currentProductService;
    this.fb = fb;
    this.cd = cd;
    this.maxLengthReviewTitle = 255;
    this.maxLengthReviewComment = 2200;
    this.maxLengthReviewerName = 64;
    this.isWritingReview = false;
    this.initialMaxListItems = 5;
    this.featureConfigService = inject(FeatureConfigService);
    this.product$ = this.currentProductService.getProduct();
    this.reviews$ = this.product$.pipe(filter(isNotNullable), map((p) => p.code ?? ""), distinctUntilChanged(), switchMap((productCode) => this.reviewService.getByProductCode(productCode)), tap(() => {
      this.resetReviewForm();
      this.maxListItems = this.initialMaxListItems;
    }));
  }
  initiateWriteReview() {
    this.isWritingReview = true;
    this.cd.detectChanges();
    if (this.titleInput && this.titleInput.nativeElement) {
      this.titleInput.nativeElement.focus();
    }
  }
  cancelWriteReview() {
    this.isWritingReview = false;
    this.resetReviewForm();
    this.cd.detectChanges();
    if (this.writeReviewButton && this.writeReviewButton.nativeElement) {
      this.writeReviewButton.nativeElement.focus();
    }
  }
  setRating(rating) {
    this.reviewForm.controls.rating.setValue(rating);
  }
  submitReview(product) {
    if (this.reviewForm.valid) {
      this.addReview(product);
    } else {
      this.reviewForm.markAllAsTouched();
    }
  }
  addReview(product) {
    const reviewFormControls = this.reviewForm.controls;
    const review = {
      headline: reviewFormControls.title.value,
      comment: reviewFormControls.comment.value,
      rating: reviewFormControls.rating.value,
      alias: reviewFormControls.reviewerName.value
    };
    this.reviewService.add(product.code ?? "", review);
    this.isWritingReview = false;
    this.resetReviewForm();
    this.cd.detectChanges();
    if (this.writeReviewButton && this.writeReviewButton.nativeElement) {
      this.writeReviewButton.nativeElement.focus();
    }
  }
  resetReviewForm() {
    const isProductReviewCharactersLeftEnabled = this.featureConfigService.isEnabled("productReviewCharactersLeft");
    this.reviewForm = this.fb.group({
      title: ["", !isProductReviewCharactersLeftEnabled ? Validators.required : [Validators.required, Validators.maxLength(this.maxLengthReviewTitle)]],
      comment: ["", !isProductReviewCharactersLeftEnabled ? Validators.required : [Validators.required, Validators.maxLength(this.maxLengthReviewComment)]],
      rating: [null, Validators.required],
      reviewerName: !isProductReviewCharactersLeftEnabled ? "" : ["", Validators.maxLength(this.maxLengthReviewerName)]
    });
  }
  static {
    this.ɵfac = function ProductReviewsComponent_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _ProductReviewsComponent)(ɵɵdirectiveInject(ProductReviewService), ɵɵdirectiveInject(CurrentProductService), ɵɵdirectiveInject(UntypedFormBuilder), ɵɵdirectiveInject(ChangeDetectorRef));
    };
  }
  static {
    this.ɵcmp = ɵɵdefineComponent({
      type: _ProductReviewsComponent,
      selectors: [["cx-product-reviews"]],
      viewQuery: function ProductReviewsComponent_Query(rf, ctx) {
        if (rf & 1) {
          ɵɵviewQuery(_c70, 5);
          ɵɵviewQuery(_c71, 5);
        }
        if (rf & 2) {
          let _t;
          ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.titleInput = _t.first);
          ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.writeReviewButton = _t.first);
        }
      },
      inputs: {
        maxLengthReviewTitle: "maxLengthReviewTitle",
        maxLengthReviewComment: "maxLengthReviewComment",
        maxLengthReviewerName: "maxLengthReviewerName"
      },
      standalone: false,
      decls: 2,
      vars: 3,
      consts: [["writeReview", ""], ["writeReviewButton", ""], ["titleInput", ""], ["class", "container", 4, "ngIf"], [1, "container"], [4, "ngIf", "ngIfElse"], [1, "header"], [1, "btn", "btn-primary", 3, "click"], ["class", "rating", 3, "rating", 4, "ngIf"], ["class", "rating", 4, "ngIf"], [1, "rating", 3, "rating"], [1, "rating"], [4, "ngIf"], ["class", "review", "tabindex", "0", 4, "ngFor", "ngForOf"], ["tabindex", "0", 1, "review"], [1, "title"], [3, "rating"], [1, "name"], [1, "date"], ["class", "text", 4, "cxFeature"], ["class", "text", 3, "text", 4, "cxFeature"], [1, "text"], [1, "text", 3, "text"], ["aria-live", "polite", 1, "btn", "btn-primary", 3, "click"], [3, "ngSubmit", "formGroup"], [1, "form-group"], [1, "label-content"], ["required", "true", "type", "text", "class", "form-control", "formControlName", "title", 4, "cxFeature"], ["required", "true", "type", "text", "class", "form-control", "formControlName", "title", 3, "maxLength", 4, "cxFeature"], ["id", "titleError", 3, "translationParams", "control"], ["required", "true", "class", "form-control", "rows", "3", "formControlName", "comment", 4, "cxFeature"], ["required", "true", "class", "form-control", "rows", "3", "formControlName", "comment", 3, "placeholder", "maxLength", 4, "cxFeature"], ["id", "commentError", 3, "translationParams", "control"], ["required", "true", "type", "number", "formControlName", "rating", 1, "rating-input"], ["tabindex", "0", 3, "change", "disabled"], ["id", "ratingError", 3, "translationParams", "control"], ["type", "text", "class", "form-control", "formControlName", "reviewerName", 4, "cxFeature"], ["type", "text", "class", "form-control", "formControlName", "reviewerName", 3, "maxLength", 4, "cxFeature"], ["id", "titleError", 3, "translationParams", "control", 4, "cxFeature"], [3, "control", 4, "cxFeature"], ["class", "cx-customer-ticket-input-hint text-right", 4, "cxFeature"], [1, "form-group", "row", "justify-content-md-end"], [1, "col-12", "col-md-4"], ["type", "button", 1, "btn", "btn-block", "btn-secondary", 3, "click"], ["type", "submit", 1, "btn", "btn-block", "btn-primary"], ["required", "true", "type", "text", "formControlName", "title", 1, "form-control"], ["required", "true", "type", "text", "formControlName", "title", 1, "form-control", 3, "maxLength"], ["required", "true", "rows", "3", "formControlName", "comment", 1, "form-control"], ["required", "true", "rows", "3", "formControlName", "comment", 1, "form-control", 3, "placeholder", "maxLength"], ["type", "text", "formControlName", "reviewerName", 1, "form-control"], ["type", "text", "formControlName", "reviewerName", 1, "form-control", 3, "maxLength"], [3, "control"], [1, "cx-customer-ticket-input-hint", "text-right"]],
      template: function ProductReviewsComponent_Template(rf, ctx) {
        if (rf & 1) {
          ɵɵtemplate(0, ProductReviewsComponent_div_0_Template, 7, 6, "div", 3);
          ɵɵpipe(1, "async");
        }
        if (rf & 2) {
          ɵɵproperty("ngIf", ɵɵpipeBind1(1, 1, ctx.product$));
        }
      },
      dependencies: [NgForOf, NgIf, ɵNgNoValidate, DefaultValueAccessor, NumberValueAccessor, NgControlStatus, NgControlStatusGroup, RequiredValidator, FormGroupDirective, FormControlName, StarRatingComponent, FormErrorsComponent, FeatureDirective, FormRequiredAsterisksComponent, FormRequiredLegendComponent, ReadMoreComponent, AsyncPipe, SlicePipe, TranslatePipe, CxDatePipe],
      encapsulation: 2,
      changeDetection: 0
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(ProductReviewsComponent, [{
    type: Component,
    args: [{
      selector: "cx-product-reviews",
      changeDetection: ChangeDetectionStrategy.OnPush,
      standalone: false,
      template: `<div class="container" *ngIf="product$ | async as product">
  <h2>
    {{ 'productDetails.reviews' | cxTranslate }} ({{ product.numberOfReviews }})
  </h2>
  <ng-container *ngIf="!isWritingReview; else writeReview">
    <div class="header">
      <h3>{{ 'productReview.overallRating' | cxTranslate }}</h3>
      <button
        #writeReviewButton
        class="btn btn-primary"
        (click)="initiateWriteReview()"
      >
        {{ 'productReview.writeReview' | cxTranslate }}
      </button>
      <cx-star-rating
        *ngIf="product.averageRating"
        class="rating"
        [rating]="product.averageRating"
      ></cx-star-rating>
      <div class="rating" *ngIf="!product.averageRating">
        {{ 'productDetails.noReviews' | cxTranslate }}
      </div>
    </div>

    <ng-container *ngIf="!isWritingReview; else writeReview">
      <ng-container *ngIf="reviews$ | async as reviews">
        <div
          class="review"
          tabindex="0"
          *ngFor="let review of reviews | slice: 0 : maxListItems"
        >
          <div class="title">{{ review.headline }}</div>
          <cx-star-rating [rating]="review.rating ?? 0"></cx-star-rating>
          <div class="name">
            {{ review.alias ? review.alias : review.principal?.name }}
          </div>
          <div class="date">{{ review.date | cxDate }}</div>
          <div *cxFeature="'!readMoreDirective'" class="text">
            {{ review.comment }}
          </div>
          <cx-read-more
            *cxFeature="'readMoreDirective'"
            class="text"
            [text]="review.comment!"
          ></cx-read-more>
        </div>
        <div *ngIf="reviews.length > initialMaxListItems">
          <button
            aria-live="polite"
            class="btn btn-primary"
            (click)="
              maxListItems === initialMaxListItems
                ? (maxListItems = reviews.length)
                : (maxListItems = initialMaxListItems)
            "
          >
            {{
              (maxListItems === initialMaxListItems
                ? 'productReview.more'
                : 'productReview.less'
              ) | cxTranslate
            }}
          </button>
        </div>
      </ng-container>
    </ng-container>
  </ng-container>

  <ng-template #writeReview>
    <!-- TODO: (CXSPA-5953) Remove feature flags next major -->
    <cx-form-required-legend />
    <form (ngSubmit)="submitReview(product)" [formGroup]="reviewForm">
      <div class="form-group">
        <label>
          <span class="label-content"
            >{{ 'productReview.reviewTitle' | cxTranslate }}
            <cx-form-required-asterisks />
          </span>
          <input
            *cxFeature="'!productReviewCharactersLeft'"
            required="true"
            #titleInput
            type="text"
            class="form-control"
            formControlName="title"
            [attr.aria-invalid]="
              reviewForm.get('title')?.touched &&
              reviewForm.get('title')?.invalid
            "
            [attr.aria-errormessage]="'titleError'"
          />
          <input
            *cxFeature="'productReviewCharactersLeft'"
            required="true"
            #titleInput
            type="text"
            class="form-control"
            formControlName="title"
            [attr.aria-invalid]="
              reviewForm.get('title')?.touched &&
              reviewForm.get('title')?.invalid
            "
            [attr.aria-errormessage]="'titleError'"
            [maxLength]="maxLengthReviewTitle"
          />

          <cx-form-errors
            id="titleError"
            [translationParams]="{
              label: 'productReview.reviewTitle' | cxTranslate,
            }"
            [control]="reviewForm.get('title')"
          ></cx-form-errors>
        </label>
      </div>
      <div class="form-group">
        <label>
          <span class="label-content"
            >{{ 'productReview.writeYourComments' | cxTranslate }}
            <cx-form-required-asterisks />
          </span>
          <textarea
            *cxFeature="'!productReviewCharactersLeft'"
            required="true"
            class="form-control"
            rows="3"
            formControlName="comment"
            [attr.aria-invalid]="
              reviewForm.get('comment')?.touched &&
              reviewForm.get('comment')?.invalid
            "
            [attr.aria-errormessage]="'commentError'"
          ></textarea>
          <textarea
            *cxFeature="'productReviewCharactersLeft'"
            required="true"
            class="form-control"
            rows="3"
            [placeholder]="'productReview.commentPlaceholder' | cxTranslate"
            formControlName="comment"
            [attr.aria-invalid]="
              reviewForm.get('comment')?.touched &&
              reviewForm.get('comment')?.invalid
            "
            [attr.aria-errormessage]="'commentError'"
            [maxLength]="maxLengthReviewComment"
          ></textarea>

          <cx-form-errors
            id="commentError"
            [translationParams]="{
              label: 'productReview.writeYourComments' | cxTranslate,
            }"
            [control]="reviewForm.get('comment')"
          ></cx-form-errors>
        </label>
      </div>
      <div class="form-group">
        <label>
          <span class="label-content"
            >{{ 'productReview.rating' | cxTranslate }}
            <cx-form-required-asterisks />
          </span>
          <input
            required="true"
            type="number"
            formControlName="rating"
            class="rating-input"
            [attr.aria-invalid]="
              reviewForm.get('rating')?.touched &&
              reviewForm.get('rating')?.invalid
            "
            [attr.aria-errormessage]="'ratingError'"
          />
          <cx-star-rating
            tabindex="0"
            [attr.aria-label]="'productReview.ratingRequired' | cxTranslate"
            (change)="setRating($event)"
            [disabled]="false"
          ></cx-star-rating>

          <cx-form-errors
            id="ratingError"
            [translationParams]="{
              label: 'productReview.rating' | cxTranslate,
            }"
            [control]="reviewForm.get('rating')"
          ></cx-form-errors>
        </label>
      </div>
      <div class="form-group">
        <label>
          <span class="label-content"
            >{{ 'productReview.reviewerName' | cxTranslate }}
          </span>
          <input
            *cxFeature="'!productReviewCharactersLeft'"
            type="text"
            class="form-control"
            formControlName="reviewerName"
          />
          <input
            *cxFeature="'productReviewCharactersLeft'"
            type="text"
            class="form-control"
            formControlName="reviewerName"
            [maxLength]="maxLengthReviewerName"
          />
          <!-- TODO: (CXSPA-7315) Remove feature toggle in the next major -->
          <cx-form-errors
            *cxFeature="'formErrorsDescriptiveMessages'"
            id="titleError"
            [translationParams]="{
              label: 'productReview.reviewerName' | cxTranslate,
            }"
            [control]="reviewForm.get('reviewerName')"
          ></cx-form-errors>

          <cx-form-errors
            *cxFeature="'!formErrorsDescriptiveMessages'"
            [control]="reviewForm.get('reviewerName')"
          ></cx-form-errors>
          <p
            *cxFeature="'productReviewCharactersLeft'"
            class="cx-customer-ticket-input-hint text-right"
          >
            {{
              'productReview.charactersLeft'
                | cxTranslate
                  : {
                      count:
                        maxLengthReviewerName -
                        (reviewForm.get('reviewerName')?.value?.length ?? 0),
                    }
            }}
          </p>
        </label>
      </div>
      <div class="form-group row justify-content-md-end">
        <div class="col-12 col-md-4">
          <button
            type="button"
            class="btn btn-block btn-secondary"
            (click)="cancelWriteReview()"
          >
            {{ 'common.cancel' | cxTranslate }}
          </button>
        </div>
        <div class="col-12 col-md-4">
          <button type="submit" class="btn btn-block btn-primary">
            {{ 'common.submit' | cxTranslate }}
          </button>
        </div>
      </div>
    </form>
  </ng-template>
</div>
`
    }]
  }], () => [{
    type: ProductReviewService
  }, {
    type: CurrentProductService
  }, {
    type: UntypedFormBuilder
  }, {
    type: ChangeDetectorRef
  }], {
    titleInput: [{
      type: ViewChild,
      args: ["titleInput", {
        static: false
      }]
    }],
    writeReviewButton: [{
      type: ViewChild,
      args: ["writeReviewButton", {
        static: false
      }]
    }],
    maxLengthReviewTitle: [{
      type: Input
    }],
    maxLengthReviewComment: [{
      type: Input
    }],
    maxLengthReviewerName: [{
      type: Input
    }]
  });
})();
var ProductReviewsModule = class _ProductReviewsModule {
  static {
    this.ɵfac = function ProductReviewsModule_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _ProductReviewsModule)();
    };
  }
  static {
    this.ɵmod = ɵɵdefineNgModule({
      type: _ProductReviewsModule,
      declarations: [ProductReviewsComponent],
      imports: [CommonModule, ReactiveFormsModule, FormsModule, I18nModule, StarRatingModule, FormErrorsModule, FeaturesConfigModule, FormRequiredAsterisksComponent, FormRequiredLegendComponent, ReadMoreComponent],
      exports: [ProductReviewsComponent]
    });
  }
  static {
    this.ɵinj = ɵɵdefineInjector({
      providers: [provideDefaultConfig({
        cmsComponents: {
          ProductReviewsTabComponent: {
            component: ProductReviewsComponent
          }
        }
      })],
      imports: [CommonModule, ReactiveFormsModule, FormsModule, I18nModule, StarRatingModule, FormErrorsModule, FeaturesConfigModule, FormRequiredAsterisksComponent, FormRequiredLegendComponent, ReadMoreComponent]
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(ProductReviewsModule, [{
    type: NgModule,
    args: [{
      imports: [CommonModule, ReactiveFormsModule, FormsModule, I18nModule, StarRatingModule, FormErrorsModule, FeaturesConfigModule, FormRequiredAsterisksComponent, FormRequiredLegendComponent, ReadMoreComponent],
      providers: [provideDefaultConfig({
        cmsComponents: {
          ProductReviewsTabComponent: {
            component: ProductReviewsComponent
          }
        }
      })],
      declarations: [ProductReviewsComponent],
      exports: [ProductReviewsComponent]
    }]
  }], null, null);
})();
var ProductTabsModule = class _ProductTabsModule {
  static {
    this.ɵfac = function ProductTabsModule_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _ProductTabsModule)();
    };
  }
  static {
    this.ɵmod = ɵɵdefineNgModule({
      type: _ProductTabsModule,
      imports: [ProductAttributesModule, ProductDetailsTabModule, ProductReviewsModule]
    });
  }
  static {
    this.ɵinj = ɵɵdefineInjector({
      imports: [ProductAttributesModule, ProductDetailsTabModule, ProductReviewsModule]
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(ProductTabsModule, [{
    type: NgModule,
    args: [{
      imports: [ProductAttributesModule, ProductDetailsTabModule, ProductReviewsModule]
    }]
  }], null, null);
})();
var StockNotificationDialogComponent = class _StockNotificationDialogComponent {
  handleClick(event) {
    if (event.target.tagName === this.el.nativeElement.tagName) {
      this.close("Cross click");
    }
  }
  constructor(interestsService, launchDialogService, el) {
    this.interestsService = interestsService;
    this.launchDialogService = launchDialogService;
    this.el = el;
    this.subscription = new Subscription();
    this.enabledPrefs = [];
    this.focusConfig = {
      trap: true,
      block: true,
      autofocus: "button",
      focusOnEscape: true
    };
  }
  close(reason) {
    this.launchDialogService.closeDialog(reason);
  }
  ngOnInit() {
    this.subscription.add(this.launchDialogService.data$.subscribe((data) => {
      if (data) {
        this.init(data.subscribeSuccess$, data.enabledPrefs);
      }
    }));
  }
  init(subscribeSuccess$, enabledPrefs) {
    this.subscribeSuccess$ = subscribeSuccess$;
    this.enabledPrefs = enabledPrefs;
  }
  ngOnDestroy() {
    if (this.subscribeSuccess$) {
      this.subscribeSuccess$.subscribe((success) => {
        if (success) {
          this.interestsService.resetAddInterestState();
        }
      }).unsubscribe();
    }
    this.subscription.unsubscribe();
  }
  static {
    this.ɵfac = function StockNotificationDialogComponent_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _StockNotificationDialogComponent)(ɵɵdirectiveInject(UserInterestsService), ɵɵdirectiveInject(LaunchDialogService), ɵɵdirectiveInject(ElementRef));
    };
  }
  static {
    this.ɵcmp = ɵɵdefineComponent({
      type: _StockNotificationDialogComponent,
      selectors: [["cx-stock-notification-dialog"]],
      hostBindings: function StockNotificationDialogComponent_HostBindings(rf, ctx) {
        if (rf & 1) {
          ɵɵlistener("click", function StockNotificationDialogComponent_click_HostBindingHandler($event) {
            return ctx.handleClick($event);
          });
        }
      },
      standalone: false,
      decls: 14,
      vars: 11,
      consts: [["loading", ""], ["role", "dialog", "aria-labelledby", "dialogTitle", 1, "modal-dialog", "modal-dialog-centered", "modal-lg", 3, "esc", "cxFocus"], [1, "modal-content"], [1, "cx-dialog-header", "cx-modal-header"], ["id", "dialogTitle", 1, "cx-dialog-title", "modal-title"], ["type", "button", "tabindex", "-1", 1, "close", 3, "click"], ["aria-hidden", "true"], [4, "ngIf", "ngIfElse"], [1, "cx-dialog-body", "modal-body"], [1, "cx-stock-notification-content"], [1, "cx-stock-notification-text"], ["class", "channels", 4, "ngFor", "ngForOf"], [1, "link-prefs", "cx-stock-notification-link", 3, "click", "routerLink"], [1, "link-interests", "cx-stock-notification-link", 3, "click", "routerLink"], [1, "cx-modal-footer"], [1, "row"], [1, "cx-dialog-actions", "col-sm-12", "col-md-4", "offset-md-8", "col-lg-3", "offset-lg-9"], ["type", "button", 1, "btn", "btn-primary", "btn-block", "btn-ok", 3, "click"], [1, "channels"], [4, "ngIf"], [1, "cx-dialog-row"], [1, "col-sm-12"]],
      template: function StockNotificationDialogComponent_Template(rf, ctx) {
        if (rf & 1) {
          const _r1 = ɵɵgetCurrentView();
          ɵɵelementStart(0, "div", 1);
          ɵɵlistener("esc", function StockNotificationDialogComponent_Template_div_esc_0_listener() {
            ɵɵrestoreView(_r1);
            return ɵɵresetView(ctx.close("Escape clicked"));
          });
          ɵɵelementStart(1, "div", 2)(2, "div", 3)(3, "h3", 4);
          ɵɵtext(4);
          ɵɵpipe(5, "cxTranslate");
          ɵɵelementEnd();
          ɵɵelementStart(6, "button", 5);
          ɵɵpipe(7, "cxTranslate");
          ɵɵlistener("click", function StockNotificationDialogComponent_Template_button_click_6_listener() {
            ɵɵrestoreView(_r1);
            return ɵɵresetView(ctx.close("Button clicked"));
          });
          ɵɵelementStart(8, "span", 6);
          ɵɵtext(9, "×");
          ɵɵelementEnd()()();
          ɵɵtemplate(10, StockNotificationDialogComponent_ng_container_10_Template, 34, 36, "ng-container", 7);
          ɵɵpipe(11, "async");
          ɵɵelementEnd();
          ɵɵtemplate(12, StockNotificationDialogComponent_ng_template_12_Template, 7, 3, "ng-template", null, 0, ɵɵtemplateRefExtractor);
          ɵɵelementEnd();
        }
        if (rf & 2) {
          const loading_r5 = ɵɵreference(13);
          ɵɵproperty("cxFocus", ctx.focusConfig);
          ɵɵadvance(4);
          ɵɵtextInterpolate1(" ", ɵɵpipeBind1(5, 5, "stockNotification.subscriptionDialog.header"), " ");
          ɵɵadvance(2);
          ɵɵattribute("aria-label", ɵɵpipeBind1(7, 7, "common.close"));
          ɵɵadvance(4);
          ɵɵproperty("ngIf", ɵɵpipeBind1(11, 9, ctx.subscribeSuccess$))("ngIfElse", loading_r5);
        }
      },
      dependencies: [NgForOf, NgIf, RouterLink, SpinnerComponent, FocusDirective, AsyncPipe, TranslatePipe, UrlPipe],
      encapsulation: 2,
      changeDetection: 0
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(StockNotificationDialogComponent, [{
    type: Component,
    args: [{
      selector: "cx-stock-notification-dialog",
      changeDetection: ChangeDetectionStrategy.OnPush,
      standalone: false,
      template: `<div
  class="modal-dialog modal-dialog-centered modal-lg"
  role="dialog"
  [cxFocus]="focusConfig"
  (esc)="close('Escape clicked')"
  aria-labelledby="dialogTitle"
>
  <div class="modal-content">
    <div class="cx-dialog-header cx-modal-header">
      <h3 id="dialogTitle" class="cx-dialog-title modal-title">
        {{ 'stockNotification.subscriptionDialog.header' | cxTranslate }}
      </h3>
      <button
        type="button"
        [attr.aria-label]="'common.close' | cxTranslate"
        class="close"
        tabindex="-1"
        (click)="close('Button clicked')"
      >
        <span aria-hidden="true">&times;</span>
      </button>
    </div>

    <ng-container *ngIf="subscribeSuccess$ | async; else loading">
      <div class="cx-dialog-body modal-body">
        <div class="cx-stock-notification-content">
          <p class="cx-stock-notification-text">
            {{
              'stockNotification.subscriptionDialog.notifiedPrefix'
                | cxTranslate
            }}
          </p>
          <p *ngFor="let preference of enabledPrefs" class="channels">
            <span>{{ preference.channel }}</span
            ><span *ngIf="preference.value">{{ ': ' + preference.value }}</span>
          </p>
          <p class="cx-stock-notification-text">
            {{
              'stockNotification.subscriptionDialog.notifiedSuffix'
                | cxTranslate
            }}
          </p>
          <p class="cx-stock-notification-text">
            {{
              'stockNotification.subscriptionDialog.manageChannelsPrefix'
                | cxTranslate
            }}
            <a
              (click)="close('Link clicked')"
              [routerLink]="{ cxRoute: 'notificationPreference' } | cxUrl"
              class="link-prefs cx-stock-notification-link"
            >
              {{
                'stockNotification.subscriptionDialog.manageChannelsLink'
                  | cxTranslate
              }}</a
            >
            {{
              'stockNotification.subscriptionDialog.manageChannelsSuffix'
                | cxTranslate
            }}
          </p>

          <p class="cx-stock-notification-text">
            {{
              'stockNotification.subscriptionDialog.manageSubscriptionsPrefix'
                | cxTranslate
            }}
            <a
              (click)="close('Link clicked')"
              [routerLink]="{ cxRoute: 'myInterests' } | cxUrl"
              class="link-interests cx-stock-notification-link"
            >
              {{
                'stockNotification.subscriptionDialog.manageSubscriptionsLink'
                  | cxTranslate
              }}</a
            >
            {{
              'stockNotification.subscriptionDialog.manageSubscriptionsSuffix'
                | cxTranslate
            }}
          </p>
        </div>
      </div>
      <div class="cx-modal-footer">
        <div class="row">
          <div
            class="cx-dialog-actions col-sm-12 col-md-4 offset-md-8 col-lg-3 offset-lg-9"
          >
            <button
              class="btn btn-primary btn-block btn-ok"
              type="button"
              (click)="close('Button clicked')"
            >
              {{ 'stockNotification.subscriptionDialog.okBtn' | cxTranslate }}
            </button>
          </div>
        </div>
      </div>
    </ng-container>
  </div>

  <ng-template #loading>
    <div class="cx-dialog-body modal-body">
      <p>
        {{ 'stockNotification.subscriptionDialog.subscribing' | cxTranslate }}
      </p>
      <div class="cx-dialog-row">
        <div class="col-sm-12">
          <cx-spinner></cx-spinner>
        </div>
      </div>
    </div>
  </ng-template>
</div>
`
    }]
  }], () => [{
    type: UserInterestsService
  }, {
    type: LaunchDialogService
  }, {
    type: ElementRef
  }], {
    handleClick: [{
      type: HostListener,
      args: ["click", ["$event"]]
    }]
  });
})();
var StockNotificationComponent = class _StockNotificationComponent {
  constructor(currentProductService, globalMessageService, translationService, interestsService, notificationPrefService, userIdService, launchDialogService, vcr) {
    this.currentProductService = currentProductService;
    this.globalMessageService = globalMessageService;
    this.translationService = translationService;
    this.interestsService = interestsService;
    this.notificationPrefService = notificationPrefService;
    this.userIdService = userIdService;
    this.launchDialogService = launchDialogService;
    this.vcr = vcr;
    this.anonymous = true;
    this.enabledPrefs = [];
    this.subscriptions = new Subscription();
  }
  ngOnInit() {
    this.outOfStock$ = combineLatest([this.currentProductService.getProduct().pipe(filter(isNotNullable)), this.userIdService.getUserId()]).pipe(tap(([product, userId]) => {
      this.productCode = product.code ?? "";
      if (userId !== OCC_USER_ID_ANONYMOUS) {
        this.anonymous = false;
        this.notificationPrefService.loadPreferences();
        this.interestsService.loadProductInterests(void 0, void 0, void 0, product.code, NotificationType.BACK_IN_STOCK);
      }
    }), map(([product]) => !!product.stock && product.stock.stockLevelStatus === "outOfStock"));
    this.hasProductInterests$ = this.interestsService.getProductInterests().pipe(map((interests) => !!interests.results && interests.results.length === 1));
    this.subscribeSuccess$ = this.interestsService.getAddProductInterestSuccess();
    this.isRemoveInterestLoading$ = this.interestsService.getRemoveProdutInterestLoading();
    this.prefsEnabled$ = this.notificationPrefService.getEnabledPreferences().pipe(tap((prefs) => this.enabledPrefs = prefs), map((prefs) => prefs.length > 0));
    this.subscriptions.add(this.interestsService.getAddProductInterestError().subscribe((error) => {
      if (error) {
        this.onInterestAddingError();
      }
    }));
    this.subscriptions.add(this.interestsService.getRemoveProdutInterestSuccess().subscribe((success) => {
      if (success) {
        this.onInterestRemovingSuccess();
      }
    }));
  }
  subscribe() {
    this.openDialog();
    this.interestsService.addProductInterest(this.productCode, NotificationType.BACK_IN_STOCK);
  }
  unsubscribe() {
    this.interestsService.removeProdutInterest({
      product: {
        code: this.productCode
      },
      productInterestEntry: [{
        interestType: NotificationType.BACK_IN_STOCK
      }]
    }, true);
  }
  onInterestRemovingSuccess() {
    this.subscriptions.add(this.translationService.translate("stockNotification.unsubscribeSuccess").pipe(first()).subscribe((text) => this.globalMessageService.add(text, GlobalMessageType.MSG_TYPE_INFO)));
    this.interestsService.resetRemoveInterestState();
  }
  onInterestAddingError() {
    this.launchDialogService.closeDialog("Interests error");
    this.interestsService.resetAddInterestState();
  }
  openDialog() {
    const modalInstanceData = {
      subscribeSuccess$: this.subscribeSuccess$,
      enabledPrefs: this.enabledPrefs
    };
    const dialog = this.launchDialogService.openDialog(LAUNCH_CALLER.STOCK_NOTIFICATION, this.element, this.vcr, modalInstanceData);
    if (dialog) {
      dialog.pipe(take(1)).subscribe();
    }
  }
  ngOnDestroy() {
    this.subscriptions.unsubscribe();
    this.interestsService.clearProductInterests();
    this.notificationPrefService.clearPreferences();
  }
  static {
    this.ɵfac = function StockNotificationComponent_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _StockNotificationComponent)(ɵɵdirectiveInject(CurrentProductService), ɵɵdirectiveInject(GlobalMessageService), ɵɵdirectiveInject(TranslationService), ɵɵdirectiveInject(UserInterestsService), ɵɵdirectiveInject(UserNotificationPreferenceService), ɵɵdirectiveInject(UserIdService), ɵɵdirectiveInject(LaunchDialogService), ɵɵdirectiveInject(ViewContainerRef));
    };
  }
  static {
    this.ɵcmp = ɵɵdefineComponent({
      type: _StockNotificationComponent,
      selectors: [["cx-stock-notification"]],
      viewQuery: function StockNotificationComponent_Query(rf, ctx) {
        if (rf & 1) {
          ɵɵviewQuery(_c43, 5);
        }
        if (rf & 2) {
          let _t;
          ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.element = _t.first);
        }
      },
      standalone: false,
      decls: 8,
      vars: 3,
      consts: [["disableNotify", ""], ["stopNotify", ""], ["loading", ""], ["loggedIn", ""], [4, "ngIf"], [4, "ngIf", "ngIfElse"], [1, "stock-notification-notes"], ["type", "button", 1, "btn", "btn-primary", "btn-block", "btn-notify", 3, "click"], ["id", "outOfStockMessage", 1, "stock-notification-notes"], ["type", "button", "aria-describedby", "outOfStockMessage", 1, "btn", "btn-primary", "btn-block", "btn-notify", 3, "click"], [3, "routerLink"], ["type", "button", 1, "btn", "btn-primary", "btn-block", "btn-stop-notify", 3, "click"], [1, "cx-dialog-body", "modal-body"], [1, "cx-dialog-row"], [1, "col-sm-12"]],
      template: function StockNotificationComponent_Template(rf, ctx) {
        if (rf & 1) {
          ɵɵtemplate(0, StockNotificationComponent_ng_container_0_Template, 3, 4, "ng-container", 4);
          ɵɵpipe(1, "async");
          ɵɵtemplate(2, StockNotificationComponent_ng_template_2_Template, 8, 6, "ng-template", null, 0, ɵɵtemplateRefExtractor)(4, StockNotificationComponent_ng_template_4_Template, 2, 4, "ng-template", null, 1, ɵɵtemplateRefExtractor)(6, StockNotificationComponent_ng_template_6_Template, 4, 0, "ng-template", null, 2, ɵɵtemplateRefExtractor);
        }
        if (rf & 2) {
          ɵɵproperty("ngIf", ɵɵpipeBind1(1, 1, ctx.outOfStock$));
        }
      },
      dependencies: [NgIf, RouterLink, SpinnerComponent, AsyncPipe, TranslatePipe, UrlPipe],
      encapsulation: 2,
      changeDetection: 0
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(StockNotificationComponent, [{
    type: Component,
    args: [{
      selector: "cx-stock-notification",
      changeDetection: ChangeDetectionStrategy.OnPush,
      standalone: false,
      template: `<ng-container *ngIf="outOfStock$ | async">
  <ng-container *ngIf="!(hasProductInterests$ | async); else stopNotify">
    <ng-container *ngIf="prefsEnabled$ | async; else disableNotify">
      <div class="stock-notification-notes">
        <p>{{ 'stockNotification.getNotified' | cxTranslate }}</p>
      </div>
      <button
        class="btn btn-primary btn-block btn-notify"
        type="button"
        (click)="subscribe()"
      >
        {{ 'stockNotification.notifyMe' | cxTranslate }}
      </button>
    </ng-container>
  </ng-container>
</ng-container>

<ng-template #disableNotify>
  <div class="stock-notification-notes" id="outOfStockMessage">
    <p>
      <ng-container *ngIf="anonymous; else loggedIn">
        <a [routerLink]="{ cxRoute: 'login' } | cxUrl">
          {{ 'miniLogin.signInRegister' | cxTranslate }}</a
        >
        {{ 'stockNotification.getNotifySuffix' | cxTranslate }}<br />
      </ng-container>
      <ng-template #loggedIn>
        {{ 'stockNotification.getNotify' | cxTranslate }}<br />
        {{ 'stockNotification.activateChannelsPrefix' | cxTranslate }}
        <a [routerLink]="{ cxRoute: 'notificationPreference' } | cxUrl">{{
          'stockNotification.channelsLink' | cxTranslate
        }}</a
        >{{ 'stockNotification.activateChannelsSuffix' | cxTranslate }}
      </ng-template>
    </p>
  </div>
  <button
    class="btn btn-primary btn-block btn-notify"
    type="button"
    [attr.disabled]="anonymous ? true : null"
    aria-describedby="outOfStockMessage"
    (click)="subscribe()"
  >
    {{ 'stockNotification.notifyMe' | cxTranslate }}
  </button>
</ng-template>

<ng-template #stopNotify>
  <ng-container *ngIf="!(isRemoveInterestLoading$ | async); else loading">
    <div class="stock-notification-notes">
      <p>{{ 'stockNotification.notified' | cxTranslate }}</p>
    </div>
    <button
      class="btn btn-primary btn-block btn-stop-notify"
      type="button"
      (click)="unsubscribe()"
    >
      {{ 'stockNotification.stopNotify' | cxTranslate }}
    </button>
  </ng-container>
</ng-template>

<ng-template #loading>
  <div class="cx-dialog-body modal-body">
    <div class="cx-dialog-row">
      <div class="col-sm-12">
        <cx-spinner></cx-spinner>
      </div>
    </div>
  </div>
</ng-template>
`
    }]
  }], () => [{
    type: CurrentProductService
  }, {
    type: GlobalMessageService
  }, {
    type: TranslationService
  }, {
    type: UserInterestsService
  }, {
    type: UserNotificationPreferenceService
  }, {
    type: UserIdService
  }, {
    type: LaunchDialogService
  }, {
    type: ViewContainerRef
  }], {
    element: [{
      type: ViewChild,
      args: ["element"]
    }]
  });
})();
var defaultStockNotificationLayoutConfig = {
  launch: {
    STOCK_NOTIFICATION: {
      inlineRoot: true,
      component: StockNotificationDialogComponent,
      dialogType: DIALOG_TYPE.DIALOG
    }
  }
};
var StockNotificationModule = class _StockNotificationModule {
  static {
    this.ɵfac = function StockNotificationModule_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _StockNotificationModule)();
    };
  }
  static {
    this.ɵmod = ɵɵdefineNgModule({
      type: _StockNotificationModule,
      declarations: [StockNotificationComponent, StockNotificationDialogComponent],
      imports: [CommonModule, RouterModule, I18nModule, SpinnerModule, UrlModule, KeyboardFocusModule, FeaturesConfigModule],
      exports: [StockNotificationComponent, StockNotificationDialogComponent]
    });
  }
  static {
    this.ɵinj = ɵɵdefineInjector({
      providers: [provideDefaultConfig(defaultStockNotificationLayoutConfig), provideDefaultConfig({
        cmsComponents: {
          StockNotificationComponent: {
            component: StockNotificationComponent
          }
        }
      }), FeaturesConfigModule],
      imports: [CommonModule, RouterModule, I18nModule, SpinnerModule, UrlModule, KeyboardFocusModule, FeaturesConfigModule]
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(StockNotificationModule, [{
    type: NgModule,
    args: [{
      declarations: [StockNotificationComponent, StockNotificationDialogComponent],
      imports: [CommonModule, RouterModule, I18nModule, SpinnerModule, UrlModule, KeyboardFocusModule, FeaturesConfigModule],
      providers: [provideDefaultConfig(defaultStockNotificationLayoutConfig), provideDefaultConfig({
        cmsComponents: {
          StockNotificationComponent: {
            component: StockNotificationComponent
          }
        }
      }), FeaturesConfigModule],
      exports: [StockNotificationComponent, StockNotificationDialogComponent]
    }]
  }], null, null);
})();
var LoginGuard = class _LoginGuard {
  constructor(authService, authConfigService, cmsPageGuard) {
    this.authService = authService;
    this.authConfigService = authConfigService;
    this.cmsPageGuard = cmsPageGuard;
  }
  canActivate(route, state) {
    return this.shouldRenderCMSPage().pipe(take(1), switchMap((shouldRenderCMSPage) => {
      if (shouldRenderCMSPage) {
        return this.cmsPageGuard.canActivate(route, state);
      } else {
        const redirected = this.authService.loginWithRedirect();
        if (!redirected) {
          return of(false);
        }
        return EMPTY;
      }
    }));
  }
  shouldRenderCMSPage() {
    return this.authService.isUserLoggedIn().pipe(take(1), map((isUserLoggedIn) => {
      return this.authConfigService.getOAuthFlow() === OAuthFlow.ResourceOwnerPasswordFlow || isUserLoggedIn;
    }));
  }
  static {
    this.ɵfac = function LoginGuard_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _LoginGuard)(ɵɵinject(AuthService), ɵɵinject(AuthConfigService), ɵɵinject(CmsPageGuard));
    };
  }
  static {
    this.ɵprov = ɵɵdefineInjectable({
      token: _LoginGuard,
      factory: _LoginGuard.ɵfac,
      providedIn: "root"
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(LoginGuard, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], () => [{
    type: AuthService
  }, {
    type: AuthConfigService
  }, {
    type: CmsPageGuard
  }], null);
})();
var LoginRouteModule = class _LoginRouteModule {
  static {
    this.ɵfac = function LoginRouteModule_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _LoginRouteModule)();
    };
  }
  static {
    this.ɵmod = ɵɵdefineNgModule({
      type: _LoginRouteModule,
      imports: [PageLayoutModule, RouterModule]
    });
  }
  static {
    this.ɵinj = ɵɵdefineInjector({
      imports: [PageLayoutModule, RouterModule.forChild([{
        // @ts-ignore
        path: null,
        canActivate: [LoginGuard],
        component: PageLayoutComponent,
        data: {
          cxRoute: "login"
        }
      }])]
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(LoginRouteModule, [{
    type: NgModule,
    args: [{
      imports: [PageLayoutModule, RouterModule.forChild([{
        // @ts-ignore
        path: null,
        canActivate: [LoginGuard],
        component: PageLayoutComponent,
        data: {
          cxRoute: "login"
        }
      }])]
    }]
  }], null, null);
})();
var LogoutGuard = class _LogoutGuard {
  constructor(auth, cms, semanticPathService, protectedRoutes, router) {
    this.auth = auth;
    this.cms = cms;
    this.semanticPathService = semanticPathService;
    this.protectedRoutes = protectedRoutes;
    this.router = router;
  }
  canActivate() {
    return from(this.logout()).pipe(switchMap(() => {
      return this.cms.hasPage({
        id: this.semanticPathService.get("logout") ?? "",
        type: PageType.CONTENT_PAGE
      }).pipe(map((hasPage) => {
        if (!hasPage) {
          return this.getRedirectUrl();
        }
        return hasPage;
      }));
    }));
  }
  logout() {
    return this.auth.coreLogout();
  }
  /**
   * Whenever there is no specific "logout" page configured in the CMS,
   * we redirect after the user is logged out.
   *
   * The user gets redirected to the homepage, unless the homepage is protected
   * (in case of a closed shop). We'll redirect to the login page instead.
   */
  getRedirectUrl() {
    const cxRoute = this.protectedRoutes.shouldProtect ? "login" : "home";
    return this.router.parseUrl(this.semanticPathService.get(cxRoute) ?? "");
  }
  static {
    this.ɵfac = function LogoutGuard_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _LogoutGuard)(ɵɵinject(AuthService), ɵɵinject(CmsService), ɵɵinject(SemanticPathService), ɵɵinject(ProtectedRoutesService), ɵɵinject(Router));
    };
  }
  static {
    this.ɵprov = ɵɵdefineInjectable({
      token: _LogoutGuard,
      factory: _LogoutGuard.ɵfac,
      providedIn: "root"
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(LogoutGuard, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], () => [{
    type: AuthService
  }, {
    type: CmsService
  }, {
    type: SemanticPathService
  }, {
    type: ProtectedRoutesService
  }, {
    type: Router
  }], null);
})();
var LogoutModule = class _LogoutModule {
  static {
    this.ɵfac = function LogoutModule_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _LogoutModule)();
    };
  }
  static {
    this.ɵmod = ɵɵdefineNgModule({
      type: _LogoutModule,
      imports: [PageLayoutModule, RouterModule]
    });
  }
  static {
    this.ɵinj = ɵɵdefineInjector({
      imports: [PageLayoutModule, RouterModule.forChild([{
        // @ts-ignore
        path: null,
        canActivate: [LogoutGuard, CmsPageGuard],
        component: PageLayoutComponent,
        data: {
          cxRoute: "logout"
        }
      }])]
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(LogoutModule, [{
    type: NgModule,
    args: [{
      imports: [PageLayoutModule, RouterModule.forChild([{
        // @ts-ignore
        path: null,
        canActivate: [LogoutGuard, CmsPageGuard],
        component: PageLayoutComponent,
        data: {
          cxRoute: "logout"
        }
      }])]
    }]
  }], null, null);
})();
var UserComponentModule = class _UserComponentModule {
  static {
    this.ɵfac = function UserComponentModule_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _UserComponentModule)();
    };
  }
  static {
    this.ɵmod = ɵɵdefineNgModule({
      type: _UserComponentModule,
      imports: [LogoutModule, LoginRouteModule, ReactiveFormsModule, RouterModule, UrlModule]
    });
  }
  static {
    this.ɵinj = ɵɵdefineInjector({
      imports: [LogoutModule, LoginRouteModule, ReactiveFormsModule, RouterModule, UrlModule]
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(UserComponentModule, [{
    type: NgModule,
    args: [{
      imports: [LogoutModule, LoginRouteModule, ReactiveFormsModule, RouterModule, UrlModule]
    }]
  }], null, null);
})();
var IS_GUEST_USER_CHECKOUT_KEY = "isGuestUserCheckout";
function getProductDetailsUrlMatcherFactory(service, defaultMatcherFactory) {
  const factory = (route) => {
    const defaultMatcher = defaultMatcherFactory(route);
    const suffixPDPMatcher = getSuffixUrlMatcher({
      marker: "p",
      paramName: "productCode"
    });
    return service.getCombined([defaultMatcher, suffixPDPMatcher]);
  };
  return factory;
}
var PRODUCT_DETAILS_URL_MATCHER = new InjectionToken("PRODUCT_DETAILS_URL_MATCHER", {
  providedIn: "root",
  factory: () => getProductDetailsUrlMatcherFactory(inject(UrlMatcherService), inject(DEFAULT_URL_MATCHER))
});
var ProductDetailsPageModule = class _ProductDetailsPageModule {
  static {
    this.ɵfac = function ProductDetailsPageModule_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _ProductDetailsPageModule)();
    };
  }
  static {
    this.ɵmod = ɵɵdefineNgModule({
      type: _ProductDetailsPageModule,
      imports: [RouterModule]
    });
  }
  static {
    this.ɵinj = ɵɵdefineInjector({
      providers: [provideDefaultConfig({
        routing: {
          routes: {
            product: {
              matchers: [PRODUCT_DETAILS_URL_MATCHER]
            }
          }
        }
      })],
      imports: [RouterModule.forChild([{
        // @ts-ignore
        path: null,
        canActivate: [CmsPageGuard],
        component: PageLayoutComponent,
        data: {
          cxRoute: "product"
        }
      }])]
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(ProductDetailsPageModule, [{
    type: NgModule,
    args: [{
      imports: [RouterModule.forChild([{
        // @ts-ignore
        path: null,
        canActivate: [CmsPageGuard],
        component: PageLayoutComponent,
        data: {
          cxRoute: "product"
        }
      }])],
      providers: [provideDefaultConfig({
        routing: {
          routes: {
            product: {
              matchers: [PRODUCT_DETAILS_URL_MATCHER]
            }
          }
        }
      })]
    }]
  }], null, null);
})();
function getProductListingUrlMatcherFactory(service, defaultMatcherFactory) {
  const factory = (route) => {
    const defaultMatcher = defaultMatcherFactory(route);
    const suffixPLPMatcher = getSuffixUrlMatcher({
      marker: "c",
      paramName: "categoryCode"
    });
    return service.getCombined([defaultMatcher, suffixPLPMatcher]);
  };
  return factory;
}
var PRODUCT_LISTING_URL_MATCHER = new InjectionToken("PRODUCT_LISTING_URL_MATCHER", {
  providedIn: "root",
  factory: () => getProductListingUrlMatcherFactory(inject(UrlMatcherService), inject(DEFAULT_URL_MATCHER))
});
var ProductListingPageModule = class _ProductListingPageModule {
  static {
    this.ɵfac = function ProductListingPageModule_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _ProductListingPageModule)();
    };
  }
  static {
    this.ɵmod = ɵɵdefineNgModule({
      type: _ProductListingPageModule,
      imports: [RouterModule]
    });
  }
  static {
    this.ɵinj = ɵɵdefineInjector({
      providers: [provideDefaultConfig({
        routing: {
          routes: {
            category: {
              matchers: [PRODUCT_LISTING_URL_MATCHER]
            }
          }
        }
      })],
      imports: [RouterModule.forChild([{
        // @ts-ignore
        path: null,
        canActivate: [CmsPageGuard],
        component: PageLayoutComponent,
        data: {
          pageLabel: "search",
          cxRoute: "search"
        }
      }, {
        // @ts-ignore
        path: null,
        canActivate: [CmsPageGuard],
        component: PageLayoutComponent,
        data: {
          cxRoute: "brand"
        }
      }, {
        // The 'category' route  may include a greedy suffix url matcher '**/c/:categoryCode'
        // So not to shadow the specific 'brand' route, the 'category' is the last route in the sequence.
        // @ts-ignore
        path: null,
        canActivate: [CmsPageGuard],
        component: PageLayoutComponent,
        data: {
          cxRoute: "category"
        }
      }])]
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(ProductListingPageModule, [{
    type: NgModule,
    args: [{
      imports: [RouterModule.forChild([{
        // @ts-ignore
        path: null,
        canActivate: [CmsPageGuard],
        component: PageLayoutComponent,
        data: {
          pageLabel: "search",
          cxRoute: "search"
        }
      }, {
        // @ts-ignore
        path: null,
        canActivate: [CmsPageGuard],
        component: PageLayoutComponent,
        data: {
          cxRoute: "brand"
        }
      }, {
        // The 'category' route  may include a greedy suffix url matcher '**/c/:categoryCode'
        // So not to shadow the specific 'brand' route, the 'category' is the last route in the sequence.
        // @ts-ignore
        path: null,
        canActivate: [CmsPageGuard],
        component: PageLayoutComponent,
        data: {
          cxRoute: "category"
        }
      }])],
      providers: [provideDefaultConfig({
        routing: {
          routes: {
            category: {
              matchers: [PRODUCT_LISTING_URL_MATCHER]
            }
          }
        }
      })]
    }]
  }], null, null);
})();
var RoutingContextService = class _RoutingContextService {
  constructor(activatedRoutesService, injector) {
    this.activatedRoutesService = activatedRoutesService;
    this.injector = injector;
    this.contextTokenMapping$ = this.activatedRoutesService.routes$.pipe(map((routes) => this.getRoutesContextTokenMapping(routes)), shareReplay({
      refCount: true,
      bufferSize: 1
    }));
  }
  /**
   * Returns the merged context token mapping, consisting of mappings
   * defined in all Activated Angular Routes.
   */
  getRoutesContextTokenMapping(routes) {
    return Object.assign({}, ...routes.map((route) => route?.data?.cxContext));
  }
  /**
   * Resolves the specified `contextToken` from `cxContext` data parameter of the activated Angular Routes.
   * @param contextToken
   *
   * @returns instance from the root injector if defined, otherwise `undefined`.
   */
  get(contextToken) {
    return this.contextTokenMapping$.pipe(switchMap((contextMapping) => {
      const providerToken = contextMapping?.[contextToken];
      return !!providerToken ? this.injector.get(providerToken) : of(void 0);
    }));
  }
  static {
    this.ɵfac = function RoutingContextService_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _RoutingContextService)(ɵɵinject(ActivatedRoutesService), ɵɵinject(UnifiedInjector));
    };
  }
  static {
    this.ɵprov = ɵɵdefineInjectable({
      token: _RoutingContextService,
      factory: _RoutingContextService.ɵfac,
      providedIn: "root"
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(RoutingContextService, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], () => [{
    type: ActivatedRoutesService
  }, {
    type: UnifiedInjector
  }], null);
})();
var ContextService = class _ContextService {
  constructor(routingContextService) {
    this.routingContextService = routingContextService;
  }
  /**
   * Returns the context for the given token.
   */
  get(contextToken) {
    return this.resolveContext(contextToken).pipe(distinctUntilChanged(), shareReplay({
      refCount: true,
      bufferSize: 1
    }));
  }
  /**
   * Resolves the context for the given token.
   */
  resolveContext(contextToken) {
    return this.routingContextService.get(contextToken);
  }
  static {
    this.ɵfac = function ContextService_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _ContextService)(ɵɵinject(RoutingContextService));
    };
  }
  static {
    this.ɵprov = ɵɵdefineInjectable({
      token: _ContextService,
      factory: _ContextService.ɵfac,
      providedIn: "root"
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(ContextService, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], () => [{
    type: RoutingContextService
  }], null);
})();
var NavigationEvent = class extends CxEvent {
  static {
    this.type = "NavigationEvent";
  }
};
var PageEvent = class extends CxEvent {
};
var HomePageEvent = class extends PageEvent {
  static {
    this.type = "HomePageEvent";
  }
};
var HomePageEventBuilder = class _HomePageEventBuilder {
  constructor(eventService) {
    this.eventService = eventService;
    this.register();
  }
  register() {
    this.eventService.register(HomePageEvent, this.buildHomePageEvent());
  }
  buildHomePageEvent() {
    return this.eventService.get(NavigationEvent).pipe(filter((navigationEvent) => navigationEvent.semanticRoute === "home"), map((navigationEvent) => createFrom(HomePageEvent, {
      navigation: navigationEvent
    })));
  }
  static {
    this.ɵfac = function HomePageEventBuilder_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _HomePageEventBuilder)(ɵɵinject(EventService));
    };
  }
  static {
    this.ɵprov = ɵɵdefineInjectable({
      token: _HomePageEventBuilder,
      factory: _HomePageEventBuilder.ɵfac,
      providedIn: "root"
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(HomePageEventBuilder, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], () => [{
    type: EventService
  }], null);
})();
var HomePageEventModule = class _HomePageEventModule {
  constructor(_homePageEventBuilder) {
  }
  static {
    this.ɵfac = function HomePageEventModule_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _HomePageEventModule)(ɵɵinject(HomePageEventBuilder));
    };
  }
  static {
    this.ɵmod = ɵɵdefineNgModule({
      type: _HomePageEventModule
    });
  }
  static {
    this.ɵinj = ɵɵdefineInjector({});
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(HomePageEventModule, [{
    type: NgModule,
    args: [{}]
  }], () => [{
    type: HomePageEventBuilder
  }], null);
})();
var NavigationEventBuilder = class _NavigationEventBuilder {
  constructor(actions, eventService) {
    this.actions = actions;
    this.eventService = eventService;
    this.register();
  }
  /**
   * Registers events
   */
  register() {
    this.eventService.register(NavigationEvent, this.buildNavigationEvent());
  }
  /**
   * Builds the navigation events
   */
  buildNavigationEvent() {
    return this.getNavigatedEvent().pipe(map((state) => createFrom(NavigationEvent, {
      context: state.context,
      semanticRoute: state.semanticRoute,
      url: state.url,
      params: state.params
    })));
  }
  getNavigatedEvent() {
    return this.actions.pipe(ofType(ROUTER_NAVIGATED), map((event) => event.payload.routerState));
  }
  static {
    this.ɵfac = function NavigationEventBuilder_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _NavigationEventBuilder)(ɵɵinject(ActionsSubject), ɵɵinject(EventService));
    };
  }
  static {
    this.ɵprov = ɵɵdefineInjectable({
      token: _NavigationEventBuilder,
      factory: _NavigationEventBuilder.ɵfac,
      providedIn: "root"
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(NavigationEventBuilder, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], () => [{
    type: ActionsSubject
  }, {
    type: EventService
  }], null);
})();
var NavigationEventModule = class _NavigationEventModule {
  constructor(_navigationEventBuilder) {
  }
  static {
    this.ɵfac = function NavigationEventModule_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _NavigationEventModule)(ɵɵinject(NavigationEventBuilder));
    };
  }
  static {
    this.ɵmod = ɵɵdefineNgModule({
      type: _NavigationEventModule
    });
  }
  static {
    this.ɵinj = ɵɵdefineInjector({});
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(NavigationEventModule, [{
    type: NgModule,
    args: [{}]
  }], () => [{
    type: NavigationEventBuilder
  }], null);
})();
var ProductDetailsPageEvent = class extends PageEvent {
  static {
    this.type = "ProductDetailsPageEvent";
  }
};
var CategoryPageResultsEvent = class extends PageEvent {
  static {
    this.type = "CategoryPageResultsEvent";
  }
};
var SearchPageResultsEvent = class extends PageEvent {
  static {
    this.type = "SearchPageResultsEvent";
  }
};
var ProductPageEventBuilder = class _ProductPageEventBuilder {
  constructor(eventService, productService, productSearchService) {
    this.eventService = eventService;
    this.productService = productService;
    this.productSearchService = productSearchService;
    this.register();
  }
  register() {
    this.eventService.register(SearchPageResultsEvent, this.buildSearchPageResultsEvent());
    this.eventService.register(ProductDetailsPageEvent, this.buildProductDetailsPageEvent());
    this.eventService.register(CategoryPageResultsEvent, this.buildCategoryResultsPageEvent());
  }
  buildProductDetailsPageEvent() {
    return this.eventService.get(NavigationEvent).pipe(filter((navigationEvent) => navigationEvent.semanticRoute === "product"), switchMap((navigationEvent) => this.productService.get(navigationEvent.context.id).pipe(filter((product) => Boolean(product)), take(1), map((product) => createFrom(ProductDetailsPageEvent, {
      navigation: navigationEvent,
      categories: product?.categories,
      code: product?.code,
      name: product?.name,
      price: product?.price
    })))));
  }
  buildCategoryResultsPageEvent() {
    const searchResults$ = this.productSearchService.getResults().pipe(
      // skipping the initial value, and preventing emission of the previous search state
      skip(1)
    );
    return this.eventService.get(NavigationEvent).pipe(switchMap((navigationEvent) => {
      if (navigationEvent?.semanticRoute !== "category") {
        return EMPTY;
      }
      return searchResults$.pipe(map((searchResults) => createFrom(CategoryPageResultsEvent, __spreadValues({
        navigation: navigationEvent
      }, {
        categoryCode: navigationEvent?.context?.id,
        numberOfResults: searchResults?.pagination?.totalResults ?? 0,
        categoryName: searchResults.breadcrumbs?.[0].facetValueName
      }))));
    }));
  }
  buildSearchPageResultsEvent() {
    const searchResults$ = this.productSearchService.getResults().pipe(
      // skipping the initial value, and preventing emission of the previous search state
      skip(1)
    );
    return this.eventService.get(NavigationEvent).pipe(switchMap((navigationEvent) => {
      if (navigationEvent?.semanticRoute !== "search") {
        return EMPTY;
      }
      return searchResults$.pipe(map((searchResults) => createFrom(SearchPageResultsEvent, __spreadValues({
        navigation: navigationEvent
      }, {
        searchTerm: searchResults?.freeTextSearch ?? "",
        numberOfResults: searchResults?.pagination?.totalResults ?? 0
      }))));
    }));
  }
  static {
    this.ɵfac = function ProductPageEventBuilder_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _ProductPageEventBuilder)(ɵɵinject(EventService), ɵɵinject(ProductService), ɵɵinject(ProductSearchService));
    };
  }
  static {
    this.ɵprov = ɵɵdefineInjectable({
      token: _ProductPageEventBuilder,
      factory: _ProductPageEventBuilder.ɵfac,
      providedIn: "root"
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(ProductPageEventBuilder, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], () => [{
    type: EventService
  }, {
    type: ProductService
  }, {
    type: ProductSearchService
  }], null);
})();
var ProductPageEventModule = class _ProductPageEventModule {
  constructor(_productPageEventBuilder) {
  }
  static {
    this.ɵfac = function ProductPageEventModule_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _ProductPageEventModule)(ɵɵinject(ProductPageEventBuilder));
    };
  }
  static {
    this.ɵmod = ɵɵdefineNgModule({
      type: _ProductPageEventModule
    });
  }
  static {
    this.ɵinj = ɵɵdefineInjector({});
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(ProductPageEventModule, [{
    type: NgModule,
    args: [{}]
  }], () => [{
    type: ProductPageEventBuilder
  }], null);
})();
var mediaConfig = {
  mediaFormats: {
    mobile: {
      width: 400
    },
    tablet: {
      width: 770
    },
    desktop: {
      width: 1140
    },
    widescreen: {
      width: 1400
    },
    // product media
    cartIcon: {
      width: 65
    },
    thumbnail: {
      width: 96
    },
    product: {
      width: 284
    },
    zoom: {
      width: 515
    }
  },
  media: {
    pictureElementFormats: {
      mobile: {
        mediaQueries: "(max-width: 480px)"
      },
      tablet: {
        mediaQueries: "(max-width: 770px)"
      },
      desktop: {
        mediaQueries: "(max-width: 960px)"
      },
      widescreen: {
        mediaQueries: "(min-width: 961px)"
      }
    },
    pictureFormatsOrder: ["mobile", "tablet", "desktop", "widescreen"]
  }
};
var layoutConfig = {
  // deferredLoading: {
  //   strategy: DeferLoadingStrategy.DEFER,
  //   intersectionMargin: '50px',
  // },
  layoutSlots: {
    header: {
      lg: {
        slots: ["PreHeader", "SiteContext", "SiteLinks", "SiteLogo", "SearchBox", "SiteLogin", "MiniCart", "NavigationBar"]
      },
      slots: ["PreHeader", "SiteLogo", "SearchBox", "MiniCart"]
    },
    navigation: {
      lg: {
        slots: []
      },
      slots: ["SiteLogin", "NavigationBar", "SiteContext", "SiteLinks"]
    },
    footer: {
      slots: ["Footer"]
    },
    LandingPage2Template: {
      pageFold: "Section2B",
      slots: ["Section1", "Section2A", "Section2B", "Section2C", "Section3", "Section4", "Section5"]
    },
    ContentPage1Template: {
      slots: ["Section2A", "Section2B"]
    },
    CategoryPageTemplate: {
      pageFold: "Section2",
      slots: ["Section1", "Section2", "Section3"]
    },
    ProductListPageTemplate: {
      slots: ["ProductLeftRefinements", "ProductListSlot"]
    },
    ProductGridPageTemplate: {
      slots: ["ProductLeftRefinements", "ProductGridSlot"]
    },
    SearchResultsListPageTemplate: {
      slots: ["Section2", "ProductLeftRefinements", "SearchResultsListSlot", "Section4"]
    },
    SearchResultsGridPageTemplate: {
      slots: ["Section2", "ProductLeftRefinements", "SearchResultsGridSlot", "Section4"]
    },
    ProductDetailsPageTemplate: {
      lg: {
        pageFold: "UpSelling"
      },
      pageFold: "Summary",
      slots: ["Summary", "UpSelling", "CrossSelling", "Tabs", "PlaceholderContentSlot"]
    },
    CartPageTemplate: {
      slots: ["TopContent", "CenterRightContentSlot", "EmptyCartMiddleContent"]
    },
    AccountPageTemplate: {
      slots: ["BodyContent", "SideContent"]
    },
    LoginPageTemplate: {
      slots: ["LeftContentSlot", "RightContentSlot"]
    },
    ErrorPageTemplate: {
      slots: ["TopContent", "MiddleContent", "BottomContent"]
    },
    OrderConfirmationPageTemplate: {
      slots: ["BodyContent", "SideContent"]
    },
    MultiStepCheckoutSummaryPageTemplate: {
      slots: ["TopContent", "BodyContent", "SideContent", "BottomContent"]
    },
    CheckoutLoginPageTemplate: {
      slots: ["RightContentSlot"]
    },
    MyAccountViewPageTemplate: {
      slots: ["LeftContentSlot", "RightContentSlot"]
    }
  }
};
function layoutConfigFactory() {
  const config = JSON.parse(JSON.stringify(layoutConfig));
  const featureToggles = inject(FeatureToggles);
  if (featureToggles.unifiedDefaultHeaderSlotsAcrossBreakpoints && config.layoutSlots && config.layoutSlots.header && "slots" in config.layoutSlots.header) {
    if ("lg" in config.layoutSlots.header) {
      delete config.layoutSlots.header.lg;
    }
    config.layoutSlots.header.slots = ["PreHeader", "SiteContext", "SiteLinks", "SiteLogo", "SearchBox", "SiteLogin", "MiniCart", "NavigationBar"];
  }
  if (featureToggles.defaultLayoutConfigWithoutPageFold) {
    const homepageConfig = config?.layoutSlots?.LandingPage2Template ?? {};
    delete homepageConfig.pageFold;
    const categoryPageConfig = config?.layoutSlots?.CategoryPageTemplate ?? {};
    delete categoryPageConfig.pageFold;
    const productDetailsPageConfig = config?.layoutSlots?.ProductDetailsPageTemplate ?? {};
    delete productDetailsPageConfig.pageFold;
    delete (productDetailsPageConfig.lg ?? {}).pageFold;
  }
  return config;
}
var defaultCmsContentProviders = [provideCmsStructure({
  componentId: "HamburgerMenuComponent",
  pageSlotPosition: "PreHeader"
}), provideCmsStructure({
  componentId: "LoginComponent",
  pageSlotPosition: "SiteLogin"
})];
var defaultOnNavigateConfig = {
  enableResetViewOnNavigate: {
    active: true,
    ignoreQueryString: false,
    ignoreRoutes: [],
    selectedHostElement: "body"
  }
};
var OnNavigateConfig = class _OnNavigateConfig {
  static {
    this.ɵfac = function OnNavigateConfig_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _OnNavigateConfig)();
    };
  }
  static {
    this.ɵprov = ɵɵdefineInjectable({
      token: _OnNavigateConfig,
      factory: function OnNavigateConfig_Factory(__ngFactoryType__) {
        let __ngConditionalFactory__ = null;
        if (__ngFactoryType__) {
          __ngConditionalFactory__ = new (__ngFactoryType__ || _OnNavigateConfig)();
        } else {
          __ngConditionalFactory__ = ɵɵinject(Config);
        }
        return __ngConditionalFactory__;
      },
      providedIn: "root"
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(OnNavigateConfig, [{
    type: Injectable,
    args: [{
      providedIn: "root",
      useExisting: Config
    }]
  }], null, null);
})();
var OnNavigateService = class _OnNavigateService {
  get hostComponent() {
    return this.injector.get(ApplicationRef)?.components?.[0];
  }
  get selectedHostElement() {
    const toSelect = this.config?.enableResetViewOnNavigate?.selectedHostElement;
    return toSelect ? this.injector.get(DOCUMENT)?.getElementsByTagName?.(toSelect)?.[0] : void 0;
  }
  constructor(config, router, viewportScroller, injector) {
    this.config = config;
    this.router = router;
    this.viewportScroller = viewportScroller;
    this.injector = injector;
    this.routerConfiguration = inject(ROUTER_CONFIGURATION, {
      optional: true
    }) || {};
  }
  /**
   * Reads configuration and enables features based on flags set.
   */
  initializeWithConfig() {
    if (this.config?.enableResetViewOnNavigate?.active) {
      this.setResetViewOnNavigate(this.config.enableResetViewOnNavigate.active);
    }
  }
  /**
   * Resets view back to the original position when performing a back navigation and to the top when performing a front navigation
   * and sets the focus back to the top of the page before skiplinks for any type of navigation
   * @param enable Enable or disable this feature
   */
  setResetViewOnNavigate(enable) {
    this.subscription?.unsubscribe();
    if (enable) {
      this.viewportScroller.setHistoryScrollRestoration("manual");
      this.subscription = this.router.events.pipe(filter((event) => event instanceof Scroll), pairwise()).subscribe((event) => {
        const previousRoute = event[0];
        const currentRoute = event[1];
        const position = currentRoute.position;
        if (position) {
          this.scrollToPosition(currentRoute, position);
        } else {
          if (this.config.enableResetViewOnNavigate?.ignoreQueryString && this.isPathEqual(previousRoute, currentRoute)) {
            return;
          }
          if (this.isChildRoute(currentRoute)) {
            return;
          }
          this.scrollToPosition(currentRoute, position);
        }
        this.focusOnHostElement();
      });
    }
  }
  /**
   * Focus on selectedHostElement if set in config.
   * Otherwise, focuses on hostComponent.
   */
  focusOnHostElement() {
    if (this.selectedHostElement) {
      this.selectedHostElement?.focus();
    } else {
      this.hostComponent?.location?.nativeElement.focus();
    }
  }
  /**
   * Scrolls to a specified position or anchor based on the current route and configuration.
   * @param currentRoute The current route containing scroll information.
   * @param position The target scroll position as [x, y] coordinates, or null.
   */
  scrollToPosition(currentRoute, position) {
    const scrollTo = (anchor, scrollPosition) => {
      if (anchor && this.routerConfiguration.anchorScrolling === "enabled") {
        this.viewportScroller.scrollToAnchor(anchor);
      } else {
        this.viewportScroller.scrollToPosition(scrollPosition);
      }
    };
    const defaultPosition = [0, 0];
    setTimeout(() => {
      const anchor = currentRoute.anchor;
      const positionToScroll = position || defaultPosition;
      scrollTo(anchor, positionToScroll);
    }, position ? 0 : 100);
  }
  /**
   * Verifies if the current route is a child route from the given ignore config route
   *
   * @param route
   * @returns boolean whether the route is a child route
   */
  isChildRoute(route) {
    return this.config.enableResetViewOnNavigate?.ignoreRoutes?.some((configRoute) => this.isNavigationEnd(route.routerEvent) && route.routerEvent.urlAfterRedirects.split("/").includes(configRoute)) ?? false;
  }
  /**
   * Verifies if the previous and current route are the same without the query string
   *
   * @param previousRoute
   * @param currentRoute
   * @returns boolean depending on the previous and current route are equal without the query strings
   */
  isPathEqual(previousRoute, currentRoute) {
    if (!this.isNavigationEnd(previousRoute.routerEvent) || !this.isNavigationEnd(currentRoute.routerEvent)) {
      return false;
    }
    return previousRoute.routerEvent.urlAfterRedirects.split("?")[0] === currentRoute.routerEvent.urlAfterRedirects.split("?")[0];
  }
  /**
   * Verifies if router event is a navigation end event
   * @private
   */
  isNavigationEnd(event) {
    return event.type === EventType.NavigationEnd;
  }
  static {
    this.ɵfac = function OnNavigateService_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _OnNavigateService)(ɵɵinject(OnNavigateConfig), ɵɵinject(Router), ɵɵinject(ViewportScroller), ɵɵinject(Injector));
    };
  }
  static {
    this.ɵprov = ɵɵdefineInjectable({
      token: _OnNavigateService,
      factory: _OnNavigateService.ɵfac,
      providedIn: "root"
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(OnNavigateService, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], () => [{
    type: OnNavigateConfig
  }, {
    type: Router
  }, {
    type: ViewportScroller
  }, {
    type: Injector
  }], null);
})();
var AppRoutingModule = class _AppRoutingModule {
  static {
    this.ɵfac = function AppRoutingModule_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _AppRoutingModule)();
    };
  }
  static {
    this.ɵmod = ɵɵdefineNgModule({
      type: _AppRoutingModule,
      imports: [RouterModule]
    });
  }
  static {
    this.ɵinj = ɵɵdefineInjector({
      providers: [provideDefaultConfig(defaultOnNavigateConfig), {
        provide: APP_INITIALIZER,
        useFactory: onNavigateFactory,
        deps: [OnNavigateService],
        multi: true
      }],
      imports: [RouterModule.forRoot([], {
        anchorScrolling: "enabled",
        initialNavigation: "enabledBlocking"
      })]
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(AppRoutingModule, [{
    type: NgModule,
    args: [{
      imports: [RouterModule.forRoot([], {
        anchorScrolling: "enabled",
        initialNavigation: "enabledBlocking"
      })],
      providers: [provideDefaultConfig(defaultOnNavigateConfig), {
        provide: APP_INITIALIZER,
        useFactory: onNavigateFactory,
        deps: [OnNavigateService],
        multi: true
      }]
    }]
  }], null, null);
})();
function onNavigateFactory(onNavigateService) {
  const isReady = () => onNavigateService.initializeWithConfig();
  return isReady;
}
var FormUtils;
(function(FormUtils2) {
  function deepUpdateValueAndValidity(control, options = {}) {
    if (control instanceof UntypedFormGroup || control instanceof UntypedFormArray) {
      Object.values(control.controls).forEach((childControl) => {
        deepUpdateValueAndValidity(childControl, options);
      });
    }
    control.updateValueAndValidity({
      onlySelf: true,
      // avoid calling `#updateValueAndValidity` for all ancestors
      emitEvent: options.emitEvent
    });
  }
  FormUtils2.deepUpdateValueAndValidity = deepUpdateValueAndValidity;
})(FormUtils || (FormUtils = {}));
function getAddressNumbers(address, textPhone, textMobile) {
  if (address.cellphone && address.phone) {
    if (address.cellphone === address.phone) {
      return textMobile + ": " + address.cellphone;
    }
    return `${textPhone}: ${address.phone}
${textMobile}: ${address.cellphone}`;
  }
  if (address.cellphone) {
    return textMobile + ": " + address.cellphone;
  }
  if (address.phone) {
    return textPhone + ": " + address.phone;
  }
  return void 0;
}
var BaseStorefrontModule = class _BaseStorefrontModule {
  static {
    this.ɵfac = function BaseStorefrontModule_Factory(__ngFactoryType__) {
      return new (__ngFactoryType__ || _BaseStorefrontModule)();
    };
  }
  static {
    this.ɵmod = ɵɵdefineNgModule({
      type: _BaseStorefrontModule,
      imports: [BaseCoreModule, RouterModule, GlobalMessageComponentModule, OutletModule, OutletRefModule, PwaModule, PageLayoutModule, SeoModule, PageComponentModule, PageSlotModule, SkipLinkModule, KeyboardFocusModule, LayoutModule, RoutingModule2, MediaModule, OutletModule, CmsLcpModule, StorefrontComponentModule],
      exports: [LayoutModule, StorefrontComponentModule]
    });
  }
  static {
    this.ɵinj = ɵɵdefineInjector({
      imports: [BaseCoreModule.forRoot(), RouterModule, GlobalMessageComponentModule, OutletModule, OutletRefModule, PwaModule, PageLayoutModule, SeoModule, PageComponentModule.forRoot(), PageSlotModule, SkipLinkModule, KeyboardFocusModule, LayoutModule, RoutingModule2.forRoot(), MediaModule.forRoot(), OutletModule.forRoot(), CmsLcpModule.forRoot(), StorefrontComponentModule, LayoutModule, StorefrontComponentModule]
    });
  }
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(BaseStorefrontModule, [{
    type: NgModule,
    args: [{
      imports: [BaseCoreModule.forRoot(), RouterModule, GlobalMessageComponentModule, OutletModule, OutletRefModule, PwaModule, PageLayoutModule, SeoModule, PageComponentModule.forRoot(), PageSlotModule, SkipLinkModule, KeyboardFocusModule, LayoutModule, RoutingModule2.forRoot(), MediaModule.forRoot(), OutletModule.forRoot(), CmsLcpModule.forRoot(), StorefrontComponentModule],
      exports: [LayoutModule, StorefrontComponentModule]
    }]
  }], null, null);
})();

export {
  CheckboxControlValueAccessor,
  DefaultValueAccessor,
  Validators,
  NgControlStatus,
  NgControlStatusGroup,
  FormGroup,
  UntypedFormGroup,
  NgForm,
  FormControl,
  UntypedFormControl,
  NgModel,
  ɵNgNoValidate,
  NumberValueAccessor,
  RadioControlValueAccessor,
  FormControlDirective,
  FormGroupDirective,
  FormGroupName,
  FormArrayName,
  FormControlName,
  MinValidator,
  RequiredValidator,
  CheckboxRequiredValidator,
  MaxLengthValidator,
  UntypedFormArray,
  FormBuilder,
  UntypedFormBuilder,
  FormsModule,
  ReactiveFormsModule,
  NgOptionComponent,
  NgSelectComponent,
  NgSelectModule,
  TrapFocus,
  SelectFocusUtility,
  KeyboardFocusService,
  FocusDirective,
  SkipFocusDirective,
  KeyboardFocusModule,
  MockKeyboardFocusDirective,
  KeyboardFocusTestingModule,
  disableTabbingForTick,
  DIALOG_TYPE,
  LAUNCH_CALLER,
  LaunchRenderStrategy,
  BREAKPOINT,
  LayoutConfig,
  LaunchDialogService,
  AnonymousConsentManagementBannerComponent,
  InlineRenderStrategy,
  InlineRootRenderStrategy,
  OutletPosition,
  AVOID_STACKED_OUTLETS,
  USE_STACKED_OUTLETS,
  OutletContextData,
  OutletService,
  OutletRefDirective,
  OutletRefModule,
  OutletRendererService,
  IntersectionService,
  DeferLoaderService,
  OutletDirective,
  provideOutlet,
  OutletModule,
  OutletRenderStrategy,
  RoutingRenderStrategy,
  LaunchDialogModule,
  ICON_TYPE,
  IconConfig,
  IconResourceType,
  fontawesomeIconConfig,
  IconLoaderService,
  DirectionMode,
  IconComponent,
  IconModule,
  MockIconComponent,
  MockIconLoaderService,
  IconTestingModule,
  SpinnerComponent,
  ConsentManagementFormComponent,
  AtMessageDirective,
  MessageComponent,
  AnonymousConsentDialogComponent,
  AnonymousConsentOpenDialogComponent,
  AnonymousConsentManagementBannerModule,
  CmsLcpModule,
  LcpPresence,
  LcpCmsComponentsConfig,
  CmsLcpService,
  LCP_PRESENCE,
  CmsComponentData,
  provideLcpPresenceForCmsComponent,
  BEFORE_CMS_PAGE_GUARD,
  BeforeCmsPageGuardService,
  PAGE_LAYOUT_HANDLER,
  BreakpointService,
  PageLayoutService,
  CmsFeaturesService,
  CmsComponentsService,
  PageSlotService,
  ComponentEvent,
  ComponentCreateEvent,
  ComponentDestroyEvent,
  ComponentHandler,
  ComponentHandlerService,
  CmsInjectorService,
  ComponentWrapperDirective,
  PageSlotComponent,
  PageTemplateDirective,
  PageLayoutComponent,
  CmsGuardsService,
  CmsRoutesService,
  CmsI18nService,
  CmsPageGuardService,
  CmsPageGuard,
  DefaultComponentHandler,
  LazyComponentHandler,
  InnerComponentsHostDirective,
  PageComponentModule,
  PageSlotModule,
  PageLayoutModule,
  PWAModuleConfig,
  defaultPWAModuleConfig,
  AddToHomeScreenService,
  AddToHomeScreenComponent,
  AddToHomeScreenBannerComponent,
  AddToHomeScreenBtnComponent,
  PwaModule,
  CmsRouteModule,
  RoutingModule2 as RoutingModule,
  getSuffixUrlMatcher,
  PageMetaLinkService,
  SeoMetaService,
  ImageLoadingStrategy,
  ImageFetchPriority,
  MediaService,
  MediaPreconnectService,
  defaultSeoConfig,
  SeoConfig,
  JsonLdScriptFactory,
  JsonLdDirective,
  SCHEMA_BUILDER,
  JSONLD_PRODUCT_BUILDER,
  StructuredDataModule,
  initSeoService,
  SeoModule,
  BreadcrumbSchemaBuilder,
  JsonLdBaseProductBuilder,
  JsonLdProductOfferBuilder,
  JsonLdProductReviewBuilder,
  CurrentProductService,
  ProductSchemaBuilder,
  JsonLdBuilderModule,
  PageSection,
  provideCmsStructure,
  SkipLinkConfig,
  SkipLink,
  SkipLinkScrollPosition,
  SkipLinkService,
  SkipLinkComponent,
  defaultSkipLinkConfig,
  SkipLinkDirective,
  SkipLinkModule,
  BtnLikeLinkDirective,
  BtnLikeLinkModule,
  DomChangeDirective,
  DomChangeModule,
  CarouselService,
  CarouselComponent,
  BannerCarouselComponent,
  SpinnerModule,
  ConsentManagementComponentService,
  ConsentManagementComponent,
  USE_MY_ACCOUNT_V2_NOTIFICATION_PREFERENCE,
  USE_MY_ACCOUNT_V2_CONSENT,
  MyAccountV2ConsentManagementFormComponent,
  MyAccountV2ConsentManagementComponent,
  MyAccountV2ConsentManagementModule,
  ConsentManagementModule,
  AtMessageModule,
  MessageComponentModule,
  AnonymousConsentsDialogModule,
  PopoverEvent,
  PositioningService,
  PopoverComponent,
  PopoverService,
  PopoverDirective,
  TruncatePipe,
  TruncateTextPopoverComponent,
  GenericLinkComponentService,
  GenericLinkComponent,
  CardComponent,
  PopoverModule,
  TruncateTextPopoverModule,
  GenericLinkModule,
  CardModule,
  MediaSourcesPipe,
  USE_LEGACY_MEDIA_COMPONENT,
  MediaComponent,
  mediaPreconnectInitializer,
  MediaModule,
  FocusableCarouselItemDirective,
  CarouselModule,
  HorizontalScrollingPositionDirective,
  CarouselScrollingComponent,
  HorizontalScrollingPositionDirectiveModule,
  CarouselScrollingModule,
  DatePickerService,
  FormErrorsComponent,
  DatePickerComponent,
  FormErrorsModule,
  DatePickerModule,
  FileUploadComponent,
  FileUploadModule,
  PasswordVisibilityToggleComponent,
  FormConfig,
  PasswordVisibilityToggleDirective,
  PasswordVisibilityToggleModule,
  FormRequiredAsterisksComponent,
  FormRequiredLegendComponent,
  ItemCounterComponent,
  ItemCounterModule,
  defaultLayoutConfig,
  DirectionConfig,
  DirectionService,
  DirectionModule,
  HamburgerMenuService,
  HamburgerMenuComponent,
  HamburgerMenuModule,
  ThemeService,
  ThemeModule,
  LayoutModule,
  StorefrontOutlets,
  GlobalMessageComponent,
  StorefrontComponent,
  GlobalMessageComponentModule,
  StorefrontComponentModule,
  NgSelectA11yDirective,
  SortingComponent,
  NgSelectA11yModule,
  SortingModule,
  defaultPaginationConfig,
  PaginationConfig,
  PaginationItemType,
  PaginationNavigationPosition,
  PaginationBuilder,
  PaginationComponent,
  PaginationModule,
  TotalComponent,
  TotalModule,
  ListNavigationModule,
  MediaConfig,
  ProgressButtonComponent,
  ProgressButtonModule,
  SplitViewService,
  SplitViewComponent,
  ViewComponent,
  SplitViewModule,
  PromotionsComponent,
  PromotionsModule,
  SiteContextType,
  SiteContextComponentService,
  SiteContextSelectorComponent,
  LanguageCurrencyComponent,
  SiteContextSelectorModule,
  SiteThemeSwitcherComponent,
  SiteThemeSwitcherModule,
  StarRatingComponent,
  StarRatingModule,
  TableDataCellComponent,
  TableHeaderCellComponent,
  defaultTableConfig,
  TableConfig,
  TableDataCellModule,
  TableHeaderCellModule,
  TableRendererService,
  TableLayout,
  TableComponent,
  TableModule,
  TableService,
  CaptchaApiConfig,
  CaptchaComponent,
  CaptchaService,
  CaptchaModule,
  FilesFormValidators,
  AvatarComponent,
  MessagingComponent,
  ChatMessagingModule,
  ReadMoreComponent,
  BannerCarouselModule,
  LcpPresenceMappingService,
  LcpContextDirective,
  BannerComponent,
  LcpContextDirectiveModule,
  BannerModule,
  LinkComponent,
  LinkModule,
  SupplementHashAnchorsPipe,
  ParagraphComponent,
  SupplementHashAnchorsModule,
  CmsParagraphModule,
  PDFComponent,
  PDFModule,
  TAB_MODE,
  TabPanelComponent,
  TabComponent,
  TabParagraphContainerComponent,
  TabModule,
  TabParagraphContainerModule,
  VideoComponent,
  VideoModule,
  MyCouponsComponentService,
  CouponCardComponent,
  MyCouponsComponent,
  CouponDialogComponent,
  CouponClaimComponent,
  ClaimDialogComponent,
  MyCouponsModule,
  MyInterestsComponent,
  MyInterestsModule,
  NotificationPreferenceComponent,
  PageTitleComponent,
  BreadcrumbComponent,
  BreadcrumbModule,
  NavigationService,
  NavigationUIComponent,
  CategoryNavigationComponent,
  NavigationComponent,
  NavigationModule,
  CategoryNavigationModule,
  FooterNavigationComponent,
  FooterNavigationModule,
  PageTitleModule,
  HighlightPipe,
  SearchBoxSuggestionSelectedEvent,
  SearchBoxProductSelectedEvent,
  SearchBoxComponentService,
  SearchBoxOutlets,
  SearchBoxComponent,
  ViewConfig,
  ProvideLcpPresenceDirective,
  ProvideLcpPresenceDirectiveModule,
  FileDownloadService,
  ExportCsvFileService,
  FileReaderService,
  ImportCsvFileService,
  CustomFormValidators,
  controlsMustMatch,
  titleScores,
  sortTitles,
  SearchBoxModule,
  ScrollToTopComponent,
  ScrollToTopModule,
  MyAccountV2NavigationComponent,
  MyAccountV2NavigationModule,
  MyAccountV2Module,
  MyAccountV2NotificationPreferenceComponent,
  MyAccountV2NotificationPreferenceModule,
  NotificationPreferenceModule,
  PaymentMethodsComponent,
  PaymentMethodsModule,
  ProductCarouselService,
  ProductListComponentService,
  ViewModes,
  ProductViewComponent,
  ProductDetailOutlets,
  ProductListOutlets,
  ProductListItemContext,
  ProductListItemContextSource,
  ProductListItemComponent,
  ProductGridItemComponent,
  ProductScrollComponent,
  ProductListComponent,
  FacetGroupCollapsedState,
  ProductFacetService,
  FacetService,
  ActiveFacetsComponent,
  ActiveFacetsModule,
  FacetComponent,
  FacetListComponent,
  FacetModule,
  FacetListModule,
  ProductFacetNavigationComponent,
  ProductFacetNavigationModule,
  defaultViewConfig,
  ProductListModule,
  ProductCarouselItemComponent,
  ProductCarouselComponent,
  ProductCarouselModule,
  ProductReferencesComponent,
  ProductReferencesModule,
  ProductImagesComponent,
  ProductImagesModule,
  ProductIntroComponent,
  ProductIntroModule,
  ProductSummaryComponent,
  ProductSummaryModule,
  ProductAttributesComponent,
  ProductAttributesModule,
  ProductDetailsTabComponent,
  ProductDetailsTabModule,
  ProductReviewsComponent,
  ProductReviewsModule,
  ProductTabsModule,
  StockNotificationDialogComponent,
  StockNotificationComponent,
  StockNotificationModule,
  LoginGuard,
  LoginRouteModule,
  LogoutGuard,
  LogoutModule,
  UserComponentModule,
  IS_GUEST_USER_CHECKOUT_KEY,
  PRODUCT_DETAILS_URL_MATCHER,
  ProductDetailsPageModule,
  PRODUCT_LISTING_URL_MATCHER,
  ProductListingPageModule,
  RoutingContextService,
  ContextService,
  NavigationEvent,
  PageEvent,
  HomePageEvent,
  HomePageEventBuilder,
  HomePageEventModule,
  NavigationEventBuilder,
  NavigationEventModule,
  ProductDetailsPageEvent,
  CategoryPageResultsEvent,
  SearchPageResultsEvent,
  ProductPageEventBuilder,
  ProductPageEventModule,
  mediaConfig,
  layoutConfig,
  layoutConfigFactory,
  defaultCmsContentProviders,
  defaultOnNavigateConfig,
  OnNavigateConfig,
  OnNavigateService,
  AppRoutingModule,
  onNavigateFactory,
  FormUtils,
  getAddressNumbers,
  BaseStorefrontModule
};
/*! Bundled license information:

@angular/service-worker/fesm2022/service-worker.mjs:
  (**
   * @license Angular v19.2.19
   * (c) 2010-2025 Google LLC. https://angular.io/
   * License: MIT
   *)
  (*!
   * @license
   * Copyright Google LLC All Rights Reserved.
   *
   * Use of this source code is governed by an MIT-style license that can be
   * found in the LICENSE file at https://angular.dev/license
   *)

@angular/forms/fesm2022/forms.mjs:
  (**
   * @license Angular v19.2.19
   * (c) 2010-2025 Google LLC. https://angular.io/
   * License: MIT
   *)
*/
//# sourceMappingURL=chunk-TKDNTTKU.js.map
